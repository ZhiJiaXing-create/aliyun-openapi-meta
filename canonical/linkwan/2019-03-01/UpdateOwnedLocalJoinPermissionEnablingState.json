{
  "camel_example": "",
  "cmd_full_name": "linkwan update-owned-local-join-permission-enabling-state",
  "cmd_name": "update-owned-local-join-permission-enabling-state",
  "deprecated": false,
  "description_en": "Enables or disables a dedicated join permission owned by the current user account",
  "description_zh": "启用、停用当前用户账号所拥有的专用入网凭证",
  "endpoint_map": {
    "cn-hangzhou": "linkwan.cn-hangzhou.aliyuncs.com",
    "cn-shanghai": "linkwan.cn-shanghai.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun linkwan update-owned-local-join-permission-enabling-state --iot-instance-id example-string --join-permission-id 123 --enabled true --api-version 2019-03-01 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "UpdateOwnedLocalJoinPermissionEnablingState",
  "operation": {
    "action": "UpdateOwnedLocalJoinPermissionEnablingState",
    "api_style": "RPC",
    "api_version": "2019-03-01",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "url": ""
  },
  "parameters": [
    {
      "help_en": "The IotInstanceId",
      "help_zh": "The IotInstanceId",
      "location": "query",
      "name": "iot_instance_id",
      "options": [
        "--iot-instance-id"
      ],
      "raw_name": "IotInstanceId",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The ID of the dedicated join permission to operate on.",
      "description_zh": "要操作的专用入网凭证ID。",
      "example": "123",
      "help_en": "The ID of the dedicated join permission to operate on",
      "help_zh": "要操作的专用入网凭证ID",
      "location": "query",
      "name": "join_permission_id",
      "options": [
        "--join-permission-id"
      ],
      "raw_name": "JoinPermissionId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "Specifies whether to enable the dedicated join permission. Valid values:\n- **true**: Enable the dedicated join permission.\n- **false**: Disable the dedicated join permission.",
      "description_zh": "**true**为启用，**false**为停用。",
      "example": "true",
      "help_en": "Specifies whether to enable the dedicated join permission. Valid values:\n- **true**: Enable the dedicated join permission.\n- **false**: Disable the dedicated join permission",
      "help_zh": "**true**为启用，**false**为停用",
      "location": "query",
      "name": "enabled",
      "options": [
        "--enabled"
      ],
      "raw_name": "Enabled",
      "required": true,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "LinkWAN",
  "protocol": "HTTPS",
  "vpc_endpoint_map": {
    "cn-shanghai": "linkwan-vpc.cn-shanghai.aliyuncs.com"
  }
}
