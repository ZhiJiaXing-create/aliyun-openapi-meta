{
  "camel_example": "aliyun ims ListUsers --region cn-hangzhou --Status active --Tag.1.Key operator --Tag.1.Value alice --Marker EXAMPLE --MaxItems 1000",
  "cmd_full_name": "ims list-users",
  "cmd_name": "list-users",
  "deprecated": false,
  "description_en": "Queries information about all Resource Access Management (RAM) users",
  "description_zh": "查询所有RAM用户的详细信息",
  "endpoint_map": {
    "cn-hangzhou": "ims.aliyuncs.com"
  },
  "global_endpoint": "ims.aliyuncs.com",
  "kebab_example": "aliyun ims list-users --marker EXAMPLE --max-items 1000 --tag Value=alice Key=operator --status active --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ListUsers",
  "operation": {
    "action": "ListUsers",
    "api_style": "RPC",
    "api_version": "2019-08-15",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The `marker`. If part of a previous response is truncated, you can use this parameter to obtain the truncated part.",
      "description_zh": "当请求的返回结果被截断时，可以使用`Marker`获取从当前截断位置之后的内容。 ",
      "example": "EXAMPLE",
      "help_en": "The `marker`. If part of a previous response is truncated, you can use this parameter to obtain the truncated part",
      "help_zh": "当请求的返回结果被截断时，可以使用`Marker`获取从当前截断位置之后的内容。 ",
      "location": "query",
      "name": "marker",
      "options": [
        "--marker"
      ],
      "raw_name": "Marker",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The number of entries per page. If a response is truncated because it reaches the value of `MaxItems`, the value of `IsTruncated` will be true.\n\nValid values: 1 to 1000. Default value: 1000.",
      "description_zh": "返回结果的条数。当返回结果达到`MaxItems`限制被截断时，返回参数`IsTruncated`将等于true。 \n\n取值范围：1~1000。默认值：1000。",
      "example": "1000",
      "help_en": "The number of entries per page. If a response is truncated because it reaches the value of `MaxItems`, the value of `IsTruncated` will be true.\n\nValid values: 1 to 1000. Default value: 1000",
      "help_zh": "返回结果的条数。当返回结果达到`MaxItems`限制被截断时，返回参数`IsTruncated`将等于true。 \n\n取值范围：1~1000。默认值：1000",
      "location": "query",
      "name": "max_items",
      "options": [
        "--max-items"
      ],
      "raw_name": "MaxItems",
      "required": false,
      "type": "int"
    },
    {
      "description_en": "The tags. A maximum number of 20 tags are supported.",
      "description_zh": "标签列表，最多包含20个子项。",
      "element_fields": [
        {
          "description_en": "The key of tag N.\n\nValid values of N: 1 to 20. N must be consecutive.",
          "description_zh": "标签键。\n\nN的取值范围：1~20，N必须连续。",
          "example": "operator",
          "help_en": "The key of tag N.\n\nValid values of N: 1 to 20. N must be consecutive",
          "help_zh": "标签键。\n\nN的取值范围：1~20，N必须连续",
          "name": "key",
          "options": [
            "--key"
          ],
          "raw_name": "Key",
          "required": false,
          "type": "string"
        },
        {
          "description_en": "The value of tag N.\n\nValid values of N: 1 to 20. N must be consecutive.",
          "description_zh": "标签值。\n\nN的取值范围：1~20，N必须连续。",
          "example": "alice",
          "help_en": "The value of tag N.\n\nValid values of N: 1 to 20. N must be consecutive",
          "help_zh": "标签值。\n\nN的取值范围：1~20，N必须连续",
          "name": "value",
          "options": [
            "--value"
          ],
          "raw_name": "Value",
          "required": false,
          "type": "string"
        }
      ],
      "element_type": "object",
      "help_en": "The tags. A maximum number of 20 tags are supported",
      "help_zh": "标签列表，最多包含20个子项",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "repeatList",
      "raw_name": "Tag",
      "required": false,
      "type": "array"
    },
    {
      "description_en": "The status of the RAM users that you want to query. Valid values: active, freeze, and active,freeze. If you leave the parameter empty, the value active is used by default. If you specify a value for the Tag parameter, users in both states are queried.",
      "description_zh": "指定查询的用户状态，支持active，freeze状态。指定全部状态时使用逗号拼接，如：active,freeze。未指定状态时默认指定active状态。\n当使用Tag标签时，默认查询全部状态的用户。",
      "example": "active",
      "help_en": "The status of the RAM users that you want to query. Valid values: active, freeze, and active,freeze. If you leave the parameter empty, the value active is used by default. If you specify a value for the Tag parameter, users in both states are queried",
      "help_zh": "指定查询的用户状态，支持active，freeze状态。指定全部状态时使用逗号拼接，如：active,freeze。未指定状态时默认指定active状态。\n当使用Tag标签时，默认查询全部状态的用户",
      "location": "query",
      "name": "status",
      "options": [
        "--status"
      ],
      "raw_name": "Status",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ims",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "cn-hangzhou": "ims.vpc-proxy.aliyuncs.com"
  }
}
