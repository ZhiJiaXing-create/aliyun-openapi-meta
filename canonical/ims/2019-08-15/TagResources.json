{
  "camel_example": "aliyun ims TagResources --region cn-hangzhou --ResourceId.1 207329002493920000 --Tag.1.Key operator --ResourcePrincipalName.1 'test@example.onaliyun.com' --Tag.1.Value alice --ResourceType user",
  "cmd_full_name": "ims tag-resources",
  "cmd_name": "tag-resources",
  "deprecated": false,
  "description_en": "Adds tags to resources",
  "description_zh": "调用TagResources接口给资源打上标签",
  "endpoint_map": {
    "cn-hangzhou": "ims.aliyuncs.com"
  },
  "global_endpoint": "ims.aliyuncs.com",
  "kebab_example": "aliyun ims tag-resources --resource-type user --resource-id 207329002493920000 --resource-principal-name test@example.onaliyun.com --tag Value=alice Key=operator --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "TagResources",
  "operation": {
    "action": "TagResources",
    "api_style": "RPC",
    "api_version": "2019-08-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The type of the resource. Valid value:\n\n- user: a Resource Access Management (RAM) user.",
      "description_zh": "资源类型。取值：\n\n- user：RAM用户。",
      "example": "user",
      "help_en": "The type of the resource. Valid value:\n\n- user: a Resource Access Management (RAM) user",
      "help_zh": "资源类型。取值：\n\n- user：RAM用户",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The ID of resource N.\n\nValid values of N: 1 to 50. If the ResourceType parameter is set to user, the resource ID is the ID of the RAM user.\n\n> You must specify only one of the following parameters: `ResourceId` and `ResourcePrincipalName`.",
      "description_zh": "资源ID。\n\nN的取值范围为：1~50。\n当资源类型为user时，资源ID为RAM用户ID。\n>必须指定 `ResourceId`、`ResourcePrincipalName`两个参数中的一个参数，但不能同时指定。",
      "element_type": "string",
      "help_en": "The ID of resource N.\n\nValid values of N: 1 to 50. If the ResourceType parameter is set to user, the resource ID is the ID of the RAM user.\n\n> You must specify only one of the following parameters: `ResourceId` and `ResourcePrincipalName`",
      "help_zh": "资源ID。\n\nN的取值范围为：1~50。\n当资源类型为user时，资源ID为RAM用户ID。\n>必须指定 `ResourceId`、`ResourcePrincipalName`两个参数中的一个参数，但不能同时指定",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": false,
      "type": "array"
    },
    {
      "description_en": "The name of resource N.\n\nValid values of N: 1 to 50. If the ResourceType parameter is set to user, the resource name is the name of the RAM user.\n\n> You must specify only one of the following parameters: `ResourceId` and `ResourcePrincipalName`.",
      "description_zh": "资源名称。\n\nN的取值范围为：1~50。\n当资源类型为user时，资源名称为RAM用户名。\n>必须指定 `ResourceId`、`ResourcePrincipalName`两个参数中的一个参数，但不能同时指定。",
      "element_type": "string",
      "example": "TagResources",
      "help_en": "The name of resource N.\n\nValid values of N: 1 to 50. If the ResourceType parameter is set to user, the resource name is the name of the RAM user.\n\n> You must specify only one of the following parameters: `ResourceId` and `ResourcePrincipalName`",
      "help_zh": "资源名称。\n\nN的取值范围为：1~50。\n当资源类型为user时，资源名称为RAM用户名。\n>必须指定 `ResourceId`、`ResourcePrincipalName`两个参数中的一个参数，但不能同时指定",
      "location": "query",
      "name": "resource_principal_name",
      "options": [
        "--resource-principal-name"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourcePrincipalName",
      "required": false,
      "type": "array"
    },
    {
      "description_en": "The key of tag N.\n\nValid values of N: 1 to 20. You cannot specify empty strings as tag keys. The tag key can be up to 128 characters in length. The tag key cannot start with aliyun or acs: and cannot contain `http://` or `https://`.",
      "description_zh": "资源的标签键。\n\nN的取值范围：1~20。一旦传入该值，则不允许为空字符串。最多支持128个字符，不能以aliyun和acs:开头，不能包含`http://`或者`https://`。",
      "element_fields": [
        {
          "description_en": "The key of tag N.\n\nValid values of N: 1 to 20. You cannot specify empty strings as tag keys. The tag key can be up to 128 characters in length. The tag key cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`.",
          "description_zh": "资源的标签键。\n\nN的取值范围：1~20。一旦传入该值，则不允许为空字符串。最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n",
          "example": "operator",
          "help_en": "The key of tag N.\n\nValid values of N: 1 to 20. You cannot specify empty strings as tag keys. The tag key can be up to 128 characters in length. The tag key cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`",
          "help_zh": "资源的标签键。\n\nN的取值范围：1~20。一旦传入该值，则不允许为空字符串。最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`",
          "name": "key",
          "options": [
            "--key"
          ],
          "raw_name": "Key",
          "required": false,
          "type": "string"
        },
        {
          "description_en": "The value of tag N.\n\nValid values of N: 1 to 20. The tag value can be an empty string. The tag value can be up to 128 characters in length and cannot contain `http://` or `https://`.",
          "description_zh": "资源的标签值。\n\nN的取值范围：1~20。可以为空字符串。最多支持128个字符，不能包含`http://`或者`https://`。",
          "example": "alice",
          "help_en": "The value of tag N.\n\nValid values of N: 1 to 20. The tag value can be an empty string. The tag value can be up to 128 characters in length and cannot contain `http://` or `https://`",
          "help_zh": "资源的标签值。\n\nN的取值范围：1~20。可以为空字符串。最多支持128个字符，不能包含`http://`或者`https://`",
          "name": "value",
          "options": [
            "--value"
          ],
          "raw_name": "Value",
          "required": false,
          "type": "string"
        }
      ],
      "element_type": "object",
      "help_en": "The key of tag N.\n\nValid values of N: 1 to 20. You cannot specify empty strings as tag keys. The tag key can be up to 128 characters in length. The tag key cannot start with aliyun or acs: and cannot contain `http://` or `https://`",
      "help_zh": "资源的标签键。\n\nN的取值范围：1~20。一旦传入该值，则不允许为空字符串。最多支持128个字符，不能以aliyun和acs:开头，不能包含`http://`或者`https://`",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "repeatList",
      "raw_name": "Tag",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "Ims",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "cn-hangzhou": "ims.vpc-proxy.aliyuncs.com"
  }
}
