{
  "camel_example": "aliyun ims UntagResources --region cn-hangzhou --All false --ResourceId.1 207329002493920000 --ResourcePrincipalName.1 'test@example.onaliyun.com' --TagKey.1 operator --ResourceType user",
  "cmd_full_name": "ims untag-resources",
  "cmd_name": "untag-resources",
  "deprecated": false,
  "description_en": "Removes tags from a resource",
  "description_zh": "调用UntagResources接口解绑资源的标签",
  "endpoint_map": {
    "cn-hangzhou": "ims.aliyuncs.com"
  },
  "global_endpoint": "ims.aliyuncs.com",
  "kebab_example": "aliyun ims untag-resources --resource-type user --all false --resource-principal-name test@example.onaliyun.com --resource-id 207329002493920000 --tag-key operator --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UntagResources",
  "operation": {
    "action": "UntagResources",
    "api_style": "RPC",
    "api_version": "2019-08-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The type of the resource. Valid value:\n\n- user: a RAM user",
      "description_zh": "资源类型。取值：\n\n- user：RAM用户。\n",
      "example": "user",
      "help_en": "The type of the resource. Valid value:\n\n- user: a RAM user",
      "help_zh": "资源类型。取值：\n\n- user：RAM用户",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "Specifies whether to remove all tags from the resource. Valid values:\n\n- true: remove all tags from the resources.\n\n- false (default): does not remove all tags from the resources.\n\n> This parameter takes effect only when TagKey.N is not set in the request.",
      "description_zh": "是否解绑资源上全部的标签。取值：\n- true：解绑全部标签。\n- false（默认值）：不解绑全部标签。\n\n> 当请求中未设置TagKey时，该参数才有效。",
      "example": "false",
      "help_en": "Specifies whether to remove all tags from the resource. Valid values:\n\n- true: remove all tags from the resources.\n\n- false (default): does not remove all tags from the resources.\n\n> This parameter takes effect only when TagKey.N is not set in the request",
      "help_zh": "是否解绑资源上全部的标签。取值：\n- true：解绑全部标签。\n- false（默认值）：不解绑全部标签。\n\n> 当请求中未设置TagKey时，该参数才有效",
      "location": "query",
      "name": "all",
      "options": [
        "--all"
      ],
      "raw_name": "All",
      "required": false,
      "type": "bool"
    },
    {
      "description_en": "The names of resources.\n\nValid values of N: 1 to 50. If the ResourceType parameter is set to user, the resource name is the name of the RAM user.\n\n> You must specify only one of the following parameters: ResourceId and ResourcePrincipalName.",
      "description_zh": "资源名称。\n\nN的取值范围为：1~50。 当资源类型为user时，资源名称为RAM用户名。\n> 必须指定 ResourceId、ResourcePrincipalName两个参数中的一个参数，但不能同时指定。",
      "element_type": "string",
      "help_en": "The names of resources.\n\nValid values of N: 1 to 50. If the ResourceType parameter is set to user, the resource name is the name of the RAM user.\n\n> You must specify only one of the following parameters: ResourceId and ResourcePrincipalName",
      "help_zh": "资源名称。\n\nN的取值范围为：1~50。 当资源类型为user时，资源名称为RAM用户名。\n> 必须指定 ResourceId、ResourcePrincipalName两个参数中的一个参数，但不能同时指定",
      "location": "query",
      "name": "resource_principal_name",
      "options": [
        "--resource-principal-name"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourcePrincipalName",
      "required": false,
      "type": "array"
    },
    {
      "description_en": "The IDs of resources.\n\nValid values of N: 1 to 50. If the ResourceType parameter is set to user, the resource ID is the ID of the RAM user.\n\n> You must specify only one of the following parameters: ResourceId and ResourcePrincipalName.",
      "description_zh": "资源ID。\n\nN的取值范围为：1~50。 当资源类型为user时，资源ID为RAM用户ID。\n\n> 必须指定 ResourceId、ResourcePrincipalName两个参数中的一个参数，但不能同时指定。",
      "element_type": "string",
      "example": "UntagResources",
      "help_en": "The IDs of resources.\n\nValid values of N: 1 to 50. If the ResourceType parameter is set to user, the resource ID is the ID of the RAM user.\n\n> You must specify only one of the following parameters: ResourceId and ResourcePrincipalName",
      "help_zh": "资源ID。\n\nN的取值范围为：1~50。 当资源类型为user时，资源ID为RAM用户ID。\n\n> 必须指定 ResourceId、ResourcePrincipalName两个参数中的一个参数，但不能同时指定",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": false,
      "type": "array"
    },
    {
      "description_en": "The tag keys of resources.\n\nValid values of N: 1 to 20. N must be consecutive.",
      "description_zh": "资源的标签键。\n\nN的取值范围：1~20，N必须连续。",
      "element_type": "string",
      "help_en": "The tag keys of resources.\n\nValid values of N: 1 to 20. N must be consecutive",
      "help_zh": "资源的标签键。\n\nN的取值范围：1~20，N必须连续",
      "location": "query",
      "name": "tag_key",
      "options": [
        "--tag-key"
      ],
      "param_style": "repeatList",
      "raw_name": "TagKey",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "Ims",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "cn-hangzhou": "ims.vpc-proxy.aliyuncs.com"
  }
}
