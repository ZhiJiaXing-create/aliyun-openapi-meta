{
  "camel_example": "aliyun ims CreateLoginProfile --region cn-hangzhou --Status Active --PasswordResetRequired false --UserPrincipalName 'test@example.onaliyun.com' --MFABindRequired false --Password mypassword",
  "cmd_full_name": "ims create-login-profile",
  "cmd_name": "create-login-profile",
  "deprecated": false,
  "description_en": "Creates a logon configuration for a Resource Access Management (RAM) user",
  "description_zh": "为RAM用户创建登录配置",
  "endpoint_map": {
    "cn-hangzhou": "ims.aliyuncs.com"
  },
  "global_endpoint": "ims.aliyuncs.com",
  "kebab_example": "aliyun ims create-login-profile --user-principal-name test@example.onaliyun.com --password mypassword --password-reset-required false --mfa-bind-required false --status Active --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateLoginProfile",
  "operation": {
    "action": "CreateLoginProfile",
    "api_style": "RPC",
    "api_version": "2019-08-15",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The logon name of the RAM user.",
      "description_zh": "RAM用户的登录名称。",
      "example": "test@example.onaliyun.com",
      "help_en": "The logon name of the RAM user",
      "help_zh": "RAM用户的登录名称",
      "location": "query",
      "name": "user_principal_name",
      "options": [
        "--user-principal-name"
      ],
      "raw_name": "UserPrincipalName",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The logon password for the RAM user.\n\nThe password must meet the password policy.",
      "description_zh": "RAM用户的控制台登录密码。\n\n密码必须符合密码强度要求。",
      "example": "mypassword",
      "help_en": "The logon password for the RAM user.\n\nThe password must meet the password policy",
      "help_zh": "RAM用户的控制台登录密码。\n\n密码必须符合密码强度要求",
      "location": "query",
      "name": "password",
      "options": [
        "--password"
      ],
      "raw_name": "Password",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "Specifies whether the RAM user must reset the password at the next logon. Valid values:\n\n- true\n\n- false (default)",
      "description_zh": "RAM用户在下次登录时是否必须重置密码。取值：\n\n- true\n- false（默认值）",
      "example": "false",
      "help_en": "Specifies whether the RAM user must reset the password at the next logon. Valid values:\n\n- true\n\n- false (default)",
      "help_zh": "RAM用户在下次登录时是否必须重置密码。取值：\n\n- true\n- false（默认值）",
      "location": "query",
      "name": "password_reset_required",
      "options": [
        "--password-reset-required"
      ],
      "raw_name": "PasswordResetRequired",
      "required": false,
      "type": "bool"
    },
    {
      "description_en": "Specifies whether the RAM user must enable multi-factor authentication (MFA). Valid values:\n\n- true: MFA is required. The RAM user must bind an MFA device at the next logon.\n\n- false (default): MFA is not required.",
      "description_zh": "是否强制要求RAM用户开启多因素认证。取值：\n\n- true：要求开启。RAM用户在下次登录时必须绑定多因素认证设备。\n- false（默认值）：不要求开启。",
      "example": "false",
      "help_en": "Specifies whether the RAM user must enable multi-factor authentication (MFA). Valid values:\n\n- true: MFA is required. The RAM user must bind an MFA device at the next logon.\n\n- false (default): MFA is not required",
      "help_zh": "是否强制要求RAM用户开启多因素认证。取值：\n\n- true：要求开启。RAM用户在下次登录时必须绑定多因素认证设备。\n- false（默认值）：不要求开启",
      "location": "query",
      "name": "mfa_bind_required",
      "options": [
        "--mfa-bind-required"
      ],
      "raw_name": "MFABindRequired",
      "required": false,
      "type": "bool"
    },
    {
      "description_en": "Specifies whether to enable password-based logon to the console. Valid values:\n\n- Active (default): Enable\n\n- Inactive: Disable",
      "description_zh": "开启或禁用控制台密码登录。取值：\n\n- Active（默认值）：开启。\n- Inactive：禁用。",
      "example": "Active",
      "help_en": "Specifies whether to enable password-based logon to the console. Valid values:\n\n- Active (default): Enable\n\n- Inactive: Disable",
      "help_zh": "开启或禁用控制台密码登录。取值：\n\n- Active（默认值）：开启。\n- Inactive：禁用",
      "location": "query",
      "name": "status",
      "options": [
        "--status"
      ],
      "raw_name": "Status",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ims",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "cn-hangzhou": "ims.vpc-proxy.aliyuncs.com"
  }
}
