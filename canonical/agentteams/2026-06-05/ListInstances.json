{
  "camel_example": "",
  "cmd_full_name": "agentteams list-instances",
  "cmd_name": "list-instances",
  "deprecated": false,
  "description_zh": "用于查询符合条件的实例列表，并支持分页和模糊匹配。支持 GET 和 POST 方法，列表按创建时间倒序返回",
  "endpoint_map": {},
  "global_endpoint": "",
  "kebab_example": "aliyun agentteams list-instances --instance-name AgentTeams --status RUNNING --max-results 10 --next-token 20 --skip 0 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ListInstances",
  "operation": {
    "action": "ListInstances",
    "api_style": "RPC",
    "api_version": "2026-06-05",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": "/"
  },
  "parameters": [
    {
      "description_zh": "* **含义**：实例名称，支持模糊匹配\n",
      "example": "AgentTeams",
      "help_en": "The InstanceName",
      "help_zh": "* **含义**：实例名称，支持模糊匹配",
      "location": "query",
      "name": "instance_name",
      "options": [
        "--instance-name"
      ],
      "raw_name": "InstanceName",
      "required": false,
      "type": "string"
    },
    {
      "description_zh": "* **含义**：实例状态\n",
      "example": "RUNNING",
      "help_en": "The Status",
      "help_zh": "* **含义**：实例状态",
      "location": "query",
      "name": "status",
      "options": [
        "--status"
      ],
      "raw_name": "Status",
      "required": false,
      "type": "string"
    },
    {
      "description_zh": "* **含义**：分页大小\n* **默认值**：20\n* **当前实现限制**：1 ~ 100\n",
      "example": "10",
      "help_en": "The MaxResults",
      "help_zh": "* **含义**：分页大小\n* **默认值**：20\n* **当前实现限制**：1 ~ 100",
      "location": "query",
      "name": "max_results",
      "options": [
        "--max-results"
      ],
      "raw_name": "MaxResults",
      "required": false,
      "type": "int"
    },
    {
      "description_zh": "* **含义**：下一页游标\n",
      "example": "20",
      "help_en": "The NextToken",
      "help_zh": "* **含义**：下一页游标",
      "location": "query",
      "name": "next_token",
      "options": [
        "--next-token"
      ],
      "raw_name": "NextToken",
      "required": false,
      "type": "string"
    },
    {
      "description_zh": "* **含义**：跳过的记录数\n* **默认值**：0\n",
      "example": "0",
      "help_en": "The Skip",
      "help_zh": "* **含义**：跳过的记录数\n* **默认值**：0",
      "location": "query",
      "name": "skip",
      "options": [
        "--skip"
      ],
      "raw_name": "Skip",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "/",
  "product_code": "AgentTeams",
  "protocol": "HTTPS",
  "vpc_endpoint_map": {}
}
