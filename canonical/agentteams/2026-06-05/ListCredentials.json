{
  "camel_example": "",
  "cmd_full_name": "agentteams list-credentials",
  "cmd_name": "list-credentials",
  "deprecated": false,
  "description_zh": "用于分页查询指定 AgentTeams 实例下的凭证列表，返回凭证摘要信息及各自绑定的 Worker 数量",
  "endpoint_map": {},
  "global_endpoint": "",
  "kebab_example": "aliyun agentteams list-credentials --instance-id ami-2ze8x9c6f --max-results 10 --next-token 0 --name-like OPENAI --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ListCredentials",
  "operation": {
    "action": "ListCredentials",
    "api_style": "RPC",
    "api_version": "2026-06-05",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "description_zh": "AgentTeams 实例 ID",
      "example": "ami-2ze8x9c6f",
      "help_en": "The InstanceId",
      "help_zh": "AgentTeams 实例 ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "description_zh": "每页返回的最大数量，取值范围 1-100，默认 10",
      "example": "10",
      "help_en": "The MaxResults",
      "help_zh": "每页返回的最大数量，取值范围 1-100，默认 10",
      "location": "query",
      "name": "max_results",
      "options": [
        "--max-results"
      ],
      "raw_name": "MaxResults",
      "required": false,
      "type": "int"
    },
    {
      "description_zh": "分页令牌，首次调用留空，后续使用上一次返回的 NextToken 值",
      "example": "0",
      "help_en": "The NextToken",
      "help_zh": "分页令牌，首次调用留空，后续使用上一次返回的 NextToken 值",
      "location": "query",
      "name": "next_token",
      "options": [
        "--next-token"
      ],
      "raw_name": "NextToken",
      "required": false,
      "type": "string"
    },
    {
      "description_zh": "凭证名称模糊匹配关键字",
      "example": "OPENAI",
      "help_en": "The NameLike",
      "help_zh": "凭证名称模糊匹配关键字",
      "location": "query",
      "name": "name_like",
      "options": [
        "--name-like"
      ],
      "raw_name": "NameLike",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AgentTeams",
  "protocol": "HTTPS",
  "vpc_endpoint_map": {}
}
