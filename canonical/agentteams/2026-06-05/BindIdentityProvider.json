{
  "camel_example": "",
  "cmd_full_name": "agentteams bind-identity-provider",
  "cmd_name": "bind-identity-provider",
  "deprecated": false,
  "description_en": "Bindsan upstream identity provider to a specified instance and triggers a synchronization task",
  "description_zh": "为指定实例绑定上游身份提供商并触发同步任务",
  "endpoint_map": {},
  "global_endpoint": "",
  "kebab_example": "aliyun agentteams bind-identity-provider --instance-id agentteams-xxx --identity-provider-type DingTalk --idp-metadata appKey_:_dingxxxx_,_appSecret_:_xxxxx_,_corpId_:_dingxxxx_,_encryptKey_:_xxxxx_,_verificationToken_:_xxxxx --login-enabled true --sync-enabled true --client-token ct-1234abcd --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "BindIdentityProvider",
  "operation": {
    "action": "BindIdentityProvider",
    "api_style": "RPC",
    "api_version": "2026-06-05",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The instance ID.",
      "description_zh": "实例 ID。\n",
      "example": "agentteams-xxx",
      "help_en": "The instance ID",
      "help_zh": "实例 ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The identity provider type. Currently, only DingTalk is supported.",
      "description_zh": "身份提供商类型，目前仅支持钉钉。\n",
      "example": "DingTalk",
      "help_en": "The identity provider type. Currently, only DingTalk is supported",
      "help_zh": "身份提供商类型，目前仅支持钉钉",
      "location": "query",
      "name": "identity_provider_type",
      "options": [
        "--identity-provider-type"
      ],
      "raw_name": "IdentityProviderType",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The identity provider metadata. In DingTalk scenarios, this is a JSON object string that contains the `appKey`, `appSecret`, `corpId`, `encryptKey`, and `verificationToken` fields. This is a sensitive parameter.",
      "description_zh": "身份提供商元数据，钉钉场景下为 JSON 对象字符串，包含 `appKey`、`appSecret`、`corpId`、`encryptKey`、`verificationToken` 字段。该参数为敏感参数。\n",
      "example": "{\"appKey\":\"dingxxxx\",\"appSecret\":\"xxxxx\",\"corpId\":\"dingxxxx\",\"encryptKey\":\"xxxxx\",\"verificationToken\":\"xxxxx\"}",
      "help_en": "The identity provider metadata. In DingTalk scenarios, this is a JSON object string that contains the `appKey`, `appSecret`, `corpId`, `encryptKey`, and `verificationToken` fields. This is a sensitive parameter",
      "help_zh": "身份提供商元数据，钉钉场景下为 JSON 对象字符串，包含 `appKey`、`appSecret`、`corpId`、`encryptKey`、`verificationToken` 字段。该参数为敏感参数",
      "location": "query",
      "name": "idp_metadata",
      "options": [
        "--idp-metadata"
      ],
      "raw_name": "IdpMetadata",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "Specifies whether to enable logon. Default value: `true`. This default value is used if the parameter is not specified or is left empty.",
      "description_zh": "是否开启登录，不传或为空时默认为 `true`。\n",
      "example": "true",
      "help_en": "Specifies whether to enable logon. Default value: `true`. This default value is used if the parameter is not specified or is left empty",
      "help_zh": "是否开启登录，不传或为空时默认为 `true`",
      "location": "query",
      "name": "login_enabled",
      "options": [
        "--login-enabled"
      ],
      "raw_name": "LoginEnabled",
      "required": false,
      "type": "bool"
    },
    {
      "description_en": "Specifies whether to enable user synchronization. Default value: `false`. User synchronization is enabled only when this parameter is explicitly set to `true`.",
      "description_zh": "是否开启用户同步，不传时默认为 `false`，仅当显式传 `true` 时开启。\n",
      "example": "true",
      "help_en": "Specifies whether to enable user synchronization. Default value: `false`. User synchronization is enabled only when this parameter is explicitly set to `true`",
      "help_zh": "是否开启用户同步，不传时默认为 `false`，仅当显式传 `true` 时开启",
      "location": "query",
      "name": "sync_enabled",
      "options": [
        "--sync-enabled"
      ],
      "raw_name": "SyncEnabled",
      "required": false,
      "type": "bool"
    },
    {
      "description_en": "The client idempotency token that is used to ensure the idempotence of the request.",
      "description_zh": "客户端幂等令牌，用于保证请求幂等性。\n",
      "example": "ct-1234abcd",
      "help_en": "The client idempotency token that is used to ensure the idempotence of the request",
      "help_zh": "客户端幂等令牌，用于保证请求幂等性",
      "location": "form",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AgentTeams",
  "protocol": "HTTPS",
  "v1_body_parameters": [
    {
      "description_en": "The client idempotency token that is used to ensure the idempotence of the request.",
      "description_zh": "客户端幂等令牌，用于保证请求幂等性。",
      "name": "ClientToken",
      "position": "body",
      "required": false,
      "type": "string"
    }
  ],
  "vpc_endpoint_map": {}
}
