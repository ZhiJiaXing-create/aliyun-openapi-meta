{
  "camel_example": "aliyun ga UpdateDomain --region cn-hangzhou --TargetDomain 'example.com' --RegionId 'cn-hangzhou' --Domain 'example.net'",
  "cmd_full_name": "ga update-domain",
  "cmd_name": "update-domain",
  "deprecated": false,
  "description_en": "Calls the UpdateDomain operation to update a domain name",
  "description_zh": "调用UpdateDomain接口更新域名",
  "endpoint_map": {
    "cn-hangzhou": "ga.cn-hangzhou.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun ga update-domain --biz-region-id cn-hangzhou --target-domain example.com --domain example.net --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpdateDomain",
  "operation": {
    "action": "UpdateDomain",
    "api_style": "RPC",
    "api_version": "2019-11-20",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The region ID of the Alibaba Cloud Global Accelerator (GA) instance. Set the value to **cn-hangzhou**.",
      "description_zh": "全球加速实例所属的地域ID，仅取值：**cn-hangzhou**。",
      "example": "cn-hangzhou",
      "help_en": "The region ID of the Alibaba Cloud Global Accelerator (GA) instance. Set the value to **cn-hangzhou**",
      "help_zh": "全球加速实例所属的地域ID，仅取值：**cn-hangzhou**",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The accelerated domain name that you want to update.",
      "description_zh": "待修改的加速域名。",
      "example": "example.com",
      "help_en": "The accelerated domain name that you want to update",
      "help_zh": "待修改的加速域名",
      "location": "query",
      "name": "target_domain",
      "options": [
        "--target-domain"
      ],
      "raw_name": "TargetDomain",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The new accelerated domain name.\n\nOnly primary domain names are supported, such as `example.net`.",
      "description_zh": "新的加速域名。\n\n仅支持添加主域名，例如`example.net`。",
      "example": "example.net",
      "help_en": "The new accelerated domain name.\n\nOnly primary domain names are supported, such as `example.net`",
      "help_zh": "新的加速域名。\n\n仅支持添加主域名，例如`example.net`",
      "location": "query",
      "name": "domain",
      "options": [
        "--domain"
      ],
      "raw_name": "Domain",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ga",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "cn-hangzhou": "ga-vpc.cn-hangzhou.aliyuncs.com"
  }
}
