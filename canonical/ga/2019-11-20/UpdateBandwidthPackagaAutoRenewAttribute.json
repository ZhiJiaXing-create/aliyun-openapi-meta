{
  "camel_example": "aliyun ga UpdateBandwidthPackagaAutoRenewAttribute --region cn-hangzhou --InstanceId 'gbwp-bp1sgzldyj6b4q7cx0000' --AutoRenew false --RenewalStatus Normal --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --Name test --AutoRenewDuration 1",
  "cmd_full_name": "ga update-bandwidth-packaga-auto-renew-attribute",
  "cmd_name": "update-bandwidth-packaga-auto-renew-attribute",
  "deprecated": false,
  "description_en": "Changes the auto-renewal status of a bandwidth plan",
  "description_zh": "调用UpdateBandwidthPackagaAutoRenewAttribute接口修改带宽包的自动续费属性",
  "endpoint_map": {
    "cn-hangzhou": "ga.cn-hangzhou.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun ga update-bandwidth-packaga-auto-renew-attribute --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-426655440000 --name test --instance-id gbwp-bp1sgzldyj6b4q7cx0000 --auto-renew false --renewal-status Normal --auto-renew-duration 1 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpdateBandwidthPackagaAutoRenewAttribute",
  "operation": {
    "action": "UpdateBandwidthPackagaAutoRenewAttribute",
    "api_style": "RPC",
    "api_version": "2019-11-20",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The ID of the region where the Global Accelerator (GA) instance is deployed. Set the value to **cn-hangzhou**.\n",
      "description_zh": "全球加速实例所在的地域ID，仅取值**cn-hangzhou**。",
      "example": "cn-hangzhou",
      "help_en": "The ID of the region where the Global Accelerator (GA) instance is deployed. Set the value to **cn-hangzhou**",
      "help_zh": "全球加速实例所在的地域ID，仅取值**cn-hangzhou**",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request.\n",
      "description_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
      "example": "123e4567-e89b-12d3-a456-426655440000",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The name of the bandwidth plan.\n\nThe name must be 2 to 128 characters in length, and can contain letters, digits, periods (.), underscores (\\_), and hyphens (-). The name must start with a letter.\n",
      "description_zh": "带宽包的名称。\n\n名称长度为2~128个英文或中文字符，以大小字母或中文开头，可包含数字、半角句号（.）、下划线（_）和短划线（-）。",
      "example": "test",
      "help_en": "The name of the bandwidth plan.\n\nThe name must be 2 to 128 characters in length, and can contain letters, digits, periods (.), underscores (\\_), and hyphens (-). The name must start with a letter",
      "help_zh": "带宽包的名称。\n\n名称长度为2~128个英文或中文字符，以大小字母或中文开头，可包含数字、半角句号（.）、下划线（_）和短划线（-）",
      "location": "query",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "Name",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The ID of the bandwidth plan.\n",
      "description_zh": "带宽包ID。",
      "example": "gbwp-bp1sgzldyj6b4q7cx****",
      "help_en": "The ID of the bandwidth plan",
      "help_zh": "带宽包ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "Specifies whether to enable auto-renewal. Valid values:\n\n*   **true**\n*   **false** (default)\n\n>  You must specify **AutoRenew** or **RenewalStatus**.\n",
      "description_zh": "是否开启自动续费。取值：\n\n- **true**：是。\n\n- **false**（默认值）：否。\n\n>**AutoRenew**和**RenewalStatus**不能同时为空。",
      "example": "false",
      "help_en": "Specifies whether to enable auto-renewal. Valid values:\n\n*   **true**\n*   **false** (default)\n\n>  You must specify **AutoRenew** or **RenewalStatus**",
      "help_zh": "是否开启自动续费。取值：\n\n- **true**：是。\n\n- **false**（默认值）：否。\n\n>**AutoRenew**和**RenewalStatus**不能同时为空",
      "location": "query",
      "name": "auto_renew",
      "options": [
        "--auto-renew"
      ],
      "raw_name": "AutoRenew",
      "required": false,
      "type": "bool"
    },
    {
      "description_en": "The auto-renewal status of the bandwidth plan. Valid values:\n\n*   **AutoRenewal**: The bandwidth plan is automatically renewed.\n*   **Normal**: You must manually renew the bandwidth plan.\n*   **NotRenewal**: The bandwidth plan is not renewed after it expires. The system sends only a non-renewal reminder three days before the expiration date. To renew a bandwidth plan for which you set RenewalStatus to NotRenewal, you can change the value of RenewalStatus from NotRenewal to Normal, and then manually renew the bandwidth plan. You can also set RenewalStatus to **AutoRenewal**.\n> *   You must specify **AutoRenew** or **RenewalStatus**.\n> *   **RenewalStatus** takes precedence over **AutoRenew**. If you do not specify **RenewalStatus**, **AutoRenew** is used.",
      "description_zh": "带宽包的自动续费状态。取值：\n\n- **AutoRenewal**：设置为自动续费。\n\n- **Normal**：手动续费。\n\n- **NotRenewal**：不再续费，系统不再发送到期提醒，只在到期前第三天发送不续费提醒。不再续费的带宽包可以修改成手动续费（Normal）后，再自行续费或配置为自动续费（**AutoRenewal**）。\n\n> - **AutoRenew**和**RenewalStatus**不能同时为空。\n> - **RenewalStatus**的优先级高于**AutoRenew**。如果不配置**RenewalStatus**，则默认以**AutoRenew**为准。",
      "example": "Normal",
      "help_en": "The auto-renewal status of the bandwidth plan. Valid values:\n\n*   **AutoRenewal**: The bandwidth plan is automatically renewed.\n*   **Normal**: You must manually renew the bandwidth plan.\n*   **NotRenewal**: The bandwidth plan is not renewed after it expires. The system sends only a non-renewal reminder three days before the expiration date. To renew a bandwidth plan for which you set RenewalStatus to NotRenewal, you can change the value of RenewalStatus from NotRenewal to Normal, and then manually renew the bandwidth plan. You can also set RenewalStatus to **AutoRenewal**.\n> *   You must specify **AutoRenew** or **RenewalStatus**.\n> *   **RenewalStatus** takes precedence over **AutoRenew**. If you do not specify **RenewalStatus**, **AutoRenew** is used",
      "help_zh": "带宽包的自动续费状态。取值：\n\n- **AutoRenewal**：设置为自动续费。\n\n- **Normal**：手动续费。\n\n- **NotRenewal**：不再续费，系统不再发送到期提醒，只在到期前第三天发送不续费提醒。不再续费的带宽包可以修改成手动续费（Normal）后，再自行续费或配置为自动续费（**AutoRenewal**）。\n\n> - **AutoRenew**和**RenewalStatus**不能同时为空。\n> - **RenewalStatus**的优先级高于**AutoRenew**。如果不配置**RenewalStatus**，则默认以**AutoRenew**为准",
      "location": "query",
      "name": "renewal_status",
      "options": [
        "--renewal-status"
      ],
      "raw_name": "RenewalStatus",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The auto-renewal duration. Unit: months. Valid values: **1** to **12**.\n\n> This parameter takes effect only if **AutoRenew** is set to **true**.",
      "description_zh": "自动续费时长。单位：月。取值范围：**1**~**12**。\n\n> 仅**AutoRenew**的为**true**时，该项才生效。",
      "example": "1",
      "help_en": "The auto-renewal duration. Unit: months. Valid values: **1** to **12**.\n\n> This parameter takes effect only if **AutoRenew** is set to **true**",
      "help_zh": "自动续费时长。单位：月。取值范围：**1**~**12**。\n\n> 仅**AutoRenew**的为**true**时，该项才生效",
      "location": "query",
      "name": "auto_renew_duration",
      "options": [
        "--auto-renew-duration"
      ],
      "raw_name": "AutoRenewDuration",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Ga",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "cn-hangzhou": "ga-vpc.cn-hangzhou.aliyuncs.com"
  }
}
