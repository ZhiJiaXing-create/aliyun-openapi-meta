{
  "camel_example": "aliyun ga UpdateServiceManagedControl --region cn-hangzhou --ServiceManaged false --ResourceId 'ga-bp149u6o36qt1as9b0000' --ResourceType Accelerator --RegionId 'cn-hangzhou' --ClientToken 123e45670000",
  "cmd_full_name": "ga update-service-managed-control",
  "cmd_name": "update-service-managed-control",
  "deprecated": false,
  "description_en": "Changes the control mode of a resource from managed mode to unmanaged mode",
  "description_zh": "通过调用UpdateServiceManagedControl接口对托管状态的资源进行解托管",
  "endpoint_map": {
    "cn-hangzhou": "ga.cn-hangzhou.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun ga update-service-managed-control --biz-region-id cn-hangzhou --client-token 123e45670000 --resource-type Accelerator --resource-id ga-bp149u6o36qt1as9b0000 --service-managed false --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "UpdateServiceManagedControl",
  "operation": {
    "action": "UpdateServiceManagedControl",
    "api_style": "RPC",
    "api_version": "2019-11-20",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The region ID of the GA instance. Set the value to cn-hangzhou.\n",
      "description_zh": "全球加速实例所在的地域ID，仅取值cn-hangzhou。",
      "example": "cn-hangzhou",
      "help_en": "The region ID of the GA instance. Set the value to cn-hangzhou",
      "help_zh": "全球加速实例所在的地域ID，仅取值cn-hangzhou",
      "location": "query",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system automatically uses the value of **RequestId** as the value of **ClientToken**. The **request ID** may be different for each request.\n",
      "description_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
      "example": "123e4567****",
      "help_en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system automatically uses the value of **RequestId** as the value of **ClientToken**. The **request ID** may be different for each request",
      "help_zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The type of the resource whose control mode you want to change.\n\nSet the value to **Accelerator**, which specifies a standard GA instance.\n",
      "description_zh": "操作的资源类型。取值：\n\n **Accelerator**：标准型全球加速实例。",
      "example": "Accelerator",
      "help_en": "The type of the resource whose control mode you want to change.\n\nSet the value to **Accelerator**, which specifies a standard GA instance",
      "help_zh": "操作的资源类型。取值：\n\n **Accelerator**：标准型全球加速实例",
      "location": "query",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The ID of the resource whose control mode you want to change.\n",
      "description_zh": "待操作的资源ID。",
      "example": "ga-bp149u6o36qt1as9b****",
      "help_en": "The ID of the resource whose control mode you want to change",
      "help_zh": "待操作的资源ID",
      "location": "query",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "raw_name": "ResourceId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The control mode of the resource.\n\nSet the value to **false**, which changes the control mode of the resource from managed mode to unmanaged mode.\n\n>  You can change the control mode only from managed mode to unmanaged mode.\n",
      "description_zh": "设置资源托管模式。取值：  \n\n**false**：解除当前资源的托管状态。\n\n> 当前仅支持解除托管。",
      "example": "false",
      "help_en": "The control mode of the resource.\n\nSet the value to **false**, which changes the control mode of the resource from managed mode to unmanaged mode.\n\n>  You can change the control mode only from managed mode to unmanaged mode",
      "help_zh": "设置资源托管模式。取值：  \n\n**false**：解除当前资源的托管状态。\n\n> 当前仅支持解除托管",
      "location": "query",
      "name": "service_managed",
      "options": [
        "--service-managed"
      ],
      "raw_name": "ServiceManaged",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Ga",
  "protocol": "HTTPS",
  "vpc_endpoint_map": {
    "cn-hangzhou": "ga-vpc.cn-hangzhou.aliyuncs.com"
  }
}
