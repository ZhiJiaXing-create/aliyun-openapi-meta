{
  "camel_example": "",
  "cmd_full_name": "wss describe-credit-usage-info",
  "cmd_name": "describe-credit-usage-info",
  "deprecated": false,
  "description_en": "Queries credit usage by a specified dimension such as user, credit package, or agent",
  "description_zh": "按指定维度查询用户、积分包或Agent的积分使用情况",
  "endpoint_map": {
    "ap-southeast-1": "wss.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "wss.cn-shanghai.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun wss describe-credit-usage-info --biz-type BUSINESS --usage-type User --instance-ids cp-0bszoespx7o1ehjxs --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeCreditUsageInfo",
  "operation": {
    "action": "DescribeCreditUsageInfo",
    "api_style": "RPC",
    "api_version": "2021-12-21",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The business type.",
      "description_zh": "业务类型",
      "example": "BUSINESS",
      "help_en": "The business type",
      "help_zh": "业务类型",
      "location": "query",
      "name": "biz_type",
      "options": [
        "--biz-type"
      ],
      "raw_name": "BizType",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The usage type. Valid values:\n*   User: Returns the usage, remaining credits, and consumption trends of the active credit packages for the current user.\n*   CreditPackage: Requires a CreditPackageId. Returns the total and remaining credits of the specified credit package.\n*   Agent: Requires an AgentId. Returns the cumulative credit usage, cumulative allocated quota, and the percentages of both.",
      "description_zh": "用量类型（UseageType）：User、CreditPackage、Agent\n*   User：返回用户当前生效积分包的用量、余量及消耗趋势\n*   CreditPackage：需要传入CreditPackageId，返回指定积分包的总量、余量\n*   Agent：需要传入AgentId，返回累计积分用量、累计分配额度及两者百分比\n",
      "example": "User",
      "help_en": "The usage type. Valid values:\n*   User: Returns the usage, remaining credits, and consumption trends of the active credit packages for the current user.\n*   CreditPackage: Requires a CreditPackageId. Returns the total and remaining credits of the specified credit package.\n*   Agent: Requires an AgentId. Returns the cumulative credit usage, cumulative allocated quota, and the percentages of both",
      "help_zh": "用量类型（UseageType）：User、CreditPackage、Agent\n*   User：返回用户当前生效积分包的用量、余量及消耗趋势\n*   CreditPackage：需要传入CreditPackageId，返回指定积分包的总量、余量\n*   Agent：需要传入AgentId，返回累计积分用量、累计分配额度及两者百分比",
      "location": "query",
      "name": "usage_type",
      "options": [
        "--usage-type"
      ],
      "raw_name": "UsageType",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The JSON string of instance IDs. This parameter can be omitted when `UsageType=User`. Set this parameter to the credit package instance ID when `UsageType=CreditPackage`, or to the `AgentId` when `UsageType=Agent`.",
      "description_zh": "实例 ID 列表 JSON 字符串；`UsageType=User` 时可省略，`CreditPackage` 时传积分包实例 ID，`Agent` 时传 `AgentId`\n",
      "element_type": "string",
      "help_en": "The JSON string of instance IDs. This parameter can be omitted when `UsageType=User`. Set this parameter to the credit package instance ID when `UsageType=CreditPackage`, or to the `AgentId` when `UsageType=Agent`",
      "help_zh": "实例 ID 列表 JSON 字符串；`UsageType=User` 时可省略，`CreditPackage` 时传积分包实例 ID，`Agent` 时传 `AgentId`",
      "location": "query",
      "name": "instance_ids",
      "options": [
        "--instance-ids"
      ],
      "param_style": "repeatList",
      "raw_name": "InstanceIds",
      "required": false,
      "type": "array"
    }
  ],
  "pathPattern": "",
  "product_code": "wss",
  "protocol": "HTTPS",
  "vpc_endpoint_map": {
    "ap-southeast-1": "wss-vpc.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "wss.vpc-proxy.aliyuncs.com"
  }
}
