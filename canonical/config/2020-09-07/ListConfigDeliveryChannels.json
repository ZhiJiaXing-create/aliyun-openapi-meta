{
  "cmd_full_name": "config list-config-delivery-channels",
  "cmd_name": "list-config-delivery-channels",
  "deprecated": false,
  "description_en": "Returns a list of delivery channels",
  "description_zh": "查询投递渠道列表",
  "endpoint_map": {
    "ap-southeast-1": "config.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "config.cn-shanghai.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun config list-config-delivery-channels --delivery-channel-ids cdc-d9106457e0d900b10000 --api-version 2020-09-07 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "ListConfigDeliveryChannels",
  "operation": {
    "action": "ListConfigDeliveryChannels",
    "api_style": "RPC",
    "api_version": "2020-09-07",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "list",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The ID of the delivery channel. Separate multiple delivery channel IDs with a comma (,).",
      "description_zh": "投递渠道ID。多个投递渠道ID之间用半角逗号（,）分隔。",
      "example": "cdc-d9106457e0d900b1****",
      "help_en": "The ID of the delivery channel. Separate multiple delivery channel IDs with a comma (,)",
      "help_zh": "投递渠道ID。多个投递渠道ID之间用半角逗号（,）分隔",
      "location": "query",
      "name": "delivery_channel_ids",
      "options": [
        "--delivery-channel-ids"
      ],
      "raw_name": "DeliveryChannelIds",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Config",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "ap-southeast-1": "config-vpc.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "config-vpc.cn-shanghai.aliyuncs.com"
  }
}
