{
  "cmd_full_name": "config untag-resources",
  "cmd_name": "untag-resources",
  "deprecated": false,
  "description_en": "Detaches tags from resources in Cloud Config",
  "description_zh": "为配置审计中的资源解绑标签",
  "endpoint_map": {
    "ap-southeast-1": "config.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "config.cn-shanghai.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun config untag-resources --biz-region-id cn-hangzhou --resource-type ACS::Config::Rule --resource-id cr-115b626622af00600000 --tag-key Env --all false --api-version 2020-09-07 --region cn-hangzhou",
  "method": "POST",
  "multi_version": true,
  "name": "UntagResources",
  "operation": {
    "action": "UntagResources",
    "api_style": "RPC",
    "api_version": "2020-09-07",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The ID of the region.",
      "description_zh": "标签所属的地域ID。",
      "example": "cn-shanghai",
      "help_en": "The ID of the region",
      "help_zh": "标签所属的地域ID",
      "location": "form",
      "name": "region_id",
      "options": [
        "--biz-region-id"
      ],
      "raw_name": "RegionId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The type of the CloudConfig resource. Valid values:\n\n- `ACS::Config::Rule`: a rule for a single account.\n\n- `ACS::Config::AggregateConfigRule`: a rule for multiple accounts.\n\n- `ACS::Config::Aggregator`: an account group.\n\n- `ACS::Config::CompliancePack`: a compliance package for a single account.\n\n- `ACS::Config::AggregateCompliancePack`: a compliance package for multiple accounts.",
      "description_zh": "配置审计的资源类型。取值：\n- `ACS::Config::Rule`：单账号下的规则。\n- `ACS::Config::AggregateConfigRule`：多账号下的规则。\n- `ACS::Config::Aggregator`：账号组。\n- `ACS::Config::CompliancePack`：单账号下的合规包。\n- `ACS::Config::AggregateCompliancePack`：多账号下的合规包。",
      "example": "ACS::Config::Rule",
      "help_en": "The type of the CloudConfig resource. Valid values:\n\n- `ACS::Config::Rule`: a rule for a single account.\n\n- `ACS::Config::AggregateConfigRule`: a rule for multiple accounts.\n\n- `ACS::Config::Aggregator`: an account group.\n\n- `ACS::Config::CompliancePack`: a compliance package for a single account.\n\n- `ACS::Config::AggregateCompliancePack`: a compliance package for multiple accounts",
      "help_zh": "配置审计的资源类型。取值：\n- `ACS::Config::Rule`：单账号下的规则。\n- `ACS::Config::AggregateConfigRule`：多账号下的规则。\n- `ACS::Config::Aggregator`：账号组。\n- `ACS::Config::CompliancePack`：单账号下的合规包。\n- `ACS::Config::AggregateCompliancePack`：多账号下的合规包",
      "location": "form",
      "name": "resource_type",
      "options": [
        "--resource-type"
      ],
      "raw_name": "ResourceType",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The IDs of the resources. You can specify a maximum of 50 resource IDs.\n\nYou can detach tags from up to 50 resources at a time.",
      "description_zh": "资源ID。\n\n单次最多支持为50个资源解绑标签。",
      "element_type": "string",
      "help_en": "The IDs of the resources. You can specify a maximum of 50 resource IDs.\n\nYou can detach tags from up to 50 resources at a time",
      "help_zh": "资源ID。\n\n单次最多支持为50个资源解绑标签",
      "location": "form",
      "name": "resource_id",
      "options": [
        "--resource-id"
      ],
      "param_style": "repeatList",
      "raw_name": "ResourceId",
      "required": true,
      "type": "array"
    },
    {
      "description_en": "The tag keys of the tags to detach.",
      "description_zh": "资源的标签。",
      "element_type": "string",
      "help_en": "The tag keys of the tags to detach",
      "help_zh": "资源的标签",
      "location": "form",
      "name": "tag_key",
      "options": [
        "--tag-key"
      ],
      "param_style": "repeatList",
      "raw_name": "TagKey",
      "required": false,
      "type": "array"
    },
    {
      "description_en": "Specifies whether to detach all tags from the resources. Valid values:\n\n- true: Detach all tags from the resources.\n\n- false (default): Detach the specified tags.",
      "description_zh": "是否解绑资源上全部的标签。取值：\n\n- true：解绑资源上全部的标签。\n\n- false（默认值）：解绑资源上指定的标签。",
      "example": "false",
      "help_en": "Specifies whether to detach all tags from the resources. Valid values:\n\n- true: Detach all tags from the resources.\n\n- false (default): Detach the specified tags",
      "help_zh": "是否解绑资源上全部的标签。取值：\n\n- true：解绑资源上全部的标签。\n\n- false（默认值）：解绑资源上指定的标签",
      "location": "form",
      "name": "all",
      "options": [
        "--all"
      ],
      "raw_name": "All",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Config",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "ap-southeast-1": "config-vpc.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "config-vpc.cn-shanghai.aliyuncs.com"
  }
}
