{
  "cmd_full_name": "config delete-aggregators",
  "cmd_name": "delete-aggregators",
  "deprecated": false,
  "description_en": "The management account or delegated administrator account of a resource directory can delete an account group",
  "description_zh": "资源目录中的管理账号或委派管理员账号可以删除账号组",
  "endpoint_map": {
    "ap-southeast-1": "config.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "config.cn-shanghai.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun config delete-aggregators --aggregator-ids ca-9190626622af00a90000 --client-token AAAAAdDWBF20000 --api-version 2020-09-07 --region cn-hangzhou",
  "method": "POST",
  "multi_version": true,
  "name": "DeleteAggregators",
  "operation": {
    "action": "DeleteAggregators",
    "api_style": "RPC",
    "api_version": "2020-09-07",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "delete",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The ID of the account group. Separate multiple IDs with commas (,).\n",
      "description_zh": "账号组ID。多个账号组ID之间用半角逗号（,）分隔。",
      "example": "ca-9190626622af00a9****",
      "help_en": "The ID of the account group. Separate multiple IDs with commas (,)",
      "help_zh": "账号组ID。多个账号组ID之间用半角逗号（,）分隔",
      "location": "form",
      "name": "aggregator_ids",
      "options": [
        "--aggregator-ids"
      ],
      "raw_name": "AggregatorIds",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The client token that you want to use to ensure the idempotency of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.\n",
      "description_zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。\n\n",
      "example": "AAAAAdDWBF2****",
      "help_en": "The client token that you want to use to ensure the idempotency of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符",
      "location": "form",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Config",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "ap-southeast-1": "config-vpc.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "config-vpc.cn-shanghai.aliyuncs.com"
  }
}
