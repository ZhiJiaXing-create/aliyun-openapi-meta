{
  "camel_example": "aliyun alidns DescribeDomainStatisticsSummary --region cn-hangzhou --StartDate '2019-07-04' --PageSize 20 --Keyword test --PageNumber 1 --Lang en --EndDate '2019-07-04' --SearchMode LIKE --Threshold 12",
  "cmd_full_name": "alidns describe-domain-statistics-summary",
  "cmd_name": "describe-domain-statistics-summary",
  "deprecated": false,
  "description_en": "Queries the number of requests for all paid domain names in your account",
  "description_zh": "查询用户账号下所有付费域名的请求量列表",
  "endpoint_map": {
    "public": "alidns.aliyuncs.com"
  },
  "global_endpoint": "alidns.aliyuncs.com",
  "kebab_example": "aliyun alidns describe-domain-statistics-summary --lang en --page-number 1 --page-size 20 --start-date 2019-07-04 --end-date 2019-07-04 --search-mode LIKE --keyword test --threshold 12 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "DescribeDomainStatisticsSummary",
  "operation": {
    "action": "DescribeDomainStatisticsSummary",
    "api_style": "RPC",
    "api_version": "2015-01-09",
    "method": "POST",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The language of the response. Valid values:\n\n- **zh** (default): Chinese\n\n- **en**: English",
      "description_zh": "设置请求和接收消息的语言类型，取值：\n\n- **zh（默认）**：中文\n- **en**：英文",
      "example": "en",
      "help_en": "The language of the response. Valid values:\n\n- **zh** (default): Chinese\n\n- **en**: English",
      "help_zh": "设置请求和接收消息的语言类型，取值：\n\n- **zh（默认）**：中文\n- **en**：英文",
      "location": "query",
      "name": "lang",
      "options": [
        "--lang"
      ],
      "raw_name": "Lang",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The page number. The start value is **1**. The default value is **1**.",
      "description_zh": "当前页数，起始值为**1**，默认为**1**。",
      "example": "1",
      "help_en": "The page number. The start value is **1**. The default value is **1**",
      "help_zh": "当前页数，起始值为**1**，默认为**1**",
      "location": "query",
      "name": "page_number",
      "options": [
        "--page-number"
      ],
      "raw_name": "PageNumber",
      "required": false,
      "type": "int"
    },
    {
      "description_en": "The number of entries per page. The maximum value is **100** and the minimum value is **1**. The default value is **20**.",
      "description_zh": "分页查询时设置的每页行数，最大值**100**，最小值为**1**，默认值为**20**。",
      "example": "20",
      "help_en": "The number of entries per page. The maximum value is **100** and the minimum value is **1**. The default value is **20**",
      "help_zh": "分页查询时设置的每页行数，最大值**100**，最小值为**1**，默认值为**20**",
      "location": "query",
      "name": "page_size",
      "options": [
        "--page-size"
      ],
      "raw_name": "PageSize",
      "required": false,
      "type": "int"
    },
    {
      "description_en": "The start date of the query. The format is **YYYY-MM-DD**.\n\nYou can query data from the last 90 days only. The value of `StartDate` must be greater than or equal to the current date minus 90 days.",
      "description_zh": "查询的开始时间，格式：**YYYY-MM-DD**。\n\n只能查询最近90天的记录，即：`StartDate >= Now - 90`。",
      "example": "2019-07-04",
      "help_en": "The start date of the query. The format is **YYYY-MM-DD**.\n\nYou can query data from the last 90 days only. The value of `StartDate` must be greater than or equal to the current date minus 90 days",
      "help_zh": "查询的开始时间，格式：**YYYY-MM-DD**。\n\n只能查询最近90天的记录，即：`StartDate >= Now - 90`",
      "location": "query",
      "name": "start_date",
      "options": [
        "--start-date"
      ],
      "raw_name": "StartDate",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The end date of the query. The format is **YYYY-MM-DD**.\n\nThe default value is the current date.",
      "description_zh": "查询的结束时间，格式：**YYYY-MM-DD**。\n\n默认为查询当天的时间。",
      "example": "2019-07-04",
      "help_en": "The end date of the query. The format is **YYYY-MM-DD**.\n\nThe default value is the current date",
      "help_zh": "查询的结束时间，格式：**YYYY-MM-DD**。\n\n默认为查询当天的时间",
      "location": "query",
      "name": "end_date",
      "options": [
        "--end-date"
      ],
      "raw_name": "EndDate",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The search mode for the keyword. Valid values:\n\n- **LIKE** (default): Fuzzy match\n\n- **EXACT**: Exact match",
      "description_zh": "关键词的搜索模式。取值：\n\n- 模糊搜索（默认）：**LIKE**\n- 精确搜索：**EXACT**",
      "example": "LIKE",
      "help_en": "The search mode for the keyword. Valid values:\n\n- **LIKE** (default): Fuzzy match\n\n- **EXACT**: Exact match",
      "help_zh": "关键词的搜索模式。取值：\n\n- 模糊搜索（默认）：**LIKE**\n- 精确搜索：**EXACT**",
      "location": "query",
      "name": "search_mode",
      "options": [
        "--search-mode"
      ],
      "raw_name": "SearchMode",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The keyword. The search is performed in \\`%KeyWord%\\` mode and is case-insensitive.",
      "description_zh": "关键词，按照“%KeyWord%”模式搜索，不区分大小写。",
      "example": "test",
      "help_en": "The keyword. The search is performed in \\`%KeyWord%\\` mode and is case-insensitive",
      "help_zh": "关键词，按照“%KeyWord%”模式搜索，不区分大小写",
      "location": "query",
      "name": "keyword",
      "options": [
        "--keyword"
      ],
      "raw_name": "Keyword",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The threshold for the number of requests. This operation returns domain names whose request count is less than or equal to the specified value. For example, if you set this parameter to 100, domain names with 100 or fewer requests are returned. If you do not specify this parameter, all domain names are returned.",
      "description_zh": "查询总量阈值，也可查询相应请求量不大于threshold的数据。例如，如果您将该参数设置为100，可查询出请求量小于100次的域名。为空时查全部的域名",
      "example": "12",
      "help_en": "The threshold for the number of requests. This operation returns domain names whose request count is less than or equal to the specified value. For example, if you set this parameter to 100, domain names with 100 or fewer requests are returned. If you do not specify this parameter, all domain names are returned",
      "help_zh": "查询总量阈值，也可查询相应请求量不大于threshold的数据。例如，如果您将该参数设置为100，可查询出请求量小于100次的域名。为空时查全部的域名",
      "location": "query",
      "name": "threshold",
      "options": [
        "--threshold"
      ],
      "raw_name": "Threshold",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Alidns",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {}
}
