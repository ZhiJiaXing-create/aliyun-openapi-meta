{
  "camel_example": "aliyun esa StopScheduledPreloadExecution --region cn-hangzhou --Id 66599bd7397885b43804901c",
  "cmd_full_name": "esa stop-scheduled-preload-execution",
  "cmd_name": "stop-scheduled-preload-execution",
  "deprecated": false,
  "description_en": "Stops a single scheduled preload execution plan based on the preload plan ID.\nPrerequisites: (1) This API only takes effect when the execution plan status is running. Execution plans in the waiting or failed status cannot be stopped. (2) Whether an execution plan can reach the running status depends on whether the site it belongs to has completed access verification (site Status=active)",
  "description_zh": "根据预热计划ID停止单个定时预热计划。\n前置条件：(1) 仅当执行计划状态为 running 时本接口才生效，waiting 或 failed 状态的执行计划无法被停止；(2) 执行计划能否到达 running 状态取决于所属站点是否已完成接入验证（站点 Status=active）",
  "endpoint_map": {
    "ap-southeast-1": "esa.ap-southeast-1.aliyuncs.com",
    "cn-hangzhou": "esa.cn-hangzhou.aliyuncs.com"
  },
  "global_endpoint": "esa.cn-hangzhou.aliyuncs.com",
  "kebab_example": "aliyun esa stop-scheduled-preload-execution --id 66599bd7397885b43804901c --region cn-hangzhou",
  "method": "POST",
  "multi_version": false,
  "name": "StopScheduledPreloadExecution",
  "operation": {
    "action": "StopScheduledPreloadExecution",
    "api_style": "RPC",
    "api_version": "2024-09-10",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The preload plan ID, obtained from the SuccessExecutions[].Id field returned by CreateScheduledPreloadExecutions. Before calling this API, you must first create a preload task by calling CreateScheduledPreloadJob, and then create an execution plan by calling CreateScheduledPreloadExecutions.",
      "description_zh": "预热计划ID，由 CreateScheduledPreloadExecutions 返回的 SuccessExecutions[].Id 字段获取。调用本接口前需先通过 CreateScheduledPreloadJob 创建预热任务，再通过 CreateScheduledPreloadExecutions 创建执行计划。",
      "example": "66599bd7397885b43804901c",
      "help_en": "The preload plan ID, obtained from the SuccessExecutions[].Id field returned by CreateScheduledPreloadExecutions. Before calling this API, you must first create a preload task by calling CreateScheduledPreloadJob, and then create an execution plan by calling CreateScheduledPreloadExecutions",
      "help_zh": "预热计划ID，由 CreateScheduledPreloadExecutions 返回的 SuccessExecutions[].Id 字段获取。调用本接口前需先通过 CreateScheduledPreloadJob 创建预热任务，再通过 CreateScheduledPreloadExecutions 创建执行计划",
      "location": "query",
      "name": "id",
      "options": [
        "--id"
      ],
      "raw_name": "Id",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "ESA",
  "protocol": "HTTPS",
  "vpc_endpoint_map": {
    "ap-southeast-1": "esa-vpc.ap-southeast-1.aliyuncs.com",
    "cn-hangzhou": "esa-vpc.cn-hangzhou.aliyuncs.com"
  }
}
