{
  "cmd_full_name": "ccc reset-agent-state",
  "cmd_name": "reset-agent-state",
  "deprecated": false,
  "description_en": "Resets the state of a specific agent in a specific instance",
  "description_zh": "调用ResetAgentState重置指定实例下指定坐席的坐席状态",
  "endpoint_map": {
    "cn-shanghai": "ccc.cn-shanghai.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun ccc reset-agent-state --instance-id ccc-test --user-id agent@ccc-test --device-id device --api-version 2020-07-01 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "ResetAgentState",
  "operation": {
    "action": "ResetAgentState",
    "api_style": "RPC",
    "api_version": "2020-07-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The ID of the Cloud Call Center (CCC) instance.",
      "description_zh": "实例ID。\n",
      "example": "ccc-test",
      "help_en": "The ID of the Cloud Call Center (CCC) instance",
      "help_zh": "实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The ID of the agent. This parameter is optional. If omitted, the agent mapped to the current RAM account is reset.",
      "description_zh": "坐席ID，非必填，不填默认使用当前RAM账户映射的坐席。",
      "example": "agent@ccc-test",
      "help_en": "The ID of the agent. This parameter is optional. If omitted, the agent mapped to the current RAM account is reset",
      "help_zh": "坐席ID，非必填，不填默认使用当前RAM账户映射的坐席",
      "location": "query",
      "name": "user_id",
      "options": [
        "--user-id"
      ],
      "raw_name": "UserId",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "A string that identifies the device. The value is not processed by the system and can be any string.",
      "description_zh": "设备ID，可以随便填写，无特殊作用，无特殊要求。",
      "example": "device",
      "help_en": "A string that identifies the device. The value is not processed by the system and can be any string",
      "help_zh": "设备ID，可以随便填写，无特殊作用，无特殊要求",
      "location": "query",
      "name": "device_id",
      "options": [
        "--device-id"
      ],
      "raw_name": "DeviceId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "CCC",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "cn-shanghai": "ccc-vpc.cn-shanghai.aliyuncs.com"
  }
}
