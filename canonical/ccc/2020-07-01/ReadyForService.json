{
  "cmd_full_name": "ccc ready-for-service",
  "cmd_name": "ready-for-service",
  "deprecated": false,
  "description_en": "You can invoke ReadyForService to make a specified agent under a specified instance ready. After becoming ready, the agent enters the idle status",
  "description_zh": "调用ReadyForService使指定实例下的指定坐席就绪，就绪后坐席进入空闲状态",
  "endpoint_map": {
    "cn-shanghai": "ccc.cn-shanghai.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun ccc ready-for-service --instance-id ccc-test --user-id user-test@ccc-test --device-id device --outbound-scenario false --api-version 2020-07-01 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": true,
  "name": "ReadyForService",
  "operation": {
    "action": "ReadyForService",
    "api_style": "RPC",
    "api_version": "2020-07-01",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "none",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "Instance ID.",
      "description_zh": "实例ID。",
      "example": "ccc-test",
      "help_en": "Instance ID",
      "help_zh": "实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "Agent ID. If not specified, it defaults to the agent mapped to the current RAM account.",
      "description_zh": "坐席ID，不填默认为当前RAM账户映射的坐席。",
      "example": "user-test@ccc-test",
      "help_en": "Agent ID. If not specified, it defaults to the agent mapped to the current RAM account",
      "help_zh": "坐席ID，不填默认为当前RAM账户映射的坐席",
      "location": "query",
      "name": "user_id",
      "options": [
        "--user-id"
      ],
      "raw_name": "UserId",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "Device ID. This parameter is meaningless and can be filled with any value.",
      "description_zh": "设备ID，无意义，可以随便填写。",
      "example": "device",
      "help_en": "Device ID. This parameter is meaningless and can be filled with any value",
      "help_zh": "设备ID，无意义，可以随便填写",
      "location": "query",
      "name": "device_id",
      "options": [
        "--device-id"
      ],
      "raw_name": "DeviceId",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "Specifies whether to enter outbound-only mode after being published. This parameter is optional and defaults to false.",
      "description_zh": "上线后是否进入仅外呼模式，选填，默认false。",
      "example": "false",
      "help_en": "Specifies whether to enter outbound-only mode after being published. This parameter is optional and defaults to false",
      "help_zh": "上线后是否进入仅外呼模式，选填，默认false",
      "location": "query",
      "name": "outbound_scenario",
      "options": [
        "--outbound-scenario"
      ],
      "raw_name": "OutboundScenario",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "CCC",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "cn-shanghai": "ccc-vpc.cn-shanghai.aliyuncs.com"
  }
}
