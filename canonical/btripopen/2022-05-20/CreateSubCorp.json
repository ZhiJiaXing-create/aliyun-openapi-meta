{
  "camel_example": "",
  "cmd_full_name": "btripopen create-sub-corp",
  "cmd_name": "create-sub-corp",
  "deprecated": false,
  "description_zh": "调用本接口，可以实现为集团型的企业客户创建子企业",
  "endpoint_map": {},
  "global_endpoint": "",
  "kebab_example": "aliyun btripopen create-sub-corp --user-id 123 --outer-corp-id corp123 --outer-corp-name '测试企业' --x-acs-btrip-corp-token feth00jqwls --api-version 2022-05-20 --region cn-hangzhou",
  "method": "POST",
  "multi_version": true,
  "name": "CreateSubCorp",
  "operation": {
    "action": "CreateSubCorp",
    "api_style": "ROA",
    "api_version": "2022-05-20",
    "method": "POST",
    "protocol": "HTTPS",
    "url": "/sub_corps/v1/corps"
  },
  "parameters": [
    {
      "description_zh": "存在于已签署《阿里商旅主账号协议》的企业的员工 ID。",
      "example": "123",
      "help_en": "The user_id",
      "help_zh": "存在于已签署《阿里商旅主账号协议》的企业的员工 ID",
      "location": "form",
      "name": "user_id",
      "options": [
        "--user-id"
      ],
      "raw_name": "user_id",
      "required": true,
      "type": "string"
    },
    {
      "description_zh": "通过本接口建立的子企业唯一 ID，重复使用时将导致调用失败。",
      "example": "corp123",
      "help_en": "The outer_corp_id",
      "help_zh": "通过本接口建立的子企业唯一 ID，重复使用时将导致调用失败",
      "location": "form",
      "name": "outer_corp_id",
      "options": [
        "--outer-corp-id"
      ],
      "raw_name": "outer_corp_id",
      "required": true,
      "type": "string"
    },
    {
      "description_zh": "通过本接口创建的子企业唯一名称，重复使用时将会导致调用失败。",
      "example": "测试企业",
      "help_en": "The outer_corp_name",
      "help_zh": "通过本接口创建的子企业唯一名称，重复使用时将会导致调用失败",
      "location": "form",
      "name": "outer_corp_name",
      "options": [
        "--outer-corp-name"
      ],
      "raw_name": "outer_corp_name",
      "required": true,
      "type": "string"
    },
    {
      "description_zh": "企业访问凭证。\n- 通过 HTTP 方式调用本接口时，需要作为**必填参数**拼接在请求地址中。获取方式请参见：[企业访问凭证](https://openapi.alibtrip.com/doc/toDocDetail?docId=3769985)。\n- 在拼接时，推荐使用 `crop_token=value` 替代。",
      "example": "feth00jqwls",
      "help_en": "The x-acs-btrip-corp-token",
      "help_zh": "企业访问凭证。\n- 通过 HTTP 方式调用本接口时，需要作为**必填参数**拼接在请求地址中。获取方式请参见：[企业访问凭证](https://openapi.alibtrip.com/doc/toDocDetail?docId=3769985)。\n- 在拼接时，推荐使用 `crop_token=value` 替代",
      "location": "header",
      "name": "x_acs_btrip_corp_token",
      "options": [
        "--x-acs-btrip-corp-token"
      ],
      "raw_name": "x-acs-btrip-corp-token",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "/sub_corps/v1/corps",
  "product_code": "btripOpen",
  "protocol": "HTTPS",
  "vpc_endpoint_map": {}
}
