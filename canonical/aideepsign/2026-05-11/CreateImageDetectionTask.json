{
  "camel_example": "",
  "cmd_full_name": "aideepsign create-image-detection-task",
  "cmd_name": "create-image-detection-task",
  "deprecated": false,
  "description_en": "Creates an asynchronous image detection task that supports AIGC and tampering detection",
  "description_zh": "创建异步图片检测任务，支持AIGC和篡改检测",
  "endpoint_map": {
    "cn-hangzhou": "aideepsign.cn-hangzhou.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun aideepsign create-image-detection-task --client-token 5A2CFF0E-5718-45B5-9D4D-70B3FF3898 --object-key deepsign/123456789/scan/abc12345.jpg --image-url https://example.com/id-card.jpg --detect-type auto --cred-type deprecated --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateImageDetectionTask",
  "operation": {
    "action": "CreateImageDetectionTask",
    "api_style": "RPC",
    "api_version": "2026-05-11",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.",
      "description_zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一。`ClientToken` 只支持 ASCII 字符，且不能超过 64 个字符。\n",
      "example": "5A2CFF0E-5718-45B5-9D4D-70B3FF3898",
      "help_en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length",
      "help_zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一。`ClientToken` 只支持 ASCII 字符，且不能超过 64 个字符",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The ObjectKey of the image to be detected in OSS. When you use ObjectKey, make sure that the key belongs to the namespace of the current caller. You must specify at least one of ImageUrl and ObjectKey.",
      "description_zh": "待检测图片在 OSS 中的 `ObjectKey`。使用 `ObjectKey` 时需确保该 Key 属于当前调用者的命名空间。`ImageUrl` 与 `ObjectKey` 二选一，至少提供一个。\n",
      "example": "deepsign/123456789/scan/abc12345.jpg",
      "help_en": "The ObjectKey of the image to be detected in OSS. When you use ObjectKey, make sure that the key belongs to the namespace of the current caller. You must specify at least one of ImageUrl and ObjectKey",
      "help_zh": "待检测图片在 OSS 中的 `ObjectKey`。使用 `ObjectKey` 时需确保该 Key 属于当前调用者的命名空间。`ImageUrl` 与 `ObjectKey` 二选一，至少提供一个",
      "location": "query",
      "name": "object_key",
      "options": [
        "--object-key"
      ],
      "raw_name": "ObjectKey",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The URL of the image to be detected. Only HTTP and HTTPS protocols are supported. You must specify at least one of ImageUrl and ObjectKey.",
      "description_zh": "待检测图片的 URL 地址，仅支持 HTTP/HTTPS 协议。`ImageUrl` 与 `ObjectKey` 二选一，至少提供一个。\n",
      "example": "https://example.com/id-card.jpg",
      "help_en": "The URL of the image to be detected. Only HTTP and HTTPS protocols are supported. You must specify at least one of ImageUrl and ObjectKey",
      "help_zh": "待检测图片的 URL 地址，仅支持 HTTP/HTTPS 协议。`ImageUrl` 与 `ObjectKey` 二选一，至少提供一个",
      "location": "query",
      "name": "image_url",
      "options": [
        "--image-url"
      ],
      "raw_name": "ImageUrl",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The detection type. Valid values:\n- auto (default): automatic detection.\n- aigc: AIGC detection only.\n- tamper: credential tampering detection.",
      "description_zh": "检测类型。取值：`auto`（自动，默认）、`aigc`（仅 AIGC 检测）、`tamper`（凭证篡改检测）。",
      "example": "auto",
      "help_en": "The detection type. Valid values:\n- auto (default): automatic detection.\n- aigc: AIGC detection only.\n- tamper: credential tampering detection",
      "help_zh": "检测类型。取值：`auto`（自动，默认）、`aigc`（仅 AIGC 检测）、`tamper`（凭证篡改检测）",
      "location": "query",
      "name": "detect_type",
      "options": [
        "--detect-type"
      ],
      "raw_name": "DetectType",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "This parameter is deprecated.",
      "description_zh": "该参数已废弃",
      "example": "deprecated",
      "help_en": "This parameter is deprecated",
      "help_zh": "该参数已废弃",
      "location": "query",
      "name": "cred_type",
      "options": [
        "--cred-type"
      ],
      "raw_name": "CredType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "AIDeepSign",
  "protocol": "HTTPS",
  "vpc_endpoint_map": {
    "cn-hangzhou": "aideepsign-vpc.cn-hangzhou.aliyuncs.com"
  }
}
