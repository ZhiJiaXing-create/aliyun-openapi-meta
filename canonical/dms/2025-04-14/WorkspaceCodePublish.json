{
  "camel_example": "",
  "cmd_full_name": "dms workspace-code-publish",
  "cmd_name": "workspace-code-publish",
  "deprecated": false,
  "description_en": "This asynchronous API returns a key. Use this key to query the WorkspaceActionStatus API for the code deployment status",
  "description_zh": "工作空间代码部署接口。这是个异步接口，请求返回一个key，请根据key查询 WorkspaceActionStatus接口获取状态",
  "endpoint_map": {
    "ap-southeast-1": "dms.ap-southeast-1.aliyuncs.com",
    "cn-beijing": "dms.cn-beijing.aliyuncs.com",
    "cn-hangzhou": "dms.cn-hangzhou.aliyuncs.com",
    "cn-hongkong": "dms.cn-hongkong.aliyuncs.com",
    "cn-shanghai": "dms.cn-shanghai.aliyuncs.com",
    "cn-shenzhen": "dms.cn-shenzhen.aliyuncs.com",
    "us-east-1": "dms.us-east-1.aliyuncs.com",
    "us-west-1": "dms.us-west-1.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun dms workspace-code-publish --workspace-id 12345678 --config repos_:_repo_:_git@xxxx.git_,_branch_:_master_,_exclude_:_/.dms_,_/username --region cn-hangzhou",
  "method": "POST",
  "multi_version": false,
  "name": "WorkspaceCodePublish",
  "operation": {
    "action": "WorkspaceCodePublish",
    "api_style": "RPC",
    "api_version": "2025-04-14",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The workspace ID (numeric ID) for the code deployment.",
      "description_zh": "部署操作的工作空间ID（数字ID）",
      "example": "12345678",
      "help_en": "The workspace ID (numeric ID) for the code deployment",
      "help_zh": "部署操作的工作空间ID（数字ID）",
      "location": "query",
      "name": "workspace_id",
      "options": [
        "--workspace-id"
      ],
      "raw_name": "WorkspaceId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The configuration for the code deployment, specified as a JSON string. The `repos` array identifies the Git repositories in the workspace and specifies the branch to deploy. The `exclude` array lists directories to skip during the deployment.",
      "description_zh": "部署的配置，json string。 repos列表为工作空间内的git repo，主要是设置发布的branch； exclude指定哪些目录跳过",
      "example": "{\"repos\":[{\"repo\":\"git@xxxx.git\", \"branch\":\"master\"}], \"exclude\":[\"/.dms\", \"/username\"]}",
      "help_en": "The configuration for the code deployment, specified as a JSON string. The `repos` array identifies the Git repositories in the workspace and specifies the branch to deploy. The `exclude` array lists directories to skip during the deployment",
      "help_zh": "部署的配置，json string。 repos列表为工作空间内的git repo，主要是设置发布的branch； exclude指定哪些目录跳过",
      "location": "form",
      "name": "config",
      "options": [
        "--config"
      ],
      "raw_name": "Config",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Dms",
  "protocol": "HTTPS",
  "vpc_endpoint_map": {
    "ap-southeast-1": "dms-vpc.ap-southeast-1.aliyuncs.com",
    "cn-beijing": "dms-vpc.cn-beijing.aliyuncs.com",
    "cn-hangzhou": "dms-vpc.cn-hangzhou.aliyuncs.com",
    "cn-hongkong": "dms-vpc.cn-hongkong.aliyuncs.com",
    "cn-shanghai": "dms-vpc.cn-shanghai.aliyuncs.com",
    "cn-shenzhen": "dms-vpc.cn-shenzhen.aliyuncs.com",
    "us-east-1": "dms-vpc.us-east-1.aliyuncs.com",
    "us-west-1": "dms-vpc.us-west-1.aliyuncs.com"
  }
}
