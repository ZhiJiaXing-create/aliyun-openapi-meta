{
  "camel_example": "",
  "cmd_full_name": "smqproxy batch-peek-message",
  "cmd_name": "batch-peek-message",
  "deprecated": false,
  "description_zh": "批量查看队列中的消息，不改变消息状态",
  "endpoint_map": {},
  "global_endpoint": "",
  "kebab_example": "aliyun smqproxy batch-peek-message --queue-name test-queue --peekonly true --num-of-messages 16 --region cn-hangzhou",
  "method": "GET",
  "multi_version": false,
  "name": "BatchPeekMessage",
  "operation": {
    "action": "BatchPeekMessage",
    "api_style": "RPC",
    "api_version": "2026-04-09",
    "method": "GET",
    "operation_type": "read",
    "protocol": "HTTPS",
    "system_operation_type": "get",
    "url": "/queues/{queueName}/messages"
  },
  "parameters": [
    {
      "description_zh": "队列名称。",
      "example": "test-queue",
      "help_en": "The queueName",
      "help_zh": "队列名称",
      "location": "path",
      "name": "queue_name",
      "options": [
        "--queue-name"
      ],
      "raw_name": "queueName",
      "required": true,
      "type": "string"
    },
    {
      "description_zh": "表示这次请求只是去查看队列顶部的消息并不会引起消息的状态改变。取值固定为true。",
      "help_en": "The peekonly",
      "help_zh": "表示这次请求只是去查看队列顶部的消息并不会引起消息的状态改变。取值固定为true",
      "location": "query",
      "name": "peekonly",
      "options": [
        "--peekonly"
      ],
      "raw_name": "peekonly",
      "required": false,
      "type": "bool"
    },
    {
      "description_zh": "本次BatchPeekMessage最多查看消息条数。",
      "example": "16",
      "help_en": "The numOfMessages",
      "help_zh": "本次BatchPeekMessage最多查看消息条数",
      "location": "query",
      "name": "num_of_messages",
      "options": [
        "--num-of-messages"
      ],
      "raw_name": "numOfMessages",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "/queues/[queueName]/messages",
  "product_code": "SMQProxy",
  "protocol": "HTTPS",
  "vpc_endpoint_map": {}
}
