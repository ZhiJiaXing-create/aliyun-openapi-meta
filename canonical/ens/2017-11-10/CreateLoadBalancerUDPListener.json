{
  "camel_example": "aliyun ens CreateLoadBalancerUDPListener --region cn-hangzhou --ListenerPort 80 --Description example --HealthCheckInterval 3 --UnhealthyThreshold 4 --Scheduler wrr --HealthCheckConnectTimeout 100 --LoadBalancerId 'lb-5q73cv04zeyh43lh74lp40000' --BackendServerPort 8080 --HealthCheckConnectPort 8080 --HealthCheckReq hello --HealthyThreshold 4 --HealthCheckExp ok --EipTransmit on --EstablishedTimeout 500",
  "cmd_full_name": "ens create-load-balancer-udp-listener",
  "cmd_name": "create-load-balancer-udp-listener",
  "deprecated": false,
  "description_en": "To create a UDP listener, call `CreateLoadBalancerUDPListener`",
  "description_zh": "调用CreateLoadBalancerUDPListener创建UDP监听",
  "endpoint_map": {
    "ap-southeast-1": "ens.ap-southeast-1.aliyuncs.com",
    "cn-beijing": "ens.aliyuncs.com",
    "cn-chengdu": "ens.aliyuncs.com",
    "cn-hangzhou": "ens.aliyuncs.com",
    "cn-heyuan": "ens.aliyuncs.com",
    "cn-shanghai": "ens.aliyuncs.com",
    "cn-wulanchabu": "ens.aliyuncs.com",
    "cn-zhangjiakou": "ens.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun ens create-load-balancer-udp-listener --load-balancer-id lb-5q73cv04zeyh43lh74lp40000 --listener-port 80 --description example --scheduler wrr --healthy-threshold 4 --unhealthy-threshold 4 --health-check-connect-timeout 100 --health-check-interval 3 --backend-server-port 8080 --health-check-connect-port 8080 --health-check-req hello --health-check-exp ok --eip-transmit on --established-timeout 500 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateLoadBalancerUDPListener",
  "operation": {
    "action": "CreateLoadBalancerUDPListener",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "operation_type": "write",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The ID of the load balancer instance.",
      "description_zh": "负载均衡实例的ID。",
      "example": "lb-5q73cv04zeyh43lh74lp4****",
      "help_en": "The ID of the load balancer instance",
      "help_zh": "负载均衡实例的ID",
      "location": "query",
      "name": "load_balancer_id",
      "options": [
        "--load-balancer-id"
      ],
      "raw_name": "LoadBalancerId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The listener port used to receive requests and forward them to backend servers. Valid values: **1** to **65535**.\n\n> For UDP listeners, ports 250, 4789, and 4790 are system-reserved and not available for use.",
      "description_zh": "接收请求并向后端服务器进行请求转发的监听端口。取值：**1**~**65535**。\n>UDP监听的250、4789和4790三个端口为系统保留端口，暂时不对外开放。",
      "example": "80",
      "help_en": "The listener port used to receive requests and forward them to backend servers. Valid values: **1** to **65535**.\n\n> For UDP listeners, ports 250, 4789, and 4790 are system-reserved and not available for use",
      "help_zh": "接收请求并向后端服务器进行请求转发的监听端口。取值：**1**~**65535**。\n>UDP监听的250、4789和4790三个端口为系统保留端口，暂时不对外开放",
      "location": "query",
      "name": "listener_port",
      "options": [
        "--listener-port"
      ],
      "raw_name": "ListenerPort",
      "required": true,
      "type": "int"
    },
    {
      "description_en": "The name of the listener. The name must be **1** to **80** characters long.\n\n> The name cannot start with `http://` or `https://`.",
      "description_zh": "设置监听名称。长度限制为**1**~**80**个字符。\n> 不能以`http://`和`https://`开头。",
      "example": "example",
      "help_en": "The name of the listener. The name must be **1** to **80** characters long.\n\n> The name cannot start with `http://` or `https://`",
      "help_zh": "设置监听名称。长度限制为**1**~**80**个字符。\n> 不能以`http://`和`https://`开头",
      "location": "query",
      "name": "description",
      "options": [
        "--description"
      ],
      "raw_name": "Description",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The scheduling algorithm. Valid values:\n\n- **wrr** (default): Weighted Round Robin. Backend servers with a higher weight receive more requests.\n\n- **wlc**: Weighted Least Connections. Requests are distributed based on both the weight and the number of current connections of each backend server. If multiple backend servers have the same weight, the one with the fewest active connections receives the next request.\n\n- **rr**: Round Robin. Requests are distributed to backend servers sequentially.\n\n- **sch**: Source IP Hash. Requests from the same source IP address are routed to the same backend server.\n\n- **tch**: Four-tuple Hash. Requests are routed to backend servers based on a hash of four values: source IP address, destination IP address, source port, and destination port. Flows with the same four values are routed to the same backend server.\n\n- **qch**: QUIC Connection ID Hash. Requests with the same QUIC Connection ID are routed to the same backend server.\n\n- **iqch**: A consistent hash algorithm for iQUIC Connection IDs. Flows with the same second, third, and fourth bytes in the iQUIC CID are routed to the same backend server.",
      "description_zh": "调度算法。取值：\n- **wrr**（默认值）：权重值越高的后端服务器，被轮询到的次数（概率）也越高。\n- **wlc**：除了根据每台后端服务器设定的权重值来进行轮询，同时还考虑后端服务器的实际负载（即连接数）。当权重值相同时，当前连接数越小的后端服务器被轮询到的次数（概率）也越高。\n- **rr**：按照访问顺序依次将外部请求依序分发到后端服务器。\n- **sch**：基于源IP地址的一致性Hash，相同的源地址会调度到相同的后端服务器。\n- **tch**：基于四元组的一致性 hash（源 IP+目的 IP+源端口+目的端口），相同的流会调度到相同的后端服务器。\n- **qch**：基于QUIC Connection ID一致性Hash，相同的QUIC Connection ID会调度到相同的后端服务器。\n- **iqch**：针对iQUIC CID的特定三个字节进行一致Hash，第2~第4三个字节相同的会调度到相同的后端服务器。",
      "example": "wrr",
      "help_en": "The scheduling algorithm. Valid values:\n\n- **wrr** (default): Weighted Round Robin. Backend servers with a higher weight receive more requests.\n\n- **wlc**: Weighted Least Connections. Requests are distributed based on both the weight and the number of current connections of each backend server. If multiple backend servers have the same weight, the one with the fewest active connections receives the next request.\n\n- **rr**: Round Robin. Requests are distributed to backend servers sequentially.\n\n- **sch**: Source IP Hash. Requests from the same source IP address are routed to the same backend server.\n\n- **tch**: Four-tuple Hash. Requests are routed to backend servers based on a hash of four values: source IP address, destination IP address, source port, and destination port. Flows with the same four values are routed to the same backend server.\n\n- **qch**: QUIC Connection ID Hash. Requests with the same QUIC Connection ID are routed to the same backend server.\n\n- **iqch**: A consistent hash algorithm for iQUIC Connection IDs. Flows with the same second, third, and fourth bytes in the iQUIC CID are routed to the same backend server",
      "help_zh": "调度算法。取值：\n- **wrr**（默认值）：权重值越高的后端服务器，被轮询到的次数（概率）也越高。\n- **wlc**：除了根据每台后端服务器设定的权重值来进行轮询，同时还考虑后端服务器的实际负载（即连接数）。当权重值相同时，当前连接数越小的后端服务器被轮询到的次数（概率）也越高。\n- **rr**：按照访问顺序依次将外部请求依序分发到后端服务器。\n- **sch**：基于源IP地址的一致性Hash，相同的源地址会调度到相同的后端服务器。\n- **tch**：基于四元组的一致性 hash（源 IP+目的 IP+源端口+目的端口），相同的流会调度到相同的后端服务器。\n- **qch**：基于QUIC Connection ID一致性Hash，相同的QUIC Connection ID会调度到相同的后端服务器。\n- **iqch**：针对iQUIC CID的特定三个字节进行一致Hash，第2~第4三个字节相同的会调度到相同的后端服务器",
      "location": "query",
      "name": "scheduler",
      "options": [
        "--scheduler"
      ],
      "raw_name": "Scheduler",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The number of consecutive successful health checks required for a backend server to be considered healthy. Valid values: **2** to **10**. Default value: **3**.",
      "description_zh": "健康检查连续成功多少次后，将后端服务器的健康检查状态由fail（后台服务器不可达）判定为success（后台服务器可达）。取值：**2**~**10**，默认值为**3**。",
      "example": "4",
      "help_en": "The number of consecutive successful health checks required for a backend server to be considered healthy. Valid values: **2** to **10**. Default value: **3**",
      "help_zh": "健康检查连续成功多少次后，将后端服务器的健康检查状态由fail（后台服务器不可达）判定为success（后台服务器可达）。取值：**2**~**10**，默认值为**3**",
      "location": "query",
      "name": "healthy_threshold",
      "options": [
        "--healthy-threshold"
      ],
      "raw_name": "HealthyThreshold",
      "required": false,
      "type": "int"
    },
    {
      "description_en": "The number of consecutive failed health checks required for a backend server to be considered unhealthy. Valid values: **2** to **10**. Default value: **3**.",
      "description_zh": "健康检查连续失败多少次后，将后端服务器的健康检查状态由success（后台服务器可达）判定为fail（后台服务器不可达）。取值：**2**~**10**，默认值为**3**。\n",
      "example": "4",
      "help_en": "The number of consecutive failed health checks required for a backend server to be considered unhealthy. Valid values: **2** to **10**. Default value: **3**",
      "help_zh": "健康检查连续失败多少次后，将后端服务器的健康检查状态由success（后台服务器可达）判定为fail（后台服务器不可达）。取值：**2**~**10**，默认值为**3**",
      "location": "query",
      "name": "unhealthy_threshold",
      "options": [
        "--unhealthy-threshold"
      ],
      "raw_name": "UnhealthyThreshold",
      "required": false,
      "type": "int"
    },
    {
      "description_en": "The maximum time to wait for a response to a health check. A health check fails if a backend ENS instance does not respond within this time.\n\n- Default value: 5.\n\n- Valid values: **1** to **300**.\n\n- Unit: seconds.\n\n> If this timeout is shorter than the health check interval (`HealthCheckInterval`), the interval value is used for the timeout instead.",
      "description_zh": "接收来自运行状况检查的响应需要等待的时间。如果后端ENS在指定的时间内没有响应，则判定为健康检查失败。\n- 默认值：5秒。\n- 取值：**1**~**300**。\n- 单位：秒。\n\n>如果HealthCheckConnectTimeout的值小于HealthCheckInterval的值，则HealthCheckConnectTimeout无效，超时时间为HealthCheckInterval的值。",
      "example": "100",
      "help_en": "The maximum time to wait for a response to a health check. A health check fails if a backend ENS instance does not respond within this time.\n\n- Default value: 5.\n\n- Valid values: **1** to **300**.\n\n- Unit: seconds.\n\n> If this timeout is shorter than the health check interval (`HealthCheckInterval`), the interval value is used for the timeout instead",
      "help_zh": "接收来自运行状况检查的响应需要等待的时间。如果后端ENS在指定的时间内没有响应，则判定为健康检查失败。\n- 默认值：5秒。\n- 取值：**1**~**300**。\n- 单位：秒。\n\n>如果HealthCheckConnectTimeout的值小于HealthCheckInterval的值，则HealthCheckConnectTimeout无效，超时时间为HealthCheckInterval的值",
      "location": "query",
      "name": "health_check_connect_timeout",
      "options": [
        "--health-check-connect-timeout"
      ],
      "raw_name": "HealthCheckConnectTimeout",
      "required": false,
      "type": "int"
    },
    {
      "description_en": "The interval between health checks, in seconds. Valid values: **1** to **50**. Default value: **2**.",
      "description_zh": "健康检查的时间间隔。取值：**1**~**50**，默认值为**2**，单位：秒。\n",
      "example": "3",
      "help_en": "The interval between health checks, in seconds. Valid values: **1** to **50**. Default value: **2**",
      "help_zh": "健康检查的时间间隔。取值：**1**~**50**，默认值为**2**，单位：秒",
      "location": "query",
      "name": "health_check_interval",
      "options": [
        "--health-check-interval"
      ],
      "raw_name": "HealthCheckInterval",
      "required": false,
      "type": "int"
    },
    {
      "description_en": "The port used by the backend servers. Valid values: **1** to **65535**.",
      "description_zh": "负载均衡实例后端服务器使用的端口，取值：**1**~**65535**。",
      "example": "8080",
      "help_en": "The port used by the backend servers. Valid values: **1** to **65535**",
      "help_zh": "负载均衡实例后端服务器使用的端口，取值：**1**~**65535**",
      "location": "query",
      "name": "backend_server_port",
      "options": [
        "--backend-server-port"
      ],
      "raw_name": "BackendServerPort",
      "required": false,
      "type": "int"
    },
    {
      "description_en": "The port used for health checks. Valid values: **1** to **65535**. If you do not specify this parameter, the port specified by `BackendServerPort` is used.",
      "description_zh": "健康检查使用的端口，取值：**1**~**65535**。不设置此参数时，表示使用后端服务端口（BackendServerPort）。",
      "example": "8080",
      "help_en": "The port used for health checks. Valid values: **1** to **65535**. If you do not specify this parameter, the port specified by `BackendServerPort` is used",
      "help_zh": "健康检查使用的端口，取值：**1**~**65535**。不设置此参数时，表示使用后端服务端口（BackendServerPort）",
      "location": "query",
      "name": "health_check_connect_port",
      "options": [
        "--health-check-connect-port"
      ],
      "raw_name": "HealthCheckConnectPort",
      "required": false,
      "type": "int"
    },
    {
      "description_en": "The request string for UDP health checks. The string must be up to 64 characters long and contain only letters and digits.",
      "description_zh": "UDP监听健康检查的请求串，只允许包含字母、数字，最大长度限制为64个字符。",
      "example": "hello",
      "help_en": "The request string for UDP health checks. The string must be up to 64 characters long and contain only letters and digits",
      "help_zh": "UDP监听健康检查的请求串，只允许包含字母、数字，最大长度限制为64个字符",
      "location": "query",
      "name": "health_check_req",
      "options": [
        "--health-check-req"
      ],
      "raw_name": "HealthCheckReq",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The expected response string for UDP health checks. The string must be up to 64 characters long and contain only letters and digits.",
      "description_zh": "UDP监听健康检查的响应串，只允许包含字母、数字，最大长度限制为64个字符。",
      "example": "ok",
      "help_en": "The expected response string for UDP health checks. The string must be up to 64 characters long and contain only letters and digits",
      "help_zh": "UDP监听健康检查的响应串，只允许包含字母、数字，最大长度限制为64个字符",
      "location": "query",
      "name": "health_check_exp",
      "options": [
        "--health-check-exp"
      ],
      "raw_name": "HealthCheckExp",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "Specifies whether EIP pass-through is enabled. Valid values:\n\n- **on**: Enables EIP pass-through.\n\n- **off** (default): Disables EIP pass-through.",
      "description_zh": "是否开启EIP透传，取值：\n- **on**：开启。\n- **off**（默认值）：关闭。",
      "example": "on",
      "help_en": "Specifies whether EIP pass-through is enabled. Valid values:\n\n- **on**: Enables EIP pass-through.\n\n- **off** (default): Disables EIP pass-through",
      "help_zh": "是否开启EIP透传，取值：\n- **on**：开启。\n- **off**（默认值）：关闭",
      "location": "query",
      "name": "eip_transmit",
      "options": [
        "--eip-transmit"
      ],
      "raw_name": "EipTransmit",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The timeout for established connections. Valid values: **10** to **900**. Default value: **900**. Unit: seconds.",
      "description_zh": "连接超时时间，取值：**10**~**900**，默认值为**900**，单位：秒。",
      "example": "500",
      "help_en": "The timeout for established connections. Valid values: **10** to **900**. Default value: **900**. Unit: seconds",
      "help_zh": "连接超时时间，取值：**10**~**900**，默认值为**900**，单位：秒",
      "location": "query",
      "name": "established_timeout",
      "options": [
        "--established-timeout"
      ],
      "raw_name": "EstablishedTimeout",
      "required": false,
      "type": "int"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "ap-southeast-1": "ens-vpc.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "ens.vpc-proxy.aliyuncs.com",
    "cn-zhangjiakou": "ens-vpc.cn-zhangjiakou.aliyuncs.com"
  }
}
