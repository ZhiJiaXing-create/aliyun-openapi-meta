{
  "camel_example": "aliyun ens RenewARMServerInstance --region cn-hangzhou --InstanceId yourInstance_ID --AutoRenew true --Period 1 --PeriodUnit Month",
  "cmd_full_name": "ens renew-arm-server-instance",
  "cmd_name": "renew-arm-server-instance",
  "deprecated": false,
  "description_en": "Renews a subscription Android in Container (AIC) instance",
  "description_zh": "调用RenewARMServerInstance续费一台包年包月AIC实例",
  "endpoint_map": {
    "ap-southeast-1": "ens.ap-southeast-1.aliyuncs.com",
    "cn-beijing": "ens.aliyuncs.com",
    "cn-chengdu": "ens.aliyuncs.com",
    "cn-hangzhou": "ens.aliyuncs.com",
    "cn-heyuan": "ens.aliyuncs.com",
    "cn-shanghai": "ens.aliyuncs.com",
    "cn-wulanchabu": "ens.aliyuncs.com",
    "cn-zhangjiakou": "ens.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun ens renew-arm-server-instance --instance-id yourInstance_ID --period 1 --period-unit Month --auto-renew true --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "RenewARMServerInstance",
  "operation": {
    "action": "RenewARMServerInstance",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The ID of the instance that you want to renew.",
      "description_zh": "待续费的服务器实例ID。",
      "example": "yourInstance ID",
      "help_en": "The ID of the instance that you want to renew",
      "help_zh": "待续费的服务器实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The renewal period. By default, instances are renewed on a monthly basis. Valid values: 1, 2, 3, 4, 5, 6, 7, 8, 9, and 12.",
      "description_zh": "续费周期，默认按月续费。取值：1，2，3，4，5，6，7，8，9，12。",
      "example": "1",
      "help_en": "The renewal period. By default, instances are renewed on a monthly basis. Valid values: 1, 2, 3, 4, 5, 6, 7, 8, 9, and 12",
      "help_zh": "续费周期，默认按月续费。取值：1，2，3，4，5，6，7，8，9，12",
      "location": "query",
      "name": "period",
      "options": [
        "--period"
      ],
      "raw_name": "Period",
      "required": true,
      "type": "int"
    },
    {
      "description_en": "The unit of the renewal period. Valid values:\n\n- Month (default)\n\n- Year",
      "description_zh": "续费时长的时间单位。取值：\n- Month（默认）\n- Year",
      "example": "Month",
      "help_en": "The unit of the renewal period. Valid values:\n\n- Month (default)\n\n- Year",
      "help_zh": "续费时长的时间单位。取值：\n- Month（默认）\n- Year",
      "location": "query",
      "name": "period_unit",
      "options": [
        "--period-unit"
      ],
      "raw_name": "PeriodUnit",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "Specifies whether to enable auto-renewal for the premium bandwidth plan. Valid values:\n\n- **true**.\n\n- **false** (default).",
      "description_zh": "是否自动续费。取值：\n\n- **true**：自动续费\n- **false**：不自动续费（默认）",
      "example": "true",
      "help_en": "Specifies whether to enable auto-renewal for the premium bandwidth plan. Valid values:\n\n- **true**.\n\n- **false** (default)",
      "help_zh": "是否自动续费。取值：\n\n- **true**：自动续费\n- **false**：不自动续费（默认）",
      "location": "query",
      "name": "auto_renew",
      "options": [
        "--auto-renew"
      ],
      "raw_name": "AutoRenew",
      "required": false,
      "type": "bool"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "ap-southeast-1": "ens-vpc.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "ens.vpc-proxy.aliyuncs.com",
    "cn-zhangjiakou": "ens-vpc.cn-zhangjiakou.aliyuncs.com"
  }
}
