{
  "camel_example": "aliyun ens AttachDisk --region cn-hangzhou --InstanceId 'i-5rr19av7tkpgi9os52ag10000' --DeleteWithInstance false --DiskId 'd-5saf13yy6sopmmg88mzsg0000'",
  "cmd_full_name": "ens attach-disk",
  "cmd_name": "attach-disk",
  "deprecated": false,
  "description_en": "Attaches a disk to an Edge Node Service (ENS) instance",
  "description_zh": "调用AttachDisk为ENS实例挂载数据盘",
  "endpoint_map": {
    "ap-southeast-1": "ens.ap-southeast-1.aliyuncs.com",
    "cn-beijing": "ens.aliyuncs.com",
    "cn-chengdu": "ens.aliyuncs.com",
    "cn-hangzhou": "ens.aliyuncs.com",
    "cn-heyuan": "ens.aliyuncs.com",
    "cn-shanghai": "ens.aliyuncs.com",
    "cn-wulanchabu": "ens.aliyuncs.com",
    "cn-zhangjiakou": "ens.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun ens attach-disk --disk-id d-5saf13yy6sopmmg88mzsg0000 --instance-id i-5rr19av7tkpgi9os52ag10000 --delete-with-instance false --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "AttachDisk",
  "operation": {
    "action": "AttachDisk",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "update",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The ID of the disk to be attached. The cloud disk and the instance must belong to the same node.",
      "description_zh": "待挂载的云盘ID。云盘（DiskId）和实例（InstanceId）必须在同一个节点。",
      "example": "d-5saf13yy6sopmmg88mzsg****",
      "help_en": "The ID of the disk to be attached. The cloud disk and the instance must belong to the same node",
      "help_zh": "待挂载的云盘ID。云盘（DiskId）和实例（InstanceId）必须在同一个节点",
      "location": "query",
      "name": "disk_id",
      "options": [
        "--disk-id"
      ],
      "raw_name": "DiskId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The ID of the instance.",
      "description_zh": "实例ID。",
      "example": "i-5rr19av7tkpgi9os52ag1****",
      "help_en": "The ID of the instance",
      "help_zh": "实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "Specifies whether the disk to be attached is released with the instance. Valid values:\n\n- true: The disk will be released when the ECS instance is released.\n\n- false: The disk will be retained when the ECS instance is released.\n\n- If you leave this parameter empty, the default value is used.",
      "description_zh": "待挂载的云盘是否随实例释放。取值：\n\n- true：释放实例时，该云盘随实例一起释放。\n- false ：释放实例时，保留该云盘，不随实例一起释放。\n- 为空则表示默认为false。",
      "example": "false",
      "help_en": "Specifies whether the disk to be attached is released with the instance. Valid values:\n\n- true: The disk will be released when the ECS instance is released.\n\n- false: The disk will be retained when the ECS instance is released.\n\n- If you leave this parameter empty, the default value is used",
      "help_zh": "待挂载的云盘是否随实例释放。取值：\n\n- true：释放实例时，该云盘随实例一起释放。\n- false ：释放实例时，保留该云盘，不随实例一起释放。\n- 为空则表示默认为false",
      "location": "query",
      "name": "delete_with_instance",
      "options": [
        "--delete-with-instance"
      ],
      "raw_name": "DeleteWithInstance",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "ap-southeast-1": "ens-vpc.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "ens.vpc-proxy.aliyuncs.com",
    "cn-zhangjiakou": "ens-vpc.cn-zhangjiakou.aliyuncs.com"
  }
}
