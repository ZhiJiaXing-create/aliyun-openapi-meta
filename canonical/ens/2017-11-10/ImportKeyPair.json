{
  "camel_example": "aliyun ens ImportKeyPair --region cn-hangzhou --KeyPairName TestKeyPairName --PublicKeyBody 'ssh-rsa_AAAAB0000'",
  "cmd_full_name": "ens import-key-pair",
  "cmd_name": "import-key-pair",
  "deprecated": false,
  "description_en": "Imports the public key of a Rivest–Shamir–Adleman (RSA)-encrypted key pair that is generated by a third-party tool",
  "description_zh": "调用ImportKeyPair导入由其他工具产生的RSA密钥对的公钥部分",
  "endpoint_map": {
    "ap-southeast-1": "ens.ap-southeast-1.aliyuncs.com",
    "cn-beijing": "ens.aliyuncs.com",
    "cn-chengdu": "ens.aliyuncs.com",
    "cn-hangzhou": "ens.aliyuncs.com",
    "cn-heyuan": "ens.aliyuncs.com",
    "cn-shanghai": "ens.aliyuncs.com",
    "cn-wulanchabu": "ens.aliyuncs.com",
    "cn-zhangjiakou": "ens.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun ens import-key-pair --biz-key-pair-name TestKeyPairName --public-key-body ssh-rsa_AAAAB0000 --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "ImportKeyPair",
  "operation": {
    "action": "ImportKeyPair",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The name of the key pair. The name must conform to the following naming conventions:\n\n- The name must be 2 to 128 characters in length.\n\n- The name must start with a letter but cannot start with `http://` or `https://`.\n\n- The name can contain letters, digits, colons (:), underscores (\\_), and hyphens (-).\n\nYou can specify the name of only one key pair.",
      "description_zh": "密钥对名称。命名规则如下所示：\n\n- 长度为2~128个英文或中文字符。\n- 必须以大小字母或中文开头，不能以`http://`和`https://`开头。\n- 可以包含数字、半角冒号（:）、下划线（_）、或者连字符（-）。\n\n仅支持传单个密钥对。",
      "example": "TestKeyPairName",
      "help_en": "The name of the key pair. The name must conform to the following naming conventions:\n\n- The name must be 2 to 128 characters in length.\n\n- The name must start with a letter but cannot start with `http://` or `https://`.\n\n- The name can contain letters, digits, colons (:), underscores (\\_), and hyphens (-).\n\nYou can specify the name of only one key pair",
      "help_zh": "密钥对名称。命名规则如下所示：\n\n- 长度为2~128个英文或中文字符。\n- 必须以大小字母或中文开头，不能以`http://`和`https://`开头。\n- 可以包含数字、半角冒号（:）、下划线（_）、或者连字符（-）。\n\n仅支持传单个密钥对",
      "location": "query",
      "name": "key_pair_name",
      "options": [
        "--biz-key-pair-name"
      ],
      "raw_name": "KeyPairName",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The public key of the key pair. You can specify only one public key.",
      "description_zh": "密钥对的公钥内容。仅支持传单个内容。",
      "example": "ssh-rsa AAAAB****",
      "help_en": "The public key of the key pair. You can specify only one public key",
      "help_zh": "密钥对的公钥内容。仅支持传单个内容",
      "location": "query",
      "name": "public_key_body",
      "options": [
        "--public-key-body"
      ],
      "raw_name": "PublicKeyBody",
      "required": true,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "ap-southeast-1": "ens-vpc.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "ens.vpc-proxy.aliyuncs.com",
    "cn-zhangjiakou": "ens-vpc.cn-zhangjiakou.aliyuncs.com"
  }
}
