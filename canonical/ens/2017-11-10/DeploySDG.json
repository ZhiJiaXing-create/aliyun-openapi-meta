{
  "camel_example": "aliyun ens DeploySDG --region cn-hangzhou --SDGId 'sdg-xxxxx' --DeploymentType common --InstanceIds '[\"aic-1\"]'",
  "cmd_full_name": "ens deploy-sdg",
  "cmd_name": "deploy-sdg",
  "deprecated": false,
  "description_en": "Deploys shared data groups (SDGs)",
  "description_zh": "调用DeploySDG部署SDG",
  "endpoint_map": {
    "ap-southeast-1": "ens.ap-southeast-1.aliyuncs.com",
    "cn-beijing": "ens.aliyuncs.com",
    "cn-chengdu": "ens.aliyuncs.com",
    "cn-hangzhou": "ens.aliyuncs.com",
    "cn-heyuan": "ens.aliyuncs.com",
    "cn-shanghai": "ens.aliyuncs.com",
    "cn-wulanchabu": "ens.aliyuncs.com",
    "cn-zhangjiakou": "ens.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun ens deploy-sdg --instance-ids aic-1 --sdg-id sdg-xxxxx --deployment-type common --region cn-hangzhou",
  "method": "GET",
  "multi_version": false,
  "name": "DeploySDG",
  "operation": {
    "action": "DeploySDG",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "GET",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The IDs of instances on which you want to deploy SDGs. You can deploy SDGs on a maximum of 100 instances at a time.",
      "description_zh": "要部署SDG的设备资源ID列表。支持批量部署，最多一次部署100个Instance。",
      "element_type": "string",
      "help_en": "The IDs of instances on which you want to deploy SDGs. You can deploy SDGs on a maximum of 100 instances at a time",
      "help_zh": "要部署SDG的设备资源ID列表。支持批量部署，最多一次部署100个Instance",
      "location": "query",
      "name": "instance_ids",
      "options": [
        "--instance-ids"
      ],
      "param_style": "json",
      "raw_name": "InstanceIds",
      "required": true,
      "type": "array"
    },
    {
      "description_en": "The SDG ID. This parameter is used to create a disk, which is attached to an instance.",
      "description_zh": "使用SDGId创建云盘，并挂载到InstanceId。",
      "example": "sdg-xxxxx",
      "help_en": "The SDG ID. This parameter is used to create a disk, which is attached to an instance",
      "help_zh": "使用SDGId创建云盘，并挂载到InstanceId",
      "location": "query",
      "name": "sdg_id",
      "options": [
        "--sdg-id"
      ],
      "raw_name": "SDGId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The SDG deployment type. Valid values:\n\n- common (default): read/write deployment. Data updates are written to disks.\n\n- overlay: read/write splitting deployment. Content in SDGs is read-only. Data updates are written to the local storage of the instance.",
      "description_zh": "SDG部署类型，包括：\n\n- common：（默认）可读写部署，数据更新内容会写入云盘。\n- overlay：读写分离部署，SDG内容只读，数据更新内容写入实例本地。",
      "example": "common",
      "help_en": "The SDG deployment type. Valid values:\n\n- common (default): read/write deployment. Data updates are written to disks.\n\n- overlay: read/write splitting deployment. Content in SDGs is read-only. Data updates are written to the local storage of the instance",
      "help_zh": "SDG部署类型，包括：\n\n- common：（默认）可读写部署，数据更新内容会写入云盘。\n- overlay：读写分离部署，SDG内容只读，数据更新内容写入实例本地",
      "location": "query",
      "name": "deployment_type",
      "options": [
        "--deployment-type"
      ],
      "raw_name": "DeploymentType",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "ap-southeast-1": "ens-vpc.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "ens.vpc-proxy.aliyuncs.com",
    "cn-zhangjiakou": "ens-vpc.cn-zhangjiakou.aliyuncs.com"
  }
}
