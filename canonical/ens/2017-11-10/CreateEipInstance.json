{
  "camel_example": "aliyun ens CreateEipInstance --region cn-hangzhou --Tag.1.Key team --Description yourDescription --InstanceId 'example-string' --InstanceChargeType PostPaid --Bandwidth 5 --Isp cmcc --Tag.1.Value tagValue --IpAddress 'example-string' --EnsRegionId 'cn-suzhou-telecom' --ClientToken '26C28756-2586-17AF-B802-0DC50D8FDEBB' --InternetChargeType 95BandwidthByMonth --Name EIP1",
  "cmd_full_name": "ens create-eip-instance",
  "cmd_name": "create-eip-instance",
  "deprecated": false,
  "description_en": "Allocates an Elastic IP Address (EIP) to your account",
  "description_zh": "用CreateEipInstance接口申请弹性公网IP",
  "endpoint_map": {
    "ap-southeast-1": "ens.ap-southeast-1.aliyuncs.com",
    "cn-beijing": "ens.aliyuncs.com",
    "cn-chengdu": "ens.aliyuncs.com",
    "cn-hangzhou": "ens.aliyuncs.com",
    "cn-heyuan": "ens.aliyuncs.com",
    "cn-shanghai": "ens.aliyuncs.com",
    "cn-wulanchabu": "ens.aliyuncs.com",
    "cn-zhangjiakou": "ens.aliyuncs.com"
  },
  "global_endpoint": "",
  "kebab_example": "aliyun ens create-eip-instance --ens-region-id cn-suzhou-telecom --bandwidth 5 --instance-charge-type PostPaid --internet-charge-type 95BandwidthByMonth --name EIP1 --isp cmcc --description yourDescription --tag Value=tagValue Key=team --client-token 26C28756-2586-17AF-B802-0DC50D8FDEBB --ip-address example-string --instance-id example-string --region cn-hangzhou",
  "method": "GET|POST",
  "multi_version": false,
  "name": "CreateEipInstance",
  "operation": {
    "action": "CreateEipInstance",
    "api_style": "RPC",
    "api_version": "2017-11-10",
    "method": "POST",
    "operation_type": "readAndWrite",
    "protocol": "HTTPS",
    "system_operation_type": "create",
    "url": ""
  },
  "parameters": [
    {
      "description_en": "The ID of the edge node.",
      "description_zh": "ENS节点ID。",
      "example": "cn-suzhou-telecom",
      "help_en": "The ID of the edge node",
      "help_zh": "ENS节点ID",
      "location": "query",
      "name": "ens_region_id",
      "options": [
        "--ens-region-id"
      ],
      "raw_name": "EnsRegionId",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The peak bandwidth of the EIP. Default value: 5. Unit: Mbit/s. Valid values: 5 to 10000.",
      "description_zh": "EIP的带宽峰值，默认值为5。取值范围：5~10000，单位：Mbps。",
      "example": "5",
      "help_en": "The peak bandwidth of the EIP. Default value: 5. Unit: Mbit/s. Valid values: 5 to 10000",
      "help_zh": "EIP的带宽峰值，默认值为5。取值范围：5~10000，单位：Mbps",
      "location": "query",
      "name": "bandwidth",
      "options": [
        "--bandwidth"
      ],
      "raw_name": "Bandwidth",
      "required": false,
      "type": "int"
    },
    {
      "description_en": "The billing method of the EIP. The value is set to **PostPaid**, which is the pay-as-you-go billing method.",
      "description_zh": "EIP的计费方式，当前取值仅支持**PostPaid**（月按量计费）。\n\n",
      "example": "PostPaid",
      "help_en": "The billing method of the EIP. The value is set to **PostPaid**, which is the pay-as-you-go billing method",
      "help_zh": "EIP的计费方式，当前取值仅支持**PostPaid**（月按量计费）",
      "location": "query",
      "name": "instance_charge_type",
      "options": [
        "--instance-charge-type"
      ],
      "raw_name": "InstanceChargeType",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The metering method of the EIP. The value is set to **95BandwidthByMonth**, which indicates the monthly 95th percentile bandwidth metering method.",
      "description_zh": "EIP的计费方式，当前取值仅支持**95BandwidthByMonth**（月95带宽计费）。\n",
      "example": "95BandwidthByMonth",
      "help_en": "The metering method of the EIP. The value is set to **95BandwidthByMonth**, which indicates the monthly 95th percentile bandwidth metering method",
      "help_zh": "EIP的计费方式，当前取值仅支持**95BandwidthByMonth**（月95带宽计费）",
      "location": "query",
      "name": "internet_charge_type",
      "options": [
        "--internet-charge-type"
      ],
      "raw_name": "InternetChargeType",
      "required": true,
      "type": "string"
    },
    {
      "description_en": "The name of the EIP instance.",
      "description_zh": "EIP实例名称。",
      "example": "EIP1",
      "help_en": "The name of the EIP instance",
      "help_zh": "EIP实例名称",
      "location": "query",
      "name": "name",
      "options": [
        "--name"
      ],
      "raw_name": "Name",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The ISP. Valid values:\n\n- **cmcc**: China Mobile\n\n- **unicom**: China Unicom\n\n- **telecom**: China Telecom",
      "description_zh": "运营商信息，取值：\n\n- **cmcc**：中国移动。\n- **unicom**：中国联通。\n- **telecom**：中国电信。",
      "example": "cmcc",
      "help_en": "The ISP. Valid values:\n\n- **cmcc**: China Mobile\n\n- **unicom**: China Unicom\n\n- **telecom**: China Telecom",
      "help_zh": "运营商信息，取值：\n\n- **cmcc**：中国移动。\n- **unicom**：中国联通。\n- **telecom**：中国电信",
      "location": "query",
      "name": "isp",
      "options": [
        "--isp"
      ],
      "raw_name": "Isp",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The description.",
      "description_zh": "描述。",
      "example": "yourDescription",
      "help_en": "The description",
      "help_zh": "描述",
      "location": "query",
      "name": "description",
      "options": [
        "--description"
      ],
      "raw_name": "Description",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The resource tags.",
      "description_zh": "资源标签",
      "element_fields": [
        {
          "description_en": "The key of tag N that is added to the instance. Valid values of N: **1** to **20**.\n\n- The tag key cannot start with `aliyun`, `acs:`, `http://`, or `https://`.\n\n- The tag key can be up to 64 characters in length.\n\n- The tag key cannot be an empty string.",
          "description_zh": "实例的标签键。N的取值范围为**1**~**20**。\n\n- 不能以`aliyun`、`acs:`、`http://`或`https://`开头。\n- 最多支持64个字符。\n- 不允许传入空字符串。",
          "example": "team",
          "help_en": "The key of tag N that is added to the instance. Valid values of N: **1** to **20**.\n\n- The tag key cannot start with `aliyun`, `acs:`, `http://`, or `https://`.\n\n- The tag key can be up to 64 characters in length.\n\n- The tag key cannot be an empty string",
          "help_zh": "实例的标签键。N的取值范围为**1**~**20**。\n\n- 不能以`aliyun`、`acs:`、`http://`或`https://`开头。\n- 最多支持64个字符。\n- 不允许传入空字符串",
          "name": "key",
          "options": [
            "--key"
          ],
          "raw_name": "Key",
          "required": false,
          "type": "string"
        },
        {
          "description_en": "The tag value. The value can be up to 128 characters in length. It cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.",
          "description_zh": "标签值。不可超过128个字符，不能以`aliyun`或`acs:`开头，不能包含`http://`或者`https://`。",
          "example": "tagValue",
          "help_en": "The tag value. The value can be up to 128 characters in length. It cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`",
          "help_zh": "标签值。不可超过128个字符，不能以`aliyun`或`acs:`开头，不能包含`http://`或者`https://`",
          "name": "value",
          "options": [
            "--value"
          ],
          "raw_name": "Value",
          "required": false,
          "type": "string"
        }
      ],
      "element_type": "object",
      "help_en": "The resource tags",
      "help_zh": "资源标签",
      "location": "query",
      "name": "tag",
      "options": [
        "--tag"
      ],
      "param_style": "repeatList",
      "raw_name": "Tag",
      "required": false,
      "type": "array"
    },
    {
      "description_en": "A client-generated token that ensures the idempotence of the request. This prevents duplicate operations if you retry the request.\n\n- You can generate a unique, case-sensitive string that contains only ASCII characters and is no more than 64 characters in length.\n\n- If you use a previously used client token with the same request parameters, the server returns the same result as the original request without changing its state.\n\n- You can safely retry requests that time out or return a `PROCESSING` error code. Idempotence is guaranteed if the original request was successful (HTTP status code `200`). However, it is not guaranteed if the original request failed with an HTTP `4xx` error code other than `PROCESSING`.\n\n- The token is valid for 10 minutes.",
      "description_zh": "保证请求幂等性，避免多次重试带来重复操作的问题。\n\n- 从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持 ASCII 字符，且不能超过 64 个字符 。\n\n- 当您使用了一个已经使用过的ClientToken，其他请求参数无变化，那么重复提交该请求后，客户端将收到与首次提交相同的结果，对您的服务端状态没有影响\n\n- 在接口超时或者错误码为PROCESSING的时候可以发起重试，幂等有效。返回结果是200 HTTPCode时，重试后客户端可以得到与上次相同的结果，但对您的服务端状态没有影响；返回结果是4xx HTTPCode时，且错误码不是PROCESSING则幂等无效。\n\n- ClientToken有效期为10分钟。",
      "example": "26C28756-2586-17AF-B802-0DC50D8FDEBB",
      "help_en": "A client-generated token that ensures the idempotence of the request. This prevents duplicate operations if you retry the request.\n\n- You can generate a unique, case-sensitive string that contains only ASCII characters and is no more than 64 characters in length.\n\n- If you use a previously used client token with the same request parameters, the server returns the same result as the original request without changing its state.\n\n- You can safely retry requests that time out or return a `PROCESSING` error code. Idempotence is guaranteed if the original request was successful (HTTP status code `200`). However, it is not guaranteed if the original request failed with an HTTP `4xx` error code other than `PROCESSING`.\n\n- The token is valid for 10 minutes",
      "help_zh": "保证请求幂等性，避免多次重试带来重复操作的问题。\n\n- 从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持 ASCII 字符，且不能超过 64 个字符 。\n\n- 当您使用了一个已经使用过的ClientToken，其他请求参数无变化，那么重复提交该请求后，客户端将收到与首次提交相同的结果，对您的服务端状态没有影响\n\n- 在接口超时或者错误码为PROCESSING的时候可以发起重试，幂等有效。返回结果是200 HTTPCode时，重试后客户端可以得到与上次相同的结果，但对您的服务端状态没有影响；返回结果是4xx HTTPCode时，且错误码不是PROCESSING则幂等无效。\n\n- ClientToken有效期为10分钟",
      "location": "query",
      "name": "client_token",
      "options": [
        "--client-token"
      ],
      "raw_name": "ClientToken",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The IP address. If you do not specify this parameter, the system automatically allocates an available IP address.",
      "description_zh": "IP地址",
      "help_en": "The IP address. If you do not specify this parameter, the system automatically allocates an available IP address",
      "help_zh": "IP地址",
      "location": "query",
      "name": "ip_address",
      "options": [
        "--ip-address"
      ],
      "raw_name": "IpAddress",
      "required": false,
      "type": "string"
    },
    {
      "description_en": "The ID of the EIP instance.",
      "description_zh": "EIP的实例ID。",
      "help_en": "The ID of the EIP instance",
      "help_zh": "EIP的实例ID",
      "location": "query",
      "name": "instance_id",
      "options": [
        "--instance-id"
      ],
      "raw_name": "InstanceId",
      "required": false,
      "type": "string"
    }
  ],
  "pathPattern": "",
  "product_code": "Ens",
  "protocol": "HTTP|HTTPS",
  "vpc_endpoint_map": {
    "ap-southeast-1": "ens-vpc.ap-southeast-1.aliyuncs.com",
    "cn-shanghai": "ens.vpc-proxy.aliyuncs.com",
    "cn-zhangjiakou": "ens-vpc.cn-zhangjiakou.aliyuncs.com"
  }
}
