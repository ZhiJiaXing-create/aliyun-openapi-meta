{
  "name": "DescribeStructureImportTaskInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例所在地域。",
        "en": "The region in which the instance resides."
      }
    },
    {
      "name": "SlinkTaskId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目标任务id。",
        "en": "The ID of the destination task."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询数据导入任务信息",
    "en": "Query data import task information"
  },
  "descriptions": {
    "zh": "CreateDataImportTask 接口用于创建数据导入任务，支持将存储在OSS、ECS或直接传入的SQL/CSV文件导入到目标数据库实例中。通过指定实例ID、数据库名、引擎类型、数据来源（如OSS路径）及导入类型，系统将异步或同步执行数据写入操作，适用于数据迁移、初始化和补录等场景，返回任务ID以便后续状态查询与管理。",
    "en": "The CreateDataImportTask operation creates a data import task. You can use this operation to import SQL or CSV files stored in OSS or ECS, or directly provided, into a destination database instance. Specify the instance ID, database name, engine type, data source (such as an OSS path), and import type. The system performs the data write operation asynchronously or synchronously. This operation is applicable to scenarios such as data migration, initialization, and data backfill. A task ID is returned for subsequent status queries and management."
  },
  "example": {
    "unifiedCli": "aliyun polardbx describe-structure-import-task-info --biz-region-id cn-hangzhou --slink-task-id etx-szr2rr6i00000 --region cn-hangzhou",
    "legacyCli": "aliyun polardbx DescribeStructureImportTaskInfo --region cn-hangzhou --RegionId 'cn-hangzhou' --SlinkTaskId 'etx-szr2rr6i00000'"
  }
}