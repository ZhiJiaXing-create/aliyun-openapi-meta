{
  "name": "ListAppPoliciesForIdentity",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用ID。 默认取值：**app-1000000**。使用说明，请参见[多应用](~~113600~~)。",
        "en": "The ID of the application. Default value: **app-1000000**. For more information, see [Overview](~~113600~~)."
      }
    },
    {
      "name": "IdentityName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "身份名称。\n\n- 类型为RamUser时，传入RAM用户ID\n- 类型为RamRole时，传入角色名称。",
        "en": "The name of the identity.\n\n*   Specifies the ID of the RAM user when the IdentityType parameter is set to RamUser.\n*   Specifies the name of the RAM role when the IdentityType parameter is set to RamRole."
      }
    },
    {
      "name": "IdentityType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "身份类型。取值：\n- **RamUser**（RAM用户）\n- **RamRole**（RAM角色）",
        "en": "The type of the identity. Valid values:\n\n*   **RamUser**: a RAM user.\n*   **RamRole**: a RAM role."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询身份实体被授予的应用权限列表",
    "en": "ListAppPoliciesForIdentity"
  },
  "descriptions": {
    "zh": "- 只有拥有管理员权限的账号身份调用该接口时，**IdentityType**和**IdentityName**有效；否则只返回当前账号身份被授予的应用权限策略。",
    "en": "> The IdentityType and IdentityName parameters take effect only when an identity assumes the application administrator role to call this operation. Otherwise, only application policies that are attached to the current identity are returned."
  },
  "example": {
    "unifiedCli": "aliyun vod list-app-policies-for-identity --identity-type RamUser --identity-name test0000name --app-id app-0000 --region cn-hangzhou",
    "legacyCli": "aliyun vod ListAppPoliciesForIdentity --region cn-hangzhou --IdentityType RamUser --AppId 'app-0000' --IdentityName test0000name"
  }
}