{
  "name": "ListAppInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNo",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码。默认值为**1**。",
        "en": "The page number. The default value is **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页返回的数据条数。默认值为**10**，最大值为**100**。",
        "en": "The number of entries to return on each page. The default value is **10**. The maximum value is **100**."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用状态，可查询指定状态的应用。应用创建后默认为**Normal**，取值范围：\n- **Normal**（正常）\n- **Disable**（停用）",
        "en": "The status of the application. You can query applications in a specific status. After an application is created, its status is **Normal** by default. Valid values:\n\n- **Normal**\n\n- **Disable**"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取应用信息列表",
    "en": "ListAppInfo"
  },
  "descriptions": {
    "zh": "### 使用说明\n支持按应用状态过滤查询。\n### QPS限制\n本接口的单用户QPS限制为30次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。更多信息，请参见[QPS限制](~~342790~~)。",
    "en": "### Usage notes\n\nYou can filter the query by application status.\n\n### QPS limits\n\nThe queries per second (QPS) limit for this operation is 30 for each user. If you exceed this limit, your API calls are throttled. This may affect your business operations. Plan your calls accordingly. For more information, see [QPS limits](~~342790~~)."
  },
  "example": {
    "unifiedCli": "aliyun vod list-app-info --status Normal --page-no 1 --page-size 10 --resource-group-id rg-aekzko7fsuj0000 --region cn-hangzhou",
    "legacyCli": "aliyun vod ListAppInfo --region cn-hangzhou --Status Normal --PageSize 10 --ResourceGroupId 'rg-aekzko7fsuj0000' --PageNo 1"
  }
}