{
  "name": "DescribeVodAIData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AIType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "AI类型。默认返回类型。支持批量查询，多个区域使用半角逗号（,）分隔。取值：\n- **AIVideoCensor**：智能审核。\n- **AIVideoFPShot**：视频DNA。\n- **AIVideoTag**：智能标签。",
        "en": "The type of video AI. If you leave this parameter empty, statistics on video AI of all types are returned. Separate multiple types with commas (,). Valid values:\n\n*   **AIVideoCensor**: automated review\n*   **AIVideoFPShot**: media fingerprinting\n*   **AIVideoTag**: smart tagging"
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "获取数据结束时间点，需晚于起始时间。格式为：`yyyy-MM-ddTHH:mm:ssZ`（UTC时间）。",
        "en": "The end of the time range to query. The end time must be later than the start time. Specify the time in the ISO 8601 standard in the `yyyy-MM-ddTHH:mm:ssZ` format. The time must be in UTC."
      }
    },
    {
      "name": "Region",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "存储地域。默认返回所有区域。支持批量查询，多个地域使用半角逗号（,）分隔。取值：\n- **cn-shanghai**：上海。\n- **cn-beijing**：北京。\n- **eu-central-1**：德国。\n- **ap-southeast-1**：新加坡。",
        "en": "The region in which you want to query data. If you leave this parameter empty, data in all regions is returned. Separate multiple regions with commas (,). Valid values:\n\n*   **cn-shanghai**: China (Shanghai)\n*   **cn-beijing**: China (Beijing)\n*   **eu-central-1**: Germany (Frankfurt)\n*   **ap-southeast-1**: Singapore"
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "获取数据起始时间点。格式为：`yyyy-MM-ddTHH:mm:ssZ`（UTC时间）。",
        "en": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the `yyyy-MM-ddTHH:mm:ssZ` format. The time must be in UTC."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询AI处理用量数据",
    "en": "DescribeVodAIData"
  },
  "descriptions": {
    "zh": "- 目前服务地址仅支持：**华东2（上海）**。\n- 当起始结束时间间隔在7天以内时，返回小时粒度的数据。当起始结束时间间隔大于7天时，返回天粒度的数据，最大间隔为31天。",
    "en": "> *   This operation is available only in the **China (Shanghai)** region.\n>*   If the time range to query is less than or equal to seven days, the system returns the statistics collected on an hourly basis. If the time range to query is greater than seven days, the system returns the statistics collected on a daily basis. The maximum time range that you can specify to query is 31 days."
  },
  "example": {
    "unifiedCli": "aliyun vod describe-vod-ai-data --start-time 2019-02-01T13:00:00Z --end-time 2019-02-01T15:00:00Z --biz-region cn-beijing --ai-type AIVideoCensor --region cn-hangzhou",
    "legacyCli": "aliyun vod DescribeVodAIData --region cn-hangzhou --EndTime '2019-02-01T15:00:00Z' --AIType AIVideoCensor --StartTime '2019-02-01T13:00:00Z' --Region 'cn-beijing'"
  }
}