{
  "name": "DescribeVodVerifyContent",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "校验的域名。仅支持单域名校验。",
        "en": "The domain name for which you want to verify the ownership. You can specify only one domain name in each call."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取归属校验内容",
    "en": "DescribeVodVerifyContent"
  },
  "descriptions": {
    "zh": "- 目前服务地域仅支持：**华东2（上海）**。",
    "en": "*   This operation is available only in the **China (Shanghai)** region."
  },
  "example": {
    "unifiedCli": "aliyun vod describe-vod-verify-content --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun vod DescribeVodVerifyContent --region cn-hangzhou --DomainName 'example.com'"
  }
}