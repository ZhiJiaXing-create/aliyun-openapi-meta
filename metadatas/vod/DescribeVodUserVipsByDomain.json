{
  "name": "DescribeVodUserVipsByDomain",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Available",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否查询健康VIP。取值：\n\n- **on**：健康VIP。\n- **off**：全部VIP。",
        "en": "Specifies whether to query healthy virtual IP addresses (VIPs). Valid values:\n\n- **on**: Healthy VIPs.\n\n- **off**: All VIPs."
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "点播加速域名，仅支持查询单个域名。",
        "en": "The accelerated domain name for Video on Demand (VOD). Only one domain name is supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询节点IP列表-L1",
    "en": "DescribeVodUserVipsByDomain"
  },
  "descriptions": {
    "zh": "本接口仅支持华东2（上海）地域。",
    "en": "This API operation is available only in the China (Shanghai) region."
  },
  "example": {
    "unifiedCli": "aliyun vod describe-vod-user-vips-by-domain --domain-name example.com --available on --region cn-hangzhou",
    "legacyCli": "aliyun vod DescribeVodUserVipsByDomain --region cn-hangzhou --DomainName 'example.com' --Available on"
  }
}