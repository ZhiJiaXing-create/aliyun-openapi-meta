{
  "name": "GetURLUploadInfos",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "JobIds",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "上传任务ID（JobId）列表。由一个或多个JobId组成，JobId为通过[UploadMediaByURL](~~86311~~)接口上传时，返回参数JobId的值。\n- 最多支持10个ID。\n- 多个ID之间使用半角逗号（,）分隔。\n\n> JobIds和UploadURLs必须指定一个，二者同时传入时只处理JobIds。",
        "en": "A list of upload job IDs. A JobId is the value of the JobId parameter returned when you call the [UploadMediaByURL](~~86311~~) operation.\n\n- You can specify up to 10 IDs.\n\n- Separate multiple IDs with commas (,).\n\n> You must specify either JobIds or UploadURLs. If you specify both, only JobIds is processed."
      }
    },
    {
      "name": "UploadURLs",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "上传视频源文件URL列表。多个URL之间使用半角逗号（,）分隔，最多支持10个。\n> - 使用前，先进行URL编码。\n> - 如果同一URL视频多次上传，建议传入单个URL进行查询。\n> - JobIds和UploadURLs必须指定一个，二者同时传入时只处理JobIds。",
        "en": "A list of source video file URLs. Separate multiple URLs with commas (,). You can specify up to 10 URLs.\n\n> - URL-encode the URLs before you use them.\n>\n> - If a video from the same URL is uploaded multiple times, specify a single URL for the query.\n>\n> - You must specify either JobIds or UploadURLs. If you specify both, only JobIds is processed."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取URL上传信息",
    "en": "GetURLUploadInfos"
  },
  "descriptions": {
    "zh": "- 通过URL上传时返回的JobId或者上传时使用的URL来获取URL上传信息，包括URL上传状态、UserData、创建时间、完成时间等。\n- **本接口仅支持查询一年内的上传任务数据。**\n- 本接口目前服务地址仅支持：**华东 2（上海**）、**新加坡**。\n- 调用[UploadMediaByURL](~~UploadMediaByURL~~)接口将媒资文件上传到视频点播中后，可调用本接口通过URL拉取上传任务ID（`JobIds`）或者媒体源文件URL（`UploadURLs`）来查询指定媒体文件的上传信息。\n- 调用本接口时，`JobIds`和`UploadURLs`必须传入一个，二者同时传入时只处理`JobIds`。\n- 如果媒资上传失败，调用本接口可以查看错误码和错误信息；如果媒资上传成功，调用本接口可以查看对应的媒资ID。",
    "en": "- You can query information about URL-based upload jobs, including the job status, UserData, creation time, and completion time, using the JobId or the source URL.\n\n- **This operation queries data only for upload jobs created within the last year.**\n\n- The endpoints for this operation are available only in the following regions: **China (Shanghai)** and **Singapore**.\n\n- After you upload a media file to ApsaraVideo VOD by calling the [UploadMediaByURL](~~UploadMediaByURL~~) operation, you can call this operation to query the upload information using the upload job ID (`JobIds`) or the source file URL (`UploadURLs`).\n\n- When you call this operation, specify either `JobIds` or `UploadURLs`. If you specify both, only `JobIds` is processed.\n\n- If a media asset fails to upload, call this operation to view the error code and error message. If the upload is successful, call this operation to view the media ID."
  },
  "example": {
    "unifiedCli": "aliyun vod get-url-upload-infos --job-ids 86c1925fba00000,7afb201e7fa0000,2cc49973780000 --upload-urls http://0000.mp4 --region cn-hangzhou",
    "legacyCli": "aliyun vod GetURLUploadInfos --region cn-hangzhou --UploadURLs 'http://0000.mp4' --JobIds '86c1925fba00000,7afb201e7fa0000,2cc49973780000'"
  }
}