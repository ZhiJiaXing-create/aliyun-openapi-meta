{
  "name": "TagLiveResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID。取值：直播域名。\nN的取值范围为**1~50**。",
        "en": "The resource ID. Set this parameter to a live streaming domain name.\nValid values of N: **1 to 50**."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。固定值：**DOMAIN**。",
        "en": "The resource type. Set this parameter to **DOMAIN**."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "标签列表。",
        "en": "The list of labels."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "标签键。N的取值范围为**1~20**。",
            "en": "The label key. Valid values of N: **1 to 20**."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签值。N的取值范围为**1~20**。",
            "en": "The label value. Valid values of N: **1 to 20**."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "添加Live资源标签",
    "en": "Add labels to live resources"
  },
  "descriptions": {
    "zh": "单用户调用频率为100次/秒。",
    "en": "The maximum number of times that each user can call this operation per second is 100."
  },
  "example": {
    "unifiedCli": "aliyun live tag-live-resources --biz-region-id cn-hangzhou --resource-id example.com --tag Value=product Key=env --resource-type DOMAIN --region cn-hangzhou",
    "legacyCli": "aliyun live TagLiveResources --region cn-hangzhou --ResourceId.1 'example.com' --Tag.1.Key env --Tag.1.Value product --ResourceType DOMAIN --RegionId 'cn-hangzhou'"
  }
}