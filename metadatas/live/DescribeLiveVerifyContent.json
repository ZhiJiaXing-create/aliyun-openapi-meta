{
  "name": "DescribeLiveVerifyContent",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "只支持单个域名。",
        "en": "The domain name. Only one domain name is supported."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询校验归属权内容",
    "en": "DescribeLiveVerifyContent"
  },
  "descriptions": {
    "zh": "- 该API支持单个域名的验证内容查询。\n- 单用户调用频率限制为30次/秒。\n- 调用此API需要提供待验证的域名作为参数。\n- 成功调用后返回校验内容及请求ID，可用于后续操作或记录。",
    "en": "- You can call this operation to query the verification content for a single domain name.\n\n- Each user can call this operation up to 30 times per second.\n\n- You must specify the domain name that you want to authenticate.\n\n- A successful call returns the verification content and a request ID for subsequent operations or for your records."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-verify-content --biz-region-id cn-hangzhou --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveVerifyContent --region cn-hangzhou --DomainName 'example.com' --RegionId 'cn-hangzhou'"
  }
}