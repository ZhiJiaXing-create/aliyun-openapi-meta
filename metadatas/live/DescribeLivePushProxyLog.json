{
  "name": "DescribeLivePushProxyLog",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "推流域名。不支持多域名查询。",
        "en": "The ingest domain. Multi-domain queries are not supported."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取日志结束时间。\n\n日期格式按照ISO8601表示法，并使用UTC+0时间，格式为yyyy-MM-ddTHH:mm:ssZ。",
        "en": "The end of the time range to query logs.\n\nSpecify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页的页数，默认值为1。取值范围为[1,9223372036854775807]。",
        "en": "The page number. Default value: 1. Valid values: [1, 9223372036854775807]."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页大小。取值范围：\\[1, 1000\\]，默认值为300。",
        "en": "The page size. Valid values: \\[1, 1000\\]. Default value: 300."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取日志起始时间。\n\n日期格式按照ISO8601表示法，并使用UTC+0时间，格式为yyyy-MM-ddTHH:mm:ssZ。",
        "en": "The beginning of the time range to query logs.\n\nSpecify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询转推离线日志下载地址",
    "en": "Query download uRLs of offline relay logs"
  },
  "descriptions": {
    "zh": "- 查询数据时间粒度：1小时。\n- 最大查询范围：最近31天的数据。\n- 如果不指定StartTime和EndTime，该接口默认读取过去24小时的数据；指定StartTime和EndTime时，按指定的起止时间查询。\n\n## QPS限制\n本接口的单用户QPS限制为100次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "- Data time granularity: 1 hour.\n- Maximum query range: data from the last 31 days.\n- If you do not specify StartTime or EndTime, this operation reads data from the last 24 hours by default. If you specify StartTime and EndTime, data is queried based on the specified time range.\n\n## QPS limit\nThe QPS limit for a single user on this operation is 100 calls per second. If the limit is exceeded, API invocations are throttled, which may affect your business. Invoke this operation as needed."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-push-proxy-log --biz-region-id cn-hangzhou --domain-name example.com --page-size 300 --page-number 2 --start-time 2023-09-20T08:00:00Z --end-time 2023-09-20T09:00:00Z --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLivePushProxyLog --region cn-hangzhou --PageSize 300 --EndTime '2023-09-20T09:00:00Z' --DomainName 'example.com' --PageNumber 2 --StartTime '2023-09-20T08:00:00Z' --RegionId 'cn-hangzhou'"
  }
}