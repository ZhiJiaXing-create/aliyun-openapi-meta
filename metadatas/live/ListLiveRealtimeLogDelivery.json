{
  "name": "ListLiveRealtimeLogDelivery",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "LiveOpenapiReserve",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "该参数暂无实际意义。",
        "en": "This parameter is not used."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询用户下所有实时日志投递",
    "en": "ListLiveRealtimeLogDelivery"
  },
  "descriptions": {
    "zh": "您可调用本接口查询用户下所有实时日志投递，无其他特殊使用说明，调用时请保证参数设置符合要求。\n\n## QPS限制\n本接口的单用户QPS限制为6000次/分钟。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "You can call this operation to query all real-time log delivery configurations. Make sure that the parameters are set as required.\n\n## QPS limits\n\nThe queries per second (QPS) limit for this operation is 6,000 calls per minute for each user. API calls that exceed this limit are throttled, which may impact your business. Call this operation at a reasonable rate."
  },
  "example": {
    "unifiedCli": "aliyun live list-live-realtime-log-delivery --biz-region-id cn-hangzhou --live-openapi-reserve '无' --region cn-hangzhou",
    "legacyCli": "aliyun live ListLiveRealtimeLogDelivery --region cn-hangzhou --RegionId 'cn-hangzhou' --LiveOpenapiReserve '无'"
  }
}