{
  "name": "DescribeLiveDomainMonitoringUsageData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要查询的主播流域名。\n\n- 支持单个或批量域名查询。批量查询时多个域名用英文逗号（,）分隔。\n- 若参数为空，默认返回所有直播域名合并后的数据。",
        "en": "The streaming domain to query.\n\n- You can specify a single domain name or multiple domain names. Separate multiple domain names with commas (,).\n- If this parameter is left empty, the merged data of all live streaming domain names is returned by default."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "结束时间。结束时间需大于起始时间；日期格式按照ISO8601表示法，并使用UTC时间。格式为：YYYY-MM-DDThh:mm:ssZ。",
        "en": "The end time. The end time must be later than the start time. Specify the time in the ISO 8601 standard in the YYYY-MM-DDThh:mm:ssZ format. The time must be in UTC."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "监播场次ID，若参数为空，默认返回所有监播场次合并后数据，支持批量查询，多个ID用半角逗号（,）分隔。",
        "en": "The monitoring session ID. If this parameter is left empty, the merged data of all monitoring sessions is returned by default. You can specify multiple IDs. Separate multiple IDs with commas (,)."
      }
    },
    {
      "name": "Interval",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询的时间粒度，可以使用**3600**和**86400**，即小时和天粒度。",
        "en": "The time granularity for the query. Valid values: **3600** (hour) and **86400** (day)."
      }
    },
    {
      "name": "Region",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "直播中心Region。若参数为空，默认返回所有Region合并后数据，支持批量查询，多个Region用半角逗号（,）分隔。",
        "en": "The live center region. If this parameter is left empty, the merged data of all regions is returned by default. You can specify multiple regions. Separate multiple regions with commas (,)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "SplitBy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分组键。默认为**resolution**，即按清晰度分组，可选值为**domain**，**region**，**instance**，**resolution**之一。可以指定多个，多个值用半角逗号（,）分隔。",
        "en": "The grouping key. Default value: **resolution**, which indicates grouping by resolution. Valid values: **domain**, **region**, **instance**, and **resolution**. You can specify multiple values. Separate multiple values with commas (,)."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "开始时间。获取数据起始时间点，日期格式按照ISO8601表示法，并使用UTC时间。\n- 格式为：YYYY-MM-DDThh:mm:ssZ。\n- 最小数据粒度为1小时。\n- 不写默认读取过去24小时数据。",
        "en": "The start time. Specify the time in the ISO 8601 standard in the YYYY-MM-DDThh:mm:ssZ format. The time must be in UTC.\n- The minimum data granularity is 1 hour.\n- If this parameter is not specified, data of the last 24 hours is returned by default."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取域名广目监播用量",
    "en": "Query domain name monitoring usage"
  },
  "descriptions": {
    "zh": "- 支持批量域名查询，多个域名用半角逗号（,）分隔。\n- 最多可获取最近90天的数据。\n- 查询数据的时间粒度为小时或天。\n- 最大支持查询时间跨度31天。\n## QPS限制\n本接口的单用户QPS限制为20次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "- You can query multiple domain names at a time. Separate multiple domain names with commas (,).\n- You can query data of up to the last 90 days.\n- The time granularity for querying data is hour or day.\n- The maximum query time span is 31 days.\n## QPS limit\nThe QPS limit for a single user on this operation is 20 calls per second. If the limit is exceeded, API calls are throttled, which may affect your business. Call this operation appropriately."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-domain-monitoring-usage-data --biz-region-id cn-hangzhou --domain-name example.com --start-time 2022-12-10T20:00:00Z --end-time 2022-12-10T22:00:00Z --biz-region cn-shanghai --instance-id e62af24d-a354-3b0c-9f1f-da592c4b0000 --interval 3600 --split-by resolution --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveDomainMonitoringUsageData --region cn-hangzhou --EndTime '2022-12-10T22:00:00Z' --DomainName 'example.com' --InstanceId 'e62af24d-a354-3b0c-9f1f-da592c4b0000' --SplitBy resolution --StartTime '2022-12-10T20:00:00Z' --Region 'cn-shanghai' --RegionId 'cn-hangzhou' --Interval 3600"
  }
}