{
  "name": "DeleteLiveRecordNotifyConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "主播流域名。",
        "en": "The main streaming domain."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除直播录制回调配置",
    "en": "DeleteLiveRecordNotifyConfig"
  },
  "descriptions": {
    "zh": "先获取主播流域名，再调用本接口删除域名级别录制回调配置。\n\n\n\n## QPS限制\n本接口的单用户QPS限制为30次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "Obtain the main streaming domain, and then call this operation to delete the configuration of callbacks for live stream recording under the main streaming domain.\n\n## [](#qps-)QPS limit\n\nYou can call this operation up to 30 times per second per account. Requests that exceed this limit are dropped and you will experience service interruptions. We recommend that you take note of this limit when you call this operation."
  },
  "example": {
    "unifiedCli": "aliyun live delete-live-record-notify-config --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteLiveRecordNotifyConfig --region cn-hangzhou --DomainName 'example.com'"
  }
}