{
  "name": "DescribeLiveSnapshotConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "直播流所属应用名称。",
        "en": "The name of the application to which the live stream belongs."
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "主播流域名。",
        "en": "The streaming domain."
      }
    },
    {
      "name": "Order",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序。取值：\n\n- **asc**（默认值）：升序。\n- **desc**：降序。",
        "en": "The sort order. Valid values:\n\n- **asc** (default): ascending order.\n- **desc**: descending order."
      }
    },
    {
      "name": "PageNum",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页的页码。默认值：**1**。",
        "en": "The page number. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页大小。取值范围：**5~30**，默认值：**10**。",
        "en": "The number of entries per page. Valid values: **5 to 30**. Default value: **10**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询域名下的截图配置",
    "en": "Query snapshot configurations under a domain name"
  },
  "descriptions": {
    "zh": "先获取主播流域名，再调用本接口查询播流域名下的截图配置。\n\n## QPS限制\n本接口的单用户QPS限制为100次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "Obtain the streaming domain first, and then call this operation to query the snapshot configurations under the streaming domain.\n\n## QPS limit\nThe single-user QPS limit for this operation is 100 calls per second. If the limit is exceeded, API calls are throttled, which may affect your business. Call this operation as needed."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-snapshot-config --domain-name example.com --app-name liveApp0000 --page-num 1 --page-size 10 --order asc --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveSnapshotConfig --region cn-hangzhou --Order asc --PageNum 1 --PageSize 10 --DomainName 'example.com' --AppName liveApp0000"
  }
}