{
  "name": "DescribeLiveStreamsBlockList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "主播流域名。",
        "en": "The main streaming domain."
      }
    },
    {
      "name": "PageNum",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "当前页码。默认值为**1**。",
        "en": "The number of the page to return. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页大小。取值范围：**1~3000**，默认值为**2000**。",
        "en": "The number of entries to return on each page. Valid values: 1 to 3000. Default value: 2000."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取域名下直播流播放的黑名单",
    "en": "DescribeLiveStreamsBlockList"
  },
  "descriptions": {
    "zh": "调用本接口获取域名下直播流播放的黑名单。其中直播流指的是主播流。\n\n\n## QPS限制\n本接口的单用户QPS限制为50次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。更多信息，请参见[QPS限制](~~343507~~)。",
    "en": "The streaming URLs refer to the URLs for playback.\n\n## QPS limit\n\nYou can call this operation up to 50 times per second per account. Requests that exceed this limit are dropped and you may experience service interruptions."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-streams-block-list --domain-name example.com --page-num 2 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveStreamsBlockList --region cn-hangzhou --PageNum 2 --PageSize 10 --DomainName 'example.com'"
  }
}