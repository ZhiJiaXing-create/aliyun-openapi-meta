{
  "name": "DeleteChannel",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用ID，仅支持传单个ID。",
        "en": "The ID of the application. Only one ID is supported."
      }
    },
    {
      "name": "ChannelId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "已入会的频道ID，仅支持传单个ID。",
        "en": "The ID of the channel. Only one ID is supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "关闭频道",
    "en": "DeleteChannel"
  },
  "descriptions": {
    "zh": "本接口的单用户QPS限制为100次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "This API is limited to 100 queries per second (QPS) for each user. If you exceed the limit, API calls are throttled. This may impact your business. We recommend that you call this API at a reasonable rate."
  },
  "example": {
    "unifiedCli": "aliyun live delete-channel --app-id eo850000 --channel-id testid --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteChannel --region cn-hangzhou --AppId eo850000 --ChannelId testid"
  }
}