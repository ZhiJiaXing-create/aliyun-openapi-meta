{
  "name": "ForbidLiveStream",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "推流所属应用名称。您可在[流管理](~~197397~~)页面查看AppName。",
        "en": "The name of the application to which the ingest stream belongs. You can view the AppName on the [Stream Management](~~197397~~) page."
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "推流域名。",
        "en": "The ingest domain."
      }
    },
    {
      "name": "LiveStreamType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "用于指定禁止推流或者播流。目前仅支持禁止推流：**publisher**。",
        "en": "Specifies whether to disable stream ingest or streaming. Currently, only disabling stream ingest is supported: **publisher**."
      }
    },
    {
      "name": "Oneshot",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否只断流不加入黑名单。取值：\n\n- **yes**：只断流不加黑名单（支持上行推送或上行播流）。\n\n- **no**：断流加入黑名单。\n\n> 不填默认为no。",
        "en": "Specifies whether to only interrupt the stream without adding it to the blacklist. Valid values:\n\n- **yes**: Only interrupts the stream without adding it to the blacklist (supports upstream ingest or upstream streaming).\n\n- **no**: Interrupts the stream and adds it to the blacklist.\n\n> Default value: no."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "ResumeTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "恢复流的时间。格式为：yyyy-MM-ddTHH:mm:ssZ（UTC时间）。\n\n> - 当**Oneshot**参数为**no**时，ResumeTime不传值时默认禁止直播流指定时间为6个月。\n> - 如果传值则可在ResumeTime指定的时间点解除限制，恢复直播流。",
        "en": "The time to resume the stream. Format: yyyy-MM-ddTHH:mm:ssZ (UTC).\n\n> - If the **Oneshot** parameter is set to **no** and ResumeTime is not specified, the live stream is disabled for 6 months by default.\n> - If a value is specified, the restriction is lifted at the time specified by ResumeTime and the live stream is resumed."
      }
    },
    {
      "name": "StreamName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "推流名称。您可在[流管理](~~197397~~)页面查看StreamName。",
        "en": "The name of the ingest stream. You can view the StreamName on the [Stream Management](~~197397~~) page."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "禁止直播流",
    "en": "Disable a live stream"
  },
  "descriptions": {
    "zh": "调用本接口禁止某条流的推送，可以预设某个时刻将流恢复。如果没有预设时间，请通过[ResumeLiveStream](~~2847831~~)接口恢复直播流。本接口目前仅支持publisher（主播推流）。\n\n> - 本接口通过添加黑名单的方式禁止直播流，上限为10,000路，超出限制将禁用失败，请您注意统计当前禁用流的数量。请通过[DescribeLiveStreamsBlockList接口](~~2847825~~)查询禁用流数量。\n> - 若只中断直播流，而不加入黑名单，则不会占用黑名单中的10000 个名额。\n\n## QPS限制\n本接口的单用户QPS限制为20次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "Calls this operation to disable stream ingest for a specified stream. You can preset a time to resume the stream. If no preset time is specified, call the [ResumeLiveStream](~~2847831~~) operation to resume the live stream. This operation currently supports only publisher (streamer ingest).\n\n> - This operation disables live streams by adding them to a blacklist. The upper limit is 10,000 streams. If this limit is exceeded, the disable operation fails. Monitor the number of currently disabled streams. Call the [DescribeLiveStreamsBlockList](~~2847825~~) operation to query the number of disabled streams.\n> - If you only interrupt a live stream without adding it to the blacklist, the stream does not count toward the 10,000-stream blacklist quota.\n\n## QPS limit\nThe maximum queries per second (QPS) per user for this operation is 20. If this limit is exceeded, API calls are throttled, which may affect your business. Call this operation appropriately."
  },
  "example": {
    "unifiedCli": "aliyun live forbid-live-stream --biz-region-id cn-hangzhou --domain-name demo.aliyundoc.com --app-name liveApp0000 --stream-name liveStream0000 --live-stream-type publisher --oneshot yes --resume-time 2015-12-01T10:37:00Z --region cn-hangzhou",
    "legacyCli": "aliyun live ForbidLiveStream --region cn-hangzhou --StreamName liveStream0000 --DomainName 'demo.aliyundoc.com' --Oneshot yes --ResumeTime '2015-12-01T10:37:00Z' --RegionId 'cn-hangzhou' --LiveStreamType publisher --AppName liveApp0000"
  }
}