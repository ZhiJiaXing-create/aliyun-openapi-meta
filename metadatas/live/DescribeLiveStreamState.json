{
  "name": "DescribeLiveStreamState",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "直播流所属应用名称。您可在[流管理](~~197397~~)页面查看AppName。",
        "en": "The AppName of the live stream. View AppNames on the [Stream Management](~~197397~~) page."
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "直播推流域名或播流域名。",
        "en": "The ingest domain or the streaming domain."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "StreamName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "直播流名称。您可在[流管理](~~197397~~)页面查看StreamName。",
        "en": "The name of the live stream. View StreamNames on the [Stream Management](~~197397~~) page."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询单路流状态",
    "en": "DescribeLiveStreamState"
  },
  "descriptions": {
    "zh": "先获取直播域名，再调用本接口实时查询单路流状态。关于不在线状态（offline）的详细信息，请根据推流回调返回的数据获取，本接口不做细分。\n\n## QPS限制\n本接口的单用户QPS限制为100次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "After you obtain a live streaming domain name, you can call this operation to query the real-time status of a single stream. For details about the offline status, use the data returned by the stream ingest callback. This operation does not provide a breakdown of the offline status.\n\n## QPS limits\n\nYou can call this operation up to 100 times per second per account. Requests that exceed this limit are dropped and you may experience service interruptions."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-stream-state --biz-region-id cn-hangzhou --domain-name demo.aliyundoc.com --app-name liveApp0000 --stream-name liveStream0000 --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveStreamState --region cn-hangzhou --StreamName liveStream0000 --DomainName 'demo.aliyundoc.com' --RegionId 'cn-hangzhou' --AppName liveApp0000"
  }
}