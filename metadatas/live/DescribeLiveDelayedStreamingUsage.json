{
  "name": "DescribeLiveDelayedStreamingUsage",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要查询的主播流域名。\n- 支持单个或批量域名查询。批量查询时多个域名用英文逗号（,）分隔。\n- 若参数为空，默认返回所有直播域名合并后的数据。",
        "en": "The streaming domain name to query.\n- You can specify a single domain name or multiple domain names. Separate multiple domain names with commas (,).\n- If this parameter is left empty, the aggregated data of all live streaming domain names is returned by default."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "结束时间需大于起始时间，时间跨度≤10小时；日期格式按照ISO8601表示法，并使用UTC时间，格式为：YYYY-MM-DDThh:mm:ssZ。",
        "en": "The end of the time range to query. The end time must be later than the start time. The time span cannot exceed 10 hours. Specify the time in the ISO 8601 standard in the YYYY-MM-DDThh:mm:ssZ format. The time must be in UTC."
      }
    },
    {
      "name": "Interval",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询数据的时间粒度。单位：秒。取值：\n- 300\n- 3600\n- 86400\n\n不传或传值不支持时，使用默认值3600秒。",
        "en": "The time granularity of the queried data. Unit: seconds. Valid values:\n- 300\n- 3600\n- 86400\n\nIf this parameter is left empty or set to an unsupported value, the default value 3600 is used."
      }
    },
    {
      "name": "Region",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询不同直播中心, 支持批量地区查询，多个地区用逗号（半角）分隔。取值：\n- cn-beijing：北京\n- cn-shanghai：上海\n- cn-shenzhen：深圳\n- cn-qingdao：青岛\n- ap-southeast-1：新加坡\n- eu-central-1：德国\n- ap-northeast-1：东京\n- ap-southeast-5：雅加达\n\n不传值时默认为所有区域数据聚合输出。",
        "en": "The live center to query. You can specify multiple regions. Separate multiple regions with commas (,). Valid values:\n- cn-beijing: Beijing\n- cn-shanghai: Shanghai\n- cn-shenzhen: Shenzhen\n- cn-qingdao: Qingdao\n- ap-southeast-1: Singapore\n- eu-central-1: Germany\n- ap-northeast-1: Tokyo\n- ap-southeast-5: Jakarta\n\nIf this parameter is left empty, the aggregated data of all regions is returned by default."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "SplitBy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分组键，若参数为空，则聚合输出用户数据，取值：\n- domain：域名。如果此参数Split（分组键）取值为domain时，返回参数Domain才生效。\n- region：直播中心地域。如果此参数Split（分组键）取值为region时，返回参数Region才生效。\n- stream：流名。如果此参数Split（分组键）取值为stream时，返回参数stream才生效。\n\n> 当分组键对应的参数不为空时才可以查询，否则返回错误。例如：当DomainName为空时不可以指定domain为分组键。",
        "en": "The grouping key. If this parameter is left empty, user data is aggregated. Valid values:\n- domain: domain name. If the SplitBy (grouping key) parameter is set to domain, the Domain response parameter takes effect.\n- region: live center region. If the SplitBy (grouping key) parameter is set to region, the Region response parameter takes effect.\n- stream: stream name. If the SplitBy (grouping key) parameter is set to stream, the stream response parameter takes effect.\n\n> You can query data only when the parameter corresponding to the grouping key is not empty. Otherwise, an error is returned. For example, when DomainName is empty, you cannot specify domain as the grouping key."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取数据起始时间点，日期格式按照ISO8601表示法，并使用UTC时间，格式为：YYYY-MM-DDThh:mm:ssZ，默认返回近七天的数据。",
        "en": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the YYYY-MM-DDThh:mm:ssZ format. The time must be in UTC. By default, data of the last seven days is returned."
      }
    },
    {
      "name": "StreamName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "流名，多个流名使用逗号（半角）分隔，默认为所有流名数据聚合输出。",
        "en": "The stream name. Separate multiple stream names with commas (,). By default, the data of all stream names is aggregated."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询延播时长数据",
    "en": "Query delayed streaming duration data"
  },
  "descriptions": {
    "zh": "- 查询用户延播服务用量数据，默认粒度为1小时粒度。\n- 最大查询时间跨度：31天。\n- 最小查询时间粒度：1小时。\n- 最大查询时间范围：31天。\n## QPS限制\n本接口的单用户QPS限制为5次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "- Queries user delayed streaming service usage data. The default granularity is 1 hour.\n- Maximum query time span: 31 days.\n- Minimum query time granularity: 1 hour.\n- Maximum query time range: 31 days.\n## QPS limit\nThe single-user QPS limit for this API is 5 queries per second. If the limit is exceeded, API calls are throttled, which may affect your business. We recommend that you call this API at a reasonable frequency."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-delayed-streaming-usage --biz-region-id cn-hangzhou --domain-name example.com --start-time 2022-10-10T20:00:00Z --end-time 2022-10-10T21:00:00Z --stream-name liveStream0000 --biz-region cn-shanghai --split-by domain --interval 3600 --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveDelayedStreamingUsage --region cn-hangzhou --StreamName liveStream0000 --EndTime '2022-10-10T21:00:00Z' --DomainName 'example.com' --SplitBy domain --StartTime '2022-10-10T20:00:00Z' --Region 'cn-shanghai' --RegionId 'cn-hangzhou' --Interval 3600"
  }
}