{
  "name": "DescribeChannelParticipants",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID，仅支持传单个ID。",
        "en": "The ID of the application. You can specify only one application ID."
      }
    },
    {
      "name": "ChannelId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加入的频道，仅支持传单个ID。",
        "en": "The ID of the channel. You can specify only one channel ID."
      }
    },
    {
      "name": "Order",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序方式，取值：\n\n- **asc**：递增。\n\n- **desc**（默认值）：递减。",
        "en": "The sort order. Valid values:\n\n*   **asc**: ascending order.\n*   **desc**: descending order. This is the default value."
      }
    },
    {
      "name": "PageNum",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "当前页码。默认值为1。",
        "en": "The number of the page to return. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页显示用户数量，默认每页显示10条用户数据。",
        "en": "The number of entries to return on each page. Default value: 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询频道内在线用户列表",
    "en": "DescribeChannelParticipants"
  },
  "descriptions": {
    "zh": "本接口用于查询频道内在线用户列表，不包含用户详细信息。\n\n## QPS限制\n本接口的单用户QPS限制为100次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "You can call this operation to query online users in a channel. The returned result does not include details about the users.\n\n## [](#qps-)QPS limit\n\nYou can call this operation up to 100 times per second per account. Requests that exceed this limit are dropped and you may experience service interruptions. We recommend that you take note of this limit when you call this operation."
  },
  "example": {
    "unifiedCli": "aliyun live describe-channel-participants --app-id aec0000 --channel-id testId --page-num 1 --page-size 10 --order asc --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeChannelParticipants --region cn-hangzhou --Order asc --AppId aec0000 --PageNum 1 --PageSize 10 --ChannelId testId"
  }
}