{
  "name": "DescribeLiveCertificateList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "您的推流域名和播流域名。多个域名用英文逗号（,）分隔。",
        "en": "The ingest domain or streaming domain. Separate multiple domain names with commas (,)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取证书列表信息",
    "en": "DescribeLiveCertificateList"
  },
  "descriptions": {
    "zh": "如果请求中指定域名，则返回域名下的证书。如果请求中未指定域名，则返回请求账号下的所有证书。\n\n## QPS限制\n本接口的单用户QPS限制为50次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "If you specify one or more domain names in the request, the certificates of the domain names are returned. If you do not specify a domain name in the request, all certificates within your Alibaba Cloud account are returned.\n\n## [](#qps-)QPS limit\n\nYou can call this operation up to 50 times per second per account. Requests that exceed this limit are dropped and you will experience service interruptions. We recommend that you take note of this limit when you call this operation."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-certificate-list --domain-name demo.aliyundoc.com,example.com --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveCertificateList --region cn-hangzhou --DomainName 'demo.aliyundoc.com,example.com'"
  }
}