{
  "name": "DescribeStreamLocationBlock",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "直播流所属应用名称。",
        "en": "The AppName of the live stream."
      }
    },
    {
      "name": "BlockType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "封禁方式。\n-  blacklist：黑名单。\n- whitelist：白名单。",
        "en": "The blocking method. Valid values:\n\n- blacklist\n\n- whitelist"
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "播放域名。",
        "en": "The streaming domain."
      }
    },
    {
      "name": "PageNum",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "取得第几页，默认1。",
        "en": "The number of the page to return. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页大小，最大100，取值：1~100之间的任意整数。",
        "en": "The number of entries to return on each page. Valid values: integers from 1 to 100."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The ID of the region."
      }
    },
    {
      "name": "StreamName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "直播流名称。",
        "en": "The name of the live stream."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询直播流粒度封禁",
    "en": "DescribeStreamLocationBlock"
  },
  "descriptions": {
    "zh": "接口查询直播流粒度封禁信息。\n## QPS限制\n本接口的单用户QPS限制为50次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "## QPS limit\n\nYou can call this operation up to 50 times per second per user. If the number of calls per second exceeds the limit, throttling is triggered. This may affect your business. Plan your calls accordingly."
  },
  "example": {
    "unifiedCli": "aliyun live describe-stream-location-block --biz-region-id cn-hangzhou --domain-name example.com --app-name liveApp0000 --stream-name stream --block-type blacklist --page-size 5 --page-num 1 --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeStreamLocationBlock --region cn-hangzhou --StreamName stream --PageSize 5 --PageNum 1 --DomainName 'example.com' --BlockType blacklist --RegionId 'cn-hangzhou' --AppName liveApp0000"
  }
}