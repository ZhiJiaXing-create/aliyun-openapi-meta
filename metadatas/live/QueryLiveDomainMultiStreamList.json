{
  "name": "QueryLiveDomainMultiStreamList",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Domain",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "主播放域名。",
        "en": "The primary streaming domain."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "结束时间。格式为：<i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z（UTC时间）。\n\n结束时间需大于起始时间。起止时间和结束时间，间隔不超过 7 天。\n默认最近 24 小时。",
        "en": "The end time. Specify the time in the <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z format (UTC time).\n\nThe end time must be later than the start time. The interval between the start time and end time cannot exceed 7 days. Default value: the last 24 hours."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "当前页码。默认值为**1**。",
        "en": "The page number. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "单页显示的数量，默认值：10。\n\n取值范围：1~100。",
        "en": "The number of entries per page. Default value: 10.\n\nValid values: 1 to 100."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询范围的起始时间。格式为：<i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z（UTC时间）。\nStartTime和EndTime时间间隔在 7 天内。",
        "en": "The start time of the query range. Specify the time in the <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z format (UTC time). The interval between StartTime and EndTime must be within 7 days."
      }
    },
    {
      "name": "StreamName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "直播流名称。用于做过滤，精确搜索。",
        "en": "The name of the live stream. This parameter is used for exact match filtering."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询双流灾备在线记录",
    "en": "Query dual-stream disaster recovery online records"
  },
  "example": {
    "unifiedCli": "aliyun live query-live-domain-multi-stream-list --domain demo.aliyundoc.com --stream-name teststream --start-time 2024-12-01T01:00:00Z --end-time 2024-12-02T01:00:00Z --page-number 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun live QueryLiveDomainMultiStreamList --region cn-hangzhou --StreamName teststream --EndTime '2024-12-02T01:00:00Z' --PageSize 10 --PageNumber 1 --StartTime '2024-12-01T01:00:00Z' --Domain 'demo.aliyundoc.com'"
  }
}