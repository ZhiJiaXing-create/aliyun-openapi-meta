{
  "name": "DescribeLiveShiftConfigs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "播流域名。",
        "en": "The streaming domain name."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The ID of the region."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询域名时移配置",
    "en": "DescribeLiveShiftConfigs"
  },
  "descriptions": {
    "zh": "先获取播流域名，再调用本接口查询指定域名的时移配置。\n\n\n## QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "After you obtain a streaming domain name, you can call this operation to query the time-shifting configurations for the specified domain name.\n\n## QPS limit\n\nYou can make up to 10 queries per second (QPS) for each user. If you exceed this limit, API calls are throttled, which may affect your business. We recommend that you call this operation at a reasonable rate."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-shift-configs --biz-region-id cn-hangzhou --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveShiftConfigs --region cn-hangzhou --DomainName 'example.com' --RegionId 'cn-hangzhou'"
  }
}