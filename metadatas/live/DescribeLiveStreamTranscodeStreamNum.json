{
  "name": "DescribeLiveStreamTranscodeStreamNum",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "播流域名。",
        "en": "The streaming domain."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "SplitType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分组类型。\n* 域名（默认）\n* 模板",
        "en": "The grouping type. Valid values:\n* domain (default)\n* template."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取直播流转码流数量",
    "en": "Query the number of live stream transcoding streams"
  },
  "descriptions": {
    "zh": "本接口的单用户QPS限制为10次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "The QPS limit for a single user on this operation is 10 calls per second. If the limit is exceeded, API calls are throttled, which may affect your business. Call this operation at a reasonable frequency."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-stream-transcode-stream-num --biz-region-id cn-hangzhou --domain-name example.com --split-type template --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveStreamTranscodeStreamNum --region cn-hangzhou --DomainName 'example.com' --SplitType template --RegionId 'cn-hangzhou'"
  }
}