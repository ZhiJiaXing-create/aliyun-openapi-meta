{
  "name": "DeleteSnapshotCallbackAuth",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "主播流域名。",
        "en": "The main streaming domain."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域id。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除截图回调鉴权配置",
    "en": "DeleteSnapshotCallbackAuth"
  },
  "descriptions": {
    "zh": "本接口支持删除主播流域名已配置的截图回调鉴权配置。注意删除配置后重新推流可生效（截图回调不再进行鉴权）。\n## QPS限制\n本接口的单用户QPS限制为30次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "After the deletion, callbacks for new streams will no longer be authenticated.\n\n## QPS limit\n\nYou can call this operation up to 30 times per second per account. Requests that exceed this limit are dropped and you may experience service interruptions."
  },
  "example": {
    "unifiedCli": "aliyun live delete-snapshot-callback-auth --biz-region-id cn-hangzhou --domain-name example.aliyundoc.com --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteSnapshotCallbackAuth --region cn-hangzhou --DomainName 'example.aliyundoc.com' --RegionId 'cn-hangzhou'"
  }
}