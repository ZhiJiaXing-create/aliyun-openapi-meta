{
  "name": "UpdateLivePackageConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "AppName 名称与推流地址中的 AppName 名称对应，模板才能生效。不超过255字符，支持数字、大小写字母、短横线（-）、下划线（_），短横线、下划线不能位于首位。也支持取值为单个星号（*）字符，即可匹配所有AppName。",
        "en": "The application name. The template applies only when this AppName matches the application name in the ingest URL. The AppName can be up to 255 characters and can contain digits, letters, hyphens (-), and underscores (\\_). It cannot start with a hyphen or an underscore. Set this parameter to an asterisk (\\*) to match all application names."
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "直播域名（主播放域名）。",
        "en": "The primary domain name for live streaming playback."
      }
    },
    {
      "name": "IgnoreTranscode",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否忽略转码流，取值：\n\n- **true**（默认值）：忽略。\n\n- **false**：不忽略。",
        "en": "Specifies whether to ignore transcoded streams. Valid values:\n\n- **true** (default): Ignore transcoded streams.\n\n- **false**: Do not ignore transcoded streams."
      }
    },
    {
      "name": "PartDuration",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "Part切片时长，单位：毫秒。\n\n> 当Protocol为LLHLS_*时，该参数为必填项。\n- SegmentDuration为1s时，取值范围100～500ms。\n- SegmentDuration为2s时，取值范围100～1000ms。",
        "en": "The duration of a part segment in milliseconds.\n\n> This parameter is required if you set \\`Protocol\\` to \\`LLHLS\\_\\*\\`.\n\n- If SegmentDuration is 1 s, the value can range from 100 to 500 ms.\n\n- If SegmentDuration is 2 s, the value can range from 100 to 1000 ms."
      }
    },
    {
      "name": "Protocol",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "直播协议与封装格式。取值：\n\n- **HLS_CMAF**\n\n- **LLHLS_TS**\n（低延迟）\n\n- **LLHLS_CMAF**（低延迟）\n- **DASH_CMAF**\n- **HLSDASH_CMAF**",
        "en": "The protocol and container format for live streaming. Valid values:\n\n- **HLS\\_CMAF**\n\n- **LLHLS\\_TS** (low latency)\n\n- **LLHLS\\_CMAF** (low latency)\n\n- **DASH\\_CMAF**\n\n- **HLSDASH\\_CMAF**"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The ID of the region."
      }
    },
    {
      "name": "SegmentDuration",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "切片时长，单位：秒。\n\n- 如Protocol为HLS_CMAF，取值范围1～10s。\n\n- 如Protocol为LLHLS_*，取值范围1～2s。",
        "en": "The segment duration in seconds.\n\n- If you set Protocol to HLS\\_CMAF, the value can range from 1 to 10 s.\n\n- If you set Protocol to LLHLS\\_\\*, the value can range from 1 to 2 s."
      }
    },
    {
      "name": "SegmentNum",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "直播M3U8切片个数。取值范围：3～10。",
        "en": "The number of M3U8 segments. The value must be an integer from 3 to 10."
      }
    },
    {
      "name": "StreamName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "StreamName 名称与推流地址中的 StreamName 名称对应，模板才能生效。不超过255字符，支持数字、大小写字母、短横线（-）、下划线（_），短横线、下划线不能位于首位。也支持取值为单个星号（*）字符，即可匹配所有StreamName。",
        "en": "The stream name. The template applies only when this StreamName matches the stream name in the ingest URL. The StreamName can be up to 255 characters and can contain digits, letters, hyphens (-), and underscores (\\_). It cannot start with a hyphen or an underscore. Set this parameter to an asterisk (\\*) to match all stream names."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新直播封装配置",
    "en": "UpdateLivePackageConfig"
  },
  "descriptions": {
    "zh": "调用本接口更新直播封装配置，重新推流后生效。\n\n## QPS限制\n本接口的单用户QPS限制为300次/分钟。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "You can call this operation to update live encapsulation configurations. The new configurations take effect after you restart the stream ingest.\n\n## QPS limit\n\nThis operation is limited to 300 queries per second (QPS) per user. If you exceed this limit, API calls are throttled. This can affect your business operations. Plan your calls accordingly."
  },
  "example": {
    "unifiedCli": "aliyun live update-live-package-config --biz-region-id cn-hangzhou --domain-name example.com --app-name AppName --stream-name StreamName --protocol HLS_CMAF --segment-num 3 --segment-duration 5 --part-duration 350 --ignore-transcode true --region cn-hangzhou",
    "legacyCli": "aliyun live UpdateLivePackageConfig --region cn-hangzhou --IgnoreTranscode true --StreamName StreamName --DomainName 'example.com' --PartDuration 350 --RegionId 'cn-hangzhou' --Protocol HLS_CMAF --SegmentNum 3 --SegmentDuration 5 --AppName AppName"
  }
}