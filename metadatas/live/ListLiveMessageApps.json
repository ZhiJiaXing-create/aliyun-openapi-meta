{
  "name": "ListLiveMessageApps",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DataCenter",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据中心，需要与[CreateLiveMessageApp](~~2848162~~)中的数据中心一致，当前支持上海（取值为cn-shanghai)、新加坡（取值为ap-southeast-1）。",
        "en": "The data center. It must be the same as the data center that was specified when you called the [CreateLiveMessageApp](~~2848162~~) operation to create the interactive messaging application. Valid values: cn-shanghai and ap-southeast-1 (Singapore)."
      }
    },
    {
      "name": "NextPageToken",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "查询页的起始位置，若为空或者-1则默认为首页。",
        "en": "The starting page number for the query. If you leave this parameter empty or set this parameter to -1, the query starts from the first page."
      }
    },
    {
      "name": "SortType",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "排序方式，按创建时间先后进行排序。取值：\n- 1：顺序\n- 2：逆序",
        "en": "The sort order based on the creation time. Valid values:\n\n*   1: ascending order\n*   2: descending order"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询直播互动应用列表",
    "en": "ListLiveMessageApps"
  },
  "descriptions": {
    "zh": "本接口的单用户QPS限制为50次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "You can call this operation up to 50 times per second per account. Requests that exceed this limit are dropped and you will experience service interruptions. We recommend that you take note of this limit when you call this operation."
  },
  "example": {
    "unifiedCli": "aliyun live list-live-message-apps --sort-type 1 --next-page-token -1 --data-center cn-shanghai --region cn-hangzhou",
    "legacyCli": "aliyun live ListLiveMessageApps --region cn-hangzhou --SortType 1 --DataCenter 'cn-shanghai' --NextPageToken '-1'"
  }
}