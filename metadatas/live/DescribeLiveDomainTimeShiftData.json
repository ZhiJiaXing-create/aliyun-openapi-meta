{
  "name": "DescribeLiveDomainTimeShiftData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要查询的主播流域名。\n\n> \n> - 传入DomainName时，请保证传入域名为直播域名，同时调用本接口的用户拥有传入域名的操作权限。\n> - 支持单个或批量域名查询。批量查询时多个域名用英文逗号（,）分隔。\n> - 若参数为空，默认返回所有直播域名合并后的数据。",
        "en": "The streaming domain to query.\n\n> \n> - When you specify DomainName, make sure that the specified domain is a live streaming domain and that the user calling this operation has permissions on the specified domain.\n> - You can specify a single domain or multiple domains. Separate multiple domains with commas (,).\n> - If this parameter is left empty, the merged data of all live streaming domains is returned by default."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "结束时间。结束时间需大于起始时间，格式为：<i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z（UTC时间）。",
        "en": "The end time. The end time must be later than the start time. Specify the time in the <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z format in UTC."
      }
    },
    {
      "name": "Interval",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询数据的时间粒度。单位：秒。取值：\n\n- 300。\n- 3600。\n- 86400。\n\n不传或传值不支持时，使用默认值300秒。",
        "en": "The time granularity of the queried data. Unit: seconds. Valid values:\n\n- 300.\n- 3600.\n- 86400.\n\nIf you do not specify this parameter or specify an unsupported value, the default value 300 is used."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "起始时间。格式为：<i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z（UTC时间）。\n\n> - 最小数据粒度为5分钟。\n> - 不写默认读取过去24小时数据。",
        "en": "The start time. Specify the time in the <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z format in UTC.\n\n> - The minimum data granularity is 5 minutes.\n> - If you do not specify this parameter, data of the last 24 hours is returned by default."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询直播时移用量数据",
    "en": "Query time shifting usage data for live streaming"
  },
  "descriptions": {
    "zh": "- 可以用来统计每个时间间隔的时移用量数据。\n- 最多可获取最近90天的数据。\n- 数据间隔固定为1小时。\n- 最长获取数据时间跨度31天。\n\n\n## QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "- Queries time shifting usage data for each time interval.\n- Retrieves data for up to the last 90 days.\n- The data interval is fixed at 1 hour.\n- The maximum data timestamp span for a single query is 31 days.\n\n\n## QPS limit\nThe single-user QPS limit for this operation is 10 calls per second. If the limit is exceeded, the API invoke is throttled, which may affect your business. Invoke this operation as needed."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-domain-time-shift-data --biz-region-id cn-hangzhou --domain-name example.com --start-time 2021-03-03T00:00:00Z --end-time 2021-03-03T02:00:00Z --interval 300 --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveDomainTimeShiftData --region cn-hangzhou --EndTime '2021-03-03T02:00:00Z' --DomainName 'example.com' --StartTime '2021-03-03T00:00:00Z' --RegionId 'cn-hangzhou' --Interval 300"
  }
}