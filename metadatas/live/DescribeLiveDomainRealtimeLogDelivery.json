{
  "name": "DescribeLiveDomainRealtimeLogDelivery",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "播流域名。",
        "en": "The streaming domain."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询域名实时日志投递信息",
    "en": "DescribeLiveDomainRealtimeLogDelivery"
  },
  "descriptions": {
    "zh": "调用本接口查询域名实时日志投递状态，无其他特殊使用说明，调用时请保证参数设置符合要求。\n## QPS限制\n本接口的单用户QPS限制为6000次/分钟。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "You can call this operation to query the status of real-time log delivery for a domain name. Ensure that the parameter settings are valid.\n\n## QPS limits\n\nThis operation is limited to 6,000 queries per second (QPS) per user. Calls that exceed this limit are throttled, which can affect your business. Plan your calls accordingly."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-domain-realtime-log-delivery --biz-region-id cn-hangzhou --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveDomainRealtimeLogDelivery --region cn-hangzhou --DomainName 'example.com' --RegionId 'cn-hangzhou'"
  }
}