{
  "name": "DescribeLiveStreamDelayConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "您的播放域名。",
        "en": "The playback domain name."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询直播延迟时间",
    "en": "DescribeLiveStreamDelayConfig"
  },
  "descriptions": {
    "zh": "先获取播流域名，再调用本接口查询直播延迟时间。\n\n## QPS限制\n本接口的单用户QPS限制为1000次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "You must first obtain a streaming domain name. You can then call this operation to query the live streaming latency.\n\n## QPS limits\n\nThis operation supports up to 1,000 queries per second (QPS) per user. If you exceed the limit, your calls are throttled, which may affect your business. Plan your calls accordingly."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-stream-delay-config --biz-region-id cn-hangzhou --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveStreamDelayConfig --region cn-hangzhou --DomainName 'example.com' --RegionId 'cn-hangzhou'"
  }
}