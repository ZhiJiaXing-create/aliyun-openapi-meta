{
  "name": "DeleteLiveStreamsNotifyUrlConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "推流域名。",
        "en": "The ingest domain."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除直播推流回调配置",
    "en": "DeleteLiveStreamsNotifyUrlConfig"
  },
  "descriptions": {
    "zh": "先获取推流域名，再调用本接口删除推流回调配置。\n\n\n## QPS限制\n本接口的单用户QPS限制为15次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "After you obtain an ingest domain, you can call this operation to delete the stream ingest callback configuration.\n\n## QPS limits\n\nThis operation is limited to 15 queries per second (QPS) for each user. If you exceed the limit, API calls are throttled, which can affect your business. Plan your calls accordingly."
  },
  "example": {
    "unifiedCli": "aliyun live delete-live-streams-notify-url-config --biz-region-id cn-hangzhou --domain-name demo.aliyundoc.com --region cn-hangzhou",
    "legacyCli": "aliyun live DeleteLiveStreamsNotifyUrlConfig --region cn-hangzhou --DomainName 'demo.aliyundoc.com' --RegionId 'cn-hangzhou'"
  }
}