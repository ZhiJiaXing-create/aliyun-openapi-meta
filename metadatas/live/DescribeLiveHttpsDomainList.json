{
  "name": "DescribeLiveHttpsDomainList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Keyword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "加速域名。",
        "en": "The accelerated domain name."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "取得第几页，取值范围为：**[1,10000]**。",
        "en": "The page number to return. Valid values: **[1,10000]**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小，默认为**20**。",
        "en": "The number of entries per page. Default value: **20**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询证书服务列表接口",
    "en": "Query certificate service list"
  },
  "descriptions": {
    "zh": "调用本接口可查询证书服务中该用户所有已配置的证书和域名信息。\n\n调用前须确保账号下至少有一个直播域名已通过 SetLiveDomainCertificate 开启 HTTPS 证书，否则将返回 NoHttpsDomain(400)。\n\n## QPS限制\n本接口的单用户QPS限制为100次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "Calls this operation to query all configured certificates and domain name information for the user in the certificate service.\n\nBefore calling this operation, make sure that at least one live streaming domain under the account has HTTPS certificates enabled through SetLiveDomainCertificate. Otherwise, the error NoHttpsDomain(400) is returned.\n\n## QPS limit\nThe single-user QPS limit for this operation is 100 calls per second. If the limit is exceeded, the API call is throttled, which may affect your business. Call this operation appropriately."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-https-domain-list --biz-region-id cn-hangzhou --page-number 5 --page-size 20 --keyword demo.aliyun.com --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveHttpsDomainList --region cn-hangzhou --PageSize 20 --Keyword 'demo.aliyun.com' --PageNumber 5 --RegionId 'cn-hangzhou'"
  }
}