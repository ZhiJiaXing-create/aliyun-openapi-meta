{
  "name": "UpdateLiveAudioAuditConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "播流所属的应用名称。AppName名称与推流地址中的AppName名称对应，模板才能生效。区分大小写。",
        "en": "The AppName of the live stream. The template takes effect only when this name matches the AppName in the ingest URL. The name is case-sensitive."
      }
    },
    {
      "name": "BizType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "业务类型。可用于选择模型，默认值为域名。",
        "en": "The business type. This parameter is used to select a model. The default value is the domain name."
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "主播流域名。",
        "en": "The main streaming domain."
      }
    },
    {
      "name": "OssBucket",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "OSS存储Bucket名称。",
        "en": "The name of the Object Storage Service (OSS) bucket."
      }
    },
    {
      "name": "OssEndpoint",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "OSS存储Endpoint名称。",
        "en": "The endpoint of the OSS bucket."
      }
    },
    {
      "name": "OssObject",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "OSS存储的录制文件名。",
        "en": "The name of the recording file stored in OSS."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "StreamName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "播流名称。StreamName名称与推流地址中的StreamName名称对应，模板才能生效。区分大小写。",
        "en": "The stream name. The template takes effect only when this name matches the StreamName in the ingest URL. The name is case-sensitive."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新直播语音审核配置",
    "en": "UpdateLiveAudioAuditConfig"
  },
  "descriptions": {
    "zh": "- 先获取主播流域名，再调用本接口更新直播语音审核配置。\n\n- 目前仅部分直播中心支持直播智能审核，具体支持该功能的直播中心，请参见[服务地域](~~193730~~)。\n\n## QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "- Before you call this operation, obtain the streaming domain.\n\n- Automated review is supported only in some live centers. For supported regions, see [Service regions](~~193730~~).\n\n## QPS limit\n\nYou can call this operation up to 10 times per second per account. Requests that exceed this limit are dropped and you may experience service interruptions."
  },
  "example": {
    "unifiedCli": "aliyun live update-live-audio-audit-config --biz-region-id cn-hangzhou --domain-name example.com --app-name liveApp0000 --stream-name liveStream0000 --oss-bucket liveBucket0000 --oss-endpoint cn-oss-0000.aliyuncs.com --oss-object liveObject0000 --biz-type example.edu --region cn-hangzhou",
    "legacyCli": "aliyun live UpdateLiveAudioAuditConfig --region cn-hangzhou --StreamName liveStream0000 --OssEndpoint 'cn-oss-0000.aliyuncs.com' --DomainName 'example.com' --OssBucket liveBucket0000 --OssObject liveObject0000 --BizType 'example.edu' --RegionId 'cn-hangzhou' --AppName liveApp0000"
  }
}