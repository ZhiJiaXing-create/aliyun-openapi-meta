{
  "name": "ResumeLiveStream",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "推流所属应用名称。可以填为*，表示所有AppName（即不限制AppName）。您可在[流管理](~~197397~~)页面查看AppName。",
        "en": "The name of the application to which the ingest stream belongs. You can set this parameter to * to indicate all AppName values (no restriction on AppName). You can view the AppName on the [Stream Management](~~197397~~) page."
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "推流域名。",
        "en": "The ingest domain."
      }
    },
    {
      "name": "LiveStreamType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "用于指定主播推流或客户端播流。目前支持：**publisher**（主播推流）。",
        "en": "Specifies whether the stream is a streamer ingest stream or a client playback stream. Currently supported value: **publisher** (streamer ingest)."
      }
    },
    {
      "name": "StreamName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "推流名称。您可在[流管理](~~197397~~)页面查看StreamName。",
        "en": "The name of the ingest stream. You can view the StreamName on the [Stream Management](~~197397~~) page."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "开始或恢复某条直播流的推送",
    "en": "Resume stream ingest for a live stream"
  },
  "descriptions": {
    "zh": "调用本接口恢复某条推流的推送。本接口目前仅支持publisher（主播推流）。\n\n## QPS限制\n本接口的单用户QPS限制为30次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "Calls this operation to resume stream ingest for a specified stream. This operation currently supports only publisher (streamer ingest).\n\n## QPS limit\nThe single-user QPS limit for this operation is 30 calls per second. If this limit is exceeded, the API call is throttled, which may affect your business. Call this operation appropriately."
  },
  "example": {
    "unifiedCli": "aliyun live resume-live-stream --domain-name demo.aliyundoc.com --live-stream-type publisher --app-name liveApp0000 --stream-name liveStream0000 --region cn-hangzhou",
    "legacyCli": "aliyun live ResumeLiveStream --region cn-hangzhou --StreamName liveStream0000 --DomainName 'demo.aliyundoc.com' --LiveStreamType publisher --AppName liveApp0000"
  }
}