{
  "name": "DescribeLiveTopDomainsByFlow",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "结束时间需大于起始时间。日期格式按照ISO8601表示法，并使用UTC时间。\n格式为：YYYY-MM-DDThh:mm:ssZ。",
        "en": "The end of the time range to query. The end time must be later than the start time. Specify the time in the ISO 8601 standard in the YYYY-MM-DDThh:mm:ssZ format. The time must be in UTC."
      }
    },
    {
      "name": "Limit",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "获取域名数量限制。默认为20，取值支持1~100。",
        "en": "The maximum number of domain names to return. Default value: 20. Valid values: 1 to 100."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取数据起始时间点，日期格式按照ISO8601表示法，并使用UTC时间。\n格式为：YYYY-MM-DDThh:mm:ssZ（UTC时间）。最小数据粒度为5分钟，不写默认读取当月数据。",
        "en": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the YYYY-MM-DDThh:mm:ssZ format. The time must be in UTC. The minimum data granularity is 5 minutes. If you do not specify this parameter, data for the current month is returned by default."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取流最多域名",
    "en": "Query top domains by traffic"
  },
  "descriptions": {
    "zh": "- 不指定StartTime和EndTime时，默认读取当月的数据，同时支持按指定的起止时间查询，两者需要同时指定。\n- 最多可获取90天的数据。\n## QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "- If you do not specify StartTime or EndTime, data for the current month is returned by default. You can also query data for a specified time range. Both StartTime and EndTime must be specified together.\n- You can query data for up to 90 days.\n## QPS limit\nThe QPS limit for a single user on this operation is 10 calls per second. If the limit is exceeded, API calls are throttled, which may affect your business. Call this operation as needed."
  },
  "example": {
    "unifiedCli": "aliyun live describe-live-top-domains-by-flow --biz-region-id cn-hangzhou --start-time 2018-03-17T16:00:00Z --end-time 2018-03-20T16:00:00Z --limit 20 --region cn-hangzhou",
    "legacyCli": "aliyun live DescribeLiveTopDomainsByFlow --region cn-hangzhou --EndTime '2018-03-20T16:00:00Z' --StartTime '2018-03-17T16:00:00Z' --RegionId 'cn-hangzhou' --Limit 20"
  }
}