{
  "name": "GetConnectionTicket",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccessType",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "接入类型，不传表示两种都显示。\n取值范围：\n互联网：INTERNET\n专线：VPC",
        "en": "The access type. If you do not specify this parameter, both types are displayed. Valid values:\n- INTERNET: Internet access.\n- VPC: Express Connect access."
      }
    },
    {
      "name": "AppId",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用ID。\n\n> 首次调用时必填，后续调用时选填。",
        "en": "The application ID.\n\n> This parameter is required for the initial call and optional for subsequent calls."
      }
    },
    {
      "name": "AppInstanceGroupIdList",
      "position": "Body",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "交付组列表。\n\n> - 当传入本参数时，仅会在参数指定的且已授权的交付组中分配应用实例。\n> - 当传入`AppInstanceId`或`AppInstancePersistentId `参数时，本参数必填。",
        "en": "The list of delivery groups.\n\n> - If you specify this parameter, application instances are allocated only from the specified authorized delivery groups.\n> - If you specify the `AppInstanceId` or `AppInstancePersistentId` parameter, this parameter is required."
      }
    },
    {
      "name": "AppInstanceId",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用实例ID。\n\n> \n> - 当传入本参数时，仅会尝试分配参数所指定的应用实例。\n> - 当传入本参数时，必须同时传入`AppInstanceGroupIdList`参数，且`AppInstanceGroupIdList`中交付组ID的数量为1。",
        "en": "The application instance ID.\n\n> \n> - If you specify this parameter, only the specified application instance is allocated.\n> - If you specify this parameter, you must also specify the `AppInstanceGroupIdList` parameter, and the number of delivery group IDs in `AppInstanceGroupIdList` must be 1."
      }
    },
    {
      "name": "AppInstancePersistentId",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "持久会话ID。",
        "en": "The persistent session ID."
      }
    },
    {
      "name": "AppPolicyId",
      "position": "Body",
      "type": "String",
      "required": false
    },
    {
      "name": "AppStartParam",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用启动参数。关于启动参数的获取方法，请参见[如何获取应用安装参数与启动参数](~~426045~~)。",
        "en": "The application startup parameter. For information about how to obtain startup parameters, see [How to obtain application installation and startup parameters](~~426045~~)."
      }
    },
    {
      "name": "AppVersion",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用版本。传入本参数时，仅会打开参数所指定版本的应用。如果不传入本参数，则会打开已授权的任意版本的应用。",
        "en": "The application version. If you specify this parameter, only the specified version of the application is opened. If you do not specify this parameter, any authorized version of the application is opened."
      }
    },
    {
      "name": "BizRegionId",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。\n\n> 当传入本参数时，仅会在参数指定地域的交付组中分配应用实例。",
        "en": "The region ID.\n\n> If you specify this parameter, application instances are allocated only from delivery groups in the specified region."
      }
    },
    {
      "name": "EndUserId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "用户名称。",
        "en": "The username."
      }
    },
    {
      "name": "EnvironmentConfig",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "环境配置。",
        "en": "The environment configuration."
      }
    },
    {
      "name": "ProductType",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "产品类型。",
        "en": "The product type."
      }
    },
    {
      "name": "TaskId",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "任务ID。\n\n> 非首次调用时必填。使用本参数查询任务状态和连接凭证。",
        "en": "The task ID.\n\n> This request parameter is required for non-initial invokes. Use this parameter to check the task status and connection credentials."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取云应用连接凭证",
    "en": "Retrieve cloud application connection credentials"
  },
  "descriptions": {
    "zh": "本接口需要多次调用（至少两次）才能最终获得连接凭证。\n\n第一次调用，会为指定的便捷账号分配应用实例，并启动应用。返回启动任务ID（`TaskID`）。\n\n后续调用，需要在请求参数中传入`TaskID`参数，查询任务是否结束，当返回的任务状态（`TaskStatus`）为已完成（`Finished`）时，会同时返回连接凭证（`Ticket`）。",
    "en": "This operation requires multiple invokes (at least two) to obtain the connection credentials.\n\nOn the first invoke, an application instance is allocated to the specified convenience account and the application is started. A startup task ID (`TaskID`) is returned.\n\nOn subsequent invokes, pass the `TaskID` request parameter to query whether the task is complete. When the returned task status (`TaskStatus`) is completed (`Finished`), the connection credentials (`Ticket`) are also returned."
  }
}