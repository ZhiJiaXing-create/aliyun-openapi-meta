{
  "name": "RemoveRuntimeModelTemplate",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ModelTemplateId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "模型分组ID。",
        "en": "The model group ID."
      }
    },
    {
      "name": "RuntimeIds",
      "position": "Body",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "Agent运行时ID列表。",
        "en": "The list of Agent runtime IDs."
      }
    },
    {
      "name": "RuntimeType",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Agent运行时类型。",
        "en": "The Agent runtime type."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "移除Agent运行时资源的模型分组",
    "en": "Remove model groups from agent runtime resources"
  },
  "descriptions": {
    "zh": "您可以为无影 Agent 管理中心中的 JVS Computer、OpenClaw、Hermes Agent等 Agent 运行时资源移除模型分组，当Agent运行时资源需要更换模型分组的时候，需要先调用本接口移除Agent运行时资源域原有模型分组的授权关系。\n\n请确保在使用该接口前，已充分了解无影 Agent 管理中心的操作与使用。",
    "en": "You can remove model groups from Agent runtime resources such as JVS Computer, OpenClaw, and Hermes Agent in the Wuying Agent Management Center. When an Agent runtime resource needs to switch to a different model group, call this operation first to remove the authorization relationship between the Agent runtime resource and the existing model group.\n\nMake sure that you are familiar with the operations and usage of the Wuying Agent Management Center before calling this operation."
  }
}