{
  "name": "LogOffAllSessionsInAppInstanceGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppInstanceGroupId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "交付组ID。",
        "en": "The delivery group ID."
      }
    },
    {
      "name": "ProductType",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "产品类型。",
        "en": "The product type."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "注销交付组下所有会话",
    "en": "Log off all sessions in a delivery group"
  },
  "descriptions": {
    "zh": "> 本接口仅适用于已启用定时扩缩容策略的按量付费资源交付组，且仅可在定时扩缩容策略中设置的扩缩容时间段以外的时间调用成功。",
    "en": "> This operation is applicable only to pay-as-you-go resource delivery groups that have scheduled auto scaling policies enabled, and can be called successfully only outside the scaling time periods configured in the scheduled auto scaling policies."
  }
}