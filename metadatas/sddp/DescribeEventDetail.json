{
  "name": "DescribeEventDetail",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Id",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "异常事件的唯一标识ID。\n\n> 查询单个异常事件详情，需要提供单个异常事件的唯一标识ID，该ID可调用**DescribeEvents**接口获取。",
        "en": "The unique ID of the anomalous activity.\n\n> To query the details of a single anomalous activity, provide the unique ID of the anomalous activity. You can call the **DescribeEvents** operation to obtain the ID."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型。取值：\n- **zh**：中文。\n- **en**：英文。",
        "en": "The language of the request and response. Valid values:\n- **zh**: Chinese.\n- **en**: English."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询异常事件详情",
    "en": "Query anomalous activity details"
  },
  "example": {
    "unifiedCli": "aliyun sddp describe-event-detail --lang zh --id 13456723343 --region cn-hangzhou",
    "legacyCli": "aliyun sddp DescribeEventDetail --region cn-hangzhou --Lang zh --Id 13456723343"
  }
}