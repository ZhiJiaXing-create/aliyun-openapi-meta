{
  "name": "ModifyRuleStatus",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Id",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "敏感数据识别规则的唯一标识ID。\n\n> 开启或关闭敏感数据识别规则的检测功能，需要提供敏感数据识别规则的唯一标识ID。可调用**DescribeRules**接口获取该ID。",
        "en": "The unique ID of the sensitive data detection rule.\n\n> To enable or disable the detection feature of a sensitive data detection rule, you must specify the unique ID of the rule. You can call the **DescribeRules** operation to obtain the ID."
      }
    },
    {
      "name": "Ids",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "敏感数据识别规则的唯一标识ID。多个ID用半角逗号隔开。\n> 开启或关闭敏感数据识别规则的检测功能，需要提供敏感数据识别规则的唯一标识ID，可调用**DescribeRules**接口获取该ID。",
        "en": "The unique IDs of the sensitive data detection rules. Separate multiple IDs with commas (,).\n> To enable or disable the detection feature of a sensitive data detection rule, you must specify the unique ID of the rule. You can call the **DescribeRules** operation to obtain the ID."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型。取值：\n- **zh**：中文。\n- **en**：英文。",
        "en": "The language of the request and response. Valid values:\n- **zh**: Chinese.\n- **en**: English."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "开启或关闭敏感数据识别规则的检测功能。取值：\n- **0**：关闭。  \n- **1**：开启。",
        "en": "Specifies whether to enable or disable the detection feature of the sensitive data detection rule. Valid values:\n- **0**: disabled.  \n- **1**: enabled."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改识别规则的检测功能状态",
    "en": "Update the detection status of a sensitive data detection rule"
  },
  "example": {
    "unifiedCli": "aliyun sddp modify-rule-status --lang zh --id 12341 --ids 1,2,3,4 --status 1 --region cn-hangzhou",
    "legacyCli": "aliyun sddp ModifyRuleStatus --region cn-hangzhou --Status 1 --Ids '1,2,3,4' --Lang zh --Id 12341"
  }
}