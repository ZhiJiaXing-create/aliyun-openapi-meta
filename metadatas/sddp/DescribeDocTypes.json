{
  "name": "DescribeDocTypes",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型，默认为**zh_cn**。取值：\n- **zh_cn**：中文。\n- **en_us**：英文。",
        "en": "The language of the request and response. Default value: **zh_cn**. Valid values:\n- **zh_cn**: Chinese.\n- **en_us**: English."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询支持识别的OSS文件类型列表",
    "en": "Query the list of detectable OSS file types"
  },
  "example": {
    "unifiedCli": "aliyun sddp describe-doc-types --lang zh_cn --region cn-hangzhou",
    "legacyCli": "aliyun sddp DescribeDocTypes --region cn-hangzhou --Lang zh_cn"
  }
}