{
  "name": "DescribeIdentifyTaskStatus",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Id",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "任务ID，通过调用CreateScanTask或ScanOssObjectV1，从返回值中的ID字段来获取。",
        "en": "The task ID. You can obtain the task ID from the ID field in the response of the CreateScanTask or ScanOssObjectV1 operation."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型，默认为**zh_cn**。取值：\n- **zh_cn**：中文（简体）\n- **en_us**：英文（美国）",
        "en": "The language of the request and response. Default value: **zh_cn**. Valid values:\n- **zh_cn**: Simplified Chinese.\n- **en_us**: English (US)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取识别任务状态",
    "en": "Query detection task status"
  },
  "descriptions": {
    "zh": "## QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用将会被限流，这可能影响您的业务，请合理调用。",
    "en": "## QPS limit\nThe single-user QPS limit for this operation is 10 calls per second. If this limit is exceeded, API calls are throttled, which may affect your business. Call this operation appropriately."
  },
  "example": {
    "unifiedCli": "aliyun sddp describe-identify-task-status --id 268 --lang zh_cn --region cn-hangzhou",
    "legacyCli": "aliyun sddp DescribeIdentifyTaskStatus --region cn-hangzhou --Lang zh_cn --Id 268"
  }
}