{
  "name": "ModifyEventTypeStatus",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FeatureType",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "此参数已废弃。",
        "en": "This parameter is deprecated."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型。-**zh**：中文-**en**：英文",
        "en": "The language of the request and response. Valid values:- **zh**: Chinese- **en**: English."
      }
    },
    {
      "name": "SubTypeIds",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "子类型异常事件唯一标识ID，多个ID以半角逗号隔开。\n\n> 开启SDDP检测子类型异常事件的功能，需要提供子类型异常事件唯一标识ID，该ID可调用**DescribeEventTypes**接口获取。",
        "en": "The unique IDs of the anomalous activity child classes, separated by commas (,).\n\n> To enable the detection of anomalous activities of specified child classes, provide the unique IDs of the child classes. You can call the **DescribeEventTypes** operation to obtain the IDs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改检测子类型异常事件状态",
    "en": "Update detection status for anomalous activity child classes"
  },
  "example": {
    "unifiedCli": "aliyun sddp modify-event-type-status --lang zh --sub-type-ids 020008 --feature-type 2 --region cn-hangzhou",
    "legacyCli": "aliyun sddp ModifyEventTypeStatus --region cn-hangzhou --FeatureType 2 --SubTypeIds 020008 --Lang zh"
  }
}