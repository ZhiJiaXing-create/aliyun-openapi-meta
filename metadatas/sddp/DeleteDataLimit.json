{
  "name": "DeleteDataLimit",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FeatureType",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "该参数已废弃。",
        "en": "This parameter is deprecated."
      }
    },
    {
      "name": "Id",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "实例、库、Bucket数据资产的唯一标识ID。\n\n可调用DescribeDataLimits接口，查看返回结果中的Id，获取数据资产的唯一标识ID。",
        "en": "The unique ID of the data asset, such as an instance, database, or bucket.\n\nCall the DescribeDataLimits operation to obtain the ID from the Id parameter in the response."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型，默认为**zh_cn**。取值：\n- **zh_cn**：中文。\n- **en_us**：英文。",
        "en": "The language of the request and response. Default value: **zh_cn**. Valid values:\n- **zh_cn**: Chinese.\n- **en_us**: English."
      }
    },
    {
      "name": "SourceIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "该参数已废弃。",
        "en": "This parameter is deprecated."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除数据资产扫描授权",
    "en": "Delete data asset scan authorization"
  },
  "descriptions": {
    "zh": "本接口一般用于对用户已授权的数据资产进行取消授权，便于对授权进行管理。\n\n## QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用将会被限流，这可能影响您的业务，请合理调用。",
    "en": "This operation is used to revoke authorization for data assets that a user has authorized, facilitating authorization management.\n\n## QPS limit\nThe single-user QPS limit for this operation is 10 calls per second. If the limit is exceeded, API calls are throttled, which may affect your business. Call this operation at a reasonable frequency."
  },
  "example": {
    "unifiedCli": "aliyun sddp delete-data-limit --id 12033 --feature-type 1 --source-ip 106.11.XX.XX --lang zh_cn --region cn-hangzhou",
    "legacyCli": "aliyun sddp DeleteDataLimit --region cn-hangzhou --FeatureType 1 --SourceIp '106.11.XX.XX' --Lang zh_cn --Id 12033"
  }
}