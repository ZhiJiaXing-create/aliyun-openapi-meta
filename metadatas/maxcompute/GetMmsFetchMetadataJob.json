{
  "name": "GetMmsFetchMetadataJob",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/mms/datasources/[sourceId]/scans/[scanId]",
  "parameters": [
    {
      "name": "scanId",
      "position": "Path",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "同步元数据的异步任务ID",
        "en": "The ID of the asynchronous task for metadata synchronization."
      }
    },
    {
      "name": "sourceId",
      "position": "Path",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "数据源ID。",
        "en": "The data source ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取“同步元数据”任务详情",
    "en": "GetMmsFetchMetadataJob"
  },
  "example": {
    "unifiedCli": "aliyun maxcompute get-mms-fetch-metadata-job --source-id 2000015 --scan-id 1000002 --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute GET /api/v1/mms/datasources/2000015/scans/1000002 --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}