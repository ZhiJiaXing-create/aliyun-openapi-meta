{
  "name": "GetRolePolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/projects/[projectName]/roles/[roleName]/policy",
  "parameters": [
    {
      "name": "projectName",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "MaxCompute项目名称",
        "en": "The name of the MaxCompute project."
      }
    },
    {
      "name": "roleName",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "角色名称。",
        "en": "The name of the role."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取角色Policy授权内容",
    "en": "GetRolePolicy"
  },
  "example": {
    "unifiedCli": "aliyun maxcompute get-role-policy --project-name projectA --role-name roleA --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute GET /api/v1/projects/projectA/roles/roleA/policy --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}