{
  "name": "ListRoles",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/projects/[projectName]/roles",
  "parameters": [
    {
      "name": "projectName",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "MaxCompute项目名称",
        "en": "The name of the MaxCompute project."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取项目级角色列表",
    "en": "ListRoles"
  },
  "example": {
    "unifiedCli": "aliyun maxcompute list-roles --project-name projectA --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute GET /api/v1/projects/projectA/roles --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}