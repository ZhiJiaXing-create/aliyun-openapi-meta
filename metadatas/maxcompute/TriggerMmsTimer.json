{
  "name": "TriggerMmsTimer",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/mms/datasources/[sourceId]/timers/[timerId]/trigger",
  "parameters": [
    {
      "name": "sourceId",
      "position": "Path",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "datasource id",
        "en": "The ID of the datasource."
      }
    },
    {
      "name": "timerId",
      "position": "Path",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "timer id",
        "en": "The ID of the timer."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "立即触发数据迁移定时任务",
    "en": "TriggerMmsTimer"
  },
  "example": {
    "unifiedCli": "aliyun maxcompute trigger-mms-timer --source-id 2000014 --timer-id 1 --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute POST /api/v1/mms/datasources/2000014/timers/1/trigger --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}