{
  "name": "GetRunningJobs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/jobs/runningJobs",
  "parameters": [
    {
      "name": "from",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "查询开始时间点。该时间是指作业的提交时间。\n\n- 请求参数**from**和**to**定义的时间区间遵循双闭原则，即该时间区间既包括区间开始时间点也包括区间结束时间点。如果**from**和**to**的值相同，则为无效区间，函数直接返回空。\n- Unix时间戳格式，表示从1970-1-1 00:00:00 UTC计算起的秒数。",
        "en": "The time when the query starts. This parameter specifies the time when a job is submitted.\n\n- The time range that is specified by the **from** and **to** request parameters is a closed interval. The start time and end time are included in the range. If the value of **from** is the same as the value of **to**, the time range is invalid, and a null value is returned.\n\n- The value is a UNIX timestamp that represents the number of seconds that have elapsed since January 1, 1970, 00:00:00 UTC."
      }
    },
    {
      "name": "jobOwnerList",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "作业的执行者列表。",
        "en": "The list of job executors."
      }
    },
    {
      "name": "pageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "页码。",
        "en": "The page number."
      }
    },
    {
      "name": "pageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "每页返回数目，默认为10，最大为20。",
        "en": "The number of entries per page. Default value: 10. Maximum value: 20."
      }
    },
    {
      "name": "quotaNicknameList",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "作业使用的配额昵称列表。",
        "en": "The list of nicknames of quotas that are used by jobs."
      }
    },
    {
      "name": "to",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "查询结束时间点。该时间是指作业的提交时间。\n\n- 请求参数**from**和**to**定义的时间区间遵循双闭原则，即该时间区间既包括区间开始时间点也包括区间结束时间点。如果**from**和**to**的值相同，则为无效区间，函数直接返回空。\n- Unix时间戳格式，表示从1970-1-1 00:00:00 UTC计算起的秒数。",
        "en": "The time when the query ends. This parameter specifies the time when a job is submitted.\n\n- The time interval that is specified by the **from** and **to** request parameters is a closed interval. The start time and end time are included in the interval. If the value of **from** is the same as the value of **to**, the interval is invalid, and a null value is returned.\n\n- The value is a UNIX timestamp that represents the number of seconds that have elapsed since January 1, 1970, 00:00:00 UTC."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取运行态的作业列表",
    "en": "GetRunningJobs"
  },
  "example": {
    "unifiedCli": "aliyun maxcompute get-running-jobs --from 1683785928 --to 1683612946 --job-owner-list ALIYUN_xxx@test.aliyunid.com --quota-nickname-list my_quota --page-number 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute GET /api/v1/jobs/runningJobs --region cn-hangzhou --pageNumber 1 --jobOwnerList 'ALIYUN_xxx@test.aliyunid.com' --quotaNicknameList my_quota --pageSize 10 --from 1683785928 --to 1683612946 --header \"Content-Type=application/json\""
  }
}