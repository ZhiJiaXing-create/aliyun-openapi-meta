{
  "name": "GetMmsDataSource",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/mms/datasources/[sourceId]",
  "parameters": [
    {
      "name": "lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定配置项的描述语言",
        "en": "The language of the description for the configuration items."
      }
    },
    {
      "name": "sourceId",
      "position": "Path",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "数据源ID。",
        "en": "The ID of the data source."
      }
    },
    {
      "name": "withConfig",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否获取数据源的具体配置项",
        "en": "Specifies whether to obtain the specific configuration items of the data source."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取单个数据源详情",
    "en": "GetMmsDataSource"
  },
  "example": {
    "unifiedCli": "aliyun maxcompute get-mms-data-source --source-id 2000015 --with-config true --lang en_US --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute GET /api/v1/mms/datasources/2000015 --region cn-hangzhou --withConfig true --lang en_US --header \"Content-Type=application/json\""
  }
}