{
  "name": "DeleteComputeQuotaPlan",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/quotas/[nickname]/computeQuotaPlan/[planName]",
  "parameters": [
    {
      "name": "nickname",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "一级Quota昵称。",
        "en": "The nickname of level-1 compute quota."
      }
    },
    {
      "name": "planName",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Quota计划名称。",
        "en": "The name of quota plan."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除计算Quota计划",
    "en": "DeleteComputeQuotaPlan"
  },
  "example": {
    "unifiedCli": "aliyun maxcompute delete-compute-quota-plan --nickname os_ComputeQuota_p --plan-name planA --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute DELETE /api/v1/quotas/os_ComputeQuota_p/computeQuotaPlan/planA --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}