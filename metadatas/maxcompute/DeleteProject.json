{
  "name": "DeleteProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/projects/[projectName]",
  "parameters": [
    {
      "name": "isLogical",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否逻辑删除，默认为true。false表示物理删除。",
        "en": "Specifies whether to perform a logical deletion. The default value is true. A value of false indicates a physical deletion."
      }
    },
    {
      "name": "projectName",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "项目名称。",
        "en": "The name of the project."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除项目",
    "en": "DeleteProject"
  },
  "example": {
    "unifiedCli": "aliyun maxcompute delete-project --project-name odps_project --is-logical true --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute DELETE /api/v1/projects/odps_project --region cn-hangzhou --isLogical true --header \"Content-Type=application/json\""
  }
}