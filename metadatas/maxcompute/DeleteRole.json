{
  "name": "DeleteRole",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/projects/[projectName]/roles/[roleName]",
  "parameters": [
    {
      "name": "projectName",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "项目名称。",
        "en": "The name of the project."
      }
    },
    {
      "name": "roleName",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "角色名称。",
        "en": "The name of the role."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除角色",
    "en": "DeleteRole"
  },
  "example": {
    "unifiedCli": "aliyun maxcompute delete-role --project-name odps_project --role-name admin --region cn-hangzhou",
    "legacyCli": "aliyun maxcompute DELETE /api/v1/projects/odps_project/roles/admin --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}