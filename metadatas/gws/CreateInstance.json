{
  "name": "CreateInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllocatePublicAddress",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否分配公网地址\n* True\n* False",
        "en": "Specifies whether to assign a public IP address. Valid values:\n* True\n* False."
      }
    },
    {
      "name": "AppList",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "应用列表。",
        "en": "The application list."
      },
      "sub_parameters": [
        {
          "name": "AppArgs",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "> 此值仅在 `WorkMode` 为 `Application` 时有效。\n\n应用的运行参数。其中 AppList.N 的允许范围是 1~10。",
            "en": "> This parameter takes effect only when WorkMode is set to Application.\n\nThe runtime parameters of the application. Valid values of N in AppList.N: 1 to 10."
          }
        },
        {
          "name": "AppName",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "> 此值仅在 `WorkMode` 为 `Application` 时有效。\n\n应用名称。其中 AppList.N 的允许范围是 1~10。",
            "en": "> This parameter takes effect only when WorkMode is set to Application.\n\nThe application name. Valid values of N in AppList.N: 1 to 10."
          }
        },
        {
          "name": "AppPath",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "> 此值仅在 `WorkMode` 为 `Application` 时有效。\n\n应用的运行路径。其中 AppList.N 的允许范围是 1~10。",
            "en": "> This parameter takes effect only when WorkMode is set to Application.\n\nThe execution path of the application. Valid values of N in AppList.N: 1 to 10."
          }
        }
      ]
    },
    {
      "name": "AutoRenew",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自动续费：\n* False（默认）\n* True",
        "en": "Specifies whether to enable auto-renewal. Valid values:\n* False (default)\n* True."
      }
    },
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群 id。",
        "en": "The cluster ID."
      }
    },
    {
      "name": "ImageId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "镜像 id。",
        "en": "The image ID."
      }
    },
    {
      "name": "InstanceChargeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例付费方式：\n* PostPaid（默认）：按量付费\n* PrePaid：包年包月",
        "en": "The billing method of the instance. Valid values:\n* PostPaid (default): pay-as-you-go.\n* PrePaid: subscription."
      }
    },
    {
      "name": "InstanceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例规格，请参见 [实例规格族](https://help.aliyun.com/document_detail/25378.html) 或调用 [DescribeInstanceTypes](https://help.aliyun.com/document_detail/25620.html) 获得规格表。",
        "en": "The instance type. For more information, see [Instance families](https://help.aliyun.com/document_detail/25378.html) or call [DescribeInstanceTypes](https://help.aliyun.com/document_detail/25620.html) to query available instance types."
      }
    },
    {
      "name": "InternetChargeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "> 此值只在 `AllocatePublicAddress` 为 `true` 时有效\n\n网络计费类型。取值范围：\n\n- PayByTraffic（默认）：按使用流量计费\n- PayByBandwidth：按固定带宽计费",
        "en": "> This parameter takes effect only when AllocatePublicAddress is set to true.\n\nThe billing method for network usage. Valid values:\n\n- PayByTraffic (default): pay-by-traffic.\n- PayByBandwidth: pay-by-bandwidth."
      }
    },
    {
      "name": "InternetMaxBandwidthIn",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "> 此值只在 `AllocatePublicAddress` 为 `true` 时有效\n\n公网入带宽最大值，单位为 Mbps\n* 取值范围 1~100\n* 默认值：4",
        "en": "> This parameter takes effect only when AllocatePublicAddress is set to true.\n\nThe maximum inbound public bandwidth. Unit: Mbit/s.\n* Valid values: 1 to 100.\n* Default value: 4."
      }
    },
    {
      "name": "InternetMaxBandwidthOut",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "> 此值只在 `AllocatePublicAddress` 为 `true` 时有效\n\n公网出带宽最大值，单位为 Mbps\n* 取值范围 1~100\n* 默认值：4",
        "en": "> This parameter takes effect only when AllocatePublicAddress is set to true.\n\nThe maximum outbound public bandwidth. Unit: Mbit/s.\n* Valid values: 1 to 100.\n* Default value: 4."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例名称。",
        "en": "The instance name."
      }
    },
    {
      "name": "Period",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "> 此值仅在 `InstanceChargeType` 为 `PrePaid`（包年包月）时有效。\n\n包年包月的时长。\n* `PeriodUnit` 为 `Week`（周）时，取值范围 1~4\n* `PeriodUnit` 为 `Month`（月）时，取值范围 1 2 3 4 5 6 7 8 9 12 24 36 48 60",
        "en": "> This parameter takes effect only when InstanceChargeType is set to PrePaid (subscription).\n\nThe subscription duration.\n* If PeriodUnit is set to Week, valid values are 1 to 4.\n* If PeriodUnit is set to Month, valid values are 1, 2, 3, 4, 5, 6, 7, 8, 9, 12, 24, 36, 48, and 60."
      }
    },
    {
      "name": "PeriodUnit",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "> 此值仅在 `InstanceChargeType` 为 `PrePaid`（包年包月）时有效。\n\n包年包月的单位。\n* Week\n* Month",
        "en": "> This parameter takes effect only when InstanceChargeType is set to PrePaid (subscription).\n\nThe unit of the subscription duration. Valid values:\n* Week\n* Month."
      }
    },
    {
      "name": "SystemDiskCategory",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "系统盘的种类：\n* cloud_efficiency：高效云盘。\n* cloud_ssd：SSD云盘。\n* cloud_essd：ESSD 云盘。（只有较新规格族的实例，例如 ecs.g6 系统，才支持 ESSD 云盘）",
        "en": "The category of the system cloud disk. Valid values:\n* cloud_efficiency: ultra cloud disk.\n* cloud_ssd: standard SSD.\n* cloud_essd: Enterprise SSD (ESSD). Only instances of newer instance families, such as ecs.g6, support ESSDs."
      }
    },
    {
      "name": "SystemDiskSize",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "系统盘大小，单位为 GB。\n* 取值范围 20~500。\n* 选取不同镜像时，要求系统盘大小 >= 镜像的大小。",
        "en": "The system cloud disk size. Unit: GB.\n* Valid values: 20 to 500.\n* When you select an image, the system cloud disk size must be greater than or equal to the image size."
      }
    },
    {
      "name": "VSwitchId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "交换机 id。\n* 当指定时，选用此交换机来创建实例。推荐在创建时使用此参数，以限定实例的网段与可用区。\n* 当不指定时，后台会根据当前实例类型，找到有库存的最新可用区（例如华北 2 可用区 G ），如果此可用区\n    * 已有交换机，则直接选用\n    * 没有交换机，则会尝试自动创建",
        "en": "The vSwitch ID.\n* If specified, the instance is created in this vSwitch. Specify this parameter when you create an instance to define the CIDR block and zone of the instance.\n* If not specified, the system automatically selects the latest active zone with available inventory based on the current instance type (for example, China (Beijing) Zone G).\n    * If a vSwitch already exists in the zone, the system uses the existing vSwitch.\n    * If no vSwitch exists in the zone, the system attempts automatic creation."
      }
    },
    {
      "name": "WorkMode",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "工作模式：\n* Desktop：桌面模式。\n* Application：应用模式。",
        "en": "The work mode. Valid values:\n* Desktop: desktop mode.\n* Application: application mode."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "CreateInstance",
    "en": "CreateInstance"
  }
}