{
  "name": "GetConsumerProgress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConsumerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Group的名称。",
        "en": "The name of the consumer group."
      }
    },
    {
      "name": "HideLastTimestamp",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否隐藏 LastTimestamp，默认值为 false，建议设置为 true。\n\n\n> \n> - 若设置为 true，则返回参数中的 LastTimestamp 会返回为 -1，反之则返回具体的值。仅支持预留实例云存储类型的 Topic。\n> - 该接口处理数据较多，比较损耗性能，建议设置为 true，可加快处理时间。",
        "en": "Specifies whether to hide the \\`LastTimestamp\\` parameter. The default value is false. Set this parameter to true for better performance.\n\n> - If you set this parameter to true, -1 is returned for the \\`LastTimestamp\\` parameter. Otherwise, a specific value is returned. This parameter is supported only for topics of cloud storage on provisioned instances.\n>\n> - This operation processes a large amount of data and consumes a high degree of performance. Set this parameter to true to reduce the processing time."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例的地域ID。",
        "en": "The region ID of the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询消费组消费状态",
    "en": "GetConsumerProgress"
  },
  "example": {
    "unifiedCli": "aliyun alikafka get-consumer-progress --instance-id alikafka_pre-cn-mp919o4v0000 --consumer-id kafka-test --biz-region-id cn-hangzhou --hide-last-timestamp true --api-version 2019-09-16 --region cn-hangzhou",
    "legacyCli": "aliyun alikafka GetConsumerProgress --region cn-hangzhou --InstanceId 'alikafka_pre-cn-mp919o4v0000' --ConsumerId 'kafka-test' --HideLastTimestamp true --RegionId 'cn-hangzhou'"
  }
}