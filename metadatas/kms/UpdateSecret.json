{
  "name": "UpdateSecret",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "凭据的描述信息。",
        "en": "The description of the secret."
      }
    },
    {
      "name": "ExtendedConfig.CustomData",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "拓展配置中的自定义数据。\n>\n> - 如果指定该参数，将会更新凭据已有的拓展配置。\n> - 通用凭据不支持设置该参数。",
        "en": "The custom data in the extended configuration of the secret.\n\n> *   If this parameter is specified, the existing extended configuration of the secret is updated.\n> *   This parameter is unavailable for generic secrets."
      }
    },
    {
      "name": "SecretName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "凭据名称或凭据资源名称（ARN）。\n>访问其他阿里云账号下的凭据时，必须输入凭据ARN。凭据ARN的格式为`acs:kms:${region}:${account}:secret/${secret-name}`。",
        "en": "The name of the secret."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新凭据的元数据",
    "en": "UpdateSecret"
  },
  "descriptions": {
    "zh": "RAM用户或RAM角色调用该OpenAPI需要被授予的权限策略详情，请参见[访问控制](~~2767210~~)。\n\n本文将提供一个示例，更新名为`secret001`凭据的元数据，将其描述信息`Description`更新为`datainfo`。",
    "en": "In this example, the metadata of the `secret001` secret is updated. The `Description` parameter is set to `datainfo`."
  },
  "example": {
    "unifiedCli": "aliyun kms update-secret --secret-name secret001 --description datainfo --extended-config-custom-data Port=3306 DBName=app1 --region cn-hangzhou",
    "legacyCli": "aliyun kms UpdateSecret --region cn-hangzhou --SecretName secret001 --ExtendedConfig.CustomData '{\"Port\":\"3306\",\"DBName\":\"app1\"}' --Description datainfo"
  }
}