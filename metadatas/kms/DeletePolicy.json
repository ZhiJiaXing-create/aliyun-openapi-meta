{
  "name": "DeletePolicy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要删除的权限策略名称。",
        "en": "The name of the permission policy that you want to delete."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除访问策略",
    "en": "DeletePolicy"
  },
  "descriptions": {
    "zh": "RAM用户或RAM角色调用该OpenAPI需要被授予的权限策略详情，请参见[访问控制](~~2767210~~)。\n\n删除前请确保该权限策略没有被任何应用接入点绑定，否则会导致相关应用无法正常访问KMS。",
    "en": "Before you delete a permission policy, make sure that the permission policy is not associated with application access points (AAPs). Otherwise, related applications cannot access Key Management Service (KMS)."
  },
  "example": {
    "unifiedCli": "aliyun kms delete-policy --name policy_test --region cn-hangzhou",
    "legacyCli": "aliyun kms DeletePolicy --region cn-hangzhou --Name policy_test"
  }
}