{
  "name": "DeleteSecret",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ForceDeleteWithoutRecovery",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否立即删除凭据，且不允许恢复。\n\n取值范围：\n- **true**\n- **false**（默认值）",
        "en": "Specifies whether to forcibly delete the secret. If this parameter is set to true, the secret cannot be recovered.\n\nValid values:\n\n*   **true**\n*   **false** (default value)"
      }
    },
    {
      "name": "RecoveryWindowInDays",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "计划删除凭据时，该参数用于指定删除窗口，窗口期内可以恢复凭据。取值：7天~30天。\n默认值：30。\n\n> 当您需要立即删除凭据，即ForceDeleteWithoutRecovery取值为true时，不允许输入本参数。",
        "en": "Specifies the recovery period of the secret if you do not forcibly delete it. Default value: 30. Unit: Days."
      }
    },
    {
      "name": "SecretName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "凭据名称或凭据资源名称（ARN）。\n>访问其他阿里云账号下的凭据时，必须输入凭据ARN。凭据ARN的格式为`acs:kms:${region}:${account}:secret/${secret-name}`。",
        "en": "The name of the secret."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除凭据对象",
    "en": "DeleteSecret"
  },
  "descriptions": {
    "zh": "- RAM用户或RAM角色调用该OpenAPI需要被授予的权限策略详情，请参见[访问控制](~~2767210~~)。\n\n- 您可以设置计划删除凭据，时间窗口支持7天~30天，窗口期内凭据可以恢复。也可以立即删除凭据，凭据立即删除且不可恢复。",
    "en": "If you call this operation without specifying a recovery period, the deleted secret can be recovered within 30 days.\n\nIf you specify a recovery period, the deleted secret can be recovered within the recovery period. You can also forcibly delete a secret. A forcibly deleted secret cannot be recovered."
  },
  "example": {
    "unifiedCli": "aliyun kms delete-secret --secret-name secret001 --force-delete-without-recovery false --recovery-window-in-days 10 --region cn-hangzhou",
    "legacyCli": "aliyun kms DeleteSecret --region cn-hangzhou --SecretName secret001 --ForceDeleteWithoutRecovery false --RecoveryWindowInDays 10"
  }
}