{
  "name": "DescribeSecret",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FetchTags",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否在返回参数中包含凭据的资源标签。取值：  \n\n- true：包含资源标签。 \n\n- false（默认值）：不包含资源标签。",
        "en": "Specifies whether to return the resource tags of the secret. Valid values:\n\n*   true: The resource tags are returned.\n*   false: The resource tags are not returned. This is the default value."
      }
    },
    {
      "name": "SecretName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "凭据名称或凭据资源名称（ARN）。\n\n> 访问其他阿里云账号下的凭据时，必须输入凭据ARN。凭据ARN的格式为`acs:kms:${region}:${account}:secret/${secret-name}`。",
        "en": "The name of the secret."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询凭据的元数据信息",
    "en": "DescribeSecret"
  },
  "descriptions": {
    "zh": "- RAM用户或RAM角色调用该OpenAPI需要被授予的权限策略详情，请参见[访问控制](~~2767210~~)。\n\n- 此接口返回指定凭据的元数据信息，不返回凭据值。\n  \n本文将提供一个示例，查询一个名称为`secret001`凭据的元数据信息。",
    "en": "This operation returns the metadata of a secret. This operation does not return the secret value.\n\nIn this example, the metadata of the secret named `secret001` is queried."
  },
  "example": {
    "unifiedCli": "aliyun kms describe-secret --secret-name secret001 --fetch-tags true --region cn-hangzhou",
    "legacyCli": "aliyun kms DescribeSecret --region cn-hangzhou --SecretName secret001 --FetchTags true"
  }
}