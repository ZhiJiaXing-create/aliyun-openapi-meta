{
  "name": "DeleteAlias",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AliasName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要操作的别名。    \n长度为1~255个字符，必须包含前缀alias/。",
        "en": "The alias that you want to delete.\n\nThe value must be 1 to 255 characters in length and must include the alias/ prefix."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除别名",
    "en": "DeleteAlias"
  },
  "descriptions": {
    "zh": "RAM用户或RAM角色调用该OpenAPI需要被授予的权限策略详情，请参见[访问控制](~~2767210~~)。"
  },
  "example": {
    "unifiedCli": "aliyun kms delete-alias --alias-name alias/example --region cn-hangzhou",
    "legacyCli": "aliyun kms DeleteAlias --region cn-hangzhou --AliasName 'alias/example'"
  }
}