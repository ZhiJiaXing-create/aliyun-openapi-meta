{
  "name": "DeleteNetworkRule",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要删除的网络控制规则的名称。",
        "en": "The name of the network control rule to delete."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除网络规则",
    "en": "DeleteNetworkRule"
  },
  "descriptions": {
    "zh": "- RAM用户或RAM角色调用该OpenAPI需要被授予的权限策略详情，请参见[访问控制](~~2767210~~)。\n\n- 删除前请确保该网络控制规则没有被任何权限策略绑定，否则会导致相关应用无法正常访问KMS。",
    "en": "- For information about the access policy that is required to call this OpenAPI as a Resource Access Management (RAM) user or RAM role, see [Resource Access Management](~~2767210~~).\n\n- Before deleting the network control rule, ensure that it is not attached to any access policies. Otherwise, related applications cannot access KMS as expected."
  },
  "example": {
    "unifiedCli": "aliyun kms delete-network-rule --name networkrule_test --region cn-hangzhou",
    "legacyCli": "aliyun kms DeleteNetworkRule --region cn-hangzhou --Name networkrule_test"
  }
}