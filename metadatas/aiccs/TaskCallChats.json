{
  "name": "TaskCallChats",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AgentId",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "坐席ID，可以为空；其中：taskId、agentId、agentTag必须一项有值；如果传多个值，以taskId > agentId > agentTag 优先级查询。只查1次优先值。",
        "en": "The agent ID. This parameter can be left empty. At least one of the following parameters must have a value: TaskId, AgentId, and AgentTag. If multiple values are specified, the query is performed based on the following priority: TaskId > AgentId > AgentTag. Only the parameter with the highest priority is used for the query."
      }
    },
    {
      "name": "AgentTag",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "建议填写坐席在贵司业务系统唯一标识，用于查询对应agentId；可以为空；其中：taskId、agentId、agentTag必须一项有值；如果传多个值，以taskId > agentId > agentTag 优先级查询。只查1次优先值。",
        "en": "The unique identifier of the agent in your business system. This parameter is used to query the corresponding AgentId. This parameter can be left empty. At least one of the following parameters must have a value: TaskId, AgentId, and AgentTag. If multiple values are specified, the query is performed based on the following priority: TaskId > AgentId > AgentTag. Only the parameter with the highest priority is used for the query."
      }
    },
    {
      "name": "CallId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "外呼ID",
        "en": "The outbound call ID."
      }
    },
    {
      "name": "TaskId",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "决策系统中的任务编号，可以为空，其中：taskId、agentId、agentTag必须一项有值；如果传多个值，以taskId > agentId > agentTag 优先级查询。只查1次优先值。",
        "en": "The task ID in the decision system. This parameter can be left empty. At least one of the following parameters must have a value: TaskId, AgentId, and AgentTag. If multiple values are specified, the query is performed based on the following priority: TaskId > AgentId > AgentTag. Only the parameter with the highest priority is used for the query."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "行业版-查询聊天记录",
    "en": "Query chat records for the industry edition"
  },
  "descriptions": {
    "zh": "行业版查询聊天记录",
    "en": "Queries chat records for the industry edition."
  }
}