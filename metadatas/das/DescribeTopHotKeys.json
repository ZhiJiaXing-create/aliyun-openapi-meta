{
  "name": "DescribeTopHotKeys",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConsoleContext",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备用参数。",
        "en": "A reserved parameter."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "查询任务结束时间，格式为Unix时间戳，单位为毫秒。\n> - 查询任务结束时间需晚于查询任务开始时间。\n> - 仅支持最大查询时间范围为4天内的数据。\n> - **StartTime**和**EndTime**最大查询间隔为3小时内。",
        "en": "The end of the query time range, specified as a UNIX timestamp in milliseconds.\n\n> - The end time must be later than the start time.\n>\n> - You can query data within the last four days.\n>\n> - The maximum time interval between **StartTime** and **EndTime** is three hours."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Redis实例ID。",
        "en": "The ID of the Redis instance."
      }
    },
    {
      "name": "NodeId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Redis实例的数据分片ID。",
        "en": "The ID of the data shard of the Redis instance."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "查询任务开始时间，格式为Unix时间戳，单位为毫秒。",
        "en": "The start of the query time range, specified as a UNIX timestamp in milliseconds."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取排名前100的热Key",
    "en": "DescribeTopHotKeys"
  },
  "descriptions": {
    "zh": "当某个Key的QPS大于3,000时会被记录为热点Key。\n\n- 在使用阿里云SDK时，需要保证aliyun-sdk-core的版本大于4.3.3，建议使用最新版本。\n- 使用DAS的SDK的版本为1.0.2或以上。\n- 在使用SDK调用DAS服务时，需要将地域指定为cn-shanghai。\n- 本接口只适用于如下Redis实例：\n    - 实例为Redis开源版或Tair（企业版）内存型。\n    - 实例的小版本为最新。",
    "en": "A key is identified as a hot key if its QPS exceeds 3,000.\n\n- If you use an Alibaba Cloud SDK, ensure that the version of aliyun-sdk-core is 4.3.3 or later. We recommend using the latest version.\n\n- The DAS SDK version must be 1.0.2 or later.\n\n- When calling the DAS service with an SDK, set the region to cn-shanghai.\n\n- This operation applies only to the following Redis instances:\n\n  - The instance is an open source Redis instance or a Tair (Enterprise Edition) memory-optimized instance.\n\n  - The instance runs on the latest minor version."
  },
  "example": {
    "unifiedCli": "aliyun das describe-top-hot-keys --console-context None --instance-id r-bp18ff4a195d0000 --node-id r-0000-db-0 --start-time 1596177993000 --end-time 1596177993001 --region cn-hangzhou",
    "legacyCli": "aliyun das DescribeTopHotKeys --region cn-hangzhou --ConsoleContext None --EndTime 1596177993001 --InstanceId 'r-bp18ff4a195d0000' --NodeId 'r-0000-db-0' --StartTime 1596177993000"
  }
}