{
  "name": "FilePutUserTags",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/v2/file/put_usertags",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求体参数。",
        "en": "The request body."
      }
    },
    {
      "name": "domain_id",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "security": [
    "BearerToken",
    "AK"
  ],
  "title": {
    "zh": "添加文件自定义标签",
    "en": "FilePutUserTags"
  },
  "descriptions": {
    "zh": "此接口为增量更新操作：\n\n- 若此次请求中存在同名标签则会覆盖之前的值\n\n- 若标签不存在则新增\n\n- 历史非同名标签不受影响。",
    "en": "This operation performs an incremental update:\n\n- If a tag with the same key already exists, its value is overwritten.\n\n- If a tag does not exist, it is added.\n\n- Existing tags with different keys are not affected."
  }
}