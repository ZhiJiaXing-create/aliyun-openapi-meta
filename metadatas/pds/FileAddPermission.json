{
  "name": "FileAddPermission",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/v2/file/add_permission",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求体参数。",
        "en": "The request body parameters."
      }
    },
    {
      "name": "domain_id",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "security": [
    "BearerToken",
    "AK"
  ],
  "title": {
    "zh": "文件共享授权给用户或组",
    "en": "Grant file sharing authorization to users or groups"
  },
  "descriptions": {
    "zh": "单Drive下最多可支持1000条授权记录，超出后授权不生效。",
    "en": "Each drive supports a maximum of 1000 authorization records. Authorization records that exceed this limit do not take effect."
  }
}