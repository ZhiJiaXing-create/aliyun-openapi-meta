{
  "name": "GetShareLinkToken",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/v2/share_link/get_share_token",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求体参数。",
        "en": "The request body."
      }
    },
    {
      "name": "domain_id",
      "position": "Host",
      "type": "String",
      "required": true
    }
  ],
  "security": [
    "Anonymous"
  ],
  "title": {
    "zh": "获取分享令牌",
    "en": "GetShareLinkToken"
  },
  "descriptions": {
    "zh": "文件分享中文件访问流程要求必须先获取share token，即使此分享的share_pwd=\"\"(非私密分享)。",
    "en": "A share token is required to access any shared file, including public ones that do not have a password (where the `share_pwd` parameter is empty)."
  }
}