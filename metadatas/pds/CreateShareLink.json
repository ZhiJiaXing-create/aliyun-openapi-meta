{
  "name": "CreateShareLink",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/v2/share_link/create",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求体参数。",
        "en": "The request body."
      }
    },
    {
      "name": "domain_id",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "en": "The domain ID."
      }
    }
  ],
  "security": [
    "BearerToken",
    "AK"
  ],
  "title": {
    "zh": "创建分享",
    "en": "Create Share"
  },
  "descriptions": {
    "zh": "分享又称之为文件分享，是一个文件视图容器，通过其可将用户空间中的一个或多个文件暴露给匿名用户访问，匿名用户在分享权限的约束下访问其中的文件。",
    "en": "A share link allows anonymous users to access one or more files from a user's drive. The permissions for the share link determine the level of access."
  }
}