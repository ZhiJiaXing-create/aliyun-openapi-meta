{
  "name": "DownloadFile",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/v2/file/download",
  "parameters": [
    {
      "name": "domain_id",
      "position": "Host",
      "type": "String",
      "required": true
    },
    {
      "name": "drive_id",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "空间id",
        "en": "The ID of the drive."
      }
    },
    {
      "name": "file_id",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件id",
        "en": "The ID of the file."
      }
    },
    {
      "name": "image_thumbnail_process",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "图片处理样式，设置后会跳转至处理后的缩略图地址",
        "en": "The image processing style. Specifying this parameter redirects the request to the URL of the processed thumbnail."
      }
    },
    {
      "name": "office_thumbnail_process",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文档处理样式，设置后会跳转至处理后的缩略图地址",
        "en": "The document processing style. Specifying this parameter redirects the request to the URL of the processed thumbnail."
      }
    },
    {
      "name": "share_id",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分享id。 如果通过分享操作文件，请携带`x-share-token header`鉴权并传递此字段，此时`drive_id`无效。否则请使用`aksk`或者`access_token`方式鉴权，并传递`drive_id`字段。 `share_id`和`drive_id`两者必须有一个字段赋值。",
        "en": "The ID of the share. If you access a file by using a share link, you must authenticate with the `x-share-token` header and specify this parameter. In this case, the `drive_id` parameter is not applicable. Otherwise, you must authenticate by using `AK/SK` or an `access_token` and specify the `drive_id` parameter. You must specify either `share_id` or `drive_id`."
      }
    },
    {
      "name": "video_thumbnail_process",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "视频处理样式，设置后会跳转至处理后的缩略图地址",
        "en": "The video processing style. Specifying this parameter redirects the request to the URL of the processed thumbnail."
      }
    }
  ],
  "security": [
    "AK",
    "BearerToken"
  ],
  "title": {
    "zh": "下载文件",
    "en": "DownloadFile"
  },
  "descriptions": {
    "zh": "<props=\"china\">最佳实践可参考：[文件下载](~~175889~~)。</props>",
    "en": "<props=\"china\">\n\nFor best practices, see [file download](~~175889~~).\n\n</props>"
  }
}