{
  "name": "ResetSmartAccessGatewayClientUserPassword",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Password",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端的新密码。\n\n- 如果您不输入本参数，系统将随机生成一个新密码。\n- 如果您输入本参数，则表示您自定义新密码。\n        \n        密码长度为8~32个字符，必须以大小写字母或数字开头，可以包含下划线（_） 、at（@）和短划线（-）。\n\n系统重置密码后，会通过邮件向您发送新的密码。",
        "en": "The new password.\n\n*   If you do not specify a new password, the system generates a random password.\n\n*   If you specify a new password, the password is reset to the specified new password.\n\n    The password must be 8 to 32 characters in length, and can contain letters, digits, underscores (\\_), at signs (@), periods (.), and hyphens (-). It must start with a letter or a digit.\n\nAfter the password is reset, an email that contains the new password is sent to the email address of the client account."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "智能接入网关App实例所属的地域ID。",
        "en": "The ID of the region where the SAG app instance is deployed."
      }
    },
    {
      "name": "SmartAGId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "智能接入网关App实例ID。",
        "en": "The ID of the SAG app instance."
      }
    },
    {
      "name": "UserName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待重置密码的客户端的用户名。",
        "en": "The username of the client account for which you want to reset the password."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "重置智能接入网关APP客户端密码",
    "en": "ResetSmartAccessGatewayClientUserPassword"
  }
}