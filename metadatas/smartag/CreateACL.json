{
  "name": "CreateACL",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AclType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问控制实例待关联的智能接入网关实例类型。取值：\n\n- **acl-hardware**（默认值）：智能接入网关硬件实例。\n- **acl-software**：智能接入网关App实例。",
        "en": "The type of the Smart Access Gateway instance to be associated with the access control instance. Valid values:\n\n- **acl-hardware** (default): Smart Access Gateway device instance.\n- **acl-software**: SAG app instance."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制实例名称。\n\n名称长度为2~128个字符，以大小写字母或中文开头，可包含数字、下划线（_）和短划线（-）。",
        "en": "The name of the access control instance.\n\nThe name must be 2 to 128 characters in length and must start with a letter or a Chinese character. It can contain digits, underscores (_), and hyphens (-)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制实例所属的地域ID。\n\n您可以通过调用[DescribeRegions](~~69813~~)接口获取地域ID。",
        "en": "The region ID of the access control instance.\n\nYou can call the [DescribeRegions](~~69813~~) operation to query region IDs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建访问控制",
    "en": "Create an access control instance"
  }
}