{
  "name": "SynchronizeSmartAGWebConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "智能接入网关地域ID。",
        "en": "The ID of the region where the SAG instance is deployed."
      }
    },
    {
      "name": "SmartAGId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "智能接入网关实例ID。",
        "en": "The ID of the SAG instance."
      }
    },
    {
      "name": "SmartAGSn",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "智能接入网关设备序列号。",
        "en": "The serial number of the SAG device."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "同步智能接入网关设备配置到云上",
    "en": "SynchronizeSmartAGWebConfig"
  },
  "descriptions": {
    "zh": "**SynchronizeSmartAGWebConfig**接口属于异步接口，即系统返回一个请求ID，但该智能接入网关设备尚未同步到云上，系统后台的同步任务仍在进行。您可以调用[DescribeSAGDeviceInfo](~~164279~~)查询智能接入网关设备的同步状态：\n- 当智能接入网关设备处于**Synchronizing**状态时，表示智能接入网关设备正在同步中。\n- 当智能接入网关设备处于**Synchronized**状态时，表示智能接入网关设备同步成功。",
    "en": "**SynchronizeSmartAGWebConfig** is an asynchronous operation. After you send a request, the request ID is returned but the operation is still being performed in the system background. You can call the [DescribeSAGDeviceInfo](~~164279~~) operation to query the status of an SAG device.\n\n*   If an SAG device is in the **Synchronizing** state, the settings of the SAG device are being synchronized to the associated SAG instance.\n*   If an SAG device is in the **Synchronized** state, the settings of the SAG device are synchronized to the associated SAG instance."
  }
}