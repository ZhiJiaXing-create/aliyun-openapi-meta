{
  "name": "DeleteACLRule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AclId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要删除的访问规则所属的访问控制ID。",
        "en": "The ID of the ACL to which the ACL rule to be deleted belongs."
      }
    },
    {
      "name": "AcrId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要删除的访问控制规则ID。",
        "en": "The ID of the ACL rule to be deleted."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要删除的访问控制规则所属地域ID。",
        "en": "The ID of the region to which the ACL rule to be deleted belongs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除访问控制规则",
    "en": "DeleteACLRule"
  }
}