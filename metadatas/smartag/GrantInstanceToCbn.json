{
  "name": "GrantInstanceToCbn",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CcnInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云连接网实例ID。",
        "en": "The ID of the CCN instance."
      }
    },
    {
      "name": "CenInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云企业网实例ID。",
        "en": "The ID of the CEN instance."
      }
    },
    {
      "name": "CenUid",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "云企业网实例所属的阿里云账号（主账号）ID。",
        "en": "The ID of the Alibaba Cloud account to which the CEN instance belongs."
      }
    },
    {
      "name": "GrantTrafficService",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否给跨账号的云企业网实例授权云连接网实例的流量服务能力。\n\n- **true**：授权。\n- **false**：（默认值）不授权。\n\n>在您授权成功后，如果云连接网下的智能接入网关实例开启了安全引流功能，则不允许取消授权。",
        "en": "Specifies whether to grant the CEN instance permissions to manage network traffic from the CCN instance. Valid values:\n\n*   **true**: grants permissions.\n*   **false**: does not grant permissions. This is the default value.\n\n>  If you set the value to true and the SAG instance connected to the CCN instance has the secure rerouting feature enabled, you cannot revoke the permissions."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云连接网实例所属的地域ID。\n您可以调用[DescribeRegions](~~69813~~l)接口查询智能接入网关支持的地域以及对应的地域ID。",
        "en": "The ID of the region where the CCN instance is deployed.\n\nYou can call the [DescribeRegions](~~69813~~l) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "将实例授权给跨账号云企业网实例",
    "en": "GrantInstanceToCbn"
  }
}