{
  "name": "DescribeSagDropTopN",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "智能接入网关实例地域ID。",
        "en": "The region ID of the Smart Access Gateway instance."
      }
    },
    {
      "name": "Size",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询智能接入网关实例的数量。默认值：**10**，且不支持修改。",
        "en": "The number of Smart Access Gateway instances to query. Default value: **10**. This value cannot be modified."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询指定地域丢包率前10名的实例",
    "en": "Query the top 10 instances with the highest packet loss rate in a specified region"
  }
}