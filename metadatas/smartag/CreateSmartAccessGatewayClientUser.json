{
  "name": "CreateSmartAccessGatewayClientUser",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Bandwidth",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "带宽峰值。单位：Kbps。取值范围：**1 Kbps~20000 Kbps**。默认值：**2000 Kbps**。",
        "en": "The maximum bandwidth value. Unit: Kbit/s. Valid values: **1 to 20000**. Default value: **2000**."
      }
    },
    {
      "name": "ClientIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "- 如果开启客户端App服务，需要设置App的IP地址。当前账号始终保持以选择的IP地址接入。\n\n> 设置App的IP地址必须在私网网段内。\n\n- 如果关闭客户端App服务，系统自动从私网网段内分配可用IP地址，每次重连IP地址都会重新分配。",
        "en": "*   If you enable the client app service, you must set the IP address of the client app. The current client account uses the specified IP address to connect to Alibaba Cloud.\n\n>  The IP address must fall within a private CIDR block.\n\n*   If you disable the client app service, an IP address within a private CIDR block is assigned to the client account. Each connection to Alibaba Cloud uses a different IP address."
      }
    },
    {
      "name": "Password",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "登录智能接入网关App的密码。\n\n密码命名规则： 8~32位字符，以（A-Za-z0-9）开头，可包含数字、英文大小写、下划线（_）、at（@）、短划线（-）。",
        "en": "The password that is used to log on to the SAG app.\n\nThe password must be 8 to 32 characters in length. It can contain letters, digits, underscores (\\_), at signs (@), and hyphens (-). It must start with a letter or a digit."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "智能接入网关App实例所在地域ID。",
        "en": "The region ID of the Smart Access Gateway (SAG) app instance."
      }
    },
    {
      "name": "SmartAGId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "智能接入网关App实例ID。",
        "en": "The ID of the SAG app instance."
      }
    },
    {
      "name": "UserMail",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "普通用户的邮箱地址，用于管理员向普通用户发送登录App的账号信息。",
        "en": "The email address of the client account. The username and password are sent to the specified email address by the administrator."
      }
    },
    {
      "name": "UserName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户名，同一个智能接入网关App实例下的用户名不可以重复。\n名称长度为7~33个字符，必须以大小写字母或数字开头，可以包含下划线（_） 、at（@）、半角句号（.）和短划线（-）。\n\n> 用户名和密码相互依赖，若指定用户名则必须指定密码，反之，若指定密码则必须指定用户名。",
        "en": "The username of the client account. The usernames of client accounts added to the same SAG app instance must be unique.\n\nThe username must be 7 to 33 characters in length, and can contain letters, digits, underscores (\\_), at signs (@), periods (.), and hyphens (-). It must start with a letter or a digit.\n\n>  For a client account, if you specify the username, you must also specify the password. If you specify the password, you must specify the username."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建用户",
    "en": "CreateSmartAccessGatewayClientUser"
  }
}