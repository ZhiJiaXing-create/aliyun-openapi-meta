{
  "name": "DescribeFlowLogSags",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FlowLogId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "流日志实例ID。",
        "en": "The flow log instance ID."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询列表的页码，起始值为**1**，默认值为**1**。",
        "en": "The page number of the query list. Minimum value: **1**. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定分页查询时每页的列表行数，最大值为**50**，默认值为**10**。",
        "en": "The number of entries per page in a paged query. Maximum value: **50**. Default value: **10**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "流日志的地域ID。",
        "en": "The region ID of the flow log."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询流日志绑定的智能接入网关实例",
    "en": "Query smart access gateway instances associated with a flow log"
  }
}