{
  "name": "GetCatalogSummaryTrend",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/dlf/v1/[catalogId]/storage-summary/trend",
  "parameters": [
    {
      "name": "catalogId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据目录ID。",
        "en": "The ID of the catalog."
      }
    },
    {
      "name": "endDate",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "结束日期（yyyy-MM-dd格式，结果包含结束日期）。",
        "en": "The end date, in yyyy-MM-dd format."
      }
    },
    {
      "name": "startDate",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "起始日期（yyyy-MM-dd格式，结果包含起始日期）。",
        "en": "The start date, in yyyy-MM-dd format."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取数据目录存储概览趋势",
    "en": "GetCatalogSummaryTrend"
  }
}