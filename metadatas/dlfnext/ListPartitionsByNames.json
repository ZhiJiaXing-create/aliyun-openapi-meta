{
  "name": "ListPartitionsByNames",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/dlf/v1/[catalogId]/databases/[database]/tables/[table]/partitions/list-by-names",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    },
    {
      "name": "catalogId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据目录ID。",
        "en": "The ID of the data catalog."
      }
    },
    {
      "name": "database",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据库名称。",
        "en": "The name of the database."
      }
    },
    {
      "name": "table",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "表名称。",
        "en": "The name of the table."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "根据名称获取分区列表",
    "en": "Retrieve partitions by name"
  }
}