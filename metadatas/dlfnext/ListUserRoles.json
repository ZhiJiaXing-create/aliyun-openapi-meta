{
  "name": "ListUserRoles",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/dlf/v1/auth/users/roles/list",
  "parameters": [
    {
      "name": "maxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页大小。",
        "en": "The number of entries per page."
      }
    },
    {
      "name": "pageToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "翻页令牌，用于获取下一页数据。如果响应结果中没有提供，则传空字符串（\"\"）或空字符（''）。",
        "en": "The pagination token used to retrieve the next page of data. If the response does not provide this token, pass an empty string (\"\")."
      }
    },
    {
      "name": "userPrincipal",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户的资源描述符。",
        "en": "The resource descriptor for the user."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取用户角色列表",
    "en": "ListUserRoles"
  }
}