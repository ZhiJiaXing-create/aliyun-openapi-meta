{
  "name": "ListRoles",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/dlf/v1/auth/roles/list",
  "parameters": [
    {
      "name": "maxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页大小。",
        "en": "The number of entries to return on each page."
      }
    },
    {
      "name": "pageToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "翻页令牌，用于获取下一页数据。如果响应结果中没有提供，则传空字符串（\"\"）或空字符（''）。",
        "en": "The token used to retrieve the next page of results. If the response does not return this parameter, pass an empty string (\"\")."
      }
    },
    {
      "name": "roleName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "角色名称。",
        "en": "The role name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取角色列表",
    "en": "ListRoles"
  }
}