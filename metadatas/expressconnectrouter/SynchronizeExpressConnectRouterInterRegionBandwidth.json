{
  "name": "SynchronizeExpressConnectRouterInterRegionBandwidth",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "A client-generated token that ensures the idempotence of the request. The token must be unique for each request and can contain only ASCII characters.\n\nGenerate a unique value from your client for each request. The ClientToken parameter can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** of each API request is different."
      }
    },
    {
      "name": "DryRun",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n\n- **true**：发送检查请求，不会修改服务资源。\n\n- **false**（默认值）：发送正常请求。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: Sends a check request. The service resource is not modified.\n\n- **false** (default): Sends a normal request."
      }
    },
    {
      "name": "EcrId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "专线网关实例ID。",
        "en": "The ID of the Express Connect Router (ECR) instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "同步专线网关ECR的跨域带宽",
    "en": "SynchronizeExpressConnectRouterInterRegionBandwidth"
  },
  "descriptions": {
    "zh": "专线网关实例为**Active**状态时才允许更新。",
    "en": "You can update an ECR instance only when it is in the **Active** state."
  },
  "example": {
    "unifiedCli": "aliyun expressconnectrouter synchronize-express-connect-router-inter-region-bandwidth --ecr-id ecr-fu8rszhgv7623c0000 --dry-run false --client-token 02fb3da4-130e-11e9-8e44-000000 --region cn-hangzhou",
    "legacyCli": "aliyun expressconnectrouter SynchronizeExpressConnectRouterInterRegionBandwidth --region cn-hangzhou --EcrId 'ecr-fu8rszhgv7623c0000' --DryRun false --ClientToken '02fb3da4-130e-11e9-8e44-000000'"
  }
}