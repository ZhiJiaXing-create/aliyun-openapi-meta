{
  "name": "CheckAddRegionToExpressConnectRouter",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nGenerate a unique token on your client. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system uses the **RequestId** as the **ClientToken**. The **RequestId** of each API request may be different."
      }
    },
    {
      "name": "DryRun",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n\n- true：发送检查请求，不会修改服务资源。\n\n- false（默认值）：发送正常请求。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- true: performs a dry run to check the request. No service resources are modified.\n\n- false (default): sends a normal request."
      }
    },
    {
      "name": "EcrId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "专线网关实例ID。",
        "en": "The ID of ECR."
      }
    },
    {
      "name": "FreshRegionId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定专线网关功能是否开通云数据传输服务的地域。",
        "en": "The ID of the region that you want to add to ECR."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "检查指定的地域加入专线网关（ECR）所需的云数据传输（CDT）服务",
    "en": "CheckAddRegionToExpressConnectRouter"
  },
  "example": {
    "unifiedCli": "aliyun expressconnectrouter check-add-region-to-express-connect-router --ecr-id ecr-fu8rszhgv7623c0000 --fresh-region-id cn-hangzhou --dry-run false --client-token 02fb3da4-130e-11e9-8e44-000000 --region cn-hangzhou",
    "legacyCli": "aliyun expressconnectrouter CheckAddRegionToExpressConnectRouter --region cn-hangzhou --EcrId 'ecr-fu8rszhgv7623c0000' --FreshRegionId 'cn-hangzhou' --DryRun false --ClientToken '02fb3da4-130e-11e9-8e44-000000'"
  }
}