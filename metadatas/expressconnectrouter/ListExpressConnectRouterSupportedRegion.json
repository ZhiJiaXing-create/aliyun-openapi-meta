{
  "name": "ListExpressConnectRouterSupportedRegion",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "A client token to ensure the idempotence of the request.\n\nGenerate a value that is unique across different requests. The client token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system uses the **RequestId** as the **ClientToken**. Each request has a unique **RequestId**."
      }
    },
    {
      "name": "NodeType",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网络实例的类型。取值：\n- **TR**：转发路由器实例。\n- **VBR**：边界路由器实例。\n- **VPC**：虚拟专有网络实例。",
        "en": "The type of network instance. Valid values:\n\n- **TR**: transit router instance.\n\n- **VBR**: virtual border router instance.\n\n- **VPC**: virtual private cloud instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询专线网关（ECR）开服地域列表",
    "en": "ListExpressConnectRouterSupportedRegion"
  },
  "example": {
    "unifiedCli": "aliyun expressconnectrouter list-express-connect-router-supported-region --node-type VBR --client-token 02fb3da4-130e-11e9-8e44-000000 --region cn-hangzhou",
    "legacyCli": "aliyun expressconnectrouter ListExpressConnectRouterSupportedRegion --region cn-hangzhou --NodeType VBR --ClientToken '02fb3da4-130e-11e9-8e44-000000'"
  }
}