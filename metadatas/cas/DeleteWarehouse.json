{
  "name": "DeleteWarehouse",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "WarehouseInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "证书仓库实例ID。",
        "en": "The certificate warehouse instance ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除证书应用仓库",
    "en": "DeleteWarehouse"
  },
  "descriptions": {
    "zh": "本接口一般用于查询证书仓库列表。\n\n### QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用将会被限流，这可能影响您的业务，请合理调用。",
    "en": "This operation deletes a certificate warehouse.\n\n### QPS limit\n\nThis operation has a QPS limit of 10 requests per second per user. Exceeding this limit causes subsequent API calls to be throttled, which can impact your services. To ensure service availability, call this operation at a reasonable rate."
  },
  "example": {
    "unifiedCli": "aliyun cas delete-warehouse --warehouse-instance-id cas-wh-Q7ID6V --api-version 2020-04-07 --region cn-hangzhou",
    "legacyCli": "aliyun cas DeleteWarehouse --region cn-hangzhou --WarehouseInstanceId 'cas-wh-Q7ID6V'"
  }
}