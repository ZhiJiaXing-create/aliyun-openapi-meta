{
  "name": "ListAssetCount",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页，当前页，默认1。",
        "en": "The page number. Default value: 1."
      }
    },
    {
      "name": "EndDate",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "证书到期日期。",
        "en": "The expiration date of the certificate."
      }
    },
    {
      "name": "ShowSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页，每页展示数据量。",
        "en": "The number of entries to return on each page. Default value: 50."
      }
    },
    {
      "name": "StartDate",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "证书签发日期。",
        "en": "The date of certificate issuance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询云产品资源统计列表",
    "en": "ListAssetCount"
  },
  "descriptions": {
    "zh": "本接口用于查询您已创建的CA证书（包括根CA证书、子CA证书）的数量。\n\n## QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用将会被限流，这可能影响您的业务，请合理调用。",
    "en": "Queries the number of created Certificate Authority (CA) certificates, including root and subordinate CA certificates.\n\n## QPS limit\n\nEach user can make up to 10 queries per second (QPS). If you exceed this limit, the system applies rate limiting to your API calls. This may affect your business. Make API calls at a reasonable rate."
  },
  "example": {
    "unifiedCli": "aliyun cas list-asset-count --start-date 1 --end-date 1 --current-page 1 --show-size 1 --api-version 2020-04-07 --region cn-hangzhou",
    "legacyCli": "aliyun cas ListAssetCount --region cn-hangzhou --StartDate 1 --CurrentPage 1 --ShowSize 1 --EndDate 1"
  }
}