{
  "name": "BatchUpdateNoticeStatus",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Ids",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待同步到证书服务的主键标识列表。多个ID使用半角逗号（,）间隔。",
        "en": "The list of primary key identifiers to be synchronized to Certificate Management Service. Separate multiple IDs with commas (,)."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定请求和接收消息的语言类型。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language type for the request and the received message. Valid values:\n\n- **zh**: Chinese\n\n- **en**: English"
      }
    },
    {
      "name": "NoticeBiz",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "消息通知的业务编码。此处固定为ssl。",
        "en": "The business code of message notification. The value is fixed as ssl."
      }
    },
    {
      "name": "NoticeStatus",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "是否开启消息通知。\n--enable：开启。\n--disable：关闭。",
        "en": "Specifies whether to enable message notification.\n\\--enable: enables message notification.\n\\--disable: disables message notification."
      }
    },
    {
      "name": "SourceIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定请求的来源IP地址。",
        "en": "The source IP address of the request."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新消息通知的状态",
    "en": "Updates the status of message notification"
  },
  "descriptions": {
    "zh": "CA证书经创建后，默认为正常签发状态。您可以调用本接口将CA证书的状态从正常签发修改为吊销。在正常签发状态下，CA证书可用于签发证书。在吊销状态下，CA证书不可用于签发证书，同时经该CA证书签发的证书也会相应失效。\n\n调用本接口前，您必须已经调用[CreateRootCACertificate](~~465962~~)创建了根CA证书、调用[CreateSubCACertificate](~~465959~~)创建了子CA证书。\n\n\n## QPS限制\n\n本接口的单用户QPS限制为10次/秒。超过限制，API调用将会被限流，这可能影响您的业务，请合理调用。",
    "en": "After a CA certificate is created, it is in the normal issuance state by default. You can call this operation to change the status of a CA certificate from normal issuance to revoked. In the normal issuance state, the CA certificate can be used to issue certificates. In the revoked state, the CA certificate cannot be used to issue certificates, and the certificates that have been issued by the CA certificate also become invalid accordingly.\n\nBefore you call this operation, you must have called [CreateRootCACertificate](~~465962~~) to create a root CA certificate and called [CreateSubCACertificate](~~465959~~) to create a sub CA certificate.\n\n## QPS limit\n\nThe QPS limit per user for this operation is 10 calls per second. If the limit is exceeded, API calls are throttled, which may affect your business. Call this operation properly."
  },
  "example": {
    "unifiedCli": "aliyun cas batch-update-notice-status --source-ip 47.98.242.200 --lang zh --ids 888 --notice-status disable --notice-biz ssl --api-version 2020-04-07 --region cn-hangzhou",
    "legacyCli": "aliyun cas BatchUpdateNoticeStatus --region cn-hangzhou --SourceIp '47.98.242.200' --NoticeStatus disable --NoticeBiz ssl --Ids 888 --Lang zh"
  }
}