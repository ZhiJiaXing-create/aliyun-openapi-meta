{
  "name": "ModifySearcherReplica",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "PUT",
  "pathPattern": "/openapi/ha3/instances/[instanceId]/replica",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求体",
        "en": "The request body."
      }
    },
    {
      "name": "instanceId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID",
        "en": "The instance ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改数据节点副本分片数",
    "en": "ModifySearcherReplica"
  },
  "descriptions": {
    "zh": "## 请求说明\n本API允许用户修改特定实例下的数据节点副本数或分片数。请求时，需提供实例ID，并在请求体中指定要修改的`replica`（副本数）或`partition`（分片数）。请注意，这两个参数都是可选的，但至少需要提供其中一个以执行更新操作。",
    "en": "## Request description\n\nThis API lets you modify the number of replicas or shards for data nodes in a specific instance. When you make a request, provide the instance ID. In the request body, specify the number of replicas (`replica`) or shards (`partition`). These parameters are optional, but you must specify at least one of them to update the instance."
  },
  "example": {
    "unifiedCli": "aliyun searchengine modify-searcher-replica --instance-id ha-cn-test --replica 2 --partition 2 --region cn-hangzhou",
    "legacyCli": "aliyun searchengine PUT /openapi/ha3/instances/ha-cn-test/replica --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"partition\":2,\"replica\":2}"
  }
}