{
  "name": "ExecuteRequestSG",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "语言。",
        "en": "The language."
      }
    },
    {
      "name": "Service",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "服务产品码。如果是自定义事件，这里填saf_de。",
        "en": "The service product code. If you use a custom event, set this parameter to saf_de."
      }
    },
    {
      "name": "ServiceParameters",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "服务入参，Json格式的字符串，完整Json字符串信息。如果是自定义事件，这里需要添加eventCode属性。",
        "en": "The service input parameters. The value is a JSON-formatted string that contains the complete JSON information. If you use a custom event, add the eventCode property."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "调用决策引擎新加坡集群",
    "en": "Call the decision engine singapore cluster"
  },
  "example": {
    "unifiedCli": "aliyun saf execute-request-sg --service-parameters accountId_:_1233000 --service account_abuse_intl_pro --lang en --region cn-hangzhou",
    "legacyCli": "aliyun saf ExecuteRequestSG --region cn-hangzhou --Service account_abuse_intl_pro --Lang en --ServiceParameters 'accountId_:_1233000'"
  }
}