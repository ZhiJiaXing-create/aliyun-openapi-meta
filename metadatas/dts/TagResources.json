{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID，传入本参数来指定实例所在地域，详情请参见[支持的地域列表](~~141033~~)。",
        "en": "The region ID. Specify this parameter to indicate the region where the instance resides. For more information, see [Supported regions](~~141033~~)."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The resource group ID."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "数据迁移、同步和订阅的实例ID，可以通过调用[DescribeDtsJobs](~~209702~~)接口查询。\n\n> N表示传入第几个实例ID。例如ResourceId.0表示传入第一个实例ID；ResourceId.1表示传入第二个实例ID。",
        "en": "The ID of the data migration, synchronization, or change tracking instance. You can call the [DescribeDtsJobs](~~209702~~) operation to query instance IDs.\n\n> N specifies the sequence number of the instance ID. For example, ResourceId.0 specifies the first instance ID, and ResourceId.1 specifies the second instance ID."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源类型定义，固定取值为：**ALIYUN::DTS::INSTANCE**。",
        "en": "The resource type. Set the value to **ALIYUN::DTS::INSTANCE**."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "要绑定的标签（键值对）列表。",
        "en": "The list of tags (key-value pairs) to bind."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "标签的键。\n> - N表示传入第几个标签的键。例如Tag.0.Key表示传入第一个标签的键；Tag.1.Key表示传入第二个标签的键。\n- 不允许传入空字符串。",
            "en": "The key of the tag.\n> - N specifies the sequence number of the tag key. For example, Tag.0.Key specifies the key of the first tag, and Tag.1.Key specifies the key of the second tag.\n- Empty strings are not allowed."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "标签的值。\n> - N表示传入第几个标签的值。例如Tag.0.Value表示传入第一个标签的值；Tag.1.Value表示传入第二个标签的值。\n- 允许传入空字符串。",
            "en": "The value of the tag.\n> - N specifies the sequence number of the tag value. For example, Tag.0.Value specifies the value of the first tag, and Tag.1.Value specifies the value of the second tag.\n- Empty strings are allowed."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建标签",
    "en": "Create tags"
  },
  "descriptions": {
    "zh": "在实例数量较多的情况下，您可以创建多个标签，为实例绑定不同的标签对其进行分类，之后通过标签进行实例筛选。\n\n- 标签由一对键（key）值（value）组成，键在同账号同地域下唯一，值无此限制。\n- 若设置的标签不存在，则自动创建该标签并绑定到目标实例。\n- 若实例已经绑定了有相同键的标签，则进行覆盖绑定。\n- 每个实例最多可以绑定20个标签。\n- 每次调用最多设置50个实例进行批量标签绑定。",
    "en": "If you have a large number of instances, you can create multiple tags and attach different tags to instances for categorization. Then, you can filter instances by tag.\n\n- A tag consists of a key-value pair. Tag keys must be unique within the same Alibaba Cloud account and region. Tag values do not have this restriction.\n- If the specified tag does not exist, the tag is automatically created and attached to the destination instance.\n- If the instance already has a tag with the same key, the existing tag is overwritten.\n- You can attach up to 20 tags to each instance.\n- You can invoke the operation to attach tags to up to 50 instances at a time."
  },
  "example": {
    "unifiedCli": "aliyun dts tag-resources --biz-region-id cn-hangzhou --resource-type ALIYUN::DTS::INSTANCE --resource-id dtsntk10k6r12v0000 --tag Value=testvalue1 Key=testkey1 --resource-group-id rg-acfmzawhxxc0000 --api-version 2020-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun dts TagResources --region cn-hangzhou --ResourceId.1 dtsntk10k6r12v0000 --Tag.1.Key testkey1 --ResourceGroupId 'rg-acfmzawhxxc0000' --Tag.1.Value testvalue1 --ResourceType 'ALIYUN::DTS::INSTANCE' --RegionId 'cn-hangzhou'"
  }
}