{
  "name": "DeleteAcrImageTags",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ArtifactType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "部署物类型。默认值为：AcrImage。可能的值：\n\n- HelmChart：Helm Chart镜像。\n\n- AcrImage：容器镜像。",
        "en": "The type of the artifact. The default value is AcrImage. Valid values:\n\n- HelmChart: A Helm Chart image.\n\n- AcrImage: A container image."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。**ClientToken**只支持ASCII字符，且不能超过64个字符。",
        "en": "A client token that you can use to ensure the idempotence of the request. This token is generated by your client and must be unique for each request. **ClientToken** can contain only ASCII characters and must be no more than 64 characters long."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域id",
        "en": "The ID of the region."
      }
    },
    {
      "name": "RepoId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "镜像仓库id",
        "en": "The ID of the image repository."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "容器镜像标签",
        "en": "The tag of the container image."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除容器镜像版本",
    "en": "DeleteAcrImageTags"
  },
  "descriptions": {
    "zh": "### 前提条件\n删除前请确保已删除容器镜像对应的部署物，删除后对应的部署物会无法使用",
    "en": "###\n\nYou must delete the deployments that use the container image before you delete the image tag. After the tag is deleted, the corresponding deployments become unusable."
  },
  "example": {
    "unifiedCli": "aliyun computenestsupplier delete-acr-image-tags --biz-region-id cn-hangzhou --repo-id crr-3gqhkza0wbxxxxxx --tag 1.0 --client-token 788E7CP0EN9D51P --artifact-type AcrImage --region cn-hangzhou",
    "legacyCli": "aliyun computenestsupplier DeleteAcrImageTags --region cn-hangzhou --ArtifactType AcrImage --RegionId 'cn-hangzhou' --Tag '1.0' --RepoId 'crr-3gqhkza0wbxxxxxx' --ClientToken 788E7CP0EN9D51P"
  }
}