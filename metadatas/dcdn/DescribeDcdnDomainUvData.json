{
  "name": "DescribeDcdnDomainUvData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加速域名，仅支持查询单个域名。\n\n默认查询所有域名。",
        "en": "The accelerated domain name. You can query data for only one domain name at a time.\n\nIf you leave this parameter empty, data for all your accelerated domain names is queried."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取数据结束时间点。日期格式按照ISO8601表示法，并使用UTC时间，格式为yyyy-MM-ddTHH:mm:ssZ。\n\n结束时间需大于起始时间。",
        "en": "The end of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.\n\nThe end time must be later than the start time."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取数据起始时间点。日期格式按照ISO8601表示法，并使用UTC时间，格式为yyyy-MM-ddTHH:mm:ssZ。",
        "en": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询UV页面访问统计",
    "en": "DescribeDcdnDomainUvData"
  },
  "descriptions": {
    "zh": "- 不指定StartTime和EndTime时，默认读取过去24小时的数据；指定StartTime和EndTime时，按指定的起止时间查询。\n- 只支持查询一个域名或当前用户下所有域名。",
    "en": "- If you do not specify StartTime and EndTime, data from the last 24 hours is queried by default. If you specify StartTime and EndTime, data in the specified time range is queried.\n\n- You can query data for a single domain name or all domain names that belong to your account."
  },
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-domain-uv-data --domain-name example.com --start-time 2015-11-29T00:00:00Z --end-time 2015-11-30T00:00:00Z --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnDomainUvData --region cn-hangzhou --EndTime '2015-11-30T00:00:00Z' --DomainName 'example.com' --StartTime '2015-11-29T00:00:00Z'"
  }
}