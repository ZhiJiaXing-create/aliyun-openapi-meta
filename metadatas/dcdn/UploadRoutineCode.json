{
  "name": "UploadRoutineCode",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CodeDescription",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "代码版本描述。",
        "en": "The description of the code version."
      }
    },
    {
      "name": "Name",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "边缘函数名, 同一个账号下命名唯一。",
        "en": "The name of the edge function. The name must be unique within the same account."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "上传边缘函数ER的代码",
    "en": "Upload edge function code"
  },
  "descriptions": {
    "zh": "> - 上传一次边缘函数ER的代码，生成一个版本，用于代码管理和发布使用。\n> - 一个边缘函数ER最多保留10个版本代码，超出限制后需要手动调用DeleteRoutineCodeRevision删除无用的版本。\n> - API调用频率：100次/秒。",
    "en": "> - Each upload of edge function code generates a version for code management and publishing.\n> - An edge function can retain a maximum of 10 code versions. If this limit is exceeded, manually call DeleteRoutineCodeRevision to delete unused versions.\n> - API call frequency: 100 calls per second."
  },
  "example": {
    "unifiedCli": "aliyun dcdn upload-routine-code --name test --code-description Hello_World --region cn-hangzhou",
    "legacyCli": "aliyun dcdn UploadRoutineCode --region cn-hangzhou --CodeDescription Hello_World --Name test"
  }
}