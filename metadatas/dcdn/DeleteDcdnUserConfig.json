{
  "name": "DeleteDcdnUserConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FunctionName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要删除的用户功能名称，当前仅支持删除DCDN WAF功能。取值默认为waf。",
        "en": "The name of the feature to delete. Currently, you can only delete the DCDN WAF feature. The default value is waf."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除用户维度的配置",
    "en": "DeleteDcdnUserConfig"
  },
  "example": {
    "unifiedCli": "aliyun dcdn delete-dcdn-user-config --function-name waf --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DeleteDcdnUserConfig --region cn-hangzhou --FunctionName waf"
  }
}