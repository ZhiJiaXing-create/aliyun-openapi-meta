{
  "name": "StopDcdnDomain",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加速域名，仅支持停用单个。",
        "en": "The accelerated domain name. You can disable only one domain name at a time."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "停用域名",
    "en": "StopDcdnDomain"
  },
  "descriptions": {
    "zh": "> - 停用加速域名后，加速域名的配置信息仍然会保留，但是阿里云DCDN将停止为该域名提供加速服务，且该域名会被解析到无效地址“offline.***.com”，无法被继续访问。\n>- 单用户调用频率：30次/秒。",
    "en": "> - After an accelerated domain name is disabled, its configuration information is retained. However, Alibaba Cloud DCDN stops providing acceleration services for the domain name. The domain name is then resolved to an invalid address, \"offline.\\*\\*\\*.com\", and becomes inaccessible.\n>\n> - The call frequency for a single user is 30 calls per second."
  },
  "example": {
    "unifiedCli": "aliyun dcdn stop-dcdn-domain --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun dcdn StopDcdnDomain --region cn-hangzhou --DomainName 'example.com'"
  }
}