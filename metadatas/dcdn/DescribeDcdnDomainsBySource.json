{
  "name": "DescribeDcdnDomainsBySource",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Sources",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "源站列表，多个用半角逗号（,）分隔，最多20个，不支持模糊匹配。",
        "en": "The origin servers. You can specify up to 20 origin servers. Separate multiple origin servers with commas (,). Fuzzy matching is not supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询域名-按源站",
    "en": "DescribeDcdnDomainsBySource"
  },
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-domains-by-source --sources example.org --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnDomainsBySource --region cn-hangzhou --Sources 'example.org'"
  }
}