{
  "name": "DescribeRoutine",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "边缘函数名, 同一个账号下命名唯一。",
        "en": "The name of the edge function. The name must be unique within the same account."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取某个边缘函数ER的元信息",
    "en": "Query metadata of an edge function"
  },
  "descriptions": {
    "zh": "> API调用频率：100次/秒。",
    "en": "> API call frequency: 100 calls per second."
  },
  "example": {
    "unifiedCli": "aliyun dcdn describe-routine --name test --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeRoutine --region cn-hangzhou --Name test"
  }
}