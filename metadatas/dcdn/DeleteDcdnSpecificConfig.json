{
  "name": "DeleteDcdnSpecificConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConfigId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "功能配置ID，多个用半角逗号（,）分隔。ConfigId查询和使用，请参见[ConfigId使用说明](~~410558~~)。",
        "en": "The ID of the feature configuration. To specify multiple IDs, separate them with commas (,). For more information, see [Usage of ConfigId](~~410558~~)."
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "您的加速域名，仅支持单个删除。",
        "en": "The accelerated domain name. You can delete a configuration for only one domain name at a time."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除域名的指定配置",
    "en": "DeleteDcdnSpecificConfig"
  },
  "descriptions": {
    "zh": "> 单用户调用频率：30次/秒。",
    "en": "> The call frequency for a single user is limited to 30 calls per second."
  },
  "example": {
    "unifiedCli": "aliyun dcdn delete-dcdn-specific-config --domain-name example.com --config-id 2117 --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DeleteDcdnSpecificConfig --region cn-hangzhou --DomainName 'example.com' --ConfigId 2117"
  }
}