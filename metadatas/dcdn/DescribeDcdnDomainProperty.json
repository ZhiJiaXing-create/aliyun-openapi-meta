{
  "name": "DescribeDcdnDomainProperty",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待查询的加速域名，只支持查询单个域名。",
        "en": "The accelerated domain name that you want to query. You can query only one domain name at a time."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询IPA的协议类型",
    "en": "DescribeDcdnDomainProperty"
  },
  "descriptions": {
    "zh": "> 单用户调用频率：10次/秒。",
    "en": "> The call frequency for a single user is 10 calls per second."
  },
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-domain-property --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnDomainProperty --region cn-hangzhou --DomainName 'example.com'"
  }
}