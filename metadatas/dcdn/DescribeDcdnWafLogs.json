{
  "name": "DescribeDcdnWafLogs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "域名，只支持单个查询。",
        "en": "The domain name. You can query only one domain name at a time."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询日志结束时间。\n\n日期格式按照ISO8601表示法，并使用UTC+0时间，格式为yyyy-MM-ddTHH:mm:ssZ。",
        "en": "The end of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.\n\nThe date format follows the ISO 8601 notation, uses UTC, and is formatted as yyyy-MM-ddTHH:mm:ssZ."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "查询页数，取值范围：大于0的任意整数。",
        "en": "The page number to return. Valid values: any integer greater than 0."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页大小，默认**300**，取值：**1~1000**。",
        "en": "The number of entries to return on each page. Default value: 300. Valid values: **1** to **1000**."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询日志起始时间。\n\n日期格式按照ISO8601表示法，并使用UTC+0时间，格式为yyyy-MM-ddTHH:mm:ssZ。",
        "en": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.\n\nThe date format follows the ISO 8601 notation and uses UTC+0 time, in the format yyyy-MM-ddTHH:mm:ssZ."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询离线WAF拦截日志下载地址",
    "en": "DescribeDcdnWafLogs"
  },
  "descriptions": {
    "zh": "> - 如果不指定StartTime和EndTime，该接口默认读取过去24小时的数据；指定StartTime和EndTime时，按指定的起止时间查询。\n> - 查询数据的时间粒度为一小时。\n> - 单个用户的调用频率为：100次/秒。",
    "en": "> - If \\`StartTime\\` and \\`EndTime\\` are not specified, data from the last 24 hours is queried by default. If both parameters are specified, data in the specified time range is queried.\n>\n> - The time granularity of the data is one hour.\n>\n> - The throttling limit for a single user is 100 calls per second."
  },
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-waf-logs --domain-name example.com --start-time 2017-12-21T08:00:00Z --end-time 2017-12-22T08:00:00Z --page-size 300 --page-number 1 --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnWafLogs --region cn-hangzhou --EndTime '2017-12-22T08:00:00Z' --PageSize 300 --DomainName 'example.com' --PageNumber 1 --StartTime '2017-12-21T08:00:00Z'"
  }
}