{
  "name": "DescribeDcdnDeletedDomains",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "当前页码，默认值**1**，取值范围：**1**~**100000**。",
        "en": "The number of the page to return. The default value is **1**. Valid values: **1** to **100000**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页显示的域名个数，默认值**20**，取值范围：**1**~**500**之前的任意整数。",
        "en": "The number of domain names to return on each page. The default value is **20**. Valid values: an integer from **1** to **500**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询已删除域名",
    "en": "DescribeDcdnDeletedDomains"
  },
  "descriptions": {
    "zh": "> 单用户调用频率：10次/秒。",
    "en": "> The call frequency is limited to 10 calls per second for each user."
  },
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-deleted-domains --page-number 1 --page-size 5 --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnDeletedDomains --region cn-hangzhou --PageSize 5 --PageNumber 1"
  }
}