{
  "name": "DescribeDcdnDomainIspData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "加速域名，仅支持查询单个域名。\n\n默认查询当前用户下所有域名。",
        "en": "The accelerated domain name. You can query data for only a single domain name.\n\nIf you leave this parameter empty, data for all domain names that belong to your account is queried."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取数据结束时间。日期格式按照ISO8601表示法，并使用UTC时间，格式为YYYY-MM-DDThh:mm:ssZ。\n\n> 结束时间需大于起始时间。",
        "en": "The end of the time range to query. Specify the time in the ISO 8601 standard in the YYYY-MM-DDThh:mm:ssZ format. The time must be in UTC.\n\n> The end time must be later than the start time."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取数据起始时间点。日期格式按照ISO8601表示法，并使用UTC时间，格式为YYYY-MM-DDThh:mm:ssZ。",
        "en": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the YYYY-MM-DDThh:mm:ssZ format. The time must be in UTC."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询用户运营商分布数据",
    "en": "DescribeDcdnDomainIspData"
  },
  "descriptions": {
    "zh": "- 单用户调用频率：100次/秒。\n- 不指定StartTime和EndTime时，默认读取过去24小时的数据，同时支持按指定的起止时间查询，两者需要同时指定；\n-  只支持一个域名，或当前用户下所有域名；\n- 最多可获取最近90天的数据。",
    "en": "- Call frequency: 100 calls per second for each user.\n\n- If you do not specify StartTime and EndTime, data from the last 24 hours is queried by default. To query data within a specific time range, specify both StartTime and EndTime.\n\n- You can query data for a single domain name or for all domain names that belong to your account.\n\n- You can query data generated within the last 90 days."
  },
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-domain-isp-data --domain-name example.com --start-time 2019-12-05T12:00:00Z --end-time 2019-12-06T12:00:00Z --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnDomainIspData --region cn-hangzhou --EndTime '2019-12-06T12:00:00Z' --DomainName 'example.com' --StartTime '2019-12-05T12:00:00Z'"
  }
}