{
  "name": "DescribeDcdnWafUsageData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询域名。若参数为空，默认返回所有加速域名。",
        "en": "The accelerated domain name. If you do not specify this parameter, data for all accelerated domain names is returned."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取数据结束时间。日期格式按照ISO8601表示法，并使用UTC+0时间，格式为yyyy-MM-ddTHH:mm:ssZ。\n\n>结束时间需大于起始时间。",
        "en": "The end of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.\n\n> The end time must be later than the start time."
      }
    },
    {
      "name": "Interval",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询数据的时间粒度，单位为秒。\n\n根据单次查询的最大时间跨度不同，该参数支持300（5分钟）、3600（1小时）和86400（1天）取值。",
        "en": "The time granularity of the data, in seconds.\n\nThe value depends on the time span of the query. Valid values: 300 (5 minutes), 3600 (1 hour), and 86400 (1 day)."
      }
    },
    {
      "name": "SplitBy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询结果分组方式。取值：\n\n- domain：按加速域名分组。\n\n- 参数为空（默认）：查询结果不分组。",
        "en": "The method used to group data. Valid values:\n\n- domain: groups data by accelerated domain name.\n\n- If you do not specify this parameter, the data is not grouped."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取数据起始时间。日期格式按照ISO8601表示法，并使用UTC+0时间，格式为yyyy-MM-ddTHH:mm:ssZ。",
        "en": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询WAF用量数据",
    "en": "DescribeDcdnWafUsageData"
  },
  "descriptions": {
    "zh": "> \n> - 单用户调用频率： 10次/秒\n> - 支持查询的最小时间粒度为5分钟；查询的最大时间跨度为31天；可查询最大历史数据时间范围为90天。",
    "en": "> - The call frequency for a single user is limited to 10 calls per second.\n>\n> - The minimum time granularity is 5 minutes. The maximum time span for a single query is 31 days. You can query data from the last 90 days."
  },
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-waf-usage-data --domain-name example.com --start-time 2018-09-30T16:00:00Z --end-time 2018-10-01T16:00:00Z --interval 300 --split-by domain --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnWafUsageData --region cn-hangzhou --EndTime '2018-10-01T16:00:00Z' --DomainName 'example.com' --SplitBy domain --StartTime '2018-09-30T16:00:00Z' --Interval 300"
  }
}