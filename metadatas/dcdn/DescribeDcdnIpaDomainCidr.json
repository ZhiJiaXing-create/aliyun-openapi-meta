{
  "name": "DescribeDcdnIpaDomainCidr",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "您的加速域名，仅支持单个域名设置。",
        "en": "The accelerated domain name. You can specify only one domain name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询IPA加速域名的回源节点IP段",
    "en": "DescribeDcdnIpaDomainCidr"
  },
  "descriptions": {
    "zh": "> 全局调用频率限制：50次/秒；单用户调用频率限制：10次/秒",
    "en": "> The call frequency is limited to 50 calls per second globally and 10 calls per second for a single user."
  },
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-ipa-domain-cidr --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnIpaDomainCidr --region cn-hangzhou --DomainName 'example.com'"
  }
}