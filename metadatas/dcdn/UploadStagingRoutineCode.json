{
  "name": "UploadStagingRoutineCode",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CodeDescription",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "版本描述。",
        "en": "The version description."
      }
    },
    {
      "name": "Name",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "边缘函数名, 同一个账号下命名唯一。",
        "en": "The name of the edge function. The name must be unique within the same account."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "上传边缘函数ER的测试代码",
    "en": "Upload staging code for an edge function"
  },
  "descriptions": {
    "zh": "- 上传一次边缘函数ER的测试代码，生成一个版本号，占用一个CodeRev的代码版本数，仅用作测试使用。\n- 该参数自动发布代码为测试环境。\n- 一个边缘函数ER最多保留10个版本号，超出限制后需要手动调用DeleteRoutineCodeRevision删除无用的版本。\n- API调用频率：100次/秒。",
    "en": "- Each upload of staging code for an edge function generates a version number, occupies one CodeRev code version slot, and is used only for testing.\n- This operation automatically publishes the code to the canary environment.\n- An edge function can retain a maximum of 10 version numbers. After the limit is exceeded, manually call DeleteRoutineCodeRevision to delete unused versions.\n- API call frequency: 100 calls per second."
  },
  "example": {
    "unifiedCli": "aliyun dcdn upload-staging-routine-code --name test --code-description desc --region cn-hangzhou",
    "legacyCli": "aliyun dcdn UploadStagingRoutineCode --region cn-hangzhou --CodeDescription desc --Name test"
  }
}