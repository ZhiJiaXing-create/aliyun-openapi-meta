{
  "name": "DescribeDcdnDomainStagingConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加速域名。",
        "en": "The accelerated domain name."
      }
    },
    {
      "name": "FunctionNames",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "功能列表名称，多个用逗号（,）分隔。",
        "en": "The names of the features. Separate multiple feature names with commas (,)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询环境配置（灰度）",
    "en": "DescribeDcdnDomainStagingConfig"
  },
  "descriptions": {
    "zh": "> 单用户调用频率：30次/秒。",
    "en": "> A single user can make up to 30 calls per second."
  },
  "example": {
    "unifiedCli": "aliyun dcdn describe-dcdn-domain-staging-config --domain-name example.com --function-names aliauth --region cn-hangzhou",
    "legacyCli": "aliyun dcdn DescribeDcdnDomainStagingConfig --region cn-hangzhou --DomainName 'example.com' --FunctionNames aliauth"
  }
}