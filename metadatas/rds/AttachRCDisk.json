{
  "name": "AttachRCDisk",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DeleteWithInstance",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "释放实例时，该磁盘是否随实例一起释放。取值范围：\n\ntrue：释放。\nfalse：不释放。磁盘会转换成按量付费数据盘而被保留下来。\n默认值：false。\n\n设置该参数时，您需要注意：\n\n将DeleteWithInstance置为false后，一旦实例被安全控制，即OperationLocks中标记了\"LockReason\" : \"security\"，释放实例时会忽略磁盘的该属性，被同时释放。\n\n若您需要挂载的目标磁盘为弹性临时盘，则必须将DeleteWithInstance参数设置为true。\n\n开启多重挂载特性的云盘，不支持设置该参数。",
        "en": "The reserved parameter. This parameter is not supported."
      }
    },
    {
      "name": "DiskId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待挂载的云盘ID。云盘（`DiskId`）和实例（`InstanceId`）必须在同一个可用区。",
        "en": "The disk ID."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目标RDS Custom实例的ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为RDS Custom实例挂载数据盘或系统盘",
    "en": "Mount a data disk or system disk to an RDS Custom instance"
  },
  "descriptions": {
    "zh": "调用该接口时，您需要注意：\n- 磁盘的状态必须为待挂载（Available）。\n- 挂载数据盘时：\n  - 目标RDS Custom实例必须处于运行中（Running）或者已停止（Stopped）状态。\n  - 如果是您单独购买的磁盘，计费方式必须是按量付费。\n  - 从RDS Custom实例上卸载的系统盘作为数据盘挂载时，不限制计费方式。\n  - 弹性临时盘一旦卸载，只能重新挂载至其原始实例。\n- 挂载系统盘时：\n  - 目标RDS Custom实例必须是卸载系统盘时的源实例。\n  - 目标RDS Custom实例必须处于已停止（Stopped）状态。\n  - 您必须设置实例登录凭证。\n  - 弹性临时盘不支持挂载为系统盘。",
    "en": "When invoking this API, note the following:\n- The disk status must be Available (ready to mount).\n- When mounting a data disk:\n  - The target RDS Custom instance must be in the running or stopped state.\n  - If the disk was purchased separately, its metering method must be pay-as-you-go.\n  - When a system disk previously detached from an RDS Custom instance is mounted as a data disk, there is no restriction on its metering method.\n  - An elastic ephemeral disk, once unmounted, can only be remounted to its original instance.\n- When mounting a system disk:\n  - The target RDS Custom instance must be the source instance from which the system disk was originally unmounted.\n  - The target RDS Custom instance must be in the stopped state.\n  - You must configure logon credentials for the instance.\n  - Elastic ephemeral disks cannot be mounted as system disks."
  },
  "example": {
    "unifiedCli": "aliyun rds attach-rc-disk --instance-id rc-dh2jf9n6j4s149260000 --disk-id rcd-wz98hnpj2sjo85zc7t2w --delete-with-instance false --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun rds AttachRCDisk --region cn-hangzhou --InstanceId 'rc-dh2jf9n6j4s149260000' --DeleteWithInstance false --RegionId 'cn-hangzhou' --DiskId 'rcd-wz98hnpj2sjo85zc7t2w'"
  }
}