{
  "name": "CheckBackupEncryptionAuthorized",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID",
        "en": "The ID of the region. For example, 'cn-hangzhou' indicates the China (Hangzhou) region."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询备份加密校验结果",
    "en": "Query Validation Results for Backup Encryption"
  },
  "example": {
    "unifiedCli": "aliyun rds check-backup-encryption-authorized --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun rds CheckBackupEncryptionAuthorized --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}