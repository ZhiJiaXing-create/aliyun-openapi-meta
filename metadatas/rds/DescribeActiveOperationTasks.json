{
  "name": "DescribeActiveOperationTasks",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllowCancel",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "是否允许取消，默认-1，取值：\n\n- **-1**：全部。\n- **0**：只返回不允许取消的任务。\n- **1**：只返回允许取消的任务。",
        "en": "The filter condition that is used to return tasks based on the settings of task cancellation. Default value: -1. Valid values:\n\n*   **-1**: returns all tasks.\n*   **0**: returns only tasks that cannot be canceled.\n*   **1**: returns only tasks that can be canceled."
      }
    },
    {
      "name": "AllowChange",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "是否允许修改时间，默认-1，取值：\n- **-1**：全部。\n- **0**：只返回不允许修改时间的任务。\n- **1**：只返回允许修改时间的任务。",
        "en": "The filter condition that is used to return tasks based on the settings of the switching time. Default value: -1. Valid values:\n\n*   **-1**: returns all tasks.\n*   **0**: returns only tasks for which the switching time cannot be changed.\n*   **1**: returns only tasks for which the switching time can be changed."
      }
    },
    {
      "name": "ChangeLevel",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "任务级别，默认为 all，取值：\n\n- **all**：全部\n- **S0**：返回异常修复级别的任务。\n- **S1**：返回系统运维级别的任务。",
        "en": "The filter condition that is used to return tasks based on the task level. Default value: all. Valid values:\n\n*   **all**: all types\n*   **S0**: returns the tasks of the exception fixing level.\n*   **S1**: returns the tasks of the system O\\&M level."
      }
    },
    {
      "name": "DbType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据库类型，默认为 all\n可选：mysql/pgsql/mssql 等",
        "en": "The type of the database. Default value: all. Valid values: mysql, pgsql, and mssql."
      }
    },
    {
      "name": "InsName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例名，可不填，填写时只允许填写至多一个实例名。",
        "en": "The name of the instance. You can leave this parameter empty. If you configure this parameter, you can specify the name only of one instance."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，大于0, 默认1。",
        "en": "The page number. Pages start from page 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数, 默认每页25条，最大100。",
        "en": "The number of entries per page. Default value: 25. Maximum value: 100."
      }
    },
    {
      "name": "ProductId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "产品名称, RDS/POLARDB/MongoDB/Redis，对于RDS实例可选RDS。",
        "en": "The name of the service. Valid values: RDS, POLARDB, MongoDB, and Redis. For RDS instances, set the value to RDS."
      }
    },
    {
      "name": "Region",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "待处理事件所属的地域ID，可调用DescribeRegions接口获取。\n> 取值为**all**表示所有地域ID。",
        "en": "The region ID of the pending event. You can call the DescribeRegions operation to query the most recent region list.\n\n>  The value **all** indicates all regions."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "任务状态，用于筛选返回任务。\n* **-1**：全部任务。\n* **3**：待处理任务。\n* **4**：处理中任务。\n* **5**：成功结束任务。\n* **6**：失败结束任务。\n* **7**：已取消任务。",
        "en": "The status of the task, which is used as a filter condition to return tasks.\n\n*   **-1**: all tasks\n*   **3**: pending\n*   **4**: being processed\n*   **5**: completed\n*   **6**: failed\n*   **7**: canceled"
      }
    },
    {
      "name": "TaskType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "任务类型，取值：\n\n* **rds\\_apsaradb\\_ha**：主从节点切换。\n* **rds\\_apsaradb\\_transfer**：实例迁移。\n* **rds\\_apsaradb\\_upgrade**：小版本升级。\n* **rds\\_apsaradb\\_maxscale**: 代理小版本升级。\n*  **all**：所有任务类型。",
        "en": "The type of the task. Valid values:\n\n*   **rds_apsaradb_ha**: primary/secondary switchover\n*   **rds_apsaradb_transfer**: instance migration\n*   **rds_apsaradb_upgrade**: update of the minor engine version\n*   **rds_apsaradb_maxscale**: update of the minor version of the proxy\n*   **all**: all types"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询多个主动运维任务信息",
    "en": "DescribeActiveOperationTasks"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS MySQL\n- RDS PostgreSQL\n- RDS SQL Server\n- RDS MariaDB\n\n### 相关功能文档\n><notice>使用该接口前，请仔细阅读功能文档，确保完全了解使用接口的前提条件及使用后造成的影响后，再进行操作。></notice>\n\n- [RDS MySQL计划内事件](~~104183~~)\n- [RDS PostgreSQL计划内事件](~~104452~~)\n- [RDS SQL Server计划内事件](~~104451~~)\n- [RDS MariaDB计划内事件](~~104454~~)",
    "en": "### [](#)Supported database engines\n\n*   MySQL\n*   PostgreSQL\n*   SQL Server\n*   MariaDB"
  },
  "example": {
    "unifiedCli": "aliyun rds describe-active-operation-tasks --biz-region cn-beijing --task-type rds_apsaradb_upgrade --page-size 25 --page-number 1 --product-id RDS --db-type all --status -1 --ins-name rm-bp191w771kd30000 --allow-change -1 --allow-cancel -1 --change-level all --region cn-hangzhou",
    "legacyCli": "aliyun rds DescribeActiveOperationTasks --region cn-hangzhou --Status '-1' --AllowCancel '-1' --PageSize 25 --PageNumber 1 --AllowChange '-1' --TaskType rds_apsaradb_upgrade --Region 'cn-beijing' --ProductId RDS --InsName 'rm-bp191w771kd30000' --DbType all --ChangeLevel all"
  }
}