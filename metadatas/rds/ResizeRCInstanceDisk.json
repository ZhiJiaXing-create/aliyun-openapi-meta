{
  "name": "ResizeRCInstanceDisk",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPay",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否自动支付。取值范围：\n- **true**（默认）：自动支付。您需要确保账户余额充足。\n- **false**：只生成订单不扣费。\n> 如果您的支付方式余额不足，可以将参数AutoPay设置为false，此时会生成未支付订单，您可以登录RDS管理控制台自行支付。\n>",
        "en": "Specifies whether to enable the automatic payment feature for the instance. Valid values:\n\n*   **true** (default): enables the feature. Make sure that your account balance is sufficient.\n*   **false**: disables the feature. An unpaid order is generated.\n\n>  If your account balance is insufficient, you can set AutoPay to false. In this case, an unpaid order is generated. You can complete the payment in the Expenses and Costs console."
      }
    },
    {
      "name": "DiskId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云盘ID。",
        "en": "The disk ID."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否对本次创建实例的操作执行预检查，取值范围：\n* **true**：执行预检查操作，不创建实例。检查项目包含请求参数、请求格式、业务限制和库存等。\n* **false**（默认）：发送正常请求，通过检查后直接创建实例。",
        "en": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\n\n*   **true**: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, service limits, and insufficient inventory errors.\n*   **false**: performs a dry run and performs the actual request. If the request passes the dry run, the instance is created."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "NewSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "磁盘扩容后的大小，单位为GiB。",
        "en": "The new disk size. Unit: GiB."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例所在地域ID。",
        "en": "The region ID of the instance."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "扩容磁盘的方式。取值范围：\n- **offline**（默认）：离线扩容。扩容后，必须重启实例生效。\n- **online**：在线扩容，无需重启实例即可完成扩容。",
        "en": "The method that you want to use to resize the disk. Valid values:\n\n*   **offline** (default): resizes disks offline. After you resize a disk offline, you must restart the instance for the resizing operation to take effect.\n*   **online**: resizes disks online. After you resize a disk online, the resizing operation takes effect immediately and you do not need to restart the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "扩容RDS Custom实例存储空间",
    "en": "Scale out the storage space of an RDS Custom instance"
  },
  "descriptions": {
    "zh": "本地盘实例不支持变更存储空间。",
    "en": "Local disk instances do not support changing storage space."
  },
  "example": {
    "unifiedCli": "aliyun rds resize-rc-instance-disk --biz-region-id cn-hangzhou --instance-id rm-uf62br2491p5l0000 --auto-pay false --dry-run false --type online --new-size 100 --disk-id rcd-x4462840nwinu6rr61m5o --region cn-hangzhou",
    "legacyCli": "aliyun rds ResizeRCInstanceDisk --region cn-hangzhou --Type online --NewSize 100 --InstanceId 'rm-uf62br2491p5l0000' --DryRun false --RegionId 'cn-hangzhou' --AutoPay false --DiskId 'rcd-x4462840nwinu6rr61m5o'"
  }
}