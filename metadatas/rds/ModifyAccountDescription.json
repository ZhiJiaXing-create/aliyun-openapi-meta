{
  "name": "ModifyAccountDescription",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountDescription",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "账号描述，长度为2~256个字符。以中文、英文字母开头，可以包含数字、中文、英文、下划线（_）、短横线（-）。\n>不能以 http:// 和 https:// 开头。",
        "en": "The description of the account. The value must be 2 to 256 characters in length. The value can contain letters, digits, underscores (\\_), and hyphens (-), and must start with a letter.\n\n>  The description cannot start with http:// or https://."
      }
    },
    {
      "name": "AccountName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "账号名称。可调用DescribeAccounts获取。",
        "en": "The username of the account. You can call the DescribeAccounts operation to obtain the username of the account."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances获取。",
        "en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改数据库账号的备注信息",
    "en": "ModifyAccountDescription"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS MySQL\n- RDS PostgreSQL\n- RDS SQL Server\n- RDS MariaDB",
    "en": "### [](#)Supported database engines\n\n*   RDS MySQL\n*   RDS PostgreSQL\n*   RDS SQL Server\n*   RDS MariaDB"
  },
  "example": {
    "unifiedCli": "aliyun rds modify-account-description --db-instance-id rm-uf6wjk50000 --account-name test1 --account-description testuser --region cn-hangzhou",
    "legacyCli": "aliyun rds ModifyAccountDescription --region cn-hangzhou --AccountDescription testuser --DBInstanceId 'rm-uf6wjk50000' --AccountName test1"
  }
}