{
  "name": "DescribeCrossBackupMetaList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupSetId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "跨地域备份集ID。可以通过接口DescribeCrossRegionBackups查询。",
        "en": "The ID of the cross-region backup file that you want to use. You can call the [DescribeCrossRegionBackups](~~121733~~) operation to query the ID of the cross-region backup file."
      }
    },
    {
      "name": "GetDbName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的数据库名。精确匹配，会返回具体的数据库名以及库内的表名。",
        "en": "The name of the database that you want to query. The system implements exact match based on the value of this parameter and returns the name of the matched database and the names of the tables in the matched database."
      }
    },
    {
      "name": "PageIndex",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "页码，取值：大于0且不超过Integer的最大值。\n>需要和**PageSize**一起传入才生效。",
        "en": "The number of the page to return. Valid values: any non-zero positive integer.\n\n> This parameter only takes effect when you specify the **PageSize** parameter."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "每页记录数。默认值：**1**。\n>需要和**PageIndex**一起传入才生效。",
        "en": "The number of entries to return per page. Default value: **1**.\n\n> This parameter only takes effect when you specify the **PageIndex** parameter."
      }
    },
    {
      "name": "Pattern",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的数据库名。模糊匹配，只返回匹配的数据库名，不返回表名。\n>您可以先模糊匹配，例如传入test匹配testdb1和testdb2，确定目标数据库名称后再精确匹配，通过传入**GetDbName**查看具体的数据库名和表名。",
        "en": "The name of the database that you want to query. The system implements fuzzy match based on the value of this parameter and returns only the name of the matched database.\n\n> You can implement fuzzy match and then exact match. For example, you can set the Pattern parameter to test to query the testdb1 and testdb2 databases. Then, you can specify the **GetDbName** parameter to query only the matched database and the tables in the matched database."
      }
    },
    {
      "name": "Region",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例所在地域。",
        "en": "The region ID of the instance."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询实例跨地域备份的库表信息",
    "en": "DescribeCrossBackupMetaList"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS MySQL\n- RDS PostgreSQL\n- RDS SQL Server\n\n### 相关功能文档\n><notice>使用该接口前，请仔细阅读功能文档，确保完全了解使用接口的前提条件及使用后造成的影响后，再进行操作。></notice>\n\n\n- [RDS MySQL跨地域备份](~~120824~~)\n- [RDS PostgreSQL跨地域备份](~~206671~~)\n- [RDS SQL Server跨地域备份](~~187923~~)",
    "en": "ApsaraDB RDS for MySQL instances support cross-region backup and restoration. For more information, see [Back up an ApsaraDB RDS for MySQL instance across regions](~~120824~~) and [Restore the data of an ApsaraDB RDS for MySQL instance across regions](~~120875~~).\n\nBefore you call this operation, make sure that the instance runs one of the following database engines:\n\n*   MySQL. For more information, see [Back up an ApsaraDB RDS for MySQL instance across regions](~~120824~~).\n*   SQL Server. For more information, see [Back up an ApsaraDB RDS for SQL Server instance across regions](~~187923~~).\n*   PostgreSQL. For more information, see [Enable cross-region backups for an ApsaraDB RDS for PostgreSQL instance](~~206671~~)."
  },
  "example": {
    "unifiedCli": "aliyun rds describe-cross-backup-meta-list --backup-set-id 123456 --get-db-name testdb1 --pattern test --page-size 30 --page-index 1 --biz-region cn-hangzhou --resource-group-id rg-acfmy0000 --region cn-hangzhou",
    "legacyCli": "aliyun rds DescribeCrossBackupMetaList --region cn-hangzhou --Pattern test --PageSize 30 --ResourceGroupId 'rg-acfmy0000' --GetDbName testdb1 --Region 'cn-hangzhou' --PageIndex 1 --BackupSetId 123456"
  }
}