{
  "name": "DeleteGadInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GadInstanceName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要删除的RDS全球多活数据库集群ID。可调用DescribeGadInstances查询。",
        "en": "The ID of the global active database cluster. You can call the GadInstanceName operation to query the cluster ID."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "集群中心节点（主节点）所在的地域ID。可调用DescribeGadInstances查询。",
        "en": "The region ID of the central node of the global active database cluster. The central node refers to the primary node. You can call the DescribeGadInstances operation to query the region ID."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。可调用DescribeDBInstanceAttribute接口获取。",
        "en": "The resource group ID. You can call the DescribeDBInstanceAttribute operation to query the resource group ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除Gad实例",
    "en": "DeleteGadInstance"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS MySQL\n\n### 注意事项\n* RDS全球多活数据库集群释放后无法恢复，请谨慎操作。\n* 删除RDS全球多活数据库集群会同时移除集群下的所有节点和DTS同步任务，但不会释放对应RDS MySQL实例，如不再需要这些实例，可调用[DeleteDBInstance](~~26229~~)手动释放实例。",
    "en": "### [](#)Supported database engines\n\n*   MySQL\n\n### [Usage notes](#)\n\n*   A global active database cluster cannot be restored after it is deleted. Proceed with caution.\n*   If you delete a global active database cluster, the system removes all nodes and Data Transmission Service (DTS) synchronization tasks from the cluster. However, the system does not release the ApsaraDB RDS for MySQL instances that run as nodes in the cluster. If you no longer need the ApsaraDB RDS for MySQL instances, you can call the [DeleteDBInstance](~~26229~~) to release the instances one after another."
  },
  "example": {
    "unifiedCli": "aliyun rds delete-gad-instance --gad-instance-name gad-rm-bp1npi2j80000 --biz-region-id cn-hangzhou --resource-group-id rg-acfmy0000 --region cn-hangzhou",
    "legacyCli": "aliyun rds DeleteGadInstance --region cn-hangzhou --ResourceGroupId 'rg-acfmy0000' --GadInstanceName 'gad-rm-bp1npi2j80000' --RegionId 'cn-hangzhou'"
  }
}