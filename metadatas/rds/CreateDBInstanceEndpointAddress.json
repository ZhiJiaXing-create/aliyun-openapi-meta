{
  "name": "CreateDBInstanceEndpointAddress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "该参数用于保证请求的幂等性，防止重复提交请求。参数值由客户端生成，保证在不同请求间唯一。\n\n该参数值由ASCII字符组成，最长不超过64个字符，不能包含非ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests.\n\nThe token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "ConnectionStringPrefix",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "外网连接地址的前缀。",
        "en": "The prefix of the public endpoint."
      }
    },
    {
      "name": "DBInstanceEndpointId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例的Endpoint ID。可调用DescribeDBInstanceEndpoints查询。",
        "en": "The endpoint ID of the instance. You can call the DescribeDBInstanceEndpoints operation to query the endpoint ID."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances查询。",
        "en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID."
      }
    },
    {
      "name": "IpType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "连接地址的网络类型，仅支持外网类型，取值为**Public**。",
        "en": "The network type of the endpoint. Only Internet is supported. Set the value to **Public**."
      }
    },
    {
      "name": "Port",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "外网连接地址端口号。",
        "en": "The port number of the public endpoint."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。可调用DescribeDBInstanceAttribute获取。",
        "en": "The resource group ID. You can call the DescribeDBInstanceAttribute operation to obtain the ID of the resource group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建Endpoint外网连接地址",
    "en": "CreateDBInstanceEndpointAddress"
  },
  "descriptions": {
    "zh": "### 适用引擎\n<props=\"china\">\n\n- RDS MySQL\n- RDS PostgreSQL\n\n</props>\n\n<props=\"intl\">RDS MySQL</props>\n\n### 注意事项\n- 创建Endpoint的外网连接地址，在一个Endpoint没有外网地址时，才可以创建该Endpoint外网地址。\n- 流量分配权重等配置与该Endpoint的内网地址的配置一致。每一个Endpoint只能有一个外网地址和内网地址。",
    "en": "### [](#)Supported database engine\n\nMySQL\n\n### [](#)Precautions\n\n*   You can create a public endpoint of an endpoint type only when no public endpoint is created for this endpoint type.\n*   The node weights and other configurations are the same as those of the internal endpoint of this endpoint type. Each type of endpoint can contain an internal endpoint and a public endpoint."
  },
  "example": {
    "unifiedCli": "aliyun rds create-db-instance-endpoint-address --client-token 6000170000591aed949d0f0000 --db-instance-id rm-0000 --db-instance-endpoint-id ep-0000 --connection-string-prefix rm-0000 --port 3306 --ip-type Public --resource-group-id rg-acfmy0000 --region cn-hangzhou",
    "legacyCli": "aliyun rds CreateDBInstanceEndpointAddress --region cn-hangzhou --ResourceGroupId 'rg-acfmy0000' --DBInstanceId 'rm-0000' --IpType Public --Port 3306 --ClientToken 6000170000591aed949d0f0000 --DBInstanceEndpointId 'ep-0000' --ConnectionStringPrefix 'rm-0000'"
  }
}