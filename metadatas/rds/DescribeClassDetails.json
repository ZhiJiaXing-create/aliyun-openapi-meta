{
  "name": "DescribeClassDetails",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClassCode",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "规格代码。",
        "en": "The code of the instance type."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the generated token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "CommodityCode",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "商品码。取值：\n\n* **bards**：主实例按量付费\n* **rds**：主实例包年包月\n* **rords**：只读实例按量付费\n* **rds\\_rordspre\\_public\\_cn**：只读实例包年包月\n* **bards_intl**：主实例按量付费\n* **rds_intl**：主实例包年包月\n* **rords_intl**：只读实例按量付费\n* **rds\\_rordspre\\_public\\_intl**：只读实例包年包月",
        "en": "The commodity code of the instance. Valid values:\n\n*   **bards_intl**: The instance is a pay-as-you-go primary instance.\n*   **rds_intl**: The instance is a subscription primary instance.\n*   **rords_intl**: The instance is a pay-as-you-go read-only instance.\n*   **rds_rordspre_public_intl**: The instance is a subscription read-only instance."
      }
    },
    {
      "name": "Engine",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据库引擎类型。",
        "en": "The type of the database engine."
      }
    },
    {
      "name": "EngineVersion",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据库版本。",
        "en": "The database engine version of the instance."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID，可以通过接口DescribeRegions查看可用的地域ID。",
        "en": "The region ID. You can call the DescribeRegions operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。可调用DescribeDBInstanceAttribute获取。",
        "en": "The resource group ID. You can call the DescribeDBInstanceAttribute operation to query the resource group ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询规格详情",
    "en": "DescribeClassDetails"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS MySQL\n- RDS PostgreSQL\n- RDS SQL Server\n- RDS MariaDB",
    "en": "### [](#)Supported database engines\n\n*   MySQL\n*   PostgreSQL\n*   SQL Server\n*   MariaDB"
  },
  "example": {
    "unifiedCli": "aliyun rds describe-class-details --client-token ETnLKlblzczshOTUbOCz0000 --biz-region-id cn-hangzhou --commodity-code rds --class-code rds.mysql.s3.large --engine-version 5.6 --engine MySQL --resource-group-id rg-acfmy0000 --region cn-hangzhou",
    "legacyCli": "aliyun rds DescribeClassDetails --region cn-hangzhou --ClassCode 'rds.mysql.s3.large' --EngineVersion '5.6' --ResourceGroupId 'rg-acfmy0000' --CommodityCode rds --RegionId 'cn-hangzhou' --ClientToken ETnLKlblzczshOTUbOCz0000 --Engine MySQL"
  }
}