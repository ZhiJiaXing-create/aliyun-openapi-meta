{
  "name": "DescribeDBInstancesAsCsv",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CachedAsync",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "废弃参数，无需配置。",
        "en": "This parameter is deprecated. You do not need to specify this parameter."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances获取。",
        "en": "The instance ID. You can call the `DescribeDBInstances` operation to query the details of all instances in a specified region."
      }
    },
    {
      "name": "ExportKey",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "废弃参数，无需配置。",
        "en": "This parameter is deprecated. You do not need to specify this parameter."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。可调用DescribeRegions获取。",
        "en": "The region ID. You can call the `DescribeRegions` operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The resource group ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "按CSV文件格式查询实例列表（停止维护）",
    "en": "DescribeDBInstancesAsCsv"
  },
  "descriptions": {
    "zh": "该接口已停止维护：**接口仍可以正常调用，但阿里云不再维护该接口**。建议您使用**DescribeDBInstances**接口。",
    "en": "This API is deprecated. **While it remains callable, Alibaba Cloud no longer maintains it**. We recommend using the **DescribeDBInstances** API instead."
  },
  "example": {
    "unifiedCli": "aliyun rds describe-db-instances-as-csv --biz-region-id cn-hangzhou --db-instance-id rm-uf6wjk50000 --resource-group-id rg-acfmy0000 --cached-async false --export-key API --region cn-hangzhou",
    "legacyCli": "aliyun rds DescribeDBInstancesAsCsv --region cn-hangzhou --ResourceGroupId 'rg-acfmy0000' --ExportKey API --DBInstanceId 'rm-uf6wjk50000' --CachedAsync false --RegionId 'cn-hangzhou'"
  }
}