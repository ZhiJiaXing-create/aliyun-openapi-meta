{
  "name": "DescribeInstanceAutoRenewalAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances获取。",
        "en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "当前页数。",
        "en": "The page number."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，取值：\n* **30**（默认）\n* **50**\n* **100**",
        "en": "The number of entries per page. Valid values:\n\n*   **30 (default value)**\n*   **50**\n*   **100**"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。可调用DescribeRegions获取。",
        "en": "The region ID. You can call the DescribeRegions operation to query the most recent region list."
      }
    },
    {
      "name": "proxyId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预留参数，无需配置。",
        "en": "This parameter is reserved. You do not need to specify this parameter."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询RDS实例自动续费情况",
    "en": "DescribeInstanceAutoRenewalAttribute"
  },
  "descriptions": {
    "zh": "### 适用引擎\n- RDS MySQL\n- RDS PostgreSQL\n- RDS SQL Server\n- RDS MariaDB",
    "en": "### [](#)Supported database engines\n\n*   MySQL\n*   PostgreSQL\n*   SQL Server\n*   MariaDB"
  },
  "example": {
    "unifiedCli": "aliyun rds describe-instance-auto-renewal-attribute --client-token ETnLKlblzczshOTUbOCz0000 --proxy-id test --biz-region-id cn-hangzhou --db-instance-id rm-bp0000 --page-size 30 --page-number 1 --region cn-hangzhou",
    "legacyCli": "aliyun rds DescribeInstanceAutoRenewalAttribute --region cn-hangzhou --PageSize 30 --DBInstanceId 'rm-bp0000' --PageNumber 1 --RegionId 'cn-hangzhou' --ClientToken ETnLKlblzczshOTUbOCz0000 --proxyId test"
  }
}