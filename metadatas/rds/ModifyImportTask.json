{
  "name": "ModifyImportTask",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances获取。",
        "en": "The instance ID. Call the DescribeDBInstances operation to query instance IDs."
      }
    },
    {
      "name": "Operation",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "取值范围如下：\n\n\n- RETRY_IMPORT：重试导入\n- CANCEL：取消任务",
        "en": "The operation to perform. Valid values:\n\n- RETRY\\_IMPORT: Retries the data import task.\n\n- CANCEL: Cancels the data import task."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目标地域ID，可以通过接口DescribeRegions查看地域ID。",
        "en": "The region ID. Call the DescribeRegions operation to query the latest list of regions."
      }
    },
    {
      "name": "TaskId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "任务ID。",
        "en": "The task ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改数据导入任务",
    "en": "Modify Data Import Job"
  },
  "descriptions": {
    "zh": "修改RDS MySQL原生复制实例数据导入任务",
    "en": "Modifies a data import task for an RDS MySQL native replication instance."
  },
  "example": {
    "unifiedCli": "aliyun rds modify-import-task --biz-region-id cn-hangzhou --db-instance-id rm-2ze63v2p3o3k0000 --task-id 1234567 --operation CANCEL --region cn-hangzhou",
    "legacyCli": "aliyun rds ModifyImportTask --region cn-hangzhou --TaskId 1234567 --DBInstanceId 'rm-2ze63v2p3o3k0000' --RegionId 'cn-hangzhou' --Operation CANCEL"
  }
}