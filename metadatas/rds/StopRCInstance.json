{
  "name": "StopRCInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ForceStop",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否强制关机。取值范围：\n\n-   **true**：强制关机。\n\n-   **false**（默认）：正常关机。",
        "en": "Specifies whether to forcefully stop the instance. Valid values:\n\n*   **true**\n*   **false** (default)"
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "StoppedMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "停止实例时，设置的停止模式。取值：\n\n  - StopCharging：节省停机模式。开启节省停机模式后：\n    - 计算资源暂停计费。\n    - 系统盘、数据盘持续计费。\n    - 由于计算资源被回收，启动时可能因库存不足导致启动失败，请您稍后再试或更换实例规格。 \n\n  - KeepCharging：普通停机模式。实例停止后仍继续收费。",
        "en": "The stop mode to use when stopping the instance. Valid values:\n\n  - StopCharging: economical mode. When economical mode is enabled:\n    - Computing resources are no longer billed.\n    - System disk and data disk continue to be billed.\n    - Because computing resources are revoked, the instance might fail to start due to insufficient inventory. In this case, try again later or change the instance type.\n\n  - KeepCharging: normal stop mode. The instance continues to incur charges after it is stopped."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "停止一台RDS Custom实例",
    "en": "Stop an RDS Custom instance"
  },
  "example": {
    "unifiedCli": "aliyun rds stop-rc-instance --instance-id rc-m5sc1271fv344a1r0000 --force-stop false --biz-region-id cn-hangzhou --stopped-mode KeepCharging --region cn-hangzhou",
    "legacyCli": "aliyun rds StopRCInstance --region cn-hangzhou --InstanceId 'rc-m5sc1271fv344a1r0000' --StoppedMode KeepCharging --ForceStop false --RegionId 'cn-hangzhou'"
  }
}