{
  "name": "ImportUserBackupFile",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupFile",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "描述OSS Bucket中备份文件信息的JSON数组。示例：\n`{\"Bucket\":\"test\", \"Object\":\"test/test_db_employees.xb\",\"Location\":\"ap-southeast-1\"}`\n\n数组中各参数的说明如下：\n* **Bucket**：备份文件所在OSS Bucket的名称。可调用[GetBucket](~~31965~~)获取。\n* **Object**：备份文件所在目录的详细路径。可调用[GetObject](~~31980~~)获取。\n* **Location**：OSS Bucket所在地域的ID。可调用[GetBucketLocation](~~31967~~)获取。",
        "en": "A JSON array that consists of the information about the full backup file stored as an object in an OSS bucket. Example: `{\"Bucket\":\"test\", \"Object\":\"test/test_db_employees.xb\",\"Location\":\"ap-southeast-1\"}`\n\nThe JSON array contains the following fields:\n\n*   **Bucket**: The name of the OSS bucket in which the full backup file is stored as an object. You can call the [GetBucket](~~31965~~) operation to query the name of the bucket.\n*   **Object**: The path of the full backup file that is stored as an object in the OSS bucket. You can call the [GetObject](~~31980~~) operation to query the path of the object.\n*   **Location**: The ID of the region in which the OSS bucket is located. You can call the [GetBucketLocation](~~31967~~) operation to query the region of the bucket."
      }
    },
    {
      "name": "BucketRegion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自建MySQL 5.7备份文件所在OSS Bucket的地域ID。可调用DescribeRegions获取。",
        "en": "The region ID of the OSS bucket where the full backup file of the self-managed MySQL database is located. You can call the DescribeRegions operation to query the most recent region list."
      }
    },
    {
      "name": "BuildReplication",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否自动搭建复制，取值说明：\n- true：是，`MasterInfo`参数必填。\n- false：否。\n\n> 仅对原生复制实例生效，需要在调用API时传入`DBInstanceId`参数。",
        "en": "Specifies whether to automatically set up replication. Valid values:\n- true: Yes. The `MasterInfo` parameter is required.\n- false: No.\n\n> This applies only to native replication instances and requires the `DBInstanceId` parameter to be passed when invoking the API."
      }
    },
    {
      "name": "Comment",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要导入的用户备份的备注信息。",
        "en": "The description of the full backup file."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "EngineVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "MySQL数据库的版本号。当前支持传入**5.7**或**8.0**。",
        "en": "The version of the database engine that is run on the self-managed MySQL database and ApsaraDB RDS for MySQL instance. Set the value to **5.7**."
      }
    },
    {
      "name": "MasterInfo",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "搭建MySQL复制的Master信息的JSON数组（大小写敏感）。示例：\n\n```\n{\"masterIp\":\"172.20.xx.xx\",\"masterPort\":\"3306\",\"masterUser\":\"replica\",\"masterPassword\":\"W33uopkehBQ=\"}\n\n```\n\n数组中各参数的说明如下：\n- `masterIp`：主库IP。\n- `masterPort`：主库端口。\n- `masterUser`：主库复制账号。\n- `masterPassword`：主库复制账号密码，需要进行Base64加密。\n\n> 仅对原生复制实例生效，需要在调用API时传入`DBInstanceId`参数。",
        "en": "A case-sensitive JSON array containing the Master information for setting up MySQL replication. Example:\n\n```\n{\"masterIp\":\"172.20.xx.xx\",\"masterPort\":\"3306\",\"masterUser\":\"replica\",\"masterPassword\":\"W33uopkehBQ=\"}\n```\n\nThe parameters in the array are described as follows:\n- `masterIp`: Primary database IP address.\n- `masterPort`: Primary database port.\n- `masterUser`: Replication account for the primary database.\n- `masterPassword`: Password for the replication account of the primary database, which must be Base64-encoded.\n\n> This applies only to native replication instances and requires the `DBInstanceId` parameter to be passed when invoking the API."
      }
    },
    {
      "name": "Mode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "导入模式，取值说明：\n\n- oss：从OSS下载备份导入。\n- stream：通过网络导入备份。",
        "en": "Import mode. Valid values:\n\n- oss: Download the backup from OSS and import it.\n- stream: Import the backup over the network."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "RDS地域ID。可调用DescribeRegions获取。\n\n> * 本参数的值为您希望创建RDS实例的地域ID。\n> * 需要和**BucketRegion**参数的值保持一致。",
        "en": "The region ID of the instance. You can call the DescribeRegions operation to query the most recent region list.\n\n> *   The value of this parameter is the ID of the region in which you want to create the instance.\n> *   The value of this parameter must be consistent with the value of **BucketRegion**."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。可调用DescribeDBInstanceAttribute获取。",
        "en": "The resource group ID. You can call the DescribeDBInstanceAttribute operation to query the resource group ID."
      }
    },
    {
      "name": "RestoreSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "还原用户备份所需存储空间大小。单位：GB。\n\n> * 默认为备份文件的5倍大小。\n> * 最小值为20。",
        "en": "The amount of storage that is required to restore the data of the full backup file. Unit: GB.\n\n> *   The default value of this parameter is 5 times the size of the full backup file.\n> *   The minimum value of this parameter is 20."
      }
    },
    {
      "name": "Retention",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "用户备份文件保留时长。单位：天。取值为大于**0**的Integer类型整数。",
        "en": "The retention period of the full backup file. Unit: days. Valid values: any **non-zero** positive integer."
      }
    },
    {
      "name": "SourceInfo",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "提供全量备份的源端信息的JSON数组（大小写敏感）。示例：\n\n```\n{\"sourceIp\":\"172.20.xx\n.xx\",\"sourcePort\":\"9999\"}\n\n```\n\n数组中各参数的说明如下：\n\n- `sourceIp`：源端IP。\n\n- `sourcePort`：源端Netcat监听的端口。\n\n> 仅对原生复制实例生效，需要在调用API时传入`DBInstanceId`参数。",
        "en": "A case-sensitive JSON array that provides the source information for a full backup. Example:\n\n```\n{\"sourceIp\":\"172.20.xx.xx\",\"sourcePort\":\"9999\"}\n```\n\nThe parameters in the array are described as follows:\n\n- `sourceIp`: Source IP address.\n\n- `sourcePort`: Port on which Netcat listens at the source.\n\n> This applies only to native replication instances and requires the `DBInstanceId` parameter to be passed when invoking the API."
      }
    },
    {
      "name": "ZoneId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "可用区ID。可调用DescribeRegions查询。\n\n> * 设置可用区后，系统会在该可用区内创建一个秒级快照，大幅节省备份导入所需要的时间。\n> * 调用CreateDBInstance使用用户备份创建新实例时，该可用区即为新实例所在的可用区。",
        "en": "The zone ID. You can call the DescribeRegions operation to query the zone ID.\n\n> *   If you specify this parameter, the system creates a snapshot in single-digit seconds, which greatly reduces the time that is required to import the full backup file.\n> *   When you call the CreateDBInstance operation to create an instance by using the full backup file, the instance is created in the zone that you specify for this parameter."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "导入用户备份",
    "en": "Import User Backup"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS MySQL\n\n### 功能说明\n用户备份即MySQL自建库的全量备份数据，您可以将用户备份恢复至云上。\n\n### 注意事项\n**调用本接口，您需要满足下述条件：**\n* 已通过XtraBackup备份自建MySQL 5.7或8.0，备份文件名以`_qp.xb`结尾。更多信息，请参见[自建MySQL 5.7、8.0数据库全量上云](~~251779~~)。\n* 已将自建MySQL 5.7或8.0的备份文件上传至对应地域的OSS Bucket。更多信息，请参见[自建MySQL 5.7、8.0数据库全量上云](~~251779~~)。",
    "en": "### Supported Engine\n\n- RDS MySQL\n\n### Feature Description\nUser backups refer to full backup data from a self-managed MySQL database. You can recover user backups to the cloud.\n\n### Notes\n**To invoke this API, you must meet the following conditions:**\n* You have backed up your self-managed MySQL 5.7 or 8.0 database using XtraBackup, and the backup file name ends with `_qp.xb`. For more information, see [Migrate Full Backup Data from Self-Managed MySQL 5.7 or 8.0 Databases to the Cloud](~~251779~~).\n* You have uploaded the backup files of your self-managed MySQL 5.7 or 8.0 database to an OSS bucket in the corresponding Region. For more information, see [Migrate Full Backup Data from Self-Managed MySQL 5.7 or 8.0 Databases to the Cloud](~~251779~~)."
  },
  "example": {
    "unifiedCli": "aliyun rds import-user-backup-file --biz-region-id cn-hangzhou --biz-mode oss --engine-version 5.7 --bucket-region cn-hangzhou --backup-file Bucket_:_test_,_Object_:_test/test_db_employees.xb_,_Location_:_ap-southeast-1 --comment BackupTest --restore-size 20 --retention 30 --zone-id cn-hangzhou-b --resource-group-id rg-acfmy0000 --db-instance-id rm-uf6wjk50000 --source-info sourceIp_:_172.20.xx.xx_,_sourcePort_:_9999 --build-replication true --master-info masterIp_:_172.20.xx.xx_,_masterPort_:_3306_,_masterUser_:_replica_,_masterPassword_:_W33uopkehBQ= --region cn-hangzhou",
    "legacyCli": "aliyun rds ImportUserBackupFile --region cn-hangzhou --BackupFile 'Bucket_:_test_,_Object_:_test/test_db_employees.xb_,_Location_:_ap-southeast-1' --Comment BackupTest --EngineVersion '5.7' --ZoneId 'cn-hangzhou-b' --ResourceGroupId 'rg-acfmy0000' --SourceInfo 'sourceIp_:_172.20.xx.xx_,_sourcePort_:_9999' --BuildReplication true --Mode oss --Retention 30 --MasterInfo 'masterIp_:_172.20.xx.xx_,_masterPort_:_3306_,_masterUser_:_replica_,_masterPassword_:_W33uopkehBQ=' --BucketRegion 'cn-hangzhou' --DBInstanceId 'rm-uf6wjk50000' --RegionId 'cn-hangzhou' --RestoreSize 20"
  }
}