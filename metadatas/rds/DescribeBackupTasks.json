{
  "name": "DescribeBackupTasks",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupJobId",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "备份任务ID。",
        "en": "The ID of the backup task."
      }
    },
    {
      "name": "BackupJobStatus",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份任务状态，取值：\n* **NoStart**：未开始\n* **Progressing**：正在进行中\n\n默认为所有状态。",
        "en": "The status of the backup task. Valid values:\n\n*   **NoStart**\n*   **Progressing**\n\nBy default, this operation returns backup tasks in both states."
      }
    },
    {
      "name": "BackupMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份模式，取值：\n* **Automated**：系统自动备份\n* **Manual**：手动备份",
        "en": "The backup mode. Valid values:\n\n*   **Automated**\n*   **Manual**"
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符。",
        "en": "Specifies the client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances获取。",
        "en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID."
      }
    },
    {
      "name": "Flag",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备用参数。",
        "en": "A reserved parameter."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询实例的备份任务列表",
    "en": "DescribeBackupTasks"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS MySQL\n- RDS PostgreSQL\n- RDS SQL Server\n- RDS MariaDB",
    "en": "### [](#)Supported database engines\n\n*   RDS MySQL\n*   RDS PostgreSQL\n*   RDS SQL Server\n*   RDS MariaDB"
  },
  "example": {
    "unifiedCli": "aliyun rds describe-backup-tasks --client-token ETnLKlblzczshOTUbOCz0000 --flag '无' --db-instance-id rm-uf6wjk50000 --backup-job-id 4760000 --backup-mode Automated --backup-job-status NoStart --region cn-hangzhou",
    "legacyCli": "aliyun rds DescribeBackupTasks --region cn-hangzhou --BackupJobStatus NoStart --DBInstanceId 'rm-uf6wjk50000' --BackupMode Automated --BackupJobId 4760000 --ClientToken ETnLKlblzczshOTUbOCz0000 --Flag '无'"
  }
}