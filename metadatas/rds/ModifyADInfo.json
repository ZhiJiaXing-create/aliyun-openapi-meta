{
  "name": "ModifyADInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ADAccountName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "AD域的账号。",
        "en": "The account of the AD domain."
      }
    },
    {
      "name": "ADDNS",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "AD域的域名（DNS）信息。",
        "en": "The DNS information about the AD domain."
      }
    },
    {
      "name": "ADPassword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "AD域的密码。",
        "en": "The password for the account of the AD domain."
      }
    },
    {
      "name": "ADServerIpAddress",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "AD域的IP地址。",
        "en": "The IP address of the AD domain."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances获取。",
        "en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。可调用DescribeRegions获取。",
        "en": "The region ID. You can call the DescribeRegions operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改RDS SQL Server的AD域配置",
    "en": "ModifyADInfo"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS SQL Server\n\n### 相关功能文档\n><notice>使用该接口前，请仔细阅读功能文档，确保完全了解使用接口的前提条件及使用后造成的影响后，再进行操作。></notice>\n\n- [SQL Server接入自建域](~~170734~~)",
    "en": "### [](#)Supported database engine\n\n*   SQL Server\n\n### [](#)References\n\n> Before you call this operation, read the following documentation and make sure that you fully understand the prerequisites and impacts of this operation.\n\n*   [Connect an RDS instance to a self-managed domain](~~170734~~)"
  },
  "example": {
    "unifiedCli": "aliyun rds modify-ad-info --client-token ETnLKlblzczshOTUbOCz0000 --db-instance-id rm-uf6wjk50000 --biz-region-id cn-hangzhou --ad-dns example.com --ad-server-ip-address 47.100.XX.XX --ad-account-name test_account --ad-password test_password --region cn-hangzhou",
    "legacyCli": "aliyun rds ModifyADInfo --region cn-hangzhou --ADServerIpAddress '47.100.XX.XX' --DBInstanceId 'rm-uf6wjk50000' --ADDNS 'example.com' --ADPassword test_password --RegionId 'cn-hangzhou' --ClientToken ETnLKlblzczshOTUbOCz0000 --ADAccountName test_account"
  }
}