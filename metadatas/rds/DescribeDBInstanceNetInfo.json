{
  "name": "DescribeDBInstanceNetInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances获取。",
        "en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID."
      }
    },
    {
      "name": "DBInstanceNetRWSplitType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "连接地址类型，取值：\n* **Normal**：普通连接地址。\n* **ReadWriteSplitting**：读写分离连接地址。\n\n>默认返回所有类型连接地址。",
        "en": "The type of the endpoint. Valid values:\n\n*   **Normal**: regular endpoint\n*   **ReadWriteSplitting**: read/write splitting endpoint\n\n> By default, the system returns both types of endpoints."
      }
    },
    {
      "name": "Flag",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "备用参数，无需配置。",
        "en": "A reserved parameter. You do not need to specify this parameter."
      }
    },
    {
      "name": "GeneralGroupName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专属集群MySQL通用版实例所属的组名。",
        "en": "The name of the dedicated cluster to which the instance belongs. This parameter takes effect only when the instance runs MySQL on RDS Standard Edition and is created in a dedicated cluster."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询实例的所有连接地址信息",
    "en": "DescribeDBInstanceNetInfo"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS MySQL\n- RDS PostgreSQL\n- RDS SQL Server\n- RDS MariaDB",
    "en": "### Supported database engines\n\n*   MySQL\n*   PostgreSQL\n*   SQL Server\n*   MariaDB"
  },
  "example": {
    "unifiedCli": "aliyun rds describe-db-instance-net-info --client-token ETnLKlblzczshOTUbOC0000 --db-instance-id rm-uf6wjk50000 --flag 1 --db-instance-net-rw-split-type Normal --general-group-name rgc-2ze0000 --region cn-hangzhou",
    "legacyCli": "aliyun rds DescribeDBInstanceNetInfo --region cn-hangzhou --DBInstanceId 'rm-uf6wjk50000' --ClientToken ETnLKlblzczshOTUbOC0000 --Flag 1 --DBInstanceNetRWSplitType Normal --GeneralGroupName 'rgc-2ze0000'"
  }
}