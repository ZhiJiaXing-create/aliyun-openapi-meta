{
  "name": "RevokeAccountPrivilege",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "账号名称。",
        "en": "The name of the account."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances获取。",
        "en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID."
      }
    },
    {
      "name": "DBName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据库名称，撤销账号对该数据库的所有权限。多个数据库用英文逗号（,）隔开。",
        "en": "The name of the database. You can revoke all permissions of the account on this database. Separate multiple databases with commas (,)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "撤销账号对数据库的访问权限",
    "en": "RevokeAccountPrivilege"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS MySQL\n- RDS SQL Server\n- RDS MariaDB\n\n### 前提条件\n* 实例状态为运行中。\n* 数据库状态为运行中。\n\n### 注意事项\n* 具体撤销的权限包括SELECT、INSERT、UPDATE、DELETE、CREATE、DROP、REFERENCES、INDEX、ALTER、CREATE TEMPORARY TABLES、LOCK TABLES、EXECUTE、CREATE VIEW、SHOW VIEW、CREATE ROUTINE 、ALTER ROUTINE、EVENT、TRIGGER；\n* 该接口暂不支持SQL Server 2017集群系列和PostgreSQL实例。",
    "en": "### [](#)Supported database engines\n\n*   MySQL\n*   SQL Server\n*   MariaDB\n\n### [](#)Prerequisites\n\n*   The instance is in the Running state.\n*   The database is in the Running state.\n\n### [](#)Precautions\n\n*   The permissions that can be revoked include SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, REFERENCES, INDEX, ALTER, CREATE TEMPORARY TABLES, LOCK TABLES, EXECUTE, CREATE VIEW, SHOW VIEW, CREATE ROUTINE, ALTER ROUTINE, EVENT, and TRIGGER.\n*   This operation is not supported for instances that run SQL Server 2017 on RDS Cluster Edition and run PostgreSQL."
  },
  "example": {
    "unifiedCli": "aliyun rds revoke-account-privilege --db-instance-id rm-uf6wjk50000 --account-name test1 --db-name testDB --region cn-hangzhou",
    "legacyCli": "aliyun rds RevokeAccountPrivilege --region cn-hangzhou --DBInstanceId 'rm-uf6wjk50000' --DBName testDB --AccountName test1"
  }
}