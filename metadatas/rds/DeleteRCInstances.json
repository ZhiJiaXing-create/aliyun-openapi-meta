{
  "name": "DeleteRCInstances",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否对本次释放实例的操作执行预检查，取值：\n* **true**：执行预检查操作，不释放实例。\n* **false**（默认）：发送正常请求，通过检查后直接释放实例。",
        "en": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\n\n*   **true**: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, service limits, and insufficient inventory errors.\n*   **false** (default): performs a dry run and performs the actual request. If the request passes the dry run, the instance is created."
      }
    },
    {
      "name": "Force",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否强制释放运行中的实例。取值范围：\n* **Yes**：强制\n* **No**（默认）：非强制",
        "en": "Specifies whether to forcefully release a running instance. Valid values:\n\n*   **Yes**\n*   **No** (default)"
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "Json",
      "required": true,
      "description": {
        "zh": "实例详情。",
        "en": "The details of the instance."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例所在地域ID。",
        "en": "The region ID of the instance."
      }
    },
    {
      "name": "TerminateSubscription",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "备用参数。",
        "en": "Specifies whether to release an expired subscription instance. Valid values:\n\n*   **true**\n*   **false** (default)"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量退订RDS Custom实例",
    "en": "DeleteRCInstances"
  },
  "descriptions": {
    "zh": "释放后，实例所使用的物理资源都被回收，相关数据全部丢失且不可恢复。",
    "en": "After an instance is released, all physical resources used by the instance are recycled. Relevant data is erased and cannot be restored."
  },
  "example": {
    "unifiedCli": "aliyun rds delete-rc-instances --biz-force false --instance-id rc-lrt4wdv22tr180f70000 --biz-region-id cn-hangzhou --terminate-subscription false --dry-run false --region cn-hangzhou",
    "legacyCli": "aliyun rds DeleteRCInstances --region cn-hangzhou --Force false --InstanceId '[\"rc-lrt4wdv22tr180f70000\"]' --TerminateSubscription false --DryRun false --RegionId 'cn-hangzhou'"
  }
}