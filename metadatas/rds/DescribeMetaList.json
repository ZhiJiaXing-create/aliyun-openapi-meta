{
  "name": "DescribeMetaList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupSetID",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "基于备份集查询时，使用的备份集的ID。可调用DescribeBackups获取。\n> **RestoreType**为**BackupSetID**时必传。",
        "en": "The ID of the backup set from which you want to restore data. You can call the DescribeBackups operation to query the IDs of data backup files.\n\n>  This parameter is required when you set the **RestoreType** parameter to **BackupSetID**."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the generated token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances获取。",
        "en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID."
      }
    },
    {
      "name": "GetDbName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的数据库名。精确匹配，会返回具体的数据库名以及库内所有的表。\n> 为空则返回包含所有数据库的列表。",
        "en": "The name of the database to query. The system implements exact match based on the value of this parameter and returns the name of the matched database and the names of all tables contained in the database.\n\n> If you leave this parameter empty, the system returns all databases that are created on the instance."
      }
    },
    {
      "name": "PageIndex",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，取值：大于**0**且不超过Integer的最大值。默认值：**1**。\n> 需要和**PageSize**一起传入才生效。",
        "en": "The number of the page to return. Valid values: any non-zero positive integer.**** Default value: **1**.\n\n> This parameter only takes effect when you specify the **PageSize** parameter."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数。默认值：**1**。\n> 需要和**PageIndex**一起传入才生效。",
        "en": "The number of entries to return on each page. Default value: **1**.\n\n> This parameter only takes effect when you specify the **PageIndex** parameter."
      }
    },
    {
      "name": "Pattern",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的数据库名。模糊匹配，只返回匹配的数据库名，不返回表名。\n> 例如传入`test`匹配`testdb1`和`testdb2`，确定目标数据库名称后再通过**GetDbName**参数传入精确的数据库名称查看目标库下所有表的信息。",
        "en": "The name of the database to query. The system implements fuzzy match based on the value of this parameter and returns only the name of the matched database.\n\n> For example, if you set the value to `test`, the system returns `testdb1` and `testdb2`. Then, you can specify the **GetDbName** parameter to query tables in the required database."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The resource group ID."
      }
    },
    {
      "name": "RestoreTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "基于时间点查询时，使用的时间节点，需要早于当前时间。格式：<i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z（UTC时间）。可调用DescribeBackups获取。\n> **RestoreType**为**RestoreTime**时必传 。",
        "en": "The point in time to which you want to restore data. The specified point in time must be earlier than the current time. Specify the time in the ISO 8601 standard in the *yyyy-MM-dd*T*HH:mm:ss*Z format. The time must be in UTC. You can call the DescribeBackups operation to query the restorable time range.\n\n>  This parameter must be specified when the **RestoreType** parameter is set to **RestoreTime**."
      }
    },
    {
      "name": "RestoreType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "恢复方式，取值：\n\n* **BackupSetID**：基于备份集恢复，您还需要传入参数**BackupSetID**。\n* **RestoreTime**：基于时间点恢复，您还需要传入参数**RestoreTime**。\n\n默认值：**BackupSetID**",
        "en": "The restoration method that you want to use. Valid values:\n\n*   **BackupSetID**: Data is restored from the backup set. If you use this value, you must also specify the **BackupSetID** parameter.\n*   **RestoreTime**: Data is restored to a specific point in time. If you use this value, you must also specify the **RestoreTime** parameter.\n\nDefault value: **BackupSetID**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询备份集的库表信息",
    "en": "DescribeMetaList"
  },
  "descriptions": {
    "zh": "### 适用引擎\nRDS MySQL\n> 仅支持MySQL 8.0、5.7、5.6高可用版（本地SSD盘）。\n\n### 功能说明\n在使用[RestoreTable](~~131510~~)接口进行[MySQL单库单表恢复](~~103175~~)之前，您可以通过本接口查询可恢复的库表信息。",
    "en": "### [](#)Supported database engines\n\nMySQL\n> This operation is available for RDS instances that run MySQL 8.0, MySQL 5.7, and MySQL 5.6 on RDS High-availability Edition with local disks.\n\n\n### [](#)Description\n\nBefore you call the [RestoreTable](~~131510~~) operation to restore individual databases or tables of an ApsaraDB RDS for MySQL instance, you can call this operation to query the information about the databases and tables that can be restored. For more information, see [Restore individual databases and tables of an ApsaraDB RDS for MySQL instance](~~103175~~)."
  },
  "example": {
    "unifiedCli": "aliyun rds describe-meta-list --client-token ETnLKlblzczshOTUbOCz0000 --db-instance-id rm-uf6wjk50000 --restore-type BackupSetID --backup-set-id 14000 --restore-time 2019-05-30T03:29:10Z --get-db-name testdb1 --pattern test --page-size 1 --page-index 1 --resource-group-id rg-acfmy0000 --region cn-hangzhou",
    "legacyCli": "aliyun rds DescribeMetaList --region cn-hangzhou --Pattern test --PageSize 1 --ResourceGroupId 'rg-acfmy0000' --DBInstanceId 'rm-uf6wjk50000' --GetDbName testdb1 --PageIndex 1 --ClientToken ETnLKlblzczshOTUbOCz0000 --RestoreType BackupSetID --BackupSetID 14000 --RestoreTime '2019-05-30T03:29:10Z'"
  }
}