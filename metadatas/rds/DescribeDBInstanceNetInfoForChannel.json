{
  "name": "DescribeDBInstanceNetInfoForChannel",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances获取。",
        "en": "The ID of the instance. You can call the [DescribeDBInstances](~~26232~~) operation to query the instance ID."
      }
    },
    {
      "name": "DBInstanceNetRWSplitType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询的连接地址类型：\n* **0**（默认）：常规连接地址。\n* **1**：共享代理的读写分离地址。",
        "en": "The type of the endpoint to query. Valid values:\n\n*   **0**: a regular endpoint. This is the default value.\n*   **1**: a read/write splitting endpoint that is assigned after the shared proxy feature is enabled."
      }
    },
    {
      "name": "Flag",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "内部参数，无需传入。",
        "en": "An internal parameter. You do not need to specify this parameter."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询实例网络信息",
    "en": "DescribeDBInstanceNetInfoForChannel"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS MySQL\n- RDS PostgreSQL\n- RDS SQL Server\n- RDS MariaDB",
    "en": "### [](#)Supported database engines\n\n*   MySQL\n*   PostgreSQL\n*   SQL Server\n*   MariaDB"
  },
  "example": {
    "unifiedCli": "aliyun rds describe-db-instance-net-info-for-channel --client-token 0c593ea1-3bea-11e9-b96b-880000 --db-instance-id rm-bp1k8s41l2o520000 --flag '无' --db-instance-net-rw-split-type 0 --region cn-hangzhou",
    "legacyCli": "aliyun rds DescribeDBInstanceNetInfoForChannel --region cn-hangzhou --DBInstanceId 'rm-bp1k8s41l2o520000' --ClientToken '0c593ea1-3bea-11e9-b96b-880000' --Flag '无' --DBInstanceNetRWSplitType 0"
  }
}