{
  "name": "CreateDBInstanceForRebuild",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "该参数用于保证请求的幂等性，防止重复提交请求。参数值由客户端生成，保证在不同请求间唯一。\n\n该参数值由ASCII字符组成，最长不超过64个字符，不能包含非ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests.\n\nThe token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "DBInstanceDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "目标实例名称，长度为2~256个字符。以中文、英文字母开头，可以包含数字、中文、英文、下划线（_）、短横线（-）。\n>不能以 http:// 和 https:// 开头。",
        "en": "The name of the instance. The name must be 2 to 256 characters in length. The name can contain letters, digits, underscores (\\_), and hyphens (-). The name must start with a letter.\n\n> : The name cannot start with http:// or https://."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances获取。",
        "en": "The instance ID. You can call the [DescribeDBInstances](~~610396~~) operation to query the ID of the instance."
      }
    },
    {
      "name": "DBInstanceNetType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例的网络连接类型，取值：\n* **Internet**：公网连接。\n* **Intranet**：内网连接。",
        "en": "The type of the network connection to the instance. Valid values:\n\n*   **Internet**\n*   **Intranet**"
      }
    },
    {
      "name": "InstanceNetworkType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "目标实例的网络类型，取值：\n\n* **VPC**：VPC网络\n* **Classic**：经典网络\n\n默认创建经典网络类型的实例。\n>当实例为云盘实例时，该参数必填，且值为**VPC**。当本参数值为**VPC**时，还需要传入参数**VpcId**、**VSwitchId**。",
        "en": "The network type of the instance. Valid values:\n\n*   **VPC**\n*   **Classic**\n\nDefault value: Classic.\n\n>  If the instance uses cloud disks, this parameter is required. Set the value to **VPC**. The **VpcId** and **VSwitchId** parameters must be specified when this parameter is set to **VPC**."
      }
    },
    {
      "name": "PayType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目标实例的付费类型，取值：\n* **Postpaid**：后付费（按量付费）\n* **Prepaid**：预付费（包年包月）",
        "en": "The billing method of the destination instance. Valid values:\n\n*   **Postpaid**: pay-as-you-go\n*   **Prepaid**: subscription"
      }
    },
    {
      "name": "Period",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定预付费实例为包年或者包月类型，取值：\n* **Year**：包年。\n* **Month**：包月。\n\n> 若付费类型为**Prepaid**则该参数必须传入。",
        "en": "The unit of the subscription duration. Valid values:\n\n*   **Year**\n*   **Month**\n\n>  If you set the PayType parameter to **Prepaid**, you must also specify this parameter."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。可调用DescribeRegions获取。",
        "en": "The region ID. You can call the [DescribeRegions](~~610399~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID，可以为空。",
        "en": "The ID of the resource group. The value of this parameter can be NULL."
      }
    },
    {
      "name": "SecurityIPList",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "该实例的[IP白名单](~~43185~~)。多条记录请以半角逗号（,）隔开，不可重复，最多1000条记录。支持如下两种格式：\n* IP地址形式，例如：10.10.XX.XX。\n* CIDR形式，例如：10.10.XX.XX/24（无类域间路由，24表示了地址中前缀的长度，范围为1~32）。\n\n如不填则默认为原实例default分组白名单信息。",
        "en": "The IP address whitelist of the serverless instance. For more information, see [Use a database client or the CLI to connect to an ApsaraDB RDS for PostgreSQL instance](~~43185~~). If the IP address whitelist contains more than one entry, separate the entries with commas (,). Each entry must be unique. You can specify up to 1,000 entries. The entries in the IP address whitelist must be in one of the following formats:\n\n*   IP addresses, such as 10.10.XX.XX.\n*   CIDR blocks, such as 10.10.XX.XX/24. In this example, 24 indicates that the prefix of each IP address in the IP address whitelist is 24 bits in length. You can replace 24 with a value within the range of 1 to 32.\n\nIf this parameter is not specified, the default IP address whitelist is used."
      }
    },
    {
      "name": "UsedTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定购买时长。取值：\n* 当参数**Period**为**Year**时，**UsedTime**取值为**1**~**5**。\n* 当参数**Period**为**Month**时，**UsedTime**取值为**1**~**11**。\n\n> 若**PayType**为**Prepaid**，需要传入该参数。",
        "en": "The subscription duration of the instance. Valid values:\n\n*   If you set **Period** to **Year**, the value of **UsedTime** ranges from **1** to **5**.\n*   If you set **Period** to **Month**, the value of **UsedTime** ranges from **1** to **11**.\n\n> If you set **PayType** to **Prepaid**, you must specify this parameter."
      }
    },
    {
      "name": "VPCId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "目标实例的VPC ID。当**InstanceNetworkType**=**VPC**时，本参数必须配置。\n> 如果传入此参数，您还需要传入参数**ZoneId**。",
        "en": "The VPC ID of the instance. If you set **InstanceNetworkType** to **VPC**, you must specify this parameter.\n\n> : If you specify this parameter, you must also specify **ZoneId**."
      }
    },
    {
      "name": "VSwitchId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "虚拟交换机ID。虚拟交换机所在的可用区必须和**ZoneId**中传入的可用区ID相对应。\n\n> - 网络类型**InstanceNetworkType**需为**VPC**。\n> - 若您填写了ZoneSlaveId1（备可用区ID），此处需填写两个交换机ID，并使用半角逗号（,）隔开。",
        "en": "The ID of the vSwitch. The vSwitch must belong to the zone that is specified by **ZoneId**.\n\n> \n\n*   If you set **InstanceNetworkType** to **VPC**, you must also specify this parameter.\n\n*   If you specify the ZoneSlaveId1 parameter, you must specify the IDs of two vSwitches for this parameter and separate the IDs with a comma (,)."
      }
    },
    {
      "name": "ZoneId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "主节点可用区ID。可以通过接口DescribeRegions查看可用区ID。\n\n>指定了VPC和交换机时，为匹配交换机对应的可用区，该参数必填。",
        "en": "The zone ID of the primary instance. You can call the [DescribeRegions](~~26243~~) operation to query the most recent zone list.\n\n> If you specify a virtual private cloud (VPC) and a vSwitch, you must specify this parameter to identify the zone for the vSwitch."
      }
    },
    {
      "name": "ZoneIdSlave1",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备可用区1。\n>非基础系列实例需要传入该参数。",
        "en": "The zone ID of the secondary instance.\n\n>  If the instance does not run RDS Basic Edition, you must specify this parameter."
      }
    },
    {
      "name": "ZoneIdSlave2",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备可用区2。\n>仅三节点企业系列实例可传入该参数。",
        "en": "The zone ID of the logger instance.\n\n>  This parameter is available only when the instance runs RDS Enterprise Edition."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "回收站重建实例",
    "en": "CreateDBInstanceForRebuild"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS MySQL\n- RDS PostgreSQL\n- RDS SQL Server\n- RDS MariaDB\n\n### 相关功能文档\n><warning>该API操作涉及费用，请仔细阅读相关功能文档后再进行操作。></warning>\n\n- [RDS MySQL回收站重建实例](~~96065~~)\n- [RDS PostgreSQL回收站重建实例](~~96752~~)\n- [RDS SQL Server回收站重建实例](~~95669~~)\n- [RDS MariaDB回收站重建实例](~~97131~~)",
    "en": "### Supported database engines\n\n*   RDS MySQL\n*   RDS PostgreSQL\n*   RDS SQL Server\n*   RDS MariaDB\n\n### References\n\n> : Fees are generated if the call is successful. Before you call this operation, carefully read the following documentation:\n\n*   [Manage ApsaraDB RDS for MySQL instances in the recycle bin](~~96065~~)\n*   [Manage ApsaraDB RDS for PostgreSQL instances in the recycle bin](~~96752~~)\n*   [Manage ApsaraDB RDS for SQL Server instances in the recycle bin](~~95669~~)\n*   [Manage ApsaraDB RDS for MariaDB instances in the recycle bin](~~97131~~)"
  },
  "example": {
    "unifiedCli": "aliyun rds create-db-instance-for-rebuild --biz-region-id cn-hangzhou --db-instance-id rm-uf6wjk50000 --pay-type Postpaid --security-ip-list 127.0.0.1 --db-instance-description testdb --zone-id cn-hangzhou-e --zone-id-slave-1 cn-hangzhou-c --zone-id-slave-2 cn-hangzhou-d --vpc-id vpc-uf6f7l4fg900000 --vswitch-id vsw-uf6adz52c2p0000 --period Month --used-time 1 --client-token ETnLKlblzczshOTUbOC0000 --db-instance-net-type Internet --instance-network-type VPC --resource-group-id rg-acfmy0000 --region cn-hangzhou",
    "legacyCli": "aliyun rds CreateDBInstanceForRebuild --region cn-hangzhou --ZoneId 'cn-hangzhou-e' --ResourceGroupId 'rg-acfmy0000' --VPCId 'vpc-uf6f7l4fg900000' --DBInstanceNetType Internet --VSwitchId 'vsw-uf6adz52c2p0000' --Period Month --PayType Postpaid --InstanceNetworkType VPC --ZoneIdSlave2 'cn-hangzhou-d' --ZoneIdSlave1 'cn-hangzhou-c' --DBInstanceId 'rm-uf6wjk50000' --SecurityIPList '127.0.0.1' --UsedTime 1 --RegionId 'cn-hangzhou' --ClientToken ETnLKlblzczshOTUbOC0000 --DBInstanceDescription testdb"
  }
}