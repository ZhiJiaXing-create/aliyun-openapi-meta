{
  "name": "DescribeSecrets",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定返回的语言。取值如下：\n\n- **zh-CN**：中文\n- **en-US**：英文\n\n> 默认值：**en-US**。",
        "en": "The language of the text within the response. Valid values:\n\n*   **zh-CN**: Chinese\n*   **en-US**: English\n\n> The default value is **en-US**."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "DbInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances获取。",
        "en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID."
      }
    },
    {
      "name": "Engine",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据库类型。\n\n> 该参数当前仅支持传入MySQL值。",
        "en": "The database engine of the database.\n\n> Only MySQL is supported."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "查询分页的页码，取值：大于0且不超过Integer的最大值。\n\n> 默认值：1。",
        "en": "The number of the page to return. Valid values: any non-zero positive integer.\n\n> The default value is 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "每页记录数。",
        "en": "The number of entries per page."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID，您可调用DescribeDBInstanceAttribute接口查看实例所在地域ID。",
        "en": "The region ID. You can call the DescribeDBInstanceAttribute operation to query the region ID."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例所在的资源组ID。",
        "en": "The ID of the resource group to which the instance belongs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询用户凭证",
    "en": "DescribeSecrets"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS MySQL",
    "en": "### [](#)Supported database engine\n\n*   MySQL"
  },
  "example": {
    "unifiedCli": "aliyun rds describe-secrets --client-token ETnLKlblzczshOTUbOCz0000 --accept-language en-US --biz-region-id cn-hangzhou --db-instance-id rm-xjkljj0000 --engine MySQL --page-number 1 --page-size 10 --resource-group-id rg-acfmxypivk000 --region cn-hangzhou",
    "legacyCli": "aliyun rds DescribeSecrets --region cn-hangzhou --PageSize 10 --ResourceGroupId 'rg-acfmxypivk000' --PageNumber 1 --AcceptLanguage 'en-US' --RegionId 'cn-hangzhou' --ClientToken ETnLKlblzczshOTUbOCz0000 --DbInstanceId 'rm-xjkljj0000' --Engine MySQL"
  }
}