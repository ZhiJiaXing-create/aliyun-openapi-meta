{
  "name": "RenewRCInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPay",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否自动支付。取值范围：\n\n- **true**：自动支付。您需要确保账户余额充足。\n- **false**：只生成订单不扣费。\n\n\n\n\n> 默认值为true。如果您的支付方式余额不足，可以将参数AutoPay设置为false，此时会生成未支付订单，您可以登录RDS管理控制台自行支付。\n>",
        "en": "Specifies whether to enable automatic payment. Valid values:\n\n*   **true**: enables the feature. You must make sure that your account balance is sufficient.\n*   **false**: disables the feature. An unpaid order is generated.\n\n>  Default value: true. If your account balance is insufficient, you can set AutoPay to false to generate an unpaid order. Then, you can log on to the ApsaraDB RDS console to complete the payment."
      }
    },
    {
      "name": "AutoRenew",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例是否自动续费，取值：\n\n* **true**：是\n* **false**（默认）：否",
        "en": "Specifies whether to enable auto-renewal for the instance. Valid values:\n\n*   **true**\n*   **false** (default)"
      }
    },
    {
      "name": "AutoUseCoupon",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否使用代金券，取值：\n* **true**（默认）：使用代金券。\n* **false**：不使用代金券。",
        "en": "Specifies whether to use a coupon. Default value: false. Valid values:\n\n*   **true**: uses a coupon.\n*   **false**: does not use a coupon."
      }
    },
    {
      "name": "BusinessInfo",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "订单附加信息。",
        "en": "The additional information about the order."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "CommodityCode",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "商品代码。\n\n<props=\"china\">默认为**rds_customprepaid_public_cn**。</props>\n\n\n\n<props=\"intl\">默认为**rds_customprepaid_public_intl**。</props>",
        "en": "The commodity code of the instance.\n\nDefault value: **rds_customprepaid_public_intl**."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "目标RDS Custom实例的ID。",
        "en": "The ID of the RDS Custom instance."
      }
    },
    {
      "name": "PayType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "目标实例的付费类型，目前仅支持**Prepaid**：预付费（包年包月）。",
        "en": "The billing method of the instance. Set the value to **PrePaid**, which indicates the subscription billing method."
      }
    },
    {
      "name": "PeriodAlign",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否包年，取值范围：\n\n- **true**：包年\n- **false**（默认）：不包年",
        "en": "Specifies whether the instance is a subscription instance. Valid values:\n\n*   **true**\n*   **false** (default)"
      }
    },
    {
      "name": "PromotionCode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "优惠券code。",
        "en": "The coupon code."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "Resource",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源。",
        "en": "The resources."
      }
    },
    {
      "name": "TimeType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "续费时长的时间单位，即参数**UsedTime**的单位。取值：\n\n- **1**：年\n- **2**（默认）：月",
        "en": "The unit of the renewal period specified by the **UsedTime** parameter. Valid values:\n\n*   **1**: year\n*   **2** (default): month"
      }
    },
    {
      "name": "UsedTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定购买时长。取值：\n* 当参数**TimeType**为**1**（年）时，UsedTime取值为**1~5**。\n* 当参数**TimeType**为**2**（月）时，UsedTime取值为**1~11**。",
        "en": "The subscription duration of the instance. Valid values:\n\n*   If you set the **TimeType** parameter to **1**, the value of the UsedTime parameter ranges from **1 to 5**. Unit: year.\n*   If you set the **TimeType** parameter to **2**, the value of the UsedTime parameter ranges from **1 to 11**. Unit: month."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "续费RDS Custom实例",
    "en": "Renew an RDS Custom Instance"
  },
  "example": {
    "unifiedCli": "aliyun rds renew-rc-instance --client-token ETnLKlblzczshOTUbOC0000 --commodity-code rds_customprepaid_public_00 --biz-region-id cn-hangzhou --instance-id rc-dh2jf9n6j4s149260000 --used-time 1 --time-type 2 --resource buy --auto-pay true --pay-type Prepaid --business-info promotion_input_param_:_promotionFilter_:_,_promotionOptionCode_:_youhui_quan --period-align true --auto-renew true --auto-use-coupon true --promotion-code 723298850000 --region cn-hangzhou",
    "legacyCli": "aliyun rds RenewRCInstance --region cn-hangzhou --PeriodAlign true --BusinessInfo 'promotion_input_param_:_promotionFilter_:_,_promotionOptionCode_:_youhui_quan' --InstanceId 'rc-dh2jf9n6j4s149260000' --AutoRenew true --PromotionCode 723298850000 --Resource buy --AutoPay true --PayType Prepaid --TimeType 2 --AutoUseCoupon true --CommodityCode rds_customprepaid_public_00 --UsedTime 1 --RegionId 'cn-hangzhou' --ClientToken ETnLKlblzczshOTUbOC0000"
  }
}