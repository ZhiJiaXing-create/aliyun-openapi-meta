{
  "name": "DeleteReplicationLink",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "灾备实例ID。",
        "en": "The ID of the DR instance."
      }
    },
    {
      "name": "PromoteToMaster",
      "position": "Query",
      "type": "Boolean",
      "required": true,
      "description": {
        "zh": "是否删除主实例和灾备实例间的数据同步链路，并提升灾备实例为主实例，取值：\n\n- **true**：是\n- **false**：否",
        "en": "Specifies whether to delete the data synchronization link between the DR instance and the primary instance and promote the DR instance to the primary instance. Valid values:\n\n*   **true**\n*   **false**"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除灾备实例的数据同步链路并将其提升为主实例",
    "en": "DeleteReplicationLink"
  },
  "descriptions": {
    "zh": "### 适用引擎\n\n- RDS PostgreSQL\n- RDS SQL Server",
    "en": "### [](#)Supported database engines\n\n*   PostgreSQL\n*   SQL Server"
  },
  "example": {
    "unifiedCli": "aliyun rds delete-replication-link --db-instance-id m-2zecuz9tolf000000 --promote-to-master true --region cn-hangzhou",
    "legacyCli": "aliyun rds DeleteReplicationLink --region cn-hangzhou --DBInstanceId 'm-2zecuz9tolf000000' --PromoteToMaster true"
  }
}