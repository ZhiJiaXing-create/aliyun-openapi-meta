{
  "name": "DeletePostgresExtensions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。可调用DescribeDBInstances获取。",
        "en": "The instance ID. You can call the DescribeDBInstances operation to query the instance ID."
      }
    },
    {
      "name": "DBNames",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "插件所在的数据库，多个数据库用英文逗号（,）分隔。",
        "en": "The database on which the extension is installed. If you want to specify multiple databases, separate the databases with commas (,)."
      }
    },
    {
      "name": "Extensions",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "插件名，多个插件用英文逗号（,）分隔。",
        "en": "The name of the extension. If you want to specify multiple extensions, separate the extension names with commas (,)."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除实例目标数据库下的指定插件",
    "en": "DeletePostgresExtensions"
  },
  "descriptions": {
    "zh": "<props=\"china\">您可以加入RDS PostgreSQL插件交流钉钉群（103525002795），进行咨询、交流和反馈，获取更多关于插件的信息。</props>\n\n### 适用引擎\nRDS PostgreSQL\n\n\n### 相关功能文档\n><notice>使用该接口前，请仔细阅读功能文档，确保完全了解使用接口的前提条件及使用后造成的影响后，再进行操作。></notice>\n\n[管理插件](~~2402409~~)",
    "en": "### Supported database engines\n\nRDS PostgreSQL\n\n### References\n\n> : Before you call this operation, carefully read the following documentation. Make sure that you fully understand the prerequisites and impacts for calling this operation.\n\n[Manage extensions](~~2402409~~)"
  },
  "example": {
    "unifiedCli": "aliyun rds delete-postgres-extensions --client-token ETnLKlblzczshOTUbOCz0000 --db-instance-id pgm-bp156o9ti4930000 --extensions citext --db-names test_db --resource-group-id rg-acfmy0000 --region cn-hangzhou",
    "legacyCli": "aliyun rds DeletePostgresExtensions --region cn-hangzhou --ResourceGroupId 'rg-acfmy0000' --DBInstanceId 'pgm-bp156o9ti4930000' --DBNames test_db --ClientToken ETnLKlblzczshOTUbOCz0000 --Extensions citext"
  }
}