{
  "name": "DescribeDBInstanceSwitchLog",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例 ID。可调用 DescribeDBInstances 获取。"
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "查询结束时间，需要晚于开始时间。格式：yyyy-MM-ddTHH:mmZ（UTC 时间）。"
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，取值：大于 0 且不超过 Integer 的最大值。\n默认值：1。"
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，取值 <= 100，默认值：30。"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域 ID。可调用 DescribeRegions 获取。"
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "查询开始时间。格式：yyyy-MM-ddTHH:mmZ（UTC 时间）。"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查看实例的主备切换日志",
    "en": "DescribeDBInstanceSwitchLog"
  },
  "descriptions": {
    "zh": "该接口用于查看实例的主备切换日志，适用于MySQL高可用版实例、MySQL三节点企业版实例、SQL Server实例、PostgreSQL实例、PPAS实例。",
    "en": "该接口用于查看实例的主备切换日志，适用于MySQL高可用版实例、MySQL三节点企业版实例、SQL Server实例、PostgreSQL实例、PPAS实例。"
  },
  "example": {
    "unifiedCli": "aliyun rds describe-db-instance-switch-log --db-instance-id rm-uf6wjk50000 --start-time 2026-04-01T00:00Z --end-time 2026-04-02T00:00Z --page-size 30 --page-number 1 --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun rds DescribeDBInstanceSwitchLog --region cn-hangzhou --EndTime '2026-04-02T00:00Z' --PageSize 30 --DBInstanceId 'rm-uf6wjk50000' --PageNumber 1 --StartTime '2026-04-01T00:00Z' --RegionId 'cn-hangzhou'"
  }
}