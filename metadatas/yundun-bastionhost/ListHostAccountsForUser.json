{
  "name": "ListHostAccountsForUser",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "HostAccountName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定要查询的主机账户名称。支持精确查询。",
        "en": "The name of the host account that you want to query. Exact match is supported."
      }
    },
    {
      "name": "HostId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定要查询的主机账户的主机ID。\n> 您可以调用[ListHosts](~~200665~~)接口获取该参数。",
        "en": "The ID of the host to query.\n\n> You can call the [ListHosts](~~200665~~) operation to query the ID of the host."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定要查询已授权主机账户列表的用户所在堡垒机的实例ID。\n> 您可以调用[DescribeInstances](~~153281~~)接口获取该参数。",
        "en": "The ID of the bastion host on which you want to perform the query. The host accounts that the specified user is authorized to manage on the specified host are queried.\n\n> You can call the [DescribeInstances](~~153281~~) operation to query the ID of the bastion host."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定分页查询时，当前页的页码。默认值为**1**。",
        "en": "The number of the page to return. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定分页查询时，每页显示的数据最大条数。  \nPageSize参数最大取值为100。每页默认显示的数据条数为20条，PageSize参数值为空时，将默认返回20条数据。\n> 建议PageSize取值不要为空。",
        "en": "The number of entries to return on each page.<br>\nMaximum value: 100. Default value: 20. If you leave this parameter empty, 20 entries are returned on each page.\n\n> We recommend that you do not leave this parameter empty."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定要查询已授权主机账户列表的用户所在堡垒机的区域ID。\n> 区域ID和区域名称的对应关系，请参见[地域和可用区](~~40654~~)。",
        "en": "The region ID of the bastion host on which you want to perform the query. The host accounts that the specified user is authorized to manage on the specified host are queried.\n\n> For more information about the mapping between region IDs and region names, see [Regions and zones](~~40654~~)."
      }
    },
    {
      "name": "UserId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定要查询已授权主机账户列表的用户ID。\n> 您可以调用[ListUsers](~~204522~~)接口获取该参数。",
        "en": "The ID of the user for which you want to query authorized host accounts.\n\n> You can call the [ListUsers](~~204522~~) operation to query the ID of the user."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询指定用户已授权和未授权的主机账户列表",
    "en": "ListHostAccountsForUser"
  },
  "example": {
    "unifiedCli": "aliyun yundun-bastionhost list-host-accounts-for-user --instance-id bastionhost-cn-st220aw0000 --biz-region-id cn-hangzhou --user-id 1 --host-id 1 --page-number 1 --page-size 20 --host-account-name root --region cn-hangzhou",
    "legacyCli": "aliyun yundun-bastionhost ListHostAccountsForUser --region cn-hangzhou --HostAccountName root --PageSize 20 --InstanceId 'bastionhost-cn-st220aw0000' --PageNumber 1 --UserId 1 --RegionId 'cn-hangzhou' --HostId 1"
  }
}