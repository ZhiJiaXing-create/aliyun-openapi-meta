{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置返回结果的语言类型。取值范围：\n- **zh-CN**（默认值）：中文\n- **en-US**：英文\n- **ja**：日文",
        "en": "The language of the response. Valid values:\n\n- **zh-CN** (default): Chinese\n\n- **en-US**: English\n\n- **ja**: Japanese"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The ID of the region."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询堡垒机实例支持的阿里云地域",
    "en": "DescribeRegions"
  },
  "example": {
    "unifiedCli": "aliyun yundun-bastionhost describe-regions --accept-language zh-CN --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun yundun-bastionhost DescribeRegions --region cn-hangzhou --AcceptLanguage 'zh-CN' --RegionId 'cn-hangzhou'"
  }
}