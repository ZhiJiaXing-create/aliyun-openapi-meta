{
  "name": "GetToken",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/tokens",
  "parameters": [
    {
      "name": "ExpireTime",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分享链接过期时间（默认为604800秒），最小值为0。",
        "en": "Time-to-live (TTL) of the share link in seconds. Default is 604800. Minimum is 0."
      }
    },
    {
      "name": "TrainingJobId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "训练任务ID。",
        "en": "ID of the training task."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取训练任务分享令牌",
    "en": "Obtain a sharing token for a training job"
  },
  "example": {
    "unifiedCli": "aliyun paistudio get-token --training-job-id traincclrt205dcs --expire-time 60 --api-version 2022-01-12 --region cn-hangzhou",
    "legacyCli": "aliyun paistudio GET /api/v1/tokens --region cn-hangzhou --TrainingJobId traincclrt205dcs --ExpireTime 60 --header \"Content-Type=application/json\""
  }
}