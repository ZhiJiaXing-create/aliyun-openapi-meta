{
  "name": "GetResourceGroupRequest",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/resources/data/request",
  "parameters": [
    {
      "name": "PodStatus",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "容器状态。包括：\n\n- Waiting\n- Running\n- Terminated",
        "en": "The container status. Valid values:\n\n- Waiting\n- Running\n- Terminated"
      }
    },
    {
      "name": "ResourceGroupID",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源组 ID，每一个资源组都有全局唯一的资源组ID。可以通过资源组ID来获取资源组的信息。",
        "en": "The resource group ID. Each resource group has a globally unique resource group ID. You can use the resource group ID to obtain information about the resource group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取资源组已经被分配的资源",
    "en": "Obtain the resources that have been allocated to a resource group"
  },
  "example": {
    "unifiedCli": "aliyun paistudio get-resource-group-request --resource-group-id rgf0zhfqn1d4ity2 --pod-status Running --api-version 2022-01-12 --region cn-hangzhou",
    "legacyCli": "aliyun paistudio GET /api/v1/resources/data/request --region cn-hangzhou --PodStatus Running --ResourceGroupID rgf0zhfqn1d4ity2 --header \"Content-Type=application/json\""
  }
}