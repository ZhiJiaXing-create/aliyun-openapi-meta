{
  "name": "DeleteAllCustomizeFlowStrategy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "regionId",
        "en": "The region ID."
      }
    },
    {
      "name": "UserId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "用户ID",
        "en": "The user ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除所有自定义流控策略",
    "en": "Delete all custom rate limiting policies"
  },
  "descriptions": {
    "zh": "请求方法：支持以HTTPS POST和GET方法发送请求。\n\n> 授权key在30分钟内有效，且不可重复使用，建议在每次激活前重新获取。",
    "en": "Request method: Supports sending requests by using the HTTPS POST and GET methods.\n\n> The authorization key is valid for 30 minutes and cannot be reused. Obtain a new key before each activation."
  },
  "example": {
    "unifiedCli": "aliyun cloudauth delete-all-customize-flow-strategy --user-id 126005125163 --biz-region-id cn-hangzhou --api-version 2019-03-07 --region cn-hangzhou",
    "legacyCli": "aliyun cloudauth DeleteAllCustomizeFlowStrategy --region cn-hangzhou --UserId 126005125163 --RegionId 'cn-hangzhou'"
  }
}