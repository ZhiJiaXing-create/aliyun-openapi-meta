{
  "name": "QueryBlackListStrategy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "所在地域ID",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询黑名单规则",
    "en": "Query blacklist rules"
  },
  "descriptions": {
    "zh": "- 请求地址：cloudauth.aliyuncs.com\n- 请求方法：HTTPS POST和GET。\n\n> 支持对于IP、身份证号、手机号、银行卡号等设置黑名单，当命中黑名单时，系统拒绝请求，返回固定错误码\n支持对于IP、身份证号、手机号、银行卡号等设置黑名单，当命中黑名单时，系统拒绝请求，返回固定错误码",
    "en": "- Request endpoint: cloudauth.aliyuncs.com\n- Request method: HTTPS POST and GET.\n\n> You can configure blacklists for IP addresses, ID card numbers, phone numbers, and bank card numbers. When a request matches a blacklist entry, the system rejects the request and returns a fixed error code.\nYou can configure blacklists for IP addresses, ID card numbers, phone numbers, and bank card numbers. When a request matches a blacklist entry, the system rejects the request and returns a fixed error code."
  },
  "example": {
    "unifiedCli": "aliyun cloudauth query-black-list-strategy --biz-region-id cn-hangzhou --api-version 2019-03-07 --region cn-hangzhou",
    "legacyCli": "aliyun cloudauth QueryBlackListStrategy --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}