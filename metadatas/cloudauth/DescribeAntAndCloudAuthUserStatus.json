{
  "name": "DescribeAntAndCloudAuthUserStatus",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询实人认证不同产品方案的开通情况",
    "en": "Query the activation status of different ID verification product plans"
  },
  "descriptions": {
    "zh": "请求方法：支持以HTTPS POST和GET方法发送请求。\n\n> 授权key在30分钟内有效，且不可重复使用，建议在每次激活前重新获取。",
    "en": "Request method: Supports sending requests by using HTTPS POST and GET methods.\n\n> The authorization key is valid for 30 minutes and cannot be reused. Obtain a new key before each activation."
  },
  "example": {
    "unifiedCli": "aliyun cloudauth describe-ant-and-cloud-auth-user-status --api-version 2019-03-07 --region cn-hangzhou",
    "legacyCli": "aliyun cloudauth DescribeAntAndCloudAuthUserStatus --region cn-hangzhou"
  }
}