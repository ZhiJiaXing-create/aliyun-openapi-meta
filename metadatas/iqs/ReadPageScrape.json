{
  "name": "ReadPageScrape",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/linked-retrieval/linked-retrieval-entry/v1/iqs/readpage/scrape",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "post body",
        "en": "post body"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "web页面解析",
    "en": "Web Page Parsing"
  },
  "descriptions": {
    "zh": "1. 当目标地址的 HTTP 响应码（httpcode）< 500 时，计为一次有效请求。\n\n2. 若目标地址的响应头中的内容类型（Content-Type）为 application/pdf，系统将自动触发 PDF 解析（pdf 处理大小不超过 20MB），此操作将额外计为一次有效请求。\n\n3. 体验说明：体验期间接口限制 5qps；体验额度为 1000次/30天；",
    "en": "1. A request is counted as valid when the HTTP status code (httpcode) of the target URL is less than 500.  \n\n2. If the content type (Content-Type) in the response header of the target URL is application/pdf, the system automatically triggers PDF parsing (PDF files up to 20 MB are supported). This operation is counted as an additional valid request.  \n\n3. Trial terms: During the trial period, the API is limited to 5 queries per second (QPS); the trial quota is 1,000 requests per 30 days."
  },
  "example": {
    "unifiedCli": "aliyun iqs read-page-scrape --api-version 2024-11-11 --region cn-hangzhou",
    "legacyCli": "aliyun iqs POST /linked-retrieval/linked-retrieval-entry/v1/iqs/readpage/scrape --region cn-hangzhou --header \"Content-Type=application/json\" --body \"example-string\""
  }
}