{
  "name": "GetLiveRoomUserStatistics",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "用户的应用ID，在控制台创建应用时生成。包含小写字母、数字，长度为6个字符。",
        "en": "The application ID of the user, which is generated when the application is created in the console. The ID consists of lowercase letters and digits and is 6 characters in length."
      }
    },
    {
      "name": "LiveId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "直播资源的唯一标识ID。",
        "en": "The unique ID of the live resource."
      }
    },
    {
      "name": "PageNumber",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询页码，从1开始，参数为空默认查询第1页。",
        "en": "The page number to query, starting from 1. If this parameter is left empty, the first page is queried by default."
      }
    },
    {
      "name": "PageSize",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "每页显示个数，最大支持50，参数为空默认显示个数为10。",
        "en": "The number of entries per page. Maximum value: 50. If this parameter is left empty, the default value is 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "分页查询直播用户数据统计",
    "en": "Paged query of live room user statistics"
  },
  "descriptions": {
    "zh": "观众的直播观看时长是用户观看时长的累加（包括多次观看，多设备观看），如果最终计算结果大于直播总时长，会按照直播总时长记录。",
    "en": "The viewing duration of a viewer is the cumulative total of the user's viewing time, including multiple views and views from multiple devices. If the calculated result exceeds the total live streaming duration, the total live streaming duration is recorded instead."
  }
}