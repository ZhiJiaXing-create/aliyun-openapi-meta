{
  "name": "UpdateIpamScope",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样。",
        "en": "The client token that is used to ensure the idempotence of the request. Generate a value for this parameter that is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the RequestId of the request as the ClientToken. The RequestId of each request is unique."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n\n- **true**：发送检查请求，不会修改IPAM作用范围信息。检查项包括是否填写了必需参数、请求格式、实例状态。如果检查不通过，则返回对应错误。如果检查通过，则返回DryRunOperation。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行修改。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: Performs a dry run. The system checks the request for potential issues, including missing required parameters, incorrect request format, and instance status. If the request fails the dry run, an error is returned. If the request passes the dry run, the DryRunOperation error code is returned.\n\n- **false** (Default): Sends a normal request. After the request passes the check, the operation is performed."
      }
    },
    {
      "name": "IpamScopeDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要修改的IPAM作用范围的描述信息。\n\n长度为1～256个字符，必须以英文大小写字母开头，但不能以`http://`或`https://`开头。不填则为空，默认值为空。",
        "en": "The description of the IPAM scope.\n\nThe description must be 1 to 256 characters long. It must start with a letter and cannot start with `http://` or `https://`. If you do not specify this parameter, the value is not modified."
      }
    },
    {
      "name": "IpamScopeId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPAM作用范围实例ID。",
        "en": "The ID of the IPAM scope."
      }
    },
    {
      "name": "IpamScopeName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要修改的IPAM作用范围的名称。\n\n长度为1～128个字符，不能以`http://`或`https://`开头。",
        "en": "The name of the IPAM scope.\n\nThe name must be 1 to 128 characters long and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPAM托管地域ID。您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region where the IPAM is hosted. Call the [DescribeRegions](~~36063~~) operation to get the region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改IPAM作用范围的信息",
    "en": "UpdateIpamScope"
  },
  "example": {
    "unifiedCli": "aliyun vpcipam update-ipam-scope --biz-region-id cn-hangzhou --ipam-scope-id ipam-scope-glfmcyldpm8lsy0000 --ipam-scope-name test --ipam-scope-description test_description --dry-run false --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun vpcipam UpdateIpamScope --region cn-hangzhou --IpamScopeId 'ipam-scope-glfmcyldpm8lsy0000' --IpamScopeName test --DryRun false --IpamScopeDescription test_description --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}