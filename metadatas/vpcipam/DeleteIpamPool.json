{
  "name": "DeleteIpamPool",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that the value is unique among different requests. The ClientToken value can contain only ASCII characters.\n\n> If you do not specify this parameter, the system uses the RequestId of the API request as the ClientToken. The RequestId may be different for each API request."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n\n- **true**：发送检查请求，不会删除IPAM地址池实例。检查项包括是否填写了必需参数、请求格式、实例状态。如果检查不通过，则返回对应错误。如果检查通过，则返回DryRunOperation。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: performs a dry run without actually deleting the IPAM pool instance. The system checks whether the required parameters are specified, the request format is valid, and the instance status is correct. If the check fails, the corresponding error is returned. If the check succeeds, DryRunOperation is returned.\n- **false** (default): sends a normal request. After the request passes the check, an HTTP 2xx status code is returned and the operation is directly performed."
      }
    },
    {
      "name": "IpamPoolId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPAM地址池的实例ID。",
        "en": "The instance ID of the IPAM pool."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPAM托管地域ID。您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the IPAM managed region. You can call the [DescribeRegions](~~36063~~) operation to obtain the region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除IPAM地址池实例",
    "en": "Delete an IPAM pool instance"
  },
  "descriptions": {
    "zh": "### 使用说明\n\n- 删除父地址池前，请确保父地址池下的子地址池已全部被删除。\n- 当父地址池设置了生效地域，且该父地址池有已经分配出去的地址时，该父地址池不允许被删除。\n- 当子地址池设置了生效地域，且该地址池有已经分配出去的地址时，该子地址池不允许被删除。\n\n- 当地址池存在共享关系时，该地址池不允许被删除。",
    "en": "### Usage notes\n\n- Before deleting a parent pool, make sure that all subpools under the parent pool have been deleted.\n- When a parent pool has an effective region configured and has addresses that have already been allocated, the parent pool cannot be deleted.\n- When a subpool has an effective region configured and has addresses that have already been allocated, the subpool cannot be deleted.\n\n- When a pool has a sharing relationship, the pool cannot be deleted."
  },
  "example": {
    "unifiedCli": "aliyun vpcipam delete-ipam-pool --biz-region-id cn-hangzhou --ipam-pool-id ipam-pool-6rcq3tobayc20t0000 --dry-run false --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun vpcipam DeleteIpamPool --region cn-hangzhou --IpamPoolId 'ipam-pool-6rcq3tobayc20t0000' --DryRun false --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}