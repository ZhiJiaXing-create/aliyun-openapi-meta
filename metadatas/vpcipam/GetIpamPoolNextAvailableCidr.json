{
  "name": "GetIpamPoolNextAvailableCidr",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CidrBlock",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要分配的CIDR。\n\n> CidrBlock、CidrMask至少输入一个。",
        "en": "The CIDR block to be allocated.\n\n> You must specify CidrBlock or CidrMask."
      }
    },
    {
      "name": "CidrMask",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "需要分配的CIDR掩码长度。\n> CidrBlock、CidrMask至少输入一个。",
        "en": "The mask length of the CIDR block to be allocated.\n\n> You must specify CidrBlock or CidrMask."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n> 若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样。",
        "en": "The client token that is used to ensure the idempotence of the request. Generate a unique value for this parameter from your client. The ClientToken value can contain only ASCII characters.\n\n> If you do not specify this parameter, the system uses the RequestId as the ClientToken. The RequestId of each request is unique."
      }
    },
    {
      "name": "IpamPoolId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPAM地址池的实例ID。",
        "en": "The instance ID of the IPAM pool."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPAM地址池所在地域。\n> 当IPAM地址池具备地域属性时，该参数为IPAM地址池生效地域；当IPAM地址池不具备地域属性时，该参数为IPAM托管地域。",
        "en": "The region of the IPAM pool.\n\n> If the IPAM pool has a region property, this parameter specifies the region where the pool is active. If the IPAM pool does not have a region property, this parameter specifies the managed region of IPAM."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取IPAM地址池可用的CIDR地址段",
    "en": "GetIpamPoolNextAvailableCidr"
  },
  "example": {
    "unifiedCli": "aliyun vpcipam get-ipam-pool-next-available-cidr --biz-region-id cn-hangzhou --ipam-pool-id ipam-pool-6rcq3tobayc20t0000 --cidr-block 172.68.0.0/26 --cidr-mask 26 --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun vpcipam GetIpamPoolNextAvailableCidr --region cn-hangzhou --IpamPoolId 'ipam-pool-6rcq3tobayc20t0000' --CidrBlock '172.68.0.0/26' --RegionId 'cn-hangzhou' --CidrMask 26 --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}