{
  "name": "CreateIpamScope",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样。",
        "en": "A client token that is used to ensure the idempotence of the request. Generate a unique value from your client. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the RequestId of the request as the ClientToken. The RequestId of each API request may be different."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n- **true**：发送检查请求，不会创建IPAM作用范围。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码`DryRunOperation`。\n\n- **false**（默认值）：发送正常请求，通过检查后返回2xx HTTP状态码并创建IPAM作用范围。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: Sends a check request but does not create the IPAM scope. The request is checked for valid AccessKeys, RAM user permissions, and required parameters. If the check fails, a corresponding error is returned. If the check passes, the `DryRunOperation` error code is returned.\n\n- **false** (default): Sends a normal request. If the request passes the check, a 2xx HTTP status code is returned and the IPAM scope is created."
      }
    },
    {
      "name": "IpamId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPAM的实例ID。",
        "en": "The ID of the IPAM instance."
      }
    },
    {
      "name": "IpamScopeDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IPAM作用范围的描述信息。\n\n长度为1～256个字符，必须以英文大小写字母开头，但不能以`http://`或`https://`开头。不填则为空，默认值为空。",
        "en": "The description of the IPAM scope.\n\nThe description must be 1 to 256 characters in length and must start with a letter. It cannot start with `http://` or `https://`. The default value is an empty string."
      }
    },
    {
      "name": "IpamScopeName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IPAM作用范围的名称。\n\n长度为1～128个字符，不能以`http://`或`https://`开头。",
        "en": "The name of the IPAM scope.\n\nThe name must be 1 to 128 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "IpamScopeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IPAM作用范围类型：\n**private**。\n\n\n> 当前只支持创建私网作用范围。",
        "en": "The type of the IPAM scope: **private**.\n\n> Currently, you can create only private scopes."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPAM托管地域ID。您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region where the IPAM instance is hosted. Call the [DescribeRegions](~~36063~~) operation to get the region ID."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IPAM作用范围的资源组ID。",
        "en": "The ID of the resource group to which the IPAM scope belongs."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表信息。",
        "en": "The tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。最多支持20个标签键。一旦传入该值，则不允许为空字符串。\n\n最多支持64个字符，必须以字母或中文开头，可包含数字、半角句号（.）、下划线（_）和短划线（-），不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag key of the resource. You can specify up to 20 tag keys. The tag key cannot be an empty string.\n\nThe tag key can be up to 64 characters in length. It must start with a letter and can contain digits, periods (.), underscores (\\_), and hyphens (-). The tag key cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。最多支持输入20个标签值。一旦传入该值，可以为空字符串。\n\n最多支持128个字符，不能包含`http://`或者`https://`。",
            "en": "The tag value of the resource. You can specify up to 20 tag values. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length and cannot contain `http://` or `https://`."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建IPAM作用范围",
    "en": "CreateIpamScope"
  },
  "example": {
    "unifiedCli": "aliyun vpcipam create-ipam-scope --biz-region-id cn-hangzhou --ipam-id ipam-ccxbnsbhew0d6t0000 --ipam-scope-name test --ipam-scope-description test_description --resource-group-id rg-acfmxazb4ph6aiy0000 --dry-run false --client-token 123e4567-e89b-12d3-a456-426655440000 --ipam-scope-type private --tag Value=FinanceJoshua Key=FinanceDept --region cn-hangzhou",
    "legacyCli": "aliyun vpcipam CreateIpamScope --region cn-hangzhou --IpamScopeName test --Tag.1.Key FinanceDept --ResourceGroupId 'rg-acfmxazb4ph6aiy0000' --IpamId 'ipam-ccxbnsbhew0d6t0000' --Tag.1.Value FinanceJoshua --DryRun false --IpamScopeDescription test_description --RegionId 'cn-hangzhou' --IpamScopeType private --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}