{
  "name": "CreateLayerVersion",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/2023-03-30/layers/[layerName]/versions",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "Json",
      "required": true,
      "description": {
        "zh": "层配置信息",
        "en": "The configuration of the layer."
      }
    },
    {
      "name": "layerName",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "层名称",
        "en": "The name of the layer."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建层版本",
    "en": "CreateLayerVersion"
  },
  "example": {
    "unifiedCli": "aliyun fc create-layer-version --layer-name my-layer --region cn-hangzhou",
    "legacyCli": "aliyun fc POST /2023-03-30/layers/my-layer/versions --region cn-hangzhou --header \"Content-Type=application/json\" --body \"example-string\""
  }
}