{
  "name": "ListProvisionConfigs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/2023-03-30/provision-configs",
  "parameters": [
    {
      "name": "functionName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "函数名称，若不指定则列出所有函数的预留配置",
        "en": "The name of the function. If you do not specify this parameter, the provisioned configurations for all functions are returned."
      }
    },
    {
      "name": "limit",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "返回的预留配置数量",
        "en": "The number of provisioned configurations to return."
      }
    },
    {
      "name": "nextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页起始标记",
        "en": "The token to start the next page of results."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列出函数预留配置",
    "en": "ListProvisionConfigs"
  },
  "example": {
    "unifiedCli": "aliyun fc list-provision-configs --function-name my-func --next-token MTIzNCNhYmM= --limit 10 --region cn-hangzhou",
    "legacyCli": "aliyun fc GET /2023-03-30/provision-configs --region cn-hangzhou --functionName 'my-func' --nextToken 'MTIzNCNhYmM=' --limit 10 --header \"Content-Type=application/json\""
  }
}