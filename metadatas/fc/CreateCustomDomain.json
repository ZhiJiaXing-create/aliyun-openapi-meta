{
  "name": "CreateCustomDomain",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/2023-03-30/custom-domains",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "Json",
      "required": true,
      "description": {
        "zh": "自定义域名信息",
        "en": "The information about the custom domain name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建自定义域名",
    "en": "CreateCustomDomain"
  },
  "descriptions": {
    "zh": "如果您希望在生产环境中通过固定域名访问函数计算中创建的应用或函数，或者解决访问HTTP触发器时强制下载行为，可以通过为应用或函数绑定自定义域名来实现。",
    "en": "You can attach a custom domain name to an application or function in Function Compute to access it through a fixed domain name in a production environment, or to resolve the forced download behavior when you access an HTTP trigger."
  },
  "example": {
    "unifiedCli": "aliyun fc create-custom-domain --region cn-hangzhou",
    "legacyCli": "aliyun fc POST /2023-03-30/custom-domains --region cn-hangzhou --header \"Content-Type=application/json\" --body \"example-string\""
  }
}