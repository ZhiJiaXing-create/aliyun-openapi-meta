{
  "name": "UpdateConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "PUT",
  "pathPattern": "/api/v2/configs/[ClusterId]/[ConfigType]/[Name]/[ConfigKey]",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "集群/地域 ID（必填），例如 `cn-hangzhou`",
        "en": "The cluster or region ID. This parameter is required. For example, `cn-hangzhou`."
      }
    },
    {
      "name": "ConfigKey",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "动态参数名称（必填）",
        "en": "The dynamic parameter name. This parameter is required."
      }
    },
    {
      "name": "ConfigType",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "配置类型（必填），目前仅支持 `service`",
        "en": "The configuration type. This parameter is required. Currently, only `service` is supported."
      }
    },
    {
      "name": "Name",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "服务名称（必填）",
        "en": "The service name. This parameter is required."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "动态参数值（必填）",
        "en": "The request body. This parameter is required."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新动态参数",
    "en": "UpdateConfig"
  },
  "descriptions": {
    "zh": "## 请求说明\n本API允许用户通过指定`ClusterId`、`ConfigType`、`Name`和`ConfigKey`来更新特定动态参数的值。请求体中需要包含新的参数值`Value`。请确保提供的路径参数准确无误，以避免更新错误的动态参数。",
    "en": "## Request\n\nUse this API to update a dynamic parameter by providing its `ClusterId`, `ConfigType`, `Name`, and `ConfigKey`. The request body must contain the new parameter value in the `Value` field. Ensure you provide the correct path parameters to avoid errors."
  },
  "example": {
    "unifiedCli": "aliyun eas update-config --cluster-id cn-hangzhou --config-type service --name my-llm-service --config-key rate_limit --value 200 --region cn-hangzhou",
    "legacyCli": "aliyun eas PUT /api/v2/configs/cn-hangzhou/service/my-llm-service/rate_limit --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"Value\":\"200\"}"
  }
}