{
  "name": "DeleteConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v2/configs/[ClusterId]/[ConfigType]/[Name]/[ConfigKey]",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "**集群/地域 ID**",
        "en": "**region ID**<br>The region ID. Although this parameter is named `ClusterId`, it requires the region ID.<br>"
      }
    },
    {
      "name": "ConfigKey",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "**要删除的配置项名称**",
        "en": "**configuration key**<br>The key of the dynamic parameter to delete.<br>"
      }
    },
    {
      "name": "ConfigType",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "**配置类型**，目前仅支持 `service`",
        "en": "**configuration type**<br>The only supported value is `service`.<br>"
      }
    },
    {
      "name": "Name",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "**服务名称**",
        "en": "**service name**<br>The name of the service that contains the configuration.<br>"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除动态参数",
    "en": "DeleteConfig"
  },
  "descriptions": {
    "zh": "## 请求说明\n该 API 允许用户通过指定集群 ID、配置类型、服务名称以及配置项名称来删除特定的配置项。请确保提供的参数值准确无误，以避免误删重要配置。",
    "en": "## Request\n\nThis API deletes a specific configuration by its region ID, configuration type, service name, and configuration key. Ensure all parameter values are accurate to avoid accidentally deleting critical configurations."
  },
  "example": {
    "unifiedCli": "aliyun eas delete-config --cluster-id cn-hangzhou --config-type service --name my-llm-service --config-key rate_limit --region cn-hangzhou",
    "legacyCli": "aliyun eas DELETE /api/v2/configs/cn-hangzhou/service/my-llm-service/rate_limit --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}