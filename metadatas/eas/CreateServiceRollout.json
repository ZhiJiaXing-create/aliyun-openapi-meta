{
  "name": "CreateServiceRollout",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/api/v2/services/[ClusterId]/[ServiceName]/rollout",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。服务所在的地域标识，可通过ListServices接口获取可用地域列表。",
        "en": "The region ID where the service is deployed. Call the `ListServices` operation to view a list of available regions."
      }
    },
    {
      "name": "ServiceName",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "服务名称。要创建发布策略的服务名称，服务名称在同一地域内必须唯一。可通过DescribeService接口获取服务详情。",
        "en": "The name of the service to update. The name must be unique within the region. Call the `DescribeService` operation to get service details."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建服务更新计划",
    "en": "CreateServiceRollout"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n- **策略互斥**：`Partition`（分区发布）和`Batch`（批量发布）两种策略只能选择其中一种，不能同时使用。\n- **请求速率限制**：每秒最多100次请求。\n- **授权信息**：需要具备`eas:CreateServiceRollout`权限才能调用此接口。\n- **资源ARN**：`acs:eas:{#regionId}:{#accountId}:service/{#ServiceName}`。\n- **暂停发布**：通过设置`Paused`参数为`true`可以暂停发布流程，之后可通过`UpdateServiceRollout`接口恢复或取消发布。\n- **监控与回滚**：在发布过程中建议持续监控服务指标，以便及时发现并处理问题；如需回滚，可以通过调整`Partition`值或删除发布策略来实现。",
    "en": "## Usage notes\n\n- **Mutually exclusive strategies**: You can use either the `Partition` or the `Batch` strategy, but not both.\n\n- **Request rate limit**: Up to 100 requests per second.\n\n- **Authorization**: This operation requires the `eas:CreateServiceRollout` permission.\n\n- **ARN**: `acs:eas:{#regionId}:{#accountId}:service/{#ServiceName}`.\n\n- **Pause a rollout**: To pause the service rollout, set the `Paused` parameter to `true`. You can then resume or cancel the rollout by calling the `UpdateServiceRollout` operation.\n\n- **Monitoring and rollback**: Monitor service metrics during the service rollout to promptly identify and resolve issues. To perform a rollback, you can adjust the `Partition` value or delete the rollout strategy."
  },
  "example": {
    "unifiedCli": "aliyun eas create-service-rollout --cluster-id cn-shanghai --service-name my-service --partition Partition=1 --batch BatchSize=1 Interval=5m --paused false --region cn-hangzhou",
    "legacyCli": "aliyun eas POST /api/v2/services/cn-shanghai/my-service/rollout --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"Partition\":{\"Partition\":\"1\"},\"Batch\":{\"BatchSize\":\"1\",\"Interval\":\"5m\"},\"Paused\":false}"
  }
}