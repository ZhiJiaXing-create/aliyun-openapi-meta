{
  "name": "IsolateLeader",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/webapi/gateway/isolateLeader",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例 ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "IsolateLeader",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启读写分离（Leader FE节点处理写请求，其余节点处理读请求）。",
        "en": "Enable read/write splitting. The Leader FE node handles write requests, and other nodes handle read requests."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "开启读写分离",
    "en": "IsolateLeader"
  },
  "descriptions": {
    "zh": "调用该接口时实例必须处于运行中（Running）状态。",
    "en": "The instance must be in the running state when you call this operation."
  },
  "example": {
    "unifiedCli": "aliyun starrocks isolate-leader --instance-id c-b25e21e243880000 --isolate-leader true --region cn-hangzhou",
    "legacyCli": "aliyun starrocks POST /webapi/gateway/isolateLeader --region cn-hangzhou --InstanceId 'c-b25e21e243880000' --IsolateLeader true --header \"Content-Type=application/json\""
  }
}