{
  "name": "UnTagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/webapi/tags",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否全部删除，只针对 tags 为空时有效。默认为 false。",
        "en": "Specifies whether to remove all tags from the resources. This parameter is valid only when the TagKey parameter is left empty. The default value is false."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域 ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "Json",
      "required": true,
      "description": {
        "zh": "资源 ID列表，最多包含 50个子项。",
        "en": "A list of resource IDs. The list can contain up to 50 resource IDs."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。",
        "en": "The resource type."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "待解绑的标签键列表，最多包含 20 个子项。",
        "en": "A list of tag keys to remove. The list can contain up to 20 tag keys."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "解绑标签",
    "en": "UnTagResources"
  },
  "example": {
    "unifiedCli": "aliyun starrocks un-tag-resources --biz-region-id cn-hangzhou --resource-type instance --resource-id c-b25e21e243889XXX --tag-key k1 --all true --region cn-hangzhou",
    "legacyCli": "aliyun starrocks DELETE /webapi/tags --region cn-hangzhou --All true --ResourceId '[\"c-b25e21e243889XXX\"]' --TagKey '[\"k1\"]' --ResourceType instance --RegionId 'cn-hangzhou' --header \"Content-Type=application/json\""
  }
}