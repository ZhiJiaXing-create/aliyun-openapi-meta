{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "区域ID。",
        "en": "The ID of the region in which you want to create the instance."
      }
    },
    {
      "name": "ResourceIds",
      "position": "Query",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "资源ID列表。数组元素个数N的取值范围：1~1。",
        "en": "The list of resource IDs. Valid values of N: 1 to 1."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。 取值范围：\n- cluster：集群。",
        "en": "The type of the resource to which the tag belongs. Valid values:\n\n*   cluster: cluster"
      }
    },
    {
      "name": "Tags",
      "position": "Query",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "绑定的标签列表。",
        "en": "The tags."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "打标签",
    "en": "TagResources"
  },
  "example": {
    "unifiedCli": "aliyun emr tag-resources --biz-region-id cn-hangzhou --resource-type cluster --resource-ids c-b933c5aac8fe0000 --tags example-string --region cn-hangzhou",
    "legacyCli": "aliyun emr TagResources --region cn-hangzhou --Tags.1 'example-string' --ResourceIds.1 'c-b933c5aac8fe0000' --ResourceType cluster --RegionId 'cn-hangzhou' --method POST --force"
  }
}