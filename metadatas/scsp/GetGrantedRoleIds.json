{
  "name": "GetGrantedRoleIds",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "坐席账号名称，实例内唯一（登录名）",
        "en": "The agent account name, which is unique within the instance (logon name)."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户请求唯一id，用于幂等校验，可以用uuid生成",
        "en": "The unique client request ID for idempotence verification. You can use a UUID to generate this ID."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "AICCS实例ID，在智能联络中心控制台上可以看到",
        "en": "The instance ID of Artificial Intelligence Cloud Call Service. You can view the instance ID in the Artificial Intelligence Cloud Call Service console."
      }
    }
  ],
  "security": [
    "AK",
    "BearerToken"
  ],
  "title": {
    "zh": "获取授权给用户的角色id",
    "en": "Retrieve role iDs granted to a user"
  }
}