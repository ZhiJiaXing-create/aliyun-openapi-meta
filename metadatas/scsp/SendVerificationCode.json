{
  "name": "SendVerificationCode",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户请求唯一id，用于幂等校验，可以用uuid生成",
        "en": "The unique client request ID used for idempotence checks. You can use a UUID to generate this ID."
      }
    },
    {
      "name": "InstanceId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID",
        "en": "The instance ID."
      }
    },
    {
      "name": "Phone",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "手机号（坐席绑定手机号码）",
        "en": "The phone number bound to the agent."
      }
    }
  ],
  "security": [
    "AK",
    "BearerToken"
  ],
  "title": {
    "zh": "发送验证码短信",
    "en": "Send verification code SMS"
  },
  "descriptions": {
    "zh": "发送验证码时间窗口：2分钟 (即：2分钟之内，一个号码不允许重复发) ；\n验证码有效时间：15分钟 ；\n发送次数验证：6个小时内相同手机号码发送验证码次数不超过20次 ；",
    "en": "Verification code sending time window: 2 minutes (a number cannot receive duplicate codes within 2 minutes).\nVerification code validity period: 15 minutes.\nSending frequency limit: The same phone number cannot receive more than 20 verification codes within 6 hours."
  }
}