{
  "name": "AddZone",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。只支持ASCII字符，且不能超过64个字符。",
        "en": "A client token that is used to ensure the idempotence of the request. You can use the client to generate a token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "DnsGroup",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "内置权威位置区。取值：\n\n- **NORMAL_ZONE**：普通区（解析应答结果会被缓存，缓存不命中的情况下才回源到内置权威普通区，解析变更生效受TTL时间影响；无法使用自定义线路解析、权重解析功能。）\n- **FAST_ZONE**：快速区（推荐：直接应答解析请求、解析时延最低，记录变更实时生效；支持自定义线路解析、权重解析。）\n\n默认：**NORMAL_ZONE**\n\n>内置权威加速区在缓存模块前，解析结果不进入缓存，因此将产生更多解析量请求费用。                          \n\n><props=\"china\">2025年4月30日（UTC+8）起，新开通云解析PrivateZone的用户在添加Zone时，默认将设置为加速区Zone。</props>",
        "en": "The location of the built-in authoritative zone. Valid values:\n\n- **NORMAL\\_ZONE**: Standard zone. DNS responses are cached. If a cache miss occurs, the query is sent to the built-in authoritative standard zone. The time to live (TTL) value affects the time when a DNS record change takes effect. You cannot use custom DNS lines or weighted round-robin.\n\n- **FAST\\_ZONE**: Accelerated zone (recommended). DNS queries are directly responded to with the lowest latency. DNS record changes take effect in real time. You can use custom DNS lines and weighted round-robin.\n\nDefault value: **NORMAL\\_ZONE**.\n\n> The built-in authoritative accelerated zone is located before the cache module. DNS responses are not cached. This may increase the number of DNS queries and your costs.\n\n<props=\"china\">\n\n> Starting from April 30, 2025 (UTC+8), when new users activate Alibaba Cloud DNS PrivateZone, added zones are set as accelerated zones by default.\n\n</props>"
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回值语言，取值：\n\n* **zh**：中文。\n\n* **en**：英文。\n\n默认：**en**",
        "en": "The language of the response. Valid values:\n\n- **zh**: Chinese.\n\n- **en**: English.\n\nDefault value: **en**."
      }
    },
    {
      "name": "ProxyPattern",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启子域名递归代理。取值：\n- **ZONE**: 关闭（当解析本域名下不存在的子域名时，直接返回NXDOMAIN，表示子域名不存在）\n- **RECORD**: 开启（当解析本域名下不存在的子域名时，会依次查询“转发”和“递归”模块，并以最终查询到的解析结果来应答解析请求）\n\n默认：**ZONE**",
        "en": "Specifies whether to enable subdomain recursive proxy. Valid values:\n\n- **ZONE**: Disables the feature. If a DNS query for a subdomain that does not exist under the current domain name is received, an NXDOMAIN error is returned.\n\n- **RECORD**: Enables the feature. If a DNS query for a subdomain that does not exist under the current domain name is received, the query is processed by the forwarding and recursion modules in sequence. The final result is used to respond to the DNS query.\n\nDefault value: **ZONE**."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group."
      }
    },
    {
      "name": "ZoneName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要新增的zone名称。",
        "en": "The name of the zone to add."
      }
    },
    {
      "name": "ZoneTag",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "暂不对用户开放，无需传值。",
        "en": "This parameter is not available to users. You do not need to specify this parameter."
      }
    },
    {
      "name": "ZoneType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "暂不对用户开放，无需传值。",
        "en": "This parameter is not available to users. You do not need to specify this parameter."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "添加zone",
    "en": "AddZone"
  },
  "descriptions": {
    "zh": "2025年4月30日（UTC+8）起，新开通云解析PrivateZone的用户在添加Zone时，默认将设置为加速区Zone。 <props=\"china\">2025年10月30日（UTC+8）</props><props=\"intl\">2026年4月30日（UTC+8）</props>起，所有用户的普通区内置权威Zone将自动切换至加速区。切换后解析请求量可能增加，导致使用成本上升。建议为[ECS开启NSCD](~~2592999~~)，以减少无本地缓存引发的解析请求增长。",
    "en": "Starting from April 30, 2025 (UTC+8), zones added by new users of Alibaba Cloud DNS PrivateZone are set as accelerated zones by default. Starting from <props=\"china\">October 30, 2025 (UTC+8)</props><props=\"intl\">April 30, 2026 (UTC+8)</props>, all built-in authoritative standard zones will be automatically switched to accelerated zones. After the switch, the number of DNS queries may increase, which can increase your costs. To reduce the increase in DNS queries caused by the absence of a local cache, [enable NSCD for your ECS instances](~~2592999~~)."
  },
  "example": {
    "unifiedCli": "aliyun pvtz add-zone --lang en --zone-name example.com --proxy-pattern ZONE --resource-group-id rg-acfmykd63gt0000 --zone-type CLOUD_PRODUCT_ZONE --zone-tag BLINK --client-token 21079fa016944979537637959d09bc --dns-group FAST_ZONE --region cn-hangzhou",
    "legacyCli": "aliyun pvtz AddZone --region cn-hangzhou --ZoneName 'example.com' --ResourceGroupId 'rg-acfmykd63gt0000' --ProxyPattern ZONE --ZoneTag BLINK --Lang en --ClientToken 21079fa016944979537637959d09bc --DnsGroup FAST_ZONE --ZoneType CLOUD_PRODUCT_ZONE"
  }
}