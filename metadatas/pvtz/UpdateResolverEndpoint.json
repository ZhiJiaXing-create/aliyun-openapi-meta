{
  "name": "UpdateResolverEndpoint",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndpointId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "终端节点ID。",
        "en": "The endpoint ID."
      }
    },
    {
      "name": "IpConfig",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "出站流量源IP地址列表（至少添加2个，最多可添加6个）。\n\n>为了保证高可用，出站终端节点要求至少添加2个出站源IP地址，而且建议这2个IP地址分配在不同的可用区，出站终端节点允许添加的出站源IP地址最多为6个。",
        "en": "A list of source IP addresses for outbound traffic. Specify at least two and up to six IP addresses.\n\n> To ensure high availability (HA), an outbound endpoint requires at least two source IP addresses. We recommend that you assign these IP addresses to different zones. An outbound endpoint supports a maximum of six source IP addresses."
      },
      "sub_parameters": [
        {
          "name": "AzId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "交换机的可用区ID。",
            "en": "The ID of the zone where the vSwitch is located."
          }
        },
        {
          "name": "CidrBlock",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "子网掩码，交换机的IPv4网段。",
            "en": "The IPv4 CIDR block of the vSwitch."
          }
        },
        {
          "name": "Ip",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "子网掩码参数范围内的IP地址（留空则系统自动分配）。",
            "en": "An IP address that is within the CIDR block of the vSwitch. If you leave this parameter empty, the system automatically assigns an IP address."
          }
        },
        {
          "name": "VSwitchId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "交换机ID。",
            "en": "The vSwitch ID."
          }
        }
      ]
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回值语言，取值：\n\n- zh：中文。\n\n- en：英文。\n\n默认值：en",
        "en": "The language of the response. Valid values:\n\n- zh: Chinese.\n\n- en: English.\n\nDefault value: en"
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "终端节点名称。",
        "en": "The name of the endpoint."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改终端节点",
    "en": "UpdateResolverEndpoint"
  },
  "example": {
    "unifiedCli": "aliyun pvtz update-resolver-endpoint --endpoint-id hr0000 --name endpoint-test-name --ip-config VSwitchId=vsw-0jlgeyq4oazkh5xue0000 Ip=172.16.XX.XX CidrBlock=172.16.XX.XX/24 AzId=cn-hangzhou-a --lang en --region cn-hangzhou",
    "legacyCli": "aliyun pvtz UpdateResolverEndpoint --region cn-hangzhou --IpConfig.1.Ip '172.16.XX.XX' --IpConfig.1.CidrBlock '172.16.XX.XX/24' --IpConfig.1.VSwitchId 'vsw-0jlgeyq4oazkh5xue0000' --EndpointId hr0000 --IpConfig.1.AzId 'cn-hangzhou-a' --Lang en --Name 'endpoint-test-name'"
  }
}