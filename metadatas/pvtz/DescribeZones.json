{
  "name": "DescribeZones",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Keyword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Zone名称关键字，不区分大小写。你可通过设置搜索模式（SearchMode）切换为“精确搜索”或“模糊搜索”，默认为“模糊搜索”。",
        "en": "The keyword of the zone name. The search is not case-sensitive. You can set the SearchMode parameter to switch between exact search and fuzzy search. The default mode is fuzzy search."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回值语言，取值：\n\n- zh：中文。\n\n- en：英文。\n\n默认值：en",
        "en": "The language of the response. Valid values:\n\n- zh: Chinese.\n\n- en: English.\n\nDefault value: en."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时设置查询的页码，起始值为 1，默认为 1。",
        "en": "The page number. Pages start from page 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数，最大值**100**，默认为**20**。",
        "en": "The number of entries to return on each page. Maximum value: **100**. Default value: **20**."
      }
    },
    {
      "name": "QueryRegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Zone关联VPC所属的地域ID。",
        "en": "The region ID of the VPC that is associated with the zone."
      }
    },
    {
      "name": "QueryVpcId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "与zone关联的VPC ID。",
        "en": "The ID of the VPC that is associated with the zone."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "zone所属的资源组ID。",
        "en": "The ID of the resource group to which the zone belongs."
      }
    },
    {
      "name": "ResourceTag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "与zone关联的资源标签。",
        "en": "The resource tags that are added to the zone."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源标签键。",
            "en": "The key of the resource tag."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源标签值。",
            "en": "The value of the resource tag."
          }
        }
      ]
    },
    {
      "name": "SearchMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "搜索模式（作用于Keyword参数）。取值：\n\n- **LIKE**：模糊搜索（默认）\n- **EXACT**：精确搜索\n\n默认值：**LIKE**",
        "en": "The search mode. This parameter takes effect only when the Keyword parameter is specified. Valid values:\n\n- **LIKE**: fuzzy search (default)\n\n- **EXACT**: exact search\n\nDefault value: **LIKE**."
      }
    },
    {
      "name": "ZoneTag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "云产品类型列表。",
        "en": "The list of cloud service types."
      }
    },
    {
      "name": "ZoneType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Zone查询类型。取值：\n\n- **AUTH_ZONE**: 查权威Zone列表。\n- **CLOUD_PRODUCT_ZONE**:使用的云产品Zone列表。\n\n默认： **AUTH_ZONE**。",
        "en": "The type of the zones to query. Valid values:\n\n- **AUTH\\_ZONE**: authoritative zones.\n\n- **CLOUD\\_PRODUCT\\_ZONE**: zones for cloud services.\n\nDefault value: **AUTH\\_ZONE**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取 zone 列表",
    "en": "DescribeZones"
  },
  "example": {
    "unifiedCli": "aliyun pvtz describe-zones --lang en --page-number 1 --page-size 20 --keyword test --search-mode EXACT --query-region-id cn-hangzhou --query-vpc-id vpc-f8zvrvr1payllgz380000 --resource-group-id rg-aekz2qj7awz0000 --zone-type AUTH_ZONE --zone-tag BLINK --resource-tag Value=daily Key=env --region cn-hangzhou",
    "legacyCli": "aliyun pvtz DescribeZones --region cn-hangzhou --ResourceTag.1.Value daily --PageSize 20 --Keyword test --ResourceGroupId 'rg-aekz2qj7awz0000' --ZoneTag.1 BLINK --PageNumber 1 --QueryRegionId 'cn-hangzhou' --QueryVpcId 'vpc-f8zvrvr1payllgz380000' --ResourceTag.1.Key env --Lang en --SearchMode EXACT --ZoneType AUTH_ZONE"
  }
}