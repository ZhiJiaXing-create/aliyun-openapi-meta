{
  "name": "DescribePvtzStatisticsZoneOverview",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "支持的语言。取值：\n\n- zh-CN：中文\n- en-US：英文\n\n默认：en-US\n>优先级高于Lang。",
        "en": "The language of the response. Valid values:\n\n- `zh-CN`: Chinese\n\n- `en-US`: English\n\nDefault value: `en-US`.\n\n> This parameter takes precedence over the `Lang` parameter."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。",
        "en": "A client token to ensure request idempotence. Your client must generate a unique token for each request. The token can contain only ASCII characters."
      }
    },
    {
      "name": "Module",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "统计模块",
        "en": "The statistical module."
      }
    },
    {
      "name": "NetworkParams",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "网络参数",
        "en": "The network parameters."
      }
    },
    {
      "name": "OverviewPeriod",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "统计周期",
        "en": "The statistical period."
      }
    },
    {
      "name": "ServerRegion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "服务区域",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "Private Zone域名解析统计概览",
    "en": "DescribePvtzStatisticsZoneOverview"
  },
  "descriptions": {
    "zh": "此接口性能不高，不建议使用。请使用 DescribeZones 获取 zone 列表。如果获取绑定的 vpc 详情，则根据 zoneId 来调用接口 DescribeZoneInfo 来获取。",
    "en": "This is a low-performance operation and is not recommended. To retrieve a list of zones, use the DescribeZones operation. To get details of the VPCs bound to a zone, call the DescribeZoneInfo operation and specify the zone ID."
  },
  "example": {
    "unifiedCli": "aliyun pvtz describe-pvtz-statistics-zone-overview --accept-language zh-CN --client-token 234534535432323... --server-region cn-hangzhou --overview-period DAY --module AUTHORITY --network-params VpcType=STANDARD VpcId=vpc-uf68l2l04nqoyg7ie1kaw VpcOwner=5209821778041881 RegionId=cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun pvtz DescribePvtzStatisticsZoneOverview --region cn-hangzhou --NetworkParams.1.VpcOwner 5209821778041881 --NetworkParams.1.VpcId 'vpc-uf68l2l04nqoyg7ie1kaw' --NetworkParams.1.VpcType STANDARD --AcceptLanguage 'zh-CN' --ServerRegion 'cn-hangzhou' --NetworkParams.1.RegionId 'cn-hangzhou' --ClientToken '234534535432323...' --Module AUTHORITY --OverviewPeriod DAY --method POST --force"
  }
}