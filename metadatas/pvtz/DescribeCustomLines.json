{
  "name": "DescribeCustomLines",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "语言。",
        "en": "The language."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页码，默认为1。",
        "en": "The page number. The default value is 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数，最大值**100**，默认为**10**。",
        "en": "The number of entries to return on each page. The maximum value is **100**. The default value is **10**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询自定义线路列表",
    "en": "DescribeCustomLines"
  },
  "example": {
    "unifiedCli": "aliyun pvtz describe-custom-lines --lang en --page-number 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun pvtz DescribeCustomLines --region cn-hangzhou --PageSize 10 --PageNumber 1 --Lang en"
  }
}