{
  "name": "SetZoneRecordStatus",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。",
        "en": "A client token that is used to ensure the idempotence of the request. The client generates this value. The value must be unique among different requests and can be up to 64 ASCII characters in length."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回值语言，取值：\n\n- zh：中文。\n\n- en：英文。\n\n默认值：en",
        "en": "The language of the response. Valid values:\n\n- zh: Chinese.\n\n- en: English.\n\nThe default value is en."
      }
    },
    {
      "name": "RecordId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "解析记录ID。",
        "en": "The ID of the DNS record."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "解析记录状态。取值：\n\n- ENABLE: 启用解析\n- DISABLE: 暂停解析",
        "en": "The status of the DNS record. Valid values:\n\n- ENABLE: Enables DNS resolution.\n\n- DISABLE: Pauses DNS resolution."
      }
    },
    {
      "name": "UserClientIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户Ip。",
        "en": "The client IP address."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置解析记录状态",
    "en": "SetZoneRecordStatus"
  },
  "example": {
    "unifiedCli": "aliyun pvtz set-zone-record-status --lang en --record-id 2075410000 --status ENABLE --user-client-ip 127.XX.XX --client-token 6447728c8578e66aacf062d2df4446dc --region cn-hangzhou",
    "legacyCli": "aliyun pvtz SetZoneRecordStatus --region cn-hangzhou --Status ENABLE --UserClientIp '127.XX.XX' --Lang en --RecordId 2075410000 --ClientToken 6447728c8578e66aacf062d2df4446dc"
  }
}