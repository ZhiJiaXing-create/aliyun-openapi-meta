{
  "name": "UpdateRecordRemark",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大不超过64个ASCII字符。",
        "en": "A client token used to ensure the idempotence of a request. The client generates this value, which must be unique across different requests. The token can be up to 64 ASCII characters in length."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回值语言，取值：\n\n- zh：中文。\n\n- en：英文。\n\n默认值：en",
        "en": "The language of the response. Valid values:\n\n- zh: Chinese.\n\n- en: English.\n\nDefault value: en."
      }
    },
    {
      "name": "RecordId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "解析记录ID。",
        "en": "The ID of the DNS record."
      }
    },
    {
      "name": "Remark",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备注信息。",
        "en": "The remarks."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新解析记录备注",
    "en": "UpdateRecordRemark"
  },
  "example": {
    "unifiedCli": "aliyun pvtz update-record-remark --lang en --record-id 2029910000 --remark test_record --client-token 6447728c8578e66aacf062d2df4446dc --region cn-hangzhou",
    "legacyCli": "aliyun pvtz UpdateRecordRemark --region cn-hangzhou --Lang en --RecordId 2029910000 --ClientToken 6447728c8578e66aacf062d2df4446dc --Remark test_record"
  }
}