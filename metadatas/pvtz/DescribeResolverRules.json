{
  "name": "DescribeResolverRules",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndpointId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "出站终端节点ID。",
        "en": "The ID of the outbound endpoint."
      }
    },
    {
      "name": "Keyword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "”转发规则名称“关键字。支持模糊查询，不区分大小写。",
        "en": "The keyword for the name of the forwarding rule. The search is case-insensitive and supports fuzzy match."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回值语言，取值：\n\n- zh：中文。\n\n- en：英文。\n\n默认值：en",
        "en": "The language of the response. Valid values:\n\n- zh: Chinese.\n\n- en: English.\n\nDefault value: en"
      }
    },
    {
      "name": "NeedDetailAttributes",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否需要额外信息（转发规则关联的VPC列表）。取值：\n\n- true：需要。\n- false：不需要。\n\n默认值：false。",
        "en": "Specifies whether to return the list of virtual private clouds (VPCs) that are associated with the forwarding rule. Valid values:\n\n- true: returns the list.\n\n- false: does not return the list.\n\nDefault value: false."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时设置查询的页码，起始值为 1，默认为 1。",
        "en": "The number of the page to return. The start value is 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页数据量，最大值 100，默认为 20。",
        "en": "The number of entries to return on each page. Maximum value: 100. Default value: 20."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取转发规则列表",
    "en": "DescribeResolverRules"
  },
  "example": {
    "unifiedCli": "aliyun pvtz describe-resolver-rules --keyword test --lang en --page-number 1 --page-size 20 --need-detail-attributes true --endpoint-id hr0000 --region cn-hangzhou",
    "legacyCli": "aliyun pvtz DescribeResolverRules --region cn-hangzhou --NeedDetailAttributes true --Keyword test --PageSize 20 --PageNumber 1 --EndpointId hr0000 --Lang en"
  }
}