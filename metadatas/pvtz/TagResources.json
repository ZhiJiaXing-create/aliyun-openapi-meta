{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回值语言，取值：\n\n- zh：中文。\n\n- en：英文。\n\n默认值：en",
        "en": "The language of the response. Valid values:\n\n- zh: Chinese.\n\n- en: English.\n\nDefault value: en"
      }
    },
    {
      "name": "OverWrite",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "修改Tag列表。取值：\n\n- True：全量覆盖原有Tag列表。\n- False（默认）：在原有的Tag列表追加Tag，若Key相同Value不同，则覆盖Value。",
        "en": "Specifies whether to modify the tag list. Valid values:\n\n- True: Overwrites the existing tag list.\n\n- False (default): Appends tags to the existing list. If a tag key already exists, its value is updated."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID列表，即ZoneId列表。传入内容数量最大值为50。",
        "en": "The list of resource IDs. These are the IDs of the zones. You can specify up to 50 IDs."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型：ZONE。",
        "en": "The resource type. The value must be ZONE."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "标签列表。",
        "en": "The list of tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源标签键。",
            "en": "The tag key."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源标签值。",
            "en": "The tag value."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "标记资源",
    "en": "TagResources"
  },
  "descriptions": {
    "zh": "##### 使用限制\n目前仅支持设置Zone资源的标签信息。",
    "en": "##### Limits\n\nYou can add tags only to zone resources."
  },
  "example": {
    "unifiedCli": "aliyun pvtz tag-resources --lang en --resource-type ZONE --over-write true --resource-id 97fe9321a476d0861f624d3f738d0000 --tag Value=daily Key=env --region cn-hangzhou",
    "legacyCli": "aliyun pvtz TagResources --region cn-hangzhou --ResourceId.1 97fe9321a476d0861f624d3f738d0000 --Tag.1.Key env --Tag.1.Value daily --Lang en --ResourceType ZONE --OverWrite true"
  }
}