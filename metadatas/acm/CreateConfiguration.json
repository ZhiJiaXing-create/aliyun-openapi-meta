{
  "name": "CreateConfiguration",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/diamond-ops/pop/configuration",
  "parameters": [
    {
      "name": "AppName",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "配置归属的应用名称",
        "en": "The name of the application to which the configuration belongs."
      }
    },
    {
      "name": "Content",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "配置的内容",
        "en": "The content of the configuration."
      }
    },
    {
      "name": "DataId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "配置的ID。允许字符为大小写字母、数字、小数点（.）、冒号（:）、星号（*）、下划线（_）和中划线（-）。当ID前缀为如下值时，ACM会自动使用KMS服务对此配置进行加密存储：\n\n- 前缀为`cipher-`：调用 KMS 服务对配置进行加解密，加解密数据的大小不超过 4 KB，最大不超过 6 KB，对特殊符号如and（&amp;）会解密错误，不推荐使用。\n\n- 前缀为`cipher-kms-aes-128-`：使用 KMS 的信封加解密方法，配置内容可以超过 6 KB，最大不超过 100 KB。配置内容的明文数据不会传输到 KMS 系统，安全性更高，推荐使用。",
        "en": "The ID of the configuration. Valid characters include letters, digits, periods (.), colons (:), asterisks (*), underscores (_), and hyphens (-). If the ID has one of the following prefixes, Application Configuration Management (ACM) automatically uses Key Management Service (KMS) to encrypt the configuration for storage:\n\n- Prefix `cipher-`: Calls KMS to encrypt and decrypt the configuration. The size of data to be encrypted or decrypted cannot exceed 4 KB and the maximum size is 6 KB. Special characters such as ampersands (&) may cause decryption errors. This method is not recommended.\n\n- Prefix `cipher-kms-aes-128-`: Uses the envelope encryption method of KMS. The configuration content can exceed 6 KB, up to a maximum of 100 KB. The plaintext data of the configuration content is not transmitted to the KMS system, which provides higher security. This method is recommended."
      }
    },
    {
      "name": "Desc",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "配置的描述说明",
        "en": "The description of the configuration."
      }
    },
    {
      "name": "Group",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "分组",
        "en": "The group."
      }
    },
    {
      "name": "NamespaceId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "命名空间ID",
        "en": "The namespace ID."
      }
    },
    {
      "name": "Tags",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "配置的标签，如存在多个标签，以半角逗号分隔",
        "en": "The tags of the configuration. Separate multiple tags with commas (,)."
      }
    },
    {
      "name": "Type",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "配置内容的格式，可选值如下：\n\n- text\n\n- json\n\n- xml\n\n- yaml\n\n- text/html\n\n- properties",
        "en": "The format of the configuration content. Valid values:\n\n- text\n\n- json\n\n- xml\n\n- yaml\n\n- text/html\n\n- properties."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建配置",
    "en": "Create a configuration"
  }
}