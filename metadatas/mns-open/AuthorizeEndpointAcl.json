{
  "name": "AuthorizeEndpointAcl",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AclStrategy",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Acl策略。取值：\n\n- **allow**：表示此次操作的为Cidr白名单。(目前仅支持allow)",
        "en": "The access control list (ACL) policy. Valid value:\n\n- **allow**: A CIDR whitelist. Only allow is supported."
      }
    },
    {
      "name": "CidrList",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "网络地址段列表。",
        "en": "A list of CIDR blocks."
      }
    },
    {
      "name": "EndpointType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "接入点类型。取值：\n\n- **public**：表示公网接入点。(目前仅支持public)",
        "en": "The type of the endpoint. Valid value:\n\n- **public**: An internet endpoint. Only public is supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "增加接入点Acl规则",
    "en": "AuthorizeEndpointAcl"
  }
}