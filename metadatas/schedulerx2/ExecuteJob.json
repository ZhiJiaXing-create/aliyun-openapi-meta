{
  "name": "ExecuteJob",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CheckJobStatus",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否检查任务状态，取值如下：**true**：任务启用才能执行任务。**false**：任务不启用也可以执行任务。",
        "en": "Specifies whether to check the job status. Valid values: -**true**: The job can be run only if the job is enabled. -**false**: The job can be run even if the job is disabled."
      }
    },
    {
      "name": "DesignateType",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定机器的类型，取值如下：**1**：指定worker。**2**：指定label",
        "en": "The type of the designated machine. Valid values: -**1**: worker. -**2**: label."
      }
    },
    {
      "name": "GroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID，在控制台的应用管理页面中获取。",
        "en": "The application ID. You can obtain the application ID on the Application Management page in the SchedulerX console."
      }
    },
    {
      "name": "InstanceParameters",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "本次触发携带参数，可以是任意字符串，`processor`代码通过`context.getInstanceParameters()`获取，区别于创建任务自定义参数。",
        "en": "The parameters that are passed to trigger the job to run. The input value can be a random string. The parameters that are passed are obtained by calling the `context.getInstanceParameters()` class in the `processor` code. The parameters are different from custom parameters for creating jobs."
      }
    },
    {
      "name": "JobId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "任务ID，在控制台的任务管理页面中获取。",
        "en": "The job ID. You can obtain the job ID on the Task Management page in the SchedulerX console."
      }
    },
    {
      "name": "Label",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "worker的标签",
        "en": "The label of the worker."
      }
    },
    {
      "name": "Namespace",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "命名空间ID，在控制台的命名空间页面中获取。",
        "en": "The namespace ID. You can obtain the namespace ID on the Namespace page in the SchedulerX console."
      }
    },
    {
      "name": "NamespaceSource",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "特殊第三方才需要填写。",
        "en": "The source of the namespace. This parameter is required only for a special third party."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "Worker",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用的worker地址，需要先通过GetWokerList接口返回workerAddress",
        "en": "The worker address of the application. To query the worker address, call the GetWokerList operation."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "触发一次任务",
    "en": "ExecuteJob"
  },
  "descriptions": {
    "zh": "> 因为`JobID`联合`ScheduleTime`是唯一索引，所以同一个任务连续调用ExecuteJob接口的时，每次要sleep一秒，否则任务有可能会失败。",
    "en": "> The combination of the `JobID` and `ScheduleTime` parameters serves as a unique index. Therefore, after the ExecuteJob operation is called to run a job once, a sleep for one second is required before the ExecuteJob operation is called to run the job again. Otherwise, the job may fail."
  },
  "example": {
    "unifiedCli": "aliyun schedulerx2 execute-job --job-id 92583 --instance-parameters test --namespace adcfc35d-e2fe-4fe9-bbaa-20e90ffc0000 --namespace-source schedulerx --group-id testSchedulerx.defaultGroup --biz-region-id cn-hangzhou --check-job-status true --designate-type 1 --worker xxxxxxx@127.0.0.1:222 --label gray --region cn-hangzhou",
    "legacyCli": "aliyun schedulerx2 ExecuteJob --region cn-hangzhou --DesignateType 1 --CheckJobStatus true --NamespaceSource schedulerx --Label gray --InstanceParameters test --Worker 'xxxxxxx@127.0.0.1:222' --RegionId 'cn-hangzhou' --JobId 92583 --Namespace 'adcfc35d-e2fe-4fe9-bbaa-20e90ffc0000' --GroupId 'testSchedulerx.defaultGroup'"
  }
}