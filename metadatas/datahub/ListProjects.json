{
  "name": "ListProjects",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Keyword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页查询时的过滤关键词",
        "en": "The filter keyword for a paged query."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询返回的最大记录数",
        "en": "The maximum number of records to return in a paged query."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页查询的令牌（Token）。取值：如果NextToken为空表示从头开始查询。                                 否则从令牌所对应上一次查询末尾开始查询。",
        "en": "The pagination token. If NextToken is empty, paged query starts from the beginning. Otherwise, paged query starts from where the previous query ended based on the token."
      }
    },
    {
      "name": "Pure",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否仅返回主键信息",
        "en": "Specifies whether to return only primary key information."
      }
    },
    {
      "name": "Skip",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时跳过的记录数",
        "en": "The number of records to skip in a paged query."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取项目列表",
    "en": "Retrieve project list"
  },
  "descriptions": {
    "zh": "查询Project列表信息。",
    "en": "Queries the list of projects."
  }
}