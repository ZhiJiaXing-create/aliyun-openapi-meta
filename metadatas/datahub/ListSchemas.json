{
  "name": "ListSchemas",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询返回的最大记录数",
        "en": "The maximum number of records to return in a paginated query."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页查询的令牌（Token）。取值：如果NextToken为空表示从头开始查询。 否则从令牌所对应上一次查询末尾开始查询。",
        "en": "The pagination token. If NextToken is empty, the query starts from the beginning. Otherwise, the query starts from where the previous query left off based on the token."
      }
    },
    {
      "name": "ProjectName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "项目名称",
        "en": "The project name."
      }
    },
    {
      "name": "Skip",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时跳过的记录数",
        "en": "The number of records to skip in a paginated query."
      }
    },
    {
      "name": "TopicName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "topic名称",
        "en": "The topic name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取Schema列表",
    "en": "List Schemas"
  },
  "descriptions": {
    "zh": "查询schema列表信息。",
    "en": "Queries schema list information."
  }
}