{
  "name": "ChangeResourceGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。更多信息，请参见[如何保证幂等性](~~25693~~)。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "NewResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "新的企业资源组ID，可在资源组控制台查看可用企业资源组。企业资源组详情请参见[查看资源组基本信息](~~151181~~)。",
        "en": "The ID of the new resource group. You can view the available resource groups in the Resource Management console. For more information, see [View basic information of a resource group](~~151181~~)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源所属的地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the resource. You can call the [DescribeRegions](~~25609~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源ID。例如，当ResourceType=diskreplicapair时，则ResourceId可以理解为异步复制对ID。",
        "en": "The ID of the resource. For example, if you set ResourceType to diskreplicapair, set this parameter to the ID of a replication pair."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型定义。取值范围：\n\n- dedicatedblockstoragecluster：专属块存储集群。\n- diskreplicapair：异步复制关系。\n- diskreplicagroup：一致性复制组。",
        "en": "The type of the resource. Valid values:\n\n*   dedicatedblockstoragecluster: dedicated block storage cluster.\n*   diskreplicapair: replication pair.\n*   diskreplicagroup: replication pair-consistent group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改资源组",
    "en": "ChangeResourceGroup"
  },
  "example": {
    "unifiedCli": "aliyun ebs change-resource-group --biz-region-id cn-hangzhou --resource-type diskreplicapair --resource-id pair-123 --new-resource-group-id rg-123 --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun ebs ChangeResourceGroup --region cn-hangzhou --NewResourceGroupId 'rg-123' --ResourceId 'pair-123' --ResourceType diskreplicapair --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}