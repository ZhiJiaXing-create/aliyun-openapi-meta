{
  "name": "SetDedicatedBlockStorageClusterDiskThroughput",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Bps",
      "position": "Body",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "每秒读写（I/O）成功传输的数据数量上限，单位：MB/s。",
        "en": "Target throughput."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "服务端生成的一串字符串，作为客户端进行请求的一个标识。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that the value is unique among different requests. The ClientToken value can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "DiskId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云盘ID。",
        "en": "The ID of the disk."
      }
    },
    {
      "name": "RegionId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "专属块存储集群所在的地域ID。",
        "en": "The region ID of disk."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置专属块存储集群AutoPL云盘吞吐上限",
    "en": "SetDedicatedBlockStorageClusterDiskThroughput"
  },
  "descriptions": {
    "zh": "接口限制：\n\n- 吞吐下限：取值范围：60\n~ 单盘最大吞吐量；更多信息，请参见[云盘性能](~~25382~~)。\n\n- 吞吐上限：受限于[AutoPL云盘](~~368372~~)规格限制。\n\n- 云盘限制：规格AutoPL，是running状态的弹性型专属存储集群下的云盘，云盘状态需要是 In_use（使用中）或者Available（待挂载）。"
  },
  "example": {
    "unifiedCli": "aliyun ebs set-dedicated-block-storage-cluster-disk-throughput --disk-id d-bp67acfmxazb4p0000 --biz-region-id cn-hangzhou --bps 100 --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun ebs SetDedicatedBlockStorageClusterDiskThroughput --region cn-hangzhou --Bps 100 --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --DiskId 'd-bp67acfmxazb4p0000'"
  }
}