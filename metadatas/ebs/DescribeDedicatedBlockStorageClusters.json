{
  "name": "DescribeDedicatedBlockStorageClusters",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AzoneId",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "可用区ID，您可以调用[DescribeZones](~~25610~~)获取可用区列表。",
        "en": "The zone ID of the dedicated block storage cluster. You can call the [DescribeZones](~~25610~~) operation to query the most recent zone list."
      }
    },
    {
      "name": "Category",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专属块存储集群可创建的云盘类型。\n\n取值仅支持cloud_essd，即ESSD云盘。",
        "en": "The category of disks that can be created in the dedicated block storage cluster.\n\nSet the value to cloud_essd. Only enhanced SSDs (ESSDs) can be created in dedicated block storage clusters."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "服务端生成的一串字符串，作为客户端进行请求的一个标识。",
        "en": "A string generated by the server as an identifier for clients to send requests."
      }
    },
    {
      "name": "DedicatedBlockStorageClusterId",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "由一个或多个专属块存储集群id组成的数组格式",
        "en": "The IDs of the dedicated block storage clusters. You can specify the IDs of multiple dedicated block storage clusters in a request."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "Token方式最大个数。",
        "en": "The maximum number of entries to return in each request that uses a token to perform a query."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询凭证（Token），取值为上一次API调用返回的NextToken参数值。",
        "en": "The query token. Set the value to the NextToken value that is returned in the last call."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时的页码。",
        "en": "The page number."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数。\n取值范围1-100。",
        "en": "The number of entries per page. Valid values: 1 to 100."
      }
    },
    {
      "name": "RegionId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。您可以调用[DescribeRegions](~~2680071~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the dedicated block storage cluster. You can call the [DescribeRegions](~~25609~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专属集群所在的企业资源组ID。您可以调用[ListResourceGroups](~~158855~~)查询资源组ID。",
        "en": "The ID of the resource group to which the dedicated block storage cluster belongs."
      }
    },
    {
      "name": "Status",
      "position": "Body",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "专属块存储集群的状态。取值范围：\n\n- Preparing：待交付。\n- Running：运行中。\n- Expired：集群到期。\n- Offline：下线。\n\n可同时设置多个状态，取值范围为1~4。",
        "en": "The states of dedicated block storage clusters. Valid values:\n\n*   Preparing\n*   Running\n*   Expired\n*   Offline\n\nMultiple states can be specified. Valid values of N: 1, 2, 3, and 4."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表，最多包含20个子项。",
        "en": "The tags. Up to 20 tags are supported."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "专属集群的标签键。",
            "en": "The tag key of the dedicated block storage cluster."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "专属集群的标签值。",
            "en": "The tag value of the dedicated block storage cluster."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询专属块存储集群",
    "en": "DescribeDedicatedBlockStorageClusters"
  },
  "descriptions": {
    "zh": "## 接口说明\n\n> 专属块存储集群功能目前支持华南2（河源）、印度尼西亚（雅加达）以及华南1（深圳）地域。\n\n- 请求参数的作用类似于一个过滤器，过滤器为逻辑与（AND）关系。如果某一参数为空，则过滤器不起作用。\n- 在分页查询时，推荐您使用MaxResults与NextToken参数进行查询。具体使用说明：分页查询首页时，仅需设置MaxResults以限制返回信息的条目数，返回结果中的NextToken将作为查询后续页的凭证。查询后续页时，将NextToken参数设置为上一次返回结果中获取到的NextToken作为查询凭证，并设置MaxResults限制返回条目数。",
    "en": "## [](#)Usage notes\n\n>  The Dedicated Block Storage Cluster feature is available only in the China (Heyuan), Indonesia (Jakarta), and China (Shenzhen) regions.\n\n*   You can specify multiple request parameters to be queried. Specified parameters are evaluated by using the AND operator. Only the specified parameters are included in the filter conditions.\n*   We recommend that you use NextToken and MaxResults to perform paged queries. We recommend that you use MaxResults to specify the maximum number of entries to return in each request. The return value of NextToken is a pagination token, which can be used in the next request to retrieve a new page of results. When you call the DescribeDedicatedBlockStorageClusters operation to retrieve a new page of results, set NextToken to the NextToken value that is returned in the previous call and specify MaxResults to limit the number of entries returned."
  },
  "example": {
    "unifiedCli": "aliyun ebs describe-dedicated-block-storage-clusters --biz-region-id cn-hangzhou --azone-id cn-heyuan-b --dedicated-block-storage-cluster-id dbsc-cn-jaj3cli530g --status Running --category cloud_essd --tag Value=TestValue Key=TestKey --resource-group-id rg-acfmvs40000 --next-token 2c4d848a9628fb5e --max-results 10 --page-size 10 --page-number 1 --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun ebs DescribeDedicatedBlockStorageClusters --region cn-hangzhou --Status.1 Running --Tag.1.Key TestKey --Category cloud_essd --ResourceGroupId 'rg-acfmvs40000' --NextToken 2c4d848a9628fb5e --PageSize 10 --MaxResults 10 --DedicatedBlockStorageClusterId.1 'dbsc-cn-jaj3cli530g' --PageNumber 1 --Tag.1.Value TestValue --RegionId 'cn-hangzhou' --AzoneId 'cn-heyuan-b' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}