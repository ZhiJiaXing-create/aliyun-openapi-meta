{
  "name": "RemoveDiskReplicaPair",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。更多信息，请参见[如何保证幂等性](~~25693~~)。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "一致性复制组所属的地域ID。",
        "en": "The region ID of the replication pair-consistent group."
      }
    },
    {
      "name": "ReplicaGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "一致性复制组ID。\n\n您可以调用[DescribeDiskReplicaGroups](~~426614~~)查询一致性复制组ID。",
        "en": "The ID of the replication pair-consistent group.\n\nYou can call the [DescribeDiskReplicaGroups](~~426614~~) operation to query the IDs of replication pair-consistent groups."
      }
    },
    {
      "name": "ReplicaPairId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "复制对ID。",
        "en": "The ID of the replication pair."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "移除一致性复制组中的复制对",
    "en": "RemoveDiskReplicaPair"
  },
  "descriptions": {
    "zh": "## 接口说明\n\n- 一致性复制组功能支持的地域请参见[云盘异步复制概述](~~314563~~)。\n\n- 一致性复制组必须处于**已创建**（`created`）、**已停止**（`stopped`）或者**失效**（`invalid`）状态。",
    "en": "## [](#)Usage notes\n\n*   For information about the regions in which the replication pair-consistent group feature is available, see [Overview](~~314563~~).\n*   The replication pair-consistent group from which you want to remove a replication pair must be in the **Created** (`created`), **Stopped** (`stopped`), or **Invalid** (`invalid`) state."
  },
  "example": {
    "unifiedCli": "aliyun ebs remove-disk-replica-pair --biz-region-id cn-hangzhou --replica-group-id pg-myreplica0000 --replica-pair-id pair-cn-dsa0000 --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun ebs RemoveDiskReplicaPair --region cn-hangzhou --RegionId 'cn-hangzhou' --ReplicaPairId 'pair-cn-dsa0000' --ReplicaGroupId 'pg-myreplica0000' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}