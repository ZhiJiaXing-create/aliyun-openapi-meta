{
  "name": "CreateDiskReplicaGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Bandwidth",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "带宽值，单位：Kbps。\n>该参数暂未开放使用。",
        "en": "The bandwidth in Kbps.\n\n> This parameter is not yet available."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。更多信息，请参见[如何保证幂等性](~~25693~~)。",
        "en": "A client token to ensure the idempotence of the request. Generate a unique value from your client for this parameter. The \\`ClientToken\\` parameter value can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "一致性组的描述信息。长度为2~256个英文或中文字符，不能以`http:/`/或`https://`开头。",
        "en": "The description of the replication pair-consistent group. The description must be 2 to 256 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "DestinationRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "灾备站点所属的地域ID。",
        "en": "The ID of the region where the disaster recovery site is located."
      }
    },
    {
      "name": "DestinationZoneId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "灾备站点所属的可用区ID。",
        "en": "The ID of the zone where the disaster recovery site is located."
      }
    },
    {
      "name": "EnableRtc",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启复制时间控制（RTC）。取值范围：\n\n- false：关闭。\n- true：开启。\n\n默认值：false。\n\n> 如果设置为ture，表示一致性组开启复制时间控制（RTC），加入该一致性组的异步复制对均开启RTC。",
        "en": "Specifies whether to enable replication time control (RTC). Valid values:\n\n- false: Disable RTC.\n\n- true: Enable RTC.\n\nDefault value: false.\n\n> If you set this parameter to true, RTC is enabled for the replication pair-consistent group. RTC is also enabled for all asynchronous replication pairs that are added to the group."
      }
    },
    {
      "name": "GroupName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "一致性复制组的名称。长度为2~128个英文或中文字符。必须以大小写字母或中文开头，不能以`http://`或`https://`开头。可以包含数字、半角冒号（:）、下划线（_）或者短划线（-）。",
        "en": "The name of the replication pair-consistent group. The name must be 2 to 128 characters in length. It must start with a letter or a Chinese character, and cannot start with `http://` or `https://`. It can contain digits, colons (:), underscores (\\_), and hyphens (-)."
      }
    },
    {
      "name": "RPO",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "一致性组设定的RPO值，单位：秒。当前仅支持900秒。",
        "en": "The recovery point objective (RPO) of the replication pair-consistent group, in seconds. The only supported value is 900."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "一致性复制组所属的地域ID，与生产站点所属的地域相同。",
        "en": "The ID of the region where the replication pair-consistent group resides. This is the same as the region of the production site."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "一致性复制组所在的企业资源组ID。",
        "en": "The ID of the resource group to which the replication pair-consistent group belongs."
      }
    },
    {
      "name": "SourceZoneId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "生产站点所属的可用区ID。",
        "en": "The ID of the zone where the production site is located."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表，最多包含20个子项。",
        "en": "The tags to add to the resource. You can add up to 20 tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "一致性复制组的标签键。",
            "en": "The key of the tag."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "一致性复制组的标签值。",
            "en": "The value of the tag."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建异步复制组",
    "en": "CreateDiskReplicaGroup"
  },
  "descriptions": {
    "zh": "## 接口说明\n\n一致性复制组能够实现业务系统跨多块云盘的容灾场景下，对多块云盘的异步复制做统一管理和操作，同时可以确保同一复制组内的多块云盘数据能够恢复到同一个时间点，以便实现容灾场景下实例级别或者多实例级别的容灾保护。\n\n创建一致性复制组时，您需要注意：\n\n- 一致性复制组功能支持的地域请参见[云盘异步复制概述](~~314563~~)。\n\n- 一致性复制组支持同地域跨可用区或跨地域的异步容灾。\n\n- 只有复制对与一致性复制组的数据复制方向相同（即生产地域、生产可用区、容灾地域、容灾可用区分别相同）时，才能将复制对加入到该一致性复制组中。\n\n- 将复制对加入到一致性复制组后，复制对原有的RPO（Recovery Point Object）会失效，并按照一致性复制组的RPO进行复制。",
    "en": "## Operation Description\n\nReplication pair-consistent groups help you manage asynchronous replication for multiple disks in disaster recovery scenarios. You can centrally manage operations for these disks. The groups ensure that data on all disks can be recovered to the same point in time. This provides disaster recovery protection for one or more instances.\n\nWhen you create a replication pair-consistent group, note the following:\n\n- For information about the regions that support replication pair-consistent groups, see [Async replication overview](~~314563~~).\n\n- Replication pair-consistent groups support asynchronous disaster recovery across zones in the same region or across regions.\n\n- A replication pair can be added to a replication pair-consistent group only if the pair and the group have the same data replication direction. This means their production regions, production zones, disaster recovery regions, and disaster recovery zones must be the same.\n\n- After a replication pair is added to a replication pair-consistent group, the original Recovery Point Object (RPO) of the pair becomes invalid. Data is then replicated based on the RPO of the group."
  },
  "example": {
    "unifiedCli": "aliyun ebs create-disk-replica-group --biz-region-id cn-hangzhou --source-zone-id cn-beijing-f --destination-region-id cn-shanghai --destination-zone-id cn-shanghai-e --group-name myreplicagrouptest --description This_is_description. --client-token 123e4567-e89b-12d3-a456-426655440000 --rpo 900 --bandwidth 5 --resource-group-id rg-acfmvs0000000 --tag Value=tag-value Key=tag-key --enable-rtc true --region cn-hangzhou",
    "legacyCli": "aliyun ebs CreateDiskReplicaGroup --region cn-hangzhou --GroupName myreplicagrouptest --Tag.1.Key 'tag-key' --Description 'This_is_description.' --ResourceGroupId 'rg-acfmvs0000000' --RPO 900 --DestinationRegionId 'cn-shanghai' --EnableRtc true --DestinationZoneId 'cn-shanghai-e' --Bandwidth 5 --Tag.1.Value 'tag-value' --RegionId 'cn-hangzhou' --SourceZoneId 'cn-beijing-f' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}