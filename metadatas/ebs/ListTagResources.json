{
  "name": "ListTagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。**ClientToken**只支持ASCII字符，且不能超过64个字符。更多详情，请参见[如何保证幂等性](~~25693~~)。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must ensure that the value is unique among different requests. The **ClientToken** value can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "下一个查询开始Token。",
        "en": "The token used to start the next query."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源所属的地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the resource. You can call the [DescribeRegions](~~25609~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源ID，最多包含50个子项。",
        "en": "The ID list of the resource. You can specify up to 50 resource IDs in each call."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型定义。取值范围：\n\n- dedicatedblockstoragecluster：专属块存储集群。\n- diskreplicapair：异步复制关系。\n- diskreplicagroup：一致性复制组。",
        "en": "The type of the resource. Valid values:\n\n*   dedicatedblockstoragecluster: dedicated block storage cluster\n*   diskreplicapair: replication pair\n*   diskreplicagroup: replication pair-consistent group"
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表，最多包含20个子项",
        "en": "The information about the tags.\n\nYou can specify at most 20 tags in each call."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "精确查找EBS资源时使用的标签键。标签键长度的取值范围为1~128。N的取值范围为1~20。\n\n`Tag.N`参数使用说明：用于精确查找绑定了指定标签的EBS资源，由一个键值对组成。\n\n- 仅指定`Tag.N.Key`时，则返回关联该标签键的所有资源。\n\n- 仅指定`Tag.N.Value`，则报错`InvalidParameter.TagValue`。\n\n- 同时指定多个标签键值对时，仅同时满足所有标签键值对的EBS资源会被查找到。",
            "en": "The key of tag N used for exact search of EBS resources. The tag key must be 1 to 128 characters in length. Valid values of N: 1 to 20.\n\nThe `Tag.N` parameter pair (Tag.N.Key and Tag.N.Value) is used for exact search of EBS resources that have specified tags added. Each tag is a key-value pair.\n\n*   If you specify only `Tag.N.Key`, all EBS resources whose tags contain the specified tag key are returned.\n*   If you specify only `Tag.N.Value`, the `InvalidParameter.TagValue` error is returned.\n*   If you specify multiple tag key-value pairs at the same time, only EBS resources that match all tag key-value pairs are returned."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "精确查找EBS资源时使用的标签值。标签值长度的取值范围为1~128。N的取值范围为1~20。",
            "en": "The value of tag N used for exact search of EBS resources. The tag value must be 1 to 128 characters in length. Valid values of N: 1 to 20."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询资源标签",
    "en": "ListTagResources"
  },
  "descriptions": {
    "zh": "## 接口说明\n\n请求中至少指定以下任一参数，以确定查询对象。\n\n- `ResourceId.N`\n- `Tag.N`（`Tag.N.Key`与`Tag.N.Value`）\n\n\n同时指定`Tag.N`和`ResourceId.N`参数时，返回结果中仅包含同时满足这两个条件的EBS资源。",
    "en": "Specify at least one of the following parameters or parameter pairs in a request to determine a query object:\n\n*   `ResourceId.N`\n*   `Tag.N` parameter pair (`Tag.N.Key` and `Tag.N.Value`)\n\nIf you set `Tag.N` and `ResourceId.N` at the same time, the EBS resources that match both the parameters are returned."
  },
  "example": {
    "unifiedCli": "aliyun ebs list-tag-resources --resource-id pair-cn-c4d2t7f0000 --tag Value=tag-value Key=tag-key --next-token token123 --biz-region-id cn-hangzhou --resource-type diskreplicagroup --client-token 0c593ea1-3bea-11e9-b96b-88e9fe630000 --region cn-hangzhou",
    "legacyCli": "aliyun ebs ListTagResources --region cn-hangzhou --ResourceId.1 'pair-cn-c4d2t7f0000' --Tag.1.Key 'tag-key' --NextToken token123 --Tag.1.Value 'tag-value' --ResourceType diskreplicagroup --RegionId 'cn-hangzhou' --ClientToken '0c593ea1-3bea-11e9-b96b-88e9fe630000'"
  }
}