{
  "name": "DescribeSolutionInstanceConfiguration",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。更多信息，请参见[如何保证幂等性](~~25693~~)。",
        "en": "The client token that is used to ensure the idempotency of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "Parameters",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "配置查询参数。",
        "en": "The parameters."
      },
      "sub_parameters": [
        {
          "name": "ParameterKey",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "模板中已定义的参数的名称。\n\n> Parameters为可选参数。若指定了Parameters，则ParameterKey为必选参数。",
            "en": "The key of the parameter."
          }
        },
        {
          "name": "ParameterValue",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "模板中已定义的参数的取值。\n\n> Parameters为可选参数。若指定了Parameters，则ParameterValue为必选参数。",
            "en": "The value of the parameter.\n\n> The Parameters parameter is optional. If you specify Parameters, you must specify ParameterValue."
          }
        }
      ]
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域。",
        "en": "The region ID."
      }
    },
    {
      "name": "SolutionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "解决方案ID。",
        "en": "The ID of the solution."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询解决方案默认配置",
    "en": "DescribeSolutionInstanceConfiguration"
  },
  "example": {
    "unifiedCli": "aliyun ebs describe-solution-instance-configuration --solution-id sln-xxxxx --biz-region-id cn-hangzhou --parameters ParameterValue=000 ParameterKey=000 --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun ebs DescribeSolutionInstanceConfiguration --region cn-hangzhou --SolutionId 'sln-xxxxx' --Parameters.1.ParameterKey 000 --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --Parameters.1.ParameterValue 000"
  }
}