{
  "name": "GetReport",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "可选值，只在ReportType=present时AppName才会生效",
        "en": "Optional, AppName only takes effect when ReportType=present."
      }
    },
    {
      "name": "RegionId",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "必填，region名称",
        "en": "Region name."
      }
    },
    {
      "name": "ReportId",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "ReportType=history时，必须传入ReportId，根据ReportId查询历史报告。",
        "en": "When ReportType=history, ReportId is required to query historical reports based on ReportId."
      }
    },
    {
      "name": "ReportType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "可选值：history/present",
        "en": "Optional values: history/present."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取用户云盘使用报告",
    "en": "Get User Cloud Disk Usage Report"
  },
  "example": {
    "unifiedCli": "aliyun ebs get-report --report-id report-74fbea80e802xxxx --biz-region-id cn-hangzhou --report-type history --app-name App1 --region cn-hangzhou",
    "legacyCli": "aliyun ebs GetReport --region cn-hangzhou --ReportId 'report-74fbea80e802xxxx' --RegionId 'cn-hangzhou' --ReportType history --AppName App1"
  }
}