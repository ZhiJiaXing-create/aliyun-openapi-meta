{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域和可用区名称的语言类型。对应返回参数中`LocalName`的返回值。取值范围：\n\n- zh-CN：中文。\n- en-US：英文。\n- ja：日文。\n\n默认值：zh-CN。",
        "en": "The language in which the regions and zones are named. This parameter corresponds to the `LocalName` response parameter. Valid values:\n\n*   zh-CN: Chinese\n*   en-US: English\n*   ja: Japanese\n\nDefault value: zh-CN."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户访问端的地域ID。",
        "en": "The ID of the region."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源类型。取值范围：\n\n- ear：异步复制。\n\n- lens：数据洞察。\n\n- dbsc: 专属块存储集群。\n\n未指定资源类型时，将会返回所有资源类型支持的地域信息。",
        "en": "The type of resource. Valid values:\n\n*   ear: async replication\n*   lens: CloudLens for EBS\n*   dbsc: Dedicated Block Storage Cluster\n\nDefault value: ear."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询地域信息",
    "en": "DescribeRegions"
  },
  "example": {
    "unifiedCli": "aliyun ebs describe-regions --resource-type ear --accept-language zh-CN --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun ebs DescribeRegions --region cn-hangzhou --AcceptLanguage 'zh-CN' --ResourceType ear --RegionId 'cn-hangzhou'"
  }
}