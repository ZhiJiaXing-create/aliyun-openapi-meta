{
  "name": "ListReplicaEdgeSupported",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Azone",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "可用区ID。",
        "en": "The zone ID."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页的最大条目数。取值范围：1~500。该参数暂未启用。\n\n默认值：10。",
        "en": "The maximum number of entries to return on each page. Valid values: 1 to 500. This parameter is not enabled.\n\nDefault value: 10."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询凭证（Token）。取值为上一次调用该接口返回的NextToken参数值，初次调用接口时无需设置该参数。如果设置了NextToken，则请求参数PageSize和PageNumber将失效，且返回数据中的TotalCount无效。该参数暂未启用。",
        "en": "The token that is used to start the next query. Set this parameter to the value of NextToken that was returned by the last call. Do not set this parameter for the first call. If you set NextToken, PageSize and PageNumber are ignored. TotalCount is also invalid in the response. This parameter is not enabled."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询可用区支持的异步复制目的地域和可用区",
    "en": "ListReplicaEdgeSupported"
  },
  "example": {
    "unifiedCli": "aliyun ebs list-replica-edge-supported --biz-region-id cn-hangzhou --azone cn-hangzhou-h --next-token e71d8a535bd9c0000 --max-results 10 --region cn-hangzhou",
    "legacyCli": "aliyun ebs ListReplicaEdgeSupported --region cn-hangzhou --Azone 'cn-hangzhou-h' --NextToken e71d8a535bd9c0000 --MaxResults 10 --RegionId 'cn-hangzhou'"
  }
}