{
  "name": "StartSyncExecution",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ExecutionName",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "执行名称。取值说明如下：\n\n- 支持英文字符（a~z）或（A~Z）、数字（0~9）、下划线（_）和短划线（-）。\n- 首字母必须为英文字母（a~z）、（A~Z）或下划线（_）。\n- 区分大小写。\n- 长度为1~128个字符。\n\n不同于StartExecution接口，考虑到同步调用的特殊性，在同步执行模式下，不再要求执行名称在同一流程内唯一，调用侧可以选择提供执行名称，对本次执行进行标识，系统会在当前执行名称后添加UUID，具体形式如 {ExecutionName}:{UUID}，如果用户没有指定相关的执行名称信息，那么系统会自动生成标识本次执行的ExecutionName。",
        "en": "The name of the execution. The name must meet the following requirements:\n\n- It can contain letters (a–z and A–Z), digits (0–9), underscores (\\_), and hyphens (-).\n\n- It must start with a letter (a–z or A–Z) or an underscore (\\_).\n\n- It is case-sensitive.\n\n- It must be 1 to 128 characters in length.\n\nUnlike the StartExecution operation, the execution name for a synchronous call does not need to be unique within a flow. You can specify an execution name to identify the execution. The system appends a UUID to the name in the \\`{ExecutionName}:{UUID}\\` format. If you do not specify an execution name, the system automatically generates one."
      }
    },
    {
      "name": "FlowName",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "开始执行的流程名称。",
        "en": "The name of the flow to execute."
      }
    },
    {
      "name": "Input",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "执行的输入，为JSON对象格式。",
        "en": "The input of the execution. The input must be in JSON format."
      }
    },
    {
      "name": "Qualifier",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定流程版本或别名",
        "en": "The version or alias of the flow."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "同步调用开始一个流程的执行",
    "en": "StartSyncExecution"
  },
  "descriptions": {
    "zh": "- 仅支持 Express 执行模式的流程。",
    "en": "- This operation supports only flows in Express mode."
  },
  "example": {
    "unifiedCli": "aliyun fnf start-sync-execution --flow-name my_flow_name --execution-name my_exec_name --input key_:_value --qualifier 1 --region cn-hangzhou",
    "legacyCli": "aliyun fnf StartSyncExecution --region cn-hangzhou --ExecutionName my_exec_name --Input 'key_:_value' --FlowName my_flow_name --Qualifier 1"
  }
}