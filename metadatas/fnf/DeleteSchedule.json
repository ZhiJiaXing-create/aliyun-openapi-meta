{
  "name": "DeleteSchedule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FlowName",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待删除调度任务绑定的流程名称。",
        "en": "The name of the workflow with which the scheduling task that you want to delete is associated."
      }
    },
    {
      "name": "ScheduleName",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待删除调度的名称。",
        "en": "The name of the scheduling task that you want to delete."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除一个定时调度（仅适用于旧版工作流）",
    "en": "DeleteSchedule"
  },
  "example": {
    "unifiedCli": "aliyun fnf delete-schedule --flow-name flow --schedule-name testScheduleName --region cn-hangzhou",
    "legacyCli": "aliyun fnf DeleteSchedule --region cn-hangzhou --FlowName flow --ScheduleName testScheduleName"
  }
}