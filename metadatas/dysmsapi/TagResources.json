{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ProdCode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "产品名。默认取值：**dysms**。",
        "en": "The product name. Default value: **dysms**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID，默认取值：**cn-hangzhou**。更多地域ID请参见[服务接入点](~~419270~~)。",
        "en": "The region ID. Default value: **cn-hangzhou**. For more region IDs, see [Endpoints](~~419270~~)."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "短信模板Code。数量不能超过20条。",
        "en": "The SMS template codes. The number of codes cannot exceed 20."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型，默认取值：**TEMPLATE**。",
        "en": "The resource type. Default value: **TEMPLATE**."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "标签。单次添加的标签数量不超过20。",
        "en": "The tags. You can add up to 20 tags at a time."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签键。",
            "en": "The tag key."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签值。",
            "en": "The tag value."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "添加模板标签",
    "en": "Add template tags"
  },
  "descriptions": {
    "zh": "- 每个模板最多可以绑定20个标签。\n- 同一个模板的标签键（Key）必须唯一，一个模板如果设置了同Key不同Value的两个标签，新值将覆盖旧值。\n- 此功能仅适用于中国站短信服务的国内文本短信。\n\n### QPS限制\n本接口的单用户QPS限制为50次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "- Each template can be bound to up to 20 tags.\n- The tag key (Key) must be unique within the same template. If a template has two tags with the same Key but different Values, the new value overwrites the old value.\n- This feature is only available for domestic text messages of Short Message Service on the China site.\n\n### QPS limit\nThe per-user QPS limit of this operation is 50 calls per second. If the limit is exceeded, API calls are throttled, which may affect your business. Call this operation at a reasonable frequency."
  }
}