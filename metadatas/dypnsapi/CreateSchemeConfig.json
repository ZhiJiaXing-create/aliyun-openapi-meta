{
  "name": "CreateSchemeConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AndroidPackageName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "包名，Platform为Android时必选。由数字、字母、短划线（-）、下划线或点组成，长度限制为1~128个字符。",
        "en": "The package name. This parameter is required when Platform is set to Android. The name must be 1 to 128 characters in length and can contain digits, letters, hyphens (-), underscores (\\_), and periods (.)."
      }
    },
    {
      "name": "AndroidPackageSign",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "包签名，Platform为Android时必选。由数字和字母组成，长度为32个字符。",
        "en": "The package signature. This parameter is required when Platform is set to Android. The signature must be 32 characters in length and can contain digits and letters."
      }
    },
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用方案的App名称，由中、英文字母构成，长度限制20个字符。",
        "en": "The app name, which can be up to 20 characters in length and can contain letters."
      }
    },
    {
      "name": "H5Origin",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保留字段，暂不支持H5方案。",
        "en": "The reserved field. HTML5 apps are not supported."
      }
    },
    {
      "name": "H5Url",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保留字段，暂不支持H5方案。",
        "en": "The reserved field. HTML5 apps are not supported."
      }
    },
    {
      "name": "IosBundleId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "BundleID，osType为iOS时必选。由数字、字母、短划线（-）、下划线或点组成，长度限制为1~128个字符。",
        "en": "The bundle ID. This parameter is required when OsType is set to iOS. The bundle ID must be 1 to 128 characters in length and can contain digits, letters, hyphens (-), underscores (\\_), and periods (.)."
      }
    },
    {
      "name": "Platform",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "方案应用平台。",
        "en": "The app platform.\n\nValid values:\n\n*   Android\n*   iOS"
      }
    },
    {
      "name": "SchemeName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "方案名称，由中、英文字母构成，长度限制为10个字符。",
        "en": "The service name, which can be up to 10 characters in length and can contain letters."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建融合认证方案号",
    "en": "CreateSchemeConfig"
  }
}