{
  "name": "CheckSmsVerifyCode",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CaseAuthPolicy",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "验证码大小写字母核验策略。取值：\n\n- 1：不区分大小写。\n- 2：区分大小写。",
        "en": "The case-sensitivity policy for verification. Valid values:\n\n- `1`: Case-insensitive.\n\n- `2`: Case-sensitive."
      }
    },
    {
      "name": "CountryCode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "号码国家编码，默认为86。",
        "en": "The country code of the phone number. The default value is `86`."
      }
    },
    {
      "name": "OutId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "外部流水号。",
        "en": "An external ID to track the request."
      }
    },
    {
      "name": "PhoneNumber",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "手机号。",
        "en": "The phone number."
      }
    },
    {
      "name": "SchemeName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "方案名称，如果不填则为“默认方案”。最多不超过20个字符。\n><notice>如果发送接口的方案名称不为空，请确保该参数不为空且与发送接口的方案名称参数一致></notice>",
        "en": "The scheme name. The name can contain up to 20 characters. If you do not specify this parameter, the API uses the default scheme.\n><notice>This value must match the `SchemeName` specified in the `SendSmsVerifyCode` request.></notice>"
      }
    },
    {
      "name": "VerifyCode",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "验证码。\n\n> - SendSmsVerifyCode接口的字段TemplateParam，配置方式有2种：\n> - - {\"code\":\"##code##\",\"min\":\"5\"}\n> - - {\"code\":\"123456\",\"min\":\"5\"}\n> - {\"code\":\"##code##\",\"min\":\"5\"}验证码是api动态生成的，阿里云接口可以完成校验。\n> - {\"code\":\"123456\",\"min\":\"5\"}验证码是用户配置的不是api动态生成，阿里云接口无法校验。\n> - 请您按照实际情况传入对应的验证码。",
        "en": "The verification code.\n\n> - The `TemplateParam` parameter in the `SendSmsVerifyCode` request can be configured in two ways:\n>\n> - - `{\"code\":\"##code##\",\"min\":\"5\"}`\n>\n> - - `{\"code\":\"123456\",\"min\":\"5\"}`\n>\n> - If you use `{\"code\":\"##code##\",\"min\":\"5\"}`, the API dynamically generates the verification code, and Alibaba Cloud can verify it.\n>\n> - If you use `{\"code\":\"123456\",\"min\":\"5\"}`, you specify the verification code instead of the API generating one. Alibaba Cloud cannot verify this code.\n>\n> - Ensure you pass the correct verification code for your configuration."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "核验验证码",
    "en": "CheckSmsVerifyCode"
  }
}