{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否全部删除，只针对TagKey.N为空时有效。取值范围：\n\n- true\n\n- false\n\n默认是 false",
        "en": "Remove all tags. This parameter takes effect only when TagKey.N is empty. Valid values:\n\n- true\n\n- false\n\nDefault value: false"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域",
        "en": "Region"
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID列表，默认为实例ID列表，n 的取值范围为 1, 50",
        "en": "List of resource IDs. Default value: list of instance IDs. Valid values for n: 1 to 50"
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型，默认为INSTANCE",
        "en": "Resource type. Default value: INSTANCE"
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签键列表，n 的取值范围为 1, 20。",
        "en": "List of tag keys. Valid values for n: 1 to 20"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除资源标签",
    "en": "Delete resource tags"
  }
}