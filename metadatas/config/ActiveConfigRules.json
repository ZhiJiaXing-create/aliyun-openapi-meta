{
  "name": "ActiveConfigRules",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConfigRuleIds",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "规则ID。\n\n多个规则ID之间以半角逗号（,）分隔，且不能超过20个。",
        "en": "The rule IDs. Separate multiple rule IDs with commas (,). You can specify a maximum of 20 rule IDs at a time."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量启用指定规则",
    "en": "ActiveConfigRules"
  }
}