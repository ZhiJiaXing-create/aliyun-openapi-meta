{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID，最多支持输入20个资源ID。",
        "en": "The resource ID. You can specify up to 20 resource IDs."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型，取值：**ANYCASTEIPADDRESS**。",
        "en": "The resource type. Set the value to **ANYCASTEIPADDRESS**."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "标签信息。",
        "en": "The tag information."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。至少输入1个标签键，最多支持输入20个标签键。一旦传入该值，则不允许为空字符串。\n\n最多支持128个字符，不能以aliyun和acs:开头，不能包含`http://`或者`https://`。\n\n> 调用该API时，**Tag.N.Key**参数必须输入。",
            "en": "The tag key of the resource. You must specify at least one tag key and at most 20 tag keys. The tag key cannot be an empty string.\n\nThe tag key can be up to 128 characters in length. It cannot start with aliyun or acs: and cannot contain `http://` or `https://`.\n\n> When you call this operation, you must specify the **Tag.N.Key** parameter."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。至少输入1个标签值，最多支持输入20个标签值。一旦传入该值，可以为空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n\n> 调用该API时，**Tag.N.Value**参数必须输入。",
            "en": "The tag value of the resource. You must specify at least one tag value and at most 20 tag values. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length. It cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`.\n\n> When you call this operation, you must specify the **Tag.N.Value** parameter."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为实例添加标签",
    "en": "TagResources"
  },
  "example": {
    "unifiedCli": "aliyun eipanycast tag-resources --resource-id aeip-2zeerraiwb7ujsxdc0000 --resource-type ANYCASTEIPADDRESS --tag Value=FinanceJoshua Key=FinanceDept --region cn-hangzhou",
    "legacyCli": "aliyun eipanycast TagResources --region cn-hangzhou --ResourceId.1 'aeip-2zeerraiwb7ujsxdc0000' --Tag.1.Key FinanceDept --Tag.1.Value FinanceJoshua --ResourceType ANYCASTEIPADDRESS"
  }
}