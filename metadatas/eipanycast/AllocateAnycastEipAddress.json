{
  "name": "AllocateAnycastEipAddress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Bandwidth",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Anycast EIP实例的带宽峰值。单位：Mbps。\n\n取值范围：**200**~**1000**。\n\n默认值：**1000**。\n\n>带宽峰值不作为业务承诺指标，仅作为参考值和带宽上限峰值。",
        "en": "The peak bandwidth of the Anycast EIP instance. Unit: Mbps.\n\nValid values: **200** to **1000**.\n\nDefault value: **1000**.\n\n> The peak bandwidth is for reference only and is not a guaranteed value. It serves as the upper limit for bandwidth."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nMake sure that the client token is unique for each request. The token can contain a maximum of 64 ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the request as the **ClientToken**. The **RequestId** may be different for each request."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Anycast EIP实例描述。\n\n长度为0~256个字符，不能以`http://`或`https://`开头。",
        "en": "The description of the Anycast EIP instance.\n\nThe description must be 0 to 256 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "InstanceChargeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Anycast EIP实例付费类型。\n\n取值：**PostPaid**，指后付费。",
        "en": "The billing method of the Anycast EIP instance.\n\nSet the value to **PostPaid**. This value specifies the pay-as-you-go billing method."
      }
    },
    {
      "name": "InternetChargeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Anycast EIP实例访问公网计费方式。\n\n取值：**PayByTraffic**，指按流量计费。",
        "en": "The metering method for Internet data transfer.\n\nSet the value to **PayByTraffic**. This value specifies the pay-by-data-transfer metering method."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Anycast EIP实例名称。\n\n长度为0~128个字符，以大小字母或中文开头，可包含数字，下划线（_）或短划线（-）。",
        "en": "The name of the Anycast EIP instance.\n\nThe name must be 0 to 128 characters in length. It must start with a letter or a Chinese character and can contain digits, underscores (\\_), and hyphens (-)."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例所属的资源组ID。",
        "en": "The ID of the resource group to which the instance belongs."
      }
    },
    {
      "name": "ServiceLocation",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Anycast EIP实例接入区域。\n\n取值：**international**，指中国内地以外的区域。",
        "en": "The access area of the Anycast EIP instance."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源的标签。",
        "en": "The resource tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。最多支持输入20个标签键。一旦传入该值，则不允许为空字符串。\n\n最多支持128个字符，不能以aliyun和acs:开头，不能包含`http://`或者`https://`。",
            "en": "The tag key. Specify up to 20 tag keys. A tag key cannot be an empty string.\n\nA tag key contains up to 128 characters, cannot begin with `aliyun` or `acs:` or contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。最多支持输入20个标签值。一旦传入该值，可以为空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag value. Specify up to 20 tag values. A tag value can be an empty string.\n\nA tag value contains up to 128 characters, cannot begin with `aliyun` or `acs:` or contain `http://` or `https://`."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建实例",
    "en": "AllocateAnycastEipAddress"
  },
  "example": {
    "unifiedCli": "aliyun eipanycast allocate-anycast-eip-address --bandwidth 200 --service-location international --instance-charge-type PostPaid --internet-charge-type PayByTraffic --client-token 02fb3da4-130e-11e9-8e44-0010000 --name doctest --description docdesc --resource-group-id rg-acfm3obzjuk0000 --tag Value=tag-value Key=tag-key --region cn-hangzhou",
    "legacyCli": "aliyun eipanycast AllocateAnycastEipAddress --region cn-hangzhou --Tag.1.Key 'tag-key' --Description docdesc --ResourceGroupId 'rg-acfm3obzjuk0000' --ServiceLocation international --InstanceChargeType PostPaid --Bandwidth 200 --Tag.1.Value 'tag-value' --ClientToken '02fb3da4-130e-11e9-8e44-0010000' --InternetChargeType PayByTraffic --Name doctest"
  }
}