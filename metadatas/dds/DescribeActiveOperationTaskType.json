{
  "name": "DescribeActiveOperationTaskType",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IsHistory",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "是否返回历史运维任务。取值：\n- **0**（默认值）：仅返回当前待处理的运维任务。\n- **1**：返回历史运维任务。",
        "en": "Specifies whether to return historical O\\&M tasks. Valid values:\n\n- **0** (default): The system returns only pending O\\&M tasks.\n\n- **1**: The system returns historical O\\&M tasks."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。您可以调用[DescribeSecurityGroupConfiguration](~~146130~~)接口查询。",
        "en": "The resource group ID. You can call the [DescribeSecurityGroupConfiguration](~~146130~~) operation to query the resource group ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询运维任务类型和任务数量",
    "en": "DescribeActiveOperationTaskType"
  },
  "descriptions": {
    "zh": "该接口即将下线，后续将不再更新。",
    "en": "This operation is no longer updated and will be unavailable."
  },
  "example": {
    "unifiedCli": "aliyun dds describe-active-operation-task-type --is-history 0 --resource-group-id rg-acfmyiu4ekp0000 --region cn-hangzhou",
    "legacyCli": "aliyun dds DescribeActiveOperationTaskType --region cn-hangzhou --ResourceGroupId 'rg-acfmyiu4ekp0000' --IsHistory 0"
  }
}