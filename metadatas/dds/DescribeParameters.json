{
  "name": "DescribeParameters",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CharacterType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例的角色类型，取值说明：\n\n- db：shard角色。\n- cs：config server角色。\n- mongos：mongos角色。",
        "en": "The role of the instance. Valid values:\n\n- db: a shard node.\n\n- cs: a Configserver node.\n\n- mongos: a mongos node."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。\n\n> 当本参数传入的是分片集群实例ID时，还需要传入**NodeId**参数。",
        "en": "The instance ID.\n\n> If you set this parameter to the ID of a sharded cluster instance, you must also specify the **NodeId** parameter."
      }
    },
    {
      "name": "ExtraParam",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "扩展参数。",
        "en": "The parameter that is available in the future."
      }
    },
    {
      "name": "NodeId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分片集群实例中的Mongos节点ID或Shard节点ID。\n\n> 当**DBInstanceId**参数传入的是分片集群实例ID时，本参数才可用。",
        "en": "The ID of the mongos or shard node in the specified sharded cluster instance.\n\n> This parameter is valid when the **DBInstanceId** parameter is set to the ID of a sharded cluster instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询实例的参数配置信息",
    "en": "DescribeParameters"
  },
  "example": {
    "unifiedCli": "aliyun dds describe-parameters --db-instance-id dds-bpxxxxxxxx --node-id d-bpxxxxxxxx --character-type mongos --extra-param terrform --region cn-hangzhou",
    "legacyCli": "aliyun dds DescribeParameters --region cn-hangzhou --ExtraParam terrform --DBInstanceId 'dds-bpxxxxxxxx' --NodeId 'd-bpxxxxxxxx' --CharacterType mongos"
  }
}