{
  "name": "ModifyActiveOperationMaintenanceConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CycleTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "周期时间。\n\n- 当CycleType为Month，输入1～28的数字，表示每月几号，用\",\"拼接。\n- 当CycleType为Week，输入1～7的数字，表示每周星期几，用\",\"拼接。",
        "en": "The day of the cycle.\n\n- If CycleType is set to Month, enter a number from 1 to 28 to specify the day of the month. Use a comma (,) to separate multiple days.\n\n- If CycleType is set to Week, enter a number from 1 to 7 to specify the day of the week. Use a comma (,) to separate multiple days."
      }
    },
    {
      "name": "CycleType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "周期类型。\n\n- Month 每月\n- Week 每周",
        "en": "The cycle type.\n\n- Month: monthly\n\n- Week: weekly"
      }
    },
    {
      "name": "MaintainEndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例运维结束时间，格式为HH:mmZ，UTC时间。",
        "en": "The end time of the O\\&M window for the instance. The time is in the HH:mmZ format and is in UTC."
      }
    },
    {
      "name": "MaintainStartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例运维开始时间，格式为HH:mmZ，UTC时间。",
        "en": "The start time of the O\\&M window for the instance. The time is in the HH:mmZ format and is in UTC."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "是否生效 \n- 1 生效 \n- 2 无效",
        "en": "Specifies whether the configuration is enabled.\n\n- 1: enabled\n\n- 2: disabled"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改实例的运维任务配置",
    "en": "ModifyActiveOperationMaintenanceConfig"
  },
  "example": {
    "unifiedCli": "aliyun dds modify-active-operation-maintenance-config --cycle-type Week --cycle-time 1,2,3 --maintain-start-time 21:00Z --maintain-end-time 22:00Z --status 1 --region cn-hangzhou",
    "legacyCli": "aliyun dds ModifyActiveOperationMaintenanceConfig --region cn-hangzhou --Status 1 --CycleTime '1,2,3' --CycleType Week --MaintainEndTime '22:00Z' --MaintainStartTime '21:00Z'"
  }
}