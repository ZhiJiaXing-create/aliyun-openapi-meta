{
  "name": "DescribeDBInstanceSSL",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询MongoDB实例的SSL设置详情",
    "en": "DescribeDBInstanceSSL"
  },
  "descriptions": {
    "zh": "调用该接口时，实例必须满足以下条件：\n- 实例状态为运行中。\n- 实例类型为副本集或分片集群实例。\n  - 副本集实例的版本需为3.4及以上版本。\n  - 分片集群实例的存储模式需要为ESSD云盘版。",
    "en": "To use this operation, the instance must meet the following requirements:\n\n- The instance status is Running.\n\n- The instance is a replica set instance or a sharded cluster instance.\n\n  - The replica set instance must be version 3.4 or later.\n\n  - The sharded cluster instance must use Enhanced SSDs (ESSDs)."
  },
  "example": {
    "unifiedCli": "aliyun dds describe-db-instance-ssl --db-instance-id dds-bpxxxxxxxx --region cn-hangzhou",
    "legacyCli": "aliyun dds DescribeDBInstanceSSL --region cn-hangzhou --DBInstanceId 'dds-bpxxxxxxxx'"
  }
}