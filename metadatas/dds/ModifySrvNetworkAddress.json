{
  "name": "ModifySrvNetworkAddress",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConnectionType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要修改的SRV地址类型，可选值：\n- **VPC**：专有网络\n- **Public**：公网连接",
        "en": "The type of the SRV connection address to modify. Valid values:\n\n- **VPC**: virtual private cloud (VPC)\n\n- **Public**: Internet connection"
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "NewConnectionString",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "新的连接地址，要求如下：\n- 以小写字母开头。\n- 以小写字母或数字结尾。\n- 由小写字母、数字和连字符（-） 组成。\n- 长度为8~63个字符。\n\n> 仅需传入连接地址的前缀部分，前缀以外的部分不可修改。",
        "en": "The new connection address. The address must meet the following requirements:\n\n- Starts with a lowercase letter.\n\n- Ends with a lowercase letter or a digit.\n\n- Contains only lowercase letters, digits, and hyphens (-).\n\n- Is 8 to 63 characters in length.\n\n> You only need to specify the prefix of the connection address. The rest of the address cannot be modified."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改MongoDB实例的SRV连接地址",
    "en": "ModifySrvNetworkAddress"
  },
  "descriptions": {
    "zh": "仅适用于云盘副本集实例与云盘分片集群实例。",
    "en": "This operation applies only to replica set instances and sharded cluster instances that use disks."
  },
  "example": {
    "unifiedCli": "aliyun dds modify-srv-network-address --db-instance-id dds-bp1fd530f2710000 --new-connection-string aliyuntest111 --connection-type VPC --region cn-hangzhou",
    "legacyCli": "aliyun dds ModifySrvNetworkAddress --region cn-hangzhou --ConnectionType VPC --DBInstanceId 'dds-bp1fd530f2710000' --NewConnectionString aliyuntest111"
  }
}