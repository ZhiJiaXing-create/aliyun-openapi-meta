{
  "name": "CancelActiveOperationTasks",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Ids",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "批量取消的运维任事件 ID，多个 ID 间使用英文逗号（,）分隔。",
        "en": "The IDs of the O\\&M events to cancel. Separate multiple IDs with commas (,)."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "取消运维事件",
    "en": "CancelActiveOperationTasks"
  },
  "descriptions": {
    "zh": "运维事件不允许取消的场景\n- 事件不可取消（AllowCancel 为 0）\n- 当前时间大于运维事件开始时间\n- 运维事件状态值不为“等待执行”（Status不为3）",
    "en": "An O\\&M event cannot be canceled in the following scenarios:\n\n- The event is not cancelable if the value of the AllowCancel parameter is 0.\n\n- The scheduled start time of the O\\&M event has passed.\n\n- The status of the O\\&M event is not Pending if the value of the Status parameter is not 3."
  },
  "example": {
    "unifiedCli": "aliyun dds cancel-active-operation-tasks --ids 1508850,1508310,1507849,1506274,1505811 --resource-group-id rg-xxxx --region cn-hangzhou",
    "legacyCli": "aliyun dds CancelActiveOperationTasks --region cn-hangzhou --ResourceGroupId 'rg-xxxx' --Ids '1508850,1508310,1507849,1506274,1505811'"
  }
}