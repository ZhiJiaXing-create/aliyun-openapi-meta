{
  "name": "DescribeRdsVSwitchs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The resource group ID."
      }
    },
    {
      "name": "VpcId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专有网络（VPC）ID。",
        "en": "The ID of VPC."
      }
    },
    {
      "name": "ZoneId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "可用区ID。",
        "en": "The zone ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取虚拟交换机列表",
    "en": "DescribeRdsVSwitchs"
  },
  "descriptions": {
    "zh": "您在新建云数据库 MongoDB 版实例时，可以通过该接口查询目标地域及可用区下可用的 vSwitch 信息。",
    "en": "Queries the vSwitches that are available in a specified region and zone. Use this before creating an ApsaraDB for MongoDB instance to select a vSwitch."
  },
  "example": {
    "unifiedCli": "aliyun dds describe-rds-vswitchs --vpc-id vpc-bp0000000000000000000 --zone-id cn-hangzhou-j --resource-group-id rg-acfm4ojaksxxxxx --region cn-hangzhou",
    "legacyCli": "aliyun dds DescribeRdsVSwitchs --region cn-hangzhou --VpcId 'vpc-bp0000000000000000000' --ZoneId 'cn-hangzhou-j' --ResourceGroupId 'rg-acfm4ojaksxxxxx'"
  }
}