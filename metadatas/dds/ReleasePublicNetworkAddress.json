{
  "name": "ReleasePublicNetworkAddress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConnectionType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "公网地址类型，可选值：\n- **SRV**\n- **Normal**\n>当释放的连接地址是SRV地址时，本参数才可用。",
        "en": "The public endpoint type. Valid values:\n\n- **SRV**\n\n- **Normal**\n\n> This parameter is valid only when you want to release an SRV endpoint."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。\n\n> 当本参数传入的是分片集群实例ID时，还需要传入**NodeId**参数。",
        "en": "The instance ID.\n\n> If you set this parameter to the ID of a sharded cluster instance, you must also specify the **NodeId** parameter."
      }
    },
    {
      "name": "NodeId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分片集群实例中Mongos节点ID、Shard节点ID或ConfigServer节点ID。\n\n> - 当**DBInstanceId**传入的是分片集群实例ID时，本参数才可用。\n> - 您可以调用[DescribeDBInstanceAttribute](~~62010~~)接口查询Mongos/Shard/ConfigServer节点ID。",
        "en": "The ID of the mongos, shard, or Configserver node in the sharded cluster instance.\n\n> - This parameter is valid only if you set the **DBInstanceId** parameter to the ID of a sharded cluster instance.\n>\n> - You can call the [DescribeDBInstanceAttribute](~~62010~~) operation to view the ID of the mongos, shard, or Configserver node."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "释放实例公网地址",
    "en": "ReleasePublicNetworkAddress"
  },
  "example": {
    "unifiedCli": "aliyun dds release-public-network-address --db-instance-id dds-bp22350000 --node-id s-bp22350000 --connection-type SRV --region cn-hangzhou",
    "legacyCli": "aliyun dds ReleasePublicNetworkAddress --region cn-hangzhou --ConnectionType SRV --DBInstanceId 'dds-bp22350000' --NodeId 's-bp22350000'"
  }
}