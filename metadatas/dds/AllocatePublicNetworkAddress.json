{
  "name": "AllocatePublicNetworkAddress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。\n\n> 当配置为分片集群实例ID时，还需要配置**NodeId**参数。",
        "en": "The ID of the instance\n\n> If you set this parameter to the ID of a sharded cluster instance, you must also specify the **NodeId** parameter."
      }
    },
    {
      "name": "NodeId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分片集群实例中Mongos节点ID、Shard节点ID或ConfigServer节点ID。您可以调用[DescribeDBInstanceAttribute](~~62010~~)接口查询Mongos节点、Shard节点或ConfigServer节点的节点ID。\n\n> 当**DBInstanceId**配置的是分片集群实例ID时，需要配置该参数。",
        "en": "The ID of the mongos, shard, or Configserver node in the sharded cluster instance. You can call the [DescribeDBInstanceAttribute](~~62010~~) operation to view the ID of the mongos, shard, or Configserver node.\n\n> This parameter is required only when you specify the **DBInstanceId** parameter to the ID of a sharded cluster instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为实例申请公网连接地址",
    "en": "AllocatePublicNetworkAddress"
  },
  "example": {
    "unifiedCli": "aliyun dds allocate-public-network-address --db-instance-id dds-bp11483712c10000 --node-id s-bp18e6d84ae30000 --region cn-hangzhou",
    "legacyCli": "aliyun dds AllocatePublicNetworkAddress --region cn-hangzhou --DBInstanceId 'dds-bp11483712c10000' --NodeId 's-bp18e6d84ae30000'"
  }
}