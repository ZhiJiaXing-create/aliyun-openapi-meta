{
  "name": "DescribeSecurityGroupConfiguration",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The ID of the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询MongoDB实例绑定的ECS安全组信息",
    "en": "DescribeSecurityGroupConfiguration"
  },
  "example": {
    "unifiedCli": "aliyun dds describe-security-group-configuration --db-instance-id dds-bpxxxxxxxx --region cn-hangzhou",
    "legacyCli": "aliyun dds DescribeSecurityGroupConfiguration --region cn-hangzhou --DBInstanceId 'dds-bpxxxxxxxx'"
  }
}