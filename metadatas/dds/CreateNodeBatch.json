{
  "name": "CreateNodeBatch",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "账号名，取值说明：\n\n- 以小写字母开头。\n- 由小写字母、数字或下划线（_）组成。\n- 长度为4~16个字符。\n\n> - 云数据库MongoDB不支持使用关键字作为账号。\n> - 账号的权限固定为只读权限。\n> - 首次申请Shard节点直连地址时，需要设置账号和密码，后续申请无需再次设置。",
        "en": "The username of the account. The username must meet the following requirements:\n\n- The username starts with a lowercase letter.\n\n- The username contains lowercase letters, digits, and underscores (\\_).\n\n- The username is 4 to 16 characters in length.\n\n> * Keywords cannot be used as account usernames.\n>\n> * The permissions of this account are fixed at read-only.\n>\n> * The username and password are required to be set only when you apply for an endpoint for the shard node for the first time."
      }
    },
    {
      "name": "AccountPassword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "账号密码，取值说明：\n- 由大写字母、小写字母、数字、特殊字符中的至少三种组成。\n- !@#$%^&*()_+-=为特殊字符。\n- 长度为8~32个字符。\n\n>云数据库MongoDB不支持重置Shard节点的账号密码。",
        "en": "The password of the account. The password must meet the following requirements:\n\n- The password contains at least three of the following character types: uppercase letters, lowercase letters, digits, and specific special characters.\n\n- These special characters include ! @ # $ % ^ & \\* ( ) \\_ + - =\n\n- The password is 8 to 32 characters in length.\n\n> The account password of the shard node cannot be reset."
      }
    },
    {
      "name": "AutoPay",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否自动付费，取值说明：\n\n- **true**：（默认值）自动付费，请确保账号有足够的余额。\n- **false**：手动付费。具体操作请参见[手动续费包年包月实例](~~85052~~)。",
        "en": "Specifies whether to enable automatic payment. Default value: true. Valid values:\n\n- **true**: enables automatic payment. Make sure that you have sufficient balance within your account.\n\n- **false**: disables automatic payment. In this case, you must manually pay for the instance. You can perform the following operations to pay for the instance: Log on to the ApsaraDB for MongoDB console. In the upper-right corner of the page, choose **Expenses** > **Orders**. On the Orders page, find the order and complete the payment."
      }
    },
    {
      "name": "BusinessInfo",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "业务信息。",
        "en": "The business information."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must ensure that it is unique among different requests. The token can only contain ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "CouponNo",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否使用优惠券，取值说明：\n- **default**或**null**（默认）：使用优惠券。\n- **youhuiquan_promotion_option_id_for_blank**：不使用优惠券。",
        "en": "Specifies whether to use coupons. Default value: null. Valid values:\n\n- **default** or **null**: uses coupons.\n\n- **youhuiquan\\_promotion\\_option\\_id\\_for\\_blank**: does not use coupons."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要添加节点的实例ID。",
        "en": "The ID of the instance for which you want to add nodes."
      }
    },
    {
      "name": "FromApp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求来源，取值说明：\n- **OpenApi**：请求来源为OpenAPI。\n- **mongo_buy**：请求来源为控制台。",
        "en": "The source of the request. Valid values:\n\n- **OpenApi**: ApsaraDB for MongoDB API\n\n- **mongo\\_buy**: ApsaraDB for MongoDB console"
      }
    },
    {
      "name": "NodesInfo",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "添加Mongos节点或Shard节点的规格信息。具体规格，请参见[实例规格表](~~57141~~)。\n\n> 分片集群实例默认最多支持32个Mongos节点和32个Shard节点。",
        "en": "The specifications of the mongos or shard node that you want to add. For more information, see [Instance types](~~57141~~).\n\n> Up to 32 mongos or shard nodes are supported for each sharded cluster instance."
      }
    },
    {
      "name": "ShardDirect",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否申请Shard节点直连地址，取值说明：\n\n- **true**：申请Shard节点直连地址。\n- **false**：（默认值）不申请Shard节点直连地址。",
        "en": "Specifies whether to apply for an endpoint for the shard node. Default value: false. Valid values:\n\n- **true**: applies for an endpoint for the shard node.\n\n- **false**: does not apply for an endpoint for the shard node."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量创建节点",
    "en": "CreateNodeBatch"
  },
  "descriptions": {
    "zh": "请确保在使用该接口前，已充分了解云数据库MongoDB产品的收费方式和<props=\"china\">[价格](https://www.aliyun.com/price/product#/mongodb/detail)</props><props=\"intl\">[价格](https://www.alibabacloud.com/zh/product/apsaradb-for-mongodb/pricing)</props>。\n\n该接口仅适用于云数据库MongoDB分片集群实例。",
    "en": "Before you call this operation, make sure that you fully understand the billing methods and [pricing](https://www.alibabacloud.com/product/apsaradb-for-mongodb/pricing) of ApsaraDB for MongoDB.\n\nThis operation is applicable only to sharded cluster instances."
  },
  "example": {
    "unifiedCli": "aliyun dds create-node-batch --client-token ETnLKlblzczshOTUbOCz0000 --from-app OpenApi --auto-pay true --db-instance-id dds-bp18b0934e700000 --nodes-info Shards_:_DBInstanceClass_:_mdb.shard.4x.large.d_,_Storage_:20 --coupon-no default --business-info '“ActivityId_:_000000000' --shard-direct false --account-name ceshi --account-password PassWord123 --region cn-hangzhou",
    "legacyCli": "aliyun dds CreateNodeBatch --region cn-hangzhou --CouponNo default --ShardDirect false --NodesInfo 'Shards_:_DBInstanceClass_:_mdb.shard.4x.large.d_,_Storage_:20' --BusinessInfo '“ActivityId_:_000000000' --DBInstanceId 'dds-bp18b0934e700000' --AutoPay true --ClientToken ETnLKlblzczshOTUbOCz0000 --FromApp OpenApi --AccountName ceshi --AccountPassword PassWord123"
  }
}