{
  "name": "ModifyDBInstanceMonitor",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The ID of the instance."
      }
    },
    {
      "name": "Granularity",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "设置监控采集粒度，取值：**1**或**300**，单位为秒。",
        "en": "The collection frequency of monitoring data. Valid values: **1** or **300**. Unit: seconds."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置MongoDB实例的监控采集粒度",
    "en": "ModifyDBInstanceMonitor"
  },
  "descriptions": {
    "zh": ">由于监控采集粒度调整功能变更，本接口仅适用于旧版本MongoDB控制台。\n\n调用本接口时，实例必须满足以下条件：\n- 实例为副本集实例或分片集群实例。\n- 实例的版本为3.4（最新的小版本）或4.0。",
    "en": "> Due to changes in the monitoring collection granularity adjustment feature, this operation is only applicable to the previous version of the MongoDB console.\n\nTo use this operation, the instance must meet the following requirements:\n\n- A replica set or sharded cluster instance is used.\n\n- MongoDB 3.4 (the latest minor version) or MongoDB 4.0 is selected."
  },
  "example": {
    "unifiedCli": "aliyun dds modify-db-instance-monitor --db-instance-id dds-bpxxxxxxxx --granularity 1 --region cn-hangzhou",
    "legacyCli": "aliyun dds ModifyDBInstanceMonitor --region cn-hangzhou --DBInstanceId 'dds-bpxxxxxxxx' --Granularity 1"
  }
}