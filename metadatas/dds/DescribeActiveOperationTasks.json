{
  "name": "DescribeActiveOperationTasks",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllowCancel",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "是否允许取消。取值如下：\n- **-1**（默认）：返回全部任务。\n- **0**：只返回不允许取消的任务。\n- **1**：只返回允许取消的任务。",
        "en": "Specifies whether the task can be canceled. Valid values:\n\n- **-1** (default): returns all tasks.\n\n- **0**: returns only tasks that cannot be canceled.\n\n- **1**: returns only tasks that can be canceled."
      }
    },
    {
      "name": "AllowChange",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "是否允许修改时间。取值如下：\n- **-1**（默认）：返回全部任务。\n- **0**：只返回不允许修改时间的任务。\n- **1**：只返回允许修改时间的任务。",
        "en": "Specifies whether the time can be changed. Valid values:\n\n- **-1** (default): returns all tasks.\n\n- **0**: returns only tasks whose time cannot be changed.\n\n- **1**: returns only tasks whose time can be changed."
      }
    },
    {
      "name": "ChangeLevel",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "任务类型。取值如下：\n- **all**（默认）：返回全部任务。\n- **S0**：返回异常修复级别的任务。\n- **S1**：返回系统运维级别的任务。",
        "en": "The level of the task. Valid values:\n\n- **all** (default): returns all tasks.\n\n- **S0**: returns tasks for exception fixing.\n\n- **S1**: returns tasks for system O\\&M."
      }
    },
    {
      "name": "DbType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据库类型，默认为**all**。",
        "en": "The database type. Default value: **all**."
      }
    },
    {
      "name": "InsName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例的名称。可不填，填写时只允许填写至多一个实例名称。",
        "en": "The name of the instance. This parameter is optional. You can specify only one instance name."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码。大于**0**，默认为**1**。",
        "en": "The page number. The value must be greater than **0**. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，取值： **30**、**50**、**100**，默认值为**30**。",
        "en": "The number of entries per page. Valid values: **30**, **50**, and **100**. Default value: **30**."
      }
    },
    {
      "name": "ProductId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "产品名称，对于MongoDB实例可传入**MongoDB**。",
        "en": "The product name. For MongoDB instances, set this parameter to **MongoDB**."
      }
    },
    {
      "name": "Region",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "待处理事件所属的地域 ID，可调用 DescribeRegions 接口获取。\n> 取值为**all**表示所有地域 ID。",
        "en": "The ID of the region where the pending event is located. Call the DescribeRegions operation to obtain the region ID.\n\n> A value of **all** indicates all region IDs."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The resource group ID."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "任务状态，用于筛选返回任务。\n\n- **-1**：全部任务。\n- **3**：待处理任务。\n- **4**：处理中任务。\n- **5**：成功结束任务。\n- **6**：失败结束任务。\n- **7**：已取消任务。",
        "en": "The task status. This parameter filters the returned tasks.\n\n- **-1**: all tasks.\n\n- **3**: pending tasks.\n\n- **4**: running tasks.\n\n- **5**: successfully completed tasks.\n\n- **6**: failed tasks.\n\n- **7**: canceled tasks."
      }
    },
    {
      "name": "TaskType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "任务类型，取值：\n\n- **rds_apsaradb_ha**：主从节点切换。\n- **rds_apsaradb_transfer**：实例迁移。\n- **rds_apsaradb_upgrade**：小版本升级。\n- **rds_apsaradb_maxscale**: 代理小版本升级。\n- **all**：所有任务类型。",
        "en": "The task type. Valid values:\n\n- **rds\\_apsaradb\\_ha**: primary-secondary node switchover.\n\n- **rds\\_apsaradb\\_transfer**: instance migration.\n\n- **rds\\_apsaradb\\_upgrade**: minor version upgrade.\n\n- **rds\\_apsaradb\\_maxscale**: proxy minor version upgrade.\n\n- **all**: all task types."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询多个主动运维任务信息",
    "en": "DescribeActiveOperationTasks"
  },
  "example": {
    "unifiedCli": "aliyun dds describe-active-operation-tasks --biz-region cn-beijing --task-type all --page-size 30 --page-number 1 --product-id MongoDB --db-type mongoDb --status -1 --ins-name dds-bp16aaccfe100000 --allow-change -1 --allow-cancel -1 --change-level 000 --resource-group-id rg-acfmyiu4ekp0000 --region cn-hangzhou",
    "legacyCli": "aliyun dds DescribeActiveOperationTasks --region cn-hangzhou --Status '-1' --AllowCancel '-1' --PageSize 30 --ResourceGroupId 'rg-acfmyiu4ekp0000' --PageNumber 1 --AllowChange '-1' --TaskType all --Region 'cn-beijing' --ProductId MongoDB --InsName 'dds-bp16aaccfe100000' --DbType mongoDb --ChangeLevel 000"
  }
}