{
  "name": "CheckRecoveryCondition",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份ID。\n\n> - 您可以通过调用[DescribeBackups](~~62172~~)接口查询备份ID。\n> - 本参数和**RestoreTime**参数两者中必须传入一项。\n> - 分片集群实例不支持传入本参数。",
        "en": "The backup ID.\n\n> *   You can call the [DescribeBackups](~~62172~~) operation to query the backup ID.\n> *   You must specify one of the **RestoreTime** and BackupId parameters.\n> *   This parameter is not applicable to sharded cluster instances."
      }
    },
    {
      "name": "DatabaseNames",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "所需恢复的数据库名，格式为JSON数组。\n> 如果不传入本参数，默认恢复所有数据库。",
        "en": "The name of the source database. The value is a JSON array.\n\n>  If you do not specify this parameter, all databases are restored by default."
      }
    },
    {
      "name": "DestRegion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "异地备份恢复时，备份集所在区域。\n\n> 当备份恢复类型为异地备份克隆时，需要且必填",
        "en": "The region of the backup set used for the cross-region backup and restoration.\n\n>  This parameter is required when you set the RestoreType parameter to 3."
      }
    },
    {
      "name": "EngineVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据库版本号，取值：\n- **8.0**\n- **7.0**\n- **6.0**\n- **5.0**\n- **4.4**\n- **4.2**\n- **4.0**",
        "en": "The database engine version of the instance.\n\n*   **6.0**\n*   **5.0**\n*   **4.4**\n*   **4.2**\n*   **4.0**\n*   **3.4**"
      }
    },
    {
      "name": "InstanceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例类型，取值：\n- replicate \n- sharding\n\n>- 当备份恢复类型为已删除实例重建时，需要且必填\n>- 当备份恢复类型为异地备份克隆时，需要且必填",
        "en": "The instance architecture. Valid values:\n\n*   replicate\n*   sharding\n\n> * This parameter is required when you set the RestoreType parameter to 2.\n> * This parameter is required when you set the RestoreType parameter to 3."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group to which the instance belongs."
      }
    },
    {
      "name": "RestoreTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例所需恢复的时间点，格式为yyyy-MM-ddTHH:mm:ssZ（UTC时间）。\n\n> - 本参数可取值为7天内的任意时间，但是须早于当前时间，且晚于实例的创建时间。\n> - 本参数和**BackupId**参数两者中必须传入一项。",
        "en": "The point in time to which the instance is restored. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.\n\n> *   The time can be a point in time within the past seven days. The time must be earlier than the current time, but later than the time when the instance was created.\n> *   You must specify one of the RestoreTime and **BackupId** parameters."
      }
    },
    {
      "name": "RestoreType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份恢复类型\n\n\n\n>- 0:  本地备份恢复\n>- 1:   本地时间点恢复\n>- 2:  已删除实例使用备份集备份恢复\n>- 3:  异地备份使用备份集备份恢复",
        "en": "The restoration type.\n\n> * 0: The data of the source instance is restored to a new instance in the same region.\n> * 1: The data of the source instance is restored to an earlier point in time.\n> * 2: The data of a deleted instance is restored to a new instance from the backup set.\n> * 3: The data of a deleted instance is restored to a new instance in another region from the backup set."
      }
    },
    {
      "name": "SourceDBInstance",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待恢复的源实例ID。",
        "en": "The ID of the source instance."
      }
    },
    {
      "name": "SrcRegion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "源实例所在区域。\n\n>- 当备份恢复类型为已删除实例重建时，需要且必填\n>- 当备份恢复类型为异地备份克隆时，需要且必填",
        "en": "The region where the source instance resides.\n\n> * This parameter is required when you set the RestoreType parameter to 2.\n> * This parameter is required when you set the RestoreType parameter to 3."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "检查实例是否满足数据恢复条件",
    "en": "CheckRecoveryCondition"
  },
  "descriptions": {
    "zh": "本接口适用于副本集实例或分片集群实例。\n\n> 调用本接口确认满足数据恢复的条件后，您可以调用[CreateDBInstance](~~61763~~)接口，将数据恢复至新实例。",
    "en": "This operation is applicable to replica set instances and sharded cluster instances.\n\n>  After you call this operation to confirm that the data of the instance can be restored, you can call the [CreateDBInstance](~~61763~~) operation to restore data to a new instance."
  },
  "example": {
    "unifiedCli": "aliyun dds check-recovery-condition --source-db-instance dds-bp13780000 --database-names db1_,_db2 --restore-time 2022-08-22T08:00:00Z --backup-id 56640000 --resource-group-id sg-bp1790000 --src-region cn-beijing --dest-region cn-hangzhou --restore-type 0 --instance-type replicate --engine-version 4.2 --region cn-hangzhou",
    "legacyCli": "aliyun dds CheckRecoveryCondition --region cn-hangzhou --SrcRegion 'cn-beijing' --EngineVersion '4.2' --ResourceGroupId 'sg-bp1790000' --SourceDBInstance 'dds-bp13780000' --DatabaseNames 'db1_,_db2' --BackupId 56640000 --RestoreType 0 --InstanceType replicate --DestRegion 'cn-hangzhou' --RestoreTime '2022-08-22T08:00:00Z'"
  }
}