{
  "name": "UpdateFileUploadLimit",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Limit",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "设置客户端上传文件的QPS限制阈值。取值范围：100~10000。",
        "en": "The QPS throttling threshold for file uploads from the client. Valid values: 100 to 10000."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改客户端上传文件的QPS上限",
    "en": "Modify the QPS upper limit for client file uploads"
  },
  "example": {
    "unifiedCli": "aliyun sas update-file-upload-limit --limit 100 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas UpdateFileUploadLimit --region cn-hangzhou --Limit 100"
  }
}