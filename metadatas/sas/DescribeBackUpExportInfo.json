{
  "name": "DescribeBackUpExportInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，当前页的页码。默认值为1。",
        "en": "The page number of the current page in a paged query. Default value: 1."
      }
    },
    {
      "name": "ExportType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要导出归档数据的类型。取值：\n- **suspiciousExport**：安全告警。",
        "en": "The type of archive data to export. Valid values:\n\n- **suspiciousExport**: security alerts."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型。取值：\n- **zh**：中文。\n- **en**：英文。",
        "en": "The language type for requests and responses. Valid values:\n\n- **zh**: Chinese.\n- **en**: English."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，显示的每页数据的最大条数。默认值为1。",
        "en": "The maximum number of entries to return on each page in a paged query. Default value: 1."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取安全告警归档数据导出列表",
    "en": "Retrieve security alert archive data export list"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-back-up-export-info --current-page 1 --page-size 20 --export-type suspiciousExport --lang zh --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeBackUpExportInfo --region cn-hangzhou --PageSize 20 --CurrentPage 1 --ExportType suspiciousExport --Lang zh"
  }
}