{
  "name": "DeleteHybridProxyCluster",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "代理集群名称。",
        "en": "The name of the proxy cluster."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "根据集群名称删除集群",
    "en": "Delete a cluster by cluster name"
  },
  "example": {
    "unifiedCli": "aliyun sas delete-hybrid-proxy-cluster --cluster-name proxy --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DeleteHybridProxyCluster --region cn-hangzhou --ClusterName proxy"
  }
}