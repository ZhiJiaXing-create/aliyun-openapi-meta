{
  "name": "DescribeImageCriteria",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Value",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询镜像时输入的模糊匹配值。\n\n> 该参数支持镜像ID、镜像标签、镜像实例ID、镜像仓名称、镜像仓ID、镜像仓命名空间、镜像地域、镜像digest、镜像仓类型的模糊查询。",
        "en": "The fuzzy match value that you enter when querying images.\n\n> This parameter supports fuzzy matching by image ID, image tag, image instance ID, image repository name, image repository ID, image repository namespace, image region, image digest, or image repository type."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取镜像检索项",
    "en": "Retrieve image search criteria"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-image-criteria --value 525 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeImageCriteria --region cn-hangzhou --Value 525"
  }
}