{
  "name": "DescribeImageVulWhiteList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AliasName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的漏洞的别名。",
        "en": "The alias of the vulnerability to query."
      }
    },
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，显示的当前页的页码。",
        "en": "The page number of the current page in a paged query. Paging is used to display results."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定请求和接收消息的语言类型，默认值为**zh**。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language type for the request and response. Default value: **zh**. Valid values:\n- **zh**: Chinese\n- **en**: English."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，显示的每页数据的最大条数。默认值为20。",
        "en": "The maximum number of entries per page in a paged query. Paging is used to display results. Default value: 20."
      }
    },
    {
      "name": "Source",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "白名单的来源。取值：\n- **image**：镜像\n- **agentless**：agentless",
        "en": "The source of the whitelist. Valid values:\n- **image**: image\n- **agentless**: agentless."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询镜像漏洞白名单",
    "en": "Query image vulnerability whitelist"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-image-vul-white-list --alias-name CVE-2007-5686:rpath_linux --lang zh --current-page 1 --page-size 20 --source image --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeImageVulWhiteList --region cn-hangzhou --PageSize 20 --CurrentPage 1 --AliasName 'CVE-2007-5686:rpath_linux' --Lang zh --Source image"
  }
}