{
  "name": "GetCanTrySas",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FromEcs",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否来自ECS控制台，取值：\n- **true**：是\n- **false**：否",
        "en": "Specifies whether the request is redirected from the Elastic Compute Service (ECS) console. Valid values:\n\n*   **true**\n*   **false**"
      }
    },
    {
      "name": "Lang",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置请求和接收消息的语言类型。默认值**zh**。取值：\n\n\n- **zh**：中文\n- **en**：英文",
        "en": "The language of the content within the request and response. Default value: **zh**. Valid values:\n\n*   **zh**: Chinese\n*   **en**: English"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询是否有云安全中心试用权限",
    "en": "GetCanTrySas"
  },
  "example": {
    "unifiedCli": "aliyun sas get-can-try-sas --from-ecs true --lang zh --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas GetCanTrySas --region cn-hangzhou --FromEcs true --Lang zh"
  }
}