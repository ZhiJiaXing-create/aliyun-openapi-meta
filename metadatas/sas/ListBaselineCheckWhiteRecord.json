{
  "name": "ListBaselineCheckWhiteRecord",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CheckIds",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "检查项ID列表。",
        "en": "The list of check item IDs."
      }
    },
    {
      "name": "CheckItemFuzzy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "检查项名称模糊匹配。",
        "en": "The check item name for fuzzy match."
      }
    },
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，显示的当前页的页码。起始值为1，默认值为1。",
        "en": "The page number of the current page when using paging. The value starts from 1. Default value: 1."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置请求和接收消息的语言类型，默认为**zh**。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language type for requests and responses. Default value: **zh**. Valid values:\n- **zh**: Chinese\n- **en**: English."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，当前页显示数据条数。",
        "en": "The number of entries per page when using paging."
      }
    },
    {
      "name": "RecordIds",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "白名单记录的ID列表。",
        "en": "The list of whitelist record IDs."
      }
    },
    {
      "name": "Source",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据来源，为空时默认查询主机。取值：\n- **default**：主机\n- **agentless**：agentless",
        "en": "The data source. If this parameter is left empty, host data is queried by default. Valid values:\n- **default**: host\n- **agentless**: agentless."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询基线白名单记录",
    "en": "Query baseline whitelist records"
  },
  "example": {
    "unifiedCli": "aliyun sas list-baseline-check-white-record --check-ids 58 --record-ids 496242 --source agentless --lang zh --check-item-fuzzy redis --current-page 1 --page-size 20 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ListBaselineCheckWhiteRecord --region cn-hangzhou --PageSize 20 --CheckIds '[58]' --CurrentPage 1 --Lang zh --CheckItemFuzzy redis --RecordIds '[496242]' --Source agentless"
  }
}