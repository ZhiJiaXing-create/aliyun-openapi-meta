{
  "name": "ModifyBackupPolicyStatus",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Id",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "要开启或关闭的防护策略的ID。",
        "en": "The ID of the anti-ransomware policy that you want to enable or shutdown."
      }
    },
    {
      "name": "PolicyVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要开启或关闭的策略版本。固定取值：**2.0.0**，表示要开启或关闭的策略的版本为2.0.0版本。",
        "en": "The version of the policy that you want to enable or disable. Set the value to **2.0.0**."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "设置要启用或停用防护策略。取值：\n\n- **enabled**：启用防护策略。启用策略后，防勒索才能保护您的服务器数据，即根据您的防护策略，备份指定服务器上的数据。\n- **disabled**：停用防护策略。停用策略后，正在运行的备份任务将停止。\n\n> 执行备份任务会占用您的网络带宽，建议您在业务低峰期启用策略，执行数据备份任务。",
        "en": "Specifies whether to enable or disable the anti-ransomware policy. Valid values:\n\n- **enabled**: Enables the anti-ransomware policy. After the policy is enabled, anti-ransomware can protect your server data by backing up data on specified servers based on your anti-ransomware policy.\n- **disabled**: Disables the anti-ransomware policy. After the policy is disabled, running backup tasks are stopped.\n\n> Backup tasks consume network bandwidth. Enable the policy during off-peak hours to execute data backup tasks."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "开启或关闭防护策略",
    "en": "Enable or shutdown an anti-ransomware policy"
  },
  "example": {
    "unifiedCli": "aliyun sas modify-backup-policy-status --id 30490 --status enabled --policy-version 2.0.0 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ModifyBackupPolicyStatus --region cn-hangzhou --Status enabled --PolicyVersion '2.0.0' --Id 30490"
  }
}