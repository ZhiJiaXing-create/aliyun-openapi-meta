{
  "name": "CreateOssScanConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllKeyPrefix",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否匹配所有前缀。此参数设置为true时，参数KeyPrefixList不生效。",
        "en": "Specifies whether to match the prefixes of all objects."
      }
    },
    {
      "name": "BucketNameList",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "Bucket名称列表。",
        "en": "The names of buckets."
      }
    },
    {
      "name": "DecompressMaxFileCount",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "最大解压文件数。最小取值为1，最大取值为1000。超过最大解压文件数时，解压操作将直接结束，已解压出文件的检测不受影响。",
        "en": "The maximum number of objects that can be extracted during decompression. Valid values: 1 to 1000. If the maximum number of objects that can be extracted is reached, the decompression operation immediately ends and the detection of extracted objects is not affected."
      }
    },
    {
      "name": "DecompressMaxLayer",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "存在多个层级的压缩包嵌套时的最大解压层数，最小取值为1，最大取值为5。超过最大解压层数时，解压操作将直接结束，已解压出文件的检测不受影响。",
        "en": "The maximum number of decompression levels when multi-level packages are decompressed. Valid values: 1 to 5. If the maximum number of decompression levels is reached, the decompression operation immediately ends and the detection of extracted objects is not affected."
      }
    },
    {
      "name": "DecryptionList",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "解密类型列表。",
        "en": "The decryption methods."
      }
    },
    {
      "name": "Enable",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "是否启用。取值：\n- **1**：开启\n- **0**：关闭",
        "en": "Specifies whether to enable the policy. Valid values:\n\n*   **1**: yes\n*   **0**: no"
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "检测停止时间，格式为HH:mm:ss。",
        "en": "The time when the scan ends. The time must be in the HH:mm:ss format."
      }
    },
    {
      "name": "KeyPrefixList",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "文件前缀列表。",
        "en": "The prefixes of the objects."
      }
    },
    {
      "name": "KeySuffixList",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "检测的文件后缀列表。",
        "en": "The suffixes of the files to scan."
      }
    },
    {
      "name": "LastModifiedStartTime",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "检测最后一次修改时间在指定时间戳之后的文件，单位毫秒。",
        "en": "The timestamp when the object was last modified. The time must be later than the timestamp that you specify. Unit: milliseconds."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "策略名称。",
        "en": "The policy name."
      }
    },
    {
      "name": "RealTimeIncr",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启实时增量检测。此参数设置为true时，参数ScanDayList、StartTime、EndTime不生效。",
        "en": "Whether to enable real-time incremental detection. When this parameter is set to true, the parameters ScanDayList, StartTime, and EndTime are not effective."
      }
    },
    {
      "name": "ScanDayList",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "扫描时间，数字表示一周中的第几天。",
        "en": "The days on which the scan is executed in a week."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "检测开始时间，格式为HH:mm:ss。",
        "en": "The time when the scan starts. The time must be in the HH:mm:ss format."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建检测策略配置",
    "en": "CreateOssScanConfig"
  },
  "example": {
    "unifiedCli": "aliyun sas create-oss-scan-config --enable 1 --scan-day-list 1 --bucket-name-list cccarvato-oss0000 --key-suffix-list .asp --start-time 00:00:00 --end-time 01:01:00 --name testName --key-prefix-list /root --all-key-prefix true --decompress-max-layer 1 --decompress-max-file-count 100 --decryption-list KMS --last-modified-start-time 1724301769834 --real-time-incr true --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas CreateOssScanConfig --region cn-hangzhou --KeySuffixList.1 '.asp' --BucketNameList.1 'cccarvato-oss0000' --ScanDayList.1 1 --EndTime '01:01:00' --StartTime '00:00:00' --DecompressMaxFileCount 100 --RealTimeIncr true --DecompressMaxLayer 1 --Name testName --AllKeyPrefix true --DecryptionList.1 KMS --KeyPrefixList.1 '/root' --LastModifiedStartTime 1724301769834 --Enable 1"
  }
}