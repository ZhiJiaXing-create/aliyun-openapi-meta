{
  "name": "DescribeFieldStatistics",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MachineTypes",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的资产类型。不指定资产类型，即查询所有类型资产的统计信息。取值：\n\n- **ecs**：服务器\n- **cloud_product**：云产品",
        "en": "The type of asset to query. If you do not specify an asset type, the statistics information of all asset types is queried. Valid values:\n\n- **ecs**: server\n- **cloud_product**: cloud product."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "服务器所在的地域的ID。",
        "en": "The ID of the region where the server resides."
      }
    },
    {
      "name": "ResourceDirectoryAccountId",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "资源目录成员账号主账号ID。\n>调用[DescribeMonitorAccounts](~~DescribeMonitorAccounts~~)接口可以获取该参数。",
        "en": "The ID of the Alibaba Cloud account of the member accounts in the resource directory.\n>You can invoke the [DescribeMonitorAccounts](~~DescribeMonitorAccounts~~) operation to obtain this parameter."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询资产中服务器的统计信息",
    "en": "Query server statistics information in assets"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-field-statistics --biz-region-id cn-hangzhou --machine-types ecs --resource-directory-account-id 12324284232340000 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeFieldStatistics --region cn-hangzhou --MachineTypes ecs --ResourceDirectoryAccountId 12324284232340000 --RegionId 'cn-hangzhou'"
  }
}