{
  "name": "ListImageRegistryRegion",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型，默认值为zh。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language type for requesting and receiving messages. Default value: zh. Valid values:\n- **zh**: Chinese\n- **en**: English"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询支持私有镜像仓库接入的地域",
    "en": "Query regions that support private image registry access"
  },
  "example": {
    "unifiedCli": "aliyun sas list-image-registry-region --lang zh --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ListImageRegistryRegion --region cn-hangzhou --Lang zh"
  }
}