{
  "name": "DescribeImageRepoDetailList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Criteria",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置搜索资产的条件。该参数JSON格式，包含以下字段：\n- **name**：检索项\n- **value**：检索项值\n- **logicalExp**：多个检索项值生效的逻辑关系。取值：\n    - **OR**：表示多个检索项值之间是**或**的关系。\n    - **AND**：表示多个检索项值之间是**与**的关系。\n> 您可以调用[DescribeImageRepoCriteria](~~DescribeImageRepoCriteria~~)接口查询支持的搜索条件。",
        "en": "The search conditions for assets. This parameter is in JSON format and contains the following fields:\n- **name**: the search item.\n- **value**: the value of the search item.\n- **logicalExp**: the logical relationship among multiple search item values. Valid values:\n    - **OR**: The search item values are evaluated by using a logical OR.\n    - **AND**: The search item values are evaluated by using a logical AND.\n> Call the [DescribeImageRepoCriteria](~~DescribeImageRepoCriteria~~) operation to query the supported search conditions."
      }
    },
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "设置从返回结果的第几页开始显示查询结果。默认值为**1**，表示从第1页开始显示。",
        "en": "The page number of the first page to return. Default value: **1**."
      }
    },
    {
      "name": "LogicalExp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置多个搜索条件间生效的逻辑关系。取值：\n- **OR**：表示多个条件间为**或**的关系。\n- **AND**：表示多个条件间为**与**的关系。",
        "en": "The logical relationship among multiple search conditions. Valid values:\n- **OR**: The conditions are evaluated by using a logical OR.\n- **AND**: The conditions are evaluated by using a logical AND."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定分页查询时，每页显示的数据最大条数。每页默认显示的数据条数为20条，PageSize参数值为空时，将默认返回20条数据。\n> 建议PageSize取值不要为空。",
        "en": "The maximum number of entries per page when using paging. Default value: 20. If you leave this parameter empty, 20 entries are returned per page.\n> Do not leave PageSize empty."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取镜像仓库信息列表",
    "en": "Retrieve image repository list"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-image-repo-detail-list --criteria name_:_vulStatus_,_value_:_YES_,_logicalExp_:_AND --logical-exp OR --page-size 20 --current-page 1 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeImageRepoDetailList --region cn-hangzhou --PageSize 20 --CurrentPage 1 --Criteria 'name_:_vulStatus_,_value_:_YES_,_logicalExp_:_AND' --LogicalExp OR"
  }
}