{
  "name": "ListVulAutoRepairConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AliasName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "漏洞别名。",
        "en": "The alias of the vulnerability."
      }
    },
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "设置从返回结果的第几页开始显示查询结果。默认值为**1**，表示从第1页开始显示。",
        "en": "The page number of the page to return. Default value: **1**, which indicates that the first page is returned."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置请求和接收消息的语言类型，默认为**zh**。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language of the request and response. Default value: **zh**. Valid values:\n- **zh**: Chinese\n- **en**: English."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "设置分页查询时，每页显示的可自动修复漏洞配置的数量。默认值为**10**，表示每页显示10条可自动修复漏洞配置。",
        "en": "The number of entries per page in a paged query. Default value: **10**, which indicates that 10 auto-fix vulnerability configurations are displayed per page."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "漏洞类型。取值：\n\n- **cve**：Linux软件漏洞\n- **sys**：Windows系统漏洞",
        "en": "The type of the vulnerability. Valid values:\n\n- **cve**: Linux software vulnerability\n- **sys**: Windows system vulnerability."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询创建的可自动修复漏洞配置",
    "en": "Query auto-fix vulnerability configurations"
  },
  "example": {
    "unifiedCli": "aliyun sas list-vul-auto-repair-config --type cve --alias-name RHSA-2022:0274-Important:_polkit_pkexec_Local_Privilege_Escalation_Vulnerability_CVE-2021-4034 --current-page 1 --page-size 10 --lang zh --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ListVulAutoRepairConfig --region cn-hangzhou --Type cve --PageSize 10 --CurrentPage 1 --AliasName 'RHSA-2022:0274-Important:_polkit_pkexec_Local_Privilege_Escalation_Vulnerability_CVE-2021-4034' --Lang zh"
  }
}