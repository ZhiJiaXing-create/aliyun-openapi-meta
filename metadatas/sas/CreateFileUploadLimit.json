{
  "name": "CreateFileUploadLimit",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Limit",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "设置客户端上传文件的QPS限制阈值。取值范围：100~10000。",
        "en": "The QPS limit on the files uploaded from the client. Valid values: 100 to 10000."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建客户端上传文件的QPS上限",
    "en": "CreateFileUploadLimit"
  },
  "example": {
    "unifiedCli": "aliyun sas create-file-upload-limit --limit 100 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas CreateFileUploadLimit --region cn-hangzhou --Limit 100"
  }
}