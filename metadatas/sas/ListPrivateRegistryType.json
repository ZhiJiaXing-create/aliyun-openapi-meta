{
  "name": "ListPrivateRegistryType",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language of the content within the request and response. Valid values:\n- **zh**: Chinese.\n- **en**: English."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询各个类型镜像仓的数量",
    "en": "Query the number of image repositories of each type"
  },
  "example": {
    "unifiedCli": "aliyun sas list-private-registry-type --lang zh --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ListPrivateRegistryType --region cn-hangzhou --Lang zh"
  }
}