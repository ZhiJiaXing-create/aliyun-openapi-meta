{
  "name": "DescribeQuaraFileDownloadInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "From",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求来源标识，固定为sas。",
        "en": "The source identifier of the request. Set the value to sas."
      }
    },
    {
      "name": "QuaraFileId",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "隔离文件ID。\n\n> 如果不输入该参数，调用RollbackSuspEventQuaraFile接口不会取消对文件隔离箱中文件的隔离，即调用该接口不生效。调用[DescribeSuspEventQuaraFiles](~~DescribeSuspEventQuaraFiles~~)接口可获取隔离文件ID（Id参数的值）。",
        "en": "The ID of the quarantined file.\n\n> If you do not specify this parameter, calling the RollbackSuspEventQuaraFile operation does not cancel the quarantine of the file in the quarantine box, which means the call does not take effect. Call the [DescribeSuspEventQuaraFiles](~~DescribeSuspEventQuaraFiles~~) operation to obtain the quarantined file ID (the value of the Id parameter)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询隔离文件下载链接",
    "en": "Query download link for a quarantined file"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-quara-file-download-info --quara-file-id 123 --from sas --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeQuaraFileDownloadInfo --region cn-hangzhou --QuaraFileId 123 --From sas"
  }
}