{
  "name": "OpenBackupAutoConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxBatchSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "防勒索托管服务自动生成策略一个批次中包含的服务器数量。\n\n> 最大为50，超过50以50为准。",
        "en": "The number of servers included in a single batch when the anti-ransomware managed service automatically generates policies.\n\n> The maximum value is 50. If you specify a value greater than 50, the value is set to 50."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "开启防勒索托管服务配置",
    "en": "Enable anti-ransomware managed service configuration"
  },
  "example": {
    "unifiedCli": "aliyun sas open-backup-auto-config --max-batch-size 20 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas OpenBackupAutoConfig --region cn-hangzhou --MaxBatchSize 20"
  }
}