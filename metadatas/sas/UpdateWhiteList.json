{
  "name": "UpdateWhiteList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegistryId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "镜像仓ID。\n>调用[PageImageRegistry](~~PageImageRegistry~~)接口可以获取该参数。",
        "en": "The image repository ID.\n>You can call the [PageImageRegistry](~~PageImageRegistry~~) operation to obtain this parameter."
      }
    },
    {
      "name": "WhiteList",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "白名单列表。若有多个加白地址，用（,）分隔。",
        "en": "The whitelist. Separate multiple whitelisted addresses with commas (,)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新镜像仓库IP白名单",
    "en": "Update the IP address whitelist of an image repository"
  },
  "example": {
    "unifiedCli": "aliyun sas update-white-list --registry-id 19882 --white-list 192.168.XXX.XXX,192.180.XXX.XXX --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas UpdateWhiteList --region cn-hangzhou --WhiteList '192.168.XXX.XXX,192.180.XXX.XXX' --RegistryId 19882"
  }
}