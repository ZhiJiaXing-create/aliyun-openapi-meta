{
  "name": "ListImageRegistryExtra",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegistryId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "镜像仓ID。\n>调用[PageImageRegistry](~~PageImageRegistry~~)接口可以获取该参数。",
        "en": "The image repository ID.\n>Call the [PageImageRegistry](~~PageImageRegistry~~) operation to obtain this parameter."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询镜像仓额外配置信息",
    "en": "Query extra configuration information of an image repository"
  },
  "example": {
    "unifiedCli": "aliyun sas list-image-registry-extra --registry-id 25363 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ListImageRegistryExtra --region cn-hangzhou --RegistryId 25363"
  }
}