{
  "name": "DescribeHybridProxyList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "代理集群名称。",
        "en": "The name of the proxy cluster."
      }
    },
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，当前页的页码。",
        "en": "The number of the page to return in a paged query."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，显示的每页数据的最大条数。默认值为20。",
        "en": "The maximum number of entries to return on each page in a paged query. Default value: 20."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "分页查询代理机器列表",
    "en": "Query proxy machines by paging"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-hybrid-proxy-list --cluster-name idc-sas-proxy --current-page 1 --page-size 20 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeHybridProxyList --region cn-hangzhou --PageSize 20 --CurrentPage 1 --ClusterName 'idc-sas-proxy'"
  }
}