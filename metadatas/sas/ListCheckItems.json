{
  "name": "ListCheckItems",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CheckId",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "检查项的ID。",
        "en": "The ID of the check item."
      }
    },
    {
      "name": "CheckShowName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自定义检查项名称。",
        "en": "The name of the custom check item."
      }
    },
    {
      "name": "CheckTypes",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "态势感知检查项的来源类型。",
        "en": "The source type of the situational awareness check item."
      }
    },
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定分页查询时，显示的当前页的页码。起始值为**1**，默认值为**1**。",
        "en": "Specifies the page number to display when performing a paginated query. The starting value is **1**, and the default value is **1**."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型。默认值为**zh**。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language type for requests and responses. The default value is **zh**. Values:\n\n- **zh**: Chinese\n\n- **en**: English"
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定分页查询时，每页显示的数据最大条数。每页默认显示的数据条数为20条，PageSize参数值为空时，将默认返回20条数据。\n> 建议PageSize取值不要为空。",
        "en": "Specifies the maximum number of data entries to display per page when performing a paginated query. The default number of data entries displayed per page is 20, and if the PageSize parameter is empty, it will default to returning 20 data entries.\n\n> It is recommended that the PageSize value is not left empty."
      }
    },
    {
      "name": "Statuses",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "检查项状态。",
        "en": "The status of the check item."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询自定义检查项",
    "en": "Query Custom Check Items"
  },
  "example": {
    "unifiedCli": "aliyun sas list-check-items --check-id 100000000001 --check-show-name testCheckItemName --check-types SYSTEM --lang zh --statuses RELEASE --page-size 20 --current-page 1 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ListCheckItems --region cn-hangzhou --CheckTypes.1 SYSTEM --Statuses.1 RELEASE --CheckId 100000000001 --PageSize 20 --CurrentPage 1 --CheckShowName testCheckItemName --Lang zh"
  }
}