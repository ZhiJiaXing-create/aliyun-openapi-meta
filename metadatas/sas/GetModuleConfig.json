{
  "name": "GetModuleConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页查询时，显示的当前页的页码。",
        "en": "The number of the page to return."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置分页查询时，每页显示的账户资产的指纹信息的数量。默认值为**10**，表示每页显示10条账户资产的指纹信息。",
        "en": "The number of entries to return on each page. Default value: **10**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取模块配置信息",
    "en": "GetModuleConfig"
  },
  "example": {
    "unifiedCli": "aliyun sas get-module-config --current-page 1 --page-size 10 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas GetModuleConfig --region cn-hangzhou --PageSize 10 --CurrentPage 1"
  }
}