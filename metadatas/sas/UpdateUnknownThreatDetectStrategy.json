{
  "name": "UpdateUnknownThreatDetectStrategy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DurationDaysAfterInit",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "模型初次建立后，学习N天后停止。",
        "en": "The number of days the model learns for after its creation."
      }
    },
    {
      "name": "DurationDaysAfterStop",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "当模型N天后没有新增进程时结束。",
        "en": "The number of consecutive days without new processes before the model stops learning."
      }
    },
    {
      "name": "Id",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要删除的策略的ID。",
        "en": "The ID of the unknown threat detection strategy to update."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "策略名称。",
        "en": "The name of the unknown threat detection strategy."
      }
    },
    {
      "name": "StudyMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "白名单模式。取值：\n\n- **hash**：进程Hash\n- **path**：进程路径",
        "en": "The whitelist mode. Valid values are:\n\n- **hash**: process hash\n\n- **path**: process path"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新智能行为分析策略",
    "en": "UpdateUnknownThreatDetectStrategy"
  },
  "example": {
    "unifiedCli": "aliyun sas update-unknown-threat-detect-strategy --id 1 --name strategy0000 --study-mode hash --duration-days-after-init 1 --duration-days-after-stop 1 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas UpdateUnknownThreatDetectStrategy --region cn-hangzhou --DurationDaysAfterStop 1 --Id 1 --DurationDaysAfterInit 1 --Name strategy0000 --StudyMode hash"
  }
}