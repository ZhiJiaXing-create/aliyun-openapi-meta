{
  "name": "DescribeSecurityEventMarkMissList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "设置从返回结果的第几页开始显示查询结果。默认值为**1**，表示从第1页开始显示。",
        "en": "The page number of the page to return. Default value: **1**, which indicates that the first page is returned."
      }
    },
    {
      "name": "EventName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "告警事件名称（子类型）。",
        "en": "The alert event name (child class)."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "分页查询时，每页显示的白名单规则数量。默认值为**20**，表示每页显示20条白名单规则。",
        "en": "The number of whitelist rules to display on each page in a paged query. Default value: **20**, which indicates that 20 whitelist rules are displayed on each page."
      }
    },
    {
      "name": "Remark",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资产检索项。可填写资产的IP、公网IP、内网IP、资产名称进行模糊匹配。",
        "en": "The asset search keyword. You can enter the IP address, public IP address, private IP address, or asset name for fuzzy matching."
      }
    },
    {
      "name": "SourceIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问源的IP地址。",
        "en": "The IP address of the access source."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询告警加白规则",
    "en": "Query alert whitelist rules"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-security-event-mark-miss-list --source-ip 125.210.XX.XX --event-name Login_with_unusual_location --remark 192.168.XX.XX --current-page 1 --page-size 20 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeSecurityEventMarkMissList --region cn-hangzhou --SourceIp '125.210.XX.XX' --PageSize 20 --CurrentPage 1 --EventName Login_with_unusual_location --Remark '192.168.XX.XX'"
  }
}