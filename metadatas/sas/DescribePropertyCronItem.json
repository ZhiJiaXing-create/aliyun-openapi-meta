{
  "name": "DescribePropertyCronItem",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "设置从返回结果的第几页开始显示查询结果。默认值为**1**，表示从第1页开始显示。",
        "en": "The number of the page to return. Default value: **1**."
      }
    },
    {
      "name": "ForceFlush",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "设置是否强制刷新待查询数据。取值：\n- **true**：强制刷新\n- **false**：不强制刷新",
        "en": "Specifies whether to forcefully refresh the data that you want to query. Valid values:\n\n*   **true**: yes\n*   **false**: no"
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定分页查询时，每页显示的数据最大条数。",
        "en": "The number of entries to return on each page."
      }
    },
    {
      "name": "Source",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的任务路径。",
        "en": "The path to the scheduled task."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询资产的计划任务列表",
    "en": "DescribePropertyCronItem"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-property-cron-item --force-flush false --source /data --current-page 1 --page-size 20 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribePropertyCronItem --region cn-hangzhou --PageSize 20 --CurrentPage 1 --Source '/data' --ForceFlush false"
  }
}