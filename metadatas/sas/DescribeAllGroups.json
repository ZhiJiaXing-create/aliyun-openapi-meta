{
  "name": "DescribeAllGroups",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型。默认值为**zh**，取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language of the request and response. Default value: **zh**. Valid values:\n- **zh**: Chinese\n- **en**: English."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询服务器分组信息",
    "en": "Query server group information"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-all-groups --lang zh --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeAllGroups --region cn-hangzhou --Lang zh"
  }
}