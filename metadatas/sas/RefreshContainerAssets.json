{
  "name": "RefreshContainerAssets",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AssetType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定要刷新的容器资产类型。取值：\n- **IMAGE**：容器镜像。\n- **CONTAINER**：容器。",
        "en": "The Asset Type of the container asset to refresh. Valid values:\n- **IMAGE**: container image.\n- **CONTAINER**: container."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "刷新资产中心的容器资产数据",
    "en": "Refresh container asset data in the asset center"
  },
  "example": {
    "unifiedCli": "aliyun sas refresh-container-assets --asset-type IMAGE --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas RefreshContainerAssets --region cn-hangzhou --AssetType IMAGE"
  }
}