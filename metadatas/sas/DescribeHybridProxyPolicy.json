{
  "name": "DescribeHybridProxyPolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "代理集群名称，用户可以通过控制台查看集群名称。",
        "en": "The name of the proxy cluster. You can view the cluster name in the console."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询代理集群的数据采集配置",
    "en": "Query data collection configuration of a proxy cluster"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-hybrid-proxy-policy --cluster-name test-idc --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeHybridProxyPolicy --region cn-hangzhou --ClusterName 'test-idc'"
  }
}