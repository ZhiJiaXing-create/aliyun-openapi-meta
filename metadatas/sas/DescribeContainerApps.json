{
  "name": "DescribeContainerApps",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "容器集群的ID。",
        "en": "The ID of the container cluster."
      }
    },
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "分页查询时的页码。",
        "en": "The page number."
      }
    },
    {
      "name": "FieldValue",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的应用值，支持模糊匹配。",
        "en": "The application value that you want to query. Fuzzy match is supported."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "指定分页查询时，每页显示的数据最大条数。每页默认显示的数据条数为20条，PageSize参数值为空时，将默认返回20条数据。\n> 建议PageSize取值不要为空。",
        "en": "The number of entries per page. Default value: 20. If you leave this parameter empty, 20 entries are returned on each page.\n\n>  We recommend that you do not leave this parameter empty."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取集群的app列表",
    "en": "DescribeContainerApps"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-container-apps --field-value cas-adad-qeqwe --current-page 1 --page-size 200 --cluster-id ca0a686115432429ca26cf780f5e9fff5 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeContainerApps --region cn-hangzhou --PageSize 200 --CurrentPage 1 --ClusterId ca0a686115432429ca26cf780f5e9fff5 --FieldValue 'cas-adad-qeqwe'"
  }
}