{
  "name": "GetBuildRiskDefineRuleConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language type for the request and response messages. Valid values:\n- **zh**: Chinese\n- **en**: English."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询镜像构建指令风险扫描配置",
    "en": "Query risk scan configuration for image build commands"
  },
  "example": {
    "unifiedCli": "aliyun sas get-build-risk-define-rule-config --lang zh --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas GetBuildRiskDefineRuleConfig --region cn-hangzhou --Lang zh"
  }
}