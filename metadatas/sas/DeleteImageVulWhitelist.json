{
  "name": "DeleteImageVulWhitelist",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Ids",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "漏洞白名单ID。多个id用英文逗号（,）分隔。",
        "en": "The vulnerability whitelist IDs. Separate multiple IDs with commas (,)."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型，默认值**zh**。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language of the content within the request and response. Default value: **zh**. Valid values:\n- **zh**: Chinese\n- **en**: English."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除镜像漏洞白名单",
    "en": "Delete image vulnerability whitelist"
  },
  "example": {
    "unifiedCli": "aliyun sas delete-image-vul-whitelist --ids 123 --lang zh --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DeleteImageVulWhitelist --region cn-hangzhou --Ids 123 --Lang zh"
  }
}