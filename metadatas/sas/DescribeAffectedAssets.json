{
  "name": "DescribeAffectedAssets",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Current",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "当前页。",
        "en": "The current page number."
      }
    },
    {
      "name": "Levels",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "紧急程度。多个采用英文半角逗号隔开，取值：\n- serious：紧急\n- suspicious：可疑\n- remind：提醒",
        "en": "The severity level. Separate multiple values with commas (,). Valid values:\n- serious: urgent\n- suspicious: suspicious\n- remind: reminder."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定分页查询时，每页显示的数据最大条数。每页默认显示的数据条数为20条，pagesize参数值为空时，将默认返回20条数据。\n>建议pagesize取值不要为空。",
        "en": "The maximum number of entries per page in a paginated query. Default value: 20. If this parameter is left empty, 20 entries are returned.\n>Do not leave PageSize empty."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询受影响的资产",
    "en": "Query affected assets"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-affected-assets --page-size 10 --current 1 --levels serious,suspicious,remind --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeAffectedAssets --region cn-hangzhou --PageSize 10 --Levels 'serious,suspicious,remind' --Current 1"
  }
}