{
  "name": "ExecStrategy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ExecAction",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置本次执行的动作，默认为**exec**。取值：\n\n- **exec**：执行。\n- **terminate**：终止。",
        "en": "The action to perform. Default value: **exec**. Valid values:\n\n- **exec**: exec.\n- **terminate**: stop."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置请求和接收消息的语言类型，默认为**zh**。取值：\n\n- **zh**：中文。\n- **en**：英文。",
        "en": "The language of the request and response. Default value: **zh**. Valid values:\n\n- **zh**: Chinese.\n- **en**: English."
      }
    },
    {
      "name": "StrategyId",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "基线检查策略ID。\n>调用[DescribeStrategy](~~DescribeStrategy~~)接口可以获取该参数。",
        "en": "The ID of the baseline check policy.\n>You can call the [DescribeStrategy](~~DescribeStrategy~~) operation to obtain this parameter."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "执行基线检查策略",
    "en": "Execute a baseline check policy"
  },
  "example": {
    "unifiedCli": "aliyun sas exec-strategy --lang zh --strategy-id 215421 --exec-action terminate --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ExecStrategy --region cn-hangzhou --ExecAction terminate --StrategyId 215421 --Lang zh"
  }
}