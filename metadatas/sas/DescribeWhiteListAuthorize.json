{
  "name": "DescribeWhiteListAuthorize",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置请求和接收消息的语言类型。默认值**zh**。取值：\n\n\n- **zh**：中文\n- **en**：英文",
        "en": "The language type for the request and response messages. Default value: **zh**. Valid values:\n\n\n- **zh**: Chinese.\n- **en**: English."
      }
    },
    {
      "name": "SourceIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求源IP。无需填写，系统自动获取。",
        "en": "The source IP address of the request. You do not need to specify this parameter. The system automatically obtains this value."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取可用授权数量",
    "en": "Retrieve available authorization count"
  },
  "descriptions": {
    "zh": "应用白名单为公测功能，目前已不再支持申请该功能。已申请或正在使用中的用户可以正常调用该接口。",
    "en": "The application whitelist is a China site China site public preview feature that is no longer open for new applications. Users who have already applied for or are using this feature can call this operation as expected."
  },
  "example": {
    "unifiedCli": "aliyun sas describe-white-list-authorize --source-ip 180.119.XX.XX --lang zh --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeWhiteListAuthorize --region cn-hangzhou --SourceIp '180.119.XX.XX' --Lang zh"
  }
}