{
  "name": "AddPublishBatch",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BatchName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "发布批次名称。",
        "en": "The name of the release batch."
      }
    },
    {
      "name": "Interval",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "发布批次间隔周期。",
        "en": "The interval between two release batches."
      }
    },
    {
      "name": "OperationBase",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "资产选择维度。取值：\n\n- **0**：机器实例\n- **1**：机器分组\n- **2**：VPC实例ID",
        "en": "The dimension based on which the asset is selected. Valid values:\n\n*   **0**: selects the asset by instance.\n*   **1**: selects the asset by machine group.\n*   **2**: selects the asset by the ID of the instance that is deployed in the virtual private cloud (VPC)."
      }
    },
    {
      "name": "UpgradeVersion",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定要升级目标版本。",
        "en": "The version to which you want to upgrade the agent."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "新增发布批次",
    "en": "AddPublishBatch"
  },
  "example": {
    "unifiedCli": "aliyun sas add-publish-batch --upgrade-version 0.0.9 --batch-name '批次1' --interval 60 --operation-base 0 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas AddPublishBatch --region cn-hangzhou --BatchName '批次1' --UpgradeVersion '0.0.9' --OperationBase 0 --Interval 60"
  }
}