{
  "name": "DescribeVulConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "SourceIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求源IP。",
        "en": "The source IP address of the request."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "漏洞管理配置类型，默认查询所有类型的配置。取值：\n-  **cve**：Linux软件漏洞\n-  **sys**：Windows系统漏洞\n-  **cms**：Web-CMS漏洞\n-  **app**：应用漏洞（web扫描器）\n-  **emg**：应急漏洞\n- **scanMode**：显示真实风险漏洞\n- **imageVulClean**：漏洞保留时长\n- **yum**：优先使用阿里云源进行漏洞修复",
        "en": "The type of vulnerability management configuration. By default, all types of configurations are queried. Valid values:\n-  **cve**: Linux software vulnerability\n-  **sys**: Windows system vulnerability\n-  **cms**: Web-CMS vulnerability\n-  **app**: application vulnerability (web scanner)\n-  **emg**: emergency vulnerability\n- **scanMode**: display real-risk vulnerabilities\n- **imageVulClean**: vulnerability retention period\n- **yum**: prioritize Alibaba Cloud sources for vulnerability fixing."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询漏洞管理配置",
    "en": "Query vulnerability management configuration"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-vul-config --source-ip 113.110.XX.XX --type cve --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeVulConfig --region cn-hangzhou --Type cve --SourceIp '113.110.XX.XX'"
  }
}