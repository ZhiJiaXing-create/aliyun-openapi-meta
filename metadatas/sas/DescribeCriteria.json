{
  "name": "DescribeCriteria",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MachineTypes",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询的资产类型。取值：\n\n- **ecs** ：表示查询所有ECS服务器。",
        "en": "The type of the asset. Valid values:\n\n*   Set the value to **ecs**, which specifies to query all Elastic Compute Service (ECS) instances."
      }
    },
    {
      "name": "SupportAutoTag",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "设置输入的模糊查询字段是否支持自动匹配。默认**false**，取值：\n\n- **true**：是。\n- **false**：否。",
        "en": "Specifies whether the keyword that you specify for fuzzy search can be automatically matched. Default value: **false**. Valid values:\n\n*   **true**\n*   **false**"
      }
    },
    {
      "name": "Value",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询资产时输入的模糊匹配值。",
        "en": "The keyword that you specify for fuzzy search when you query the asset."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询资产时的查询条件",
    "en": "DescribeCriteria"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-criteria --machine-types ecs --value 47.96 --support-auto-tag true --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeCriteria --region cn-hangzhou --SupportAutoTag true --MachineTypes ecs --Value '47.96'"
  }
}