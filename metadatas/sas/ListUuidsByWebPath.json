{
  "name": "ListUuidsByWebPath",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "分页查询时，显示的当前页的页码。",
        "en": "The page number of the current page to display in a paging query."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "分页查询时，显示的每页数据的最大条数。",
        "en": "The maximum number of entries to display on each page in a paging query."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Web路径类型。取值：\n\n- **def**：系统自动识别\n- **customize**：手动添加",
        "en": "The type of the web path. Valid values:\n\n- **def**: automatically identified by the system\n- **customize**: manually added."
      }
    },
    {
      "name": "WebPath",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Web路径。",
        "en": "The web path."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "根据Web路径查询防护资产",
    "en": "Query protected assets by web path"
  },
  "example": {
    "unifiedCli": "aliyun sas list-uuids-by-web-path --web-path /root/www0000 --current-page 1 --page-size 20 --type def --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ListUuidsByWebPath --region cn-hangzhou --Type def --PageSize 20 --CurrentPage 1 --WebPath '/root/www0000'"
  }
}