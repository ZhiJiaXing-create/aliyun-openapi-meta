{
  "name": "CreateUnknownThreatDetectStrategy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AssetSelectionType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资产选择标识。",
        "en": "The asset selection identifier."
      }
    },
    {
      "name": "DurationDaysAfterInit",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "模型初次建立后，学习N天后结束。",
        "en": "The number of days after the model is initially created before learning ends."
      }
    },
    {
      "name": "DurationDaysAfterStop",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "当模型N天后没有新增进程时结束。",
        "en": "The number of days after which the model ends learning if no new processes are added."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "策略名称。",
        "en": "The policy name."
      }
    },
    {
      "name": "StudyMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "白名单模式。取值：\n\n- **hash**：进程Hash\n- **path**：进程路径",
        "en": "The whitelist mode. Valid values:\n\n- **hash**: process hash\n- **path**: process path"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建智能行为分析策略",
    "en": "Create an intelligent behavior analytics policy"
  },
  "example": {
    "unifiedCli": "aliyun sas create-unknown-threat-detect-strategy --name strategy0000 --study-mode hash --asset-selection-type UNKNOWN_THREAT_DETECT_CONFIG_0000 --duration-days-after-init 1 --duration-days-after-stop 1 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas CreateUnknownThreatDetectStrategy --region cn-hangzhou --AssetSelectionType UNKNOWN_THREAT_DETECT_CONFIG_0000 --DurationDaysAfterStop 1 --DurationDaysAfterInit 1 --Name strategy0000 --StudyMode hash"
  }
}