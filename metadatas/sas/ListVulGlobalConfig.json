{
  "name": "ListVulGlobalConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConfigKey",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "配置项的key。取值：\n- **vul_scan_ip_list**：漏洞扫描IP集合。",
        "en": "The key of the configuration item. Valid values:\n- **vul_scan_ip_list**: the collection of vulnerability scanning IP addresses."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询漏洞全局配置",
    "en": "Query vulnerability global configuration"
  },
  "example": {
    "unifiedCli": "aliyun sas list-vul-global-config --config-key vul_scan_ip_list --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ListVulGlobalConfig --region cn-hangzhou --ConfigKey vul_scan_ip_list"
  }
}