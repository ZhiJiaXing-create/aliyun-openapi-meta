{
  "name": "ListMaliciousFileWhitelistConfigs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "返回数据当前页的页码。起始值为1，默认值为1。",
        "en": "The page number of the current page to return. Minimum value: 1. Default value: 1."
      }
    },
    {
      "name": "EventName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "告警名称：\n- ALL： 全部告警类型",
        "en": "The alerting name. Valid values:\n- ALL: all Alarm Metric values."
      }
    },
    {
      "name": "IdList",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "事件ID。\n\n><notice>字段已废弃。></notice>",
        "en": "The event ID.\n\n><notice>This field is deprecated.></notice>."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型，默认值为**zh**。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language of the request and response. Default value: **zh**. Valid values:\n- **zh**: Chinese\n- **en**: English."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定分页查询时，每页显示的数据最大条数。每页默认显示的数据条数为20条。",
        "en": "The maximum number of entries to return per page in a paging query. Default value: 20."
      }
    },
    {
      "name": "Source",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "业务来源，可为空，默认为agentless。",
        "en": "The business source. This parameter can be left empty. Default value: agentless."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询无代理检测敏感文件告警加白规则列表",
    "en": "Query allowlist rules for agentless sensitive file detection alerts"
  },
  "example": {
    "unifiedCli": "aliyun sas list-malicious-file-whitelist-configs --source agentless --lang zh --current-page 1 --page-size 20 --event-name ALL --id-list 123 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ListMaliciousFileWhitelistConfigs --region cn-hangzhou --PageSize 20 --CurrentPage 1 --Lang zh --EventName ALL --Source agentless --IdList 123"
  }
}