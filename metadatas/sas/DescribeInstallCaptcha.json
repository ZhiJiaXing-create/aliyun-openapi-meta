{
  "name": "DescribeInstallCaptcha",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Deadline",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定安装验证码的有效期。该参数的取值为空时，只查询有效的安装验证码。\n> 安装验证码只有在有效期内才能使用。已过期的安装验证码无法安装云安全中心Agent。",
        "en": "The expiration time of the installation verification code. If this parameter is left empty, only valid installation verification codes are queried.\n> The installation verification code can be used only within its validity period. An expired installation verification code cannot be used to install the Security Center Agent."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求和接收消息的语言类型。默认**zh**，取值：\n- **zh**：中文。\n- **en**：英文。",
        "en": "The language type for requests and responses. Default value: **zh**. Valid values:\n- **zh**: Chinese.\n- **en**: English."
      }
    },
    {
      "name": "SourceIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问源的IP地址。",
        "en": "The IP address of the access source."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取手动安装Agent的安装验证码",
    "en": "Retrieve the installation verification code for manual agent installation"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-install-captcha --source-ip 192.168.XX.XX --lang zh --deadline 2020-10-11_16:26:22 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeInstallCaptcha --region cn-hangzhou --SourceIp '192.168.XX.XX' --Deadline '2020-10-11_16:26:22' --Lang zh"
  }
}