{
  "name": "DescribeHybridProxyClusterList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "代理集群名称。",
        "en": "The name of the proxy cluster."
      }
    },
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，显示的当前页的页码。",
        "en": "The page number of the current page to display in a paged query."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，显示的每页数据的最大条数。",
        "en": "The maximum number of entries to display on each page in a paged query."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "分页查询代理集群列表",
    "en": "Query proxy clusters by using paging"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-hybrid-proxy-cluster-list --cluster-name test --current-page 1 --page-size 20 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeHybridProxyClusterList --region cn-hangzhou --PageSize 20 --CurrentPage 1 --ClusterName test"
  }
}