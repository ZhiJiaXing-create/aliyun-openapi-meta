{
  "name": "ListPublishBatch",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BatchName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "发布批次名称。",
        "en": "The name of the release batch."
      }
    },
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "分页查询时，当前页的页码。",
        "en": "The page number of the current page when paging is used."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "分页查询时，显示每页数据的最大条数。",
        "en": "The maximum number of entries per page when paging is used."
      }
    },
    {
      "name": "UpgradeVersion",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要升级目标版本。",
        "en": "The target version to upgrade to."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询发布批次",
    "en": "Query release batches"
  },
  "example": {
    "unifiedCli": "aliyun sas list-publish-batch --upgrade-version 0.0.9 --current-page 1 --page-size 20 --batch-name '批次1' --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ListPublishBatch --region cn-hangzhou --BatchName '批次1' --PageSize 20 --CurrentPage 1 --UpgradeVersion '0.0.9'"
  }
}