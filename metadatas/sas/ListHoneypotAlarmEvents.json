{
  "name": "ListHoneypotAlarmEvents",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "设置从返回结果的第几页开始显示查询结果。默认值为**1**，表示从第1页开始显示。",
        "en": "The page number. Default value: **1**."
      }
    },
    {
      "name": "Dealed",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否已处理。默认值为**a**。取值：\n- **a**：所有\n- **y**：已经处理 \n- **n**：未处理",
        "en": "The status of the alert event. Default value: **a**. Valid values:\n\n*   **a**: all states\n*   **y**: handled\n*   **n**: unhandled"
      }
    },
    {
      "name": "DstIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "目的IP。",
        "en": "The destination IP address."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，每页显示的数据最大条数。每页默认显示的数据条数为100条，PageSize参数值为空时，将默认返回100条数据。\n> 建议PageSize取值不要为空且最大值为100。",
        "en": "The number of entries per page. Default value: 100. If you leave this parameter empty, 100 entries are returned on each page.\n\n>  We recommend that you do not leave this parameter empty. We recommend that you set the value to a value no greater than 100."
      }
    },
    {
      "name": "RiskLevelList",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "风险等级列表。",
        "en": "The risk levels."
      }
    },
    {
      "name": "SrcIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "源IP。",
        "en": "The source IP address."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取告警事件",
    "en": "ListHoneypotAlarmEvents"
  },
  "example": {
    "unifiedCli": "aliyun sas list-honeypot-alarm-events --dealed a --src-ip 185.224.XX.XX --dst-ip 183.224.XX.XX --risk-level-list 2 --current-page 1 --page-size 100 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ListHoneypotAlarmEvents --region cn-hangzhou --PageSize 100 --CurrentPage 1 --DstIp '183.224.XX.XX' --RiskLevelList.1 2 --Dealed a --SrcIp '185.224.XX.XX'"
  }
}