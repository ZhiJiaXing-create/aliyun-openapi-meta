{
  "name": "DescribeExcludeSystemPath",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "设置从返回结果的第几页开始显示查询结果。默认值为1，表示从第1页开始显示。",
        "en": "The page number of the page to return. Default value: 1, which indicates that the first page is returned."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "指定分页查询时，每页显示的数据最大条数。",
        "en": "The maximum number of entries to return on each page when paging is used for a paged query."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询防勒索系统排除目录",
    "en": "Query anti-ransomware system excluded directories"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-exclude-system-path --page-size 20 --current-page 1 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeExcludeSystemPath --region cn-hangzhou --PageSize 20 --CurrentPage 1"
  }
}