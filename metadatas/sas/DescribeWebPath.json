{
  "name": "DescribeWebPath",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，显示的当前页的页码。",
        "en": "The page number of the current page in a paged query. Paging is used to display results."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，显示的每页数据的最大条数。",
        "en": "The maximum number of entries per page in a paged query. Paging is used to display results."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "告警配置类型。取值：\n\n- **web_path**",
        "en": "The alert configuration type. Valid values:\n\n- **web_path**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询安全告警Web自定义目录",
    "en": "Query custom web directories for security alerts"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-web-path --type web_path --current-page 1 --page-size 20 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeWebPath --region cn-hangzhou --Type web_path --PageSize 20 --CurrentPage 1"
  }
}