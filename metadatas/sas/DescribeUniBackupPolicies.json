{
  "name": "DescribeUniBackupPolicies",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "设置从返回结果的第几页开始显示查询结果。默认值为**1**，表示从第1页开始显示。",
        "en": "The page number from which to start displaying query results. Default value: **1**, which indicates that query results are displayed starting from page 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定分页查询时，每页显示的数据最大条数。每页默认显示的数据条数为20条，PageSize参数值为空时，将默认返回20条数据。\n> 建议PageSize取值不要为空。",
        "en": "The maximum number of entries to display per page for a paginated query. The default number of entries per page is 20. If the PageSize parameter is left empty, 20 entries are returned by default.\n> We recommend that you do not leave PageSize empty."
      }
    },
    {
      "name": "PolicyName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据库防勒索备份策略的名称。",
        "en": "The name of the database anti-ransomware backup policy."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询防勒索策略",
    "en": "Query anti-ransomware policies"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-uni-backup-policies --policy-name auto_oracle_Hpm --page-size 20 --current-page 1 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeUniBackupPolicies --region cn-hangzhou --PageSize 20 --CurrentPage 1 --PolicyName auto_oracle_Hpm"
  }
}