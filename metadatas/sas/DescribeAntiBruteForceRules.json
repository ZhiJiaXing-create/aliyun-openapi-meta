{
  "name": "DescribeAntiBruteForceRules",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "设置从返回结果的第几页开始显示查询结果。默认值为**1**，表示从第1页开始显示。",
        "en": "The page number of the page to return. Default value: **1**, which indicates the first page."
      }
    },
    {
      "name": "Id",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "防暴力破解规则ID。\n>调用[DescribeAntiBruteForceRules](~~DescribeAntiBruteForceRules~~)接口可以获取该参数。",
        "en": "The ID of the brute-force attacks prevention rule.\n>You can invoke the [DescribeAntiBruteForceRules](~~DescribeAntiBruteForceRules~~) operation to obtain this parameter."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "暴力破解规则的名称。",
        "en": "The name of the brute-force attacks prevention rule."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页查询时，显示的每页数据的最大条数。",
        "en": "The maximum number of entries to return on each page in a paged query. This parameter is used for paging."
      }
    },
    {
      "name": "SourceIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问源的IP地址。",
        "en": "The IP address of the access source."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询已创建的防暴力破解规则",
    "en": "Query brute-force attacks prevention rules"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-anti-brute-force-rules --source-ip 121.69.XX.XX --id 11410000 --current-page 1 --page-size 10 --name testName --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeAntiBruteForceRules --region cn-hangzhou --SourceIp '121.69.XX.XX' --PageSize 10 --CurrentPage 1 --Id 11410000 --Name testName"
  }
}