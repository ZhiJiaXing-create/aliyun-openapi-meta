{
  "name": "EnableCustomBlockRecord",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BlockIp",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "防暴力破解IP地址。",
        "en": "The IP address that is specified in the policy."
      }
    },
    {
      "name": "Bound",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "服务器暴力破解拦截策略阻断IP的方向。\n取值：\n- **in** ：入方向 \n- **out**：出方向",
        "en": "The traffic direction that is specified in the policy. Valid values:\n\n*   **in**: inbound\n*   **out**: outbound"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "开启自定义拦截",
    "en": "EnableCustomBlockRecord"
  },
  "example": {
    "unifiedCli": "aliyun sas enable-custom-block-record --block-ip 43.248.XX.XX --bound in --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas EnableCustomBlockRecord --region cn-hangzhou --BlockIp '43.248.XX.XX' --Bound in"
  }
}