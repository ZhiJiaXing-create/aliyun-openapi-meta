{
  "name": "ListDockerhubImage",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Query",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "镜像查询条件，支持`[namespace/]repoName[:version]`的形式查询，`[]`内查询条件可选。",
        "en": "The image query condition. The format is `[namespace/]repoName[:version]`. The query conditions in `[]` are optional."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询dockerhub镜像",
    "en": "Query docker hub images"
  },
  "example": {
    "unifiedCli": "aliyun sas list-dockerhub-image --query python:3.9 --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas ListDockerhubImage --region cn-hangzhou --Query 'python:3.9'"
  }
}