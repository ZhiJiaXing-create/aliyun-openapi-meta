{
  "name": "DescribeStrategy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CustomType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的策略的类型。取值：\n\n- **common**：标准策略\n- **custom**：自定义策略",
        "en": "The type of the baseline check policy that you want to query. Valid values:\n\n*   **common**: standard baseline check policy\n*   **custom**: custom baseline check policy"
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置请求和接收消息的语言类型，默认为**zh**。取值：\n\n- **zh**：中文\n- **en**：英文",
        "en": "The language of the content within the request and response. Default value: **zh**. Valid values:\n\n*   **zh**: Chinese\n*   **en**: English"
      }
    },
    {
      "name": "SourceIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问源IP地址。",
        "en": "The source IP address of the request."
      }
    },
    {
      "name": "StrategyIds",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的策略的ID。多个ID之间使用半角逗号（,）隔开。",
        "en": "The ID of the baseline check policy that you want to query. Separate multiple IDs with commas (,)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询基线检查策略",
    "en": "DescribeStrategy"
  },
  "example": {
    "unifiedCli": "aliyun sas describe-strategy --source-ip 1.2.X.X --lang zh --strategy-ids 8164248 --custom-type custom --api-version 2018-12-03 --region cn-hangzhou",
    "legacyCli": "aliyun sas DescribeStrategy --region cn-hangzhou --CustomType custom --SourceIp '1.2.X.X' --Lang zh --StrategyIds 8164248"
  }
}