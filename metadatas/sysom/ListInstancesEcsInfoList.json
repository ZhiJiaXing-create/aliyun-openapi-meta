{
  "name": "ListInstancesEcsInfoList",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/am/instance/listInstancesEcsInfoList",
  "parameters": [
    {
      "name": "info_type",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要获取的信息类型",
        "en": "The type of information to retrieve."
      }
    },
    {
      "name": "instance_id",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定本字段，则过滤出指定实例的Agent安装情况",
        "en": "Specifies the instance ID to filter and return the Agent installation status of the specified instance."
      }
    },
    {
      "name": "managed_type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例的纳管状态",
        "en": "The management status of the instance."
      }
    },
    {
      "name": "plugin_id",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定本参数则过滤出对应组件的实例信息列表",
        "en": "Specifies the component ID to filter and return the instance information list for the corresponding component."
      }
    },
    {
      "name": "region",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "通过区域过滤实例",
        "en": "The region used to filter instances."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取实例的ecs信息的列表",
    "en": "Retrieve ECS information lists for instances"
  },
  "descriptions": {
    "zh": "当前接口获取的实例列表是已经被 SysOM 纳管的机器列表，如果某个ECS实例存在但是没有被 SysOM 纳管，则不会出现在列表当中。",
    "en": "The instance list retrieved by this operation contains only machines that are managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list."
  }
}