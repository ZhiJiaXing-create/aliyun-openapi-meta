{
  "name": "ListClusters",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/am/cluster/list_clusters",
  "parameters": [
    {
      "name": "cluster_id",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "通过集群 ID 过滤\n\n> 这个集群ID不是 ACK 集群ID，而是通过本接口返回的数据里面的 `id` 字段",
        "en": "Filter by cluster ID.\n\n> This cluster ID is not the ACK cluster ID, but the `id` field in the data returned by this API."
      }
    },
    {
      "name": "cluster_status",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "- `Running` : 集群纳管正常；\n- `Installing`: 集群有安装任务正在进行；\n- `Uninstalling`: 集群有卸载任务正在进行；\n- `Upgrading`: 集群有更新任务正在进行；\n- `Offline`: 集群离线，纳管异常。",
        "en": "- `Running`: The cluster is managed normally.\n- `Installing`: The cluster has an installation task in progress.\n- `Uninstalling`: The cluster has an uninstallation task in progress.\n- `Upgrading`: The cluster has an upgrade task in progress.\n- `Offline`: The cluster is offline and management is abnormal."
      }
    },
    {
      "name": "cluster_type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "- `ACK`: ACK 集群\n- `CUSTOM`: 自定义集群（默认集群属于自定义集群）",
        "en": "- `ACK`: ACK cluster.\n- `CUSTOM`: Custom cluster (the default cluster belongs to custom clusters)."
      }
    },
    {
      "name": "current",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "当前页数（从第1页开始）",
        "en": "Current page number (starting from page 1)"
      }
    },
    {
      "name": "id",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "本字段已废弃，请使用 cluster_id 字段过滤",
        "en": "This field is deprecated. Use the cluster_id field to filter instead."
      }
    },
    {
      "name": "name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "通过插件的名字过滤插件",
        "en": "Filter by plugin name"
      }
    },
    {
      "name": "pageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "每页大小",
        "en": "Page size"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取当前用户纳管集群列表",
    "en": "Retrieve the managed cluster list of the current user"
  }
}