{
  "name": "DescribeMetricList",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/openapi/proxy/get/describeMetricList",
  "parameters": [
    {
      "name": "endTime",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "截止时间，秒级时间戳",
        "en": "The end time as a UNIX timestamp in seconds."
      }
    },
    {
      "name": "instance",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "metricName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "监控项名称",
        "en": "The metric name."
      }
    },
    {
      "name": "startTime",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "起始时间，秒级时间戳",
        "en": "The start time as a UNIX timestamp in seconds."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询指标数据",
    "en": "Query metric data"
  },
  "descriptions": {
    "zh": "当前接口获取的实例列表是已经被 SysOM 纳管的机器列表，如果某个ECS实例存在但是没有被 SysOM 纳管，则不会出现在列表当中。",
    "en": "The instance list returned by this operation contains only instances that are managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list."
  }
}