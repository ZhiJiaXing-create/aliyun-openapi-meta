{
  "name": "ListInstances",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/am/instance/list_instances",
  "parameters": [
    {
      "name": "cluster_id",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "集群ID",
        "en": "The cluster ID."
      }
    },
    {
      "name": "current",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "页码分页时存在该字段，表示当前页",
        "en": "The current page number. This field exists when pagination is used."
      }
    },
    {
      "name": "instance",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "通过 ECS 实例ID过滤",
        "en": "The ECS instance ID used to filter results."
      }
    },
    {
      "name": "pageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "每页大小，默认值为20，取值范围【1,100】",
        "en": "The number of entries per page. Default value: 20. Valid values: 1 to 100."
      }
    },
    {
      "name": "region",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "通过区域过滤实例",
        "en": "Filters instances by region."
      }
    },
    {
      "name": "status",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "通过状态过滤实例",
        "en": "Filters instances by status."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取纳管实例列表",
    "en": "Retrieve managed instance list"
  },
  "descriptions": {
    "zh": "当前接口获取的实例列表是已经被 SysOM 纳管的机器列表，如果某个ECS实例存在但是没有被 SysOM 纳管，则不会出现在列表当中。",
    "en": "This operation retrieves the list of instances that are already managed by SysOM. If an ECS instance exists but is not managed by SysOM, it does not appear in the list."
  }
}