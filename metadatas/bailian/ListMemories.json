{
  "name": "ListMemories",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/[workspaceId]/memories",
  "parameters": [
    {
      "name": "maxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页行数。取值范围[1-50]。\n\n默认值： 当不设置值时，默认值为 10。",
        "en": "The number of entries per page in a paged query. Valid values: 1 to 50.\n\nDefault value: 10."
      }
    },
    {
      "name": "nextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询凭证（Token），取值为上一次 API 调用返回的 NextToken 参数值。",
        "en": "The pagination token. Set this parameter to the NextToken value returned in the previous API call."
      }
    },
    {
      "name": "workspaceId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "长期记忆体所属的业务空间 ID。获取方式请参见[如何使用业务空间](~~2587495~~)。",
        "en": "The ID of the workspace to which the long-term memory entities belong. For information about how to obtain the workspace ID, see [How to use workspaces](~~2587495~~)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取长期记忆体列表",
    "en": "List long-term memories"
  },
  "descriptions": {
    "zh": "- 分页查询首页时，仅需设置`MaxResults`以限制返回信息的条目数，返回结果中的`NextToken`将作为查询后续页的凭证。查询后续页时，将`NextToken`参数设置为上一次返回结果中获取到的`NextToken`作为查询凭证（如果`NextToken`为空，表示结果已经完全返回，不需要再请求），并设置`MaxResults`限制返回条目数。\n- 本接口具有幂等性。\n\n**限流说明：** 请确保两次请求间隔至少 1 秒，否则可能触发系统限流。如遇限流，请稍后重试。",
    "en": "- When querying the first page of a paging query, set only `MaxResults` to limit the number of entries returned. The `NextToken` value in the response serves as the credential for querying subsequent pages. When querying subsequent pages, set the `NextToken` parameter to the `NextToken` value obtained from the previous response as the query credential (if `NextToken` is empty, all results have been returned and no further requests are needed), and settings `MaxResults` to limit the number of entries returned.\n- This operation supports idempotence.\n\n**Rate limit:** Ensure that the interval between two consecutive requests is at least 1 second. Otherwise, throttling may be triggered. If throttling occurs, retry later."
  }
}