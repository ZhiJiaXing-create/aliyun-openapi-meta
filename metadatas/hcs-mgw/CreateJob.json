{
  "name": "CreateJob",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/job",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "Struct",
      "required": false,
      "description": {
        "zh": "创建迁移任务请求体。",
        "en": "The request body for creating the migration task."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建任务",
    "en": "CreateJob"
  },
  "descriptions": {
    "zh": "- 创建迁移任务，您必须有mgw:CreateImportJob权限。\n- 创建迁移任务前，您需要先创建数据地址。\n- 迁移任务可以运行多轮，每轮运行有一个执行ID。",
    "en": "*   To create a migration task, you must have the permission on mgw:CreateImportJob.\n*   Before you create a migration task, you must create data addresses.\n*   A migration task can run multiple rounds. Each round has an execution ID."
  }
}