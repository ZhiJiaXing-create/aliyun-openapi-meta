{
  "name": "DeleteRecallManagementTable",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/recallmanagementtables/[RecallManagementTableId]",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "RecallManagementTableId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据表ID。",
        "en": "The ID of the recall management table."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除召回管理数据表",
    "en": "DeleteRecallManagementTable"
  },
  "descriptions": {
    "zh": "## 请求说明\n- **路径参数** `RecallManagementTableId` 是必需的，用来指定要删除的数据表ID。\n- **查询参数** `InstanceId` 同样是必需的，用来指定执行操作的实例ID。\n- 成功执行后，将返回一个包含请求ID (`RequestId`) 的响应体，可用于追踪本次请求。",
    "en": "## Request\n\n- The required **path parameter** `RecallManagementTableId` specifies the ID of the recall management table to delete.\n\n- The required **query parameter** `InstanceId` specifies the ID of the instance.\n\n- A successful operation returns a `RequestId` in the response body for request tracking."
  }
}