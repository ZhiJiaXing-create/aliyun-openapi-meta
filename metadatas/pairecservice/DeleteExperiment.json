{
  "name": "DeleteExperiment",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/experiments/[ExperimentId]",
  "parameters": [
    {
      "name": "ExperimentId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实验ID，可通过接口ListExperiments获取。",
        "en": "Experiment ID, which can be obtained by calling the ListExperiments API."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID，可通过接口ListInstances获取。",
        "en": "Instance ID, which can be obtained by calling the ListInstances API."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除实验",
    "en": "Delete Experiment"
  }
}