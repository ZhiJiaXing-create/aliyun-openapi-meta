{
  "name": "GetRecallManagementServiceVersionConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/recallmanagementservices/[RecallManagementServiceId]/versions/[RecallManagementServiceVersionId]/configs/[RecallManagementServiceVersionConfigId]",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。",
        "en": "The ID of the instance."
      }
    },
    {
      "name": "RecallManagementServiceId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "召回管理服务ID。",
        "en": "The ID of the RecallManagementService."
      }
    },
    {
      "name": "RecallManagementServiceVersionConfigId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "召回管理服务版本配置ID。",
        "en": "The ID of the RecallManagementServiceVersionConfig."
      }
    },
    {
      "name": "RecallManagementServiceVersionId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "召回管理服务版本ID。",
        "en": "The ID of the RecallManagementServiceVersion."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取召回管理服务版本配置",
    "en": "Get Retrieval Management Service Version Configuration"
  },
  "descriptions": {
    "zh": "## 请求说明\n通过指定召回管理服务ID、版本ID以及配置ID，可以获取到该召回服务版本的具体配置详情。请确保提供的参数值准确无误，特别是`InstanceId`，它对于定位正确的实例至关重要。\n\n- **注意**：所有路径参数（`RecallManagementServiceId`, `RecallManagementServiceVersionId`, `RecallManagementServiceVersionConfigId`）都是必需的，并且必须与实际存在的资源相匹配。\n- **扩展配置**：返回的数据中包含`ExtendedConfig`字段，用于未来功能扩展或自定义设置，请根据需要解析使用。",
    "en": "## Request\n\nRetrieves the configuration details for a specific version of a Recall Management Service by specifying its service, version, and configuration IDs. Ensure the parameter values are correct. The `InstanceId` is crucial for locating the correct instance.\n\n- **Note**: All path parameters (`RecallManagementServiceId`, `RecallManagementServiceVersionId`, and `RecallManagementServiceVersionConfigId`) are required and must reference an existing resource.\n\n- **Extended configuration**: The response includes the `ExtendedConfig` field, which is used for future extensions and custom settings. Parse this field as needed."
  }
}