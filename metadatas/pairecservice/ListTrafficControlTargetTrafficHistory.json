{
  "name": "ListTrafficControlTargetTrafficHistory",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/trafficcontroltargets/[TrafficControlTargetId]/traffichistories",
  "parameters": [
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "结束时间",
        "en": "The end of the time range."
      }
    },
    {
      "name": "Environment",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "参数所属的环境。Daily-日常环境 Pre-预发环境 Prod-生产环境",
        "en": "The target environment. Valid values: Daily, Pre, and Prod."
      }
    },
    {
      "name": "ExperimentGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实验组ID。",
        "en": "The ID of the experiment group."
      }
    },
    {
      "name": "ExperimentId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实验ID。",
        "en": "The ID of the experiment."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。",
        "en": "The ID of the instance."
      }
    },
    {
      "name": "ItemId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "物品ID。",
        "en": "The ID of the item."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "开始时间",
        "en": "The start of the time range."
      }
    },
    {
      "name": "Threshold",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "阈值",
        "en": "The threshold value."
      }
    },
    {
      "name": "TrafficControlTargetId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "流量调控目标ID。",
        "en": "The ID of the traffic control target."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取流量调控任务历史",
    "en": "Retrieve traffic control task history"
  },
  "descriptions": {
    "zh": "## 请求说明\n- `TrafficControlTargetId`、`InstanceId` 和 `Environment` 是必填参数。\n- `StartTime` 和 `EndTime` 可以用来限制查询的时间范围，格式为 `YYYY-MM-DD HH:MM:SS`。\n- `Threshold` 参数用于设置阈值，但不是必选。\n- `ExperimentId` 和 `ExperimentGroupId` 用于过滤特定实验或实验组的数据。\n- `ItemId` 用于过滤特定物品的流量数据。\n- 当前支持的环境包括：Daily（日常环境）、Pre（预发环境）和 Prod（生产环境）。",
    "en": "## Usage notes\n\n- The `TrafficControlTargetId`, `InstanceId`, and `Environment` parameters are required.\n\n- You can use `StartTime` and `EndTime` to specify the time range.\n\n- The `Threshold` parameter is optional.\n\n- Use `ExperimentId` and `ExperimentGroupId` to filter data for a specific experiment or experiment group.\n\n- Use `ItemId` to filter traffic data for a specific item.\n\n- The supported environments are the Daily environment, pre-production environment (Pre), and production environment (Prod)."
  }
}