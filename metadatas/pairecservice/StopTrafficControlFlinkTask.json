{
  "name": "StopTrafficControlFlinkTask",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/trafficcontroltasks/[TrafficControlTaskId]/action/stopflink",
  "parameters": [
    {
      "name": "TrafficControlTaskId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "流量调控 ID。",
        "en": "The traffic control ID."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "停止流量调控Flink任务",
    "en": "Stop a traffic control flink task"
  },
  "descriptions": {
    "zh": "## 请求说明\n通过此API，您可以根据提供的`TrafficControlTaskId`来停止一个特定的流量调控Flink任务。请确保您已经准备好正确的`InstanceId`以及该实例所属的环境（Daily-日常环境, Pre-预发环境, Prod-生产环境）。在请求体中必须包含这些信息以便正确执行操作。",
    "en": "## Request description\nYou can call this operation to stop a specific traffic control Flink task based on the specified TrafficControlTaskId. Make sure that you have prepared the correct InstanceId and the environment to which the instance belongs (Daily for daily environment, Pre for staging environment, Prod for production environment). Include this information in the request body to ensure that the operation is correctly performed."
  }
}