{
  "name": "DeleteDataDiagnosis",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/datadiagnoses/[DataDiagnosisId]",
  "parameters": [
    {
      "name": "DataDiagnosisId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据诊断ID。",
        "en": "The ID of the data diagnosis."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The ID of the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除数据诊断",
    "en": "Delete data diagnosis"
  },
  "descriptions": {
    "zh": "## 请求说明\n本API用于删除特定的数据诊断配置。在调用此接口时，请确保提供的`DataDiagnosisId`与`InstanceId`准确无误，以避免误删重要配置。",
    "en": "## Description\n\nEnsure you provide the correct `DataDiagnosisId` and `InstanceId` to avoid accidental deletion."
  }
}