{
  "name": "ListRecallManagementServiceVersions",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/recallmanagementservices/[RecallManagementServiceId]/versions",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "Order",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序方式。ASC-升序。DESC-降序。",
        "en": "The sort order. Valid values: `ASC` for ascending order and `DESC` for descending order."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "页码。",
        "en": "The page number."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "页大小。",
        "en": "The number of entries to return on each page."
      }
    },
    {
      "name": "RecallManagementServiceId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据表ID。",
        "en": "The ID of the Recall Management Service."
      }
    },
    {
      "name": "SortBy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序依据。GmtCreateTime-创建时间。GmtModifiedTime-更新时间。",
        "en": "The field to sort the results by. Valid values: `GmtCreateTime` for creation time and `GmtModifiedTime` for modification time."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "召回管理服务版本列表",
    "en": "List of Retrieval Management Service Versions"
  },
  "descriptions": {
    "zh": "## 请求说明\n通过此API，您可以查询特定召回管理服务的所有版本详情，包括版本ID、名称、是否生效状态以及创建和修改时间等信息。请确保提供正确的`RecallManagementServiceId`与`InstanceId`以获取准确的数据。\n\n- `PageNumber` 和 `PageSize` 参数用于分页控制，默认从第一页开始，每页显示50条记录。\n- 使用 `SortBy` 参数可按创建时间或更新时间对结果进行排序，默认按照创建时间升序排列。\n- `Order` 参数定义了排序方式，支持升序（ASC）和降序（DESC）两种模式。",
    "en": "## Description\n\nThis operation queries the details of all versions for a specific Recall Management Service, including the version ID, name, effective status, creation time, and modification time. For accurate results, provide the correct `RecallManagementServiceId` and `InstanceId`.\n\n- Use the `PageNumber` and `PageSize` parameters for pagination. By default, the query starts from the first page and returns 50 entries per page.\n\n- Use the `SortBy` parameter to sort the results by creation time or modification time. By default, the results are sorted by creation time in ascending order.\n\n- The `Order` parameter specifies the sort order. Valid values are `ASC` for ascending order and `DESC` for descending order."
  }
}