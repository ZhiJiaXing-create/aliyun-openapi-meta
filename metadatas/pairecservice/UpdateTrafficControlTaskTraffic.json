{
  "name": "UpdateTrafficControlTaskTraffic",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/trafficcontroltasks/[TrafficControlTaskId]/action/traffic",
  "parameters": [
    {
      "name": "TrafficControlTaskId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "流量调控任务ID。",
        "en": "The ID of the traffic control task."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    },
    {
      "name": "new-param-3",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "无效参数，请忽略。",
        "en": "This parameter is invalid and should be ignored."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新流量调控任务流量",
    "en": "Update Traffic for a Traffic Control Job"
  },
  "descriptions": {
    "zh": "## 请求说明\n本API允许用户更新特定流量调控任务下的流量配置，包括但不限于流量调控目标ID、记录时间、目标流量及整体流量等信息。请确保提供的`TrafficControlTaskId`有效且属于当前用户权限范围内。此外，`Traffics`数组中的每个对象都必须包含必要的字段以保证请求的成功处理。",
    "en": "## Request\n\nThis API updates the traffic configuration for a specific traffic control task. The configuration includes the traffic control target ID, record time, target traffic, and overall traffic. Ensure that the provided`TrafficControlTaskId` is valid and within your permission scope. Additionally, each object in the`Traffics` array must contain the required fields."
  }
}