{
  "name": "CreateRecallManagementServiceVersionConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/recallmanagementservices/[RecallManagementServiceId]/versions/[RecallManagementServiceVersionId]/configs",
  "parameters": [
    {
      "name": "RecallManagementServiceId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "召回管理服务ID。",
        "en": "The recall management service ID."
      }
    },
    {
      "name": "RecallManagementServiceVersionId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "召回管理服务版本ID。",
        "en": "The recall management service version ID."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建召回管理服务版本配置",
    "en": "Create Retrieval Management Service Version Configuration"
  },
  "descriptions": {
    "zh": "## 请求说明\n- 该API允许用户为特定的召回管理服务版本创建配置。\n- 配置类型可以是召回（Recall）或归并（Merge），具体取决于`ConfigType`参数。\n- `RecallConfig`和`MergeConfig`分别对应召回配置和归并配置的具体内容，根据实际需求填写相应字段。\n- 注意确保所有必填参数都已正确提供，并且遵循给定的数据格式要求。\n- 对于非必需但提供的参数，请确保其值符合业务逻辑预期。",
    "en": "## Request\n\n- This API creates a configuration for a specific version of the Recall Management Service.\n\n- The `ConfigType` parameter specifies the configuration type, which can be either recall or merge.\n\n- Use the `RecallConfig` and `MergeConfig` parameters to provide the recall and merge configurations, respectively.\n\n- Required parameters must be provided in the specified data formats.\n\n- Optional parameter values must be consistent with your business logic."
  }
}