{
  "name": "DeployTrafficControlTaskCode",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/trafficcontroltasks/[TrafficControlTaskId]/action/deploycode",
  "parameters": [
    {
      "name": "TrafficControlTaskId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "流量调控任务ID。",
        "en": "The traffic control task ID."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "部署流量调控任务代码",
    "en": "Deploy traffic control job code"
  },
  "descriptions": {
    "zh": "## 请求说明\n- 该API允许用户为特定的流量调控任务部署Flink代码。\n- `TrafficControlTaskId` 是路径参数，必须提供有效的任务ID。\n- `InstanceId` 和 `Environment` 是必需的请求体参数，分别指定了实例ID和环境（日常/预发/生产）。\n- 可选参数 `RetryDeploy` 允许设置是否在部署失败时自动重试，默认值为 `false`。\n- 确保提供的`Environment`值符合预期选项之一：Daily、Pre或Prod。",
    "en": "## Overview\n\n- This API deploys Flink code for a specific traffic control task.\n\n- `TrafficControlTaskId` is a path parameter and requires a valid task ID.\n\n- `InstanceId` and `Environment` are required request body parameters that specify the instance ID and the target deployment environment.\n\n- The optional `RetryDeploy` parameter specifies whether to automatically retry the deployment on failure. The default value is `false`.\n\n- The value for `Environment` must be one of the following: Daily, Pre, or Prod."
  }
}