{
  "name": "GetDataDiagnosis",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/datadiagnoses/[DataDiagnosisId]",
  "parameters": [
    {
      "name": "DataDiagnosisId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据诊断任务配置ID。",
        "en": "The configuration ID of the data diagnosis task."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取数据诊断详情",
    "en": "Obtain data diagnosis details"
  },
  "descriptions": {
    "zh": "## 请求说明\n- 该API用于根据提供的`DataDiagnosisId`（数据诊断任务配置ID）和`InstanceId`（实例ID）来获取特定数据诊断任务的详细信息。\n- `CycleTime`字段表示周期运行时间，如果为空，则意味着不进行周期运行。\n- 根据不同的`Type`值，`Config`字段的内容会有所不同，请参考文档中关于每种类型所需的具体配置项。\n- `GmtCreateTime`与`GmtModifiedTime`分别代表了记录创建及最后修改的时间戳。",
    "en": "## Request\n\n- This API retrieves the details of a specific data diagnosis task using the provided `DataDiagnosisId` (data diagnosis task configuration ID) and `InstanceId` (instance ID).\n\n- The `CycleTime` field specifies the time for periodic execution. If this field is empty, the task does not execute periodically.\n\n- The value of `Type` determines the content of the `Config` field. For details about the required configuration for each type, see the relevant documentation.\n\n- `GmtCreateTime` and `GmtModifiedTime` are timestamps for the record's creation and modification times, respectively."
  }
}