{
  "name": "CreateDataDiagnosisJobs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/batch/datadiagnosisjobs/create",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建数据诊断任务",
    "en": "Create a Data Diagnosis Job"
  },
  "descriptions": {
    "zh": "## 请求说明\n通过调用此API，您可以为特定实例在给定的时间范围内启动一个新的数据诊断任务。请确保提供的`DataDiagnosisId`, `InstanceId`, `StartDate`和`EndDate`参数值准确无误，以保证任务能够正确执行。",
    "en": "## Description\n\nThis operation creates a data diagnosis (rerun) job for a specific instance within a specified time frame. To ensure the job runs correctly, provide accurate values for the `DataDiagnosisId`, `InstanceId`, `StartDate`, and `EndDate` parameters."
  }
}