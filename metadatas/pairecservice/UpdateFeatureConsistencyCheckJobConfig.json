{
  "name": "UpdateFeatureConsistencyCheckJobConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/api/v1/featureconsistencycheck/jobconfigs/[FeatureConsistencyCheckJobConfigId]",
  "parameters": [
    {
      "name": "FeatureConsistencyCheckJobConfigId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "特征一致性检查配置ID。如何获取配置ID，请参见[ListFeatureConsistencyCheckJobConfigs](~~2557567~~)。",
        "en": "The feature consistency check configuration ID. For information about how to obtain the configuration ID, see [ListFeatureConsistencyCheckJobConfigs](~~2557567~~)."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新特征一致性检查任务配置",
    "en": "Update feature consistency check task configuration"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n本API允许用户更新一个已存在的特征一致性检查任务的配置信息。通过提供新的配置参数，可以修改包括实例ID、名称、场景ID等在内的多个属性。请确保在请求中包含所有必选参数，并根据需要选择性地提供可选参数。\n\n- **FeatureConsistencyCheckJobConfigId** 是路径参数，用于指定要更新的具体特征一致性检查任务。\n- 所有其他参数均位于请求体中，其中部分为必选参数（如InstanceId, Name等），其余为可选参数。\n- 特别注意`SampleRate`应为0到1之间的浮点数，表示采样比例。\n- 如果使用了`FeatureStore`相关功能，请确保正确设置`IsUseFeatureStore`标志以及相关的`FeatureStore*`字段。\n- 对于涉及网络配置的部分（如VpcId, SwitchId等），请确保提供的值与您的阿里云环境相匹配。",
    "en": "## Operation description\n\nThis API operation allows you to update the configuration of an existing feature consistency check task. By providing new configuration parameters, you can modify multiple properties including the instance ID, name, and scene ID. Ensure that all required parameters are included in the request, and provide optional parameters as needed.\n\n- **FeatureConsistencyCheckJobConfigId** is a path parameter that specifies the feature consistency check task to update.\n- All other parameters are in the request body. Some are required (such as InstanceId and Name), and the rest are optional.\n- The SampleRate value must be a floating-point number between 0 and 1, which indicates the sampling ratio.\n- If you use FeatureStore-related features, make sure that you correctly set the IsUseFeatureStore flag and the related FeatureStore* fields.\n- For network configuration parameters (such as VpcId and SwitchId), make sure that the values match your Alibaba Cloud environment."
  }
}