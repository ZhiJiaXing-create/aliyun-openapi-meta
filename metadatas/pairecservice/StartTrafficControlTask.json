{
  "name": "StartTrafficControlTask",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/trafficcontroltasks/[TrafficControlTaskId]/action/start",
  "parameters": [
    {
      "name": "TrafficControlTaskId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "流量调控 ID",
        "en": "The traffic control task ID."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "开启流量调控任务",
    "en": "Enable traffic control job"
  },
  "descriptions": {
    "zh": "## 请求说明\n- 该API用于开启特定`TrafficControlTaskId`标识的流量调控任务。\n- `InstanceId`指定了要操作的具体实例。\n- `Environment`参数定义了执行此操作的目标环境（日常、预发或生产）。\n- 确保在调用前已正确设置所有必需参数，特别是`TrafficControlTaskId`必须存在于系统中。",
    "en": "## Request details\n\n- This operation starts the traffic control task identified by `TrafficControlTaskId`.\n\n- `InstanceId` specifies the target instance.\n\n- `Environment` specifies the target environment. Valid values: Daily, Pre, and Prod.\n\n- Ensure that all required parameters are set correctly before you call this operation. The specified `TrafficControlTaskId` must exist in the system."
  }
}