{
  "name": "GetTrafficControlTarget",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/trafficcontroltargets/[TrafficControlTargetId]",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "TrafficControlTargetId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "流量调控目标ID。",
        "en": "The ID of the traffic control target."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取流量调控目标详情",
    "en": "Retrieve traffic regulation target details"
  },
  "descriptions": {
    "zh": "## 请求说明\n通过提供流量调控目标ID和实例ID，可以获取到该流量调控目标的详细配置信息，包括但不限于调控目标名称、时间范围、条件设置等。请注意，`TrafficControlTargetId` 和 `InstanceId` 是必需参数。",
    "en": "## Request\n\nRetrieves the detailed configuration of a traffic control target. The configuration includes the target name, time range, and condition settings. This operation requires the `TrafficControlTargetId` and `InstanceId` parameters."
  }
}