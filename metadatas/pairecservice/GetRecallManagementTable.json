{
  "name": "GetRecallManagementTable",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/recallmanagementtables/[RecallManagementTableId]",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "RecallManagementTableId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据表ID。",
        "en": "The ID of the recall management table."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取召回管理数据表详情",
    "en": "Get Recall Management Data Table Detail"
  },
  "descriptions": {
    "zh": "## 请求说明\n本接口用于根据给定的数据表ID（`RecallManagementTableId`）和实例ID（`InstanceId`），获取召回管理表的详细信息。请确保在请求时正确提供这两个参数值。\n\n- **注意**：`CanDelete` 字段表示该表是否支持删除操作，请谨慎处理相关逻辑。\n- `Fields` 列表中包含了表内字段的具体定义，如名称、类型及属性等重要信息。\n- 表的波动阈值设置（行数与大小）可通过相应字段进行配置或查询，以监控数据变化情况。",
    "en": "## Request\n\nThis API retrieves details of a specific recall management table using the provided `RecallManagementTableId` and `InstanceId`. Ensure you provide the correct values for these parameters.\n\n- **Note**: The `CanDelete` field indicates whether the data table supports delete operations. Use this value to control delete functionality in your application.\n\n- The `Fields` list contains the definitions for each field in the data table, including their name, type, and properties.\n\n- To monitor data changes, you can configure or query the fluctuation thresholds for row count and size using the corresponding fields."
  }
}