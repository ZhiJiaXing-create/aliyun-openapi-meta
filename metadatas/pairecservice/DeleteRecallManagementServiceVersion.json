{
  "name": "DeleteRecallManagementServiceVersion",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/recallmanagementservices/[RecallManagementServiceId]/versions/[RecallManagementServiceVersionId]",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "RecallManagementServiceId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "召回管理服务ID。",
        "en": "The ID of the recall management service."
      }
    },
    {
      "name": "RecallManagementServiceVersionId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "召回管理服务版本ID。",
        "en": "The ID of the recall management service version."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除召回管理服务版本",
    "en": "DeleteRecallManagementServiceVersion"
  },
  "descriptions": {
    "zh": "## 请求说明\n该API允许用户通过提供召回管理服务ID、召回管理服务版本ID以及实例ID来删除特定版本的召回管理服务。请确保在调用此接口前已经备份了重要数据，因为删除操作不可逆。",
    "en": "## Request\n\nThis operation deletes a specific version of a recall management service. You must provide the recall management service ID, the recall management service version ID, and the instance ID. This operation is irreversible, so back up all critical data before proceeding."
  }
}