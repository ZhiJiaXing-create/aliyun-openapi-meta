{
  "name": "ReleaseTrafficControlTask",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/trafficcontroltasks/[TrafficControlTaskId]/action/release",
  "parameters": [
    {
      "name": "TrafficControlTaskId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "流量调控 ID",
        "en": "The traffic control task ID."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "发布流量调控任务",
    "en": "Publish traffic control job"
  },
  "descriptions": {
    "zh": "## 请求说明\n通过此API，您可以针对特定的实例ID及环境（日常、预发或生产）发布流量调控任务。请确保在请求中正确填写`TrafficControlTaskId`、`InstanceId`以及`Environment`参数值。\n\n- `TrafficControlTaskId`：需要发布的流量调控任务的唯一标识符。\n- `InstanceId`：目标实例的ID，该实例将应用此次流量调控设置。\n- `Environment`：指定执行流量调控任务的环境类型，支持三种选项之一：Daily（日常）、Pre（预发）、Prod（生产）。根据实际需求选择合适的环境进行操作。\n\n注意：只有当所有必填字段均被正确提供时，请求才会成功处理并返回相应的RequestId作为响应的一部分。",
    "en": "## Request\n\nUse this API to release a traffic control task for a specific instance and environment (Daily, Pre, or Prod). Your request must include the `TrafficControlTaskId`, `InstanceId`, and `Environment` parameters.\n\n- `TrafficControlTaskId`: The unique ID of the traffic control task.\n\n- `InstanceId`: The ID of the target instance.\n\n- `Environment`: The execution environment for the traffic control task. Valid values: `Daily`, `Pre`, and `Prod`.\n\nThe request succeeds only if all required parameters are provided correctly. A successful response includes a `RequestId`."
  }
}