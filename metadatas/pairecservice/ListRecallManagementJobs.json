{
  "name": "ListRecallManagementJobs",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/recallmanagementjobs",
  "parameters": [
    {
      "name": "Condition",
      "position": "Query",
      "type": "Struct",
      "required": false,
      "description": {
        "zh": "**过滤条件**，目前仅有Table类型的过滤条件，如：`{\"RecallManagementTableId\":\"1\"}`",
        "en": "The filter condition. Filtering is supported only for the `Table` type. For example: `{\"RecallManagementTableId\":\"1\"}`"
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "**实例 ID。**",
        "en": "The instance ID."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "暂不生效。",
        "en": "This parameter is not used."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "暂不生效。",
        "en": "This parameter is not used."
      }
    },
    {
      "name": "Order",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "**排序方式**。`ASC`-升序。`DESC`-降序。",
        "en": "The sort order. Valid values: `ASC` (ascending) and `DESC` (descending)."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "**页码。**",
        "en": "The page number. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页大小。",
        "en": "The page size. Default value: 10."
      }
    },
    {
      "name": "SortBy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "**排序依据**。`GmtCreateTime`-创建时间。`GmtModifiedTime`-更新时间。",
        "en": "The sorting basis. Valid values: `GmtCreateTime` (creation time) and `GmtModifiedTime` (update time)."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "**任务类型**",
        "en": "The task type."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取召回管理任务列表",
    "en": "Retrieve the recall management job list"
  },
  "descriptions": {
    "zh": "## 请求说明\n- 本接口用于获取召回管理任务的列表信息。\n- `InstanceId` 和 `Type` 是必填参数，其他为可选参数。\n- `Condition` 参数支持对特定表类型的过滤条件进行设置，例如通过 `RecallManagementTableId` 进行筛选。\n- 可以使用 `SortBy` 和 `Order` 参数来控制返回结果的排序方式，默认按照创建时间升序排列。\n- 分页功能通过 `PageNumber` 和 `PageSize` 参数实现，其中 `PageNumber` 默认值为1，`PageSize` 默认值为10。\n- 返回的数据中包含每个召回管理任务的基本信息及其状态等详细内容。",
    "en": "## Request\n\n- Use this API operation to retrieve a list of recall management tasks.\n\n- The `InstanceId` and `Type` parameters are required. All other parameters are optional.\n\n- Use the `Condition` parameter to set filter conditions on specific table types, such as filtering by `RecallManagementTableId`.\n\n- Use the `SortBy` and `Order` parameters to control the sort order of the results. The default sort order is ascending by creation time.\n\n- Use the `PageNumber` and `PageSize` parameters for pagination. The `PageNumber` parameter defaults to 1, and the `PageSize` parameter defaults to 10.\n\n- The response includes details about each recall management task, such as its basic information and status."
  }
}