{
  "name": "GetExperimentGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/experimentgroups/[ExperimentGroupId]",
  "parameters": [
    {
      "name": "ExperimentGroupId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实验组ID，可通过接口ListExperimentGroups获取。",
        "en": "The ID of the experiment group. You can call the `ListExperimentGroups` operation to obtain this ID."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID，可通过接口ListInstances获取。",
        "en": "The ID of the instance. You can call the `ListInstances` operation to obtain this ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取实验组详细信息",
    "en": "Obtain experiment group details"
  }
}