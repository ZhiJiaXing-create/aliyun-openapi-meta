{
  "name": "UpdateTrafficControlTask",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/api/v1/trafficcontroltasks/[TrafficControlTaskId]",
  "parameters": [
    {
      "name": "TrafficControlTaskId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "流量调控任务ID。",
        "en": "The ID of the traffic control task to update."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新流量调控任务",
    "en": "Update Traffic Control Job"
  },
  "descriptions": {
    "zh": "## 请求说明\n- 本API用于更新已存在的流量调控任务。\n- 当`ExecutionTime`设置为`TimeRange`时，必须同时提供`StartTime`和`EndTime`。\n- `TrafficControlTargets`数组中每个元素代表一个具体的流量调控目标，需要确保每个目标的时间范围、条件类型等信息完整且合理。\n- 如果设置了`UserConditionType`或`ItemConditionType`为`Expression`，则相应的表达式字段（如`UserConditionExpress`）不能为空。\n- `ServiceIds`与`EffectiveSceneIds`均为可选参数，但若使用，则需保证提供的ID列表格式正确。\n- 特别注意：在更新过程中，请确保所有必填项均已被正确填写，否则可能导致请求失败。",
    "en": "## Usage notes\n\n- Use this API to update an existing traffic control task.\n\n- When `ExecutionTime` is set to `TimeRange`, you must also provide `StartTime` and `EndTime`.\n\n- Each element in the `TrafficControlTargets` array is a traffic control target. Ensure each target's time range, condition type, and other information are complete and valid.\n\n- If you set `UserConditionType` or `ItemConditionType` to `Expression`, you must specify the corresponding expression field (for example, `UserConditionExpress`).\n\n- `ServiceIds` and `EffectiveSceneIds` are optional parameters. If you include them, ensure the ID lists are correctly formatted.\n\n- Ensure you complete all required fields to avoid a failed request."
  }
}