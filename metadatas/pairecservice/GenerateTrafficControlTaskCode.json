{
  "name": "GenerateTrafficControlTaskCode",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/trafficcontroltasks/[TrafficControlTaskId]/action/generatecode",
  "parameters": [
    {
      "name": "TrafficControlTaskId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "流量调控任务ID。",
        "en": "The traffic control task ID."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "生成流量调控任务代码",
    "en": "Generate Traffic Control Job Code"
  },
  "descriptions": {
    "zh": "## 请求说明\n- 该API用于基于指定的流量调控任务ID、实例ID及环境类型来生成对应的Flink代码。\n- `Environment`参数支持三种值：`Daily`（日常环境）、`Pre`（预发环境）和`Prod`（生产环境），请确保正确选择以匹配您的需求。\n- 注意检查返回结果中的`PreNeedConfig`字段，如果为`True`则表示在预发环境中可能缺少必要的配置信息，请根据实际情况调整或补充相关设置。",
    "en": "## Description\n\n- This API generates Flink code for a specified traffic control task ID, instance ID, and environment type.\n\n- The `Environment` parameter accepts three values: `Daily` for the daily environment, `Pre` for the pre-release environment, and `Prod` for the production environment.\n\n- Check the `PreNeedConfig` field in the response. A `true` value indicates that necessary configuration information might be missing in the pre-release environment. If this occurs, add or adjust the required settings."
  }
}