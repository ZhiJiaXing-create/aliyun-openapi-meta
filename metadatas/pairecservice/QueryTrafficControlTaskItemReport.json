{
  "name": "QueryTrafficControlTaskItemReport",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/trafficcontroltasks/[TrafficControlTaskId]/action/queryitemreport",
  "parameters": [
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "结束时间",
        "en": "The end time."
      }
    },
    {
      "name": "Environment",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "所属的环境。Daily-日常环境 Pre-预发环境 Prod-生产环境",
        "en": "The execution environment for the task. Valid values: Daily (development environment), Pre (staging environment), Prod (production environment)."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "开始时间",
        "en": "The start time."
      }
    },
    {
      "name": "TrafficControlTaskId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "调控任务ID。",
        "en": "The ID of the traffic control task."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询流量调控任务单品报表",
    "en": "Query item-level report for traffic control job"
  },
  "descriptions": {
    "zh": "## 请求说明\n- 本API用于查询特定流量调控任务在给定时间范围内的单品调控效果。\n- `TrafficControlTaskId` 是唯一标识一个流量调控任务的ID。\n- `InstanceId` 指定了执行该任务的具体实例。\n- `Environment` 参数定义了任务运行的环境，支持三种选项：Daily（日常环境）、Pre（预发环境）和Prod（生产环境）。\n- `StartTime` 和 `EndTime` 分别代表查询报告的时间段起始点与结束点，格式为\"YYYY-MM-DD HH:MM:SS\"。\n- 确保提供的开始时间和结束时间是合理的，并且属于同一个自然日或连续的自然日内。",
    "en": "## Description\n\n- This API retrieves the item control results for a specific traffic control task within a given time range.\n\n- `TrafficControlTaskId` is the unique identifier for a traffic control task.\n\n- `InstanceId` specifies the instance that runs the task.\n\n- The `Environment` parameter specifies the task's execution environment. Valid values are Daily (development environment), Pre (staging environment), and Prod (production environment).\n\n- `StartTime` and `EndTime` specify the start and end of the time range for the report, respectively. The format is \"YYYY-MM-DD HH:MM:SS\".\n\n- The specified start and end times must be valid and span no more than two consecutive calendar days."
  }
}