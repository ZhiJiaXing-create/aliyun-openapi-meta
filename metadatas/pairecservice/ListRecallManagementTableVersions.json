{
  "name": "ListRecallManagementTableVersions",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/recallmanagementtables/[RecallManagementTableId]/versions",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The ID of the instance."
      }
    },
    {
      "name": "Order",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序方式。`ASC`-升序。`DESC`-降序。",
        "en": "The sort order. Valid values: `ASC` (ascending order) and `DESC` (descending order)."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码。",
        "en": "The page number."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页大小。",
        "en": "The page size."
      }
    },
    {
      "name": "RecallManagementTableId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "表ID。",
        "en": "The ID of the RecallManagementTable."
      }
    },
    {
      "name": "SortBy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序依据。`GmtCreateTime`-创建时间。`GmtModifiedTime`-更新时间。",
        "en": "The field to sort by. Valid values: `GmtCreateTime` (creation time) and `GmtModifiedTime` (update time)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取召回管理表版本列表",
    "en": "ListRecallManagementTableVersions"
  },
  "descriptions": {
    "zh": "## 请求说明\n- 通过提供`RecallManagementTableId`和`InstanceId`，可以获取特定召回管理表的版本历史。\n- 使用`SortBy`参数可以根据创建时间或更新时间对结果进行排序，默认按照创建时间升序排列。\n- `PageNumber`与`PageSize`参数支持分页查询功能，允许用户控制返回数据的数量及查看的具体页码。\n- 如果不指定`Order`参数，则默认按升序排列结果。",
    "en": "## Usage\n\n- To retrieve the version history of a specific RecallManagementTable, provide the `RecallManagementTableId` and `InstanceId`.\n\n- Use the `SortBy` parameter to sort the results by creation time or update time. By default, the results are sorted by creation time in ascending order.\n\n- The `PageNumber` and `PageSize` parameters enable pagination, which allows you to control the number of items to return and the page to display.\n\n- If the `Order` parameter is not specified, the results are sorted in ascending order by default."
  }
}