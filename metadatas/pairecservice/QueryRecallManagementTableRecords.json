{
  "name": "QueryRecallManagementTableRecords",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/recallmanagementtables/[RecallManagementTableId]/queryrecords",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "PrimaryKeys",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询主键",
        "en": "The primary keys of the records to retrieve."
      }
    },
    {
      "name": "RecallManagementTableId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "表ID。",
        "en": "The recall management table ID."
      }
    },
    {
      "name": "RecallManagementTableVersionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "召回管理表的版本ID。默认为当前发布版本",
        "en": "The recall management table version ID. If you omit this parameter, the API uses the currently published version."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询召回管理表数据",
    "en": "QueryRecallManagementTableRecords"
  },
  "descriptions": {
    "zh": "## 请求说明\n本API用于根据给定的主键从特定的召回管理表中检索数据记录。请确保提供的`InstanceId`和`RecallManagementTableId`是有效的，并且`PrimaryKeys`列表不为空。如果指定了`RecallManagementTableVersionId`，则返回该版本的数据；否则，默认使用当前发布的版本。",
    "en": "## Request\n\nThis API retrieves records from a specific recall management table using the provided primary keys. You must provide a valid `InstanceId` and `RecallManagementTableId`, and a non-empty `PrimaryKeys` list. If you specify `RecallManagementTableVersionId`, the API returns records from that version; otherwise, it uses the currently published version."
  }
}