{
  "name": "CreateTrafficControlTarget",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/trafficcontroltargets",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建流量调控目标",
    "en": "Create Traffic Control Target"
  },
  "descriptions": {
    "zh": "## 请求说明\n- `ItemConditionType` 支持两种格式：数组格式（Array）和表达式格式（Expression）。根据选择的不同，需要提供相应的`ItemConditionArray`或`ItemConditionExpress`。\n- `StatisPeriod` 默认为按天统计，如果需要按小时统计，请明确指定。\n- `ToleranceValue` 和 `NewProductRegulation` 是可选参数，仅在有特定需求时填写。\n- `Status` 字段允许用户控制新创建的流量调控目标是否立即生效，默认情况下可能为关闭状态。",
    "en": "## Usage notes\n\n- The `ItemConditionType` parameter supports two formats: array format (Array) and expression format (Expression). Based on the format you select, you must provide either `ItemConditionArray` or `ItemConditionExpress`.\n\n- The `StatisPeriod` parameter defaults to daily. For hourly statistics, you must set this parameter explicitly.\n\n- The `ToleranceValue` and `NewProductRegulation` parameters are optional.\n\n- The `Status` parameter controls whether a new traffic control target takes effect immediately. By default, new targets are inactive."
  }
}