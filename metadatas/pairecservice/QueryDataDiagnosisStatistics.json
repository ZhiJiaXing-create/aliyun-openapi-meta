{
  "name": "QueryDataDiagnosisStatistics",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/datadiagnoses/[DataDiagnosisId]/statistics/action/query",
  "parameters": [
    {
      "name": "DataDiagnosisId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据诊断ID。",
        "en": "The ID of the data diagnosis task."
      }
    },
    {
      "name": "EndDate",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "结束日期。",
        "en": "The end date."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "RemainRateType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "留存率报表类型。\n\n- Period-周期报表。",
        "en": "The retention rate report type.\n\n- Period: A periodic report."
      }
    },
    {
      "name": "StartDate",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "开始日期。",
        "en": "The start date."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "数据诊断统计查询",
    "en": "Data Diagnosis Statistics Query"
  },
  "descriptions": {
    "zh": "## 请求说明\n- `DataDiagnosisId` 是必填项，用于指定要查询的数据诊断任务。\n- `InstanceId` 也是必填参数，用来标识具体的实例。\n- `StartDate` 和 `EndDate` 分别表示统计的起始和结束日期，格式为 YYYY-MM-DD。\n- `RemainRateType` 参数是可选的，如果提供，则可以指定留存率报表类型，默认值为 'Period' 表示周期报表。\n- 返回结果中包含请求ID (`RequestId`) 以及统计数据对象 (`Statistics`)，其中列出了任务失败的日期列表 (`FailedDates`) 和缺失任务的日期列表 (`NoDataDates`)。",
    "en": "## Request description\n\n- The `DataDiagnosisId` parameter is required and specifies the data diagnosis task.\n\n- The `InstanceId` parameter is also required and specifies the instance.\n\n- The `StartDate` and `EndDate` parameters specify the start and end dates of the time range. The format is YYYY-MM-DD.\n\n- The `RemainRateType` parameter is optional. It specifies the retention rate report type. The default value is 'Period', which indicates a periodic report.\n\n- The response includes the request ID (`RequestId`) and a `Statistics` object. This object contains the dates of task failures (`FailedDates`) and dates with missing task data (`NoDataDates`)."
  }
}