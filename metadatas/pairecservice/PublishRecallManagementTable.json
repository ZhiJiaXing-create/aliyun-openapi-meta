{
  "name": "PublishRecallManagementTable",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/api/v1/recallmanagementtables/[RecallManagementTableId]/action/publish",
  "parameters": [
    {
      "name": "RecallManagementTableId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "表Id",
        "en": "The table ID."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "同步召回管理表",
    "en": "Synchronize Recall Management Table"
  },
  "descriptions": {
    "zh": "## 请求说明\n该API用于将指定的MaxCompute表同步至召回引擎，并允许用户指定发布的表分区、是否跳过阈值检查以及同步模式（聚合或覆盖）。请确保在请求体中正确填写实例ID及表ID，在路径参数中提供正确的`RecallManagementTableId`。如果需要指定特定的表分区进行发布，请通过`Partitions`字段以键值对形式给出。此外，还可以选择是否跳过数据量阈值检查以及设置同步模式。",
    "en": "## Request details\n\nThis API synchronizes a specified MaxCompute table with the recall engine. You must provide the correct `RecallManagementTableId` in the path parameter and the instance ID in the request body. You can also specify the table partitions to publish, whether to skip the threshold check, and the synchronization mode. To publish specific partitions, provide them as key-value pairs in the `Partitions` field."
  }
}