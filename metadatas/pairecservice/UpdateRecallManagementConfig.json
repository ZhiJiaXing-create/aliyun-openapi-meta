{
  "name": "UpdateRecallManagementConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "PUT",
  "pathPattern": "/api/v1/recallmanagementconfigs",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新召回管理配置",
    "en": "Update Retrieval Management Configuration"
  },
  "descriptions": {
    "zh": "## 请求说明\n- `InstanceId` 是必填项，用于指定要更新配置的实例。\n- `Password` 和 `NetworkConfigs` 为可选项，根据实际需求选择性提供。\n- 在 `NetworkConfigs` 中，可以进一步指定 VPC ID (`VpcId`) 和可用区与交换机ID映射(`VswitchIds`)来配置网络设置。\n- 注意：敏感信息如密码在传输过程中应确保安全。",
    "en": "## Request\n\n- `InstanceId` is required. It specifies the instance to update.\n\n- `Password` and `NetworkConfigs` are optional.\n\n- Use `NetworkConfigs` to define the network by specifying the Virtual Private Cloud (VPC) ID (`VpcId`) and mapping availability zones to VSwitch IDs (`VswitchIds`).\n\n- Note: Ensure that sensitive information, such as the password, is transmitted securely."
  }
}