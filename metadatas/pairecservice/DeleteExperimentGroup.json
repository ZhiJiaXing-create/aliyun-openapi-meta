{
  "name": "DeleteExperimentGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/experimentgroups/[ExperimentGroupId]",
  "parameters": [
    {
      "name": "ExperimentGroupId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实验组ID，可通过接口ListExperimentGroups获取。",
        "en": "Experiment group ID, which can be obtained by calling the ListExperimentGroups API."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID，可通过接口ListInstances获取。",
        "en": "Instance ID, which can be obtained by calling the ListInstances API."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除实验组",
    "en": "Delete experiment group"
  }
}