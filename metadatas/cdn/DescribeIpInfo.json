{
  "name": "DescribeIpInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IP",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定IP地址，不支持批量。",
        "en": "The IP address to query. Batch queries are not supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询IP是否为CDN节点",
    "en": "Query whether an IP address belongs to a CDN node"
  },
  "descriptions": {
    "zh": "> 单用户调用频率：50次/秒。",
    "en": "> Single-user call frequency: 50 calls per second."
  },
  "example": {
    "unifiedCli": "aliyun cdn describe-ip-info --ip 192.168.0.1 --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeIpInfo --region cn-hangzhou --IP '192.168.0.1'"
  }
}