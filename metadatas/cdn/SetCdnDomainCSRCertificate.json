{
  "name": "SetCdnDomainCSRCertificate",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要设置的加速域名，需属于HTTPS加速类型。",
        "en": "The accelerated domain name for which you want to configure an SSL certificate. The domain name must have HTTPS secure acceleration enabled."
      }
    },
    {
      "name": "ServerCertificate",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "证书内容。该证书必须是通过[CreateCdnCertificateSigningRequest](~~144478~~)接口创建的CSR对应的签名证书，内部必须是PEM格式的证书，Base64编码后再通过encodeURIComponent。",
        "en": "The content of the certificate. The certificate must match the certificate signing request (CSR) created by calling the [CreateCdnCertificateSigningRequest](~~144478~~) operation. Make sure that the content of the certificate is encoded in Base64 and then encoded by encodeURIComponent."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置CSR证书",
    "en": "SetCdnDomainCSRCertificate"
  },
  "descriptions": {
    "zh": "> 单用户调用频率：100次/秒。",
    "en": "> Each account can call this operation up to 100 times per second."
  },
  "example": {
    "unifiedCli": "aliyun cdn set-cdn-domain-csr-certificate --server-certificate ----BEGIN_CERTIFICATE-----_MIIFz0000-----END_CERTIFICATE----- --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn SetCdnDomainCSRCertificate --region cn-hangzhou --ServerCertificate '----BEGIN_CERTIFICATE-----_MIIFz0000-----END_CERTIFICATE-----' --DomainName 'example.com'"
  }
}