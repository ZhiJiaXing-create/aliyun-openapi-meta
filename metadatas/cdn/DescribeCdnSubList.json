{
  "name": "DescribeCdnSubList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询报表定制任务",
    "en": "DescribeCdnSubList"
  },
  "descriptions": {
    "zh": "- 调用该接口默认返回所有已定制的报表，但目前只支持展示单个定制任务，因此调用结果只会返回一个定制报表任务。\n- 单用户调用频率：3次/秒。",
    "en": "- By default, this operation queries all customized report tasks. However, only one task is returned in the response.\n\n- The call frequency is limited to 3 calls per second for each user."
  },
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-sub-list --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnSubList --region cn-hangzhou"
  }
}