{
  "name": "StopCdnDomain",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加速域名，仅支持停用单个。",
        "en": "The accelerated domain name that you want to disable. You can specify only one domain name in each request."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "停用域名",
    "en": "StopCdnDomain"
  },
  "descriptions": {
    "zh": "- 停用加速域名后，加速域名的配置信息仍然会保留，但是阿里云CDN将停止为该域名提供加速服务，且该域名会被解析到无效地址“offline.***.com”，无法被继续访问。\n- 单用户调用频率：40次/秒。",
    "en": "- After an accelerated domain is disabled, Alibaba Cloud CDN retains its information and routes all the requests that are destined for the accelerated domain to the origin server.\n\n- Each account can call this operation up to 40 times per second."
  },
  "example": {
    "unifiedCli": "aliyun cdn stop-cdn-domain --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn StopCdnDomain --region cn-hangzhou --DomainName 'example.com'"
  }
}