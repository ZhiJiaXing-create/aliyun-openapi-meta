{
  "name": "SetCdnDomainSMCertificate",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CertIdentifier",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "国密证书ID。\n证书ID+\"-cn-hangzhou\"。\n如果证书ID=123，CertIdentifier=\"123-cn-hangzhou\"",
        "en": "The ID of the SM certificate that you want to configure. The identifier of the certificate. The value is Certificate ID-cn-hangzhou. For example, if the certificate ID is 123, set the value of this parameter to 123-cn-hangzhou."
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "证书所属的加速域名。\n\n> 证书需属于HTTPS加速类型。",
        "en": "The accelerated domain name for which you want to configure the SM certificate.\n\n> The domain name must use HTTPS acceleration."
      }
    },
    {
      "name": "SSLProtocol",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "HTTPS证书是否启用。取值：\n\n- **on**：启用。\n- **off**：不启用。",
        "en": "Specifies whether to enable the SSL certificate. Valid values:\n\n- **on**\n\n- **off**"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置国密证书",
    "en": "SetCdnDomainSMCertificate"
  },
  "descriptions": {
    "zh": "> 单用户调用频率：30次/秒。",
    "en": "> Each account can call this operation up to 30 times per second."
  },
  "example": {
    "unifiedCli": "aliyun cdn set-cdn-domain-sm-certificate --domain-name example.com --cert-identifier 12340000-cn-hangzhou --ssl-protocol on --region cn-hangzhou",
    "legacyCli": "aliyun cdn SetCdnDomainSMCertificate --region cn-hangzhou --DomainName 'example.com' --CertIdentifier '12340000-cn-hangzhou' --SSLProtocol on"
  }
}