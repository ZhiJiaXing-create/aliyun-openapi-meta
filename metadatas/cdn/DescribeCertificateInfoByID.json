{
  "name": "DescribeCertificateInfoByID",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CertId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "证书ID，仅支持查询单个。",
        "en": "The ID of the certificate. You can query only one certificate in each call."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询证书信息-按ID",
    "en": "DescribeCertificateInfoByID"
  },
  "descriptions": {
    "zh": "- 单用户调用频率：100次/秒。\n- 域名绑定证书后，但证书未启用时，通过该接口查询会提示证书不存在。",
    "en": "- Each account can call this operation up to 100 times per second.\n\n- If a certificate is associated with a domain name but the certificate is not enabled, the result of this operation shows that the certificate does not exist."
  },
  "example": {
    "unifiedCli": "aliyun cdn describe-certificate-info-by-id --cert-id 1644xx --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCertificateInfoByID --region cn-hangzhou --CertId 1644xx"
  }
}