{
  "name": "SetWaitingRoomConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllowPct",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "允许回源比例，取值范围：**0**~**100**。",
        "en": "The percentage of requests that are allowed to be redirected to the origin server. Valid values: **0** to **100**."
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "您的加速域名，仅支持单个设置。",
        "en": "The accelerated domain name. You can specify only one domain name."
      }
    },
    {
      "name": "GapTime",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "离开排队后，跳过排队时长，单位：秒。",
        "en": "The length of waiting time to skip after an exit from the queue. Unit: seconds."
      }
    },
    {
      "name": "MaxTimeWait",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "进入排队后，排队时长，单位：秒。",
        "en": "The maximum length of waiting time in the queue. Unit: seconds."
      }
    },
    {
      "name": "WaitUri",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "开启waiting_room功能的URI正则字符串。",
        "en": "The regular expression that is used to match URI strings for which the virtual waiting room feature is enabled."
      }
    },
    {
      "name": "WaitUrl",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "排队等待页面URL。",
        "en": "The URL of the waiting page."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "配置WaitingRoom",
    "en": "SetWaitingRoomConfig"
  },
  "descriptions": {
    "zh": "> 单用户调用频率：30次/秒。",
    "en": "> Each account can call this operation up to 30 times per second."
  },
  "example": {
    "unifiedCli": "aliyun cdn set-waiting-room-config --domain-name example.com --wait-uri .0/aaa/bbb --allow-pct 30 --max-time-wait 30 --gap-time 20 --wait-url https://example.com/waitingroom.html --region cn-hangzhou",
    "legacyCli": "aliyun cdn SetWaitingRoomConfig --region cn-hangzhou --GapTime 20 --AllowPct 30 --DomainName 'example.com' --WaitUri '.0/aaa/bbb' --WaitUrl 'https://example.com/waitingroom.html' --MaxTimeWait 30"
  }
}