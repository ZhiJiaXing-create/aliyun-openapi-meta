{
  "name": "DescribeDomainsBySource",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Sources",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "源站，多个用半角逗号（,）分隔，不支持模糊匹配。",
        "en": "The origin servers. Separate multiple origin servers with commas (,). Fuzzy match is not supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询域名-按源站",
    "en": "DescribeDomainsBySource"
  },
  "descriptions": {
    "zh": "> 单用户调用频率：30次/秒。",
    "en": "> Each account can call this operation up to 30 times per second."
  },
  "example": {
    "unifiedCli": "aliyun cdn describe-domains-by-source --sources example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeDomainsBySource --region cn-hangzhou --Sources 'example.com'"
  }
}