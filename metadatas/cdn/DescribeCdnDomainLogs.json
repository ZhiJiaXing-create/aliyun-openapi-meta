{
  "name": "DescribeCdnDomainLogs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "域名，只支持单个查询。",
        "en": "The domain name. You can specify only one domain name."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取日志结束时间。\n\n日期格式按照ISO8601表示法，并使用UTC+0时间，格式为yyyy-MM-ddTHH:mm:ssZ。\n\n> 结束时间需晚于起始时间。",
        "en": "The end of the time range to query.\n\nSpecify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC.\n\n> The end time must be later than the start time."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "取得第几页，取值范围：大于**1**的任意整数。",
        "en": "The number of the page to return. Pages start from page **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页大小，默认**300**，最大**1000**，取值：**1**~**1000**。",
        "en": "The number of entries to return on each page. Default value: **300**. Maximum value: **1000**. Valid values: **1** to **1000**."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取日志起始时间。\n\n日期格式按照ISO8601表示法，并使用UTC+0时间，格式为yyyy-MM-ddTHH:mm:ssZ。",
        "en": "The beginning of the time range to query.\n\nSpecify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询离线日志下载地址",
    "en": "DescribeCdnDomainLogs"
  },
  "descriptions": {
    "zh": "- 如果不指定**StartTime**和**EndTime**，该接口默认读取过去24小时的数据；指定**StartTime**和**EndTime**时，按指定的起止时间查询。\n- 查询数据的时间粒度为一小时。\n- 单个用户的调用频率为：100次/秒。\n- 仅支持查询最近一个月的日志记录（开始时间和当前时间跨度不大于31天）。",
    "en": "- If you do not set **StartTime** or **EndTime**, the request returns the data collected in the last 24 hours. If you set both **StartTime** and **EndTime**, the request returns the data collected within the specified time range.\n\n- The log data is collected every hour.\n\n- Each account can call this operation up to 100 times per second.\n\n- You can query only logs in the last month. The start time and the current time cannot exceed 31 days."
  },
  "example": {
    "unifiedCli": "aliyun cdn describe-cdn-domain-logs --domain-name example.com --page-size 300 --page-number 2 --start-time 2017-12-21T08:00:00Z --end-time 2017-12-22T08:00:00Z --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeCdnDomainLogs --region cn-hangzhou --PageSize 300 --EndTime '2017-12-22T08:00:00Z' --DomainName 'example.com' --PageNumber 2 --StartTime '2017-12-21T08:00:00Z'"
  }
}