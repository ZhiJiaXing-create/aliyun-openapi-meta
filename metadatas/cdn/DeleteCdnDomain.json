{
  "name": "DeleteCdnDomain",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要删除的CDN的域名，仅支持删除单个域名。",
        "en": "The accelerated domain name that you want to remove. You can specify only one domain name in each call."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除域名",
    "en": "DeleteCdnDomain"
  },
  "descriptions": {
    "zh": "- 删除域名前建议您前往域名解析服务商处恢复域名A记录，以免删除域名后导致域名不可访问。\n- DeleteCdnDomain接口调用成功后将删除本条加速域名的全部相关记录，如果您仅需要暂停该加速域名的使用，请调用StopCdnDomain接口。\n- 单用户调用频率：10次/秒。",
    "en": "- We recommend that you add an A record for the domain name in the system of your DNS service provider before you remove the domain name from Alibaba Cloud CDN. Otherwise, the domain name may become inaccessible. Proceed with caution.\n\n- After you successfully call the DeleteCdnDomain operation, all records of the removed domain name are deleted. If you need to only disable the domain name, we recommend that you call the StopCdnDomain operation.\n\n- Each account can call this operation up to 10 times per second."
  },
  "example": {
    "unifiedCli": "aliyun cdn delete-cdn-domain --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn DeleteCdnDomain --region cn-hangzhou --DomainName 'example.com'"
  }
}