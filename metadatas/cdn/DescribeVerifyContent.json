{
  "name": "DescribeVerifyContent",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "只支持单个域名。",
        "en": "The domain name of which the ownership was verified. You can specify only one domain name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询校验归属权内容",
    "en": "DescribeVerifyContent"
  },
  "descriptions": {
    "zh": "> 单用户调用频率：100次/秒。"
  },
  "example": {
    "unifiedCli": "aliyun cdn describe-verify-content --domain-name example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeVerifyContent --region cn-hangzhou --DomainName 'example.com'"
  }
}