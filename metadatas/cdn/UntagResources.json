{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "删除所有标签。取值：\n- **true**：是。\n- **false**：否。\n\n默认取值为**false**。",
        "en": "Specifies whether to remove all tags. Valid values:\n\n- **true**: yes.\n\n- **false**: no.\n\nDefault value: **false**."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID列表。列表元素数量最多50个。",
        "en": "The list of resource IDs. You can specify up to 50 resource IDs in the list."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。固定值：**DOMAIN**。",
        "en": "The type of the resources from which you want to remove tags. Set this parameter to **DOMAIN**."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签键列表。列表元素数量最大20个。",
        "en": "The list of tag keys. You can specify up to 20 tag keys in the list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除资源标签",
    "en": "UntagResources"
  },
  "descriptions": {
    "zh": "> 单用户调用频率：100次/秒。",
    "en": "> Each account can call this operation up to 100 times per second."
  },
  "example": {
    "unifiedCli": "aliyun cdn untag-resources --resource-type DOMAIN --all false --resource-id test-res.dutenews.com --tag-key env --region cn-hangzhou",
    "legacyCli": "aliyun cdn UntagResources --region cn-hangzhou --All false --ResourceId.1 'test-res.dutenews.com' --TagKey.1 env --ResourceType DOMAIN"
  }
}