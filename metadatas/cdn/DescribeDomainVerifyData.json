{
  "name": "DescribeDomainVerifyData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加速域名，仅支持查询单个域名。",
        "en": "The accelerated domain name. You can query only one domain name at a time."
      }
    },
    {
      "name": "GlobalResourcePlan",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启全球资源计划。\n支持：\n- **on**（开启）\n- **off**（不开启）",
        "en": "Specifies whether the global resource plan is enabled.\nValid values:\n\n- **on** (enabled)\n\n- **off** (disabled)"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询域名归属校验内容",
    "en": "DescribeDomainVerifyData"
  },
  "example": {
    "unifiedCli": "aliyun cdn describe-domain-verify-data --domain-name www.yourdomain.com --global-resource-plan off --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeDomainVerifyData --region cn-hangzhou --DomainName 'www.yourdomain.com' --GlobalResourcePlan off"
  }
}