{
  "name": "DescribeUserVipsByDomain",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Available",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否查询健康VIP。取值：\n\n- **on**：健康VIP。\n- **off**：全部VIP。",
        "en": "Specifies whether to query the virtual IP addresses of only healthy CDN POPs. Valid values:\n\n- **on**: healthy CDN edge nodes.\n\n- **off**: all CDN edge nodes."
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "域名，只支持单个域名。",
        "en": "The accelerated domain name. You can specify only one domain name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询节点IP列表-L1",
    "en": "DescribeUserVipsByDomain"
  },
  "descriptions": {
    "zh": "> 单用户调用频率：30次/秒。",
    "en": "> Each account can call this operation up to 30 times per second."
  },
  "example": {
    "unifiedCli": "aliyun cdn describe-user-vips-by-domain --domain-name example.com --available on --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeUserVipsByDomain --region cn-hangzhou --DomainName 'example.com' --Available on"
  }
}