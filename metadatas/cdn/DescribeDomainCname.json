{
  "name": "DescribeDomainCname",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要查询的加速域名，多个用逗号（,）分隔。参数不可为空",
        "en": "The accelerated domain names to query. To query multiple domain names, separate them with commas (,)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "检测加速域名的Cname是否完成配置",
    "en": "DescribeDomainCname"
  },
  "example": {
    "unifiedCli": "aliyun cdn describe-domain-cname --domain-name example.com,example.org --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeDomainCname --region cn-hangzhou --DomainName 'example.com,example.org'"
  }
}