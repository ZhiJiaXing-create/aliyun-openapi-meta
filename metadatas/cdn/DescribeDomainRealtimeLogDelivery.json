{
  "name": "DescribeDomainRealtimeLogDelivery",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Domain",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "开启实时日志投递服务域名，仅支持查询单个。",
        "en": "The accelerated domain name for which real-time log delivery is enabled. You can specify only one domain name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询实时日志投递信息",
    "en": "DescribeDomainRealtimeLogDelivery"
  },
  "descriptions": {
    "zh": "> 单用户调用频率：100次/秒。",
    "en": "> Each account can call this operation up to 100 times per second."
  },
  "example": {
    "unifiedCli": "aliyun cdn describe-domain-realtime-log-delivery --domain example.com --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeDomainRealtimeLogDelivery --region cn-hangzhou --Domain 'example.com'"
  }
}