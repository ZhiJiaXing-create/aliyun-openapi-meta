{
  "name": "DescribeUserConfigs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Config",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要查询的配置，支持持单个查询。当前支持oss、green_manager、waf、cc_rule、ddos_dispatch、edge_safe、blocked_regions、http_acl_policy、bot_manager和ip_reputation配置。",
        "en": "The feature whose configurations you want to query. You can specify only one feature in each request. Valid values: oss, green_manager, waf, cc_rule, ddos_dispatch, edge_safe, blocked_regions, http_acl_policy, bot_manager, and ip_reputation."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询安全功能相关的配置",
    "en": "DescribeUserConfigs"
  },
  "example": {
    "unifiedCli": "aliyun cdn describe-user-configs --config oss --region cn-hangzhou",
    "legacyCli": "aliyun cdn DescribeUserConfigs --region cn-hangzhou --Config oss"
  }
}