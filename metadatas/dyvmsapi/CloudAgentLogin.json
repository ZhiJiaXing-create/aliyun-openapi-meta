{
  "name": "CloudAgentLogin",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BindTel",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "绑定电话",
        "en": "The bound phone number."
      }
    },
    {
      "name": "BindType",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "取值 1.普通电话,2.分机,3.webrtc </br>说明：绑定类型如果是分机则必须先让分机电话设备注册成功。如果绑定类型为webrtc，就算调用接口成功也是无法呼叫的。如果在企业设置页面开启了自适应绑定电话类型，则系统会根据绑定的号码来找到对应的绑定类型，使用系统找到的绑定类型，此处的设置优先级低。",
        "en": "The binding type. Valid values: `1` (Phone), `2` (extension), and `3` (WebRTC).<br>Note: If the binding type is set to `WebRTC`, a successful API call does not enable you to make calls. If the binding type is set to `extension`, the device must be registered first. If the adaptive binding feature is enabled in your enterprise settings, the system automatically determines the binding type based on the phone number. In this case, the system-determined type overrides this parameter.<br>"
      }
    },
    {
      "name": "Cno",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "座席工号；取值3-10位正整数",
        "en": "The agent ID. The value must be a 3- to 10-digit positive integer."
      }
    },
    {
      "name": "EnterpriseId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "呼叫中心 id",
        "en": "The contact center ID."
      }
    },
    {
      "name": "LoginStatus",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "登录状态，1：置闲，2：置忙，默认为1",
        "en": "The agent's status after login. Valid values: `1` (set to idle) and `2` (set to busy). The default value is `1`."
      }
    },
    {
      "name": "PauseDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "置忙描述",
        "en": "The description for the busy state."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "座席上线",
    "en": "CloudAgentLogin"
  }
}