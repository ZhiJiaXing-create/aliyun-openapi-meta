{
  "name": "CloudGetRecordUrl",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CallType",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "呼叫类型；说明：开启分线录音时，获取客户侧或座席侧录音需要，recordFormat=1时有效，recordFormat=0时忽略。取值范围：1,2,4,5（数字含义：呼入,webcall,预览外呼,预测外呼）",
        "en": "The call type. This parameter is required to retrieve `customer-side` or `agent-side` recordings when `separate-channel recording` is enabled. It is effective only when `RecordFormat` is set to `1` (WAV) and is ignored if `RecordFormat` is set to `0` (MP3). Valid values: `1` (inbound call), `2` (Webcall), `4` (preview outbound call), and `5` (predictive outbound call)."
      }
    },
    {
      "name": "Download",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "是否下载；１为下载，空或０表示试听",
        "en": "Specifies whether to generate a download link. Set this to `1` for a download URL. If you set this to `0` or leave it empty, a preview URL is generated."
      }
    },
    {
      "name": "EnterpriseId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "呼叫中心 id",
        "en": "The ID of the call center."
      }
    },
    {
      "name": "RecordFile",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "录音文件名；如7000101-20160815140025-01087120766-01087120766--record-sip-1-1471240825.87",
        "en": "The recording file name. Example: `7000101-20160815140025-01087120766-01087120766--record-sip-1-1471240825.87`"
      }
    },
    {
      "name": "RecordFormat",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "录音文件类型；取值说明：0为mp3，1为wav，默认为mp3类型",
        "en": "The recording file type. Valid values: `0` for MP3 (default) and `1` for WAV."
      }
    },
    {
      "name": "RecordSide",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分线录音侧；说明：开启分线录音时，获取客户侧或座席侧录音需要，recordFormat=1时有效，recordFormat=0时忽略。取值范围：1,2 (数字含义：客户侧，座席侧)recordSide不为空时，callType必选",
        "en": "Specifies the recording side for a `separate-channel recording`. This parameter is required to retrieve `customer-side` or `agent-side` recordings when `separate-channel recording` is enabled. It is effective only when `RecordFormat` is set to `1` (WAV) and is ignored if `RecordFormat` is set to `0` (MP3). If you specify this parameter, `CallType` is also required. Valid values: `1` (customer side) and `2` (agent side)."
      }
    },
    {
      "name": "RecordType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "录音类型，不填可根据 recordFile 解析；record:录音voicemail:留言tsi:彩铃、当开启号码录音状态识别，发起预览外呼，客户号码是手机且客户未接听时返回该地址 rasr:语音机器人客户侧录音 transfer:转接consult:咨询threeway:三方",
        "en": "The recording type. If you do not specify this parameter, the system automatically determines the type from the `RecordFile` name. Valid values: `record` (a standard recording), `voicemail` (a voicemail), `tsi` (a color ringback tone used for a preview outbound call to a mobile number that goes unanswered when recording status detection is enabled), `rasr` (a customer-side recording for a voice bot), `transfer` (a transfer), `consult` (a consult), and `threeway` (a three-way call)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取通话录音地址",
    "en": "CloudGetRecordUrl"
  }
}