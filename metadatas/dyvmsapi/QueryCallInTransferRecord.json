{
  "name": "QueryCallInTransferRecord",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CallInCaller",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "呼入主叫。",
        "en": "The calling number of the inbound call."
      }
    },
    {
      "name": "PageNo",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "页码。默认取值为**1**。",
        "en": "The page number. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "页数。取值范围：**1~10**。",
        "en": "The number of entries per page. Valid values: 1 to 10."
      }
    },
    {
      "name": "PhoneNumber",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "呼转号码。",
        "en": "The phone number to which a call is transferred."
      }
    },
    {
      "name": "QueryDate",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "查询时间。格式：YYYY-MM-DD hh:mm:ss。\n\n> 查询结果为当天的所有的呼转记录。",
        "en": "The time at which call transfer records are queried, in the YYYY-MM-DD hh:mm:ss format.\n\n> The query result is all the call transfer records of the specified day."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询400号码呼转记录",
    "en": "QueryCallInTransferRecord"
  },
  "descriptions": {
    "zh": "### QPS限制\n单用户调用频率：100次/秒。",
    "en": "### QPS limits\n\nYou can call this operation up to 100 times per second per account."
  }
}