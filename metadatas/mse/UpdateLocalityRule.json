{
  "name": "UpdateLocalityRule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回结果显示的语言。取值：zh（默认值）：中文，en：英文",
        "en": "The language in which the returned results are displayed. Valid values: zh (default): Chinese. en: English"
      }
    },
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用id。",
        "en": "Application ID."
      }
    },
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用名称",
        "en": "Application name"
      }
    },
    {
      "name": "Enable",
      "position": "Query",
      "type": "Boolean",
      "required": true,
      "description": {
        "zh": "是否开启同可用区优先。",
        "en": "Specifies whether to enable the Intra-zone Provider First feature."
      }
    },
    {
      "name": "Namespace",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "MSE命名空间名字",
        "en": "The name of the Microservices Engine (MSE) namespace"
      }
    },
    {
      "name": "Region",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域",
        "en": "Region"
      }
    },
    {
      "name": "Rules",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保留参数，暂未使用。",
        "en": "This parameter is reserved and not in use."
      }
    },
    {
      "name": "Source",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "规则来源",
        "en": "Rule Source"
      }
    },
    {
      "name": "Threshold",
      "position": "Query",
      "type": "Double",
      "required": false,
      "description": {
        "zh": "同可用区优先生效阈值。当前应用在某个可用区部署的实例占比超过此阈值时，才会开启该可用区的同可用区优先路由功能。即Consumer向该应用发起请求时，会优先将请求路由到与Consumer同一个可用区的实例上。",
        "en": "The threshold for the Intra-zone Provider First feature to take effect. If the percentage of instances of the application that are deployed in a zone exceeds the specified threshold, the Intra-zone Provider First feature is enabled for the zone. This indicates that when a consumer initiates a request to the application, the request is preferentially routed to an instance in the same zone as the consumer."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新同可用区优先路由规则",
    "en": "UpdateLocalityRule"
  },
  "example": {
    "unifiedCli": "aliyun mse update-locality-rule --biz-region cn-hangzhou --namespace myNamespace --app-name example-app --app-id hkhon1po62@c3df23522000000 --enable true --rules No_input_required --source edasmsc --threshold 0.2 --accept-language zh --region cn-hangzhou",
    "legacyCli": "aliyun mse UpdateLocalityRule --region cn-hangzhou --AppId 'hkhon1po62@c3df23522000000' --Region 'cn-hangzhou' --Enable true --AcceptLanguage zh --Rules No_input_required --Namespace myNamespace --Source edasmsc --AppName 'example-app' --Threshold '0.2'"
  }
}