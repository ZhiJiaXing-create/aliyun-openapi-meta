{
  "name": "RemoveAuthPolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回结果显示的语言。取值：zh-CN（默认值）：中文，en-US：英文",
        "en": "The language in which the returned results are displayed. Values: zh-CN (default): Chinese, en-US: English"
      }
    },
    {
      "name": "PolicyId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "策略ID。",
        "en": "Policy ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除服务鉴权规则",
    "en": "RemoveAuthPolicy"
  },
  "example": {
    "unifiedCli": "aliyun mse remove-auth-policy --policy-id 259 --accept-language zh --region cn-hangzhou",
    "legacyCli": "aliyun mse RemoveAuthPolicy --region cn-hangzhou --AcceptLanguage zh --PolicyId 259"
  }
}