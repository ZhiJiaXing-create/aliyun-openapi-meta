{
  "name": "QueryNamespace",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回结果显示的语言。取值：zh（默认值）：中文，en：英文",
        "en": "The language in which you want the results to be displayed. Valid values: zh (default): Chinese. en: English."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要搜索的命名空间名称。",
        "en": "The name of the namespace that you want to query."
      }
    },
    {
      "name": "Region",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "操作的地域。",
        "en": "The region to which the namespace belongs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询MSE命名空间",
    "en": "QueryNamespace"
  },
  "example": {
    "unifiedCli": "aliyun mse query-namespace --biz-region cn-hangzhou --name default --accept-language zh --region cn-hangzhou",
    "legacyCli": "aliyun mse QueryNamespace --region cn-hangzhou --Region 'cn-hangzhou' --AcceptLanguage zh --Name default"
  }
}