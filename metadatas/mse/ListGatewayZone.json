{
  "name": "ListGatewayZone",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回结果显示的语言。取值：zh（默认值）：中文，en：英文",
        "en": "The language of the response. Valid values: `zh` (Chinese) and `en` (English). Default value: `zh`."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取网关可用区列表",
    "en": "ListGatewayZone"
  },
  "example": {
    "unifiedCli": "aliyun mse list-gateway-zone --accept-language zh --region cn-hangzhou",
    "legacyCli": "aliyun mse ListGatewayZone --region cn-hangzhou --AcceptLanguage zh"
  }
}