{
  "name": "UpdateCircuitBreakerRule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回结果显示的语言。取值：zh-CN（默认值）：中文，en-US：英文",
        "en": "The language of the response. Valid values: zh-CN and en-US. Default value: zh-CN. The value zh-CN indicates Chinese, and the value en-US indicates English."
      }
    },
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用ID。",
        "en": "The ID of the application."
      }
    },
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用名称。",
        "en": "The name of the application."
      }
    },
    {
      "name": "Enable",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "规则的启用状态。",
        "en": "Specifies whether to enable the rule.\n\nValid values:\n\n*   true\n\n    <!-- -->\n\n    <!-- -->\n\n    <!-- -->\n\n*   false\n\n    <!-- -->\n\n    <!-- -->\n\n    <!-- -->"
      }
    },
    {
      "name": "HalfOpenBaseAmountPerStep",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "熔断恢复每步最小通过数目，默认值为1。",
        "en": "The minimum number of requests that can be passed in each step after circuit breaking recovers. Default value: 1."
      }
    },
    {
      "name": "HalfOpenRecoveryStepNum",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "熔断恢复阶段数，默认值为1。",
        "en": "The number of circuit breaking recovery steps. Default value: 1."
      }
    },
    {
      "name": "MaxAllowedRtMs",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "慢调用RT（单位毫秒）：超过该时长则判定为慢请求，阈值类型为慢调用比例时必填。",
        "en": "The maximum response time (RT). Unit: milliseconds. If the RT of a request is greater than the value of this parameter, a slow call is counted. If you set Strategy to 0, you must specify this parameter."
      }
    },
    {
      "name": "MinRequestAmount",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "触发熔断的最小请求数目，若当前统计窗口请求数小于此值，即使达到熔断条件规则也不会触发。默认值为10。",
        "en": "The minimum number of requests to trigger circuit breaking. If the number of requests in the current time window is less than the value of this parameter, circuit breaking is not triggered even if the circuit breaking rule is met. Default value: 10."
      }
    },
    {
      "name": "Namespace",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用所属的微服务命空间。",
        "en": "The microservice namespace to which the application belongs."
      }
    },
    {
      "name": "RetryTimeoutMs",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "熔断时长（单位毫秒）：即熔断触发后持续的时间。资源进入熔断状态后，在配置的熔断时长内，请求都会快速失败。取值需要为1000的整数倍，默认值为10000，即10秒。",
        "en": "The period in which circuit breaking is implemented. Unit: milliseconds. If circuit breaking is implemented on the requests for the route, the calls to all the requests for the route fail in the configured circuit breaking period. The value must be an integral multiple of 1,000. Default value: 10000. This value indicates 10 seconds."
      }
    },
    {
      "name": "RuleId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "规则ID。",
        "en": "The rule ID."
      }
    },
    {
      "name": "StatIntervalMs",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "统计窗口时长（单位毫秒）：统计的时间窗口长度，取值范围为1秒~120分钟。默认值为20000，即20秒。",
        "en": "The length of the time window. Unit: milliseconds. The valid range is from 1 second to 120 minutes. The default value is 20000. This value indicates 20 seconds."
      }
    },
    {
      "name": "Strategy",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "阈值类型：选择以慢调用比例或异常比例作为阈值。",
        "en": "The threshold type.\n\nValid values:\n\n*   0\n\n    <!-- -->\n\n    :\n\n    <!-- -->\n\n    slow call proportion\n\n    <!-- -->\n\n*   1\n\n    <!-- -->\n\n    :\n\n    <!-- -->\n\n    abnormal proportion\n\n    <!-- -->"
      }
    },
    {
      "name": "Threshold",
      "position": "Query",
      "type": "Float",
      "required": false,
      "description": {
        "zh": "熔断比例阈值：触发熔断的比例阈值，取值范围为0 - 1，代表0% - 100%。",
        "en": "A percentage threshold for triggering circuit breaking. Valid values: 0-1. These values represent 0% to 100%."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新熔断规则",
    "en": "UpdateCircuitBreakerRule"
  },
  "example": {
    "unifiedCli": "aliyun mse update-circuit-breaker-rule --namespace default --app-name spring-cloud-a --threshold 0.8 --enable true --app-id hkhon1po62@c3df23522bXXXXX --strategy 0 --retry-timeout-ms 10000 --stat-interval-ms 20000 --max-allowed-rt-ms 200 --min-request-amount 10 --half-open-base-amount-per-step 5 --half-open-recovery-step-num 1 --rule-id 1 --accept-language zh --region cn-hangzhou",
    "legacyCli": "aliyun mse UpdateCircuitBreakerRule --region cn-hangzhou --MinRequestAmount 10 --RuleId 1 --Namespace default --AppName 'spring-cloud-a' --RetryTimeoutMs 10000 --AppId 'hkhon1po62@c3df23522bXXXXX' --HalfOpenRecoveryStepNum 1 --Enable true --Strategy 0 --AcceptLanguage zh --MaxAllowedRtMs 200 --HalfOpenBaseAmountPerStep 5 --StatIntervalMs 20000 --Threshold '0.8'"
  }
}