{
  "name": "DeleteIsolationRules",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回结果显示的语言。取值：zh（默认值）：中文，en：英文",
        "en": "The language in which the response is displayed. Valid values: zh (default): Chinese; en: English."
      }
    },
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用名称。",
        "en": "The application name."
      }
    },
    {
      "name": "Ids",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "待删除的规则ID列表。",
        "en": "The list of rule IDs to be deleted."
      }
    },
    {
      "name": "Namespace",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用所属的微服务命名空间。",
        "en": "The microservice namespace to which the application belongs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除隔离规则",
    "en": "DeleteIsolationRules"
  },
  "example": {
    "unifiedCli": "aliyun mse delete-isolation-rules --namespace default --ids 1 --app-name spring-cloud-a --accept-language zh --region cn-hangzhou",
    "legacyCli": "aliyun mse DeleteIsolationRules --region cn-hangzhou --Ids '[1]' --AcceptLanguage zh --Namespace default --AppName 'spring-cloud-a'"
  }
}