{
  "name": "ListTagResourcesForRegion",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用来返回更多结果。第一次查询不需要提供这个参数，如果第一次查询没有返回全部结果，则可在后续查询中传入前一次返回的token继续查询。",
        "en": "The token that is used to retrieve the next page of results. Optional for the first request. If the query does not return all results, the response contains a token. You can use this token in the next request to continue the query."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID",
        "en": "The region ID."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源类型，取值范围:\n\n- cluster: 集群实例\n- aicluster: AI集群实例",
        "en": "The resource type. Valid values:\n\n- cluster: cluster instance\n\n- aicluster: AI cluster instance"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "ListTagResourcesForRegion",
    "en": "ListTagResourcesForRegion"
  },
  "example": {
    "unifiedCli": "aliyun polardb list-tag-resources-for-region --biz-region-id cn-hangzhou --next-token 212db86sca4384811e0b5e8707e000000 --resource-type cluster --region cn-hangzhou",
    "legacyCli": "aliyun polardb ListTagResourcesForRegion --region cn-hangzhou --NextToken 212db86sca4384811e0b5e8707e000000 --ResourceType cluster --RegionId 'cn-hangzhou'"
  }
}