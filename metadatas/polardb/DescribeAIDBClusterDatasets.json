{
  "name": "DescribeAIDBClusterDatasets",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ContinuationToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "本次初始token，查询列表时不生效",
        "en": "The token used to retrieve the next page of results. This value is obtained from the response of a previous request. For the first request, leave this parameter empty."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "AI模型服务的PolarDB数据库实例ID",
        "en": "The ID of the PolarDB cluster for AI model services."
      }
    },
    {
      "name": "DatasetId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据集ID",
        "en": "The dataset ID."
      }
    },
    {
      "name": "DatasetType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据集的类型，取值如下：\n* **train**：训练集\n* **eval**：评测集",
        "en": "The type of the dataset. Valid values:\n\n- **train**: The training set.\n\n- **eval**: The evaluation set."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询页码，默认值为1。",
        "en": "The page number to return. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "每页记录数，取值为**30**、**50**或**100**。\n                              \n默认值为**30**。",
        "en": "The number of entries to return on each page. Valid values: **30**, **50**, and **100**.\n\nDefault value: **30**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "TrainMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据集应对的训练模式，取值如下：\n* **sft**：监督微调\n* **grpo**：强化学习\n* **text**：文本生成",
        "en": "The training mode. Valid values:\n\n- **sft**: supervised fine-tuning.\n\n- **grpo**: reinforcement learning.\n\n- **text**: text generation."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询数据集列表",
    "en": "DescribeAIDBClusterDatasets"
  },
  "example": {
    "unifiedCli": "aliyun polardb describe-aidb-cluster-datasets --biz-region-id cn-hangzhou --db-cluster-id pc-2ze88000 --dataset-type train --train-mode grpo --continuation-token EFSDF-DF-000 --page-size 30 --page-number 1 --dataset-id pds-2ze88000 --region cn-hangzhou",
    "legacyCli": "aliyun polardb DescribeAIDBClusterDatasets --region cn-hangzhou --ContinuationToken 'EFSDF-DF-000' --PageSize 30 --PageNumber 1 --DBClusterId 'pc-2ze88000' --DatasetType train --RegionId 'cn-hangzhou' --TrainMode grpo --DatasetId 'pds-2ze88000'"
  }
}