{
  "name": "DeleteDBCluster",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupRetentionPolicyOnClusterDeletion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "删除集群时备份集保留策略，取值范围如下：\n* **ALL**：长期保留全部备份。\n* **LATEST**：长期保留最后一个备份（删除前自动备份）。\n* **NONE**：集群删除时不保留备份集。",
        "en": "The retention policy for backup sets when the cluster is deleted. Valid values:\n\n- **ALL**: permanently retains all backup sets.\n\n- **LATEST**: permanently retains the last backup set. The system automatically creates a new backup before the cluster is deleted.\n\n- **NONE**: does not retain any backup sets after the cluster is deleted."
      }
    },
    {
      "name": "CloudProvider",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例所属的云服务商",
        "en": "The cloud service provider of the instance."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "释放按量付费的PolarDB集群",
    "en": "DeleteDBCluster"
  },
  "example": {
    "unifiedCli": "aliyun polardb delete-db-cluster --cloud-provider ENS --db-cluster-id pc-00000000000000 --backup-retention-policy-on-cluster-deletion NONE --region cn-hangzhou",
    "legacyCli": "aliyun polardb DeleteDBCluster --region cn-hangzhou --CloudProvider ENS --BackupRetentionPolicyOnClusterDeletion NONE --DBClusterId 'pc-00000000000000'"
  }
}