{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解绑全部标签，取值为**true**或**false**，默认值为**false**。\n> 仅当`TagKey.n`取值为空时该参数生效。",
        "en": "Specifies whether to detach all tags. Valid values: **true** and **false**. Default value: **false**.\n\n> This parameter takes effect only when the `TagKey.n` parameter is empty."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。\n\n> 可通过接口[DescribeRegions](~~98041~~)查看可用的地域。",
        "en": "The region ID.\n\n> Call the [DescribeRegions](~~98041~~) operation to view available regions."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "目标集群ID。",
        "en": "The ID of the target cluster."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型，取值固定为**cluster**。",
        "en": "The resource type. Set the value to **cluster**."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签键。",
        "en": "The tag key."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "将标签从集群上解绑",
    "en": "UntagResources"
  },
  "example": {
    "unifiedCli": "aliyun polardb untag-resources --biz-region-id cn-hangzhou --resource-type cluster --all true --resource-id pc-000000000000000 --tag-key type --region cn-hangzhou",
    "legacyCli": "aliyun polardb UntagResources --region cn-hangzhou --All true --ResourceId.1 'pc-000000000000000' --TagKey.1 type --ResourceType cluster --RegionId 'cn-hangzhou'"
  }
}