{
  "name": "FailoverDBCluster",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，保证在不同请求间唯一，大小写敏感、不超过64个ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that it is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length. The token is case-sensitive."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据库集群ID。",
        "en": "The ID of the cluster."
      }
    },
    {
      "name": "RollBackForDisaster",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否是故障切换后的回切至原主可用区，取值范围如下：\n\n- true：是。\n- false：不是。",
        "en": "Specifies whether to switch back services to the original primary zone when the original primary zone recovers.\n\n*   true\n*   false"
      }
    },
    {
      "name": "TargetDBNodeId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "将要切换为主节点的节点ID，如果不填，则系统自动选举。您可以通过接口[DescribeDBClusters](~~98094~~)查询节点ID等信息。\n\n> - 自动选举主节点： 系统找出当前可以被选举的所有只读节点。选择优先级最高的一个或多个只读节点。如果切换第一个节点失败（例如，网络原因、复制状态异常等），则会尝试切换下一个，直至成功。\n>- 当集群为PolarDB PostgreSQL版（兼容Oracle）或PolarDB PostgreSQL版时，该参数必填；当集群为PolarDB MySQL版时，该参数非必填。",
        "en": "The ID of the read-only node that you want to promote to the primary node. You can call the [DescribeDBClusters](~~98094~~) operation to query node information, such as node IDs.\n\n> *   If you leave this parameter empty, the system selects one or more available read-only nodes that have the highest failover priority as candidate primary nodes. If the failover to the first read-only node fails due to network issues, abnormal replication status, or other reasons, the system attempts to fail over your applications to the next read-only node until the failover is successful.\n>*  This parameter is required for PolarDB for Oracle and PolarDB for PostgreSQL clusters. This parameter is optional for PolarDB for MySQL clusters."
      }
    },
    {
      "name": "TargetZoneType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否是主可用区内主备切换，取值如下：\n- Primary：主可用区内主备切换。\n- Standby：切换至存储热备集群。",
        "en": "Whether it is a primary-standby switch within the primary availability zone, with the following values:\n\nPrimary: Primary-standby switch within the primary availability zone.\nStandby: Switch to the storage hot backup cluster."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "对集群进行手动主备切换",
    "en": "FailoverDBCluster"
  },
  "example": {
    "unifiedCli": "aliyun polardb failover-db-cluster --db-cluster-id pc-00000000000000 --target-db-node-id pi-00000000000 --roll-back-for-disaster false --client-token 6000170000591aed949d0f54a343f1a4233c1e7d1c5000000 --target-zone-type Primary --region cn-hangzhou",
    "legacyCli": "aliyun polardb FailoverDBCluster --region cn-hangzhou --RollBackForDisaster false --TargetZoneType Primary --DBClusterId 'pc-00000000000000' --TargetDBNodeId 'pi-00000000000' --ClientToken 6000170000591aed949d0f54a343f1a4233c1e7d1c5000000"
  }
}