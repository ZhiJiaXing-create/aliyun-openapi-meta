{
  "name": "DescribePendingMaintenanceAction",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IsHistory",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "是否返回历史任务，取值范围如下：\n* **0**：返回当前任务\n* **1**：返回历史任务\n\n默认取值为**0**。",
        "en": "Specifies whether to return the historical tasks. Valid values:\n\n- **0**: returns the current task.\n\n- **1**: returns the historical tasks.\n\nDefault value: **0**."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，取值为大于0且不超过Integer数据类型的最大值，默认值为**1**。",
        "en": "The number of the page to return. Specify the parameter to a positive integer that does not exceed the maximum value of the INTEGER data type. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，取值为**30**、**50**或**100**。\n\n默认值为**30**。",
        "en": "The number of entries per page. Valid values: **30**, **50**, and **100**.\n\nDefault value: **30**."
      }
    },
    {
      "name": "Region",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待处理事件所在地域的地域ID，您可以调用[DescribeRegions](~~98041~~)接口查看PolarDB支持的地域和可用区。\n\n> * 您可以输入**all**查看您账户下所有的待处理事件。\n> * 当`Region`设置为**all**时，`TaskType`也必须设置为**all**。",
        "en": "The region ID of the pending event. You can call the [DescribeRegions](~~98041~~) operation to query the regions and zones that are supported by PolarDB.\n\n> - You can set this parameter to **all** to view all pending events within your account.\n>\n> - If you set `Region` to **all**, you must set `TaskType` to **all**."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group."
      }
    },
    {
      "name": "TaskType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待处理事件的任务类型，取值范围如下：\n* **DatabaseSoftwareUpgrading**：数据库软件升级\n* **DatabaseHardwareMaintenance**：硬件维护与升级\n* **DatabaseStorageUpgrading**：数据库存储升级\n* **DatabaseProxyUpgrading**：代理小版本升级\n* **all**：表示查询上述所有类型的待处理事件详情\n\n> 当`Region`设置为**all**时，`TaskType`也必须设置为**all**。",
        "en": "The task type of pending events. Valid values:\n\n- **DatabaseSoftwareUpgrading**: database software upgrades\n\n- **DatabaseHardwareMaintenance**: hardware maintenance and upgrades\n\n- **DatabaseStorageUpgrading**: database storage upgrades\n\n- **DatabaseProxyUpgrading**: minor version upgrades of the proxy\n\n- **all**: returns pending events of all preceding types.\n\n> If the `Region` parameter is set to **all**, the `TaskType` parameter must be set to **all**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询待处理事件的详情",
    "en": "DescribePendingMaintenanceAction"
  },
  "example": {
    "unifiedCli": "aliyun polardb describe-pending-maintenance-action --biz-region all --task-type all --is-history 0 --page-size 30 --page-number 1 --resource-group-id rg-000000000000 --region cn-hangzhou",
    "legacyCli": "aliyun polardb DescribePendingMaintenanceAction --region cn-hangzhou --PageSize 30 --ResourceGroupId 'rg-000000000000' --PageNumber 1 --TaskType all --Region all --IsHistory 0"
  }
}