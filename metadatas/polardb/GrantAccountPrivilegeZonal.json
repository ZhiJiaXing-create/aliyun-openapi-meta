{
  "name": "GrantAccountPrivilegeZonal",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "账号名称。",
        "en": "The account name."
      }
    },
    {
      "name": "AccountPrivilege",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "账号权限，取值范围如下：\n\n- ReadWrite：读写\n- ReadOnly：只读\n- DMLOnly：只允许 DML\n- DDLOnly：只允许 DDL\n- ReadIndex：只读+索引",
        "en": "The account permissions. Valid values:\n\n- ReadWrite: Read and write permissions.\n\n- ReadOnly: Read-only permissions.\n\n- DMLOnly: DML-only permissions.\n\n- DDLOnly: DDL-only permissions.\n\n- ReadIndex: Read-only and index permissions."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "该参数用于保证请求的幂等性。由客户端生成该参数值，保证在不同请求间该参数值的唯一性。该参数区分大小写、长度不超过64个ASCII字符。",
        "en": "A client-generated token that ensures the idempotence of the request. Make sure that the token is unique among different requests. The token is case-sensitive and can be up to 64 ASCII characters in length."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    },
    {
      "name": "DBName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要授权的数据库名称。支持同时对一个或多个数据库授权，多个数据库名称之间用英文逗号（,）隔开。",
        "en": "The name of the database for which to grant permissions. To grant permissions for multiple databases, separate the database names with a comma (,)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "授权边缘集群普通账号访问数据库",
    "en": "GrantAccountPrivilegeZonal"
  },
  "example": {
    "unifiedCli": "aliyun polardb grant-account-privilege-zonal --db-cluster-id pc-0000000000000000 --account-name testacc --db-name testdb_1,testdb_2 --account-privilege ReadWrite,ReadOnly --client-token 6000170000591aed949d0f500000000000000000000 --region cn-hangzhou",
    "legacyCli": "aliyun polardb GrantAccountPrivilegeZonal --region cn-hangzhou --AccountPrivilege 'ReadWrite,ReadOnly' --DBClusterId 'pc-0000000000000000' --DBName 'testdb_1,testdb_2' --ClientToken 6000170000591aed949d0f500000000000000000000 --AccountName testacc"
  }
}