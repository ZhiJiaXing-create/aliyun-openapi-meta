{
  "name": "CancelActiveOperationTasks",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。\n> * 您可以调用[DescribeRegions](~~98041~~)接口查看目标账号下所有的集群的地域信息。\n> * 若该参数留空，默认查询当前账号下所有地域内的计划任务。",
        "en": "The region ID.\n\n> \n\n*   You can call the [DescribeRegions](~~98041~~) operation to query the region information about all clusters within a specified account.\n\n*   If you do not specify this parameter, scheduled tasks on your clusters that are deployed in all regions are queried."
      }
    },
    {
      "name": "TaskIds",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "批量取消的运维事件 ID，多个 ID 间使用英文逗号（,）分隔。",
        "en": "The IDs of O\\&M events that are canceled at a time. Separate multiple IDs with commas (,)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "取消运维事件",
    "en": "CancelActiveOperationTasks"
  },
  "example": {
    "unifiedCli": "aliyun polardb cancel-active-operation-tasks --biz-region-id cn-hangzhou --task-ids 2355,2352 --region cn-hangzhou",
    "legacyCli": "aliyun polardb CancelActiveOperationTasks --region cn-hangzhou --TaskIds '2355,2352' --RegionId 'cn-hangzhou'"
  }
}