{
  "name": "DeleteDBNodes",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，保证在不同请求间唯一，大小写敏感、不超过64个ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. The client generates this value, which must be unique for each request. The token can contain only ASCII characters and cannot exceed 64 characters in length. The token is case-sensitive."
      }
    },
    {
      "name": "CloudProvider",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "节点所属的服务商",
        "en": "The service provider of the node."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    },
    {
      "name": "DBNodeId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "集群节点ID。\n>您可以通过[DescribeDBClusters](~~185342~~)接口查看账号下所有集群的详细信息，包括节点ID。",
        "en": "The IDs of the nodes to be deleted.\n\n> Call the [DescribeDBClusters](~~185342~~) operation to query the details of all clusters in your account, including node IDs."
      }
    },
    {
      "name": "DBNodeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "节点类型。取值范围：\n- RO\n- STANDBY\n- DLNode",
        "en": "The node type. Valid values:\n\n- RO\n\n- STANDBY\n\n- DLNode"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除PolarDB集群的只读节点",
    "en": "DeleteDBNodes"
  },
  "example": {
    "unifiedCli": "aliyun polardb delete-db-nodes --db-cluster-id pc-00000000000000 --client-token 6000170000591aed949d0f54a343f1a4200000000000 --db-node-type RO --db-node-id pi-0000000000000000 --cloud-provider ENS --region cn-hangzhou",
    "legacyCli": "aliyun polardb DeleteDBNodes --region cn-hangzhou --DBNodeId.1 'pi-0000000000000000' --CloudProvider ENS --DBClusterId 'pc-00000000000000' --DBNodeType RO --ClientToken 6000170000591aed949d0f54a343f1a4200000000000"
  }
}