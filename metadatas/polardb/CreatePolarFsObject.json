{
  "name": "CreatePolarFsObject",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "/",
  "parameters": [
    {
      "name": "Path",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目标路径",
        "en": "The absolute path of the directory to create."
      }
    },
    {
      "name": "PolarFsInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "PolarFs实例ID",
        "en": "The PolarFS instance id."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建目录接口",
    "en": "CreatePolarFsObject"
  },
  "descriptions": {
    "zh": "## 请求说明\n- **Path**：需要创建的目录绝对路径。\n- **Recursive**：是否递归创建父目录，默认为 `false`。\n- 该接口支持在指定的PolarFS实例中创建单个或多个层级的目录结构。\n- 如果设置 `Recursive` 为 `true`，则会自动创建所有不存在的父目录。\n- 创建目录时，请确保具有足够的权限。",
    "en": "## Description\n\n- **Path**: The absolute path of the directory to create.\n\n- **Recursive**: Specifies whether to recursively create parent directories. The default value is `false`.\n\n- This operation creates a directory structure with one or more levels in the specified PolarFS instance.\n\n- Setting `Recursive` to `true` automatically creates all non-existent parent directories.\n\n- Ensure that you have sufficient permissions to create the directory."
  },
  "example": {
    "unifiedCli": "aliyun polardb create-polar-fs-object --polar-fs-instance-id pfs-2ze0i700000 --path /test --region cn-hangzhou",
    "legacyCli": "aliyun polardb CreatePolarFsObject --region cn-hangzhou --Path '/test' --PolarFsInstanceId 'pfs-2ze0i700000'"
  }
}