{
  "name": "DescribeDatabases",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The ID of the cluster."
      }
    },
    {
      "name": "DBName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据库名。\n\n> 不支持填入多个数据库名称。",
        "en": "The name of the database.\n\n> You cannot specify multiple database names."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，取值范围为大于0但不超过Integer最大值的整数，默认值为**1**。",
        "en": "The number of the page to return. The value must be an integer that is greater than 0 and does not exceed the maximum value of an integer. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，取值范围如下：\n* **30**\n* **50**\n* **100**\n\n默认值为**30**。",
        "en": "The number of entries to return on each page. Valid values:\n\n- **30**\n\n- **50**\n\n- **100**\n\nDefault value: **30**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询PolarDB指定集群的数据库详情",
    "en": "DescribeDatabases"
  },
  "example": {
    "unifiedCli": "aliyun polardb describe-databases --db-cluster-id pc-00000000000000 --db-name test_db --page-number 1 --page-size 30 --region cn-hangzhou",
    "legacyCli": "aliyun polardb DescribeDatabases --region cn-hangzhou --PageSize 30 --PageNumber 1 --DBClusterId 'pc-00000000000000' --DBName test_db"
  }
}