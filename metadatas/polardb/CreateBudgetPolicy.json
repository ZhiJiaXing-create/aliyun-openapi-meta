{
  "name": "CreateBudgetPolicy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AlertThresholdPct",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "告警阈值百分比（0-100）",
        "en": "The alert threshold, as a percentage. Valid values: 0 to 100."
      }
    },
    {
      "name": "BudgetDimensionRefId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "维度对象ID（当 BudgetType为 ConsumerGroup 或 Consumer 时必填）",
        "en": "The ID of the dimension object. This parameter is required if `BudgetType` is set to `ConsumerTotal` or `ConsumerGroupTotal`."
      }
    },
    {
      "name": "BudgetPoints",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "预算点数",
        "en": "The number of budget points."
      }
    },
    {
      "name": "BudgetType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "预算类型，枚举值：\n\n- **GlobalTotal**                     全局总预算\n- **ConsumerTotal**               用户总预算\n- **ConsumerGroupTotal**     用户组总预算",
        "en": "The budget type. Valid values:\n\n- `GlobalTotal`: global total budget\n\n- `ConsumerTotal`: consumer total budget\n\n- `ConsumerGroupTotal`: consumer group total budget"
      }
    },
    {
      "name": "GwClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网关实例ID。",
        "en": "The gateway cluster ID."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "ResetDayOfMonth",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "每月重置日（1-28）",
        "en": "The day of the month on which the budget resets. Valid values: 1 to 28."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建预算策略",
    "en": "CreateBudgetPolicy"
  },
  "example": {
    "unifiedCli": "aliyun polardb create-budget-policy --gw-cluster-id pg-xxxxxxx --budget-type ConsumerTotal --budget-dimension-ref-id mi-xxxxx --reset-day-of-month 1 --budget-points 1000 --alert-threshold-pct 10 --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun polardb CreateBudgetPolicy --region cn-hangzhou --BudgetDimensionRefId 'mi-xxxxx' --GwClusterId 'pg-xxxxxxx' --ResetDayOfMonth 1 --RegionId 'cn-hangzhou' --AlertThresholdPct 10 --BudgetType ConsumerTotal --BudgetPoints 1000"
  }
}