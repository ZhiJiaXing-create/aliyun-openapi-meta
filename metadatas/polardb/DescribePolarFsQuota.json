{
  "name": "DescribePolarFsQuota",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "集群ID。\n> 企业、基础、湖仓版：您可以调用[DescribeDBClusters](~~2319131~~)接口可以查询集群ID。",
        "en": "The cluster ID.\n\n> To find the cluster ID for enterprise, basic, or data lakehouse edition clusters, call the [DescribeDBClusters](~~2319131~~) operation."
      }
    },
    {
      "name": "Path",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "目标路径",
        "en": "The destination path."
      }
    },
    {
      "name": "PolarFsInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Polarlakebase实例ID",
        "en": "The PolarFS instance ID."
      }
    },
    {
      "name": "QuotaType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询的Quota类型",
        "en": "The quota type to query."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。\n\n> 您可以通过[DescribeRegions](~~98041~~)接口查看当前账号下的所有可用地域信息，包括地域ID。",
        "en": "The region ID.\n\n> Call the [DescribeRegions](~~98041~~) operation to find the IDs of all available regions in your account."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询Polarlakebase配额规则",
    "en": "DescribePolarFsQuota"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n- 本API用于查询特定Polarlakebase实例下的所有配额规则，支持通过`QuotaNameMatch`和`PatternMatch`参数进行模糊匹配。\n- 分页功能暂未公开，待内核准备好后将开放使用。目前返回结果无分页限制。\n- `PolarFsInstanceId`是必填项，用于指定要查询的Polarlakebase实例。\n- 可选参数如`QuotaNameMatch`、`PatternMatch`等可用于进一步筛选符合条件的配额规则。\n- 返回的数据包括但不限于配额规则总数、当前页规则数量以及具体的规则详情（如规则ID、名称、描述、容量限制等）。",
    "en": "## Request\n\n- This operation queries all quota rules for a specific PolarFS instance. It supports wildcard matching with the `QuotaNameMatch` and `PatternMatch` parameters.\n\n- Paging is not yet available and will be released in a future kernel update.\n\n- The `PolarFsInstanceId` parameter is required to specify the PolarFS instance to query.\n\n- You can use optional parameters, such as `QuotaNameMatch` and `PatternMatch`, to filter quota rules.\n\n- The returned data includes the total number of quota rules, the number of rules on the current page, and details for each rule, such as the rule ID, name, description, and capacity limit."
  },
  "example": {
    "unifiedCli": "aliyun polardb describe-polar-fs-quota --polar-fs-instance-id pfs-2ze0i74ka60700000 --db-cluster-id pc-00000000000000 --biz-region-id cn-hangzhou --path /data --quota-type quotaPolicy --region cn-hangzhou",
    "legacyCli": "aliyun polardb DescribePolarFsQuota --region cn-hangzhou --Path '/data' --PolarFsInstanceId 'pfs-2ze0i74ka60700000' --QuotaType quotaPolicy --DBClusterId 'pc-00000000000000' --RegionId 'cn-hangzhou'"
  }
}