{
  "name": "CreateAccount",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "账号备注说明，需满足如下要求：\n\n- 不能以`http://`或`https://`开头。\n- 长度为2~256个字符。",
        "en": "The description of the account. The description must meet the following requirements:\n\n- Cannot start with `http://` or `https://`.\n- Is 2 to 256 characters in length."
      }
    },
    {
      "name": "AccountName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "账号名称，需符合如下要求：\n\n* 以小写字母开头，以字母或数字结尾。\n* 由小写字母、数字或下划线组成。\n* 长度为2~16个字符。\n* 不能使用某些预留的用户名，如root、admin等。",
        "en": "The account name. The name must meet the following requirements:\n\n* Starts with a lowercase letter and ends with a letter or digit.\n* Contains only lowercase letters, digits, or underscores (_).\n* Is 2 to 16 characters in length.\n* Cannot use certain reserved usernames such as root or admin."
      }
    },
    {
      "name": "AccountPassword",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "账号密码，需符合如下要求：\n* 至少包含大写字母、小写字母、数字或特殊字符中的任意三种。\n* 长度为8~32个字符。\n* 特殊字符为`!@#$%^&*()_+-=`。",
        "en": "The account password. The password must meet the following requirements:\n* Contains at least three of the following character types: uppercase letters, lowercase letters, digits, and special characters.\n* Is 8 to 32 characters in length.\n* Special characters include `!@#$%^&*()_+-=`."
      }
    },
    {
      "name": "AccountPrivilege",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "账号权限，取值范围如下： \n\n* **ReadWrite**：读写\n* **ReadOnly**：只读\n* **DMLOnly**：只允许DML\n* **DDLOnly**：只允许DDL\n*  **ReadIndex**：只读+索引\n\n>* 必须要传入`DBName`参数，`AccountPrivilege`才能生效。\n>* 若为`DBName`参数传入了多个数据库名称，您需要为目标数据库分别授予相应的权限，多个账号权限之间使用英文逗号（,）分隔，且需保证`AccountPrivilege`的字符串长度不超过900。例如，需要授权数据库DB1读写权限，授权数据库DB2只读权限，您需要设置`DBName`为`DB1,DB2`，并设置`AccountPrivilege`为`ReadWrite,ReadOnly`。\n> * 仅PolarDB MySQL版集群的普通账号支持设置该参数。",
        "en": "The permissions of the account. Valid values: \n\n* **ReadWrite**: read and write\n* **ReadOnly**: read-only\n* **DMLOnly**: DML only\n* **DDLOnly**: DDL only\n* **ReadIndex**: read-only and index\n\n>* The DBName parameter must be specified for AccountPrivilege to take effect.\n>* If you specify multiple database names for the DBName parameter, you must grant the corresponding permissions to each database. Separate multiple permissions with commas (,) and make sure that the total length of the AccountPrivilege string does not exceed 900 characters. For example, to grant read and write permissions on database DB1 and read-only permissions on database DB2, set DBName to `DB1,DB2` and set AccountPrivilege to `ReadWrite,ReadOnly`.\n> * This parameter is supported only for standard accounts of PolarDB for MySQL clusters."
      }
    },
    {
      "name": "AccountType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "账号类型，取值范围如下：\n\n- **Normal**：普通账号。 \n- **Super**：高权限账号。 \n- **DynamoDB**：DynamoDB账号。 \n\n\n \n> * 若该参数留空，则默认创建**Super**账号。\n> * 当集群为PolarDB PostgreSQL版（兼容Oracle）或PolarDB PostgreSQL版时，每个集群允许创建多个高权限账号，高权限账号相比普通账号拥有更多权限，创建数据库账号详情参见[创建数据库账号](~~68508~~)。\n> * 当集群为PolarDB MySQL版时，每个集群最多只允许创建1个高权限账号，高权限账号相比普通账号拥有更多权限，创建数据库账号详情参见[创建数据库账号](~~68508~~)。\n> * DynamoDB账号是为PolarDB PostgreSQL版DynamoDB兼容功能创建的专用账号，详情参见[DynamoDB使用说明](~~2979941~~)。",
        "en": "The account type. Valid values:\n\n- **Normal**: standard account. \n- **Super**: privileged account. \n- **DynamoDB**: DynamoDB account. \n\n\n \n> * If this parameter is left empty, a **Super** account is created by default.\n> * If the cluster is a PolarDB for PostgreSQL (Compatible with Oracle) or PolarDB for PostgreSQL cluster, you can create multiple privileged accounts for each cluster. Privileged accounts have more permissions than standard accounts. For more information, see [Create a database account](~~68508~~).\n> * If the cluster is a PolarDB for MySQL cluster, you can create at most one privileged account for each cluster. Privileged accounts have more permissions than standard accounts. For more information, see [Create a database account](~~68508~~).\n> * DynamoDB accounts are dedicated accounts created for the DynamoDB compatibility feature of PolarDB for PostgreSQL. For more information, see [DynamoDB usage instructions](~~2979941~~)."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，保证在不同请求间唯一，大小写敏感、不超过64个ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value. Make sure that the value is unique among different requests. The token is case-sensitive and cannot exceed 64 ASCII characters in length."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    },
    {
      "name": "DBName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "允许目标账号访问的数据库名称，支持传入多个数据库名称，多个数据库名称间用英文逗号（,）分隔。\n\n> 仅PolarDB MySQL版集群的普通账号支持该参数。",
        "en": "The name of the database that the account is authorized to access. You can specify multiple database names separated by commas (,).\n\n> This parameter is supported only for standard accounts of PolarDB for MySQL clusters."
      }
    },
    {
      "name": "NodeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "节点类型。取值范围：\n\n- Search：PolarDB Search 节点账号时需要传入",
        "en": "The node type. Valid values:\n\n- Search: required when creating an account for a PolarDB Search node"
      }
    },
    {
      "name": "PrivForAllDB",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否授权当前集群所有库及后续新增所有库的权限。取值范围如下：\n\n- **0或不填**：不授权。\n\n- **1**：授权。\n>* 必须要传入`AccountPrivilege`参数才能生效。\n>* 当参数设置为`1`时，表示`AccountPrivilege`会被授权到所有的库。",
        "en": "Specifies whether to grant permissions on all existing databases and all new databases in the current cluster. Valid values:\n\n- **0 or empty**: does not grant permissions.\n\n- **1**: grants permissions.\n>* The AccountPrivilege parameter must be specified for this parameter to take effect.\n>* If this parameter is set to `1`, the permissions specified by AccountPrivilege are granted on all databases."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建PolarDB数据库账号",
    "en": "Create a polarDB database account"
  },
  "example": {
    "unifiedCli": "aliyun polardb create-account --db-cluster-id pc-00000000000000 --account-name testacc --account-password Test1111 --account-type Normal --account-description testdes --db-name testdb --account-privilege ReadWrite --client-token 6000170000591aed949d0f54a343f1a4233c1e7d1c5c000000 --priv-for-all-db 0 --node-type Search --region cn-hangzhou",
    "legacyCli": "aliyun polardb CreateAccount --region cn-hangzhou --AccountDescription testdes --AccountPrivilege ReadWrite --NodeType Search --DBClusterId 'pc-00000000000000' --DBName testdb --ClientToken 6000170000591aed949d0f54a343f1a4233c1e7d1c5c000000 --AccountType Normal --AccountName testacc --AccountPassword Test1111 --PrivForAllDB 0"
  }
}