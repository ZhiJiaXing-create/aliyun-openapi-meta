{
  "name": "CancelPolarFsFileQuota",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用依赖的PolarDB实例ID",
        "en": "The ID of the PolarDB cluster that the application depends on."
      }
    },
    {
      "name": "FilePathIds",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件路径，多个路径通过`,`分隔",
        "en": "The file paths. Separate multiple paths with a comma (`,`)."
      }
    },
    {
      "name": "PolarFsInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Polarlakebase实例ID",
        "en": "The ID of the Polarlakebase instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除Polarlakebase文件配额",
    "en": "CancelPolarFsFileQuota"
  },
  "descriptions": {
    "zh": "## 请求说明\n- `PolarFsInstanceId` 是必需的，用来标识要操作的Polarlakebase实例。\n- `FilePathIds` 参数也是必需的，它接收一个字符串形式的文件路径ID列表，这些ID对应于需要删除其配额规则的具体文件路径。确保提供的每个ID都是有效的，并且属于指定的Polarlakebase实例。\n- 本API一次调用可以处理多个文件路径的配额删除请求，但请注意控制请求大小以避免性能问题。",
    "en": "## Request description\n\n- The `PolarFsInstanceId` parameter is required. It specifies the Polarlakebase instance for the operation.\n\n- The `FilePathIds` parameter is required. It accepts a string containing the IDs of the file paths for which you want to delete quota rules. Ensure that each ID is valid and belongs to the specified Polarlakebase instance.\n\n- A single API call can delete quotas for multiple file paths. However, limit the number of paths in a single request to avoid performance issues."
  },
  "example": {
    "unifiedCli": "aliyun polardb cancel-polar-fs-file-quota --polar-fs-instance-id pfs-2ze0i74ka60700000 --file-path-ids /path1,/path2 --db-cluster-id pc-00000000000000 --region cn-hangzhou",
    "legacyCli": "aliyun polardb CancelPolarFsFileQuota --region cn-hangzhou --PolarFsInstanceId 'pfs-2ze0i74ka60700000' --DBClusterId 'pc-00000000000000' --FilePathIds '/path1,/path2'"
  }
}