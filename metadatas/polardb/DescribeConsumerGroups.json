{
  "name": "DescribeConsumerGroups",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConsumerGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户组ID。",
        "en": "The ID of the consumer group."
      }
    },
    {
      "name": "GwClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网关实例ID。",
        "en": "The ID of the gateway instance."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询页码，默认值为1。",
        "en": "The page number to return. The default value is 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，取值范围如下：\n* **30**\n* **50**\n* **100**\n\n默认值为30。",
        "en": "The number of records to return per page. Valid values:\n\n- **30**\n\n- **50**\n\n- **100**\n\nThe default value is 30."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域 ID。",
        "en": "The ID of the region."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询消费者组列表",
    "en": "DescribeConsumerGroups"
  },
  "example": {
    "unifiedCli": "aliyun polardb describe-consumer-groups --gw-cluster-id pg-xxxxxxx --consumer-group-id cg-xxxxxxxx --page-size 30 --page-number 1 --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun polardb DescribeConsumerGroups --region cn-hangzhou --ConsumerGroupId 'cg-xxxxxxxx' --GwClusterId 'pg-xxxxxxx' --PageSize 30 --PageNumber 1 --RegionId 'cn-hangzhou'"
  }
}