{
  "name": "DisablePolarClawChannel",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ApplicationId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID",
        "en": "The application ID."
      }
    },
    {
      "name": "ChannelId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "通道唯一标识",
        "en": "The unique ID of the channel."
      }
    },
    {
      "name": "Restart",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "禁用后是否重启网关，默认true",
        "en": "Specifies whether to restart the gateway after the channel is disabled. Defaults to true."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "禁用PolarClaw通道",
    "en": "DisablePolarClawChannel"
  },
  "descriptions": {
    "zh": "## 请求说明\n通过调用`DisablePolarClawChannel` API，用户可以禁用特定应用程序下的某个PolarClaw通道。在执行此操作前，请确保您已知目标通道的ID以及所属应用的ApplicationId。",
    "en": "## Description\n\nCall the`DisablePolarClawChannel` API to disable a PolarClaw channel for a specific application. Before you perform this operation, make sure you have the target channel ID and the application ID."
  },
  "example": {
    "unifiedCli": "aliyun polardb disable-polar-claw-channel --application-id pa-00000000000000 --channel-id feishu --restart true --region cn-hangzhou",
    "legacyCli": "aliyun polardb DisablePolarClawChannel --region cn-hangzhou --ApplicationId 'pa-00000000000000' --ChannelId feishu --Restart true"
  }
}