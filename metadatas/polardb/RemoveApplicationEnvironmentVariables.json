{
  "name": "RemoveApplicationEnvironmentVariables",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ApplicationId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID",
        "en": "The application ID."
      }
    },
    {
      "name": "Restart",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "删除后是否重启网关，默认true",
        "en": "Specifies whether to restart the gateway after removing the variables. The default is `true`."
      }
    },
    {
      "name": "VariableNames",
      "position": "Query",
      "type": "Json",
      "required": true,
      "description": {
        "zh": "要删除的环境变量名列表",
        "en": "A list of the environment variable names to remove."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除应用环境变量",
    "en": "RemoveApplicationEnvironmentVariables"
  },
  "example": {
    "unifiedCli": "aliyun polardb remove-application-environment-variables --application-id pa-00000000000000000000 --variable-names TEST_ENV --restart true --region cn-hangzhou",
    "legacyCli": "aliyun polardb RemoveApplicationEnvironmentVariables --region cn-hangzhou --VariableNames '[\"TEST_ENV\"]' --ApplicationId 'pa-00000000000000000000' --Restart true"
  }
}