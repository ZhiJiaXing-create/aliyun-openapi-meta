{
  "name": "DeleteAccount",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "账号名称。",
        "en": "The account name."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除数据库账号",
    "en": "DeleteAccount"
  },
  "descriptions": {
    "zh": "> 集群状态必须为运行中，否则操作将失败。",
    "en": "> The cluster must be in the running state for this operation to succeed."
  },
  "example": {
    "unifiedCli": "aliyun polardb delete-account --db-cluster-id pc-0000000000000 --account-name testacc --region cn-hangzhou",
    "legacyCli": "aliyun polardb DeleteAccount --region cn-hangzhou --DBClusterId 'pc-0000000000000' --AccountName testacc"
  }
}