{
  "name": "CloseDBClusterMigration",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ContinueEnableBinlog",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否继续打开Binlog，取值范围如下： \n* **true**：继续打开Binlog\n* **false**：关闭Binlog\n\n默认取值为**true**。 \n\n> 关闭Binlog将会重启您的PolarDB集群。",
        "en": "Specifies whether to continue to enable binary logging. Valid values:\n\n- **True**: continues to enable binary logging.\n\n- **False**: disables binary logging.\n\nDefault value: **True**.\n\n> If binary logging is disabled, your PolarDB cluster is restarted."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The ID of the cluster."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "取消或完成迁移",
    "en": "CloseDBClusterMigration"
  },
  "descriptions": {
    "zh": "* 在切换前调用该接口会取消迁移。\n* 在切换完成后调用该口会完成迁移。\n\n > 调用该接口时，集群必须已经创建了一键升级任务。创建一键升级任务的接口为[CreateDBCluster](~~98169~~)，参数**CreationOption**取值需为**MigrationFromRDS**。更多信息，请参见[一键升级RDS for MySQL到PolarDB MySQL版](~~121582~~)。",
    "en": "- Cancel the migration task before data migration.\n\n- Perform the migration task after data migration.\n\n> Before you call this operation, ensure that a one-click upgrade task has been created for the cluster. You can call the [CreateDBCluster](~~98169~~)Operation to create an upgrade task. Set the **CreationOption**Parameter to **MigrationFromRDS**. For more information, see [Create a PolarDB for MySQL cluster by using the Migration from RDS method](~~121582~~)."
  },
  "example": {
    "unifiedCli": "aliyun polardb close-db-cluster-migration --db-cluster-id pc-00000000000000 --continue-enable-binlog true --region cn-hangzhou",
    "legacyCli": "aliyun polardb CloseDBClusterMigration --region cn-hangzhou --DBClusterId 'pc-00000000000000' --ContinueEnableBinlog true"
  }
}