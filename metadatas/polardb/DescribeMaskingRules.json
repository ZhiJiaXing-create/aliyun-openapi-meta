{
  "name": "DescribeMaskingRules",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。\n> 您可以通过[DescribeDBClusters](~~98094~~)接口查看账号下所有集群的详细信息，包括集群ID。",
        "en": "The ID of the cluster.\n\n> You can call the [DescribeDBClusters](~~98094~~) operation to query the details of the clusters that belong to your Alibaba Cloud account, such as cluster IDs."
      }
    },
    {
      "name": "InterfaceVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "获取脱敏加密规则。取值范围如下：\n\nv1：获取脱敏规则。\nv2：获取加密规则。",
        "en": "Queries data masking rules or encryption rules. Valid values:\n\nv1: queries data masking rules. v2: queries data encryption rules."
      }
    },
    {
      "name": "RuleNameList",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "脱敏规则名称。",
        "en": "The name of the masking rule."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询脱敏规则列表",
    "en": "DescribeMaskingRules"
  },
  "example": {
    "unifiedCli": "aliyun polardb describe-masking-rules --db-cluster-id pc-00000000000000000 --rule-name-list testrule --interface-version v1 --region cn-hangzhou",
    "legacyCli": "aliyun polardb DescribeMaskingRules --region cn-hangzhou --InterfaceVersion v1 --RuleNameList testrule --DBClusterId 'pc-00000000000000000'"
  }
}