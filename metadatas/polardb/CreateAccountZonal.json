{
  "name": "CreateAccountZonal",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "账号备注说明，需满足如下要求：\n\n- 不能以`http://`或`https://`开头。\n\n- 长度为 2~256 个字符。",
        "en": "The description of the account. The description must meet the following requirements:\n\n- Cannot start with `http://` or `https://`.\n\n- Be 2 to 256 characters in length."
      }
    },
    {
      "name": "AccountName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "账号名称，需符合如下要求：\n\n- 以小写字母开头，以字母或数字结尾。\n\n- 由小写字母、数字或下划线组成。\n\n- 长度为 2~16 个字符。\n\n- 不能使用某些预留的用户名，如 root、admin 等。",
        "en": "The account name. The name must meet the following requirements:\n\n- Start with a lowercase letter and end with a letter or a digit.\n\n- Contain only lowercase letters, digits, and underscores (\\_).\n\n- Be 2 to 16 characters in length.\n\n- Cannot be a reserved username, such as root or admin."
      }
    },
    {
      "name": "AccountPassword",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "账号密码，需符合如下要求：\n\n- 至少包含大写字母、小写字母、数字或特殊字符中的任意三种。\n\n- 长度为 8~32 个字符。\n\n- 特殊字符为`!@#$%^&*()_+-=`。",
        "en": "The account password. The password must meet the following requirements:\n\n- Contain at least three of the following character types: uppercase letters, lowercase letters, digits, and special characters.\n\n- Be 8 to 32 characters in length.\n\n- Special characters are `!@#$%^&*()_+-=`."
      }
    },
    {
      "name": "AccountPrivilege",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "账号权限，取值范围如下：\n\n- ReadWrite：读写。\n\n- ReadOnly：只读。\n\n- DMLOnly：只允许 DML。\n\n- DDLOnly：只允许 DDL。\n\n- ReadIndex：只读+索引。",
        "en": "The permissions of the account. Valid values:\n\n- ReadWrite: read and write permissions.\n\n- ReadOnly: read-only permissions.\n\n- DMLOnly: DML permissions only.\n\n- DDLOnly: DDL permissions only.\n\n- ReadIndex: read and index permissions."
      }
    },
    {
      "name": "AccountType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "账号类型，取值范围如下：\n\n- Normal：普通账号。\n\n- Super：高权限账号。",
        "en": "The account type. Valid values:\n\n- Normal: a standard account.\n\n- Super: a privileged account."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，保证在不同请求间唯一，大小写敏感、不超过 64 个 ASCII 字符。",
        "en": "A client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token is case-sensitive and can contain a maximum of 64 ASCII characters."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群 ID。",
        "en": "The cluster ID."
      }
    },
    {
      "name": "DBName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "允许目标账号访问的数据库名称，支持传入多个数据库名称，多个数据库名称间用英文逗号（,）分隔。",
        "en": "The name of the database that the destination account can access. You can specify multiple database names. Separate them with commas (,)."
      }
    },
    {
      "name": "NodeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "节点类型。",
        "en": "The node type."
      }
    },
    {
      "name": "PrivForAllDB",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否授权当前集群所有库及后续新增所有库的权限。取值范围如下：\n\n- 0 或不填：不授权。\n\n- 1：授权。",
        "en": "Specifies whether to grant permissions on all current and future databases in the cluster. Valid values:\n\n- 0 or empty: Does not grant permissions.\n\n- 1: Grants permissions."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建边缘集群账号",
    "en": "CreateAccountZonal"
  },
  "example": {
    "unifiedCli": "aliyun polardb create-account-zonal --db-cluster-id pc-00000000000000 --account-name testacc --account-password Test1111 --account-type Normal --account-description testdes --db-name testdb --account-privilege ReadWrite --client-token 6000170000591aed949d0f54a343f1a4233c1e7d1c5c000000 --priv-for-all-db 0 --node-type Normal --region cn-hangzhou",
    "legacyCli": "aliyun polardb CreateAccountZonal --region cn-hangzhou --AccountDescription testdes --AccountPrivilege ReadWrite --NodeType Normal --DBClusterId 'pc-00000000000000' --DBName testdb --ClientToken 6000170000591aed949d0f54a343f1a4233c1e7d1c5c000000 --AccountType Normal --AccountName testacc --AccountPassword Test1111 --PrivForAllDB 0"
  }
}