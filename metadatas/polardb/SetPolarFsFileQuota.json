{
  "name": "SetPolarFsFileQuota",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    },
    {
      "name": "FilePathQuotas",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "目录应用规则详情",
        "en": "An array of objects defining the file quota rules for specific directories."
      },
      "sub_parameters": [
        {
          "name": "Capacity",
          "position": "Query",
          "type": "Long",
          "required": false,
          "description": {
            "zh": "配额容量，单位G",
            "en": "The capacity quota in GB."
          }
        },
        {
          "name": "FilePathId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "目录",
            "en": "The absolute path of the directory."
          }
        },
        {
          "name": "Inodes",
          "position": "Query",
          "type": "Long",
          "required": false,
          "description": {
            "zh": "配额Inodes",
            "en": "The inode quota."
          }
        },
        {
          "name": "MaxDepth",
          "position": "Query",
          "type": "Integer",
          "required": false,
          "description": {
            "zh": "对于`FilePathId`下的目录遍历几层，1表示遍历的一级子目录，0表示遍历到最深",
            "en": "The maximum depth of subdirectories to traverse under the path specified by `FilePathId`. A value of 1 traverses only the first level of subdirectories. A value of 0 traverses to the deepest level."
          }
        },
        {
          "name": "QuotaIds",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "待应用的规则ID列表，用`,`分隔",
            "en": "A list of file quota rule IDs, separated by a comma (`,`)."
          }
        },
        {
          "name": "Strategy",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "规则对现有文件的生效模式，取值如下：\n- **missing**：仅缺失时生效，默认值\n- **all**：全部生效",
            "en": "Specifies how to apply the rule to existing files. Valid values:\n\n- **missing**: Applies the rule only if one does not already exist. (Default)\n\n- **all**: Applies the rule to all files."
          }
        }
      ]
    },
    {
      "name": "PolarFsInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Polarlakebase实例ID",
        "en": "The ID of the PolarFS instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为目录应用配额规则",
    "en": "SetPolarFsFileQuota"
  },
  "descriptions": {
    "zh": "## 请求说明\n本接口用于为Polarlakebase实例中的特定目录设置文件配额规则。通过配置`FilePathQuotas`参数，可以指定要应用配额规则的具体目录及其相关属性。注意，当使用`FilePathId`指定目录时，请确保路径是从根目录出发的绝对路径而非挂载点路径。此外，可以通过`Strategy`参数控制规则对现有文件的生效模式，默认情况下仅在规则缺失时生效。\n\n### 特别提示\n- `FilePathQuotas`列表的最大长度限制为21。\n- 如果需要对`FilePathId`下的多层子目录应用规则，请设置`MaxDepth`参数；若希望遍历至最深层级，则将此值设为0。",
    "en": "## Request\n\nThis operation sets file quota rules for specific directories in a PolarFS instance. Use the `FilePathQuotas` parameter to define the target directories and their properties. The path for `FilePathId` must be an absolute path from the root directory, not from a mount target. The `Strategy` parameter controls how the rule applies to existing files. By default, the operation applies a rule only if one does not already exist.\n\n### Notes\n\n- The `FilePathQuotas` list can contain up to 21 items.\n\n- To apply the rule to subdirectories at multiple levels under `FilePathId`, set the `MaxDepth` parameter. To traverse to the deepest level, set this value to 0."
  },
  "example": {
    "unifiedCli": "aliyun polardb set-polar-fs-file-quota --polar-fs-instance-id pfs-2ze0i74ka60700000 --file-path-quotas QuotaIds=1,2 MaxDepth=1 Capacity=100 FilePathId=/a/project Strategy=missing Inodes=100 --db-cluster-id pc-00000000000000 --region cn-hangzhou",
    "legacyCli": "aliyun polardb SetPolarFsFileQuota --region cn-hangzhou --FilePathQuotas.1.MaxDepth 1 --FilePathQuotas.1.FilePathId '/a/project' --FilePathQuotas.1.Inodes 100 --PolarFsInstanceId 'pfs-2ze0i74ka60700000' --FilePathQuotas.1.Strategy missing --DBClusterId 'pc-00000000000000' --FilePathQuotas.1.Capacity 100 --FilePathQuotas.1.QuotaIds '1,2'"
  }
}