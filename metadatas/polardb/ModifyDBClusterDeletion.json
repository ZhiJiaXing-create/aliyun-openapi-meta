{
  "name": "ModifyDBClusterDeletion",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目标集群ID。\n\n> 您可以通过[DescribeDBClusters](~~98094~~)接口查看集群ID。",
        "en": "The ID of the target cluster.\n\n> Call the [DescribeDBClusters](~~98094~~) operation to find the cluster ID."
      }
    },
    {
      "name": "Protection",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "开启或者关闭集群保护锁，默认值为false，取值：\n\n- **true**：开启集群保护锁，开启后不能直接释放该集群，需要关闭集群保护锁后重新释放。\n- **false**：关闭集群保护锁。",
        "en": "Specifies whether to enable or disable the cluster lock. The default value is false. Valid values:\n\n- **true**: Enables the cluster lock. When this feature is enabled, the cluster cannot be released. To release the cluster, first disable the cluster lock.\n\n- **false**: Disables the cluster lock."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "开启或者关闭集群保护锁",
    "en": "ModifyDBClusterDeletion"
  },
  "example": {
    "unifiedCli": "aliyun polardb modify-db-cluster-deletion --db-cluster-id pc-bp1313h70cd5m0000 --protection true --region cn-hangzhou",
    "legacyCli": "aliyun polardb ModifyDBClusterDeletion --region cn-hangzhou --DBClusterId 'pc-bp1313h70cd5m0000' --Protection true"
  }
}