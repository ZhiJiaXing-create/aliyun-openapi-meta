{
  "name": "CreateAccessKey",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserPrincipalName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "RAM用户的登录名称。\n\n如果为空，默认为当前用户创建访问密钥。",
        "en": "The logon name of the RAM user.\n\nIf this parameter is empty, an AccessKey pair is created for the current user."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建主账号或RAM用户访问密钥",
    "en": "CreateAccessKey"
  },
  "example": {
    "unifiedCli": "aliyun ims create-access-key --user-principal-name test@example.onaliyun.com --region cn-hangzhou",
    "legacyCli": "aliyun ims CreateAccessKey --region cn-hangzhou --UserPrincipalName 'test@example.onaliyun.com'"
  }
}