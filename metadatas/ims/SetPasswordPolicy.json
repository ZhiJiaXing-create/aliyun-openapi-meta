{
  "name": "SetPasswordPolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "HardExpire",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "密码过期后是否限制登录。取值：\n\n- true：密码过期后，不能登录控制台。需要通过主账号或具有管理员权限的RAM用户重置该RAM用户的密码后，才能正常登录。\n- false（默认值）：密码过期后，RAM用户可以自行更改密码，然后正常登录。",
        "en": "Specifies whether logon is blocked after a password expires.\n\n- true: After a password expires, the RAM user cannot log on to the console. An Alibaba Cloud account owner or a RAM administrator must reset the password before the RAM user can log on.\n\n- false (default): After a password expires, the RAM user can change the password and then log on."
      }
    },
    {
      "name": "InitialPasswordAge",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "初始密码有效期。初始密码指新创建或重新启用控制台登录设置的密码。\n\n取值范围：0-90。单位：天。\n\n默认值：14。\n\n0表示不开启初始密码有效期限制。",
        "en": "Validity period for initial passwords. Initial passwords apply to newly created RAM users or users whose console logon settings are re-enabled.\n\nValid values: 0 to 90. Unit: days.\n\nDefault value: 14.\n\nA value of 0 disables this constraint."
      }
    },
    {
      "name": "InterceptRiskPasswordOnApi",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "使用OpenAPI设置密码时，是否拦截风险密码的开关。\n\n取值：\n- true：使用OpenAPI设置密码时，新密码不得为风险密码。\n- false：使用OpenAPI设置密码时，新密码可以为风险密码。\n\n默认值：false",
        "en": "Specifies whether threat passwords are blocked when set using APIs.\n\nDefault value: false\n\n- true\n\n- false (default)"
      }
    },
    {
      "name": "MaxLoginAttemps",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "密码重试次数约束。连续输入错误密码达到设定次数后，账号将被锁定一小时。\n\n取值范围：0~32。\n\n默认值：0，表示不启用密码重试约束。",
        "en": "Maximum number of failed password attempts. After the specified number of consecutive incorrect password attempts, the account is locked for one hour.\n\nValid values: 0 to 32.\n\nDefault value: 0, which disables this constraint."
      }
    },
    {
      "name": "MaxPasswordAge",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "密码有效期。\n\n取值范围：0~1095。单位：天。\n\n默认值：0，表示永不过期。",
        "en": "Password validity period.\n\nValid values: 0 to 1095. Unit: days.\n\nDefault value: 0, which means passwords never expire."
      }
    },
    {
      "name": "MinimumPasswordDifferentCharacter",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "密码中最少包含的不同字符数量。\n\n取值范围：0~8。\n\n默认值：0，表示不限制密码中的不同字符数量。",
        "en": "Minimum number of unique characters in a password.\n\nValid values: 0 to 8.\n\nDefault value: 0, which imposes no restriction."
      }
    },
    {
      "name": "MinimumPasswordLength",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "最小密码长度。\n\n取值范围：8~32。默认值：8。",
        "en": "Minimum password length.\n\nValid values: 8 to 32.\n\nDefault value: 8."
      }
    },
    {
      "name": "PasswordNotContainUserName",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "密码中是否不允许包含用户名。取值：\n\n- true：密码中不能包含用户名。\n- false（默认值）：密码中可以包含用户名。",
        "en": "Specifies whether passwords must not contain the user name.\n\n- true\n\n- false (default)"
      }
    },
    {
      "name": "PasswordReusePrevention",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "历史密码检查策略。\n\n禁止使用前N次的历史密码，N的取值范围：0~24。\n\n默认值：0，表示不启用历史密码检查策略。",
        "en": "Prevents reuse of previous passwords.\n\nValid values: 0 to 24. This value specifies how many previous passwords are blocked from reuse.\n\nDefault value: 0, which disables this constraint."
      }
    },
    {
      "name": "RequireLowercaseCharacters",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "密码中是否必须包含小写字母。取值：\n\n- true\n- false（默认值）",
        "en": "Specifies whether passwords must contain lowercase letters.\n\n- true\n\n- false (default)"
      }
    },
    {
      "name": "RequireNumbers",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "密码中是否必须包含数字。取值：\n\n- true\n- false（默认值）",
        "en": "Specifies whether passwords must contain numbers.\n\n- true\n\n- false (default)"
      }
    },
    {
      "name": "RequireSymbols",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "密码中是否必须包含符号。取值：\n\n- true\n- false（默认值）",
        "en": "Specifies whether passwords must contain special characters.\n\n- true\n\n- false (default)"
      }
    },
    {
      "name": "RequireUppercaseCharacters",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "密码中是否必须包含大写字母。取值：\n\n- true\n- false（默认值）",
        "en": "Specifies whether passwords must contain uppercase letters.\n\n- true\n\n- false (default)"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置RAM用户密码强度策略",
    "en": "SetPasswordPolicy"
  },
  "example": {
    "unifiedCli": "aliyun ims set-password-policy --minimum-password-length 8 --require-lowercase-characters false --require-uppercase-characters false --require-numbers false --require-symbols false --hard-expire false --max-login-attemps 0 --password-reuse-prevention 0 --max-password-age 0 --minimum-password-different-character 0 --password-not-contain-user-name false --initial-password-age 14 --intercept-risk-password-on-api false --region cn-hangzhou",
    "legacyCli": "aliyun ims SetPasswordPolicy --region cn-hangzhou --MinimumPasswordLength 8 --RequireLowercaseCharacters false --HardExpire false --RequireNumbers false --MaxLoginAttemps 0 --PasswordReusePrevention 0 --MaxPasswordAge 0 --PasswordNotContainUserName false --InterceptRiskPasswordOnApi false --RequireUppercaseCharacters false --InitialPasswordAge 14 --MinimumPasswordDifferentCharacter 0 --RequireSymbols false"
  }
}