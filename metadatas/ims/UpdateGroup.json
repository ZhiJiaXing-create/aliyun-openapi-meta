{
  "name": "UpdateGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GroupName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户组名称。",
        "en": "The name of the RAM user group."
      }
    },
    {
      "name": "NewComments",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "新的备注。\n\n最大长度128个字符。",
        "en": "The new description.\n\nThe description can be up to 128 characters in length."
      }
    },
    {
      "name": "NewDisplayName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "新的用户组显示名称。\n\n最大长度24个字符。",
        "en": "The new display name of the RAM user group.\n\nThe name can be up to 24 characters in length."
      }
    },
    {
      "name": "NewGroupName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "新的用户组名称。\n\n最大长度64个字符，可包含英文字母、数字、英文句点（.）、下划线（_）和短划线（-）。",
        "en": "The new name of the RAM user group.\n\nThe name can be up to 64 characters in length and can contain letters, digits, periods (.), underscores (\\_), and hyphens (-)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改指定用户组的信息",
    "en": "UpdateGroup"
  },
  "example": {
    "unifiedCli": "aliyun ims update-group --new-comments '测试团队' --new-display-name Test-Team --new-group-name Test-Team --group-name Dev-Team --region cn-hangzhou",
    "legacyCli": "aliyun ims UpdateGroup --region cn-hangzhou --GroupName 'Dev-Team' --NewDisplayName 'Test-Team' --NewGroupName 'Test-Team' --NewComments '测试团队'"
  }
}