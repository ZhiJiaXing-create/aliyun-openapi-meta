{
  "name": "ListAccessKeys",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserPrincipalName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "RAM用户的登录名称。\n\n如果为空，默认查询当前用户的所有访问密钥。",
        "en": "The logon name of the RAM user.\n\nIf this parameter is empty, the AccessKey pairs of the current user are queried."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询主账号或RAM用户访问密钥列表",
    "en": "ListAccessKeys"
  },
  "example": {
    "unifiedCli": "aliyun ims list-access-keys --user-principal-name test@example.onaliyun.com --region cn-hangzhou",
    "legacyCli": "aliyun ims ListAccessKeys --region cn-hangzhou --UserPrincipalName 'test@example.onaliyun.com'"
  }
}