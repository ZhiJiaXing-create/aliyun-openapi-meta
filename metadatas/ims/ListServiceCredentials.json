{
  "name": "ListServiceCredentials",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllUsers",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否查询云账号下所有RAM用户的服务凭证。\n\n值为true时，不能同时指定UserPrincipalName",
        "en": "Specifies whether to query service credentials for all Resource Access Management (RAM) users under the Alibaba Cloud account.\n\nIf this parameter is set to true, you cannot specify UserPrincipalName at the same time."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页的最大数据条数。\n取值范围1~100，默认值10。",
        "en": "The maximum number of entries per page. Valid values: 1 to 100. Default value: 10."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询返回结果下一页的令牌。\n首次调用 API 不需要该字段。",
        "en": "The token that is used to retrieve the next page of results. You do not need to specify this parameter for the first API call."
      }
    },
    {
      "name": "ServiceName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "阿里云服务名称",
        "en": "The service name of the Alibaba Cloud service."
      }
    },
    {
      "name": "UserPrincipalName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "RAM用户登录名称。\n\n查询指定RAM用户的服务凭证。",
        "en": "The logon name of the Resource Access Management (RAM) user.\n\nQueries the service credentials of the specified RAM user."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取服务凭证列表",
    "en": "Retrieve service credential list"
  },
  "example": {
    "unifiedCli": "aliyun ims list-service-credentials --all-users true --user-principal-name test@example.onaliyun.com --service-name xxx.aliyuncs.com --max-results 20 --next-token EXAMPLE0000000 --region cn-hangzhou",
    "legacyCli": "aliyun ims ListServiceCredentials --region cn-hangzhou --NextToken EXAMPLE0000000 --ServiceName 'xxx.aliyuncs.com' --MaxResults 20 --UserPrincipalName 'test@example.onaliyun.com' --AllUsers true"
  }
}