{
  "name": "DeleteSAMLProvider",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "SAMLProviderName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要删除的身份提供商名称。",
        "en": "The name of the IdP that you want to delete."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除指定的角色SSO身份提供商",
    "en": "DeleteSAMLProvider"
  },
  "example": {
    "unifiedCli": "aliyun ims delete-saml-provider --saml-provider-name test-provider --region cn-hangzhou",
    "legacyCli": "aliyun ims DeleteSAMLProvider --region cn-hangzhou --SAMLProviderName 'test-provider'"
  }
}