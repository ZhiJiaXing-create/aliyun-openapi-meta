{
  "name": "ListUsersInRecycleBin",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Filter",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "过滤条件，查询回收站中指定RAM用户登录名的信息。\n\n格式：`UserPrincipalName eq <username>@<AccountAlias>.onaliyun.com`。",
        "en": "The filter condition that is used to query information about a specified RAM user in the recycle bin.\n\nYou must specify this parameter in the `UserPrincipalName eq <username>@<AccountAlias>.onaliyun.com` format."
      }
    },
    {
      "name": "Marker",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "当请求的返回结果被截断时，可以使用`Marker`获取从当前截断位置之后的内容。",
        "en": "The `marker`. If part of a previous response is truncated, you can use this parameter to obtain the truncated part."
      }
    },
    {
      "name": "MaxItems",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "返回结果的条数。当返回结果达到`MaxItems`限制被截断时，返回参数`IsTruncated`将等于true。 \n\n取值范围：1~100。默认值：100。",
        "en": "The number of entries to return. If a response is truncated because it reaches the value of `MaxItems`, the value of `IsTruncated` will be true.\n\nValid values: 1 to 100. Default value: 100."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询回收站中的所有RAM用户",
    "en": "ListUsersInRecycleBin"
  },
  "example": {
    "unifiedCli": "aliyun ims list-users-in-recycle-bin --marker EXAMPLE --max-items 100 --filter UserPrincipalName_eq_test@example.onaliyun.com --region cn-hangzhou",
    "legacyCli": "aliyun ims ListUsersInRecycleBin --region cn-hangzhou --Filter 'UserPrincipalName_eq_test@example.onaliyun.com' --Marker EXAMPLE --MaxItems 100"
  }
}