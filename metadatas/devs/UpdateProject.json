{
  "name": "UpdateProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/2023-07-14/projects/[name]",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "计划更新的项目实体内容。您指定的可写字段将被更新，未指定的字段将保持不变",
        "en": "The project entity to update. Only specified writable fields are updated; unspecified fields remain unchanged."
      }
    },
    {
      "name": "name",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "对象名称",
        "en": "The name of the project."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改项目",
    "en": "Modify a project"
  },
  "descriptions": {
    "zh": "1. 使用修改（Update）相关的接口时，您指定的可写字段将被更新，未指定的字段将保持不变\n2. 当且仅当模板参数（spec.templateConfig.parameters）发生变化时，才会触发自动部署\n3. 使用环境（Environment）相关API修改默认环境（Default）配置后，不再支持更新项目",
    "en": "1. Only the specified writable fields are updated. Unspecified fields remain unchanged.\n\n2. An automatic deployment is triggered only if the template parameters (spec.templateConfig.parameters) change.\n\n3. A project can no longer be updated after its default environment configuration is modified using environment-related APIs."
  }
}