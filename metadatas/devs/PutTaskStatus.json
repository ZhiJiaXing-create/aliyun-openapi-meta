{
  "name": "PutTaskStatus",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/2023-07-14/tasks/[name]/status",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "流水线任务",
        "en": "The pipeline task object."
      }
    },
    {
      "name": "force",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "强制更新",
        "en": "Specifies whether to force the update, which ignores the resource version check."
      }
    },
    {
      "name": "name",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "对象名称",
        "en": "The pipeline task name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "替换流水线任务执行状态",
    "en": "Replace the execution status of a pipeline Job"
  },
  "descriptions": {
    "zh": "API提供了乐观锁机制，在不设置强制更新的场景下，需要基于最新的流水线触发器配置修改后提交，否则将触发提交冲突异常。",
    "en": "This API uses optimistic locking to prevent conflicting updates. When you update a pipeline task, your request must be based on the most recent resource version. If a newer version exists, the update fails unless you set the `force` parameter to true."
  }
}