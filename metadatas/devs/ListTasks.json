{
  "name": "ListTasks",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/2023-07-14/tasks",
  "parameters": [
    {
      "name": "labelSelector",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "利用实体的labels字段进行筛选。精确匹配labelSelector数组中的所有条件，不同条件之间是“且”的关系",
        "en": "The labels used to filter the results. This operation returns only the tasks that match all specified labels. These labels are combined with a logical AND."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量查询流水线任务执行",
    "en": "Batch query pipeline task executions"
  }
}