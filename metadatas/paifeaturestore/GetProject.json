{
  "name": "GetProject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/instances/[InstanceId]/projects/[ProjectId]",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID，可通过接口ListInstances获取。",
        "en": "The ID of the instance. Call the `ListInstances` operation to get this ID."
      }
    },
    {
      "name": "ProjectId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "项目ID，可通过接口ListProjects获取。",
        "en": "The ID of the project. Call the `ListProjects` operation to get this ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取项目详细信息",
    "en": "Get Project Details"
  },
  "example": {
    "unifiedCli": "aliyun paifeaturestore get-project --instance-id fs-cn-00000000 --project-id 4 --region cn-hangzhou",
    "legacyCli": "aliyun paifeaturestore GET /api/v1/instances/fs-cn-00000000/projects/4 --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}