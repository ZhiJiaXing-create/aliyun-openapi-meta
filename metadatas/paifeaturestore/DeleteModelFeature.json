{
  "name": "DeleteModelFeature",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v1/instances/[InstanceId]/modelfeatures/[ModelFeatureId]",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID，可从接口ListInstances获取。",
        "en": "The ID of the instance. You can call the `ListInstances` operation to get this ID."
      }
    },
    {
      "name": "ModelFeatureId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "模型特征ID，可从接口ListModelFeatures获取。",
        "en": "The ID of the model feature. You can call the `ListModelFeatures` operation to get this ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除模型特征",
    "en": "Delete model feature"
  },
  "example": {
    "unifiedCli": "aliyun paifeaturestore delete-model-feature --instance-id fs-cn-00000000 --model-feature-id 4 --region cn-hangzhou",
    "legacyCli": "aliyun paifeaturestore DELETE /api/v1/instances/fs-cn-00000000/modelfeatures/4 --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}