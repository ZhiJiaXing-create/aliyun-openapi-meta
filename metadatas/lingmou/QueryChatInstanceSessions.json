{
  "name": "QueryChatInstanceSessions",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/openapi/chat/instances/[instanceId]/sessions",
  "parameters": [
    {
      "name": "instanceId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "购买开通的实例id，可在订单页查看。",
        "en": "The ID of the chat instance. You can find the instance ID on the order page."
      }
    },
    {
      "name": "sessionIds",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "指定查询的会话id列表。",
        "en": "A list of session IDs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询实例下的活跃会话",
    "en": "Query Active Sessions Under an Instance"
  },
  "descriptions": {
    "zh": "查询指定实例下的活跃会话信息。",
    "en": "Retrieves information about active sessions for a specified instance."
  }
}