{
  "name": "CredentialSubmitIntl",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CredentialOcrPictureBase64",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "图片Base64编码。如果您选择此方式传入照片，请注意检查照片大小，不要传入过大的照片。",
        "en": "The Base64-encoded image. If you use this method to submit a photo, check the photo size and do not submit an excessively large photo."
      }
    },
    {
      "name": "CredentialOcrPictureUrl",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "图片地址，公网可访问的HTTP、HTTPS链接。",
        "en": "The URL of the image. The URL must be a publicly accessible HTTP or HTTPS link."
      }
    },
    {
      "name": "DocType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "凭证类型：\n- 02：车辆登记证明",
        "en": "The credential type. Valid values:\n- 02: vehicle registration certificate."
      }
    },
    {
      "name": "FraudCheck",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "是否开启篡改检测\n- true：开启\n- false：关闭",
        "en": "Specifies whether to enable tampering detection. Valid values:\n- true: Enable.\n- false: Disable."
      }
    },
    {
      "name": "MerchantBizId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "商户侧自定义的业务唯一标识，用于后续定位排查问题使用。支持字母和数字的组合，长度最长32位，请确保唯一。",
        "en": "The merchant-side custom business unique identifier, used for subsequent troubleshooting. The value can be a combination of letters and digits with a maximum length of 32 characters. Ensure that the value is unique."
      }
    },
    {
      "name": "OcrArea",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "提取类型：\n\n- 0201：泰国车辆登记证明",
        "en": "The extraction type. Valid values:\n\n- 0201: Thailand vehicle registration certificate."
      }
    },
    {
      "name": "ProductCode",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要接入的产品方案。取值CREDENTIAL_RECOGNITION。",
        "en": "The product solution to use. Set this parameter to CREDENTIAL_RECOGNITION."
      }
    },
    {
      "name": "SceneCode",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "您自定义的认证场景ID，用于后续控制台输入此场景ID查询相关记录使用。支持长度为10位的字母、数字或下划线的组合。",
        "en": "The custom authentication scenario ID. You can use this scenario ID to query related records in the console. The value can be a combination of letters, digits, or underscores with a maximum length of 10 characters."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "凭证识别任务提交（异步）",
    "en": "Submit credential recognition task (asynchronous)"
  },
  "descriptions": {
    "zh": "凭证识别OCR初始化接口，通过本接口获取transactionId。",
    "en": "Initializes the credential recognition OCR operation and retrieves a transactionId."
  }
}