{
  "name": "Mobile2MetaVerifyIntl",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Mobile",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "手机号码。\n\n- 当paramType取值normal时，输入明文\n- 当paramType取值md5时，传入手机号码密文，输入32位小写md5字符串",
        "en": "The phone number.\n\n- If ParamType is set to normal, pass in the phone number in plaintext.\n- If ParamType is set to md5, pass in the phone number in ciphertext as a 32-character lowercase MD5 string."
      }
    },
    {
      "name": "ParamType",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "参数类型：\n\n- normal：不加密\n- md5：md5加密",
        "en": "The parameter type. Valid values:\n\n- normal: not encrypted\n- md5: MD5-encrypted."
      }
    },
    {
      "name": "ProductCode",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "产品code(固定值：MOBILE_2META)",
        "en": "The product code. Set this parameter to MOBILE_2META."
      }
    },
    {
      "name": "UserName",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "姓名：\n\n- paramType 传 normal 时：传入姓名明文。\n- paramType 传 md5 时：传入姓名密文（输入32位小写md5字符串）。",
        "en": "The name. Valid values:\n\n- If ParamType is set to normal, pass in the name in plaintext.\n- If ParamType is set to md5, pass in the name in ciphertext as a 32-character lowercase MD5 string."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "手机号二要素核验",
    "en": "Two-factor phone number verification"
  },
  "descriptions": {
    "zh": "传入手机号、姓名，通过权威数据源验证其真实性和一致性。请确保在使用该接口前，已充分了解手机二要素产品的收费方式和价格",
    "en": "Pass in a phone number and name to verify their authenticity and consistency through an authoritative data source. Before using this operation, make sure that you fully understand the billing method and pricing of the two-factor phone number verification service."
  }
}