{
  "name": "DeleteNamespace",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/pop/v1/paas/namespace",
  "parameters": [
    {
      "name": "NameSpaceShortId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "短版命名空间ID，不需要带上Region。推荐使用该参数。",
        "en": "The short-format ID of the namespace. You do not need to specify the region. We recommend that you use this parameter."
      }
    },
    {
      "name": "NamespaceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "长版命名空间ID。如果设置了该参数会继续生效而忽略NameSpaceShortId，该参数是为了兼容保留，推荐使用短版命名空间ID，简化参数。",
        "en": "The long-format ID of the namespace. If you specify this parameter, NameSpaceShortId is ignored. This parameter is for backward compatibility. We recommend that you use the short-format namespace ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除命名空间",
    "en": "DeleteNamespace"
  },
  "example": {
    "unifiedCli": "aliyun sae delete-namespace --namespace-id cn-beijing:test --name-space-short-id test --region cn-hangzhou",
    "legacyCli": "aliyun sae DELETE /pop/v1/paas/namespace --region cn-hangzhou --NameSpaceShortId test --NamespaceId 'cn-beijing:test' --header \"Content-Type=application/json\""
  }
}