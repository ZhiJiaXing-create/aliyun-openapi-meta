{
  "name": "BindNlb",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/pop/v1/sam/app/nlb",
  "parameters": [
    {
      "name": "AddressType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地址类型：\n- Internet：公网地址。\n- Intranet：私网地址。",
        "en": "The address type of the NLB instance.\n\n- `Internet`: a public IP address.\n\n- `Intranet`: a private IP address."
      }
    },
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "目标应用ID。",
        "en": "The ID of the target application."
      }
    },
    {
      "name": "Listeners",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "传入要操作的Listeners的JSON数组串，每个Listener的具体结构描述如下：\n\n- **Port**，Integer类型，必选，负载均衡监听端口。取值范围：0~65535。\n\n- **TargetPort**，Integer类型，必选，实例容器监听端口。取值范围：0~65535。\n\n- **Protocol**，String类型，必选，监听协议。取值：TCP、UDP 或 TCPSSL。\n\n- **CertIds**，String类型，可选，服务器证书列表信息。仅对 TCPSSL 监听生效。",
        "en": "The listeners, specified as a JSON-formatted string. Each listener object contains the following fields:\n\n- **Port**: Integer. Required. The listener port. Valid values: 0 to 65535.\n\n- **TargetPort**: Integer. Required. The port on the application instance that receives traffic. Valid values: 0 to 65535.\n\n- **Protocol**: String. Required. The listener protocol. Valid values: `TCP`, `UDP`, and `TCPSSL`.\n\n- **CertIds**: String. Optional. The server certificate IDs. This parameter is required only for `TCPSSL` listeners."
      }
    },
    {
      "name": "NlbId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "NLB实例ID",
        "en": "The ID of the NLB instance."
      }
    },
    {
      "name": "ZoneMappings",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "可用区及交换机映射列表的JSON字符串。最多支持添加 10 个可用区。若当前地域支持 2 个及以上的可用区，至少需要添加 2 个可用区。一个ZoneMapping结构为：\n\n- VSwitchId，String类型，可用区对应的交换机，每个可用区只能使用一台交换机和一个子网。\n\n- ZoneId，String类型，网络型负载均衡实例的可用区 ID。",
        "en": "The mappings between zones and vSwitches, specified as a JSON-formatted string. You can add up to 10 zones. If the current region supports two or more zones, you must specify at least two zones. Each `ZoneMapping` object contains the following fields:\n\n- **VSwitchId**: String. The ID of the vSwitch in the specified zone. Each zone can have only one vSwitch and one subnet.\n\n- ZoneId, String, the zone ID of the Network Load Balancer instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为应用绑定NLB",
    "en": "BindNlb"
  },
  "example": {
    "unifiedCli": "aliyun sae bind-nlb --app-id 7171a6ca-d1cd-4928-8642-7d5cfe690000 --nlb-id nlb-7z7jjbzz44d82c9000 --listeners example-string --zone-mappings vsw-sersdf0000_cn-hangzhou-a --address-type Internet --region cn-hangzhou",
    "legacyCli": "aliyun sae POST /pop/v1/sam/app/nlb --region cn-hangzhou --NlbId 'nlb-7z7jjbzz44d82c9000' --Listeners 'example-string' --AppId '7171a6ca-d1cd-4928-8642-7d5cfe690000' --ZoneMappings 'vsw-sersdf0000_cn-hangzhou-a' --AddressType Internet --header \"Content-Type=application/json\""
  }
}