{
  "name": "DescribeNamespaceList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v1/sam/namespace/describeNamespaceList",
  "parameters": [
    {
      "name": "ContainCustom",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否返回自定义命名空间。取值说明如下：\n\n- **true**：返回自定义命名空间。\n- **false**：不返回自定义命名空间。",
        "en": "Specifies whether to return custom namespaces. Valid values:\n\n- **true**: The system returns custom namespaces.\n\n- **false**: The system does not return custom namespaces."
      }
    },
    {
      "name": "HybridCloudExclude",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否排除混合云命名空间。取值说明如下：\n\n- **true**：排除混合云命名空间。   \n- **false**：不排除混合云命名空间。",
        "en": "Indicates whether hybrid cloud namespaces are excluded. Valid values:\n\n- **true**: Hybrid cloud namespaces are excluded.\n\n- **false**: Hybrid cloud namespaces are included."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取命名空间列表",
    "en": "DescribeNamespaceList"
  },
  "example": {
    "unifiedCli": "aliyun sae describe-namespace-list --contain-custom true --hybrid-cloud-exclude true --region cn-hangzhou",
    "legacyCli": "aliyun sae GET /pop/v1/sam/namespace/describeNamespaceList --region cn-hangzhou --ContainCustom true --HybridCloudExclude true --header \"Content-Type=application/json\""
  }
}