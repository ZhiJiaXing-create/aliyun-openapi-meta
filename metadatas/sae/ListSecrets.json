{
  "name": "ListSecrets",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v1/sam/secret/secrets",
  "parameters": [
    {
      "name": "NamespaceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Secret实例所在命名空间ID。如果所在命名空间为默认命名空间，则只需填写RegionId，例如`cn-beijing`。",
        "en": "The ID of the namespace that contains the Secret. For the default namespace, use the region ID, such as `cn-beijing`."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "ListSecrets",
    "en": "ListSecrets"
  },
  "example": {
    "unifiedCli": "aliyun sae list-secrets --namespace-id cn-beijing:test --region cn-hangzhou",
    "legacyCli": "aliyun sae GET /pop/v1/sam/secret/secrets --region cn-hangzhou --NamespaceId 'cn-beijing:test' --header \"Content-Type=application/json\""
  }
}