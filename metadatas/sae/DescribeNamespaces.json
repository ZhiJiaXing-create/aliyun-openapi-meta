{
  "name": "DescribeNamespaces",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v1/paas/namespaces",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "当前页码。",
        "en": "The current page number."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "分页查询时设置的每页数量。取值范围\\[0,10000\\]。",
        "en": "The number of entries per page. Valid values: 0 to 10000."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询命名空间列表",
    "en": "DescribeNamespaces"
  },
  "example": {
    "unifiedCli": "aliyun sae describe-namespaces --current-page 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun sae GET /pop/v1/paas/namespaces --region cn-hangzhou --PageSize 10 --CurrentPage 1 --header \"Content-Type=application/json\""
  }
}