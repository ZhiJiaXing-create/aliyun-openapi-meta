{
  "name": "GetArmsTopNMetric",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v1/sam/getArmsTopNMetric",
  "parameters": [
    {
      "name": "AppSource",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "SAE应用类型。\n\n- **micro_service。**\n- **web。**\n- **job。**",
        "en": "The CPU allocation policy. Valid values:\n\n*   **request**: CPU cores are allocated only when a request is initiated.\n*   **always**: Fixed CPU cores are always allocated."
      }
    },
    {
      "name": "CpuStrategy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "CPU分配策略。\n\n- **request**：仅在有请求时分配CPU。\n- **always**：始终分配固定CPU。",
        "en": "The additional information that is returned. The following limits are imposed on the ID:\n\n*   success: If the call is successful, **success** is returned.\n*   An error code: If the call fails, an error code is returned."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "查询终止时间。",
        "en": "The SAE application type. Valid values:\n\n*   **micro_service**\n*   **web**\n*   **job**"
      }
    },
    {
      "name": "Limit",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "返回数据条数，取值范围[0,100]。",
        "en": "The beginning of the time range to query."
      }
    },
    {
      "name": "OrderBy",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "排序字段，根据查询返回字段中的某个字段排序。",
        "en": "The number of entries to return. Valid values: 0 to 100."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The field based on which you want to sort the returned entries."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "查询起始时间。",
        "en": "The end of the time range to query."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "GetArmsTopNMetric",
    "en": "GetArmsTopNMetric"
  },
  "example": {
    "unifiedCli": "aliyun sae get-arms-topn-metric --biz-region-id cn-hangzhou --order-by count --limit 10 --start-time 1675823135951 --end-time 1675824035951 --app-source micro_service --cpu-strategy always --region cn-hangzhou",
    "legacyCli": "aliyun sae GET /pop/v1/sam/getArmsTopNMetric --region cn-hangzhou --OrderBy count --EndTime 1675824035951 --CpuStrategy always --StartTime 1675823135951 --RegionId 'cn-hangzhou' --Limit 10 --AppSource micro_service --header \"Content-Type=application/json\""
  }
}