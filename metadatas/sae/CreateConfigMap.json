{
  "name": "CreateConfigMap",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/pop/v1/sam/configmap/configMap",
  "parameters": [
    {
      "name": "Data",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "ConfigMap数据。",
        "en": "The ID of the namespace to which the ConfigMap instance belongs."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "描述信息，不可输入空格，且不超过255个字符。",
        "en": "The key-value pairs of the ConfigMap in the JSON format. Format:\n\n{\"Data\":\"{\"k1\":\"v1\", \"k2\":\"v2\"}\"}\n\nk specifies a key and v specifies a value. For more information, see [Manage a Kubernetes ConfigMap](~~171326~~)."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "ConfigMap实例名称。允许数字、字母和下划线（_）组合，仅允许以字母开头。",
        "en": "The name of the ConfigMap. The name can contain digits, letters, and underscores (\\_). The name must start with a letter."
      }
    },
    {
      "name": "NamespaceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "ConfigMap实例所在命名空间ID。",
        "en": "The ID of the namespace to which the ConfigMap instance belongs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建ConfigMap实例",
    "en": "CreateConfigMap"
  },
  "example": {
    "unifiedCli": "aliyun sae create-config-map --name name --namespace-id cn-hangzhou --data '{env.home=/root, env.shell=/bin/sh}' --description test-desc --region cn-hangzhou",
    "legacyCli": "aliyun sae POST /pop/v1/sam/configmap/configMap --region cn-hangzhou --Description 'test-desc' --NamespaceId 'cn-hangzhou' --Name name --header \"Content-Type=application/json\" --body {\"env.home\":\"/root\",\"env.shell\":\"/bin/sh\"}"
  }
}