{
  "name": "ListApplications",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v1/sam/app/listApplications",
  "parameters": [
    {
      "name": "AppName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用名称。",
        "en": "The application name."
      }
    },
    {
      "name": "AppSource",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "SAE应用类型。\n\n- **micro_service。**\n- **web。**\n- **job。**",
        "en": "The Serverless App Engine (SAE) application type.\n\n- **micro_service.**\n- **web.**\n- **job.**"
      }
    },
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "当前页数。",
        "en": "The current page number."
      }
    },
    {
      "name": "FieldType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置筛选应用的维度。取值说明如下：\n\n- **appName**：应用名称。\n- **appIds**：应用ID。\n- **slbIps**：SLB IP地址。\n- **instanceIps**：实例IP地址。",
        "en": "The dimension by which to filter applications. Valid values:\n\n- **appName**: application name.\n- **appIds**: application ID.\n- **slbIps**: SLB IP address.\n- **instanceIps**: instance IP address."
      }
    },
    {
      "name": "FieldValue",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "输入目标应用的应用名称、应用ID、SLB IP地址或实例IP地址。",
        "en": "The application name, application ID, SLB IP address, or instance IP address of the target application."
      }
    },
    {
      "name": "IsStateful",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否为有状态应用。",
        "en": "Specifies whether the application is stateful."
      }
    },
    {
      "name": "NamespaceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "命名空间ID。",
        "en": "The namespace ID."
      }
    },
    {
      "name": "NewSaeVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用版本：\n\n- lite：轻量版\n- std：标准版\n- pro：专业版",
        "en": "The application version. Valid values:\n\n- lite: Lite Edition\n- std: Standard Edition\n- pro: Professional Edition"
      }
    },
    {
      "name": "OrderBy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "对应用进行排序。取值说明如下：\n\n- **runnings**：按照当前实例数进行排序。\n- **instances**：按照目标实例数进行排序。",
        "en": "The field by which to sort applications. Valid values:\n\n- **runnings**: sorts by the current number target instances.\n- **instances**: sorts by the target number target instances."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时获取的每页数量。取值范围\\[0,10000]。",
        "en": "The number of entries per page in a paging query. Valid values: \\[0,10000]."
      }
    },
    {
      "name": "ProgrammingLanguage",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "Reverse",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "根据应用实例运行状态对其进行排序。如果状态相同，则按照实例ID排序。取值说明如下：\n  - **true**：按升序排序。表示根据启动实例的步骤来排列，例如实例最终想要变更为运行状态，需先经过启动容器、拉取镜像、初始化实例等步骤。\n  - **false**：按降序排序。\n\n实例升序排列如下：\n\n1. **Error**：实例启动过程中出错。\n2. **CrashLoopBackOff**：容器无法启动，在启动过程中出错，重启后又出错。\n3. **ErrImagePull**：拉取实例中的容器镜像出错。\n4. **ImagePullBackOff**：无法获取容器镜像。\n5. **Pending**：实例等待调度中。\n6. **Unknown**：未知异常。\n7. **Terminating**：实例终止接入中。\n8. **NotFound**：无法找到实例。\n9. **PodInitializing**：实例初始化中。\n10. **Init:0/1**：实例初始化。\n11. **Running**：实例运行中。",
        "en": "Specifies whether to sort application instances by running status. If instances have the same status, they are sorted by instance ID. Valid values:\n  - **true**: sorts in ascending order. Instances are arranged based on the startup sequence. For example, to reach the running state, an instance must go through steps such as starting the container, pulling the image, and initializing the instance.\n  - **false**: sorts in descending order.\n\nThe ascending order of instances is as follows:\n\n1. **Error**: an error occurred during instance startup.\n2. **CrashLoopBackOff**: the container failed to start, encountered an error during startup, and encountered an error again after restart.\n3. **ErrImagePull**: an error occurred while pulling the container image for the instance.\n4. **ImagePullBackOff**: the container image cannot be obtained.\n5. **Pending**: the instance is waiting to be scheduled.\n6. **Unknown**: an unknown exception occurred.\n7. **Terminating**: the instance is being terminated.\n8. **NotFound**: the instance cannot be found.\n9. **PodInitializing**: the instance is being initialized.\n10. **Init:0/1**: the instance is initializing.\n11. **Running**: the instance is running."
      }
    },
    {
      "name": "Tags",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "标签键值对。取值说明如下：\n- **key**：标签键，长度范围\\[1,128]。\n- **value**：标签值，长度范围\\[1,128]。\n\n区分大小写。如果指定多个标签，则会为指定资源同时创建并绑定多个标签。同一个资源上的同一个标签键只能对应一个标签值。如果您尝试添加已有标签键，则对应的标签值会更新为新值。\n\n不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
        "en": "The tag key-value pairs. Valid values:\n- **key**: the tag key. The length must be in the range of \\[1,128].\n- **value**: the tag value. The length must be in the range of \\[1,128].\n\nTags are case-sensitive. If you specify multiple tags, all specified tags are created and attached to the resource. Each tag key on the same resource can have only one tag value. If you add a tag key that already exists, the corresponding tag value is updated to the new value.\n\nTags cannot start with `aliyun` or `acs:`, and cannot contain `http://` or `https://`."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取应用列表",
    "en": "List applications"
  },
  "example": {
    "unifiedCli": "aliyun sae list-applications --current-page 1 --page-size 20 --app-name demo-app --namespace-id cn-beijing:demo --tags key_:_key_,_value_:_value --order-by runnings --reverse true --field-type appName --field-value demo-app --app-source micro_service --is-stateful example-string --new-sae-version example-string --region cn-hangzhou",
    "legacyCli": "aliyun sae GET /pop/v1/sam/app/listApplications --region cn-hangzhou --OrderBy runnings --PageSize 20 --Reverse true --IsStateful 'example-string' --AppName 'demo-app' --FieldType appName --AppSource micro_service --NewSaeVersion 'example-string' --CurrentPage 1 --NamespaceId 'cn-beijing:demo' --FieldValue 'demo-app' --ProgrammingLanguage 'example-string' --Tags 'key_:_key_,_value_:_value' --header \"Content-Type=application/json\""
  }
}