{
  "name": "CreateMonitorGroupInstances",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用分组ID。",
        "en": "The ID of the application group."
      }
    },
    {
      "name": "Instances",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "实例列表。",
        "en": "The instances that you want to add to the application group."
      },
      "sub_parameters": [
        {
          "name": "Category",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "云服务名称缩写。\n\n关于如何获取云服务名称缩写，请参见[DescribeProjectMeta](~~114916~~)接口的返回参数`Labels`中的`metricCategory`。",
            "en": "The abbreviation of the Alibaba Cloud service name.\n\nTo obtain the abbreviation of an Alibaba Cloud service name, call the [DescribeProjectMeta](~~114916~~) operation. The `metricCategory` tag in the `Labels` response parameter indicates the abbreviation of the Alibaba Cloud service name."
          }
        },
        {
          "name": "InstanceId",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "实例ID。",
            "en": "The instance ID."
          }
        },
        {
          "name": "InstanceName",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "实例名称。",
            "en": "The instance name."
          }
        },
        {
          "name": "RegionId",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "实例所在地域。",
            "en": "The region ID of the instance."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "添加指定资源到指定应用分组",
    "en": "CreateMonitorGroupInstances"
  },
  "descriptions": {
    "zh": "一个应用分组中，一个云服务最多添加3000个资源，单次最多添加1000个资源，应用分组中的总资源数无限制。\n\n本文将提供一个示例，将地域`cn-hangzhou`的云服务器ECS的实例（实例ID为`i-2ze26xj5wwy12****`，实例名称为`test-instance-ecs`）添加到应用分组`3607****`。",
    "en": "You can add a maximum of 1,000 instances to an application group at a time. You can add a maximum of 3,000 instances of an Alibaba Cloud service to an application group. The total number of instances that you can add to an application group is unlimited.\n\nIn this example, an Elastic Compute Service (ECS) instance in the `China (Hangzhou)` region is added to the `3607****` application group. The instance ID is `i-2ze26xj5wwy12****` and the instance name is `test-instance-ecs`."
  },
  "example": {
    "unifiedCli": "aliyun cms create-monitor-group-instances --group-id 36070000 --instances InstanceName=test-instance-ecs Category=ecs InstanceId=i-2ze26xj5wwy120000 RegionId=cn-hangzhou --api-version 2019-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun cms CreateMonitorGroupInstances --region cn-hangzhou --Instances.1.InstanceName 'test-instance-ecs' --Instances.1.InstanceId 'i-2ze26xj5wwy120000' --Instances.1.Category ecs --Instances.1.RegionId 'cn-hangzhou' --GroupId 36070000"
  }
}