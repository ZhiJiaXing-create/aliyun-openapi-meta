{
  "name": "DeleteMonitorGroupDynamicRule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Category",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "动态规则对应的云产品类型，取值：ecs、rds、slb。",
        "en": "The service to which the rule applies. Valid values: ecs, rds, and slb."
      }
    },
    {
      "name": "GroupId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "应用分组ID。",
        "en": "The ID of the application group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除指定应用分组的动态规则",
    "en": "DeleteMonitorGroupDynamicRule"
  },
  "example": {
    "unifiedCli": "aliyun cms delete-monitor-group-dynamic-rule --group-id 123456 --category ecs --api-version 2019-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun cms DeleteMonitorGroupDynamicRule --region cn-hangzhou --Category ecs --GroupId 123456"
  }
}