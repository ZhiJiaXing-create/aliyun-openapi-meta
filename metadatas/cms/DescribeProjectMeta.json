{
  "name": "DescribeProjectMeta",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Labels",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "标签。根据标签过滤，标签为每个报警增加特殊标记。\n\n目前仅支持按照产品过滤，即`name`为`product`的过滤方式，例如：{\"name\":\"product\",\"value\":\"ECS\"}。\n>对于阿里云中云监控控制台的特殊标签，不建议您使用。",
        "en": "The tags. Tags are used to filter alerts, and each alert can be marked with special tags.\n\nCurrently, only filtering by product is supported. That is, the `name` is `product`. For example: {\"name\":\"product\",\"value\":\"ECS\"}.\n>We do not recommend that you use the special tags for the CloudMonitor console in Alibaba Cloud."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码。\n\n取值范围：1~100。\n\n默认值：1。",
        "en": "The page number.\n\nValid values: 1 to 100.\n\nDefault value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小。\n\n取值范围：1~10000。\n\n默认值：30。\n\n>目前阿里云未限制该参数，如果您需要获取所有结果，则将分页大小设置为较大的值即可。",
        "en": "The number of entries per page.\n\nValid values: 1 to 10000.\n\nDefault value: 30.\n\n>Currently, Alibaba Cloud does not impose a limit on this parameter. If you need to obtain all results, set the page size to a large value."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询云监控支持的时序类监控项产品列表",
    "en": "Queries the list of cloud services that support time series monitoring metrics in CloudMonitor"
  },
  "descriptions": {
    "zh": "获取接入的云产品信息，包括产品的描述信息、Namespace和标签。",
    "en": "Obtains the information about the connected cloud services, including the description, namespace, and tags of each service."
  },
  "example": {
    "unifiedCli": "aliyun cms describe-project-meta --labels name_:_product_,_value_:_ECS --page-number 1 --page-size 30 --api-version 2019-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun cms DescribeProjectMeta --region cn-hangzhou --PageSize 30 --PageNumber 1 --Labels 'name_:_product_,_value_:_ECS'"
  }
}