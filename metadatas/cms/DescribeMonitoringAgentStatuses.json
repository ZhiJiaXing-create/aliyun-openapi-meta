{
  "name": "DescribeMonitoringAgentStatuses",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "HostAvailabilityTaskId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "The ID of the availability monitoring task.",
        "en": "The ID of the availability monitoring task."
      }
    },
    {
      "name": "InstanceIds",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "The instance IDs. Separate multiple instance IDs with commas (,).",
        "en": "The instance IDs. Separate multiple instance IDs with commas (,)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询云监控插件运行状态",
    "en": "Query the running status of the CloudMonitor agent"
  },
  "descriptions": {
    "zh": "This topic describes how to query the status of the CloudMonitor agent that is installed on the `i-hp3dunahluwajv6f****` instance. The result indicates that the CloudMonitor agent is in the `running` state.",
    "en": "This topic describes how to query the status of the CloudMonitor agent that is installed on the `i-hp3dunahluwajv6f****` instance. The result indicates that the CloudMonitor agent is in the `running` state."
  },
  "example": {
    "unifiedCli": "aliyun cms describe-monitoring-agent-statuses --instance-ids i-hp3dunahluwajv6f0000 --host-availability-task-id 1260000 --api-version 2019-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun cms DescribeMonitoringAgentStatuses --region cn-hangzhou --HostAvailabilityTaskId 1260000 --InstanceIds 'i-hp3dunahluwajv6f0000'"
  }
}