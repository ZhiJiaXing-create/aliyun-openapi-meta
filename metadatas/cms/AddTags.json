{
  "name": "AddTags",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GroupIds",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "应用分组ID。\n\nN的取值范围：1~20。\n\n关于如何获取应用分组ID，请参见[DescribeMonitorGroups](~~2513168~~)。",
        "en": "The IDs of the application groups.\n\nThe value of N can be from 1 to 20.\n\nFor information about how to obtain application group IDs, see [DescribeMonitorGroups](~~2513168~~)."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "标签列表。",
        "en": "A list of tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "标签键。\n\nN的取值范围：1~3。Key的取值范围：1~64个字符。\n\n您可以输入新标签键，也可以使用云监控已存在的标签键。关于如何获取标签键，请参见[DescribeTagKeyList](~~2513189~~)。\n\n> Key不能以`aliyun`和`acs:`开头，且标签键（`Tag.N.Key`）和标签值（`Tag.N.Value`）必须同时设置。",
            "en": "The tag key.\n\nThe value of N can be from 1 to 3. The tag key must be 1 to 64 characters in length.\n\nEnter a new tag key or use an existing tag key from Cloud Monitor. For information about how to obtain tag keys, see [DescribeTagKeyList](~~2513189~~).\n\n> The tag key cannot start with `aliyun` or `acs:`. The `Tag.N.Key` and `Tag.N.Value` parameters must be specified together."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "标签值。\n\nN的取值范围：1~3。Value的取值范围：1~64个字符。\n\n您可以输入新标签值，也可以使用云监控已存在的标签值。关于如何获取标签值，请参见[DescribeTagValueList](~~2513188~~)。\n\n> Value不能以`aliyun`和`acs:`开头，且标签键（`Tag.N.Key`）和标签值（`Tag.N.Value`）必须同时设置。",
            "en": "The tag value.\n\nThe value of N can be from 1 to 3. The tag value must be 1 to 64 characters in length.\n\nEnter a new tag value or use an existing tag value from Cloud Monitor. For information about how to obtain tag values, see [DescribeTagValueList](~~2513188~~).\n\n> The tag value cannot start with `aliyun` or `acs:`. The `Tag.N.Key` and `Tag.N.Value` parameters must be specified together."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为指定应用分组绑定标签",
    "en": "AddTags"
  },
  "descriptions": {
    "zh": "本文将提供一个示例，为应用分组`7301****`绑定一个标签，其中标签键为`key1`，标签值为`value1`。",
    "en": "This topic provides an example of how to attach a tag to the application group `7301****`. In this example, the tag key is `key1` and the tag value is `value1`."
  },
  "example": {
    "unifiedCli": "aliyun cms add-tags --tag Value=value1 Key=key1 --group-ids 73010000 --api-version 2019-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun cms AddTags --region cn-hangzhou --Tag.1.Key key1 --Tag.1.Value value1 --GroupIds.1 73010000"
  }
}