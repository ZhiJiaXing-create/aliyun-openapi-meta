{
  "name": "DescribeMonitorGroupInstances",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Category",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云产品名称缩写。N的取值范围为1~200。\n\n> 关于如何获取云产品名称缩写，请参见[DescribeProjectMeta](~~114916~~)接口的返回参数`Labels`中的`metricCategory`。",
        "en": "The abbreviation of the cloud service name. Valid values of N: 1 to 200.\n\n> For more information about how to obtain the abbreviation of a cloud service name, see `metricCategory` in the response parameter `Labels` of the [DescribeProjectMeta](~~114916~~) operation."
      }
    },
    {
      "name": "GroupId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "应用分组ID。",
        "en": "The ID of the application group."
      }
    },
    {
      "name": "InstanceIds",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按照实例ID搜索。",
        "en": "The instance ID. You can query multiple instances by specifying multiple IDs."
      }
    },
    {
      "name": "Keyword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按照实例名称模糊查询。",
        "en": "The keyword used to search for instances. Fuzzy search based on instance names is supported."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码。默认值：1。",
        "en": "The page number. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页显示记录条数。默认值：10。",
        "en": "The number of entries per page. Default value: 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询指定应用分组内包含的资源列表",
    "en": "DescribeMonitorGroupInstances"
  },
  "example": {
    "unifiedCli": "aliyun cms describe-monitor-group-instances --page-size 10 --page-number 1 --group-id 1230000 --category ecs --keyword test-s1 --instance-ids i-bp12g4xbl4i0brkn0000 --api-version 2019-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun cms DescribeMonitorGroupInstances --region cn-hangzhou --Category ecs --PageSize 10 --Keyword 'test-s1' --PageNumber 1 --InstanceIds 'i-bp12g4xbl4i0brkn0000' --GroupId 1230000"
  }
}