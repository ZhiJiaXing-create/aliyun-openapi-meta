{
  "name": "AddPrivateDnsDomainName",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccessInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "私有 DNS 解析实例 ID。",
        "en": "The ID of the private DNS resolution instance."
      }
    },
    {
      "name": "DomainNameList",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "添加的私有域名列表。",
        "en": "The list of private domain names to add."
      }
    },
    {
      "name": "RegionNo",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例所在的区域ID。\n\n> 关于云防火墙支持地域的详细信息，请参见[支持的地域](~~195657~~)。",
        "en": "The region ID of the instance.\n\n> For more information about the regions supported by Cloud Firewall, see [Supported regions](~~195657~~)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "添加私网DNS域名",
    "en": "Add a private DNS domain name"
  },
  "descriptions": {
    "zh": "本接口一般用于获取域名DNS的解析结果。当前仅支持从阿里云云解析DNS获取解析结果。要查询的域名必须使用云解析DNS，才能查询到其解析结果。\n\n## QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "This operation is used to obtain DNS resolution results for a domain name. Currently, only resolution results from Alibaba Cloud DNS are supported. The domain name that you want to query must use Alibaba Cloud DNS before you can obtain its resolution results.\n\n## QPS limit\nThe single-user QPS limit for this operation is 10 calls per second. If the limit is exceeded, API calls are throttled, which may affect your business. Call this operation at an appropriate frequency."
  },
  "example": {
    "unifiedCli": "aliyun cloudfw add-private-dns-domain-name --access-instance-id cfw-xxx --region-no cn-shenzhen --domain-name-list internal.xxx.com --region cn-hangzhou",
    "legacyCli": "aliyun cloudfw AddPrivateDnsDomainName --region cn-hangzhou --RegionNo 'cn-shenzhen' --DomainNameList.1 'internal.xxx.com' --AccessInstanceId 'cfw-xxx'"
  }
}