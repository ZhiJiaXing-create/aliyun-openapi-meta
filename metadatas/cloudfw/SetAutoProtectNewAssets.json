{
  "name": "SetAutoProtectNewAssets",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoProtect",
      "position": "Query",
      "type": "Boolean",
      "required": true,
      "description": {
        "zh": "设置是否开启自动引流。取值：-**true**：开启。-**false**：不开启。",
        "en": "Specifies whether to enable automatic traffic redirection. Valid values:\n\n**true****false**"
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "语言类型。取值：-**zh**（默认值）：中文-**en**：英文",
        "en": "The language. Valid values are zh (Chinese) and **en** (English). The default value is **zh**."
      }
    },
    {
      "name": "SourceIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问者源ip地址。",
        "en": "The source IP address of the visitor."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "开启自动保护新入资产",
    "en": "SetAutoProtectNewAssets"
  },
  "descriptions": {
    "zh": "每个云防火墙实例最多支持创建100个TLS检查策略的关联关系。",
    "en": "Each Cloud Firewall instance supports up to 100 associations with TLS inspection policies."
  },
  "example": {
    "unifiedCli": "aliyun cloudfw set-auto-protect-new-assets --source-ip 60.182.79.XXX --lang zh --auto-protect true --region cn-hangzhou",
    "legacyCli": "aliyun cloudfw SetAutoProtectNewAssets --region cn-hangzhou --SourceIp '60.182.79.XXX' --AutoProtect true --Lang zh"
  }
}