{
  "name": "CreateNatFirewallControlPolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AclAction",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "安全访问控制策略中访问流量通过云防火墙的方式（动作）。\n\n取值：\n\n- **accept**：放行\n- **drop**：拒绝\n- **log**：观察",
        "en": "The action that Cloud Firewall performs on traffic that matches the access control policy.\n\nValid values:\n\n- **accept**: Allows the traffic.\n\n- **drop**: Drops the traffic.\n\n- **log**: Logs the traffic."
      }
    },
    {
      "name": "ApplicationNameList",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "访问控制策略支持的应用类型列表。",
        "en": "The list of applications to which the access control policy applies."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制策略的描述信息。",
        "en": "The description of the access control policy."
      }
    },
    {
      "name": "DestPort",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问控制策略中流量访问的目的端口。取值：\n\n- 当协议类型为ICMP时，DestPort取值为空。\n   \n> 协议类型为ICMP时，不支持对目的端口进行访问控制。\n\n- 当协议类型为TCP、UDP或ANY时，并且目的端口类型（DestPortType）为group时，DestPort取值为空。\n\n> 访问控制策略目的端口类型选择group（目的端口地址簿）时，您无需设置具体的目的端口号。需要该访问控制策略管控的所有端口会包含在目的端口地址簿中。\n\n- 当协议类型为TCP、UDP或ANY时，并且目的端口类型（DestPortType）为port时，DestPort取值为目的端口号。",
        "en": "The destination port for the traffic. The value of this parameter depends on the `Proto` and `DestPortType` parameters.\n\n- If `Proto` is `ICMP`, leave this parameter empty.\n\n> Access control cannot be configured based on the destination port for ICMP traffic.\n\n- If the destination port type (`DestPortType`) is `group`, leave this parameter empty.\n\n> If `DestPortType` is set to `group`, you do not need to specify a destination port because the required ports are defined in the selected port address book.\n\n- If the protocol is TCP, UDP, or ANY and the destination port type (`DestPortType`) is `port`, specify the destination port number."
      }
    },
    {
      "name": "DestPortGroup",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问控制策略中访问流量的目的端口地址簿名称。\n\n\n> DestPortType设置为group时，您需要设置目的端口地址簿名称。",
        "en": "The name of the destination port address book.\n\n> This parameter is required if `DestPortType` is set to `group`."
      }
    },
    {
      "name": "DestPortType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "安全访问控制策略中访问流量的目的端口类型。\n\n- **port**：端口\n- **group**：端口地址簿",
        "en": "The type of the destination port.\n\n- **port**: Port or port range\n\n- **group**: Port address book"
      }
    },
    {
      "name": "Destination",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制策略中的目的地址段。\n\n取值：\n- 当DestinationType为net时，Destination为目的CIDR。\n  \n  例如：1.2.XX.XX/24\n\n- 当DestinationType为group时，Destination为目的地址簿名称。\n\n  例如：db_group\n\n- 当DestinationType为domain时，Destination为目的域名。\n\n  例如：*.aliyuncs.com\n\n- 当DestinationType为location时，Destination为目的区域。\n\n  例如： \\[\"BJ11\", \"ZB\"\\]",
        "en": "The destination address in the access control policy.\n\nThe value of this parameter varies based on the value of `DestinationType`:\n\n- If `DestinationType` is `net`, set this parameter to the destination CIDR.\n\n  Example: `1.2.XX.XX/24`\n\n- If `DestinationType` is `group`, set this parameter to the name of the destination address book.\n\n  Example: `db_group`\n\n- If `DestinationType` is `domain`, set this parameter to the destination domain.\n\n  Example: \\*.aliyuncs.com\n\n- If `DestinationType` is `location`, set this parameter to the destination location.\n\n  Example: \\[\"BJ11\", \"ZB\"]"
      }
    },
    {
      "name": "DestinationType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制策略中的目的地址类型。\n\n取值：\n\n- **net**：目的网段（CIDR地址）\n- **group**：目的地址簿\n- **domain**：目的域名",
        "en": "The type of the destination address in the access control policy.\n\nValid values:\n\n- **net**: Destination CIDR\n\n- **group**: Destination address book\n\n- **domain**: Destination domain"
      }
    },
    {
      "name": "Direction",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制策略的流量方向。取值：\n\n- **out**：内对外流量访问控制",
        "en": "The traffic direction for the access control policy. Valid values:\n\n- **out**: outbound traffic"
      }
    },
    {
      "name": "DomainResolveType",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "访问控制策略的域名解析方式。取值：\n\n- **0**：基于FQDN\n- **1**：基于DNS动态解析\n- **2**：基于FQDN与DNS动态解析\n\n> 域名识别模式包含FQDN时，只允许配置TCP协议，应用可选HTTP/HTTPS/SMTP/SMTPS/SSL/IMAPS/POPS。",
        "en": "The domain name resolution method. Valid values:\n\n- **0**: FQDN-based resolution\n\n- **1**: Dynamic DNS-based resolution\n\n- **2**: FQDN-based and dynamic DNS-based resolution\n\n> If the resolution method includes FQDN, you can set the protocol only to TCP. The supported applications are HTTP, HTTPS, SMTP, SMTPS, and SSL."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "访问控制策略的策略有效期的结束时间。使用秒级时间戳格式表示。必须为整点或半点时间，且大于开始时间至少半小时。\n> 当RepeatType为Permanent时，EndTime为空。当RepeatType为None、Daily、Weekly、Monthly时，EndTime必须有值，您需要设置结束时间。",
        "en": "The end time of the policy's validity period, specified as a Unix timestamp. The time must be on the hour or half-hour and be at least 30 minutes after the start time.\n\n> This parameter is required if `RepeatType` is `None`, `Daily`, `Weekly`, or `Monthly`. If `RepeatType` is `Permanent`, leave this parameter empty."
      }
    },
    {
      "name": "IpVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "支持的IP地址版本。取值：\n\n- **4**（默认）：代表IPv4地址",
        "en": "The IP version for the access control policy. Valid values:\n\n- **4** (default): IPv4"
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "接收消息的语言类型。\n\n取值：\n\n- **zh**：（默认）中文\n- **en**：英文",
        "en": "The language of the response messages.\n\nValid values:\n\n- **zh**: Chinese (default)\n\n- **en**: English"
      }
    },
    {
      "name": "NatGatewayId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "NAT网关的实例ID。",
        "en": "The instance ID of the NAT Gateway."
      }
    },
    {
      "name": "NewOrder",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制策略生效的优先级。优先级数字从1开始顺序递增，优先级数字越小，优先级越高。",
        "en": "The priority of the access control policy. Values start from 1. A smaller value indicates a higher priority."
      }
    },
    {
      "name": "Proto",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制策略中流量访问的安全协议类型。\n\n取值：\n\n- ANY（表示查询所有协议类型）\n- TCP\n- UDP\n- ICMP\n\n> 访问的目的地址为域名类型的威胁情报地址簿或云服务地址簿时，只允许配置TCP协议，应用可选HTTP/HTTPS/SMTP/SMTPS/SSL五种。",
        "en": "The protocol for the traffic in the access control policy.\n\nValid values:\n\n- ANY: all protocols\n\n- TCP\n\n- UDP\n\n- ICMP\n\n> If the destination is a domain, a threat intelligence address book, or a cloud service address book, you can only set this parameter to `TCP`. The supported application types are HTTP, HTTPS, SMTP, SMTPS, and SSL."
      }
    },
    {
      "name": "Release",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问控制策略的启用状态。策略创建后默认启用该策略。取值：\n\n- **true**：启用访问控制策略\n- **false**：不启用访问控制策略",
        "en": "Specifies whether the access control policy is enabled. By default, policies are enabled upon creation. Valid values:\n\n- **true**: Enables the policy.\n\n- **false**: Disables the policy."
      }
    },
    {
      "name": "RepeatDays",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "访问控制策略的策略有效期的重复日期集合。\n- 当RepeatType为`Permanent`、`None`、`Daily`时，RepeatDays为空集合。\n  例如：[]\n- 当RepeatType为Weekly时，RepeatDays不能为空。\n  例如：[0, 6]\n> RepeatType设置为Weekly时，RepeatDays不允许重复。\n- 当RepeatType为`Monthly`时，RepeatDays不能为空。\n  例如：[1, 31]\n> RepeatType设置为Monthly时，RepeatDays不允许重复。",
        "en": "The days of the week or month on which the policy recurs.\n\n- If `RepeatType` is set to `Permanent`, `None`, or `Daily`, leave this parameter empty. Example: `[]`\n\n- If `RepeatType` is `Weekly`, this parameter is required. Example: `[0, 6]`\n\n> If `RepeatType` is `Weekly`, the values in `RepeatDays` must be unique.\n\n- If `RepeatType` is `Monthly`, this parameter is required. Example: `[1, 31]`\n\n> If `RepeatType` is `Monthly`, the values in `RepeatDays` must be unique."
      }
    },
    {
      "name": "RepeatEndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问控制策略的策略有效期的重复结束时间。例如：23:30，必须为整点或半点时间，且大于重复开始时间至少半小时。\n> 当RepeatType为Permanent、None时，RepeatEndTime为空。当RepeatType为Daily、Weekly、Monthly时，RepeatEndTime必须有值，您需要设置重复结束时间。\n> 时间格式为HH:mm（24小时制），例如08:00、23:30。",
        "en": "The end time of the recurrence. The time must be on the hour or half-hour, and must be at least 30 minutes later than the start time.\n\n> This parameter is required if `RepeatType` is set to `Daily`, `Weekly`, or `Monthly`. If `RepeatType` is `Permanent` or `None`, leave this parameter empty.\n> The time is in the HH:mm format (24-hour). For example, `08:00` or `23:30`."
      }
    },
    {
      "name": "RepeatStartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问控制策略的策略有效期的重复开始时间。例如：08:00，必须为整点或半点时间，且小于重复结束时间至少半小时。\n> 当RepeatType为Permanent、None时，RepeatStartTime为空。当RepeatType为Daily、Weekly、Monthly时，RepeatStartTime必须有值，您需要设置重复开始时间。\n> 时间格式为HH:mm（24小时制），例如08:00、23:30。",
        "en": "The start time of the recurrence. The time must be on the hour or half-hour, and must be at least 30 minutes earlier than the end time.\n\n> This parameter is required if `RepeatType` is set to `Daily`, `Weekly`, or `Monthly`. If `RepeatType` is `Permanent` or `None`, leave this parameter empty.\n> The time is in the HH:mm format (24-hour). For example, `08:00` or `23:30`."
      }
    },
    {
      "name": "RepeatType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问控制策略的策略有效期的重复类型。取值：\n- **Permanent**（默认）：总是\n- **None**：指定单次时间\n- **Daily**：每天\n- **Weekly**：每周\n- **Monthly**：每月",
        "en": "The recurrence type for the policy validity period. Valid values:\n\n- **Permanent** (default): The policy is always active.\n\n- **None**: The policy runs once for a specified duration.\n\n- **Daily**: The policy recurs daily.\n\n- **Weekly**: The policy recurs weekly within a specified time range.\n\n- **Monthly**: The policy recurs monthly within a specified time range."
      }
    },
    {
      "name": "Source",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制策略中的源地址。\n\n取值：\n\n- 当**SourceType**为`net`时，Source为源CIDR地址。\n\n  例如：10.2.4.0/24\n\n- 当**SourceType**为`group`时，Source为源地址簿名称。\n\n  例如：db_group",
        "en": "The source address in the access control policy.\n\nThe value of this parameter varies based on the value of `SourceType`:\n\n- If **SourceType** is `net`, set this parameter to the source CIDR.\n\n  Example: 10.2.4.0/24\n\n- If **SourceType** is `group`, set this parameter to the name of the source address book.\n\n  Example: db\\_group"
      }
    },
    {
      "name": "SourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制策略中的源地址类型。\n\n取值：\n\n- **net**：源网段（CIDR地址）\n- **group**：源地址簿",
        "en": "The type of the source address in the access control policy.\n\nValid values:\n\n- **net**: Source CIDR\n\n- **group**: Source address book"
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "访问控制策略的策略有效期的开始时间。使用秒级时间戳格式表示。必须为整点或半点时间，且小于结束时间至少半小时。\n> 当RepeatType为Permanent时，StartTime为空。当RepeatType为None、Daily、Weekly、Monthly时，StartTime必须有值，您需要设置开始时间。",
        "en": "The start time of the policy's validity period, specified as a Unix timestamp. The time must be on the hour or half-hour and be at least 30 minutes before the end time.\n\n> This parameter is required if `RepeatType` is `None`, `Daily`, `Weekly`, or `Monthly`. If `RepeatType` is `Permanent`, leave this parameter empty."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "添加NAT防火墙访问控制策略",
    "en": "CreateNatFirewallControlPolicy"
  },
  "descriptions": {
    "zh": "使用本接口创建一条对经过NAT防火墙流量放行、拒绝或观察的策略。",
    "en": "This API creates a policy to allow, deny, or observe traffic through the NAT Firewall."
  },
  "example": {
    "unifiedCli": "aliyun cloudfw create-nat-firewall-control-policy --lang zh --acl-action log --application-name-list ANY --description '放行流量' --dest-port 80 --destination XX.XX.XX.XX/24 --destination-type net --nat-gateway-id ngw-2vc2ustolqn6sr0000000 --proto ANY --source 192.168.0.25/32 --source-type net --new-order 1 --dest-port-type port --dest-port-group my_port_group --release true --domain-resolve-type 0 --ip-version 4 --direction out --repeat-type Permanent --repeat-days 1 --repeat-start-time 08:00 --repeat-end-time 23:30 --start-time 1694761200 --end-time 1694764800 --region cn-hangzhou",
    "legacyCli": "aliyun cloudfw CreateNatFirewallControlPolicy --region cn-hangzhou --Destination 'XX.XX.XX.XX/24' --Description '放行流量' --EndTime 1694764800 --IpVersion 4 --SourceType net --DestPort 80 --StartTime 1694761200 --AclAction log --Lang zh --DestinationType net --Direction out --Source '192.168.0.25/32' --ApplicationNameList.1 ANY --DestPortType port --Proto ANY --RepeatEndTime '23:30' --RepeatDays.1 1 --DomainResolveType 0 --RepeatType Permanent --RepeatStartTime '08:00' --NatGatewayId 'ngw-2vc2ustolqn6sr0000000' --NewOrder 1 --Release true --DestPortGroup my_port_group --method POST --force"
  }
}