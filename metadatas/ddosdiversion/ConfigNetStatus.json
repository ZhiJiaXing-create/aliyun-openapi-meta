{
  "name": "ConfigNetStatus",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Net",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "代播实例的网段。",
        "en": "The CIDR block of the anti-DDoS diversion instance."
      }
    },
    {
      "name": "Regions",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "网段需要宣告/撤销的区域列表，当列表为空时，表示操作所有网段配置的区域。\n\n> 您可以调用[QueryNetList](~~2639086~~)获取网段配置的区域信息。",
        "en": "The regions in which the CIDR block needs to be advertised or withdrawn from advertising. If you leave this parameter empty, the CIDR blocks in all regions are configured.\n\n> You can call the [QueryNetList](~~2639086~~) operation to obtain the regions of the CIDR blocks."
      }
    },
    {
      "name": "SaleId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "代播实例的ID。",
        "en": "The ID of the anti-DDoS diversion instance."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "状态。取值：\n\n- enable：宣告。\n- disable：撤销。",
        "en": "The status of the CIDR block. Valid values:\n\n- enable: advertises the CIDR block.\n\n- disable: withdraws the advertising of the CIDR block."
      }
    },
    {
      "name": "SubNets",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "代播实例网段的子网段列表。",
        "en": "The subnet CIDR blocks of the CIDR block."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "网段宣告配置",
    "en": "ConfigNetStatus"
  },
  "example": {
    "unifiedCli": "aliyun ddosdiversion config-net-status --sale-id ddos_diversion_public_cn-xxxxxxxxxxxxx --net 192.168.XX.XX/22 --status enable --regions oe26 --sub-nets 192.168.XX.XX/24 --region cn-hangzhou",
    "legacyCli": "aliyun ddosdiversion ConfigNetStatus --region cn-hangzhou --Status enable --SubNets.1 '192.168.XX.XX/24' --Regions.1 oe26 --SaleId 'ddos_diversion_public_cn-xxxxxxxxxxxxx' --Net '192.168.XX.XX/22' --method POST --force"
  }
}