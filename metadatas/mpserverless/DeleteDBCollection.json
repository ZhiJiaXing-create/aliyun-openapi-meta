{
  "name": "DeleteDBCollection",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Body",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要删除的数据集。格式为JSON字符串，其中command固定值为drop，collection值为要删除的集合名称。",
        "en": "The dataset to delete. The format is a JSON string where the static field \"command\" is set to \"drop\" and the \"collection\" value is the name of the collection to delete."
      }
    },
    {
      "name": "SpaceId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "服务空间ID。",
        "en": "Service Space ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除数据库集合",
    "en": "Delete Database Collection"
  }
}