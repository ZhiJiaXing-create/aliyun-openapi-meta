{
  "name": "UnassociateEipAddress",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllocationId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The allocation ID of the EIP."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "This parameter is optional because the system automatically determines the instance ID from the provided `AllocationId`."
      }
    },
    {
      "name": "InstanceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The type of the associated instance. Valid values:\n\n- `EcsInstance`: An ECS instance. This is the default.\n- `SlbInstance`: A Classic Load Balancer (CLB) instance.\n- `Nat`: A NAT gateway.\n- `HaVip`: A high-availability virtual IP address (HaVip).\n- `NetworkInterface`: A secondary elastic network interface."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The ID of the region where the EIP is located. To get the latest list of regions, call the `DescribeRegions` operation."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "UnassociateEipAddress"
  },
  "example": {
    "unifiedCli": "aliyun ecs unassociate-eip-address --biz-region-id cn-hangzhou --allocation-id example-string --instance-id example-string --instance-type example-string --region cn-hangzhou",
    "legacyCli": "aliyun ecs UnassociateEipAddress --region cn-hangzhou --InstanceId 'example-string' --AllocationId 'example-string' --RegionId 'cn-hangzhou' --InstanceType 'example-string'"
  }
}