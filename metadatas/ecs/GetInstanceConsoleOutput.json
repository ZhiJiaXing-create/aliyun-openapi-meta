{
  "name": "GetInstanceConsoleOutput",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例所在地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the instance. You can call [DescribeRegions](~~25609~~) to query the most recent list of Alibaba Cloud regions."
      }
    },
    {
      "name": "RemoveSymbols",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否去除返回文本中的排版符号。取值范围：\n- true：去除\n- false：不去除\n\n默认值：false",
        "en": "Specifies whether to remove formatting symbols from the returned text. Valid values:\n\n- true: removes formatting symbols.\n- false: does not remove formatting symbols.\n\nDefault value: false."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取实例系统命令行输出",
    "en": "Retrieve instance system command-line output"
  },
  "descriptions": {
    "zh": "- 云服务器ECS是虚拟化的云上服务，无法接入显示设备，也无法手动截屏。但是阿里云缓存了实例最近一次启动、重启或者关机时的系统命令行输出，您可以调用GetInstanceConsoleOutput获取。\n\n- [已停售的实例规格](~~55263~~)无法获取系统命令行输出。\n\n- Windows实例不支持获取系统命令行输出。",
    "en": "- Elastic Compute Service (ECS) is a virtualization-based cloud server that cannot be connected to display devices or manually screenshotted. However, Alibaba Cloud caches the system command line output from the last time an instance was started, restarted, or shut down. You can invoke GetInstanceConsoleOutput to retrieve this output.\n\n- System command line output cannot be retrieved for [retired instance types](~~55263~~).\n\n- Windows instances do not support retrieving system command line output."
  },
  "example": {
    "unifiedCli": "aliyun ecs get-instance-console-output --biz-region-id cn-hangzhou --instance-id i-bp1c1xhsrac2coiw0000 --remove-symbols false --region cn-hangzhou",
    "legacyCli": "aliyun ecs GetInstanceConsoleOutput --region cn-hangzhou --RemoveSymbols false --InstanceId 'i-bp1c1xhsrac2coiw0000' --RegionId 'cn-hangzhou'"
  }
}