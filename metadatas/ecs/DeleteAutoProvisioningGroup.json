{
  "name": "DeleteAutoProvisioningGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoProvisioningGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "弹性供应组的ID。",
        "en": "The ID of the auto provisioning group."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "弹性供应组所在地域的ID。",
        "en": "The region ID of the auto provisioning group."
      }
    },
    {
      "name": "TerminateInstances",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "删除弹性供应组时是否释放组内实例。取值范围：\n\n- true：释放组内实例。\n- false：组内实例继续运行。\n\n>此参数值默认继承于调用`CreateAutoProvisioningGroup`创建弹性供应组时设置的`TerminateInstances`参数值，您也可以在调用本接口删除弹性供应组时，重新设置`TerminateInstances`参数值。",
        "en": "Specifies whether to release instances in the auto provisioning group. Valid values:\n\n- true\n\n- false\n\n> By default, this parameter inherits the value of `TerminateInstances` that you specified when you call the `CreateAutoProvisioningGroup` operation to create an auto provisioning group. You can also change the value of `TerminateInstances` when you call the DeleteAutoProvisioningGroup operation to delete the auto provisioning group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除弹性供应组",
    "en": "DeleteAutoProvisioningGroup"
  },
  "example": {
    "unifiedCli": "aliyun ecs delete-auto-provisioning-group --biz-region-id cn-hangzhou --auto-provisioning-group-id apg-bpuf6jel2bbl62wh130000 --terminate-instances true --region cn-hangzhou",
    "legacyCli": "aliyun ecs DeleteAutoProvisioningGroup --region cn-hangzhou --AutoProvisioningGroupId 'apg-bpuf6jel2bbl62wh130000' --RegionId 'cn-hangzhou' --TerminateInstances true"
  }
}