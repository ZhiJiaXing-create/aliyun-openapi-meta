{
  "name": "DescribeManagedInstances",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ActivationId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "激活码ID。",
        "en": "The ID of the activation code."
      }
    },
    {
      "name": "Connected",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "托管实例是否已连接。\n\ntrue：托管实例已连接，您可以通过云助手管理托管实例。\n\nfalse：托管实例未连接，服务器可能停机或者云助手Agent未正确安装。",
        "en": "Indicates whether the managed instance is connected.\n\ntrue: The managed instance is connected, and you can manage it by using Cloud Assistant.\n\nfalse: The managed instance is not connected. The server may be shut down or the Cloud Assistant Agent may not be installed correctly."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "托管实例的ID。N的取值范围为1~50。",
        "en": "The ID of managed instance N. Valid values of N: 1 to 50."
      }
    },
    {
      "name": "InstanceIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "托管实例的内网IP或公网IP。",
        "en": "The internal or public IP address of the managed instance."
      }
    },
    {
      "name": "InstanceName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "托管实例的名称。",
        "en": "The name of the managed instance."
      }
    },
    {
      "name": "MachineId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "在注册托管实例时，所指定的MachineId参数的值，最多允许36字符。\n注册脚本示例：\n```\naliyun-service --register \\\n  --RegionId=cn-hangznou \\\n  --ActivationId=xxxxxxxxxxx \\\n  --ActivationCode=xxxxxxxxx \\\n  --MachineId=xxxxxx \\ # 可选参数，表示此机器的唯一标识\n  --ForceResue                 \n```\n\n- 若在注册时指定了MachineId与ForceResult参数，则云助手将为此MachineId生成固定不变的托管实例ID。\n- 若未显式指定MachineId参数，云助手将根据机器的硬件信息自动生成MachineId值。\n- 推荐：显式指定参数MachineId与ForceResult，用于标记托管实例与线下机器的对应关系。",
        "en": "The value of the MachineId parameter that you specify when you register a managed instance. A maximum of 36 characters are allowed. Sample registration script:\n\n```\naliyun-service --register \\\n  --RegionId=cn-hangznou \\\n  --ActivationId=xxxxxxxxxxx \\\n  --ActivationCode=xxxxxxxxx \\\n--MachineId=xxxxxx \\ # Optional. The unique identifier of the machine.\n  --ForceResue                 \n```\n\n- If the MachineId and ForceResult parameters are specified during registration, the Cloud Assistant generates a fixed managed instance ID for this MachineId.\n\n- If the MachineId parameter is not explicitly specified, the Cloud Assistant will automatically generate a MachineId value based on the hardware information of the machine.\n\n- We recommend that you explicitly specify the MachineId and ForceResult parameters to mark the mapping between a managed instance and an on-premises machine."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页的最大条目数。\n\n最大值为50。\n\n默认值为10。",
        "en": "The maximum number of entries per page.\n\nValid values: 1 to 50.\n\nDefault value: 10."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询凭证（Token），取值为上一次API调用返回的NextToken参数值。",
        "en": "The pagination token that is used in the next request to retrieve a new page of results. You must specify the token that is obtained from the previous query as the value of NextToken."
      }
    },
    {
      "name": "OsType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "托管实例的操作系统类型，取值范围：\n\n- windows。\n- linux。\n- FreeBSD。",
        "en": "The operating system type of the managed instance. Valid values:\n\n- windows\n\n- linux\n\n- FreeBSD"
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "> 该参数即将下线，推荐您使用NextToken与MaxResults完成分页查询操作。",
        "en": "> This parameter will be removed in the future. We recommend that you use NextToken and MaxResults for a paged query."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "> 该参数即将下线，推荐您使用NextToken与MaxResults完成分页查询操作。",
        "en": "> This parameter will be removed in the future. We recommend that you use NextToken and MaxResults for a paged query."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。目前支持的地域：华北1（青岛）、华北2（北京）、华北3（张家口）、华北5（呼和浩特）、华北6（乌兰察布）、华东1（杭州）、华东2（上海）、华南1（深圳）、华南2（河源）、华南3（广州）、西南1（成都）、中国香港、新加坡、日本（东京）、美国（硅谷）、美国（弗吉尼亚）。\n\n您可以调用[DescribeRegions](~~25609~~)查看地域对应的ID等信息。",
        "en": "The region ID. Supported regions: China (Qingdao), China (Beijing), China (Zhangjiakou), China (Hohhot), China (Ulanqab), China (Hangzhou), China (Shanghai), China (Shenzhen), China (Heyuan), China (Guangzhou), China (Chengdu), China (Hong Kong), Singapore, Japan (Tokyo), US (Silicon Valley), and US (Virginia).\n\nYou can call the [DescribeRegions](~~25609~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "托管实例所属的资源组ID。",
        "en": "The ID of the resource group to which the managed instance belongs."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表。",
        "en": "The tags of the managed instance."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "托管实例的标签键。N的取值范围为1~20。一旦传入该值，则不允许为空字符串。\n\n使用一个标签过滤资源，查询到该标签下的资源数量不能超过1000个。使用多个标签过滤资源，查询到同时绑定了多个标签的资源数量不能超过1000个。如果资源数量超过1000个，您需要使用[ListTagResources](~~110425~~)接口进行查询。\n\n最多支持64个字符，不能以`aliyun`或`acs:`开头，不能包含`http://`或`https://`。",
            "en": "The key of tag N of the managed instance. Valid values of N: 1 to 20. The tag key cannot be an empty string.\n\nIf a single tag is specified to query resources, up to 1,000 resources that have this tag added can be displayed in the response. If multiple tags are specified to query resources, up to 1,000 resources that have all these tags added can be displayed in the response. To query more than 1,000 resources that have specified tags added, call the [ListTagResources](~~110425~~) operation.\n\nThe tag key can be up to 64 characters in length and cannot start with `acs:` or `aliyun`. It cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "托管实例的标签值。N的取值范围为1~20。该值可以为空字符串。\n\n最多支持128个字符，不能包含`http://`或`https://`。",
            "en": "The value of tag N of the managed instance. Valid values of N: 1 to 20. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length and cannot contain `http://` or `https://`."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取托管实例",
    "en": "DescribeManagedInstances"
  },
  "descriptions": {
    "zh": "分页查询首页时，仅需设置`MaxResults`以限制返回信息的条目数，返回结果中的`NextToken`将作为查询后续页的凭证。查询后续页时，将`NextToken`参数设置为上一次返回结果中获取到的`NextToken`作为查询凭证，并设置`MaxResults`限制返回条目数。",
    "en": "When performing a paged query on the home page, you only need to set `MaxResults` to limit the number of entries in the return result. The `NextToken` in the returned result serves as the credential for querying subsequent pages. When querying subsequent pages, set the `NextToken` parameter to the `NextToken` obtained from the previous response as the query credential, and set `MaxResults` to limit the number of returned entries."
  },
  "example": {
    "unifiedCli": "aliyun ecs describe-managed-instances --biz-region-id cn-hangzhou --os-type windows --instance-ip 192.168.00.00 --activation-id 4ECEEE12-56F1-4FBC-9AB1-890F74940000 --machine-id GOG4X8312A0188 --instance-name my-webapp-server --page-number 1 --page-size 10 --instance-id mi-hz018jrc1o00000 --tag Value=TestValue Key=TestKey --max-results 10 --next-token AAAAAdDWBF20000 --resource-group-id rg-123000000 --connected true --region cn-hangzhou",
    "legacyCli": "aliyun ecs DescribeManagedInstances --region cn-hangzhou --MachineId GOG4X8312A0188 --Tag.1.Key TestKey --PageSize 10 --NextToken AAAAAdDWBF20000 --ResourceGroupId 'rg-123000000' --Connected true --MaxResults 10 --ActivationId '4ECEEE12-56F1-4FBC-9AB1-890F74940000' --InstanceIp '192.168.00.00' --InstanceName 'my-webapp-server' --OsType windows --PageNumber 1 --Tag.1.Value TestValue --RegionId 'cn-hangzhou' --InstanceId.1 'mi-hz018jrc1o00000'"
  }
}