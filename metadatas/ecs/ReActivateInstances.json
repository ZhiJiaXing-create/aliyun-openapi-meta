{
  "name": "ReActivateInstances",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要重开机的实例ID。",
        "en": "The instance ID of the instance that you want to restart."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例所属的地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the instance. You can call [DescribeRegions](~~25609~~) to query the most recent list of regions."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "重新激活已过期或欠费回收的实例",
    "en": "Reactivate an expired or recycled instance"
  },
  "descriptions": {
    "zh": "按量付费实例结清欠费账单后，您无需调用该接口激活实例，系统会自动重开机将您的实例恢复到欠费之前的状态。自动重开机偶尔会出现失败的情况，请您关注自动重开机是否成功。如果自动重开机失败，请通过该接口进行手动重开机。\n\n<props=\"china\">- 结清欠费后，您的阿里云账户余额（即现金余额）和代金券的总值不得小于100.00元人民币，否则无法重新启动实例。</props>\n<props=\"china\">- 实例必须处于**已过期**（`Stopped`）或者**欠费回收中**（`Stopped`）的状态。</props>\n<props=\"intl\">- 实例必须处于**已过期**（`Stopped`）的状态。</props>\n\n- 被安全锁定的实例不能通过调用该接口启动。更多信息，请参见[资源安全锁定对调用API的影响](~~25695~~)。",
    "en": "After you settle the overdue bill for a pay-as-you-go instance, you do not need to call this operation to activate the instance. The system automatically restarts the instance and restores it to the state before the overdue payment. Automatic restarts may occasionally fail. Check whether the automatic restart succeeds. If the automatic restart fails, call this operation to manually restart the instance.\n\n<props=\"china\">- After you settle the overdue bill, the total value of your Alibaba Cloud account balance (cash balance) and vouchers must be at least CNY 100.00. Otherwise, the instance cannot be restarted.</props>\n<props=\"china\">- The instance must be in the **expired** (`Stopped`) or **being recycled due to overdue payment** (`Stopped`) state.</props>\n<props=\"intl\">- The instance must be in the **expired** (`Stopped`) state.</props>\n\n- Instances that are locked for security reasons cannot be started by calling this operation. For more information, see [Impact of resource security locks on API calls](~~25695~~)."
  },
  "example": {
    "unifiedCli": "aliyun ecs re-activate-instances --biz-region-id cn-hangzhou --instance-id i-bp67acfmxazb4p0000 --region cn-hangzhou",
    "legacyCli": "aliyun ecs ReActivateInstances --region cn-hangzhou --InstanceId 'i-bp67acfmxazb4p0000' --RegionId 'cn-hangzhou'"
  }
}