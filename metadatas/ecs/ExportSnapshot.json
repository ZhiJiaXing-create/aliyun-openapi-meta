{
  "name": "ExportSnapshot",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "OssBucket",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The name of the destination OSS bucket. The bucket must be in the same region as the snapshot."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the region where the snapshot is located. You can call the `DescribeRegions` operation to query the latest region list."
      }
    },
    {
      "name": "RoleName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The name of the RAM role that authorizes ECS to export the snapshot. This role must grant ECS permission to write to the specified OSS bucket. If you omit this parameter, ECS uses its default service-linked role."
      }
    },
    {
      "name": "SnapshotId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the snapshot to export."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "ExportSnapshot"
  },
  "example": {
    "unifiedCli": "aliyun ecs export-snapshot --snapshot-id example-string --biz-region-id cn-hangzhou --oss-bucket example-string --role-name example-string --region cn-hangzhou",
    "legacyCli": "aliyun ecs ExportSnapshot --region cn-hangzhou --SnapshotId 'example-string' --RoleName 'example-string' --OssBucket 'example-string' --RegionId 'cn-hangzhou'"
  }
}