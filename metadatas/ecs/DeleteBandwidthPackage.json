{
  "name": "DeleteBandwidthPackage",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BandwidthPackageId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the bandwidth package to delete."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the region where the bandwidth package is located."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "DeleteBandwidthPackage"
  },
  "example": {
    "unifiedCli": "aliyun ecs delete-bandwidth-package --biz-region-id cn-hangzhou --bandwidth-package-id example-string --region cn-hangzhou",
    "legacyCli": "aliyun ecs DeleteBandwidthPackage --region cn-hangzhou --BandwidthPackageId 'example-string' --RegionId 'cn-hangzhou'"
  }
}