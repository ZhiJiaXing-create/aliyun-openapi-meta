{
  "name": "DeletePhysicalConnection",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "A client-generated, case-sensitive token used to ensure request idempotency. This token must be unique across all requests and contain a maximum of 64 ASCII characters."
      }
    },
    {
      "name": "PhysicalConnectionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the physical connection."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The region ID of the physical connection."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "DeletePhysicalConnection"
  },
  "example": {
    "unifiedCli": "aliyun ecs delete-physical-connection --biz-region-id cn-hangzhou --physical-connection-id example-string --client-token example-string --region cn-hangzhou",
    "legacyCli": "aliyun ecs DeletePhysicalConnection --region cn-hangzhou --PhysicalConnectionId 'example-string' --RegionId 'cn-hangzhou' --ClientToken 'example-string'"
  }
}