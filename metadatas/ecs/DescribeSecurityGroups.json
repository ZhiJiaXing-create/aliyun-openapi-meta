{
  "name": "DescribeSecurityGroups",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值范围：\n         \n- true：发送检查请求，不会查询资源状况。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码DryRunOperation。\n- false：发送正常请求，通过检查后返回2XX HTTP状态码并直接查询资源状况。\n\n默认值为false。",
        "en": "Specifies whether to perform only a dry run. Valid values:\n         \n- true: performs only a dry run. The system checks the request for potential issues, including invalid AccessKey pairs, unauthorized Resource Access Management (RAM) users, and missing parameter values. If the request fails the dry run, an error message is returned. If the request passes the dry run, the DryRunOperation error code is returned.\n- false: performs a dry run and sends the Normal request. If the request passes the dry run, a 2xx HTTP status code is returned and the authorization is verified.\n\nDefault value: false."
      }
    },
    {
      "name": "FuzzyQuery",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": ">该参数已废弃。",
        "en": "> This parameter is deprecated."
      }
    },
    {
      "name": "IsQueryEcsCount",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否查询安全组的容量信息。传True时，返回值中的`EcsCount`和`AvailableInstanceAmount`有效。\n>该参数已废弃。",
        "en": "Specifies whether to query the capacity information of the security group. If you set this parameter to True, the `EcsCount` and `AvailableInstanceAmount` values in the response are valid.\n> This parameter is deprecated."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页的最大条目数。一旦设置该参数，即表示使用`MaxResults`与`NextToken`组合参数的查询方式。\n\n最大值为100。\n\n默认值为10。",
        "en": "The maximum number of entries per page for paging query. If you set this parameter, the `MaxResults` and `NextToken` paging method is used.\n\nMaximum value: 100.\n\nDefault value: 10."
      }
    },
    {
      "name": "NetworkType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "安全组的网络类型。取值范围：\n\n- vpc：专有网络。\n- classic：经典网络。经典网络功能已下线，详情请参见[下线公告](~~2833134~~)。",
        "en": "The network type of the security group. Valid values:\n\n- vpc: VPC.\n- classic: classic network. The classic network is deprecated. For more information, see [Deprecation notice](~~2833134~~)."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询凭证（Token）。取值为上一次调用该接口返回的NextToken参数值，初次调用接口时无需设置该参数。",
        "en": "The pagination token. Set this parameter to the NextToken value returned in the previous request. You do not need to set this parameter for the first request."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "> 该参数即将下线，推荐您使用NextToken与MaxResults完成分页查询操作。",
        "en": "> This parameter will be offline. Use NextToken and MaxResults for paging."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "> 该参数即将下线，推荐您使用NextToken与MaxResults完成分页查询操作。",
        "en": "> This parameter will be offline. Use NextToken and MaxResults for paging."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID. You can call [DescribeRegions](~~25609~~) to query the most recent region list."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "安全组所在的企业资源组ID。使用该参数过滤资源时，资源数量不能超过1000个。您可以调用[ListResourceGroups](~~158855~~)查询资源组列表。\n\n>不支持默认资源组过滤。",
        "en": "The ID of the resource group to which the security group belongs. When you use this parameter to filter resources, the resource count cannot exceed 1,000. You can invoke [ListResourceGroups](~~158855~~) to query resource groups.\n\n> Filtering by the default resource group is not supported."
      }
    },
    {
      "name": "SecurityGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "安全组ID。",
        "en": "The ID of the security group."
      }
    },
    {
      "name": "SecurityGroupIds",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "安全组ID列表。一次最多支持100个安全组ID，ID之间用半角逗号（,）隔开，格式为JSON数组。",
        "en": "The IDs of security groups. You can specify up to 100 security group IDs. Separate multiple IDs with commas (,) in a JSON array format."
      }
    },
    {
      "name": "SecurityGroupName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "安全组名称。",
        "en": "The name of the security group."
      }
    },
    {
      "name": "SecurityGroupType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "安全组类型。取值范围：\n- normal：普通安全组。\n- enterprise：企业安全组。\n\n> 当不为该参数传值时，表示查询所有类型的安全组。",
        "en": "The type of the security group. Valid values:\n- normal: basic security group.\n- enterprise: advanced security group.\n\n> If you do not specify this parameter, security groups of all types are queried."
      }
    },
    {
      "name": "ServiceManaged",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否为托管安全组。取值范围：\n\n- true：是托管安全组。\n- false：不是托管安全组。",
        "en": "Specifies whether managed security group is managed. Valid values:\n\n- true: Managed security group is managed.\n- false: Managed security group is not managed."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表。",
        "en": "The tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "安全组的标签键。N的取值范围为1~20。\n\n使用一个标签过滤资源，查询到该标签下的资源数量不能超过1000个；使用多个标签过滤资源，查询到同时绑定了多个标签的资源数量不能超过1000个。如果资源数量超过1000个，请使用[ListTagResources](~~110425~~)接口进行查询。",
            "en": "The tag key of the security group. Valid values of N: 1 to 20.\n\nIf you use a single tag to filter resources, the resource count with the specified tag cannot exceed 1,000. If you use multiple tags to filter resources, the resource count that have all specified tags attached cannot exceed 1,000. If the resource count exceeds 1,000, call [ListTagResources](~~110425~~) to query resources."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "安全组的标签值。N的取值范围：1~20。",
            "en": "The tag value of the security group. Valid values of N: 1 to 20."
          }
        },
        {
          "name": "key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "安全组的标签键。\n\n> 为提高兼容性，建议您尽量使用Tag.N.Key参数。",
            "en": "The tag key of the security group.\n\n> For better compatibility, use the Tag.N.Key parameter."
          }
        },
        {
          "name": "value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "安全组的标签值。\n\n> 为提高兼容性，建议您尽量使用Tag.N.Value参数。",
            "en": "The tag value of the security group.\n\n> For better compatibility, use the Tag.N.Value parameter."
          }
        }
      ]
    },
    {
      "name": "VpcId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "安全组所在的专有网络ID。",
        "en": "The ID of the virtual private cloud (VPC) to which the security group belongs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询安全组基本信息列表",
    "en": "Query basic information about security groups"
  },
  "descriptions": {
    "zh": "- **分页查询**：推荐您使用`MaxResults`与`NextToken`参数进行查询。\n  - 当返回结果中没有`NextToken`时，表示该页为末页，不再有后续页。\n  - 分页查询首页时，仅需设置`MaxResults`以限制返回信息的条目数，返回结果中的`NextToken`将作为查询后续页的凭证。\n  - 查询后续页时，将`NextToken`参数设置为上一次返回结果中获取到的`NextToken`作为查询凭证，并设置`MaxResults`限制返回条目数。\n- 通过阿里云CLI调用API时，不同数据类型的请求参数取值必须遵循一定的格式要求。更多信息，请参见[CLI参数格式说明](~~110340~~)。",
    "en": "- **Paging query**: Use `MaxResults` and `NextToken` for paging query.\n  - If `NextToken` is not returned, the current page is the last page.\n  - For the first page, set only `MaxResults` to limit the number of entries to return. The `NextToken` value in the response is used to query subsequent pages.\n  - For subsequent pages, set `NextToken` to the `NextToken` value returned in the previous response and set `MaxResults` to limit the number of entries to return.\n- When you invoke an API operation by using Cloud Assistant CLI, specify request parameters in the required format. For more information, see [Parameter format of CLI commands](~~110340~~)."
  },
  "example": {
    "unifiedCli": "aliyun ecs describe-security-groups --biz-region-id cn-hangzhou --security-group-ids sg-bp67acfmxazb4p0000_,_sg-bp67acfmxazb4p0000_,_sg-bp67acfmxazb4p0000_,.... --vpc-id vpc-bp67acfmxazb4p0000 --security-group-type normal --next-token e71d8a535bd9cc11 --max-results 10 --network-type vpc --security-group-name SGTestName --is-query-ecs-count false --resource-group-id rg-bp67acfmxazb4p0000 --tag Value=TestValue key=testkey --dry-run false --security-group-id sg-bp67acfmxazb4p0000 --fuzzy-query false --page-number 1 --page-size 10 --service-managed false --region cn-hangzhou",
    "legacyCli": "aliyun ecs DescribeSecurityGroups --region cn-hangzhou --IsQueryEcsCount false --NextToken e71d8a535bd9cc11 --SecurityGroupName SGTestName --ResourceGroupId 'rg-bp67acfmxazb4p0000' --PageSize 10 --ServiceManaged false --MaxResults 10 --SecurityGroupId 'sg-bp67acfmxazb4p0000' --SecurityGroupType normal --DryRun false --SecurityGroupIds 'sg-bp67acfmxazb4p0000_,_sg-bp67acfmxazb4p0000_,_sg-bp67acfmxazb4p0000_,....' --VpcId 'vpc-bp67acfmxazb4p0000' --PageNumber 1 --NetworkType vpc --FuzzyQuery false --Tag.1.Value TestValue --RegionId 'cn-hangzhou' --Tag.1.key testkey"
  }
}