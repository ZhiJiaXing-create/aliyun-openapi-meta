{
  "name": "SendFile",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。**ClientToken**只支持ASCII字符，且不能超过64个字符。更多信息，请参见[如何保证幂等性](~~25693~~)。",
        "en": "Ensures request idempotence. Generate a parameter value from your client and ensure that this parameter value is unique across different requests. **ClientToken** supports only ASCII characters and cannot exceed 64 characters. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "Content",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件内容。文件内容在Base64编码后，大小不能超过32 KB。\n\n- 当`ContentType`参数为`PlainText`时，该字段为明文格式的普通文本。\n- 当`ContentType`参数为`Base64`时，该字段为Base64编码的文本。",
        "en": "The content of the file. The file must not exceed 32 KB in size after it is encoded in Base64.\n\n- If `ContentType` is set to `PlainText`, the value of Content is in plaintext.\n\n- If `ContentType` is set to `Base64`, the value of Content is Base64-encoded."
      }
    },
    {
      "name": "ContentType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件内容类型。\n- PlainText：普通文本。\n- Base64：Base64编码。\n\n默认值为PlainText。",
        "en": "The content type of the file. Valid values:\n\n- PlainText: The file content is not encoded.\n\n- Base64: The file content is encoded in Base64.\n\nDefault value: PlainText."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "描述信息。支持全字符集，长度不得超过512个字符。",
        "en": "The description of the file. The description can be up to 512 characters in length and can contain any characters."
      }
    },
    {
      "name": "FileGroup",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件的用户组。只对Linux实例生效，默认为root。长度不得超过64个字符。\n\n>使用其他用户组时，请确保实例中存在该用户组。",
        "en": "The group of the file. This parameter takes effect only on Linux instances. Default value: root. The value can be up to 64 characters in length.\n\n> If you want to use a non-root user group, make sure that the user group exists in the instances."
      }
    },
    {
      "name": "FileMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件的权限。只对Linux实例生效，设置方式与chmod命令相同。\n\n默认值为0644，表示用户具有读写权限，用户组和其它用户具有只读权限。",
        "en": "The permissions on the file. This parameter takes effect only on Linux instances. You can configure this parameter in the same way as you configure the chmod command.\n\nDefault value: 0644, which indicates that the owner of the file has the read and write permissions on the file and that the user group of the file and other users have the read-only permissions on the file."
      }
    },
    {
      "name": "FileOwner",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件的用户。只对Linux实例生效，默认为root。长度不得超过64个字符。\n\n>使用其他用户时，请确保实例中存在该用户。",
        "en": "The owner of the file. This parameter takes effect only on Linux instances. Default value: root. The value can be up to 64 characters in length.\n\n> If you want to use a non-root user, make sure that the user exists in the instances."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "需要执行命令的ECS实例列表。最多能指定50台ECS实例ID。N的取值范围为1~50。",
        "en": "The IDs of instances to which to send the file. You can specify up to 50 instance IDs in each request. Valid values of N: 1 to 50."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件名称。支持全字符集，长度不得超过255个字符。",
        "en": "The name of the file. The name can be up to 255 characters in length and can contain any characters."
      }
    },
    {
      "name": "Overwrite",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "如果同名文件在目标目录已存在，是否覆盖文件。\n- true：覆盖。\n- false：不覆盖。\n\n默认值为false。",
        "en": "Specifies whether to overwrite a file in the destination directory if the file has the same name as the sent file.\n\n- true\n\n- false\n\nDefault value: false."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目标ECS实例所在的地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the instance to which to send the file. You can call the [DescribeRegions](~~25609~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件下发的资源组ID，当指定该参数时：\n\n- InstanceId对应的ECS实例必须属于该资源组。\n\n- 支持通过指定该参数筛选出对应的文件下发结果（通过调用[DescribeSendFileResults](~~184117~~)）。",
        "en": "The ID of the resource group. When you specify this parameter, take note of the following items:\n\n- The instance specified by the InstanceId parameter must belong to the specified resource group.\n\n- If you specify this parameter, you can call the [DescribeSendFileResults](~~184117~~) operation to query file sending results in the specified resource group."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表。",
        "en": "The tags to add to the file sending task."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "文件下发的标签键。N的取值范围为1~20。一旦传入该值，则不允许为空字符串。\n\n使用一个标签过滤资源，查询到该标签下的资源数量不能超过1000个。使用多个标签过滤资源，查询到同时绑定了多个标签的资源数量不能超过1000个。如果资源数量超过1000个，您需要使用[ListTagResources](~~110425~~)接口进行查询。\n\n最多支持64个字符，不能以`aliyun`或`acs:`开头，不能包含`http://`或`https://`。",
            "en": "The key of tag N of the file sending task. Valid values of N: 1 to 20. The tag key cannot be an empty string.\n\nIf a single tag is specified to query resources, up to 1,000 resources that have this tag added can be displayed in the response. If multiple tags are specified to query resources, up to 1,000 resources that have all the tags added can be displayed in the response. To query more than 1,000 resources that have specified tags, call the [ListTagResources](~~110425~~) operation.\n\nThe tag key can be up to 64 characters in length and cannot contain `http://` or `https://`. The tag key cannot start with `acs:` or `aliyun`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "文件下发的标签值。N的取值范围为1~20。该值可以为空字符串。\n\n最多支持128个字符，不能包含`http://`或`https://`。",
            "en": "The value of tag N to add to the file sending task. Valid values of N: 1 to 20. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length and cannot contain `http://` or `https://`."
          }
        }
      ]
    },
    {
      "name": "TargetDir",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件下发目标ECS实例中的目录。如不存在则会自动创建。长度不得超过255个字符。",
        "en": "The destination directory on the instance to which to send the file. If the specified directory does not exist, the system creates the directory on the instance. The value cannot exceed 255 characters in length."
      }
    },
    {
      "name": "Timeout",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "下发文件的超时时间。单位：秒。\n\n- 当因为进程原因、缺失模块、缺失云助手Agent等原因无法下发文件时，会出现超时现象。\n- 当设置的超时时间小于10秒时，为确保下发成功，系统会将超时时间自动设置为10秒。\n\n默认值为60。",
        "en": "The timeout period for the file sending task. Unit: seconds.\n\n- A timeout error occurs when a file cannot be sent because the process slows down or because a specific module or Cloud Assistant Agent does not exist.\n\n- If the specified timeout period is less than 10 seconds, the system sets the timeout period to 10 seconds to ensure that the file can be sent to the instances.\n\nDefault value: 60."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "上传本地文件到ECS实例",
    "en": "SendFile"
  },
  "descriptions": {
    "zh": "## 接口说明\n\n- 目标ECS实例的状态必须为运行中（`Running`）。\n- 目标ECS实例必须预先安装[云助手Agent](~~64921~~)。\n- 云助手Agent版本需要高于以下对应的版本才能支持下发文件。如果结果返回`ClientNeedUpgrade`错误码，请将客户端更新至最新版本。\n    - Linux：1.0.2.569\n    - Windows：1.0.0.149\n- 文件内容在进行Base64编码后，大小不能超过32 KB。\n- 文件下发可能会因为目标ECS实例的状态异常、网络异常或云助手Agent异常而出现失败的情况。请调用[DescribeSendFileResults](~~184117~~)，或参考[执行失败常见错误及修复建议](~~87029~~)进行问题排查。",
    "en": "## API Guide\n\n- The target ECS instances must be in the running (`Running`) status.\n\n- The target ECS instances must have the [Cloud Assistant Agent](~~64921~~) installed in advance.\n\n- The Cloud Assistant Agent version must be higher than the following versions to support file delivery. If the response returns the `ClientNeedUpgrade` error code, update the client to the latest version:\n\n  - Linux: 1.0.2.569\n\n  - Windows: 1.0.0.149\n\n- After Base64 encoding, the file content size must not exceed 32 KB.\n\n- File delivery might fail due to abnormal status of the target ECS instance, network issues, or Cloud Assistant Agent abnormalities. To troubleshoot, invoke [DescribeSendFileResults](~~184117~~) or refer to [Common errors and troubleshooting suggestions for execution failures](~~87029~~)."
  },
  "example": {
    "unifiedCli": "aliyun ecs send-file --biz-region-id cn-hangzhou --resource-group-id rg-bp67acfmxazb4p0000 --name file.txt --description This_is_a_test_file. --timeout 60 --target-dir /home --content-type PlainText --content '#!/bin/bash_echo_Current_User_is_:_echo_ps_grep_awk_print_2_--------_oss://bucketName/objectName' --file-owner test --file-group test --file-mode 0644 --overwrite true --client-token 123e4567-e89b-12d3-a456-426655440000 --instance-id i-bp185dy2o3o6n0000 --tag Value=TestValue Key=TestKey --region cn-hangzhou",
    "legacyCli": "aliyun ecs SendFile --region cn-hangzhou --FileMode 0644 --Tag.1.Key TestKey --Overwrite true --Description 'This_is_a_test_file.' --ResourceGroupId 'rg-bp67acfmxazb4p0000' --ContentType PlainText --Timeout 60 --TargetDir '/home' --FileGroup test --FileOwner test --Name 'file.txt' --Content '#!/bin/bash_echo_Current_User_is_:_echo_ps_grep_awk_print_2_--------_oss://bucketName/objectName' --Tag.1.Value TestValue --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --InstanceId.1 'i-bp185dy2o3o6n0000'"
  }
}