{
  "name": "DescribeUserBusinessBehavior",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The ID of the region. Call the `DescribeRegions` operation to query the latest list of regions."
      }
    },
    {
      "name": "statusKey",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The key of the business attribute to query."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "DescribeUserBusinessBehavior"
  },
  "example": {
    "unifiedCli": "aliyun ecs describe-user-business-behavior --biz-region-id cn-hangzhou --status-key example-string --region cn-hangzhou",
    "legacyCli": "aliyun ecs DescribeUserBusinessBehavior --region cn-hangzhou --statusKey 'example-string' --RegionId 'cn-hangzhou'"
  }
}