{
  "name": "ResetDiskDefaultKMSKeyId",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。您可以调用[DescribeRegions](~~2679950~~)查看最新的阿里云地域列表。",
        "en": "The ID of the region for which you want to disable Account-level EBS Default Encryption. You can call the [DescribeRegions](~~2679950~~) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "重置块存储账号级默认加密使用的密钥",
    "en": "ResetDiskDefaultKMSKeyId"
  },
  "descriptions": {
    "zh": "- RAM用户（子账号）需要`AliyunECSFullAccess`权限，具体授权操作，请参见[为RAM用户授权](~~116146~~)。\n- 指定地域需要开启**块存储账号级默认加密**功能。",
    "en": "- You must grant the RAM user the `AliyunECSFullAccess` permissions. For information about how to grant permissions to a RAM user, see [Grant permissions to a RAM user](~~116146~~).\n\n- Before you can call this operation in a region, make sure that the **Account-level Elastic Block Storage (EBS) Default Encryption** feature is enabled in the region."
  },
  "example": {
    "unifiedCli": "aliyun ecs reset-disk-default-kms-key-id --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun ecs ResetDiskDefaultKMSKeyId --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}