{
  "name": "DescribeVRouters",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "en": "The page number.\n\nDefault value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "en": "The number of entries per page.\n\nValid values: 1 to 50.\n\nDefault value: 10."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the region where the VRouter is deployed. You can call the `DescribeRegions` operation to query the latest region list."
      }
    },
    {
      "name": "VRouterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The ID of the VRouter."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询指定地域的路由器列表",
    "en": "Query the vRouter List in a Specified Region"
  },
  "example": {
    "unifiedCli": "aliyun ecs describe-vrouters --vrouter-id example-string --biz-region-id cn-hangzhou --page-number 1 --page-size 1 --region cn-hangzhou",
    "legacyCli": "aliyun ecs DescribeVRouters --region cn-hangzhou --VRouterId 'example-string' --PageSize 1 --PageNumber 1 --RegionId 'cn-hangzhou'"
  }
}