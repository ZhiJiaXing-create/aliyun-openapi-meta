{
  "name": "AddTags",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源所在的地域。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the resource. You can call [DescribeRegions](~~25609~~) to query the most recent region list."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要绑定标签的资源ID。例如，当资源类型（ResourceType）为实例（instance）时，资源ID可以理解为实例ID。",
        "en": "The ID of the resource to which you want to add tags. For example, when ResourceType is set to instance, ResourceId is the instance ID."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型定义。取值范围：\n\n- instance：ECS实例\n- disk：磁盘\n- snapshot：快照\n- image：镜像\n- securitygroup：安全组\n- volume：存储卷\n- eni：弹性网卡\n- ddh：专有宿主机\n- keypair：SSH密钥对\n- launchtemplate：启动模板\n- reservedinstance：预留实例券\n- snapshotpolicy：自动快照策略\n\n以上取值均为小写。",
        "en": "The type of the resource. Valid values:\n\n- instance: ECS instance\n- disk: cloud disk\n- snapshot: snapshot\n- image: image\n- securitygroup: security group\n- volume: storage volume\n- eni: Elastic Network Interface (ENI)\n- ddh: dedicated host\n- keypair: SSH key pair\n- launchtemplate: launch template\n- reservedinstance: reserved instance\n- snapshotpolicy: automatic snapshot policy\n\nAll valid values are in lowercase."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "标签列表",
        "en": "The tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。N的取值范围：1~20。一旦传入该值，则不允许为空字符串。最多支持128个字符，不能以aliyun和acs:开头，不能包含http://或者https:// 。",
            "en": "The tag key of the resource. Valid values of N: 1 to 20. The tag key cannot be an empty string. The tag key can be up to 128 characters in length and cannot start with aliyun or acs:. The tag key cannot contain http:// or https://."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。N的取值范围：1~20。一旦传入该值，可以为空字符串。最多支持128个字符，不能以aliyun和acs:开头，不能包含http://或者https:// 。",
            "en": "The tag value of the resource. Valid values of N: 1 to 20. The tag value can be an empty string. The tag value can be up to 128 characters in length and cannot start with aliyun or acs:. The tag value cannot contain http:// or https://."
          }
        },
        {
          "name": "key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。\n\n>为提高兼容性，建议您尽量使用Tag.N.Key参数。",
            "en": "The tag key of the resource.\n\n> To improve compatibility, use the Tag.N.Key parameter."
          }
        },
        {
          "name": "value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。\n\n>为提高兼容性，建议您尽量使用Tag.N.Value参数。",
            "en": "The tag value of the resource.\n\n> To improve compatibility, use the Tag.N.Value parameter."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "添加或者覆盖一个或者多个标签到云服务器ECS的各项资源上",
    "en": "Add or overwrite one or more tags on ECS resources"
  },
  "descriptions": {
    "zh": "调用该接口时，您需要注意：\n\n-   单项云服务器ECS资源最多可以添加20个标签。\n-   标签键（`Tag.N.Key`）与标签值（`Tag.N.Value`）必须键值匹配。\n-   如果标签键（`Tag.N.Key`）在指定的资源上已经存在，则使用新的标签值（`Tag.N.Value`）自动覆盖原标签值。",
    "en": "When you invoke this operation, take note of the following items:\n\n-   You can add up to 20 tags to each Elastic Compute Service (ECS) resource.\n-   The tag key (Tag.N.Key) and tag value (Tag.N.Value) must be specified as a key-value pair.\n-   If the tag key (Tag.N.Key) already exists on the specified resource, the original tag value is automatically overwritten with the new tag value (Tag.N.Value)."
  },
  "example": {
    "unifiedCli": "aliyun ecs add-tags --biz-region-id cn-hangzhou --resource-type instance --resource-id i-bp1gtjxuuvwj17zr0000 --tag Value=TestValue key=test --region cn-hangzhou",
    "legacyCli": "aliyun ecs AddTags --region cn-hangzhou --ResourceId 'i-bp1gtjxuuvwj17zr0000' --Tag.1.Value TestValue --ResourceType instance --RegionId 'cn-hangzhou' --Tag.1.key test"
  }
}