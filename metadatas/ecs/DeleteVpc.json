{
  "name": "DeleteVpc",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The ID of the region where the VPC is located. Call the [DescribeRegions](~~DescribeRegions~~) operation to get the latest list of regions."
      }
    },
    {
      "name": "VpcId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the VPC to delete."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "DeleteVpc"
  },
  "example": {
    "unifiedCli": "aliyun ecs delete-vpc --vpc-id example-string --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun ecs DeleteVpc --region cn-hangzhou --VpcId 'example-string' --RegionId 'cn-hangzhou'"
  }
}