{
  "name": "DescribeImagePipelineExecutions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ExecutionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "镜像构建任务ID。",
        "en": "The ID of the image building task."
      }
    },
    {
      "name": "ImagePipelineId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "镜像模板ID。",
        "en": "The ID of the image template."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页行数。取值范围：1~500。\n\n默认值：50。",
        "en": "The number of entries to return on each page. Valid values: 1 to 500.\n\nDefault value: 50."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询凭证（Token）。取值为上一次调用该接口返回的`NextToken`参数值，初次调用接口时无需设置该参数。",
        "en": "The query token. Set the value to the `NextToken` value returned from a previous call to this operation. This parameter is not required for the first call."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "所属的地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID. You can call the [DescribeRegions](~~25609~~) operation to query the latest list of Alibaba Cloud regions."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "镜像构建任务的状态。支持同时设置多个值，每个值之间以半角逗号（,）间隔，格式示例为`BUILDING,DISTRIBUTING`。取值范围：\n\n- PREPARING：准备中。创建临时中转实例等资源。\n- REPAIRING：修复中。修复源镜像。\n- BUILDING：构建中。执行用户自定义命令以及创建镜像。\n- TESTING：测试中。执行用户自定义测试命令。\n- DISTRIBUTING：分发中。执行镜像复制以及共享。\n- RELEASING：资源回收中。构建过程中生成的临时资源。\n- SUCCESS：成功。构建成功。\n- PARTITION_SUCCESS：部分成功。镜像已构建成功，但可能在分发或者资源清理等步骤出现了异常。\n- FAILED：失败。构建镜像失败。\n- TEST_FAILED：测试失败。镜像创建成功，但测试失败。\n- CANCELLING：取消中。正在取消构建流程。\n- CANCELLED：已取消。构建流程已取消。\n\n> 当参数值为空时，默认查询所有状态的镜像构建任务",
        "en": "The status of the image building task. You can specify multiple values, separated by commas. Example: `BUILDING,DISTRIBUTING`. Valid values:\n\n- PREPARING: The system is preparing resources, such as a temporary transit instance.\n\n- REPAIRING: The system is repairing the source image.\n\n- BUILDING: The system is building the image. This includes executing user-defined commands and creating the image.\n\n- TESTING: The system is testing the created image by running user-defined test commands.\n\n- DISTRIBUTING: The system is distributing the image. This includes copying and sharing the image.\n\n- RELEASING: The system is releasing temporary resources generated during the build process.\n\n- SUCCESS: The task completed successfully.\n\n- PARTITION\\_SUCCESS: The task is partially successful. The image was created, but an error may have occurred during distribution or resource cleanup.\n\n- FAILED: The image building task failed.\n\n- TEST\\_FAILED: The image was created successfully, but it failed the user-defined tests.\n\n- CANCELLING: The system is canceling the image building task.\n\n- CANCELLED: The image building task was canceled.\n\n> If you omit this parameter, the operation returns image building tasks of all statuses."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表。",
        "en": "The list of tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签键。N 的取值范围：1~20。",
            "en": "The key of tag N. The value of N can be from 1 to 20."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签值。N 的取值范围：1~20。",
            "en": "The value of tag N. The value of N can be from 1 to 20."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询镜像构建任务的详细信息",
    "en": "DescribeImagePipelineExecutions"
  },
  "descriptions": {
    "zh": "## 接口说明\n\n- 指定的镜像模板ID`ImagePipelineId`不能是已删除的镜像模板，已删除的镜像模板会同步删除对应的构建任务。\n- 镜像模板ID`ImagePipelineId`和构建任务ID`ExecutionId`不能同时为空。\n- 您可以设置`NextToken`查询凭证（Token），其取值是上一次调用`DescribeImagePipelineExecutions`返回的`NextToken`参数值，再通过`MaxResults`设置单页查询的最大条目数进行查询。",
    "en": "## API\n\n- The `ImagePipelineId` cannot refer to a deleted image pipeline. Deleting an image pipeline also deletes its associated build tasks.\n\n- The `ImagePipelineId` and `ExecutionId` parameters cannot both be empty.\n\n- To paginate the results, set `NextToken` to the value returned by the previous call to `DescribeImagePipelineExecutions`. Use `MaxResults` to specify the maximum number of results per page."
  },
  "example": {
    "unifiedCli": "aliyun ecs describe-image-pipeline-executions --biz-region-id cn-hangzhou --tag Value=TestValue Key=TestKey --image-pipeline-id ip-2ze5tsl5bp6nf2b30000 --execution-id exec-5fb8facb8ed7427c0000 --status BUILDING --next-token AAAAAdDWBF20000 --max-results 50 --region cn-hangzhou",
    "legacyCli": "aliyun ecs DescribeImagePipelineExecutions --region cn-hangzhou --Status BUILDING --Tag.1.Key TestKey --NextToken AAAAAdDWBF20000 --ImagePipelineId 'ip-2ze5tsl5bp6nf2b30000' --MaxResults 50 --Tag.1.Value TestValue --RegionId 'cn-hangzhou' --ExecutionId 'exec-5fb8facb8ed7427c0000'"
  }
}