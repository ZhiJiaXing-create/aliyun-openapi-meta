{
  "name": "DescribeCapacityReservations",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceChargeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例的计费方式。取值范围：\n\n- PostPaid：按量付费。\n- PrePaid：包年包月。\n\n默认值：PostPaid。",
        "en": "The billing method of the instance. Valid values:\n\n- PostPaid: pay-as-you-go.\n\n- PrePaid: subscription.\n\nDefault value: PostPaid."
      }
    },
    {
      "name": "InstanceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例规格。通过实例规格只可以查询还在生效中的容量预定服务，已释放的服务只能通过PrivatePoolOptions.Ids查询。",
        "en": "The instance type. You can use this parameter to query only active capacity reservations. To query released capacity reservations, you must specify `PrivatePoolOptions.Ids`."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页行数。\n\n最大值：100。\n\n默认值：10。",
        "en": "The number of entries to return on each page.\n\nMaximum value: 100.\n\nDefault value: 10."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "容量预定服务查询起始标志。由上一次的请求结果中获取。",
        "en": "The query token. Set the value to the `NextToken` value returned in the previous call to retrieve the next page of results."
      }
    },
    {
      "name": "Platform",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例的操作系统。取值范围：\n\n- windows：仅查询Windows系统的容量预定服务。\n- linux：仅查Linux系统的容量预定服务。\n- all：查询所有容量预定服务。\n\n默认值：all。",
        "en": "The operating system of the instance. Valid values:\n\n- windows: Returns only capacity reservations for Windows.\n\n- linux: Returns only capacity reservations for Linux.\n\n- all: Returns all capacity reservations.\n\nDefault value: all."
      }
    },
    {
      "name": "PrivatePoolOptions.Ids",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "容量预定服务ID列表。取值可以由多个ID组成一个JSON数组，最多支持100个ID，ID之间用半角逗号（,）隔开。",
        "en": "The IDs of the capacity reservations. The value can be a JSON array that consists of up to 100 capacity reservation IDs."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "容量预定服务所属地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the capacity reservation. You can call the [DescribeRegions](~~25609~~) operation to query the latest list of Alibaba Cloud regions."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。使用该参数过滤资源时，资源数量不能超过1000个。\n\n>不支持默认资源组过滤。",
        "en": "The ID of the resource group. When you use this parameter to filter resources, the operation returns a maximum of 1,000 resources.\n\n> Filtering by the default resource group is not supported."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "容量预定服务的状态。取值范围：\n\n- All：所有状态。\n- Pending：初始化。指定时间生效的容量预定会先进入初始化状态。\n- Preparing：准备中。指定时间生效的容量预定在资源交付进行阶段，处于准备中状态。\n- Prepared：待生效。指定时间生效的容量预定在资源交付完成，服务正式生效之前，处于待生效状态。\n- Active：生效中。\n- Released：已释放，包括手动释放与到期自动释放。\n\n若不指定，则查询除Pending、Released以外状态的容量预定。",
        "en": "The status of the capacity reservation. Valid values:\n\n- All: all statuses.\n\n- Pending: The capacity reservation is initializing. This is the initial status of a scheduled capacity reservation.\n\n- Preparing: The system is preparing resources for the scheduled capacity reservation.\n\n- Prepared: The resources are prepared, and the scheduled capacity reservation is waiting to take effect.\n\n- Active: The capacity reservation is active.\n\n- Released: The capacity reservation is released, either manually or automatically upon expiration.\n\nIf you do not specify this parameter, the operation returns capacity reservations in all states except `Pending` and `Released`."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "容量预定服务绑定的标签对集合。",
        "en": "The tags attached to the capacity reservations."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签键。N表示可以设置多个标签键进行过滤，N的取值范围为1~20。\n\n使用一个标签过滤资源，查询到该标签下的资源数量不能超过1000个；使用多个标签过滤资源，查询到同时绑定了多个标签的资源数量不能超过1000个。如果资源数量超过1000个，请使用[ListTagResources](~~110425~~)接口进行查询。",
            "en": "The key of the Nth tag. You can specify up to 20 tags.\n\nA maximum of 1,000 resources that match the specified tags can be returned. If you specify multiple tags, only resources that have all of these tags are returned. If the number of matching resources exceeds 1,000, call the [ListTagResources](~~110425~~) operation to query the resources."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签值。N表示可以设置多个标签值进行过滤，N的取值范围为1~20。",
            "en": "The value of the Nth tag. You can specify up to 20 tags."
          }
        }
      ]
    },
    {
      "name": "ZoneId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "容量预定服务所属的可用区ID。",
        "en": "The zone ID of the capacity reservation."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询容量预定服务的信息",
    "en": "DescribeCapacityReservations"
  },
  "example": {
    "unifiedCli": "aliyun ecs describe-capacity-reservations --biz-region-id cn-hangzhou --resource-group-id rg-bp67acfmxazb4p0000 --tag Value=TestValue Key=TestKey --max-results 10 --next-token caeba0bbb2be03f84eb48b699f0a4883 --private-pool-options-ids crp-bp1gubrkqutenqdd0000_,_crp-bp67acfmxazb50000 --platform linux --instance-type ecs.c6.large --zone-id cn-hangzhou-h --instance-charge-type PostPaid --status Active --region cn-hangzhou",
    "legacyCli": "aliyun ecs DescribeCapacityReservations --region cn-hangzhou --Status Active --Tag.1.Key TestKey --ResourceGroupId 'rg-bp67acfmxazb4p0000' --NextToken caeba0bbb2be03f84eb48b699f0a4883 --Platform linux --ZoneId 'cn-hangzhou-h' --InstanceChargeType PostPaid --MaxResults 10 --Tag.1.Value TestValue --PrivatePoolOptions.Ids 'crp-bp1gubrkqutenqdd0000_,_crp-bp67acfmxazb50000' --RegionId 'cn-hangzhou' --InstanceType 'ecs.c6.large'"
  }
}