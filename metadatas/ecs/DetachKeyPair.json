{
  "name": "DetachKeyPair",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceIds",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "解绑SSH密钥对的实例ID。取值可以是由多台实例ID组成的一个JSON数组，最多支持50个ID，ID之间用半角逗号（,）隔开。",
        "en": "The IDs of the instances from which you want to unbind the SSH key pair. The value is a JSON array that consists of up to 50 instance IDs. Separate multiple IDs with commas (,)."
      }
    },
    {
      "name": "KeyPairName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "SSH密钥对名称。",
        "en": "The name of the SSH key pair."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "SSH密钥对所在的地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the SSH key pair. You can call [DescribeRegions](~~25609~~) to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "Linux实例解绑密钥对",
    "en": "Unbind SSH key pairs from linux instances"
  },
  "descriptions": {
    "zh": "使用该接口时，请注意：\n\n-  解绑SSH密钥对后，您需要重启实例（[RebootInstance](~~25502~~)）使更改生效。  \n-  解绑SSH密钥对后，实例默认使用用户名和密码的验证方式。",
    "en": "Take note of the following items when you call this operation:\n\n-  After you unbind an SSH key pair, restart the instance ([RebootInstance](~~25502~~)) for the changes to take effect.  \n-  After you unbind an SSH key pair, the instance uses the username and password authentication method by default."
  },
  "example": {
    "unifiedCli": "aliyun ecs detach-key-pair --biz-region-id cn-hangzhou --biz-key-pair-name testKeyPairName --instance-ids 'i-bp1d6tsvznfghy7y0000_,_i-bp1ippxbaql9zet70000_,_…_i-bp1ib7bcz07l0000' --region cn-hangzhou",
    "legacyCli": "aliyun ecs DetachKeyPair --region cn-hangzhou --KeyPairName testKeyPairName --RegionId 'cn-hangzhou' --InstanceIds 'i-bp1d6tsvznfghy7y0000_,_i-bp1ippxbaql9zet70000_,_…_i-bp1ib7bcz07l0000'"
  }
}