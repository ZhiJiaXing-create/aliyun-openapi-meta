{
  "name": "ModifyDeploymentSetAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Affinity",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "部署集的亲和度级别，在部署集的实例会按该亲和度分布，取值范围：1-10，默认为1。\n><notice>亲和度设置仅在部署策略为高可用(`Strategy = Availability`)可用。></notice>",
        "en": "The affinity level of the deployment set. Instances in the deployment set are distributed based on this affinity level. Valid values: 1 to 10. Default value: 1.><notice> The affinity setting is available only when the deployment strategy is set to high availability (`Strategy = Availability`).></notice>"
      }
    },
    {
      "name": "DeploymentSetId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "部署集ID。",
        "en": "The ID of the deployment set."
      }
    },
    {
      "name": "DeploymentSetName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "新的部署集名称。长度为2~128个字符，支持Unicode中letter分类下的字符（其中包括英文、中文和数字等）。可以包含半角冒号（:）、下划线（_）、半角句号（.）或者短划线（-）。",
        "en": "The new name of the deployment set. The name must be 2 to 128 characters long and can contain letters, digits, Chinese characters, colons (:), underscores (\\_), periods (.), and hyphens (-)."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "新的部署集描述信息。长度为2~256个英文或中文字符，不能以http://和https://开头。",
        "en": "The new description of the deployment set. It must be 2 to 256 characters long and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "部署集所属地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the deployment set. You can call the [DescribeRegions](~~25609~~) operation to obtain the latest list of Alibaba Cloud regions."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改部署集的名称和描述信息",
    "en": "ModifyDeploymentSetAttribute"
  },
  "example": {
    "unifiedCli": "aliyun ecs modify-deployment-set-attribute --deployment-set-id ds-bp1frxuzdg87zh4p0000 --description TestDescription --deployment-set-name DeploymentSetTestName --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun ecs ModifyDeploymentSetAttribute --region cn-hangzhou --DeploymentSetId 'ds-bp1frxuzdg87zh4p0000' --Description TestDescription --DeploymentSetName DeploymentSetTestName --RegionId 'cn-hangzhou'"
  }
}