{
  "name": "CreateDisk",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AdvancedFeatures",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "该参数暂未开放使用。",
        "en": "This parameter is not publicly available."
      }
    },
    {
      "name": "Arn",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": ">该参数暂未开放使用。",
        "en": "> This parameter is not publicly available."
      },
      "sub_parameters": [
        {
          "name": "AssumeRoleFor",
          "position": "Query",
          "type": "Long",
          "required": false,
          "description": {
            "zh": ">该参数暂未开放使用。",
            "en": "> This parameter is not publicly available."
          }
        },
        {
          "name": "RoleType",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": ">该参数暂未开放使用。",
            "en": "> This parameter is not publicly available."
          }
        },
        {
          "name": "Rolearn",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": ">该参数暂未开放使用。",
            "en": "> This parameter is not publicly available."
          }
        }
      ]
    },
    {
      "name": "BurstingEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启Burst（性能突发）。取值范围：\n\n- true：是。\n- false：否。\n\n>当`DiskCategory`取值为`cloud_auto`时才支持设置该参数。更多信息，请参见[ESSD AutoPL云盘](~~368372~~)。",
        "en": "Specifies whether to enable the performance burst feature. Valid values:\n\n- true: enables the performance burst feature.\n- false: does not enable the performance burst feature.\n\n> This parameter is supported only when `DiskCategory` is set to `cloud_auto`. For more information, see [ESSD AutoPL disks](~~368372~~)."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。**ClientToken**只支持ASCII字符，且不能超过64个字符。更多信息，请参见[如何保证幂等性](~~25693~~)。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. The **ClientToken** value can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "磁盘描述。长度为2~256个英文或中文字符，不能以`http://`或`https://`开头。\n\n默认值：空。",
        "en": "The description of the disk. The description must be 2 to 256 characters in length and cannot start with `http://` or `https://`.\n\nDefault value: empty."
      }
    },
    {
      "name": "DiskCategory",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据盘的磁盘种类。取值范围：\n\n- cloud：普通云盘。\n- cloud_efficiency：高效云盘。\n- cloud_ssd：SSD云盘。\n- cloud_essd：ESSD云盘。\n- cloud_auto：ESSD AutoPL云盘。\n- cloud_essd_entry：ESSD Entry云盘。\n- cloud_regional_disk_auto：ESSD同城冗余云盘。\n- elastic_ephemeral_disk_standard：弹性临时盘-标准版。\n- elastic_ephemeral_disk_premium：弹性临时盘-高级版。\n\n默认值：cloud。",
        "en": "The category of the data disk. Valid values:\n\n- cloud: basic disk.\n- cloud_efficiency: ultra disk.\n- cloud_ssd: standard SSD.\n- cloud_essd: enterprise SSD.\n- cloud_auto: ESSD AutoPL disk.\n- cloud_essd_entry: ESSD Entry disk.\n- cloud_regional_disk_auto: regional Enterprise SSD (ESSD).\n- elastic_ephemeral_disk_standard: elastic ephemeral disk - Standard Edition.\n- elastic_ephemeral_disk_premium: elastic ephemeral disk - Premium Edition.\n\nDefault value: cloud."
      }
    },
    {
      "name": "DiskName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "磁盘名称。长度为2~128个字符，支持Unicode中letter分类下的字符（其中包括英文、中文等），ASCII数字（0-9）。可以包含半角冒号（:）、下划线（_）、半角句号（.）或者短划线（-）。必须以Unicode中letter分类下的字符开头。\n\n默认值：空。",
        "en": "The name of the disk. The name must be 2 to 128 characters in length and can contain letters in the Unicode character set (including English and Chinese characters) and ASCII digits (0-9). The name can contain colons (:), underscores (_), periods (.), or hyphens (-). The name must start with a letter in the Unicode character set.\n\nDefault value: empty."
      }
    },
    {
      "name": "EncryptAlgorithm",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "该参数暂未开放使用。",
        "en": "This parameter is not publicly available."
      }
    },
    {
      "name": "Encrypted",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否加密云盘。取值范围：\n\n- true：是。\n- false：否。\n\n默认值：false。",
        "en": "Specifies whether to encrypt the disk. Valid values:\n\n- true: encrypts the disk.\n- false: does not encrypt the disk.\n\nDefault value: false."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "创建一块包年包月磁盘，并自动挂载到指定的包年包月实例（InstanceId）上。\n\n- 设置实例ID后，会忽略您设置的ResourceGroupId、Tag.N.Key、Tag.N.Value、ClientToken和KMSKeyId参数。\n- 您不能同时指定ZoneId和InstanceId。\n\n默认值：空，代表创建的是按量付费云盘，云盘所属地由RegionId和ZoneId确定。",
        "en": "The ID of the subscription instance to which the created subscription disk is automatically attached.\n\n- After you specify the instance ID, the ResourceGroupId, Tag.N.Key, Tag.N.Value, ClientToken, and KMSKeyId parameters are ignored.\n- You cannot specify both ZoneId and InstanceId.\n\nDefault value: empty. This indicates that a pay-as-you-go disk is created. The region of the disk is determined by RegionId and ZoneId."
      }
    },
    {
      "name": "KMSKeyId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云盘使用的KMS密钥ID。\n\n> 若Encrypted设置为true，不指定KMSKeyId，则使用默认的密钥进行加密，并在创建实例成功后返回KMSKeyId值。\n> - - 云盘通过非共享而来的加密快照创建：默认使用该快照所使用的加密密钥进行加密。\n> - - 云盘通过共享而来的加密快照创建：默认使用服务密钥进行加密。\n> - - 在已开启块存储账号级默认加密的地域创建：默认使用指定的账号级密钥进行加密。\n> - - 其他情况：默认使用服务密钥进行加密。",
        "en": "The ID of the Key Management Service (KMS) key used by the disk.\n\n> If Encrypted is set to true and KMSKeyId is not specified, the default key is used for encryption. The KMSKeyId value is returned after the instance is created.\n> - - If the disk is created from a non-shared encrypted snapshot: the encryption key used by the snapshot is used by default.\n> - - If the disk is created from a shared encrypted snapshot: the service key is used by default.\n> - - If the disk is created in a region where account-level default encryption for block storage is enabled: the specified account-level key is used by default.\n> - - In other cases: the service key is used by default."
      }
    },
    {
      "name": "MultiAttach",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启多重挂载特性。取值范围：\n\n- Disabled: 不开启。\n- Enabled : 开启。目前仅ESSD云盘支持设置为`Enabled`。\n\n默认值：Disabled。\n\n> 开启多重挂载特性的云盘只支持按量付费的计费方式。因此`MultiAttach=Enabled `时，不能同时设置`InstanceId`参数。您可以在创建云盘后调用[AttachDisk](~~25515~~)进行挂载，但需要注意开启多重挂载功能的云盘只能作为数据盘进行挂载。",
        "en": "Specifies whether to enable the multi-attach attribute. Valid values:\n\n- Disabled: disables the multi-attach attribute.\n- Enabled: enables the multi-attach attribute. Only enterprise SSDs support this value.\n\nDefault value: Disabled.\n\n> Disks with the multi-attach attribute enabled support only the pay-as-you-go billing method. Therefore, when `MultiAttach=Enabled`, you cannot specify the `InstanceId` parameter. You can invoke [AttachDisk](~~25515~~) to attach the disk after it is created. Note that a disk with the multi-attach feature enabled can be attached only as a data disk."
      }
    },
    {
      "name": "PerformanceLevel",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "创建一块ESSD云盘时，设置云盘的性能等级。取值范围：\n\n- PL0：单盘最高随机读写IOPS 1万。\n- PL1：单盘最高随机读写IOPS 5万。\n- PL2：单盘最高随机读写IOPS 10万。\n- PL3：单盘最高随机读写IOPS 100万。\n\n默认值：PL1。\n\n有关如何选择ESSD性能等级，请参见[ESSD云盘](~~122389~~)。",
        "en": "The performance level of the enterprise SSD. Valid values:\n\n- PL0: a single disk can deliver up to 10,000 random read/write IOPS.\n- PL1: a single disk can deliver up to 50,000 random read/write IOPS.\n- PL2: a single disk can deliver up to 100,000 random read/write IOPS.\n- PL3: a single disk can deliver up to 1,000,000 random read/write IOPS.\n\nDefault value: PL1.\n\nFor information about how to select an ESSD performance level, see [ESSD cloud disks](~~122389~~)."
      }
    },
    {
      "name": "ProvisionedIops",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "ESSD AutoPL云盘（单盘）预配置性能的读写IOPS。可能值：\n\n- 容量（GiB）<=3：不支持设置预配置性能。\n\n- 容量（GiB）>=4：[0，min{(1,000\n\n IOPS/GiB*容量-基准IOPS)，50,000}]\n\n\n基准性能=max{min{1,800+50*容量，50,000}, 3,000}。\n\n\n>当`DiskCategory` = `cloud_auto`时才支持设置该参数。更多信息，请参见[ESSD AutoPL云盘](~~368372~~)。",
        "en": "The provisioned read/write IOPS of the ESSD AutoPL disk. Valid values:\n\n- Capacity (GiB) <= 3: provisioned performance is not supported.\n\n- Capacity (GiB) >= 4: [0, min{(1,000 IOPS/GiB × Capacity - Baseline IOPS), 50,000}]\n\n\nBaseline performance = max{min{1,800 + 50 × Capacity, 50,000}, 3,000}.\n\n\n> This parameter is supported only when `DiskCategory` is set to `cloud_auto`. For more information, see [ESSD AutoPL disks](~~368372~~)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "所属的地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID. You can call [DescribeRegions](~~25609~~) to query the most recent region list."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "磁盘所在的企业资源组ID。",
        "en": "The ID of the resource group to which the disk belongs."
      }
    },
    {
      "name": "Size",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "容量大小。单位：GiB。您必须为该参数传入参数值。取值范围：\n\n-   cloud：5~2,000。\n-   cloud_efficiency：20~32,768。\n-   cloud_ssd：20~32,768。\n-   cloud_essd：具体取值范围与`PerformanceLevel`的取值有关。\n    - PL0：1~65,536。\n    - PL1：20~65,536。\n    - PL2：461~65,536。\n    - PL3：1,261~65,536。\n- cloud_auto：1~65,536。\n- cloud_essd_entry：10~32,768。\n- cloud_regional_disk_auto：10~65,536。\n- elastic_ephemeral_disk_standard：64～8,192。\n- elastic_ephemeral_disk_premium：64～8,192。\n\n如果您指定了`SnapshotId`参数，`SnapshotId`参数和`Size`参数存在以下限制：\n\n- 如果`SnapshotId`参数对应的快照容量大于设置的`Size`参数值，实际创建的云盘大小为指定快照的大小。\n- 如果`SnapshotId`参数对应的快照容量小于设置的`Size`参数值，实际创建的云盘大小为指定的`Size`参数值。",
        "en": "The size of the disk. Unit: GiB. You must specify a value for this parameter. Valid values:\n\n-   cloud: 5 to 2,000.\n-   cloud_efficiency: 20 to 32,768.\n-   cloud_ssd: 20 to 32,768.\n-   cloud_essd: The valid value range depends on the value of `PerformanceLevel`.\n    - PL0: 1 to 65,536.\n    - PL1: 20 to 65,536.\n    - PL2: 461 to 65,536.\n    - PL3: 1,261 to 65,536.\n- cloud_auto: 1 to 65,536.\n- cloud_essd_entry: 10 to 32,768.\n- cloud_regional_disk_auto: 10 to 65,536.\n- elastic_ephemeral_disk_standard: 64 to 8,192.\n- elastic_ephemeral_disk_premium: 64 to 8,192.\n\nIf you specify `SnapshotId`, the `SnapshotId` and `Size` parameters have the following restrictions:\n\n- If the snapshot specified by `SnapshotId` is larger than the specified `Size` value, the snapshot size of the created disk equals the size of the specified snapshot.\n- If the snapshot specified by `SnapshotId` is smaller than the specified `Size` value, the snapshot size of the created disk equals the specified `Size` value."
      }
    },
    {
      "name": "SnapshotId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "创建云盘使用的快照ID。2013年7月15日及以前的快照不能用来创建云盘。 \n\n`SnapshotId`参数和`Size`参数存在以下限制：\n\n- 如果`SnapshotId`参数对应的快照容量大于设置的`Size`参数值，实际创建的云盘大小为指定快照的大小。\n- 如果`SnapshotId`参数对应的快照容量小于设置的`Size`参数值，实际创建的云盘大小为指定的`Size`参数值。\n- 不支持使用快照创建弹性临时盘。",
        "en": "The ID of the snapshot used to create the disk. Snapshots created on or before July 15, 2013 cannot be used to create disks. \n\nThe `SnapshotId` and `Size` parameters have the following restrictions:\n\n- If the snapshot specified by `SnapshotId` is larger than the specified `Size` value, the snapshot size of the created disk equals the size of the specified snapshot.\n- If the snapshot specified by `SnapshotId` is smaller than the specified `Size` value, the snapshot size of the created disk equals the specified `Size` value.\n- Elastic ephemeral disks cannot be created from snapshots."
      }
    },
    {
      "name": "StorageClusterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专属块存储集群ID。如果您需要在指定的专属块存储集群中创建云盘，请指定该参数。\n\n> 存储集相关参数（`StorageSetId`、`StorageSetPartitionNumber`）与专属块存储集群参数（`StorageClusterId`）只能选择其中之一设置。如果同时设置，那么调用接口将会失败。",
        "en": "The ID of the dedicated block storage cluster. To create a disk in a specific dedicated block storage cluster, specify this parameter.\n\n> The storage set parameters (`StorageSetId` and `StorageSetPartitionNumber`) and the dedicated block storage cluster parameter (`StorageClusterId`) are mutually exclusive. If you specify both, the API call fails."
      }
    },
    {
      "name": "StorageSetId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "存储集ID。\n\n> 存储集相关参数（`StorageSetId`、`StorageSetPartitionNumber`）与专属块存储集群参数（`StorageClusterId`）只能选择其中之一设置。如果同时设置，那么调用接口将会失败。",
        "en": "The ID of the storage set.\n\n> The storage set parameters (`StorageSetId` and `StorageSetPartitionNumber`) and the dedicated block storage cluster parameter (`StorageClusterId`) are mutually exclusive. If you specify both, the API call fails."
      }
    },
    {
      "name": "StorageSetPartitionNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "存储集分区数。取值范围：大于等于2，最高不能超过调用[DescribeAccountAttributes](~~73772~~)后显示的权益配额限制。\n\n默认值：2。",
        "en": "The number of partitions in the storage set. Valid values: greater than or equal to 2, up to the privilege quota limit returned by [DescribeAccountAttributes](~~73772~~).\n\nDefault value: 2."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "磁盘的标签信息列表。",
        "en": "The list of tags for the disk."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "磁盘的标签键。N的取值范围：1~20。一旦传入Tag.N.Key值，则不允许为空字符串。最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag key of the disk. Valid values of N: 1 to 20. The tag key cannot be an empty string. The tag key can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "磁盘的标签值。N的取值范围：1~20。一旦传入Tag.N.Value值，可以为空字符串。最多支持128个字符，不能包含`http://`或者`https://`。",
            "en": "The tag value of the disk. Valid values of N: 1 to 20. The tag value can be an empty string. The tag value can be up to 128 characters in length and cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "磁盘的标签键。\n\n>为提高代码兼容性，请尽量使用Tag.N.Key参数。",
            "en": "The tag key of the disk.\n\n> For better code compatibility, use Tag.N.Key instead."
          }
        },
        {
          "name": "value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "磁盘的标签值。\n\n> 为提高代码兼容性，请尽量使用Tag.N.Value参数。",
            "en": "The tag value of the disk.\n\n> For better code compatibility, use Tag.N.Value instead."
          }
        }
      ]
    },
    {
      "name": "ZoneId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "在指定可用区内创建一块按量付费磁盘。\n\n- 如果您不设置InstanceId，则ZoneId为必填参数。\n- 您不能同时指定ZoneId和InstanceId。\n\n\n> `cloud_regional_disk_auto`类型的磁盘无需设置 ZoneId。",
        "en": "The zone in which to create a pay-as-you-go disk.\n\n- If you do not specify InstanceId, ZoneId is required.\n- You cannot specify both ZoneId and InstanceId.\n\n\n> Disks of the `cloud_regional_disk_auto` type do not require ZoneId to be specified."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建数据盘",
    "en": "Create a data disk"
  },
  "descriptions": {
    "zh": "- 创建磁盘时支持开启多重挂载特性（`MultiAttach`）。建议您先了解该特性及使用限制，更多信息，请参见[ESSD云盘支持NVMe](~~256487~~)以及[使用多重挂载功能](~~262105~~)。\n- 支持创建的磁盘类型：普通云盘、高效云盘、SSD云盘、ESSD云盘、ESSD Entry云盘、ESSD同城冗余、ESSD AutoPL云盘、弹性临时盘-标准版和弹性临时盘-高级版。\n- 创建磁盘需要通过实名认证。请前往会员信息[实名认证](https://account.console.aliyun.com/#/auth/home)。\n- 创建磁盘会涉及到资源计费，建议您提前了解云服务器ECS的计费方式。更多信息，请参见[计费概述](~~25398~~)。\n\n- 创建磁盘时，默认设置：\n\n  - 删除磁盘时删除其自动快照，即`DeleteAutoSnapshot`取值为`true`。您可以调用[DescribeDisks](~~2679767~~)查询该参数配置，调用[ModifyDiskAttribute](~~25517~~)修改该参数。\n  - 创建ESSD云盘时，如果您不设置磁盘性能等级，默认为PL1等级。您可以通过[ModifyDiskSpec](~~123780~~)修改磁盘性能等级。\n  - 计费方式为按量付费，即默认`Portable`属性为`true`。",
    "en": "- You can enable the multi-attach attribute (`MultiAttach`) when you create a disk. Learn about this attribute and its limits before you use it. For more information, see [ESSD cloud disks that support NVMe](~~256487~~) and [Use the multi-attach feature](~~262105~~).\n- The following disk types can be created: basic disks, ultra disks, standard SSDs, enterprise SSDs, ESSD Entry disks, regional Enterprise SSDs (ESSDs), ESSD AutoPL disks, elastic ephemeral disks - Standard Edition, and elastic ephemeral disks - Premium Edition.\n- Complete real-name registration before you create a disk. Go to [Real-name registration](https://account.console.aliyun.com/#/auth/home) in your account information.\n- Creating a disk incurs charges. Learn about the billing methods of Elastic Compute Service (ECS) in advance. For more information, see [Billing overview](~~25398~~).\n\n- Default settings when you create a disk:\n\n  - Automatic snapshots of the disk are deleted when the disk is deleted. That is, `DeleteAutoSnapshot` is set to `true`. You can invoke [DescribeDisks](~~2679767~~) to query the parameter settings, and invoke [ModifyDiskAttribute](~~25517~~) to modify the property.\n  - When you create an enterprise SSD, if you do not configure the performance level, the default level is PL1. You can invoke [ModifyDiskSpec](~~123780~~) to change the performance level.\n  - The billing method is pay-as-you-go. That is, the `Portable` property defaults to `true`."
  },
  "example": {
    "unifiedCli": "aliyun ecs create-disk --biz-region-id cn-hangzhou --zone-id cn-hangzhou-g --snapshot-id s-bp67acfmxazb4p0000 --disk-name testDiskName --size 2000 --disk-category cloud_ssd --description testDescription --encrypted false --client-token 123e4567-e89b-12d3-a456-426655440000 --instance-id i-bp18pnlg1ds9rky40000 --resource-group-id rg-bp67acfmxazb4p0000 --kms-key-id 0e478b7a-4262-4802-b8cb-00d3fb40826X --performance-level PL1 --advanced-features hide --storage-set-id ss-bp67acfmxazb4p0000 --encrypt-algorithm hide --storage-set-partition-number 3 --storage-cluster-id dbsc-j5e1sf2vaf5he8m20000 --multi-attach Disabled --tag Value=TestValue key=Test --arn AssumeRoleFor=1000000000 RoleType=hide Rolearn=hide --provisioned-iops 40000 --bursting-enabled false --region cn-hangzhou",
    "legacyCli": "aliyun ecs CreateDisk --region cn-hangzhou --Description testDescription --KMSKeyId '0e478b7a-4262-4802-b8cb-00d3fb40826X' --ResourceGroupId 'rg-bp67acfmxazb4p0000' --Size 2000 --Encrypted false --StorageSetPartitionNumber 3 --MultiAttach Disabled --EncryptAlgorithm hide --StorageSetId 'ss-bp67acfmxazb4p0000' --Tag.1.key Test --BurstingEnabled false --StorageClusterId 'dbsc-j5e1sf2vaf5he8m20000' --ZoneId 'cn-hangzhou-g' --InstanceId 'i-bp18pnlg1ds9rky40000' --PerformanceLevel PL1 --DiskCategory cloud_ssd --Arn.1.Rolearn hide --DiskName testDiskName --ProvisionedIops 40000 --Arn.1.AssumeRoleFor 1000000000 --SnapshotId 's-bp67acfmxazb4p0000' --Tag.1.Value TestValue --Arn.1.RoleType hide --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --AdvancedFeatures hide"
  }
}