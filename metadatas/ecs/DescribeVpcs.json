{
  "name": "DescribeVpcs",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IsDefault",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "en": "Specifies whether the VPC is a default VPC. Set to `true` to query the default VPC, or `false` to query non-default VPCs."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "en": "The page number. Default: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "en": "The number of entries to return on each page. Default: 10. Valid values: 1 to 50."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the region."
      }
    },
    {
      "name": "VpcId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The ID of the VPC."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "DescribeVpcs"
  },
  "example": {
    "unifiedCli": "aliyun ecs describe-vpcs --vpc-id example-string --biz-region-id cn-hangzhou --is-default true --page-number 1 --page-size 1 --region cn-hangzhou",
    "legacyCli": "aliyun ecs DescribeVpcs --region cn-hangzhou --IsDefault true --VpcId 'example-string' --PageSize 1 --PageNumber 1 --RegionId 'cn-hangzhou'"
  }
}