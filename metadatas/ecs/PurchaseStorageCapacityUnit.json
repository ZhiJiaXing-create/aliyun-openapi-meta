{
  "name": "PurchaseStorageCapacityUnit",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Amount",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "购买的SCU的数量。取值范围：1~20。\n\n默认值：1。",
        "en": "The number of SCUs that you want to purchase. Valid values: 1 to 20.\n\nDefault value: 1."
      }
    },
    {
      "name": "Capacity",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "SCU容量大小，单位为GiB。取值范围：20、40、100、200、500、1024、2048、5210、10240、20480、52100。",
        "en": "The capacity of the SCU. Unit: GiB. Valid values: 20, 40, 100, 200, 500, 1024, 2048, 5210, 10240, 20480, and 52100."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。`ClientToken`只支持ASCII字符，且不能超过64个字符。更多信息，请参见[如何保证幂等性](~~25693~~)。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique across requests. The `token` can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "SCU的描述信息，长度为2~256个英文或中文字符，不能以`http://`或`https://`开头。",
        "en": "The description of the SCU. The description must be 2 to 256 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "FromApp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于备注请求来源，默认值为OpenAPI，无需手动设置。",
        "en": "The source of the request. The value is automatically set to OpenAPI and does not need to be changed. Default value: OpenAPI."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "SCU的名称，长度为2~128个英文或中文字符。必须以大小写字母或中文开头，不能以`http://`或`https://`开头。可以包含数字、半角冒号（:）、下划线（_）或者短划线（-）。",
        "en": "The name of the SCU. The name must be 2 to 128 characters in length. It must start with a letter and cannot start with `http://` or `https://`. The name can contain letters, digits, colons (:), underscores (\\_), and hyphens (-)."
      }
    },
    {
      "name": "Period",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "SCU的有效期时长。取值范围：\n\n- 当PeriodUnit=Month时，Period参数的有效取值为1、2、3、6。\n- 当PeriodUnit=Year时，Period参数的有效取值为1、3、5。\n\n默认值：1。",
        "en": "The validity period of the SCU. Valid values:\n\n- Valid values when PeriodUnit is set to Month: 1, 2, 3, and 6.\n\n- Valid values when PeriodUnit is set to Year: 1, 3, and 5.\n\nDefault value: 1."
      }
    },
    {
      "name": "PeriodUnit",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "SCU的有效期时长单位。取值范围：\n\n- Month：月。\n- Year：年。\n\n默认值：Month。",
        "en": "The unit of the validity period of the SCU. Valid values:\n\n- Month\n\n- Year\n\nDefault value: Month."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "SCU所属的地域ID。确定地域后，SCU只能抵扣该地域下云盘的按量付费账单。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The ID of the region in which to purchase the SCU. The purchased SCU can offset the bills of pay-as-you-go disks that reside in the specified region. You can call the [DescribeRegions](~~25609~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "存储容量单位包所在的资源组ID。您只能传入已有权限的资源组ID。",
        "en": "The ID of the resource group to which to add the SCU. You can specify only the IDs of the resource groups that you have permissions to access."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "SCU的生效时间。生效时间不能超过创建时间开始的180天。按照[ISO 8601](~~25696~~)标准表示，并使用UTC +0时间，格式为yyyy-MM-ddTHHZ。\n\n默认值：无，表示从创建时间开始生效。",
        "en": "The time at which the SCU takes effect. The time can be up to 180 days from the creation time of the SCU. Specify the time in the [ISO 8601](~~25696~~) standard in the yyyy-MM-ddTHHZ format. The time must be in UTC.\n\nThis parameter is left empty by default, which indicates that the SCU takes effect immediately after it is created."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签数组。数组长度：1~20。",
        "en": "The tags to add to the SCU. You can specify up to 20 tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "SCU的标签键。一旦传入该值，则不允许为空字符串。最多支持128个字符，不能以`aliyun`或`acs:`开头，不能包含`http://`或`https://`。",
            "en": "The key of tag N to add to the SCU. The tag key cannot be an empty string. The tag key can be up to 128 characters in length and cannot contain `http://` or `https://`. The tag key cannot start with `acs:` or `aliyun`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "SCU的标签值。一旦传入该值，可以为空字符串。最多支持128个字符，不能以`acs:`开头，不能包含`http://`或`https://`。",
            "en": "The value of tag N to add to the SCU. The tag value can be an empty string. The tag value can be up to 128 characters in length and cannot contain `http://` or `https://`. The tag value cannot start with `acs:`."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "购买存储容量单位包",
    "en": "PurchaseStorageCapacityUnit"
  },
  "descriptions": {
    "zh": "<props=\"china\">\n\n**请确保在使用该接口前，已充分了解存储容量单位包的[计费方式](~~137897~~)和[价格](https://www.aliyun.com/price/product#/ecs/detail)。**\n\n</props>\n\n<props=\"intl\">\n\n**请确保在使用该接口前，已充分了解存储容量单位包的[计费方式](~~137897~~)和[价格](https://www.alibabacloud.com/zh/pricing-calculator#/commodity/vm_intl)。**\n\n</props>",
    "en": "\\*\\*Before you call this operation, make sure that you understand the [billing methods](~~137897~~) and [pricing](https://www.alibabacloud.com/zh/pricing-calculator#/commodity/vm_intl) of SCUs."
  },
  "example": {
    "unifiedCli": "aliyun ecs purchase-storage-capacity-unit --biz-region-id cn-hangzhou --name ScuPurchaseDemo --capacity 20 --description ScuPurchaseDemo --start-time 2020-09-09T02Z --period 1 --period-unit Month --from-app OpenAPI --client-token 123e4567-e89b-12d3-a456-426655440000 --amount 1 --tag Value=TestValue Key=TestKey --resource-group-id rg-bp67acfmxazb4p0000 --region cn-hangzhou",
    "legacyCli": "aliyun ecs PurchaseStorageCapacityUnit --region cn-hangzhou --Tag.1.Key TestKey --Description ScuPurchaseDemo --ResourceGroupId 'rg-bp67acfmxazb4p0000' --Amount 1 --StartTime '2020-09-09T02Z' --Period 1 --Name ScuPurchaseDemo --Capacity 20 --Tag.1.Value TestValue --RegionId 'cn-hangzhou' --FromApp OpenAPI --ClientToken '123e4567-e89b-12d3-a456-426655440000' --PeriodUnit Month"
  }
}