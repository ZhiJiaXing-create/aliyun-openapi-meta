{
  "name": "DeleteKeyPairs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "KeyPairNames",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "SSH密钥对名称。取值可以由多个SSH密钥对名称组成一个JSON数组，最多支持100对SSH密钥对，名称之间用半角逗号（,）隔开。\n\n>在进行删除SSH密钥对前，您可以调用[DescribeKeyPairs](~~51773~~)查询已有密钥对。",
        "en": "The names of SSH key pairs. The value can be a JSON array that consists of up to 100 SSH key pair names. Separate multiple names with commas (,).\n\n>Before you delete SSH key pairs, you can call [DescribeKeyPairs](~~51773~~) to query existing key pairs."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "SSH密钥对所在的地域。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region where the SSH key pairs reside. You can call [DescribeRegions](~~25609~~) to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量删除密钥对",
    "en": "Delete key pairs in batches"
  },
  "descriptions": {
    "zh": "删除SSH密钥对后，您需要注意：\n\n- 无法通过[DescribeKeyPairs](~~51773~~)查询该SSH密钥对。  \n- 若已有ECS实例绑定了该SSH密钥对，则该SSH密钥对不支持被删除。",
    "en": "After you delete an SSH key pair, take note of the following items:\n\n- You cannot query the SSH key pair by calling [DescribeKeyPairs](~~51773~~).\n- If an ECS instance is bound to the SSH key pair, the SSH key pair cannot be deleted."
  },
  "example": {
    "unifiedCli": "aliyun ecs delete-key-pairs --biz-region-id cn-hangzhou --key-pair-names 'TestKeyPairName-1_,_TestKeyPairName-2_,_…_TestKeyPairName-100' --region cn-hangzhou",
    "legacyCli": "aliyun ecs DeleteKeyPairs --region cn-hangzhou --RegionId 'cn-hangzhou' --KeyPairNames 'TestKeyPairName-1_,_TestKeyPairName-2_,_…_TestKeyPairName-100'"
  }
}