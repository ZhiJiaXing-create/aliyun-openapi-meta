{
  "name": "ModifyDedicatedHostsChargeType",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPay",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否自动支付。取值范围：\n- true：自动支付。您需要确保账户余额充足，如果账户余额不足会生成异常订单，并只能作废订单。\n- false：只生成订单不扣费。\n\n默认值为true。\n\n>如果您的支付方式余额不足，可以将参数`AutoPay`置为`false`，此时会生成未支付订单，您可以自行支付。",
        "en": "Specifies whether to automatically complete the payment. Valid value:\n\n- true: The payment is automatically completed. Ensure that your account balance is sufficient. Otherwise, your order becomes invalid and must be canceled.\n\n- false: An order is generated but no payment is made.\n\nDefault value: true.\n\n> If you do not have sufficient balance in your account, you can set `AutoPay` to `false` to generate an unpaid order. Then, you can pay for the order."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。`ClientToken`只支持ASCII字符，且不能超过64个字符。更多信息，请参见[如何保证幂等性](~~25693~~)。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The `token` can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "DedicatedHostChargeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专有宿主机需要修改的目标计费方式。取值范围：\n- PrePaid：将按量付费转换为包年包月。\n- PostPaid：将包年包月转换为按量付费。\n\n默认值为PrePaid。",
        "en": "The new billing method for the dedicated host. Valid value:\n\n- PrePaid: changes the billing method from pay-as-you-go to subscription.\n\n- PostPaid: changes the billing method from subscription to pay-as-you-go.\n\nDefault value: PrePaid."
      }
    },
    {
      "name": "DedicatedHostIds",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "专有宿主机ID。取值可以由多台专有宿主机ID组成一个JSON数组，最多支持20个ID，ID之间用半角逗号（,）隔开。",
        "en": "The IDs of the dedicated hosts. The value can be a JSON array that consists of up to 20 dedicated host IDs. Separate the IDs with commas (,)."
      }
    },
    {
      "name": "DetailFee",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "包年包月转换为按量付费时，是否返回订单费用详情。\n\n默认值为false。",
        "en": "Specifies whether to return the billing details of the order when the billing method is changed from subscription to pay-as-you-go.\n\nIndicates whether the nfs volume is set to the read-only mode. Default value: false."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值范围：\n- true：发送检查请求，不会查询资源状况。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码`DryRunOperation`。\n- false：发送正常请求，通过检查后返回2XX的HTTP状态码并直接查询资源状况。\n\n默认值为false。",
        "en": "Specifies whether to perform only a dry run. Valid value:\n\n- true: performs only a dry run. The system checks the request for potential issues, including invalid AccessKey pairs, unauthorized Resource Access Management (RAM) users, and missing parameter values. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\n- false: performs a dry run and performs the actual request. If the request passes the dry run, a 2xx HTTP status code is returned and the operation is performed.\n\nIndicates whether the nfs volume is set to the read-only mode. Default value: false."
      }
    },
    {
      "name": "Period",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "包年包月续费时长。取值范围：\n- `PeriodUnit=Week`时，`Period`的有效取值：1、2、3、4。\n- `PeriodUnit=Month`时，`Period`的有效取值：1、2、3、4、5、6、7、8、9、12、24、36、48、60。",
        "en": "The renewal duration of the subscription dedicated hosts. Valid values:\n\n- If you set `PeriodUnit` to Week, valid values of `Period` are 1, 2, 3, and 4.\n\n- If you set `PeriodUnit` to Month, valid values of `Period` are 1, 2, 3, 4, 5, 6, 7, 8, 9, 12, 24, 36, 48, and 60."
      }
    },
    {
      "name": "PeriodUnit",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "续费时长的时间单位，即参数`Period`的单位。取值范围：\n- Week：周。\n- Month：月。\n\n默认值为Month。",
        "en": "The unit of the renewal duration specified by `Period`. Valid values:\n\n- Week\n\n- Month\n\nDefault value: Month."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "专有宿主机所属的地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the dedicated hosts. You can call the [DescribeRegions](~~25609~~) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改专有宿主机的付费类型",
    "en": "ModifyDedicatedHostsChargeType"
  },
  "example": {
    "unifiedCli": "aliyun ecs modify-dedicated-hosts-charge-type --dedicated-host-ids dh-bp181e5064b5sotr0000_,_dh-bp18064b5sotrr9c0000 --biz-region-id cn-hangzhou --period 1 --period-unit Month --dry-run false --auto-pay false --dedicated-host-charge-type PrePaid --client-token e4567-e89b-12d3-a456-426655440000 --detail-fee false --region cn-hangzhou",
    "legacyCli": "aliyun ecs ModifyDedicatedHostsChargeType --region cn-hangzhou --DetailFee false --DryRun false --Period 1 --DedicatedHostChargeType PrePaid --RegionId 'cn-hangzhou' --AutoPay false --ClientToken 'e4567-e89b-12d3-a456-426655440000' --DedicatedHostIds 'dh-bp181e5064b5sotr0000_,_dh-bp18064b5sotrr9c0000' --PeriodUnit Month"
  }
}