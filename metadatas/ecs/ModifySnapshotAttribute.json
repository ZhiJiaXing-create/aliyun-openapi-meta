{
  "name": "ModifySnapshotAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "快照的描述。可为空，长度为最大256 个英文或中文字符，不能以http://和https://开头。",
        "en": "The snapshot description. It can be empty or up to 256 characters in length. It cannot start with http\\:// or https\\://."
      }
    },
    {
      "name": "DisableInstantAccess",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "关闭快照极速可用功能。取值范围：\n\n- true：关闭快照极速可用。\n- false：不关闭快照极速可用。\n\n默认值：false。\n\n>该参数已弃用。ESSD云盘标准快照已升级为[默认极速可用](~~193667~~)，您无需额外配置，且不产生额外花销。",
        "en": "Specifies whether to disable the instant access feature. Valid values:\n\n- true\n\n- false\n\nDefault value: false.\n\n> This parameter is no longer used. By default, new standard snapshots of Enterprise SSDs (ESSDs) are upgraded to instant access snapshots free of charge without the need for additional configurations. For more information, see [Use the instant access feature](~~193667~~)."
      }
    },
    {
      "name": "RetentionDays",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "快照保留天数。设置该值时，快照保留时间会从快照**创建时间**(按照ISO 8601标准表示，并使用UTC +0时间，格式为yyyy-MM-ddTHH:mm:ssZ)开始计算。取值范围：1~65536。\n\n>快照保留时间仅支持延长，不支持缩短快照已有保留时间。",
        "en": "The retention period of the snapshot. After you specify this parameter, the end time of the new retention period is the specified number of days apart from the **creation time** of the snapshot, which follows the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format and is displayed in UTC. Valid values: 1 to 65536.\n\n> You can extend the retention period of the snapshot and cannot shorten the retention period."
      }
    },
    {
      "name": "SnapshotId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "快照ID。",
        "en": "The ID of the snapshot."
      }
    },
    {
      "name": "SnapshotName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "快照的显示名称。长度为2~128个英文或中文字符。必须以大小写字母或中文开头，不能以http://和https://开头。可以包含数字、半角冒号（:）、下划线（_）或者连字符（-）。\n\n为防止和自动快照的名称冲突，不能以auto开头。",
        "en": "The name of the snapshot. The name must be 2 to 128 characters in length. It must start with a letter and cannot start with http\\:// or https\\://. It can contain letters, digits, colons (:), underscores (\\_), and hyphens (-).\n\nThe name cannot start with auto because snapshots whose names start with auto are recognized as automatic snapshots."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改快照属性",
    "en": "ModifySnapshotAttribute"
  },
  "example": {
    "unifiedCli": "aliyun ecs modify-snapshot-attribute --snapshot-id s-bp199lyny9bb47pa0000 --snapshot-name testSnapshotName --description testDescription --disable-instant-access false --retention-days 10 --region cn-hangzhou",
    "legacyCli": "aliyun ecs ModifySnapshotAttribute --region cn-hangzhou --SnapshotId 's-bp199lyny9bb47pa0000' --Description testDescription --RetentionDays 10 --DisableInstantAccess false --SnapshotName testSnapshotName"
  }
}