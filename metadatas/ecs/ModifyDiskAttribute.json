{
  "name": "ModifyDiskAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BurstingEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "针对支持Burst（性能突发）的磁盘是否开启此功能，取值范围：\n\n- true：是。\n- false：否。\n\n> 对于不支持Burst功能的磁盘，传入任意值将会报错。",
        "en": "Specifies whether to enable the performance burst feature for disks that support this feature. Valid values:\n\n- true: Enabled.\n- false: Disabled.\n\n> An error is returned if you specify this parameter for a disk that does not support the performance burst feature."
      }
    },
    {
      "name": "DeleteAutoSnapshot",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "删除磁盘时，是否同时删除其自动快照。取值范围：\n\n- true：启用。\n\n- false：关闭。\n\n默认值：无，无表示不改变当前的值。",
        "en": "Specifies whether to delete the automatic snapshots of the disk when the disk is deleted. Valid values:\n\n- true: Enabled.\n\n- false: Disabled.\n\nDefault value: null, which indicates that the current value is not changed."
      }
    },
    {
      "name": "DeleteWithInstance",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "磁盘是否随实例释放。默认值：无，无表示不改变当前的值。\n\n<props=\"china\">开启多重挂载特性的云盘，不支持设置该参数。</props>\n\n在下列两种情况下，将参数`DeleteWithInstance`设置成`false`时会报错。 \n         \n- 磁盘的种类（category）为本地盘（ephemeral）时。  \n- 磁盘的种类（category）为普通云盘（cloud），且不可以卸载（Portable=false）时。  \n\n><warning>如果您设置了不随实例释放（DeleteWithInstance=false），一旦磁盘挂载的ECS实例被安全锁定且OperationLocks中标记了\"LockReason\" : \"security\"的锁定状态，释放实例时会忽略磁盘的DeleteWithInstance属性而被同时释放。></warning>",
        "en": "Specifies whether to release the disk along with the instance. Default value: null, which indicates that the current value is not changed.\n\n<props=\"china\">This parameter is not supported for disks that have the multi-attach feature enabled.</props>\n\nAn error is returned if you set DeleteWithInstance to `false` in either of the following cases: \n         \n- The category of the disk is local disk (ephemeral).  \n- The category of the disk is basic disk (cloud) and the disk is not detachable (Portable=false).  \n\n><warning>If you set DeleteWithInstance to false and the ECS instance to which the disk is attached is security-locked with \"LockReason\" : \"security\" in OperationLocks, the DeleteWithInstance attribute is ignored and the disk is released along with the instance.></warning>."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "磁盘描述。 长度为2~256个英文或中文字符，不能以`http://`和`https://`开头。",
        "en": "The description of the disk. The description must be 2 to 256 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "DiskId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "待修改属性的磁盘ID。\n> `DiskId`和`DiskIds.N`两个参数不能同时被调用，请您根据需求任选其一传值。",
        "en": "The ID of the disk whose attributes you want to modify.\n> The DiskId and DiskIds.N parameters cannot be specified at the same time. Specify one of them as needed."
      }
    },
    {
      "name": "DiskIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "待修改多个磁盘的ID列表。N的取值范围为0 ~ 100。\n> `DiskId`和`DiskIds.N`两个参数不能同时被调用，请您根据需求任选其一传值。",
        "en": "The IDs of the disks whose attributes you want to modify. Valid values of N: 0 to 100.\n> The DiskId and DiskIds.N parameters cannot be specified at the same time. Specify one of them as needed."
      }
    },
    {
      "name": "DiskName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "磁盘名称。长度为2~128个字符，支持Unicode中letter分类下的字符（其中包括英文、中文和数字等）。可以包含半角冒号（:）、下划线（_）、半角句号（.）或者短划线（-）。",
        "en": "The name of the disk. The name must be 2 to 128 characters in length and can contain letters, digits, and characters categorized as letter in Unicode, including Chinese characters. The name can contain colons (:), underscores (_), periods (.), and hyphens (-)."
      }
    },
    {
      "name": "EnableAutoSnapshot",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "磁盘是否启用自动快照策略功能。取值范围：\n\n- true：启用。\n- false：关闭。\n\n默认值：无，表示不改变当前的值。\n\n> 该参数已弃用。创建后的云盘默认启用自动快照策略功能，您只需要为云盘绑定自动快照策略即可正常使用。",
        "en": "Specifies whether to enable the automatic snapshot policy for the disk. Valid values:\n\n- true: Enabled.\n- false: Disabled.\n\nDefault value: null, which indicates that the current value is not changed.\n\n> This parameter is deprecated. The automatic snapshot policy is enabled by default for disks after they are created. You only need to associate an automatic snapshot policy with the disk."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID. You can call [DescribeRegions](~~25609~~) to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改块存储属性",
    "en": "Modify block storage attributes"
  },
  "descriptions": {
    "zh": "您可以调用`DiskId`参数修改一个块存储设备的名称、描述、是否随实例释放等属性，调用`DiskIds.N`参数批量修改多个块存储设备的属性。",
    "en": "You can call the DiskId parameter to modify the name, description, release behavior, and other attributes of a single block storage device, or call the DiskIds.N parameter to modify the attributes of multiple block storage devices at a time."
  },
  "example": {
    "unifiedCli": "aliyun ecs modify-disk-attribute --disk-id d-bp1famypsnar20bv0000 --disk-name MyDiskName --description TestDescription --delete-with-instance false --delete-auto-snapshot false --enable-auto-snapshot true --biz-region-id cn-hangzhou --disk-ids d-bp1famypsnar20bv0000 --bursting-enabled false --region cn-hangzhou",
    "legacyCli": "aliyun ecs ModifyDiskAttribute --region cn-hangzhou --DiskIds.1 'd-bp1famypsnar20bv0000' --BurstingEnabled false --Description TestDescription --DeleteWithInstance false --DeleteAutoSnapshot false --EnableAutoSnapshot true --RegionId 'cn-hangzhou' --DiskName MyDiskName --DiskId 'd-bp1famypsnar20bv0000'"
  }
}