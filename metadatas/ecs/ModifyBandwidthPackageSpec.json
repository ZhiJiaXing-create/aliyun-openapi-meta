{
  "name": "ModifyBandwidthPackageSpec",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Bandwidth",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The new bandwidth of the bandwidth package. Unit: Mbit/s. The value must be an integer."
      }
    },
    {
      "name": "BandwidthPackageId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the bandwidth package."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the region where the bandwidth package is located. You can call the `DescribeRegions` operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "ModifyBandwidthPackageSpec"
  },
  "example": {
    "unifiedCli": "aliyun ecs modify-bandwidth-package-spec --biz-region-id cn-hangzhou --bandwidth-package-id example-string --bandwidth example-string --region cn-hangzhou",
    "legacyCli": "aliyun ecs ModifyBandwidthPackageSpec --region cn-hangzhou --Bandwidth 'example-string' --BandwidthPackageId 'example-string' --RegionId 'cn-hangzhou'"
  }
}