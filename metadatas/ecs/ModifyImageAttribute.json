{
  "name": "ModifyImageAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BootMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "修改镜像的启动模式。取值范围：\n- BIOS：BIOS启动模式。\n- UEFI：UEFI启动模式。\n- UEFI-Preferred：双启动模式。\n\n\n<notice>\n\n   为了避免使用镜像不支持的启动模式导致实例无法正常启动，请您务必在修改之前了解目标镜像支持的启动模式。镜像启动模式详情，请参见[镜像启动模式](~~2244655#b9caa9b8bb1wf~~)。\n\n</notice>",
        "en": "The boot mode of the image. Valid values:\n\n- `BIOS`: BIOS boot mode.\n\n- `UEFI`: UEFI boot mode.\n\n- `UEFI-Preferred`: UEFI-preferred boot mode.\n\n><notice>\n\nTo prevent startup failures, verify the boot modes that the image supports before you change its boot mode. For more information, see [Boot modes](~~2244655#b9caa9b8bb1wf~~).\n\n></notice>"
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自定义镜像的描述信息。长度为2~256个字符。不能以http://或https://开头。\n\n默认值：空，表示保持原有描述信息不变。",
        "en": "The new description of the custom image. The description must be 2 to 256 characters in length and cannot start with `http://` or `https://`.\n\nIf you do not specify this parameter, the original description is retained."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "en": "Specifies whether to perform a dry run to check whether the request is valid. Valid values:\n\n- `true`: performs a dry run to check the request for validity, syntax, and required permissions. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n- `false` (default): sends the request. If the request passes the validation checks, the operation is performed."
      }
    },
    {
      "name": "Features",
      "position": "Query",
      "type": "Struct",
      "required": false,
      "description": {
        "zh": "镜像特性相关属性。",
        "en": "The features of the image."
      }
    },
    {
      "name": "ImageFamily",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "镜像族系名称。长度为2~128个字符。必须以大小字母或中文开头，不能以aliyun或acs:开头，不能包含http://或者https://。可以包含数字、半角句号（.）、半角冒号（:）、下划线（_）或者短划线（-）。\n\n默认值：空。",
        "en": "The name of the image family. The name must be 2 to 128 characters in length. It must start with a letter or a Chinese character. The name cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`. It can contain digits, periods (.), colons (:), underscores (\\_), and hyphens (-).\n\nBy default, this parameter is empty."
      }
    },
    {
      "name": "ImageId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "自定义镜像的ID。",
        "en": "The ID of the custom image."
      }
    },
    {
      "name": "ImageName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自定义镜像的名称。长度为2~128个字符。必须以大小字母或中文开头，不能以aliyun或acs:开头，不能包含http://或者https://。可以包含数字、半角句号（.）、半角冒号（:）、下划线（_）或者短划线（-）。\n\n默认值：空，表示保持原有名称不变。",
        "en": "The name of the custom image. The name must be 2 to 128 characters in length. It must start with a letter or a Chinese character. The name cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`. It can contain digits, periods (.), colons (:), underscores (\\_), and hyphens (-).\n\nIf you do not specify this parameter, the original name is retained."
      }
    },
    {
      "name": "LicenseType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "导入镜像后，激活操作系统采用的许可证类型。当前仅支持BYOL。\n\nBYOL：源操作系统自带的许可证。采用BYOL时，您必须确保您的许可证密钥支持在阿里云使用。",
        "en": "The license type for activating the operating system after you import the image. The only valid value is `BYOL`.\n\n`BYOL`: Bring Your Own License. If you use the BYOL license type, you must ensure that your license key is supported for use on Alibaba Cloud."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "自定义镜像所在的地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The ID of the region where the custom image is located. You can call the [DescribeRegions](~~25609~~) operation to view the latest list of Alibaba Cloud regions."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "镜像状态，取值范围：\n\n- Deprecated：将镜像设置为弃用状态。如果您已经共享的自定义镜像，必须先取消共享才能修改为弃用状态。对处于弃用状态镜像，不能共享和复制镜像。但是可以使用镜像创建实例或更换系统盘。\n- Available：将镜像设置为可用状态。您可以将弃用状态的镜像恢复可用。\n\n> 如果您需要回滚镜像族系中的自定义镜像至上一个版本，可以将最新可用的自定义镜像设置为弃用状态，但如果该镜像为镜像族系中唯一一个可用状态的自定义镜像，则弃用镜像以后该镜像族系将无可用状态自定义镜像用来创建实例，因此请谨慎操作。",
        "en": "The image status. Valid values:\n\n- `Deprecated`: Deprecates the image. If a custom image that you want to deprecate is shared, you must unshare it first. You cannot share or copy a deprecated image. However, you can use the image to create an instance or replace a system disk.\n\n- `Available`: Makes the image available. You can change the status of a deprecated image to `Available`.\n\n> However, if this is the only available custom image in the image family, deprecating it prevents the creation of instances from any image in that family. Use this option with caution."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改自定义镜像属性",
    "en": "ModifyImageAttribute"
  },
  "example": {
    "unifiedCli": "aliyun ecs modify-image-attribute --biz-region-id cn-hangzhou --image-id m-bp18ygjuqnwhechc0000 --image-name testImageName --status Deprecated --image-family hangzhou-daily-update --boot-mode BIOS --license-type BYOL --description testDescription --features NvmeSupport=supported ImdsSupport=v2 --dry-run true --region cn-hangzhou",
    "legacyCli": "aliyun ecs ModifyImageAttribute --region cn-hangzhou --Status Deprecated --Features.NvmeSupport supported --ImageName testImageName --Features.ImdsSupport v2 --Description testDescription --BootMode BIOS --ImageId 'm-bp18ygjuqnwhechc0000' --DryRun true --ImageFamily 'hangzhou-daily-update' --RegionId 'cn-hangzhou' --LicenseType BYOL --method POST --force"
  }
}