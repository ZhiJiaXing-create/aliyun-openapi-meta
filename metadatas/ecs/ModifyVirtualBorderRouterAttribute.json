{
  "name": "ModifyVirtualBorderRouterAttribute",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CircuitCode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The circuit code of the Express Connect circuit. The circuit code is provided by your connectivity provider."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "A client-generated token that ensures the request is idempotent. The token must be unique for each request, contain only ASCII characters, and not exceed 64 characters in length."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The description of the VBR.\n\nThe description must be 2 to 256 characters long. It must start with a letter and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "LocalGatewayIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The local gateway IP address for the BGP session."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The name of the VBR.\n\nThe name must be 2 to 128 characters long. It must start with a letter and can contain letters, digits, underscores (\\_), and hyphens (-)."
      }
    },
    {
      "name": "PeerGatewayIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The peer gateway IP address for the BGP session."
      }
    },
    {
      "name": "PeeringSubnetMask",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The subnet mask for the `LocalGatewayIp` and `PeerGatewayIp`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the region where the virtual border router (VBR) is deployed.\n\nTo get a list of the latest regions, call the `DescribeRegions` operation."
      }
    },
    {
      "name": "UserCidr",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The CIDR block of the on-premises network that is connected to the VBR."
      }
    },
    {
      "name": "VbrId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the VBR to modify."
      }
    },
    {
      "name": "VlanId",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "en": "The VLAN ID of the VBR. The VLAN ID must be unique on the physical connection.\n\nValid values: 0 to 4095.\n\nA value of 0 indicates that the VBR does not use a VLAN."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "ModifyVirtualBorderRouterAttribute"
  },
  "example": {
    "unifiedCli": "aliyun ecs modify-virtual-border-router-attribute --biz-region-id cn-hangzhou --vbr-id example-string --vlan-id 1 --circuit-code example-string --local-gateway-ip example-string --peer-gateway-ip example-string --peering-subnet-mask example-string --description example-string --name example-string --client-token example-string --user-cidr example-string --region cn-hangzhou",
    "legacyCli": "aliyun ecs ModifyVirtualBorderRouterAttribute --region cn-hangzhou --LocalGatewayIp 'example-string' --PeerGatewayIp 'example-string' --Description 'example-string' --CircuitCode 'example-string' --RegionId 'cn-hangzhou' --VbrId 'example-string' --ClientToken 'example-string' --PeeringSubnetMask 'example-string' --VlanId 1 --Name 'example-string' --UserCidr 'example-string'"
  }
}