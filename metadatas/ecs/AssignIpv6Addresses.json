{
  "name": "AssignIpv6Addresses",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。**ClientToken**只支持ASCII字符，且不能超过64个字符。更多信息，请参见[如何保证幂等性](~~25693~~)。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. **ClientToken** can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "Ipv6Address",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "为弹性网卡指定一个或多个IPv6地址。支持设置最多10个IPv6地址，即N的取值范围：1~10。\n\n取值示例：Ipv6Address.1=2001:db8:1234:1a00::\\*\\*\\*\\*\n\n> 调用该接口时，您必须设置`Ipv6Addresses.N`参数或者`Ipv6AddressCount`参数的其中一个，但不能同时设置这两个参数。",
        "en": "One or more IPv6 addresses to assign to the network interface controller (NIC). You can set up to 10 IPv6 addresses. Valid values of N: 1 to 10.\n\nExample: Ipv6Address.1=2001:db8:1234:1a00::\\*\\*\\*\\*\n\n> When you invoke this operation, you must set either the `Ipv6Addresses.N` parameter or the `Ipv6AddressCount` parameter, but not both."
      }
    },
    {
      "name": "Ipv6AddressCount",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "为弹性网卡指定随机生成的IPv6地址数量。取值范围：1~10。\n\n> 调用该接口时，您必须设置`Ipv6Addresses.N`参数或者`Ipv6AddressCount`参数的其中一个，但不能同时设置这两个参数。",
        "en": "The number of IPv6 addresses to randomly generate for the network interface controller (NIC). Valid values: 1 to 10.\n\n> When you invoke this operation, you must set either the `Ipv6Addresses.N` parameter or the `Ipv6AddressCount` parameter, but not both."
      }
    },
    {
      "name": "Ipv6Prefix",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "为弹性网卡指定一个或多个IPv6前缀。N的取值范围：1~10。\n> 如果您需要为弹性网卡设置IPv6前缀，则必须设置Ipv6Prefix.N或者Ipv6PrefixCount的其中一个，但不能同时设置这两个参数。",
        "en": "One or more IPv6 prefixes to assign to the network interface controller (NIC). Valid values of N: 1 to 10.\n> To set IPv6 prefixes for the ENI, you must set either the Ipv6Prefix.N parameter or the Ipv6PrefixCount parameter, but not both."
      }
    },
    {
      "name": "Ipv6PrefixCount",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "为弹性网卡指定一个或多个IPv6前缀。取值范围：1~10。\n> 如果您需要为弹性网卡设置IPv6前缀，则必须设置Ipv6Prefix.N或者Ipv6PrefixCount的其中一个，但不能同时设置这两个参数。",
        "en": "The number of IPv6 prefixes to assign to the network interface controller (NIC). Valid values: 1 to 10.\n> To set IPv6 prefixes for the ENI, you must set either the Ipv6Prefix.N parameter or the Ipv6PrefixCount parameter, but not both."
      }
    },
    {
      "name": "NetworkInterfaceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "弹性网卡ID。",
        "en": "The ID of the network interface controller (NIC)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "弹性网卡所在地域的ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the network interface controller (NIC). You can invoke [DescribeRegions](~~25609~~) to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为弹性网卡分配IPv6地址",
    "en": "Assign IPv6 addresses to an ENI"
  },
  "descriptions": {
    "zh": "## 接口说明\n您可以指定弹性网卡所属交换机下CIDR的IPv6地址，也可以指定IPv6地址数量自动创建IPv6地址。您需要注意：\n\n- 弹性网卡所属的交换机必须开通了IPv6。更多信息，请参见[为已有交换机开通IPv6网段](~~98923~~)。\n\n- 弹性网卡必须处于可用（Available）或已附加（InUse）状态。\n\n- 操作主网卡时，网卡附加的实例必须处于运行中（Running）或已停止（Stopped）状态。\n\n- 单个网卡能够分配的IPv6地址数量和网卡附加的实例规格有关。\n    - 如果弹性网卡处于可用（Available）状态，最多可以分配10个IPv6地址。\n\n    - 如果弹性网卡附加到实例上，能够分配的IPv6地址数将受到实例规格限制。更多信息，请参见[实例规格族](~~25378~~)。\n\n- 成功调用该接口后，您可以从返回结果中获取已分配的IPv6地址信息。",
    "en": "## Operation description\nYou can specify IPv6 addresses from the CIDR block of the vSwitch to which the Elastic Network Interface (ENI) belongs, or specify the number of IPv6 addresses for automatic creation. Note the following items:\n\n- The vSwitch to which the ENI belongs must have IPv6 enabled. For more information, see [Enable an IPv6 CIDR block for an existing vSwitch](~~98923~~).\n\n- The ENI must be in the Active (Available) or InUse state.\n\n- When you operate a primary ENI, the instance to which the network interface controller (NIC) is attached must be in the Running or Stopped state.\n\n- The number of IPv6 addresses that can be allocated to a single network interface controller (NIC) depends on the instance type of the instance to which the ENI is attached.\n    - If the ENI is in the Active (Available) state, you can allocate up to 10 IPv6 addresses to it.\n\n    - If the ENI is attached to an instance, the number of IPv6 addresses that can be allocated is limited by the instance type. For more information, see [Instance family](~~25378~~).\n\n- After you successfully invoke this operation, you can obtain the allocated IPv6 address information from the response."
  },
  "example": {
    "unifiedCli": "aliyun ecs assign-ipv6-addresses --biz-region-id cn-hangzhou --network-interface-id eni-bp1iqejowblx6h8j0000 --ipv6-address-count 1 --ipv6-address 2001:db8:1234:1a00::0000 --ipv6-prefix 2001:db8:1234:1a00:0000::/80 --ipv6-prefix-count 1 --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun ecs AssignIpv6Addresses --region cn-hangzhou --Ipv6Address.1 '2001:db8:1234:1a00::0000' --Ipv6Prefix.1 '2001:db8:1234:1a00:0000::/80' --Ipv6PrefixCount 1 --Ipv6AddressCount 1 --RegionId 'cn-hangzhou' --NetworkInterfaceId 'eni-bp1iqejowblx6h8j0000' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}