{
  "name": "JoinSecurityGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。\n\n> 当该参数传入值时，`NetworkInterfaceId`必须为空。",
        "en": "The ID of the instance.\n\n> If you specify this parameter, you must leave `NetworkInterfaceId` empty."
      }
    },
    {
      "name": "NetworkInterfaceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "弹性网卡ID。\n\n> 当该参数传入值时，`InstanceId`必须为空。",
        "en": "The ID of the elastic network interface.\n\n> If you specify this parameter, you must leave `InstanceId` empty."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。\n\n- 实例加入安全组的操作可以不指定地域ID。\n- 弹性网卡加入安全组的操作必须指定弹性网卡所在的地域ID。",
        "en": "The ID of the region. Call [DescribeRegions](~~25609~~) to view the latest list of Alibaba Cloud regions.\n\n- This parameter is optional when you add an instance to a security group.\n\n- This parameter is required when you add an elastic network interface to a security group. The value must be the ID of the region where the elastic network interface is located."
      }
    },
    {
      "name": "SecurityGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "安全组ID。您可以调用[DescribeSecurityGroups](~~25556~~)查看您可用的安全组。",
        "en": "The ID of the security group. Call [DescribeSecurityGroups](~~25556~~) to view your available security groups."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "加入安全组",
    "en": "JoinSecurityGroup"
  },
  "descriptions": {
    "zh": "> 该API已不推荐使用。推荐您调用[ModifyInstanceAttribute](~~25503~~)接口将ECS实例加入或移除安全组；调用[ModifyNetworkInterfaceAttribute](~~58513~~)接口将弹性网卡（ENI）加入或移除安全组。\n- 该接口不支持同时将实例和弹性网卡加入一个安全组，即参数`InstanceId`和`NetworkInterfaceId`不能同时传值。\n-  您的安全组和实例必须属于同一个阿里云地域。 \n-  您的安全组和实例的网络类型必须相同。如果网络类型为专有网络VPC，则安全组和实例必须属于同一个VPC。\n-  加入安全组之前，实例必须处于**已停止**（Stopped）或者**运行中**（Running）状态。\n- 一台实例或一张弹性网卡最多可以加入五个安全组。更多信息，请参见[安全组使用限制](~~25412#SecurityGroupQuota1~~)。",
    "en": "> This API is deprecated. We recommend calling the [ModifyInstanceAttribute](~~25503~~) operation to add or remove an ECS instance from a security group, or calling the [ModifyNetworkInterfaceAttribute](~~58513~~) operation to add or remove an elastic network interface (ENI) from a security group.\n\n- You cannot add an instance and an elastic network interface to a security group in the same call. The `InstanceId` and `NetworkInterfaceId` parameters are mutually exclusive.\n\n- The security group and the instance must be in the same Alibaba Cloud region.\n\n- The security group and the instance must have the same network type. If the network type is VPC, they must also belong to the same VPC.\n\n- You can add an instance to a security group only if the instance is in the **Stopped** or **Running** state.\n\n- An instance or an elastic network interface can belong to a maximum of five security groups. For more information, see [Security group limitations](~~25412#SecurityGroupQuota1~~)."
  },
  "example": {
    "unifiedCli": "aliyun ecs join-security-group --security-group-id sg-bp67acfmxazb4p0000 --instance-id i-bp67acfmxazb4p0000 --network-interface-id eni-bp13kd656hxambfe0000 --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun ecs JoinSecurityGroup --region cn-hangzhou --InstanceId 'i-bp67acfmxazb4p0000' --SecurityGroupId 'sg-bp67acfmxazb4p0000' --RegionId 'cn-hangzhou' --NetworkInterfaceId 'eni-bp13kd656hxambfe0000'"
  }
}