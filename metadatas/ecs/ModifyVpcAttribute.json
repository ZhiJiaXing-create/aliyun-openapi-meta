{
  "name": "ModifyVpcAttribute",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CidrBlock",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The primary IPv4 cidr block for the VPC. You can only expand this cidr block, for example, from `192.168.0.0/24` to `192.168.0.0/16`. You cannot modify the primary cidr block if ClassicLink is enabled."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The description must be 2 to 256 characters long. It must start with a letter and cannot begin with `http://` or `https://`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The ID of the VPC's region."
      }
    },
    {
      "name": "UserCidr",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "A user cidr block to add to the VPC. You can add up to three user cidr blocks. They cannot overlap with the primary cidr block, each other, or the reserved `100.64.0.0/10` cidr block."
      }
    },
    {
      "name": "VpcId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the VPC to modify."
      }
    },
    {
      "name": "VpcName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The name must be 2 to 128 characters long, start with a letter, and can contain letters, digits, underscores (\\_), and hyphens (-)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "ModifyVpcAttribute"
  },
  "example": {
    "unifiedCli": "aliyun ecs modify-vpc-attribute --vpc-id example-string --description example-string --vpc-name example-string --cidr-block example-string --biz-region-id cn-hangzhou --user-cidr example-string --region cn-hangzhou",
    "legacyCli": "aliyun ecs ModifyVpcAttribute --region cn-hangzhou --Description 'example-string' --VpcId 'example-string' --CidrBlock 'example-string' --VpcName 'example-string' --RegionId 'cn-hangzhou' --UserCidr 'example-string'"
  }
}