{
  "name": "ModifyReservedInstanceAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预留实例券的描述。长度为2~256个英文或中文字符，不能以`http://`和`https://`开头。\n\n默认值：空。",
        "en": "The error code."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源所在的地域ID。",
        "en": "The ID of the reserved instance."
      }
    },
    {
      "name": "ReservedInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "预留实例券ID。",
        "en": "The new name of the reserved instance. The name must be 2 to 128 characters in length. It must start with a letter and cannot start with `http://` or `https://`. It can contain letters, digits, colons (:), underscores (\\_), and hyphens (-)."
      }
    },
    {
      "name": "ReservedInstanceName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预留实例券的名称。长度为2~128个英文或中文字符。必须以大小写字母或中文开头，不能以`http://`和`https://`开头。可以包含数字、半角冒号（:）、下划线（_）或者短划线（-）。",
        "en": "The new description of the reserved instance. The description must be 2 to 256 characters in length. It cannot start with `http://` or `https://`.\n\nThis parameter is empty by default."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改预留实例券属性",
    "en": "ModifyReservedInstanceAttribute"
  },
  "example": {
    "unifiedCli": "aliyun ecs modify-reserved-instance-attribute --biz-region-id cn-hangzhou --reserved-instance-id ecsri-uf61hdhue4kcorqsk0000 --reserved-instance-name testReservedInstanceName --description ri-example --region cn-hangzhou",
    "legacyCli": "aliyun ecs ModifyReservedInstanceAttribute --region cn-hangzhou --Description 'ri-example' --ReservedInstanceId 'ecsri-uf61hdhue4kcorqsk0000' --RegionId 'cn-hangzhou' --ReservedInstanceName testReservedInstanceName"
  }
}