{
  "name": "ModifyRouterInterfaceAttribute",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The new description of the router interface. The description must be 2 to 256 characters long and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "HealthCheckSourceIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The source IP address used for the health check."
      }
    },
    {
      "name": "HealthCheckTargetIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The destination IP address used for the health check."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The new name of the router interface. The name must be 2 to 128 characters long and can contain letters, digits, underscores (\\_), and hyphens (-). It must start with a letter."
      }
    },
    {
      "name": "OppositeInterfaceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The ID of the opposite router interface."
      }
    },
    {
      "name": "OppositeInterfaceOwnerId",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "en": "The ID of the Alibaba Cloud account that owns the opposite router interface."
      }
    },
    {
      "name": "OppositeRouterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The ID of the opposite router."
      }
    },
    {
      "name": "OppositeRouterType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The type of the opposite router. Valid values: `VRouter` and `VBR`. The default value is `VRouter`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the region where the router interface is deployed."
      }
    },
    {
      "name": "RouterInterfaceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the router interface that you want to modify."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "ModifyRouterInterfaceAttribute"
  },
  "example": {
    "unifiedCli": "aliyun ecs modify-router-interface-attribute --biz-region-id cn-hangzhou --router-interface-id example-string --name example-string --description example-string --opposite-interface-id example-string --opposite-router-id example-string --opposite-router-type example-string --opposite-interface-owner-id 1 --health-check-source-ip example-string --health-check-target-ip example-string --region cn-hangzhou",
    "legacyCli": "aliyun ecs ModifyRouterInterfaceAttribute --region cn-hangzhou --OppositeInterfaceId 'example-string' --OppositeInterfaceOwnerId 1 --Description 'example-string' --OppositeRouterId 'example-string' --OppositeRouterType 'example-string' --HealthCheckSourceIp 'example-string' --RegionId 'cn-hangzhou' --RouterInterfaceId 'example-string' --HealthCheckTargetIp 'example-string' --Name 'example-string'"
  }
}