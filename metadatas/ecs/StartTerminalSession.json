{
  "name": "StartTerminalSession",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。**ClientToken**只支持ASCII字符，且不能超过64个字符。更多信息，请参见[如何保证幂等性](~~25693~~)。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. The **ClientToken** value can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "CommandLine",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "发起会话后，指定执行的命令内容。长度不能超过512个字符。\n\n> 指定了`CommandLine`后，不能再指定`PortNumber`和`TargetServer`。",
        "en": "The command to run after the session is initiated. The command can be up to 512 characters in length.\n\n> After you specify `CommandLine`, you cannot specify `PortNumber` or `TargetServer`."
      }
    },
    {
      "name": "ConnectionType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "远程连接实例所需WebSocket URL的网络类型。\n- Internet：公网（默认）\n- Intranet：内网。",
        "en": "The network type of the WebSocket URL required for the remote connection to the instance. Valid values:\n- Internet: the Internet. This is the default value.\n- Intranet: the internal network."
      }
    },
    {
      "name": "EncryptionOptions",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "会话加密配置项。",
        "en": "The session encryption configuration."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "实例ID列表。",
        "en": "The instance ID list."
      }
    },
    {
      "name": "PasswordName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "在 Windows 实例中使用Session Manager时用户的密码名称。长度不得超过 255 个字符。\n当您希望以非默认用户（System）在 Windows 实例中使用Session Manager时，需要同时传入Username和该参数。为降低密码泄露的风险，需要将密码明文托管在系统运维管理的参数仓库中，此处仅传入密码的名称。更多信息，请参见[加密参数](~~186828~~)。",
        "en": "The name of the password for the user when you use Session Manager on a Windows instance. The name can be up to 255 characters in length.\nWhen you want to use Session Manager on a Windows instance as a non-default user (System), specify both Username and this parameter. To reduce the risk of password leaks, store the plaintext password in the parameter repository of operations management and specify only the password name here. For more information, see [Encryption parameters](~~186828~~)."
      }
    },
    {
      "name": "PortNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定ECS实例的端口号，用于数据转发。一旦设置该参数，云助手Agent的数据转发会传到指定的端口号，用于端口转发。例如，SSH使用22端口。\n\n默认值为空，表示不设置数据转发的端口号。",
        "en": "The port number of the ECS instance for data forwarding. After this parameter is specified, Cloud Assistant Agent forwards data to the specified port for port forwarding. For example, SSH uses port 22.\n\nDefault value: empty, which indicates that no port number is specified for data forwarding."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例所在地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the instance. You can call [DescribeRegions](~~25609~~) to query the most recent region list."
      }
    },
    {
      "name": "TargetServer",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定通过实例访问VPC内目标服务地址。\n\n>当该参数不为空时，`PortNumber`表示通过托管实例访问VPC内目标服务的端口号。",
        "en": "The address of the destination server in the VPC that you want to access through the instance.\n\n> If this parameter is not empty, `PortNumber` specifies the port number of the destination server in the VPC that you want to access through the managed instance."
      }
    },
    {
      "name": "Username",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定连接时的用户名称。",
        "en": "The username used for the connection."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "开始终端会话",
    "en": "Start a terminal session"
  },
  "descriptions": {
    "zh": "## 接口说明\n\n当您通过代码定制化远程连接客户端时，可以调用该接口获取远程连接ECS实例的WebSocketUrl。调用该接口时您需要注意：\n\n- 指定的ECS实例必须处于运行中（Running）状态。\n- 指定的ECS实例必须安装了云助手Agent。您可以调用[DescribeCloudAssistantStatus](~~87346~~)查询ECS实例是否已安装云助手Agent，并可以查询云助手Agent的版本号。\n    - 如果您的ECS实例没有安装云助手Agent，请调用[InstallCloudAssistant](~~85916~~)安装。\n    - 云助手Agent的版本需要高于以下版本才支持会话管理功能。如果您需要升级云助手Agent版本，请参见[升级或禁止升级云助手Agent](~~134383~~)。\n        - Linux操作系统：2.2.3.256\n        - Windows操作系统：2.1.3.256\n- 成功调用该接口后，WebSocketUrl有效时长为10分钟。\n- 在会话建立后，3分钟没有数据传输，云助手会关闭链接。\n- 同一地域下，已创建并可用的会话不能超过 100 个，单台 ECS 实例处于连接状态的会话不能超过 20 个，单个会话连接的带宽限制为 200kb/s。\n- 端口转发功能，目前只支持TCP端口转发，不支持UDP。\n- 若想永久关闭会话，并使WebSocketUrl失效，请调用EndTerminalSession接口。",
    "en": "## Operation description\n\nWhen you customize a remote connection client by using code, you can invoke this operation to obtain the WebSocketUrl for establishing a remote connection to an ECS instance. Take note of the following items:\n\n- The specified ECS instance must be in the Running state.\n- Cloud Assistant Agent must be installed on the specified ECS instance. You can invoke [DescribeCloudAssistantStatus](~~87346~~) to check whether Cloud Assistant Agent is installed on the ECS instance and query the version number of Cloud Assistant Agent.\n    - If Cloud Assistant Agent is not installed on the ECS instance, invoke [InstallCloudAssistant](~~85916~~) to install it.\n    - The version number of Cloud Assistant Agent must be later than the following versions to support the session management feature. To upgrade Cloud Assistant Agent, see [Update or disable updates for Cloud Assistant Agent](~~134383~~).\n        - Linux operating system: 2.2.3.256\n        - Windows operating system: 2.1.3.256\n- After this operation is invoked, the WebSocketUrl is valid for 10 minutes.\n- After a session is established, Cloud Assistant closes the connection if no data is transmitted for 3 minutes.\n- In a single region, a maximum of 100 sessions can be created and active. A maximum of 20 sessions can be in the connected state for a single ECS instance. The bandwidth limit for a single session is 200 KB/s.\n- The port forwarding feature supports only TCP port forwarding. UDP is not supported.\n- To permanently shutdown a session and invalidate the WebSocketUrl, invoke the EndTerminalSession operation."
  },
  "example": {
    "unifiedCli": "aliyun ecs start-terminal-session --biz-region-id cn-hangzhou --instance-id i-bp1eifrtpxa9tb0000 --port-number 22 --command-line ssh_root@192.168.0.246 --target-server 192.168.0.246 --username testUser --connection-type Intranet --password-name axtSecretPassword --client-token 123e4567-e89b-12d3-a456-426655440000 --encryption-options KMSKeyId=xxx Mode=Auto Enabled=true --region cn-hangzhou",
    "legacyCli": "aliyun ecs StartTerminalSession --region cn-hangzhou --CommandLine 'ssh_root@192.168.0.246' --PasswordName axtSecretPassword --ConnectionType Intranet --Username testUser --RegionId 'cn-hangzhou' --TargetServer '192.168.0.246' --PortNumber 22 --ClientToken '123e4567-e89b-12d3-a456-426655440000' --EncryptionOptions '{\"KMSKeyId\":\"xxx\",\"Mode\":\"Auto\",\"Enabled\":true}' --InstanceId.1 'i-bp1eifrtpxa9tb0000'"
  }
}