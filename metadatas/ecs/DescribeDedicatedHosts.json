{
  "name": "DescribeDedicatedHosts",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DedicatedHostClusterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专有宿主机集群ID。",
        "en": "The ID of the dedicated host cluster."
      }
    },
    {
      "name": "DedicatedHostIds",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专有宿主机ID列表。最多支持100个ID ，ID之间用半角逗号（,）隔开。",
        "en": "The IDs of dedicated hosts. You can specify up to 100 dedicated host IDs in a JSON array."
      }
    },
    {
      "name": "DedicatedHostName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专有宿主机的名称。",
        "en": "The name of the dedicated host."
      }
    },
    {
      "name": "DedicatedHostType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专有宿主机的规格。您可以调用[DescribeDedicatedHostTypes](~~134240~~)接口获得最新的专有宿主机规格列表。",
        "en": "The dedicated host type. Call the [`DescribeDedicatedHostTypes`](~~134240~~) operation to get the latest list of dedicated host types."
      }
    },
    {
      "name": "LockReason",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专有宿主机被锁定的原因。取值范围：\n- financial：因欠费被锁定。\n- security：因安全原因被锁定。",
        "en": "The reason that the dedicated host is locked. Valid values:\n\n- `financial`: The dedicated host is locked due to an overdue payment.\n\n- `security`: The dedicated host is locked for security reasons."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页的最大条目数。一旦设置该参数，即表示使用MaxResults与NextToken组合参数的查询方式。\n\n最大值为 100。\n\n默认值为 10。",
        "en": "The maximum number of results to return per page.\n\nMaximum value: 100.\n\nDefault value: 10."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询凭证（Token）。取值为上一次调用该接口返回的 NextToken 参数值，初次调用接口时无需设置该参数。",
        "en": "The token used to retrieve the next page of results. Do not set this parameter for the first request. For subsequent requests, set this parameter to the `NextToken` value returned from the previous response."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "> 该参数即将下线，推荐您使用NextToken与MaxResults完成分页查询操作。",
        "en": "> This parameter is deprecated. Use `NextToken` and `MaxResults` for pagination."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "> 该参数即将下线，推荐您使用NextToken与MaxResults完成分页查询操作。",
        "en": "> This parameter is deprecated. Use `NextToken` and `MaxResults` for pagination."
      }
    },
    {
      "name": "QueryInventory",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "专有宿主机所属的地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The ID of the region where the dedicated host resides. Call the [`DescribeRegions`](~~25609~~) operation to get the latest list of Alibaba Cloud regions."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专有宿主机所在资源组ID。使用该参数过滤资源时，资源数量不能超过1000个。\n\n>不支持默认资源组过滤。",
        "en": "The ID of the resource group to which the dedicated host belongs. When you use this parameter to filter resources, the number of resources cannot exceed 1,000.\n\n> Filtering by the default resource group is not supported."
      }
    },
    {
      "name": "SocketDetails",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否展示Socket维度容量信息。您可以通过Socket维度容量信息查看剩余资源信息（vCPU、内存使用量、剩余量和总量信息），从而判断是否可以创建出对应规格的ECS实例。取值范围：\n\n- true：展示。仅部分规格的DDH支持显示Socket维度资源信息，详情请参见[查看和导出DDH信息](~~68989~~)。\n- false：不展示。\n\n><notice>\n\n每个DDH一般有两个CPU，每个CPU对应的编号为Socket 0、Socket 1。在独享的DDH中，为保证DDH上的ECS发挥最大性能，ECS不会跨Socket创建，即ECS仅会基于一个Socket创建。\n\n- 如果其中一个Socket剩余的算力资源大于等于待创建的ECS规格，ECS将创建成功。\n- 如果每个Socket剩余的算力资源小于待创建的ECS规格，ECS将创建失败。虽然DDH上两个Socket剩余的资源大于ECS规格，但是仍然无法创建出ECS实例。\n</notice>",
        "en": "Specifies whether to return socket-level capacity information. You can use the information to check the remaining vCPU and memory resources and determine whether an ECS instance of a specific instance type can be created on the dedicated host. Valid values:\n\n- `true`: returns the information. Only specific dedicated host types support this feature. For more information, see [View and export information about dedicated hosts](~~68989~~).\n\n- `false`: does not return the information.\n\n><notice>\n\nA dedicated host typically has two CPUs, which correspond to Socket 0 and Socket 1. To maximize performance, an ECS instance created on a dedicated host is allocated to a single socket and does not span sockets.\n\n- If the remaining resources on a socket are sufficient for the specified ECS instance type, the instance can be created.\n\n- If the remaining resources on each socket are insufficient for the specified ECS instance type, the instance cannot be created, even if the total remaining resources on both sockets are sufficient.\n\n></notice>"
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专有宿主机的使用状态。取值范围：\n\n- Available：运行中。专有宿主机的正常运行状态。\n\n- UnderAssessment：物理机风险，即故障潜伏期，其物理机处于可用状态，但可能导致专有宿主机中的ECS实例出现问题。\n\n- PermanentFailure：永久性故障，专有宿主机不可用。\n\n- TempUnavailable：宿主机临时不可用。\n\n- Redeploying：宿主机恢复中。\n\n默认值：Available。",
        "en": "The state of the dedicated host. Valid values:\n\n- `Available`: The dedicated host is running as expected.\n\n- `UnderAssessment`: The dedicated host is being assessed for physical hardware risks. The host is available but may have hardware issues that could affect its ECS instances.\n\n- `PermanentFailure`: The dedicated host has a permanent failure and is unavailable.\n\n- `TempUnavailable`: The dedicated host is temporarily unavailable.\n\n- `Redeploying`: The dedicated host is being redeployed.\n\nThe default value is `Available`."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表。列表长度范围：0~20。",
        "en": "The tags used to filter dedicated hosts. You can specify up to 20 tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "专有宿主机的标签键。一旦传入该值，则不允许为空字符串。最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或`https://`。",
            "en": "The tag key. The key can be up to 128 characters long. It cannot be an empty string, start with `aliyun` or `acs:`, or contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "专有宿主机的标签值。一旦传入该值，可以为空字符串。最多支持128个字符，不能包含`http://`或者`https://`。",
            "en": "The tag value. The value can be up to 128 characters long and cannot contain `http://` or `https://`. You can leave the value empty."
          }
        }
      ]
    },
    {
      "name": "ZoneId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "可用区ID。您可以调用[DescribeZones](~~25610~~)查看最新的阿里云可用区列表。",
        "en": "The zone ID. Call the [`DescribeZones`](~~25610~~) operation to get the latest list of Alibaba Cloud zones."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询专有宿主机详细信息",
    "en": "Query detailed information about Dedicated Hosts"
  },
  "descriptions": {
    "zh": "## 接口说明\n\n您可以通过以下任一方式查询一台或多台专有宿主机的详细信息：\n\n- 指定`DedicatedHostIds`查询专有宿主机的详细信息。\n- 指定`DedicatedHostClusterId`查询专有宿主机集群内专有宿主机的详细信息。",
    "en": "## API\n\nYou can query the details of one or more dedicated hosts in one of the following ways:\n\n- Specify `DedicatedHostIds` to query specific dedicated hosts.\n\n- Specify `DedicatedHostClusterId` to query all dedicated hosts in a dedicated host cluster."
  },
  "example": {
    "unifiedCli": "aliyun ecs describe-dedicated-hosts --biz-region-id cn-hangzhou --zone-id cn-hangzhou-g --dedicated-host-ids dh-bp165p6xk2tlw61e0000_,_dh-bp1f9vxmno7emy960000 --dedicated-host-name MyDDHTestName --status Available --dedicated-host-type ddh.g5 --lock-reason financial --page-number 1 --page-size 10 --resource-group-id rg-aek3b6jzp660000 --tag Value=TestValue Key=TestKey --dedicated-host-cluster-id dc-bp12wlf6am0vz9v20000 --socket-details true --next-token e71d8a535bd9cc11 --max-results 10 --query-inventory true --region cn-hangzhou",
    "legacyCli": "aliyun ecs DescribeDedicatedHosts --region cn-hangzhou --Status Available --QueryInventory true --Tag.1.Key TestKey --ZoneId 'cn-hangzhou-g' --PageSize 10 --ResourceGroupId 'rg-aek3b6jzp660000' --NextToken e71d8a535bd9cc11 --MaxResults 10 --DedicatedHostType 'ddh.g5' --SocketDetails true --DedicatedHostName MyDDHTestName --PageNumber 1 --Tag.1.Value TestValue --RegionId 'cn-hangzhou' --LockReason financial --DedicatedHostIds 'dh-bp165p6xk2tlw61e0000_,_dh-bp1f9vxmno7emy960000' --DedicatedHostClusterId 'dc-bp12wlf6am0vz9v20000'"
  }
}