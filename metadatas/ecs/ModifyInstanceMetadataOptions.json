{
  "name": "ModifyInstanceMetadataOptions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "HttpEndpoint",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "是否启用实例元数据的访问通道。取值范围：\n- enabled：启用。\n- disabled：禁用。\n\n默认值：enabled。\n> 有关实例元数据的更多信息，请参见[实例元数据概述](~~49122~~)。",
        "en": "Specifies whether to enable the access channel for instance metadata. Valid values:\n\n- enabled\n\n- disabled\n\nDefault value: enabled.\n\n> For information about instance metadata, see [Obtain instance metadata](~~49122~~)."
      }
    },
    {
      "name": "HttpPutResponseHopLimit",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": ">该参数暂未开放使用。",
        "en": "> This parameter is not publicly available."
      }
    },
    {
      "name": "HttpTokens",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问实例元数据时是否强制使用加固模式。取值范围：\n- optional：不强制使用。\n- required：强制使用。设置该取值后，普通模式无法访问实例元数据。\n\n默认值：optional。\n> 有关实例元数据访问模式的更多信息，请参见[实例元数据访问模式](~~150575~~)。",
        "en": "Specifies whether to forcefully use the security hardening mode (IMDSv2) to access instance metadata. Valid values:\n\n- optional: does not forcefully use the security hardening mode (IMDSv2).\n\n- required: forcefully uses the security hardening mode (IMDSv2). After you set this parameter to required, you cannot access instance metadata in normal mode.\n\nDefault value: optional.\n\n> For more information about modes of accessing instance metadata, see [Obtain instance metadata](~~150575~~)."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定的实例ID。",
        "en": "The ID of the instance."
      }
    },
    {
      "name": "InstanceMetadataTags",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否启用实例元数据的访问标签能力。取值范围：\n- enabled：启用。\n- disabled：禁用。\n\n默认值：disabled。\n<notice>\n标签键需为字母、数字、@、半角冒号（:）、下划线（_）、短划线（-）、句点（.）、等号（=）、半角逗号（,）的组合，且不能为\".\"或者\"..\"，否则无法在元数据中访问。\n</notice>",
        "en": "Specifies whether to enable the access channel for instance metadata. Valid values:\n\n- enabled\n\n- disabled\n\nDefault value: disabled.\n\n> The tag key must be a combination of letters, digits, @, colons (:), underscores (\\_), hyphens (-), periods (.), equal signs (=), and commas (,). The tag key cannot be \".\" or \"..\". Otherwise, the tag key cannot be accessed in the metadata."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例所在地域的ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the instance. You can call the [DescribeRegions](~~25609~~) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改实例内的元数据访问设置",
    "en": "ModifyInstanceMetadataOptions"
  },
  "example": {
    "unifiedCli": "aliyun ecs modify-instance-metadata-options --biz-region-id cn-hangzhou --instance-id i-bp67acfmxaz0000 --http-endpoint enabled --http-tokens optional --http-put-response-hop-limit 0 --instance-metadata-tags disabled --region cn-hangzhou",
    "legacyCli": "aliyun ecs ModifyInstanceMetadataOptions --region cn-hangzhou --HttpPutResponseHopLimit 0 --HttpTokens optional --InstanceId 'i-bp67acfmxaz0000' --RegionId 'cn-hangzhou' --InstanceMetadataTags disabled --HttpEndpoint enabled"
  }
}