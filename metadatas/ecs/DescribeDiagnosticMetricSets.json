{
  "name": "DescribeDiagnosticMetricSets",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页行数。最大值为100。\n\n默认值：\n\n- 当不设置值时，默认值为10。\n- 当设置的值大于100时，默认值为100。",
        "en": "The number of entries per page. Valid values: 1 to 100.\n\nDefault value:\n\n- If this parameter is left empty, the default value is 10.\n\n- If you set this parameter to a value that is greater than 100, the default value is 100."
      }
    },
    {
      "name": "MetricSetIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "诊断项集合列表。",
        "en": "The IDs of diagnostic metric sets."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询凭证（Token）。取值为上一次调用接口返回的`NextToken`参数值，初次调用接口时无需设置该参数。",
        "en": "The pagination token that is used in the request to retrieve a new page of results. You do not need to specify this parameter for the first request. You must specify the token that is obtained from the previous query as the value of `NextToken`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the diagnostic metric set. You can call the [DescribeRegions](~~25609~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源类型。",
        "en": "The resource type supported by the diagnostic metric set."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "诊断项集合类型。取值范围：\n\n- User：用户。\n- Common：公共。\n\n默认值：User。",
        "en": "The type of the diagnostic metric set. Valid values:\n\n- User: custom diagnostic metric set\n\n- Common: public diagnostic metric set\n\nDefault value: User."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询资源诊断集合列表",
    "en": "DescribeDiagnosticMetricSets"
  },
  "example": {
    "unifiedCli": "aliyun ecs describe-diagnostic-metric-sets --biz-region-id cn-hangzhou --metric-set-ids dms-uf6i0tv2refv8wz00000 --type User --resource-type instance --next-token caeba0bbb2be03f84eb48b699f0a4883 --max-results 10 --region cn-hangzhou",
    "legacyCli": "aliyun ecs DescribeDiagnosticMetricSets --region cn-hangzhou --Type User --NextToken caeba0bbb2be03f84eb48b699f0a4883 --MaxResults 10 --MetricSetIds.1 'dms-uf6i0tv2refv8wz00000' --ResourceType instance --RegionId 'cn-hangzhou'"
  }
}