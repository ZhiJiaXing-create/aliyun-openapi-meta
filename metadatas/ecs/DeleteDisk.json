{
  "name": "DeleteDisk",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DiskId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要释放的云盘设备ID。",
        "en": "The ID of the disk that you want to release."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "释放按量付费数据盘",
    "en": "Release a pay-as-you-go data disk"
  },
  "descriptions": {
    "zh": "调用该接口时，您需要注意：\n\n- 您的磁盘手动快照会被保留。\n- 您可以通过[ModifyDiskAttribute](~~25517~~)设置是否保留或者同时释放自动快照。建议您及时删除不必要的快照，以保持足够的快照额度完成周期性的自动快照策略。\n- 释放磁盘时，云盘的状态必须为待挂载（Available）。\n- 如果指定ID的磁盘不存在，请求将被忽略。",
    "en": "When you invoke this operation, take note of the following items:\n\n- Manual snapshots of the disk are retained.\n- You can invoke [ModifyDiskAttribute](~~25517~~) to configure the Settings for whether to retain or release automatic snapshots together with the disk. Delete unnecessary snapshots promptly to maintain sufficient snapshot quota for epoch-based automatic snapshot policy.\n- The disk must be in the Pending Attach (Available) state when you release it.\n- If the disk with the specified ID does not exist, the request is ignored."
  },
  "example": {
    "unifiedCli": "aliyun ecs delete-disk --disk-id d-bp14k9cxvr5uzy50000 --region cn-hangzhou",
    "legacyCli": "aliyun ecs DeleteDisk --region cn-hangzhou --DiskId 'd-bp14k9cxvr5uzy50000'"
  }
}