{
  "name": "ModifyAutoProvisioningGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoProvisioningGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "弹性供应组的ID。",
        "en": "The auto-provisioning group ID."
      }
    },
    {
      "name": "AutoProvisioningGroupName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "弹性供应组的名称。长度为2~128个英文或中文字符。必须以大小字母或中文开头，不能以http://和https://开头。可以包含数字、半角冒号（:）、下划线（_）或者连字符（-）。",
        "en": "The name of the auto-provisioning group. The name must be 2 to 128 characters in length. It must start with a letter and cannot start with http\\:// or https\\://.[ It can contain letters, digits, colons (:), underscores (\\_), and hyphens (-).](http://https://%E3%80%82%E3%80%81%EF%BC%88:%EF%BC%89%E3%80%81%EF%BC%88_%EF%BC%89%EF%BC%88-%EF%BC%89%E3%80%82)"
      }
    },
    {
      "name": "DefaultTargetCapacityType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "PayAsYouGoTargetCapacity和SpotTargetCapacity之和小于TotalTargetCapacity时，指定差额容量的计费方式。取值范围：\n\n- PayAsYouGo：按量付费实例。\n- Spot：抢占式实例。",
        "en": "The type of supplemental instances. When the sum of the PayAsYouGoTargetCapacity and SpotTargetCapacity values is smaller than the TotalTargetCapacity value, the auto-provisioning group creates instances of the specified type to meet the target capacity. Valid values:\n\n- PayAsYouGo: pay-as-you-go instances\n\n- Spot: spot instances"
      }
    },
    {
      "name": "ExcessCapacityTerminationPolicy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "弹性供应组实时容量超过目标容量并触发缩容时，是否释放缩容的实例。取值范围：\n\n- termination：释放缩容的实例。\n- no-termination：只将缩容的实例移出弹性供应组。",
        "en": "Specifies whether to release the removed instances when the real-time capacity of the auto-provisioning group exceeds the target capacity and a scale-in event is triggered. Valid values:\n\n- termination: releases the removed instances.\n\n- no-termination: removes the instances from the auto-provisioning group but does not release them."
      }
    },
    {
      "name": "LaunchTemplateConfig",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "扩展启动模板列表。",
        "en": "The extended configurations of the launch template."
      },
      "sub_parameters": [
        {
          "name": "InstanceType",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "扩展启动模板对应的实例规格，N的取值范围：1~20。取值范围：请参见[实例规格族](~~25378~~)。",
            "en": "The instance type in extended configuration N. Valid values of N: 1 to 20. For more information about the valid values of this parameter, see [Instance families](~~25378~~)."
          }
        },
        {
          "name": "MaxPrice",
          "position": "Query",
          "type": "Double",
          "required": false,
          "description": {
            "zh": "扩展启动模板中，抢占式实例的价格上限。",
            "en": "The maximum price of spot instances in extended configuration N."
          }
        },
        {
          "name": "Priority",
          "position": "Query",
          "type": "Integer",
          "required": false,
          "description": {
            "zh": "扩展启动模板的优先级，取值为0时优先级最高。取值范围：大于0。",
            "en": "The priority of extended configuration N. A value of 0 indicates the highest priority. The value must be greater than 0."
          }
        },
        {
          "name": "VSwitchId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "扩展启动模板中，ECS实例加入的虚拟交换机的ID。扩展模板中启动的ECS实例的可用区由虚拟交换机决定。",
            "en": "The ID of the vSwitch in extended configuration N. The zone of the instances created from the extended configuration is determined by the vSwitch."
          }
        },
        {
          "name": "WeightedCapacity",
          "position": "Query",
          "type": "Double",
          "required": false,
          "description": {
            "zh": "扩展启动模板中，实例规格的权重。取值越高，单台实例满足计算力需求的能力越大，所需的实例数量越小。取值范围：大于0。\n\n您可以根据指定实例规格的计算力和集群单节点最低计算力得出权重值。假设单节点最低计算力为8 vCPU、60 GiB，则：\n\n- 8 vCPU、60 GiB的实例规格权重可以设置为1。\n- 16 vCPU、120 GiB的实例规格权重可以设置为2。",
            "en": "The weight of the instance type specified in the extended configuration. A greater weight indicates that a single instance has more computing power and fewer instances are required. The value must be greater than 0.\n\nThe weight is calculated based on the computing power of the instance type and the minimum computing power of a single instance in the cluster that can created by the auto-provisioning group. For example, assume that the minimum computing power of a single instance is 8 vCPUs and 60 GiB of memory.\n\n- For an instance type with 8 vCPUs and 60 GiB of memory, you can set the weight to 1.\n\n- For an instance type with 16 vCPUs and 120 GiB of memory, you can set the weight to 2."
          }
        }
      ]
    },
    {
      "name": "MaxSpotPrice",
      "position": "Query",
      "type": "Float",
      "required": false,
      "description": {
        "zh": "弹性供应组内抢占式实例的最高价格。\n\n> 同时设置了MaxSpotPrice和LaunchTemplateConfig.N.MaxPrice时，以最低值为准。LaunchTemplateConfig.N.MaxPrice在创建弹性供应组时设置，且不可修改。",
        "en": "The maximum price of spot instances in the auto-provisioning group.\n\n> When both the MaxSpotPrice and LaunchTemplateConfig.N.MaxPrice parameters are specified, the smaller one of the two parameter values is used. The LaunchTemplateConfig.N.MaxPrice parameter is specified when the auto-provisioning group is created, and cannot be modified."
      }
    },
    {
      "name": "PayAsYouGoTargetCapacity",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "弹性供应组内，按量付费实例的目标容量。取值范围：小于TotalTargetCapacity的参数取值。",
        "en": "The target capacity of pay-as-you-go instances in the auto-provisioning group. Valid values: Set this parameter to a value smaller than the TotalTargetCapacity value."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "弹性供应组所在地域的ID。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the auto-provisioning group. You can call the [DescribeRegions](~~25609~~) operation to query the most recent region list."
      }
    },
    {
      "name": "SpotTargetCapacity",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "弹性供应组内，抢占式实例的目标容量。取值范围：小于TotalTargetCapacity的参数取值。",
        "en": "The target capacity of spot instances in the auto-provisioning group. Valid values: Set this parameter to a value smaller than the TotalTargetCapacity value."
      }
    },
    {
      "name": "TerminateInstancesWithExpiration",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "弹性供应组到期时，是否释放组内实例。取值范围：\n\n- true：释放组内实例。\n- false：只将组内实例移出弹性供应组。",
        "en": "Specifies whether to release instances that are located in the auto-provisioning group after the group expires. Valid values:\n\n- true: releases instances that are located in the auto-provisioning group.\n\n- false: removes instances from the auto-provisioning group but does not release them."
      }
    },
    {
      "name": "TotalTargetCapacity",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "弹性供应组的目标总容量。取值范围：正整数。\n\n总容量必须大于等于PayAsYouGoTargetCapacity（指定的按量付费实例目标容量）和SpotTargetCapacity（指定的抢占式实例目标容量）取值之和。",
        "en": "The total target capacity of the auto-provisioning group. The value must be a positive integer.\n\nThe total target capacity of the auto-provisioning group must be greater than or equal to the sum of the target capacity of pay-as-you-go instances specified by the PayAsYouGoTargetCapacity parameter as well as the target capacity of spot instances specified by the SpotTargetCapacity parameter."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改弹性供应组的配置",
    "en": "ModifyAutoProvisioningGroup"
  },
  "descriptions": {
    "zh": "修改弹性供应组前，请注意：\n\n- 如果修改了供应组容量或者容量相关设置，供应组会在修改完成后执行一次调度任务。\n- 如果供应组处于删除中状态，无法修改供应组。",
    "en": "Before you call this operation, take note of the following items:\n\n- If you modify the capacity or capacity-related settings of an auto-provisioning group, the group executes a scheduling task after the group is modified.\n\n- You cannot modify an auto-provisioning group when the group is being deleted."
  },
  "example": {
    "unifiedCli": "aliyun ecs modify-auto-provisioning-group --biz-region-id cn-hangzhou --auto-provisioning-group-id apg-bp67acfmxazb4ph0000 --excess-capacity-termination-policy no-termination --default-target-capacity-type Spot --terminate-instances-with-expiration false --max-spot-price 0.5 --total-target-capacity 70 --pay-as-you-go-target-capacity 30 --spot-target-capacity 30 --auto-provisioning-group-name apg-test --launch-template-config WeightedCapacity=2.0 Priority=1 VSwitchId=vsw-sn5bsitu4lfzgc5o70000 MaxPrice=3.0 InstanceType=ecs.g5.large --region cn-hangzhou",
    "legacyCli": "aliyun ecs ModifyAutoProvisioningGroup --region cn-hangzhou --TotalTargetCapacity 70 --AutoProvisioningGroupName 'apg-test' --ExcessCapacityTerminationPolicy 'no-termination' --LaunchTemplateConfig.1.InstanceType 'ecs.g5.large' --LaunchTemplateConfig.1.WeightedCapacity '2.0' --PayAsYouGoTargetCapacity 30 --TerminateInstancesWithExpiration false --DefaultTargetCapacityType Spot --LaunchTemplateConfig.1.MaxPrice '3.0' --SpotTargetCapacity 30 --AutoProvisioningGroupId 'apg-bp67acfmxazb4ph0000' --MaxSpotPrice '0.5' --LaunchTemplateConfig.1.Priority 1 --RegionId 'cn-hangzhou' --LaunchTemplateConfig.1.VSwitchId 'vsw-sn5bsitu4lfzgc5o70000'"
  }
}