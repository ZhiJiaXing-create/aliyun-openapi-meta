{
  "name": "CreateForwardEntry",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ExternalIp",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The public IP address for the DNAT entry. This must be a public IP address of the NAT gateway associated with the DNAT table."
      }
    },
    {
      "name": "ExternalPort",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The external port used for DNAT. Valid values: 1 to 65535."
      }
    },
    {
      "name": "ForwardTableId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the DNAT table."
      }
    },
    {
      "name": "InternalIp",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The private IP address to which traffic is forwarded."
      }
    },
    {
      "name": "InternalPort",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The internal port to which traffic is forwarded. Valid values: 1 to 65535."
      }
    },
    {
      "name": "IpProtocol",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The protocol. Valid values: TCP, UDP, and Any. If you set this parameter to Any, the DNAT entry applies to all protocols."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the region where the DNAT table is located. You can call the [DescribeRegions](https://help.aliyun.com/document_detail/36063.html) operation to retrieve a list of available regions."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "CreateForwardEntry"
  },
  "example": {
    "unifiedCli": "aliyun ecs create-forward-entry --biz-region-id cn-hangzhou --forward-table-id example-string --external-ip example-string --external-port example-string --internal-ip example-string --internal-port example-string --ip-protocol example-string --region cn-hangzhou",
    "legacyCli": "aliyun ecs CreateForwardEntry --region cn-hangzhou --ExternalPort 'example-string' --ExternalIp 'example-string' --IpProtocol 'example-string' --RegionId 'cn-hangzhou' --InternalPort 'example-string' --InternalIp 'example-string' --ForwardTableId 'example-string'"
  }
}