{
  "name": "CreateAutoSnapshotPolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CopiedSnapshotsRetentionDays",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "跨地域复制快照的保留时间，单位为天。取值范围：\n\n- -1：永久保存\n- 1~65535：指定保存天数\n\n默认值：-1。",
        "en": "The retention period of cross-region snapshot replicas. Unit: days. Valid values:\n\n- -1: Snapshot replicas are permanently retained.\n- 1 to 65535: Snapshot replicas are retained for the specified number of days.\n\nDefault value: -1."
      }
    },
    {
      "name": "CopyEncryptionConfiguration",
      "position": "Query",
      "type": "Struct",
      "required": false,
      "description": {
        "zh": "快照异地备份加密参数对象",
        "en": "The backup encryption parameter object for snapshot geo-redundancy."
      }
    },
    {
      "name": "EnableCrossRegionCopy",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否允许自动跨地域复制。\n\n- true：允许。\n- false：不允许。",
        "en": "Specifies whether to enable automatic cross-region replication.\n\n- true: enables automatic cross-region replication.\n- false: disables automatic cross-region replication."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group."
      }
    },
    {
      "name": "StorageLocationArn",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "> 该参数暂未开放使用。",
        "en": "> This parameter is not publicly available."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "自动快照策略标签列表。",
        "en": "The tags of the automatic snapshot policy."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "自动快照策略的标签键。N的取值范围：1~20。一旦传入该值，则不允许为空字符串。最多支持128个字符，不能以aliyun或acs:开头，不能包含http://或https://。",
            "en": "The tag key of the automatic snapshot policy. Valid values of N: 1 to 20. The tag key cannot be an empty string. The tag key can be up to 128 characters in length and cannot start with aliyun or acs:. The tag key cannot contain http:// or https://."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "自动快照策略的标签值。N的取值范围：1~20。一旦传入该值，可以为空字符串。最多支持128个字符，不能以acs:开头，不能包含http://或https://。",
            "en": "The tag value of the automatic snapshot policy. Valid values of N: 1 to 20. The tag value can be an empty string. The tag value can be up to 128 characters in length and cannot start with acs:. The tag value cannot contain http:// or https://."
          }
        }
      ]
    },
    {
      "name": "TargetCopyRegions",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "跨地域复制快照的目标地域。目前支持设置一个目标地域。",
        "en": "The destination region to which snapshots are replicated. You can specify only one destination region."
      }
    },
    {
      "name": "autoSnapshotPolicyName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自动快照策略的名称。长度为2~128个英文或中文字符。必须以大小写字母或中文开头，不能以http://或https://开头。可以包含数字、半角冒号（:）、下划线（_）或者短划线（-）。\n\n默认值：空",
        "en": "The name of the automatic snapshot policy. The name must be 2 to 128 characters in length. The name must start with a letter and cannot start with http:// or https://. The name can contain digits, colons (:), underscores (_), and hyphens (-).\n\nDefault value: null."
      }
    },
    {
      "name": "regionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "自动快照策略所属的地域。您可以调用[DescribeRegions](~~25609~~)查看最新的阿里云地域列表。",
        "en": "The region to which the automatic snapshot policy belongs. You can call [DescribeRegions](~~25609~~) to query the most recent region list."
      }
    },
    {
      "name": "repeatWeekdays",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "自动快照的重复日期，单位为天，周期为星期。取值范围：1~7，例如1表示周一。格式说明：\n\n- 传入的参数需要保持JSON数组格式。例如，\\[\"1\"\\]表示每周一重复创建自动快照。\n- 当一星期内需要创建多次自动快照时，可以传入多个时间点，时间点之间用半角逗号（,）隔开，最多传入7个时间点。例如，\\[\"1\",\"3\",\"5\"\\]表示每周一、周三、周五重复创建自动快照。",
        "en": "The days of the week on which automatic snapshots are created. Unit: days. The cycle is weekly. Valid values: 1 to 7. For example, 1 indicates Monday. Format description:\n\n- The parameter value must be a JSON array. For example, \\[\"1\"\\] indicates that automatic snapshots are created every Monday.\n- To create multiple automatic snapshots within a week, specify multiple days separated by commas (,). You can specify a maximum of 7 days. For example, \\[\"1\",\"3\",\"5\"\\] indicates that automatic snapshots are created every Monday, Wednesday, and Friday."
      }
    },
    {
      "name": "retentionDays",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "自动快照的保留时间，单位为天。取值范围：\n\n- -1：永久保存\n- 1~65535：指定保存天数\n\n默认值：-1。",
        "en": "The retention period of automatic snapshots. Unit: days. Valid values:\n\n- -1: Automatic snapshots are permanently retained.\n- 1 to 65535: Automatic snapshots are retained for the specified number of days.\n\nDefault value: -1."
      }
    },
    {
      "name": "timePoints",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "自动快照的创建时间点。使用UTC +8时间，单位为小时。取值范围：0~23，代表00:00至23:00共24个时间点，例如1表示01:00。格式说明：\n\n- 传入的参数需要保持JSON数组格式。例如，\\[\"1\"\\]表示01:00创建自动快照。\n- 当一天内需要创建多次自动快照时，可以传入多个时间点，时间点之间用半角逗号（,）隔开，最多传入24个时间点。例如，\\[\"1\",\"3\",\"5\"\\]表示01:00、03:00、05:00时创建自动快照。\n\n>如果云盘数据较多，单次创建自动快照的时长超过两个时间点之间的间隔，则自动跳过下一时间点。例如：您分别设置了09:00、10:00、11:00和12:00为自动快照时间点。由于云盘数据较多，09:00开始创建，10:20完成创建，实际耗时80分钟。系统会跳过时间点10:00，等到11:00继续为您创建自动快照。",
        "en": "The points in time at which automatic snapshots are created. The time is displayed in UTC+8. Unit: hours. Valid values: 0 to 23, which represent the 24 points in time from 00:00 to 23:00. For example, 1 indicates 01:00. Format description:\n\n- The parameter value must be a JSON array. For example, \\[\"1\"\\] indicates that automatic snapshots are created at 01:00.\n- To create multiple automatic snapshots within a day, specify multiple points in time separated by commas (,). You can specify a maximum of 24 points in time. For example, \\[\"1\",\"3\",\"5\"\\] indicates that automatic snapshots are created at 01:00, 03:00, and 05:00.\n\n> If a disk contains a large amount of data and the time required to create an automatic snapshot exceeds the interval between two consecutive points in time, the next point in time is skipped. For example, you set 09:00, 10:00, 11:00, and 12:00 as the points in time for automatic snapshot creation. The snapshot creation starts at 09:00 and is completed at 10:20, which takes 80 minutes. The system skips the 10:00 point in time and creates the next automatic snapshot at 11:00."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建自动快照策略",
    "en": "Create an automatic snapshot policy"
  },
  "descriptions": {
    "zh": "调用该接口之前，请认真阅读[创建自动快照策略](~~127767~~)。\n\n调用该接口时，您需要注意：\n\n-  一个阿里云账户在一个地域最多能创建100条自动快照策略。 当某块云盘的自动快照数量达到额度上限，系统在创建新的快照任务时，会删除由自动快照策略所生成的时间最早的自动快照点。\n\n\n- 当ECS实例存在停止或者重启任务时，系统不会执行自动快照策略。\n- 已启用快照跨地域复制且在未配置加密参数场景下，加密快照默认使用目的端服务密钥进行加密拷贝。跨地域复制快照的注意事项，请参见[复制快照](~~159441~~)。\n\n创建自动快照策略后，需要调用[ApplyAutoSnapshotPolicy](~~25531~~)将策略应用到目标云盘。如果您需要调整自动快照策略的内容，请调用[ModifyAutoSnapshotPolicyEx](~~25529~~)。",
    "en": "Before you call this operation, read [Create an automatic snapshot policy](~~127767~~).\n\nTake note of the following items:\n\n- You can create a maximum of 100 automatic snapshot policies in a region for an Alibaba Cloud account. When the number of automatic snapshots for a disk reaches the quota limit, the system deletes the earliest automatic snapshot created by the automatic snapshot policy when a new snapshot task is initiated.\n\n\n- The system does not execute automatic snapshot policies when an ECS instance has a pending stop or restart task.\n- If cross-region snapshot replication is enabled and no encryption parameters are configured, encrypted snapshots are replicated to the destination region by using the service key of the destination region by default. For more information about cross-region snapshot replication, see [Copy a snapshot](~~159441~~).\n\nAfter you create an automatic snapshot policy, call [ApplyAutoSnapshotPolicy](~~25531~~) to apply the policy to disks. To modify an automatic snapshot policy, call [ModifyAutoSnapshotPolicyEx](~~25529~~)."
  },
  "example": {
    "unifiedCli": "aliyun ecs create-auto-snapshot-policy --biz-region-id cn-hangzhou --auto-snapshot-policy-name TestName --time-points '0_,_1_,_…_23' --repeat-weekdays 1_,_2 --retention-days 30 --enable-cross-region-copy false --target-copy-regions cn-hangzhou --storage-location-arn example-string --copied-snapshots-retention-days 30 --tag Value=TestValue Key=TestKey --resource-group-id rg-aek2kkmhmhs0000 --copy-encryption-configuration '{\"KMSKeyId\":\"0e478b7a-4262-4802-b8cb-00d3fb40826X\",\"Encrypted\":false,\"Arn\":[{\"AssumeRoleFor\":1000000000,\"RoleType\":\"hide\",\"Rolearn\":\"hide\"}]}' --region cn-hangzhou",
    "legacyCli": "aliyun ecs CreateAutoSnapshotPolicy --region cn-hangzhou --repeatWeekdays '1_,_2' --Tag.1.Key TestKey --ResourceGroupId 'rg-aek2kkmhmhs0000' --CopyEncryptionConfiguration.Arn.1.Rolearn hide --CopyEncryptionConfiguration.Arn.1.AssumeRoleFor 1000000000 --StorageLocationArn 'example-string' --CopyEncryptionConfiguration.KMSKeyId '0e478b7a-4262-4802-b8cb-00d3fb40826X' --timePoints '0_,_1_,_…_23' --CopiedSnapshotsRetentionDays 30 --TargetCopyRegions 'cn-hangzhou' --regionId 'cn-hangzhou' --autoSnapshotPolicyName TestName --retentionDays 30 --CopyEncryptionConfiguration.Encrypted false --CopyEncryptionConfiguration.Arn.1.RoleType hide --EnableCrossRegionCopy false --Tag.1.Value TestValue --method POST --force"
  }
}