{
  "name": "DeleteRouteEntry",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DestinationCidrBlock",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The destination CIDR block of the route entry."
      }
    },
    {
      "name": "NextHopId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The ID of the next hop.\n\n> `NextHopId` and `NextHopList` are mutually exclusive. You can specify one but not both."
      }
    },
    {
      "name": "NextHopList",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "en": "A list of next hops for an ECMP route.\n\n> `NextHopId` and `NextHopList` are mutually exclusive. You can specify one but not both."
      },
      "sub_parameters": [
        {
          "name": "NextHopId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "en": "The ID of a next hop in the ECMP route."
          }
        },
        {
          "name": "NextHopType",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "en": "The type of a next hop in the ECMP route. Set the value to `Instance`."
          }
        }
      ]
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The ID of the region."
      }
    },
    {
      "name": "RouteTableId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the route table that contains the route entry."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "DeleteRouteEntry"
  },
  "example": {
    "unifiedCli": "aliyun ecs delete-route-entry --biz-region-id cn-hangzhou --route-table-id example-string --destination-cidr-block example-string --next-hop-id example-string --next-hop-list NextHopType=example-string NextHopId=example-string --region cn-hangzhou",
    "legacyCli": "aliyun ecs DeleteRouteEntry --region cn-hangzhou --RouteTableId 'example-string' --NextHopList.1.NextHopId 'example-string' --NextHopId 'example-string' --NextHopList.1.NextHopType 'example-string' --RegionId 'cn-hangzhou' --DestinationCidrBlock 'example-string'"
  }
}