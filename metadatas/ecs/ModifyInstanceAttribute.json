{
  "name": "ModifyInstanceAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CpuOptions",
      "position": "Query",
      "type": "Struct",
      "required": false
    },
    {
      "name": "CpuOptions.Core",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "CPU核心数。该参数不支持自定义设置，只能采用默认值。\n\n<props=\"china\">默认值：请参见[自定义CPU选项](~~145895~~)。</props>",
        "en": "The number of CPU cores. This parameter does not support custom values and can only use the default value.\n\n<props=\"china\">Default value: see [Customize CPU options](~~145895~~).</props>"
      }
    },
    {
      "name": "CpuOptions.ThreadsPerCore",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "CPU线程数。ECS实例的vCPU数=`CpuOptions.Core`取值*`CpuOptions.ThreadsPerCore`取值。\n\n- `CpuOptions.ThreadsPerCore=1`表示关闭CPU超线程。\n\n- 仅部分实例规格支持设置CPU线程数。\n\n<props=\"china\">取值范围和默认值：请参见[自定义CPU选项](~~145895~~)。</props>",
        "en": "The number of threads per CPU core. The number of vCPUs of the ECS instance = CpuOptions.Core value × CpuOptions.ThreadsPerCore value.\n\n- CpuOptions.ThreadsPerCore=1 indicates that hyper-threading is disabled.\n\n- Only some instance types support custom thread counts.\n\n<props=\"china\">Valid values and default value: see [Customize CPU options](~~145895~~).</props>"
      }
    },
    {
      "name": "CpuOptions.TopologyType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例的Cpu拓扑类型。取值范围：\n\n- ContinuousCoreToHTMapping：实例的Cpu拓扑中，实例的同一个Core的HT是连续的。\n- DiscreteCoreToHTMapping：实例的同一个Core的HT是离散的。\n\n默认值：无。\n\n使用该参数时请注意：\n- 实例必须为已停止（`Stopped`）状态。\n\n> 仅部分实例规格族支持使用本参数，具体支持实例规格族请参见[查看和修改CPU拓扑结构](~~2636059~~)。",
        "en": "The CPU topology type of the instance. Valid values:\n\n- ContinuousCoreToHTMapping: the hyper-threads of the same core in the CPU topology are continuous.\n- DiscreteCoreToHTMapping: the hyper-threads of the same core are discrete.\n\nDefault value: none.\n\nTake note of the following items:\n- The instance must be in the Stopped state.\n\n> Only some instance families support this parameter. For information about the supported instance families, see [View and modify the CPU topology structure](~~2636059~~)."
      }
    },
    {
      "name": "CreditSpecification",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "修改突发性能实例的运行模式。取值范围：\n\n- Standard：标准模式。\n- Unlimited：无性能约束模式。\n\n关于突发性能实例运行模式的更多信息，请参见[什么是突发性能实例](~~59977~~)。",
        "en": "The running mode of the burstable instance. Valid values:\n\n- Standard: standard mode.\n- Unlimited: unlimited mode.\n\nFor more information about the running modes of burstable instances, see [What are burstable instances?](~~59977~~)."
      }
    },
    {
      "name": "DeletionProtection",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "实例释放保护属性。指定是否支持通过控制台或API（[DeleteInstance](~~25507~~)）释放实例。\n\n> 该属性仅适用于按量付费实例，且只能限制手动释放操作，对系统释放操作不生效。",
        "en": "The release protection attribute of the instance. Specifies whether the instance can be released from the console or by calling [DeleteInstance](~~25507~~).\n\n> This attribute applies only to pay-as-you-go instances and only prevents manual release operations. It does not apply to system-initiated release operations."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例描述。长度为2~256个英文或中文字符，不能以`http://`或`https://`开头。",
        "en": "The description of the instance. The description must be 2 to 256 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "EnableJumboFrame",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "实例MTU是否开启Jumbo frame通信模式，取值范围：\n\n- true：开启。\n- false：不开启。\n\n使用该参数时请注意：\n- 实例必须处于运行中（`Running`）或者已停止（`Stopped`）状态。\n- 实例必须为虚拟专有网络（Virtual Private Cloud, VPC）下的ECS实例。\n- 开启Jumbo Frame特性后，实例的MTU值将变为8500，关闭后则回退为1500 。\n目前仅部分规格支持开启Jumbo frame。更多信息，请参见[ECS实例MTU](~~200512~~)。",
        "en": "Specifies whether to enable the Jumbo Frame feature for the instance MTU. Valid values:\n\n- true: enables the feature.\n- false: does not enable the feature.\n\nTake note of the following items:\n- The instance must be in the Running or Stopped state.\n- The instance must be a VPC-connected instance.\n- After the Jumbo Frame feature is enabled, the MTU value of the instance changes to 8500. After the feature is disabled, the MTU value reverts to 1500.\nOnly some instance types support the Jumbo Frame feature. For more information, see [ECS instance MTU](~~200512~~)."
      }
    },
    {
      "name": "EnableNetworkEncryption",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "开启/关闭VPC网络流量加密，可能值：\n\n- true：开启。\n- false：关闭。\n> 该参数正在邀测中，暂未开放使用。",
        "en": "Specifies whether to enable VPC network traffic encryption. Valid values:\n\n- true: enables the encryption.\n- false: disables the encryption.\n> This parameter is in invitational preview and is not publicly available."
      }
    },
    {
      "name": "HostName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "操作系统的主机名。使用该参数时请注意：\n\n- 实例不能处于创建中（`Pending`）或启动中（`Starting`）的状态，否则可能出现主机名以及`/etc/hosts`配置失效等情况。您可以调用[DescribeInstances](~~25506~~)查询实例当前的状态信息。\n\n- 重启实例后生效。您可以在ECS控制台重启（具体操作，请参见[重启实例](~~25440~~)）或者调用[RebootInstance](~~25502~~)重启，在操作系统内部重启不能生效。\n\n\n不同操作系统主机名存在以下限制：\n\n- Windows Server系统：长度为2-15个字符，允许使用大小写字母、数字或短划线（-）。不能以短划线（-）开头或结尾，不能连续使用短划线（-），也不能仅使用数字。\n\n- 其他类型实例（Linux等）：长度为2-64个字符，允许使用半角句号（.）分隔字符成多段，每段允许使用大小写字母、数字或连字符（-），但不能连续使用半角句号（.）或短划线（-）。不能以半角句号（.）或短划线（-）开头或结尾。",
        "en": "The hostname of the operating system. Take note of the following items:\n\n- The instance cannot be in the Pending or Starting state. Otherwise, the hostname and `/etc/hosts` configuration may not take effect. You can call [DescribeInstances](~~25506~~) to query the current status of the instance.\n\n- The new hostname takes effect after you restart the instance. You can restart the instance in the ECS console (for more information, see [Restart an instance](~~25440~~)) or by calling [RebootInstance](~~25502~~). Restarting the instance from within the operating system does not take effect.\n\n\nThe hostname has the following limits for different operating systems:\n\n- Windows Server: The hostname must be 2 to 15 characters in length and can contain uppercase letters, lowercase letters, digits, and hyphens (-). It cannot start or end with a hyphen (-), cannot contain consecutive hyphens (-), and cannot contain only digits.\n\n- Other instances (such as Linux): The hostname must be 2 to 64 characters in length. You can use periods (.) to separate the hostname into multiple segments. Each segment can contain uppercase letters, lowercase letters, digits, and hyphens (-), but cannot contain consecutive periods (.) or hyphens (-). The hostname cannot start or end with a period (.) or hyphen (-)."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "InstanceName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例名称。长度为2~128个英文或中文字符。必须以大小写字母或中文开头，不能以`http://`或`https://`开头。可以包含数字、半角冒号（:）、下划线（_）或者短划线（-）。",
        "en": "The name of the instance. The name must be 2 to 128 characters in length. It must start with an uppercase letter, lowercase letter, or Chinese character and cannot start with `http://` or `https://`. The name can contain digits, colons (:), underscores (_), and hyphens (-)."
      }
    },
    {
      "name": "NetworkInterfaceQueueNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "主网卡队列数。使用该参数时请注意：\n- 实例必须为已停止（`Stopped`）状态。\n- 不能超过实例规格允许的单块网卡最大队列数，实例的所有网卡累加队列数不能超过实例规格允许的队列数总配额。实例规格的单块网卡最大队列数和总配额可以通过[DescribeInstanceTypes](~~25620~~)查询。\n- 当取值为-1时，将重置主网卡队列数为对应实例规格的默认值。实例规格的弹性网卡默认队列数可以通过[DescribeInstanceTypes](~~25620~~)查询。",
        "en": "The number of queues for the primary network interface controller (NIC). Take note of the following items:\n- The instance must be in the Stopped state.\n- The value cannot exceed the maximum number of queues per NIC allowed by the instance type. The total number of queues for all NICs on the instance cannot exceed the total queue quota allowed by the instance type. You can call [DescribeInstanceTypes](~~25620~~) to query the maximum number of queues per NIC and the total queue quota for an instance type.\n- If you set this parameter to -1, the number of queues for the primary NIC is reset to the default value for the instance type. You can call [DescribeInstanceTypes](~~25620~~) to query the default number of Elastic Network Interface (ENI) queues for an instance type."
      }
    },
    {
      "name": "Password",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例的密码。支持长度为8~30个字符，必须同时包含大小写英文字母、数字和特殊符号中的三类字符。特殊符号可以是：**()`~!@#$%^&*-_+=|{}[]:;'<>,.?/**\n。Windows实例不能以斜线号（/）为密码首字符。使用该参数时请注意：\n\n- 实例不能处于启动中（`Starting`）状态。\n- 重启实例后生效。您可以在ECS控制台重启（具体操作，请参见[重启实例](~~25440~~)）或者调用[RebootInstance](~~25502~~)重启，在操作系统内部重启不能生效。\n\n> 如果传入`Password`参数，建议您使用HTTPS协议发送请求，避免密码泄露。",
        "en": "The password of the instance. The password must be 8 to 30 characters in length and must contain at least three of the following character types: uppercase letters, lowercase letters, digits, and special characters. The following special characters are supported: **()`~!@#$%^&*-_+=|{}[]:;'<>,.?/**\n. For Windows instances, the password cannot start with a forward slash (/). Take note of the following items:\n\n- The instance cannot be in the Starting state.\n- The new password takes effect after you restart the instance. You can restart the instance in the ECS console (for more information, see [Restart an instance](~~25440~~)) or by calling [RebootInstance](~~25502~~). Restarting the instance from within the operating system does not take effect.\n\n> If you specify the Password parameter, use HTTPS to send the request to avoid password leaks."
      }
    },
    {
      "name": "PrivateDnsNameOptions",
      "position": "Query",
      "type": "Struct",
      "required": false,
      "description": {
        "zh": "实例的私网域名配置信息集合。\n\n\n关于私网域名解析，请参见[ECS私网域名解析\n](~~2844797~~)。",
        "en": "The private domain name configuration of the instance.\n\n\nFor more information about private private domain resolution, see [ECS private private domain resolution](~~2844797~~)."
      }
    },
    {
      "name": "Recyclable",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": ">该参数正在邀测中，暂不开放使用。",
        "en": "> This parameter is in invitational preview and is not publicly available."
      }
    },
    {
      "name": "RemoteConnectionOptions",
      "position": "Query",
      "type": "Struct",
      "required": false,
      "description": {
        "zh": ">该参数正在邀测中，暂不开放使用。",
        "en": "> This parameter is in invitational preview and is not publicly available."
      }
    },
    {
      "name": "SecurityGroupIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "实例重新加入的安全组ID数组。使用该参数时请注意：\n\n- 数组内的安全组ID不能重复，数组长度与实例能够加入安全组配额有关。更多信息，请参见[使用限制](~~25412#SecurityGroupQuota1~~)。\n- 实例会离开当前的安全组，如需保留设置，您需要在数组中添加当前的安全组ID。\n- 支持切换安全组类型，但设置的安全组列表中不能同时包含普通安全组和企业安全组。\n- 安全组必须和实例属于同一个VPC。\n\n> 修改安全组后很快会生效于对应的实例，但可能有较小的延迟。",
        "en": "The IDs of the security groups to which the instance is reassigned. Take note of the following items:\n\n- Security group IDs in the array cannot be duplicated. The maximum length of the array depends on the maximum number of security groups to which the instance can belong. For more information, see [Limits](~~25412#SecurityGroupQuota1~~).\n- The instance leaves its current security groups. To retain the current security groups, add their IDs to the array.\n- You can switch between security group types, but the list cannot contain both basic security groups and advanced security groups at the same time.\n- The security groups must belong to the same VPC as the instance.\n\n> Changes to security groups take effect on the instance shortly after the modification, but a slight delay may occur."
      }
    },
    {
      "name": "UserData",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例自定义数据，建议Base64编码后传入。使用该参数时请注意：\n\n- 实例必须满足自定义数据使用限制。更多信息，请参见[生成实例自定义数据](~~49121~~)。\n- 重启实例后，实例上将展示新的自定义数据，但不会运行数据。\n\n> 编码前，原始数据不能超过32 KB。建议不要明文传入敏感信息，例如密码和私钥等。如果必须传入敏感信息，建议您加密后再以Base64编码传入，在实例内部以同样的方式解密。",
        "en": "The instance user data. We recommend that you Base64-encode the data before you pass it in. Take note of the following items:\n\n- The instance must meet the usage limits for instance user data. For more information, see [Create instance user data](~~49121~~).\n- After you restart the instance, the new user data is displayed on the instance but is not run.\n\n> Before Base64 encoding, the raw data cannot exceed 32 KB. Do not pass in sensitive information such as passwords and private keys in plaintext. If you must pass in sensitive information, encrypt the information, Base64-encode it, and then decrypt it in the same way within the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改实例属性信息",
    "en": "Modify instance attributes"
  },
  "descriptions": {
    "zh": "被安全锁定的实例不支持调用该接口。更多信息，请参见[资源安全锁定对调用API的影响](~~25695~~)。",
    "en": "Instances that are locked for security reasons do not support this operation. For more information, see [Impact of resource security locks on API calls](~~25695~~)."
  },
  "example": {
    "unifiedCli": "aliyun ecs modify-instance-attribute --instance-id i-bp67acfmxazb4ph0000 --password Test123456 --host-name testHostName --instance-name testInstanceName --description testInstanceDescription --user-data ZWNobyBoZWxsbyBlY3Mh --recyclable true --credit-specification Standard --deletion-protection false --network-interface-queue-number 8 --security-group-ids sg-bp15ed6xe1yxeycg7o0000 --remote-connection-options Type=hide Password=hide --enable-jumbo-frame false --cpu-options-core 2 --cpu-options-threads-per-core 2 --cpu-options-topology-type DiscreteCoreToHTMapping --private-dns-name-options EnableInstanceIdDnsARecord=false HostnameType=Custom EnableIpDnsPtrRecord=false EnableIpDnsARecord=false EnableInstanceIdDnsAAAARecord=false --cpu-options NestedVirtualization=enabled --enable-network-encryption true --region cn-hangzhou",
    "legacyCli": "aliyun ecs ModifyInstanceAttribute --region cn-hangzhou --CpuOptions.TopologyType DiscreteCoreToHTMapping --PrivateDnsNameOptions.EnableIpDnsARecord false --Description testInstanceDescription --RemoteConnectionOptions.Password hide --InstanceId 'i-bp67acfmxazb4ph0000' --UserData ZWNobyBoZWxsbyBlY3Mh --EnableNetworkEncryption true --PrivateDnsNameOptions.EnableInstanceIdDnsARecord false --RemoteConnectionOptions.Type hide --PrivateDnsNameOptions.HostnameType Custom --DeletionProtection false --CpuOptions.NestedVirtualization enabled --InstanceName testInstanceName --CpuOptions.Core 2 --NetworkInterfaceQueueNumber 8 --EnableJumboFrame false --PrivateDnsNameOptions.EnableIpDnsPtrRecord false --Recyclable true --PrivateDnsNameOptions.EnableInstanceIdDnsAAAARecord false --SecurityGroupIds.1 'sg-bp15ed6xe1yxeycg7o0000' --CpuOptions.ThreadsPerCore 2 --HostName testHostName --CreditSpecification Standard --Password Test123456 --method POST --force"
  }
}