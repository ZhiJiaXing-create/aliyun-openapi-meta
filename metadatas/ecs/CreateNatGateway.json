{
  "name": "CreateNatGateway",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BandwidthPackage",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "en": "Configurations for the bandwidth packages to create and associate with the nat gateway."
      },
      "sub_parameters": [
        {
          "name": "Bandwidth",
          "position": "Query",
          "type": "Integer",
          "required": false,
          "description": {
            "en": "The peak bandwidth for the EIPs in the bandwidth package. Unit: Mbit/s."
          }
        },
        {
          "name": "IpCount",
          "position": "Query",
          "type": "Integer",
          "required": false,
          "description": {
            "en": "The number of EIPs to create in the bandwidth package. Valid values: 1 to 10."
          }
        },
        {
          "name": "Zone",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "en": "The ID of the zone in which to create the EIPs. If you do not specify a zone, the system randomly selects one."
          }
        }
      ]
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "A client token to ensure the idempotence of the request.\n\nThis token is client-generated and must be unique for each request. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "A description of the nat gateway.\n\nThe description must be 2 to 256 characters in length. It must start with a letter or a Chinese character but cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "A name for the nat gateway.\n\nThe name must be 2 to 128 characters in length. It must start with a letter or a Chinese character but cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the region in which to create the nat gateway."
      }
    },
    {
      "name": "VpcId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "en": "The ID of the VPC in which to create the nat gateway."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "CreateNatGateway"
  },
  "example": {
    "unifiedCli": "aliyun ecs create-nat-gateway --biz-region-id cn-hangzhou --vpc-id example-string --name example-string --description example-string --client-token example-string --bandwidth-package IpCount=1 Zone=example-string Bandwidth=1 --region cn-hangzhou",
    "legacyCli": "aliyun ecs CreateNatGateway --region cn-hangzhou --BandwidthPackage.1.IpCount 1 --Description 'example-string' --BandwidthPackage.1.Zone 'example-string' --VpcId 'example-string' --BandwidthPackage.1.Bandwidth 1 --RegionId 'cn-hangzhou' --ClientToken 'example-string' --Name 'example-string'"
  }
}