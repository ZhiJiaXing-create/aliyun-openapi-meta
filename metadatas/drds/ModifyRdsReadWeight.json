{
  "name": "ModifyRdsReadWeight",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DbName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据库名称。",
        "en": "The name of the database."
      }
    },
    {
      "name": "DrdsInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "DRDS实例ID。",
        "en": "The instance ID of the DRDS instance."
      }
    },
    {
      "name": "InstanceNames",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "RDS实例列表，多个列表间用英文逗号（,）隔开。",
        "en": "The list of ApsaraDB RDS instances. Separate multiple instances with commas (,)."
      }
    },
    {
      "name": "Weights",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "RDS各个实例对应的权重值，多个权重间用英文逗号（,）隔开。",
        "en": "The weight values corresponding to each ApsaraDB RDS instance. Separate multiple weight values with commas (,)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改RDS实例的读写权重",
    "en": "Modify read and write weights of an apsaraDB RDS instance"
  },
  "example": {
    "unifiedCli": "aliyun drds modify-rds-read-weight --drds-instance-id drds000000000000 --db-name test --instance-names rm-0000000000000000,rm-0000000000000000 --weights 14,86 --api-version 2019-01-23 --region cn-hangzhou",
    "legacyCli": "aliyun drds ModifyRdsReadWeight --region cn-hangzhou --InstanceNames 'rm-0000000000000000,rm-0000000000000000' --DrdsInstanceId drds000000000000 --Weights '14,86' --DbName test"
  }
}