{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否删除资源的所有标签。",
        "en": "Specifies whether to remove all tags from the resources."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID。",
        "en": "The resource ID."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型，取值固定为INSTANCE。",
        "en": "The resource type. Set this parameter to INSTANCE."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签的键。\n> N表示传入第几个标签的键。例如：* Tag.1.Key表示传入第一个标签的键。* Tag.2.Key表示传入第二个标签的键。",
        "en": "The key of the tag.\n> N specifies the sequence number of the tag key. For example: * Tag.1.Key specifies the key of the first tag. * Tag.2.Key specifies the key of the second tag."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "取消资源的标签",
    "en": "Remove tags from resources"
  },
  "example": {
    "unifiedCli": "aliyun drds untag-resources --biz-region-id cn-hangzhou --resource-type INSTANCE --all false --resource-id example-string --tag-key example-string --api-version 2019-01-23 --region cn-hangzhou",
    "legacyCli": "aliyun drds UntagResources --region cn-hangzhou --All false --ResourceId.1 'example-string' --TagKey.1 'example-string' --ResourceType INSTANCE --RegionId 'cn-hangzhou'"
  }
}