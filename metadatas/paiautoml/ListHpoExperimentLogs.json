{
  "name": "ListHpoExperimentLogs",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/automl/v1/hpo/experiment/[ExperimentId]/logs",
  "parameters": [
    {
      "name": "ExperimentId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "HPO实验id",
        "en": "The ID of the HPO experiment."
      }
    },
    {
      "name": "LogName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "log名称。目前支持‘nnimanager.log'和'dispatcher.log'",
        "en": "The name of the log. Valid values: nnimanager.log and dispatcher.log."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码。起始值：1。不填则返回log文件里所有内容。",
        "en": "The page number. Page start from page 1. If you leave this parameter empty, all content in the log file is returned."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页包含的日志行数。不填则返回log文件里所有内容。",
        "en": "The number of entries per page. If you leave this parameter empty, all content in the log file is returned."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "显示HPO实验一个指定log的内容",
    "en": "ListHpoExperimentLogs"
  },
  "example": {
    "unifiedCli": "aliyun paiautoml list-hpo-experiment-logs --experiment-id abcde --log-name nnimanager.log --page-number 1 --page-size 40 --region cn-hangzhou",
    "legacyCli": "aliyun paiautoml GET /api/automl/v1/hpo/experiment/abcde/logs --region cn-hangzhou --PageSize 40 --PageNumber 1 --LogName 'nnimanager.log' --header \"Content-Type=application/json\""
  }
}