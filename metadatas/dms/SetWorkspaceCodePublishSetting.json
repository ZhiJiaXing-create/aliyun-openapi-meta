{
  "name": "SetWorkspaceCodePublishSetting",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Config",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "发布的配置，json string。 repos列表为工作空间内的git repo，主要是设置发布的branch； exclude指定哪些目录跳过",
        "en": "The deployment configuration, provided as a JSON string. The repos array specifies the Git repositories in the workspace, primarily for setting the branch for publishing. The exclude parameter specifies which directories to skip."
      }
    },
    {
      "name": "WorkspaceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "发布的工作空间ID（数字ID）",
        "en": "The numeric ID of the workspace."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置工作空间的代码发布配置",
    "en": "SetWorkspaceCodePublishSetting"
  },
  "descriptions": {
    "zh": "设置工作空间的默认代码部署配置，主要是git repo的分支，以及需要排除的目录",
    "en": "Sets the default code deployment configuration for a workspace. This configuration includes the Git repository branch and the directories to exclude from deployment."
  }
}