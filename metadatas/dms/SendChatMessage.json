{
  "name": "SendChatMessage",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AgentId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "AgentId，必填。您可通过CreateAgentSession接口的返回值获取当前的AgentID。Agent资源存在生命周期，因此您每次请求需要填入的AgentID可能变化。",
        "en": "The agent ID. This parameter is required. You can obtain the current agent ID from the response of the CreateAgentSession operation. Agent resources have a lifecycle, so the agent ID that you specify in each request may change."
      }
    },
    {
      "name": "DMSUnit",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "您当前所在的DMS单元。若您选择分析数据库，该信息将用于后续通过DMS正确连接到您的DMS实例。您可前往DMS控制台查看您当前所在的DMS单元，若您是阿里云中国站用户，可以直接填入cn-hangzhou。",
        "en": "The Data Management unit you are currently in. If you choose to analyze a database, this information is used to correctly connect to your Data Management instance. You can go to the Data Management console to view your current Data Management unit. If you are a user of the Alibaba Cloud China Website, set this parameter to ap-southeast-1."
      }
    },
    {
      "name": "DataSource",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据源信息，可为空。此参数仅能传入一个数据源，建议更换为DataSources参数。",
        "en": "The data source information. This parameter is optional. You can pass only one data source in this parameter. Use the DataSources parameter instead."
      }
    },
    {
      "name": "DataSources",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "数据源信息详细，可为空",
        "en": "The detailed data source information. This parameter is optional."
      },
      "sub_parameters": [
        {
          "name": "DataSourceId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "已废弃，无需填入",
            "en": "Deprecated. You do not need to specify this parameter."
          }
        },
        {
          "name": "DataSourceType",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "数据源类型。可取值[remote_data_center, database]，分别表示本次分析是文件或者数据库。",
            "en": "The data source type. Valid values: remote_data_center and database, indicating that the analysis is performed on a file or a database."
          }
        },
        {
          "name": "Database",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "已废弃，无需填入",
            "en": "Deprecated. You do not need to specify this parameter."
          }
        },
        {
          "name": "DbName",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "数据库名称。",
            "en": "The database name."
          }
        },
        {
          "name": "DmsDatabaseId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "数据库在 DMS 的 ID",
            "en": "The ID of the database in Data Management."
          }
        },
        {
          "name": "DmsInstanceId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "实例在 DMS 的 ID",
            "en": "The ID of the instance in Data Management."
          }
        },
        {
          "name": "Engine",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "数据库类型",
            "en": "The database engine type."
          }
        },
        {
          "name": "FileId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "文件 ID",
            "en": "The file ID."
          }
        },
        {
          "name": "Location",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "已废弃，无需填入",
            "en": "Deprecated. You do not need to specify this parameter."
          }
        },
        {
          "name": "RegionId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "地域ID。",
            "en": "The region ID."
          }
        },
        {
          "name": "Tables",
          "position": "Query",
          "type": "RepeatList",
          "required": false,
          "description": {
            "zh": "需要分析的表名称的列表",
            "en": "The list of table names to analyze."
          }
        }
      ]
    },
    {
      "name": "Message",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "本次向Agent发送的消息内容",
        "en": "The message content to send to the agent."
      }
    },
    {
      "name": "MessageType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "消息类型，默认为 `[primary]`。  \n\n- 常规情况下，和Agent交互，消息类型为 `[primary]`\n\n- 当该消息为响应Agent的Human-in-Loop问题时，类型应当是`[additional]`\n\n- 当该消息是要触发一次报告绘制时，类型应当是 `[report]`\n\n- 当该消息是要取消当前会话时，类型应当是`[cancel]`。",
        "en": "The message type. Default value: `[primary]`.  \n\n- In normal cases, set the message type to `[primary]` when interacting with the agent.\n\n- When the message is a response to a human-in-the-loop question from the agent, set the type to `[additional]`.\n\n- When the message is intended to trigger report generation, set the type to `[report]`.\n\n- When the message is intended to cancel the current session, set the type to `[cancel]`."
      }
    },
    {
      "name": "ParentSessionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "主会话id",
        "en": "The parent session ID."
      }
    },
    {
      "name": "Question",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "当消息类型为`additional`时为必须字段，传入Agent通过Human-in-Loop向用户提问的具体问题。",
        "en": "The specific question that the agent asks the user through human-in-the-loop. This parameter is required when the message type is `additional`."
      }
    },
    {
      "name": "QuotedMessage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "传入当前引用内容，通常在与Agent交互时使用。",
        "en": "The quoted content. This parameter is typically used when interacting with the agent."
      }
    },
    {
      "name": "ReplyTo",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "**重要**\n\n当本消息是回复Agent消息（如：Agent通过 ASK_HUMAN进行问题澄清），则reply_to应当精确传入这条Agent消息携带的 Checkpoint 序号。若本消息无针对性的回复，如分析完成后要求Agent进一步深度分析，则 reply_to 可不填入或者填入\"0\"。  \n\n该字段会影响Agent决策如何处理该消息，若传递错误可能导致分析效果不及预期。",
        "en": "**Important**\n\nWhen this message is a reply to an agent message (for example, the agent asks a clarifying question through ASK_HUMAN), set reply_to to the exact Checkpoint sequence number carried in that agent message. If this message is not a targeted reply, such as requesting the agent to perform further in-depth analysis after analysis is complete, you can leave reply_to empty or set it to \"0\".  \n\nThis field affects how the agent decides to process the message. Passing an incorrect value may cause the analysis results to be less effective than expected."
      }
    },
    {
      "name": "SessionConfig",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "本次会话的特殊配置。同一个Session只有第一次SendMessage的时候带下来的配置才会生效。",
        "en": "The special configuration for this session. Only the configuration passed in the first SendMessage call within the same session takes effect."
      }
    },
    {
      "name": "SessionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "会话 ID，必须字段。您可以通过CreateAgentSession获取SessionId。",
        "en": "The session ID. This parameter is required. You can call the CreateAgentSession operation to obtain the session ID."
      }
    },
    {
      "name": "TaskConfig",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "仅影响本次任务的配置项。",
        "en": "The configuration items that affect only the current task."
      }
    },
    {
      "name": "UserOssBucket",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户OSS的Bucket，若不填，分析过程将安全存储在内置存储中。",
        "en": "The OSS bucket of the user. If this parameter is not specified, the analysis process is securely stored in built-in storage."
      }
    },
    {
      "name": "WorkspaceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "工作空间ID。",
        "en": "The workspace ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "发送聊天消息",
    "en": "Send chat message"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n- `agent_id` 和 `session_id` 是必填字段。\n- `message_type` 默认为 `primary`，当需要追加信息或取消会话时，请设置为 `additional` 或 `cancel`。\n- `reply_to` 字段用于表示这条消息是对哪条Agent消息的响应，默认值为 `0`。\n- 当 `message_type` 为 `additional` 时，`question` 字段是必须的。\n- `quoted_message` 可以用来引用用户之前的消息内容。\n- `data_source`, `dms_user`, `db_metadata`, `session_config` 等字段均为可选，但提供了更详细的上下文信息。",
    "en": "## Request description\n\n- `agent_id` and `session_id` are required fields.\n- `message_type` defaults to `primary`. Set it to `additional` or `cancel` when you need to append information or cancel a session.\n- The `reply_to` field indicates which agent message this message is responding to. The default value is `0`.\n- When `message_type` is `additional`, the `question` field is required.\n- `quoted_message` can be used to quote the content of a previous user message.\n- Fields such as `data_source`, `dms_user`, `db_metadata`, and `session_config` are optional but provide more detailed context information."
  }
}