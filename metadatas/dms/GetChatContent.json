{
  "name": "GetChatContent",
  "deprecated": false,
  "protocol": "HTTPS|SSE",
  "method": "POST",
  "pathPattern": "/api/v1/chat/stream",
  "parameters": [
    {
      "name": "AgentId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "AgentId，必填",
        "en": "The agent ID. This parameter is required."
      }
    },
    {
      "name": "Checkpoint",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "后端将从这个接口开始返回输出",
        "en": "The backend will return output starting from this checkpoint."
      }
    },
    {
      "name": "DMSUnit",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "当前所在的DMS单元，阿里云国内站用户默认填入cn-hangzhou。",
        "en": "The current DMS unit. Chinese mainland users of Alibaba Cloud should use cn-hangzhou by default."
      }
    },
    {
      "name": "SessionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "会话 ID，必须字段",
        "en": "The session ID. This parameter is required."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取聊天内容",
    "en": "GetChatContent"
  },
  "descriptions": {
    "zh": "## 请求说明\n- 响应将以SSE流的形式返回，其中每个事件都遵循`SSEEvent`模式，包含有关消息级别的元信息等。\n- 每个SSE事件中的`content`字段可能携带实际的消息文本或JSON对象，具体取决于`content_type`的值。",
    "en": "## Request Description\n- The response is returned as an SSE stream, where each event follows the `SSEEvent` schema and contains meta-information such as the message level.\n- The `content` field in each SSE event may carry actual message text or a JSON object, depending on the value of `content_type`."
  }
}