{
  "name": "DescribeVSwitches",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n- **true**：发送检查请求，不会创建资源（接口功能）。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作。",
        "en": "Specifies whether to perform a dry run, without performing the actual request. Valid values:\n\n*   **true**: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n*   **false** (default): performs a dry run and performs the actual request. If the request passes the dry run, a 2xx HTTP status code is returned and the operation is performed."
      }
    },
    {
      "name": "EnableIpv6",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否查询指定地域下开启IPv6网段的交换机，取值：\n\n- **true**：查询指定地域下开启IPv6网段的交换机。\n\n- **false**：不查询指定地域下开启IPv6网段的交换机。\n\n如果不传入该参数，系统默认查询指定地域下的所有交换机。",
        "en": "Specifies whether to query vSwitches with IPv6 enabled in the region. Valid values:\n\n*   **true**\n*   **false**\n\nIf you do not set this parameter, the system queries all vSwitches in the specified region by default."
      }
    },
    {
      "name": "IsDefault",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否查询指定地域下的默认交换机，取值： \n\n- **true**：查询指定地域下的默认交换机。  \n\n- **false**：不查询指定地域下的默认交换机。  \n\n如果不传入该参数，系统默认查询指定地域下的所有交换机。",
        "en": "Specifies whether to query the default vSwitches in the specified region. Valid values:\n\n*   **true**\n*   **false**\n\nIf you do not set this parameter, the system queries all vSwitches in the specified region by default."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "列表的页码，默认值为**1**。",
        "en": "The page number. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页的行数，最大值为**50**。默认值为**10**。",
        "en": "The number of entries per page. Maximum value: **50**. Default value: **10**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "交换机所属地域的ID。您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。\n\n> **RegionId**和**VpcId**参数至少输入一个。",
        "en": "The region ID of the vSwitch. You can call the [DescribeRegions](~~36063~~) operation to query the most recent region list.\n\n>  You must set at least one of **RegionId** and **VpcId**."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "交换机所属的资源组ID。",
        "en": "The ID of the resource group to which the vSwitch belongs."
      }
    },
    {
      "name": "RouteTableId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "路由表的ID。",
        "en": "The ID of the route table."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源的标签。",
        "en": "The tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。最多支持输入20个标签键。如需传入该值，则不能输入空字符串。\n\n一个标签键最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag key. You can specify at most 20 tag keys. The tag key cannot be an empty string.\n\nThe tag key can be up to 128 characters in length. It cannot start with `aliyun` or `acs:`, and cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。最多支持输入20个标签值。如需传入该值，可以输入空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag value. You can specify at most 20 tag values. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length, and cannot contain `http://` or `https://`. The tag value cannot start with `aliyun` or `acs:`."
          }
        }
      ]
    },
    {
      "name": "VSwitchId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的交换机的ID。",
        "en": "The ID of the vSwitch that you want to query."
      }
    },
    {
      "name": "VSwitchName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "交换机的名称。\n\n名称长度为1～128个字符，不能以`http://`或`https://`开头。",
        "en": "The exact name of the vSwitch that you want to query. Fuzzy match is not supported.\n\nThe name must be 1 to 128 characters in length, and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "VSwitchOwnerId",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "资源归属的阿里云账号ID。",
        "en": "The ID of the Alibaba Cloud account to which the vSwitch belongs."
      }
    },
    {
      "name": "VpcId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的交换机所属VPC的ID。 \n\n> **RegionId**和**VpcId**参数至少输入一个。",
        "en": "The ID of the virtual private cloud (VPC) to which the vSwitches belong.\n\n>  You must set at least one of **RegionId** and **VpcId**."
      }
    },
    {
      "name": "ZoneId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "交换机所属可用区的ID。您可以通过调用[DescribeZones](~~36064~~)接口获取可用区ID。",
        "en": "The ID of the zone to which the vSwitches belong. You can call the [DescribeZones](~~36064~~) operation to query the most recent zone list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询可组网信息",
    "en": "DescribeVSwitches"
  },
  "example": {
    "unifiedCli": "aliyun vpc describe-vswitches --vpc-id vpc-25cdvfeq58pl0000 --vswitch-id vsw-23dscddcffvf30000 --zone-id cn-hangzhou-d --biz-region-id cn-hangzhou --vswitch-name vSwitch --dry-run true --is-default true --route-table-id vtb-bp145q7glnuzdvzu20000 --resource-group-id rg-bp67acfmxazb4ph0000 --page-number 1 --page-size 10 --vswitch-owner-id 25460731706910000 --enable-ipv6 false --tag Value=FinanceJoshua Key=FinanceDept --region cn-hangzhou",
    "legacyCli": "aliyun vpc DescribeVSwitches --region cn-hangzhou --IsDefault true --Tag.1.Key FinanceDept --RouteTableId 'vtb-bp145q7glnuzdvzu20000' --ZoneId 'cn-hangzhou-d' --ResourceGroupId 'rg-bp67acfmxazb4ph0000' --PageSize 10 --VSwitchId 'vsw-23dscddcffvf30000' --DryRun true --EnableIpv6 false --VpcId 'vpc-25cdvfeq58pl0000' --PageNumber 1 --VSwitchOwnerId 25460731706910000 --Tag.1.Value FinanceJoshua --VSwitchName vSwitch --RegionId 'cn-hangzhou'"
  }
}