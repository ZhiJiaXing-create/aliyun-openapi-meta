{
  "name": "OpenTrafficMirrorService",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "流量镜像所属的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region to which the mirrored traffic belongs.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK",
    "APP",
    "PrivateKey",
    "BearerToken"
  ],
  "title": {
    "zh": "开通流量镜像功能",
    "en": "OpenTrafficMirrorService"
  },
  "descriptions": {
    "zh": "流量镜像功能是按照地域粒度开通，**OpenTrafficMirrorService**接口不支持在同一个地域内并发开通流量镜像功能。",
    "en": "流量镜像功能是按照地域粒度开通，**OpenTrafficMirrorService**接口不支持在同一个地域内并发开通流量镜像功能。"
  },
  "example": {
    "unifiedCli": "aliyun vpc open-traffic-mirror-service --client-token 123e4567-e89b-12d3-a456-426655442222 --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun vpc OpenTrafficMirrorService --region cn-hangzhou --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655442222'"
  }
}