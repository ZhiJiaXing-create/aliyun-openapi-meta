{
  "name": "DeleteRouteEntry",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DestinationCidrBlock",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "路由条目的目标网段，支持IPv4网段、IPv6网段、前缀列表网段和前缀列表的实例ID，和RouteEntryId参数互斥。\n> 当**RouteEntryId**参数未输入时，**DestinationCidrBlock**和**RouteTableId**参数必填，并根据需求配置**NextHopId**或**NextHopList**参数。",
        "en": "The destination CIDR block of the route. IPv4 CIDR blocks, IPv6 CIDR blocks, prefix list CIDR blocks, and prefix list instance IDs are supported. This parameter is mutually exclusive with the RouteEntryId parameter.\n> If the **RouteEntryId** parameter is not specified, the **DestinationCidrBlock** and **RouteTableId** parameters are required. Configure the **NextHopId** or **NextHopList** parameter as needed."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n**true**：发送请求，不会删除路由条目。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码`DryRunOperation`。\n\n**false**（默认值）：发送正常请求，通过检查后返回2xx HTTP状态码并直接删除路由条目。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n**true**: performs a dry run without deleting the route. The system checks the AccessKey pair, the authorization of the Resource Access Management (RAM) user, and the required parameters. If the check fails, the corresponding error is returned. If the check succeeds, the error code `DryRunOperation` is returned.\n\n**false** (default): sends a normal request. After the check succeeds, a 2xx HTTP status code is returned and the route is deleted."
      }
    },
    {
      "name": "NextHopId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "下一跳实例的ID。\n\n- 删除ECMP外的其他路由条目时，请配置**NextHopId**，无需配置**NextHopList**。\n- 删除ECMP路由条目时，请配置**NextHopList**，无需配置**NextHopId**。",
        "en": "The ID of the next hop instance.\n\n- To delete a non-ECMP route, specify **NextHopId**. Do not specify **NextHopList**.\n- To delete an ECMP route, specify **NextHopList**. Do not specify **NextHopId**."
      }
    },
    {
      "name": "NextHopList",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "ECMP路由的下一跳实例信息。最多支持16个下一跳实例信息。",
        "en": "The information about the next hop instances of the ECMP route. A maximum of 16 next hop instances are supported."
      },
      "sub_parameters": [
        {
          "name": "NextHopId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "ECMP路由条目的下一跳实例的ID。最多支持16个下一跳实例信息。",
            "en": "The ID of the next hop instance of the ECMP route. A maximum of 16 next hop instances are supported."
          }
        },
        {
          "name": "NextHopType",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "ECMP路由的下一跳的类型，目前只能取值**RouterInterface**（路由器接口）。最多支持16个下一跳实例信息。",
            "en": "The type of the next hop of the ECMP route. Set the value to **RouterInterface** (router interface). A maximum of 16 next hop instances are supported."
          }
        }
      ]
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "路由表所属地域的ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region where the route table resides.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the region ID."
      }
    },
    {
      "name": "RouteEntryId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要删除的路由条目ID，和DestinationCidrBlock参数互斥。\n> 当**DestinationCidrBlock**参数未输入时，**RouteEntryId**参数必填。",
        "en": "The ID of the route that you want to delete. This parameter is mutually exclusive with the DestinationCidrBlock parameter.\n> If the **DestinationCidrBlock** parameter is not specified, the **RouteEntryId** parameter is required."
      }
    },
    {
      "name": "RouteTableId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "路由条目所在的路由表的ID。  \n> 当**RouteEntryId**参数未输入时，**DestinationCidrBlock**和**RouteTableId**参数必填，并根据需求配置**NextHopId**或**NextHopList**参数。",
        "en": "The ID of the route table that contains the route.  \n> If the **RouteEntryId** parameter is not specified, the **DestinationCidrBlock** and **RouteTableId** parameters are required. Configure the **NextHopId** or **NextHopList** parameter as needed."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除自定义路由条目",
    "en": "Delete a custom route entry"
  },
  "descriptions": {
    "zh": "调用本接口删除路由条目时，请注意： \n\n- 只有处于**Available**状态的路由条目可以被删除。  \n\n- 路由表所在的VPC正在进行创建或删除交换机或路由条目时，无法删除路由条目。 \n\n- 如果需要删除边界路由器的路由表中的路由条目，请通过[DescribeRouteEntryList](~~138148~~)接口查询要删除的路由条目的**NextHopId** 后，再通过本接口删除路由条目。\n\n- **DeleteRouteEntry**接口属于异步接口，即系统会返回一个请求ID，但该路由条目尚未删除成功，系统后台的删除任务仍在进行。您可以调用[DescribeRouteEntryList](~~138148~~)查询路由条目的删除状态：\n    - 当路由条目处于**Deleting**状态时，表示路由条目正在删除中。\n    - 当查询不到指定的路由条目时，表示路由条目删除成功。\n- **DeleteRouteEntry**接口不支持在同一个VPC或者同一个VBR中并发删除路由表中的路由条目。",
    "en": "When you call this operation to delete a route, take note of the following items:\n\n- Only routes in the **Available** state can be deleted.\n\n- A route cannot be deleted if the VPC that contains the route table is creating or deleting a vSwitch or route.\n\n- To delete a route from the route table of a VBR, call the [DescribeRouteEntryList](~~138148~~) operation to query the **NextHopId** of the route that you want to delete, and then call this operation to delete the route.\n\n- The **DeleteRouteEntry** operation is asynchronous. After you send a request, the system returns a request ID, but the route has not been deleted yet. The deletion task runs in the background. You can call [DescribeRouteEntryList](~~138148~~) to query the deletion status of the route:\n    - If the route is in the **Deleting** state, the route is being deleted.\n    - If the specified route cannot be found, the route is deleted.\n- The **DeleteRouteEntry** operation does not support concurrent deletion of routes from the route table within the same VPC or VBR."
  },
  "example": {
    "unifiedCli": "aliyun vpc delete-route-entry --biz-region-id cn-hangzhou --route-table-id vtb-2ze3jgygk9bmsj23s0000 --route-entry-id rte-bp1mnnr2al0naomnpv0000 --destination-cidr-block 47.100.XX.XX/16 --next-hop-id ri-2zeo3xzyf38r4urzd0000 --next-hop-list NextHopType=RouterInterface NextHopId=ri-2zeo3xzyf38r43cd0000 --dry-run true --region cn-hangzhou",
    "legacyCli": "aliyun vpc DeleteRouteEntry --region cn-hangzhou --RouteEntryId 'rte-bp1mnnr2al0naomnpv0000' --RouteTableId 'vtb-2ze3jgygk9bmsj23s0000' --NextHopList.1.NextHopId 'ri-2zeo3xzyf38r43cd0000' --NextHopId 'ri-2zeo3xzyf38r4urzd0000' --DryRun true --NextHopList.1.NextHopType RouterInterface --RegionId 'cn-hangzhou' --DestinationCidrBlock '47.100.XX.XX/16'"
  }
}