{
  "name": "DeleteIPv6Translator",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters."
      }
    },
    {
      "name": "Ipv6TranslatorId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPv6转换服务实例ID。",
        "en": "The ID of the IPv6 Translation Service instance."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPv6转换服务实例的地域ID。",
        "en": "The region ID of the IPv6 Translation Service instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除IPv6转换服务实例",
    "en": "DeleteIPv6Translator"
  },
  "example": {
    "unifiedCli": "aliyun vpc delete-ipv6-translator --client-token ClientToken --biz-region-id cn-hangzhou --ipv6-translator-id ipv6trans-bp1i8ahxut1ie0000 --region cn-hangzhou",
    "legacyCli": "aliyun vpc DeleteIPv6Translator --region cn-hangzhou --RegionId 'cn-hangzhou' --Ipv6TranslatorId 'ipv6trans-bp1i8ahxut1ie0000' --ClientToken ClientToken"
  }
}