{
  "name": "ListNatIpCidrs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the value, but you must make sure that it is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.\n\n>  If you do not set this parameter, the system automatically uses **RequestId** as **ClientToken**. **RequestId** may be different for each API request."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n- **true**：发送检查请求，不会查询NAT IP地址段列表。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码DryRunOperation。\n\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并查询NAT IP地址段列表。",
        "en": "Specifies whether to only precheck this request. Valid values:\n\n*   **true**: checks the API request. The CIDR blocks of the NAT gateway are not queried if the API request passes the precheck. The system checks whether your AccessKey pair is valid, whether the Resource Access Management (RAM) user is authorized, and whether the required parameters are set. If the request fails to pass the precheck, the corresponding error message is returned. If the check succeeds, the DryRunOperation error code is returned.\n*   **false**: sends the API request. If the request passes the precheck, 2xx HTTP status code is returned and the CIDR blocks of the NAT gateway are queried. This is the default value."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分批次查询时每次显示的条目数。取值范围：**1**~**100**，默认值为**20**。",
        "en": "The number of entries to return on each page. Valid values: **1** to **100**. Default value: **20**."
      }
    },
    {
      "name": "NatGatewayId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要查询NAT IP地址段的VPC NAT网关的实例ID。",
        "en": "The ID of the VPC NAT gateway that you want to query."
      }
    },
    {
      "name": "NatIpCidr",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的NAT IP地址段。",
        "en": "The CIDR block of the NAT gateway that you want to query."
      }
    },
    {
      "name": "NatIpCidrName",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "要查询的NAT IP地址段的名称。**N**取值为**1**~**20**。",
        "en": "The name of the CIDR block that you want to query. Valid values of **N**: **1** to **20**."
      }
    },
    {
      "name": "NatIpCidrStatus",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的NAT IP地址段的状态。取值：**Available**，表示可用状态。",
        "en": "The status of the CIDR block that you want to query. Set the value to **Available**."
      }
    },
    {
      "name": "NatIpCidrs",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "要查询的NAT IP地址段。**N**取值为**1**~**20**。",
        "en": "The CIDR block of the NAT gateway that you want to query. Valid values of **N**: **1** to **20**."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否拥有下一次查询的令牌（Token）。取值：\n- 第一次查询和没有下一次查询时，均无需填写。\n- 如果有下一次查询，取值为上一次API调用返回的NextToken值。",
        "en": "The token that is used for the next query. Set the value as needed.\n\n*   If this is your first query or no next query is to be sent, ignore this parameter.\n*   If a next query is to be sent, set the value to the value of NextToken that is returned from the last call."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要查询的NAT IP地址段所属VPC NAT网关的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the Virtual Private Cloud (VPC) NAT gateway that you want to query.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询NAT IP地址段列表",
    "en": "ListNatIpCidrs"
  },
  "example": {
    "unifiedCli": "aliyun vpc list-nat-ip-cidrs --biz-region-id cn-hangzhou --nat-gateway-id ngw-gw8v16wgvtq26vh590000 --nat-ip-cidr 172.16.0.0/24 --dry-run false --client-token 5A2CFF0E-5718-45B5-9D4D-70B3FF3898 --nat-ip-cidr-status Available --next-token caeba0bbb2be03f84eb48b699f0a4883 --max-results 20 --nat-ip-cidr-name cidrname --nat-ip-cidrs 172.16.0.0/24 --region cn-hangzhou",
    "legacyCli": "aliyun vpc ListNatIpCidrs --region cn-hangzhou --NatIpCidrs.1 '172.16.0.0/24' --NextToken caeba0bbb2be03f84eb48b699f0a4883 --NatIpCidr '172.16.0.0/24' --MaxResults 20 --DryRun false --NatIpCidrName.1 cidrname --RegionId 'cn-hangzhou' --ClientToken '5A2CFF0E-5718-45B5-9D4D-70B3FF3898' --NatGatewayId 'ngw-gw8v16wgvtq26vh590000' --NatIpCidrStatus Available"
  }
}