{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回值语言，取值： \n          \n- **zh-CN**（默认值）：中文。\n\n- **en-US**：英文。",
        "en": "The language of the response. Valid values: \n          \n- **zh-CN** (default): Chinese.\n\n- **en-US**: English."
      }
    },
    {
      "name": "ProductType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "产品类型，默认值为**VPC**。",
        "en": "The product type. Default value: **VPC**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询可以使用的阿里云地域",
    "en": "Query available Alibaba Cloud regions"
  },
  "example": {
    "unifiedCli": "aliyun vpc describe-regions --product-type VPC --accept-language zh-CN --region cn-hangzhou",
    "legacyCli": "aliyun vpc DescribeRegions --region cn-hangzhou --ProductType VPC --AcceptLanguage 'zh-CN'"
  }
}