{
  "name": "CreateFullNatEntry",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccessDomain",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "FULLNAT 地址转换的后端域名。",
        "en": "The backend domain name for FULLNAT address translation."
      }
    },
    {
      "name": "AccessIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "FULLNAT地址转换的后端IP地址。",
        "en": "The backend IP address to be modified in FULLNAT address translation."
      }
    },
    {
      "name": "AccessPort",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "FULLNAT端口映射的后端端口，取值范围：**1**~**65535**。",
        "en": "The backend port to be modified in the mapping of FULLNAT port. Valid values: **1** to **65535**."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 如果未配置该参数，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate a value, and you must make sure that each request has a unique token value. The client token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system automatically uses the value of **RequestId** as the value of **ClientToken**. The **request ID** may be different for each request."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n- **true**：发送检查请求，不会添加FULLNAT条目。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后返回2xx HTTP状态码并添加FULLNAT条目。",
        "en": "Specifies whether to only precheck this request. Valid values:\n\n*   **true**: prechecks the request without adding the FULLNAT entry. The system checks whether your AccessKey pair is valid, whether RAM users are granted required permissions, and whether the required parameters are set. If the request fails to pass the precheck, an error code is returned. If the request passes the precheck, the `DryRunOperation` error code is returned.\n*   **false**: sends the API request. This is the default value. After the request passes the precheck, a 2XX HTTP status code is returned and the FULLNAT entry is added."
      }
    },
    {
      "name": "FullNatEntryDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "FULLNAT条目的描述信息。\n\n描述可以为空；或填写2~256个字符，不能以`http://`或`https://`开头。",
        "en": "The description of the FULLNAT entry.\n\nThis parameter is optional. If you enter a description, the description must be 2 to 256 characters in length, and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "FullNatEntryName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "FULLNAT条目的名称。 长度为2~128个字符，必须以大小写字母或中文开头，但不能以http://或https://开头。",
        "en": "The FULLNAT entry name. The name must be 2 to 128 characters in length. It must start with a letter but cannot start with http:// or https://."
      }
    },
    {
      "name": "FullNatTableId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "FullNAT条目所属的FullNAT列表的ID。",
        "en": "The ID of the FULLNAT table to which the FULLNAT entry belongs."
      }
    },
    {
      "name": "IpProtocol",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "转发端口的协议类型，取值：\n\n-  **TCP**：转发TCP协议的报文。 \n-  **UDP**：转发UDP协议的报文。",
        "en": "The protocol of the packets that are forwarded by the port. Valid values:\n\n*   **TCP**\n*   **UDP**"
      }
    },
    {
      "name": "NatIp",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "提供地址转换的NAT IP地址。",
        "en": "The NAT IP address that provides address translation."
      }
    },
    {
      "name": "NatIpPort",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "FULLNAT端口映射的前端端口，取值范围：**1**~**65535**。\n\n> 如果未配置该参数，系统将随机分配一个可用端口。",
        "en": "The frontend port to be modified in the mapping of FULLNAT port. Valid values: **1** to **65535**."
      }
    },
    {
      "name": "NetworkInterfaceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "弹性网卡ID。",
        "en": "The elastic network interface (ENI) ID."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "FULLNAT条目的VPC NAT网关所在地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the Virtual Private Cloud (VPC) NAT gateway to which the FULLNAT entry to be added belongs.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "添加FULLNAT条目",
    "en": "CreateFullNatEntry"
  },
  "descriptions": {
    "zh": "**CreateFullNatEntry**接口属于异步接口，即系统会先返回一个FULLNAT条目，但该FULLNAT条目并未添加完成，系统后台的添加任务仍在进行。您可以调用[ListFullNatEntries](~~348779~~)查询FULLNAT条目添加的状态：\n- 当FULLNAT条目处于**Creating**状态时，表示FULLlNAT条目正在添加中，在该状态下，您只能执行查询操作，不能执行其他操作。\n\n- 当FULLNAT条目处于**Available**状态时，表示FULLlNAT条目已添加完成。",
    "en": "*   **CreateFullNatEntry** is an asynchronous operation. After you send a request to call this operation, the system returns a FULLNAT entry and the FULLNAT entry is being added in the backend. You can call the [ListFullNatEntries](~~348779~~) operation to query the status of a FULLNAT entry.\n\n    *   If the FULLNAT entry is in the **Creating** state, the system is adding the FULLNAT entry. You can only query the status of the FULLNAT entry, but cannot perform other operations.\n    *   If the FULLNAT entry is in the **Available** state, the FULLNAT entry is added.\n\n*   You cannot repeatedly call the **CreateFullNatEntry** operation for the same VPN gateway within the specified period of time."
  },
  "example": {
    "unifiedCli": "aliyun vpc create-full-nat-entry --client-token 5A2CFF0E-5718-45B5-9D4D-70B3FF3898 --full-nat-table-id fulltb-gw88z7hhlv43rmb260000 --network-interface-id eni-gw8g131ef2dnbu3k0000 --nat-ip 192.168.XX.XX --nat-ip-port 80 --access-ip 192.168.XX.XX --access-port 80 --ip-protocol TCP --full-nat-entry-name test --full-nat-entry-description abc --dry-run false --biz-region-id cn-hangzhou --access-domain xxx.com --region cn-hangzhou",
    "legacyCli": "aliyun vpc CreateFullNatEntry --region cn-hangzhou --AccessIp '192.168.XX.XX' --FullNatTableId 'fulltb-gw88z7hhlv43rmb260000' --FullNatEntryName test --DryRun false --NatIpPort 80 --NetworkInterfaceId 'eni-gw8g131ef2dnbu3k0000' --NatIp '192.168.XX.XX' --FullNatEntryDescription abc --AccessDomain 'xxx.com' --IpProtocol TCP --AccessPort 80 --RegionId 'cn-hangzhou' --ClientToken '5A2CFF0E-5718-45B5-9D4D-70B3FF3898'"
  }
}