{
  "name": "DeleteFailoverTestJob",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request."
      }
    },
    {
      "name": "JobId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "故障演练任务ID。",
        "en": "The ID of the failover test."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "故障演练任务所在的地域ID。 \n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the failover test.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除高速通道故障演练任务",
    "en": "DeleteFailoverTestJob"
  },
  "descriptions": {
    "zh": "只支持删除处于**待演练**和**已完成**状态的故障演练任务。",
    "en": "You can delete only failover tests that are in the **Pending** or **Complete** state."
  },
  "example": {
    "unifiedCli": "aliyun vpc delete-failover-test-job --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-426655440000 --job-id ftj-bp1yh6mvi13aq3g8w0000 --region cn-hangzhou",
    "legacyCli": "aliyun vpc DeleteFailoverTestJob --region cn-hangzhou --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --JobId 'ftj-bp1yh6mvi13aq3g8w0000'"
  }
}