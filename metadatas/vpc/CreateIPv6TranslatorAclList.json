{
  "name": "CreateIPv6TranslatorAclList",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AclName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制策略组名称。",
        "en": "The ACL name."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPv6转换服务实例的地域。 您可以通过调用 DescribeRegions接口获取地域ID。",
        "en": "The region of the IPv6 Translation Service instance. You can call the DescribeRegions operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建访问控制策略组",
    "en": "CreateIPv6TranslatorAclList"
  },
  "example": {
    "unifiedCli": "aliyun vpc create-ipv6-translator-acl-list --biz-region-id cn-hangzhou --client-token sha123456 --acl-name test --region cn-hangzhou",
    "legacyCli": "aliyun vpc CreateIPv6TranslatorAclList --region cn-hangzhou --RegionId 'cn-hangzhou' --ClientToken sha123456 --AclName test"
  }
}