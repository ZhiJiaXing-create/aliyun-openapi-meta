{
  "name": "DeleteRouterInterface",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "路由器接口所在的地域。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region where the router interface is deployed.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list."
      }
    },
    {
      "name": "RouterInterfaceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "路由器接口的ID。",
        "en": "The ID of the router interface."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除路由器接口",
    "en": "DeleteRouterInterface"
  },
  "descriptions": {
    "zh": "调用本接口删除路由器接口时，请注意： \n\n- 只能删除处于**Idle**和**Inactive**状态的路由器接口。 \n- 删除路由器接口之前，需要删除所有指向它的自定义路由条目。",
    "en": "When you call this operation, take note of the following limits:\n\n*   You can delete only a router interface that is in the **Idle** or **Inactive** state.\n*   Before you delete a router interface, you must delete all custom route entries destined for the router interface."
  },
  "example": {
    "unifiedCli": "aliyun vpc delete-router-interface --biz-region-id cn-hangzhou --router-interface-id ri-2zeo3xzyf38r4urz0000 --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun vpc DeleteRouterInterface --region cn-hangzhou --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --RouterInterfaceId 'ri-2zeo3xzyf38r4urz0000'"
  }
}