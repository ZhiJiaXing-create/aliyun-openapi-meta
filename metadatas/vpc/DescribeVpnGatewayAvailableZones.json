{
  "name": "DescribeVpnGatewayAvailableZones",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回结果显示的语言。取值：\n\n- **zh-CN**：中文。\n- **en-US**（默认值）：英文。",
        "en": "The language in which the returned results are displayed. Valid values:\n\n- **zh-CN**: Chinese.\n- **en-US** (default): English."
      }
    },
    {
      "name": "GatewayType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "VPN 网关类型，取值：\n- **Traditional**：返回可以创建传统型 VPN 网关的可用区信息。\n- **Enhanced.SiteToSite**：返回可以创建增强型站点入云 VPN网关的可用区信息。\n- **默认值**：返回可以创建所有类型VPN网关的可用区信息。",
        "en": "The VPN gateway type. Valid values:\n- **Traditional**: Returns zone information for creating traditional VPN gateways.\n- **Enhanced.SiteToSite**: Returns zone information for creating enhanced site-to-cloud VPN gateways.\n- **Default value**: Returns zone information for creating all types of VPN gateways."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "Spec",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "带宽规格。\n- 对于IPsec连接绑定VPN网关实例的场景，该参数表示VPN网关实例的带宽规格。\n- 对于IPsec连接绑定转发路由器的场景，该参数表示您期望的IPsec连接可以支持的带宽规格。\n\n不同的带宽规格可能会影响查询出的可用区信息。取值：\n\n- **5M**\n- **10M**\n- **20M**\n- **50M**\n- **100M**\n- **200M**\n- **500M**\n- **1000M**",
        "en": "The bandwidth specification.\n- If the IPsec-VPN connection is associated with a VPN gateway instance, this parameter specifies the bandwidth specification of the VPN gateway instance.\n- If the IPsec-VPN connection is associated with a transit router, this parameter specifies the expected bandwidth specification that the IPsec-VPN connection can support.\n\nDifferent bandwidth specifications may affect the zone information returned. Valid values:\n\n- **5M**\n- **10M**\n- **20M**\n- **50M**\n- **100M**\n- **200M**\n- **500M**\n- **1000M**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询指定地域支持部署IPsec连接的可用区列表",
    "en": "Query zones that support iPsec-VPN connection deployment in a specified region"
  },
  "example": {
    "unifiedCli": "aliyun vpc describe-vpn-gateway-available-zones --biz-region-id cn-hangzhou --spec 5M --accept-language zh-CN --gateway-type Traditional --region cn-hangzhou",
    "legacyCli": "aliyun vpc DescribeVpnGatewayAvailableZones --region cn-hangzhou --AcceptLanguage 'zh-CN' --GatewayType Traditional --RegionId 'cn-hangzhou' --Spec 5M"
  }
}