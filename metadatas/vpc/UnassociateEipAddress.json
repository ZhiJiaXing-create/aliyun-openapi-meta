{
  "name": "UnassociateEipAddress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllocationId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要解绑的EIP的ID。",
        "en": "The ID of the EIP to disassociate."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** as the **ClientToken**. The **RequestId** may be different for each API request."
      }
    },
    {
      "name": "Force",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "当EIP绑定了NAT网关，且NAT网关添加了DNAT或SNAT条目时，是否强制解绑EIP，取值：\n\n- **false**（默认值）：不强制解绑EIP。\n\n- **true**：强制解绑EIP。",
        "en": "Specifies whether to forcefully disassociate the EIP when the EIP is associated with a NAT gateway that has DNAT or SNAT entries configured. Valid values:\n\n- **false** (default): does not forcefully disassociate the EIP.\n\n- **true**: forcefully disassociates the EIP."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要解绑EIP的云产品实例的ID。\n支持输入NAT网关实例ID、传统型负载均衡CLB实例ID、云服务器ECS实例ID、辅助弹性网卡实例ID、高可用虚拟IP实例ID、IP地址。",
        "en": "The ID of the cloud resource instance from which to disassociate the EIP.\nYou can enter the instance ID of a NAT gateway, Classic Load Balancer (CLB) instance, ECS instance, secondary elastic network interface controller (NIC) instance, high-availability virtual IP address instance, or an IP address."
      }
    },
    {
      "name": "InstanceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要解绑EIP的云产品类型，取值： \n\n- **EcsInstance**（默认值）：专有网络类型的ECS实例。   \n\n- **SlbInstance**：专有网络类型的SLB实例。   \n\n- **NetworkInterface**：专有网络类型的辅助弹性网卡。\n\n- **Nat**：NAT网关。  \n\n- **HaVip**：高可用虚拟IP。\n- **IpAddress**：IP 地址。",
        "en": "The type of the cloud resource from which to disassociate the EIP. Valid values: \n\n- **EcsInstance** (default): an ECS instance in a virtual private cloud (VPC).   \n\n- **SlbInstance**: an SLB instance in a virtual private cloud (VPC).   \n\n- **NetworkInterface**: a secondary elastic network interface controller (NIC) in a virtual private cloud (VPC).\n\n- **Nat**: a NAT gateway.  \n\n- **HaVip**: a high-availability virtual IP address.\n- **IpAddress**: an IP address."
      }
    },
    {
      "name": "PrivateIpAddress",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要解绑EIP的ECS实例或辅助弹性网卡实例的私网IP地址。",
        "en": "The private IP address of the ECS instance or secondary elastic network interface controller (NIC) instance from which to disassociate the EIP."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要解绑的EIP的地域ID。\n 您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the EIP to disassociate.\n You can call [DescribeRegions](~~36063~~) to query the region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "将EIP从云产品解绑",
    "en": "Disassociate an EIP from a cloud resource"
  },
  "descriptions": {
    "zh": "-  **UnassociateEipAddress**接口属于异步接口，即系统会先返回一个请求ID，但EIP并未完成解绑，系统后台的解绑任务仍在进行。您可以调用[DescribeEipAddresses](~~120193~~)查询EIP的状态： \n    - 当EIP处于**Unassociating**状态时，表示EIP正在解绑中，在该状态下，您只能执行查询操作，不能执行其他操作。\n    - 当EIP处于**Available**时，表示EIP完成解绑。\n- **UnassociateEipAddress**接口不支持并发解绑同一个EIP。",
    "en": "-  The **UnassociateEipAddress** operation is asynchronous. After you call this operation, the system returns a request ID, but the EIP is not immediately disassociated. The disassociation task runs in the background. You can call [DescribeEipAddresses](~~120193~~) to query the status of the EIP: \n    - If the EIP is in the **Unassociating** state, the EIP is being disassociated. In this state, you can only perform query operations.\n    - If the EIP is in the **Available** state, the EIP is disassociated.\n- The **UnassociateEipAddress** operation does not support concurrent disassociation of the same EIP."
  },
  "example": {
    "unifiedCli": "aliyun vpc unassociate-eip-address --biz-force false --biz-region-id cn-hangzhou --allocation-id eip-2zeerraiwb7uj6i0d0000 --instance-id i-hp3akk9irtd69jad0000 --instance-type EcsInstance --private-ip-address 192.XX.XX.2 --client-token 02fb3da4-130e-110000 --region cn-hangzhou",
    "legacyCli": "aliyun vpc UnassociateEipAddress --region cn-hangzhou --PrivateIpAddress '192.XX.XX.2' --Force false --InstanceId 'i-hp3akk9irtd69jad0000' --AllocationId 'eip-2zeerraiwb7uj6i0d0000' --RegionId 'cn-hangzhou' --InstanceType EcsInstance --ClientToken '02fb3da4-130e-110000'"
  }
}