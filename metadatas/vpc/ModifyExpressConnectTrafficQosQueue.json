{
  "name": "ModifyExpressConnectTrafficQosQueue",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BandwidthPercent",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "QoS队列带宽百分比。\n* 当QoS队列类型为**Medium**的时候，此字段必须输入，取值范围为：1~100。\n* 当QoS队列类型为**Default**的时候，此字段为\"-\"。",
        "en": "The percentage of bandwidth allocated to the QoS queue.\n\n*   If QueueType is set to **Medium**, this parameter is required. Valid values: 1 to 100.\n*   If QueueType is set to **Default**, a value of - is returned."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the value, but you must ensure that the value is unique among all requests. The client token can contain only ASCII characters.\n\n>  If you do not set this parameter, the system uses **RequestId** as **ClientToken**. **RequestId** might be different for each API request."
      }
    },
    {
      "name": "QosId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "QoS策略ID。",
        "en": "The ID of the QoS policy."
      }
    },
    {
      "name": "QueueDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "QoS队列的描述信息。\n\n长度为0～256个字符，不能以`http://`或`https://`开头。",
        "en": "The description of the QoS queue.\n\nThe description must be 0 to 256 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "QueueId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "QoS队列ID。",
        "en": "The ID of the QoS queue."
      }
    },
    {
      "name": "QueueName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "QoS队列的名称。\n\n长度为0～128个字符，不能以`http://`或`https://`开头。",
        "en": "The name of the QoS queue.\n\nThe name must be 0 to 128 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "QoS策略所在的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the QoS policy.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改高速通道QoS队列",
    "en": "ModifyExpressConnectTrafficQosQueue"
  },
  "example": {
    "unifiedCli": "aliyun vpc modify-express-connect-traffic-qos-queue --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-426655440000 --queue-name qos-queue-test --queue-description qos-queue-test --qos-id qos-2giu0a6vd5x0mv0000 --queue-id qos-queue-9nyx2u7n71s2rc0000 --bandwidth-percent 100 --region cn-hangzhou",
    "legacyCli": "aliyun vpc ModifyExpressConnectTrafficQosQueue --region cn-hangzhou --QosId 'qos-2giu0a6vd5x0mv0000' --QueueId 'qos-queue-9nyx2u7n71s2rc0000' --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --BandwidthPercent 100 --QueueName 'qos-queue-test' --QueueDescription 'qos-queue-test'"
  }
}