{
  "name": "GrantInstanceToCen",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CenId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定要授权的云企业网实例的ID。",
        "en": "The ID of the CEN instance to which you want to grant permissions."
      }
    },
    {
      "name": "CenOwnerId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "指定要授权的云企业网实例所属账号的UID。",
        "en": "The user ID (UID) of the Alibaba Cloud account to which the CEN instance belongs."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网络实例的ID。",
        "en": "The ID of the network instance."
      }
    },
    {
      "name": "InstanceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网络实例的类型，取值：\n\n- **VPC**：专有网络。\n\n- **VBR**：边界路由器。",
        "en": "The type of the network instance. Valid values:\n\n*   **VPC**\n*   **VBR**"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网络实例所在的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region where the network instance is deployed.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为云企业网实例授权",
    "en": "GrantInstanceToCen"
  },
  "descriptions": {
    "zh": "- 在加载其他阿里云账号（主账号）的网络实例前，需要在网络实例所在的阿里云账号下为云企业网实例授权。\n\n>**GrantInstanceToCen**接口是VPC的API，所以调用该接口需要使用`vpc.aliyuncs.com`域名。API version为`2016-04-28`。 \n\n- **GrantInstanceToCen**接口不支持在同一个VPC、VBR或者CCN中并发为云企业网实例授权。",
    "en": "- 在加载其他阿里云账号（主账号）的网络实例前，需要在网络实例所在的阿里云账号下为云企业网实例授权。\n\n>**GrantInstanceToCen**接口是VPC的API，所以调用该接口需要使用`vpc.aliyuncs.com`域名。API version为`2016-04-28`。 \n\n- **GrantInstanceToCen**接口不支持在同一个VPC、VBR或者CCN中并发为云企业网实例授权。"
  },
  "example": {
    "unifiedCli": "aliyun vpc grant-instance-to-cen --biz-region-id cn-hangzhou --instance-id vpc-uf6o8d1dj8sjwxi6o0000 --instance-type VPC --cen-id cen-7qthudw0ll6jmc0000 --cen-owner-id 123456789 --client-token 0c593ea1-3bea-11e9-b96b-88e9fe637760 --region cn-hangzhou",
    "legacyCli": "aliyun vpc GrantInstanceToCen --region cn-hangzhou --InstanceId 'vpc-uf6o8d1dj8sjwxi6o0000' --CenOwnerId 123456789 --CenId 'cen-7qthudw0ll6jmc0000' --RegionId 'cn-hangzhou' --InstanceType VPC --ClientToken '0c593ea1-3bea-11e9-b96b-88e9fe637760'"
  }
}