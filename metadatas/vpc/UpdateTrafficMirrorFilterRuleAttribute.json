{
  "name": "UpdateTrafficMirrorFilterRuleAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the value, but you must make sure that it is unique among different requests. The client token can contain only ASCII characters.\n\n> If you do not set this parameter, the system uses **RequestId** as **ClientToken**. **RequestId** may be different for each API request."
      }
    },
    {
      "name": "DestinationCidrBlock",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要修改的入方向或出方向规则网络流量的目的地址网段。",
        "en": "The new destination CIDR block of the inbound or outbound traffic."
      }
    },
    {
      "name": "DestinationPortRange",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要修改的入方向或出方向规则网络流量的目的端口范围。\n\n> 当**Protocol**取值为**ICMP**时，不支持修改端口范围。",
        "en": "The new destination port range of the inbound or outbound traffic.\n\n> If you set **Protocol** to **ICMP**, you cannot change the port range."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n- **true**：发送检查请求，不会修改入方向和出方向规则的配置信息。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n\n- **false**（默认值）：发送正常请求，通过检查后返回2xx HTTP状态码并直接修改入方向和出方向规则的配置信息。",
        "en": "Specifies whether to check the request without performing the operation. Valid values:\n\n- **true**: only checks the API request. The configuration of the inbound or outbound rule is not modified. The system checks the required parameters, request syntax, and limits. If the request fails to pass the check, an error message is returned. If the request passes the precheck, the `DryRunOperation` error code is returned.\n\n- **false**: sends the request. This is the default value. If the request passes the check, a 2xx HTTP status code is returned and the configuration of the inbound or outbound rule is modified."
      }
    },
    {
      "name": "Priority",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "要修改的入方向或出方向规则的优先级。数字越小，优先级越高。",
        "en": "The new priority of the inbound or outbound rule. A smaller value indicates a higher priority."
      }
    },
    {
      "name": "Protocol",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要修改的入方向或出方向规则需要镜像的网络流量使用的协议类型，取值：\n\n- **ALL**：所有协议。\n\n- **ICMP**：网络控制报文协议。\n\n- **TCP**：传输控制协议。\n\n- **UDP**：用户数据报协议。",
        "en": "The new protocol that is used by the traffic to be mirrored by the inbound or outbound rule. Valid values:\n\n- **ALL**: all protocols\n\n- **ICMP**: Internet Control Message Protocol (ICMP)\n\n- **TCP**: TCP\n\n- **UDP**: User Datagram Protocol (UDP)"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "流量镜像的所属地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。关于流量镜像支持的地域，请参见[流量镜像概述](~~207513~~)。",
        "en": "The ID of the region to which the mirrored traffic belongs.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list. For more information about regions that support traffic mirroring, see [Overview of traffic mirroring](~~207513~~)."
      }
    },
    {
      "name": "RuleAction",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要修改的入方向或出方向规则的采集策略，取值：\n\n- **accept**：采集网络流量。\n\n- **drop**：不采集网络流量。",
        "en": "The new action of the inbound or outbound rule. Valid values:\n\n- **accept**: accepts network traffic.\n\n- **drop**: drops network traffic."
      }
    },
    {
      "name": "SourceCidrBlock",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要修改的入方向或出方向规则网络流量的源地址网段。",
        "en": "The new source CIDR block of the inbound or outbound traffic."
      }
    },
    {
      "name": "SourcePortRange",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要修改的入方向或出方向规则网络流量的源端口范围。\n> 当**Protocol**取值为**ICMP**时，不支持修改端口范围。",
        "en": "The new source port range of the inbound or outbound traffic.\n\n> If you set **Protocol** to **ICMP**, you cannot change the port range."
      }
    },
    {
      "name": "TrafficMirrorFilterRuleId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "流量镜像入方向或出方向规则的实例ID。",
        "en": "The ID of the inbound or outbound rule."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改流量镜像入方向或出方向规则的配置信息",
    "en": "UpdateTrafficMirrorFilterRuleAttribute"
  },
  "descriptions": {
    "zh": "- **UpdateTrafficMirrorFilterRuleAttribute**接口属于异步接口，即系统返回一个请求ID，但该流量镜像入方向或出方向规则的配置信息尚未修改成功，系统后台的修改任务仍在进行。您可以调用[ListTrafficMirrorFilters](~~261353~~)查询流量镜像入方向或出方向规则的状态：\n    - 当流量镜像入方向或出方向规则的状态处于**Modifying**状态时，表示流量镜像入方向或出方向规则的配置信息正在修改中。\n    - 当流量镜像入方向或出方向规则的状态处于**Created**状态时，表示流量镜像入方向或出方向规则的配置信息修改成功。\n- **UpdateTrafficMirrorFilterRuleAttribute**接口不支持并发修改同一个流量镜像入方向或出方向规则的配置信息。",
    "en": "- The **UpdateTrafficMirrorFilterRuleAttribute** operation is asynchronous. After you send the request, the system returns a request ID. However, the operation is still being performed in the system background. You can call the [ListTrafficMirrorFilters](~~261353~~) operation to query the status of an inbound or outbound rule:\n\n  - If the rule is in the **Modifying** state, the rule is being modified.\n\n  - If the rule is in the **Created** state, the rule is modified.\n\n- You cannot repeatedly call the **UpdateTrafficMirrorFilterRuleAttribute** operation to modify an inbound or outbound rule within the specified period of time."
  },
  "example": {
    "unifiedCli": "aliyun vpc update-traffic-mirror-filter-rule-attribute --traffic-mirror-filter-rule-id tmr-j6c89rzmtd3hhdugq0000 --client-token 0c593ea1-3bea-11e9-b96b-88e9fe637760 --dry-run false --priority 1 --protocol ICMP --rule-action accept --destination-cidr-block 10.0.0.0/24 --source-cidr-block 0.0.0.0/0 --destination-port-range -1/-1 --source-port-range 22/40 --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun vpc UpdateTrafficMirrorFilterRuleAttribute --region cn-hangzhou --TrafficMirrorFilterRuleId 'tmr-j6c89rzmtd3hhdugq0000' --DestinationPortRange '-1/-1' --SourcePortRange '22/40' --Priority 1 --RuleAction accept --DryRun false --SourceCidrBlock '0.0.0.0/0' --DestinationCidrBlock '10.0.0.0/24' --RegionId 'cn-hangzhou' --ClientToken '0c593ea1-3bea-11e9-b96b-88e9fe637760' --Protocol ICMP"
  }
}