{
  "name": "EnablePhysicalConnection",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ByPassSp",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "表示是否跳过SP（即订单生命周期），取值：\n- **true**：跳过SP，说明阿里云售卖系统不再管理该实例，可免费使用。\n- **false**（默认值）：不跳过SP。\n\n> 该功能需要向客户经理申请使用。",
        "en": "Specifies whether to bypass the service provider (SP) subscription lifecycle. Valid values:\n\n- **true**: Bypasses the SP subscription lifecycle. This means the instance is no longer managed by the Alibaba Cloud subscription system.\n\n- **false** (default): Does not bypass the SP subscription lifecycle.\n\n> To use this feature, contact your account manager."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。",
        "en": "A client-generated token that is used to ensure the idempotence of the request.\n\nThe token must be unique across requests. The token can contain a maximum of 64 ASCII characters."
      }
    },
    {
      "name": "PhysicalConnectionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "物理专线的ID。",
        "en": "The ID of the physical connection."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "物理专线所在的地域。\n\n您可以通过调用DescribeRegions接口获取地域ID。",
        "en": "The ID of the region where the physical connection is located.\n\nYou can call `DescribeRegions` to get the latest region IDs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "开通物理专线",
    "en": "EnablePhysicalConnection"
  },
  "descriptions": {
    "zh": "调用该接口时，请注意：\n\n- 只允许开通处于**Confirmed**状态的物理专线。\n\n- 开通完成后物理专线进入**Enabled**状态。\n\n- **EnablePhysicalConnection**接口属于异步接口，即系统会返回一个实例ID，但该物理专线尚未开通完成，系统后台的开通任务仍在进行。您可以调用[DescribePhysicalConnections](~~2982519~~)查询物理专线的开通状态。\n\n\n- **EnablePhysicalConnection**接口不支持并发开通同一个处于**Confirmed**状态的物理专线。",
    "en": "Note the following when you call this API:\n\n- You can enable only a physical connection that is in the **Confirmed** state.\n\n- After the physical connection is enabled, it enters the **Enabled** state.\n\n- This is an asynchronous API call. The system returns a request ID immediately, but the connection is enabled in the background. You can call [DescribePhysicalConnections](~~2982519~~) to query the connection status.\n\n- Do not call **EnablePhysicalConnection** concurrently on the same physical connection in the **Confirmed** state."
  },
  "example": {
    "unifiedCli": "aliyun vpc enable-physical-connection --biz-region-id cn-hangzhou --physical-connection-id pc-2zeoaxkq3x0000 --client-token 02fb3da4-130e-11e9-8e44-0016e04115b --by-pass-sp false --region cn-hangzhou",
    "legacyCli": "aliyun vpc EnablePhysicalConnection --region cn-hangzhou --ByPassSp false --PhysicalConnectionId 'pc-2zeoaxkq3x0000' --RegionId 'cn-hangzhou' --ClientToken '02fb3da4-130e-11e9-8e44-0016e04115b'"
  }
}