{
  "name": "DeleteEnhancedVpnGateway",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。<br>\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过 64 个 ASCII 字符。\n> 若您未指定，则系统自动使用 API 请求的 **RequestId** 作为 **ClientToken** 标识。每次 API 请求的 **RequestId** 不一样。",
        "en": "The client token used to ensure the idempotence of the request.<br> You can generate this token by using your client, but you must ensure that it is unique for each request. The token can contain only ASCII characters and must not exceed 64 characters in length.<br>\n\n> If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "增强型 VPN 网关实例所在的地域ID。 \n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region where the enhanced VPN gateway is deployed.\n\nYou can call the [DescribeRegions](~~36063~~) operation to obtain the region ID."
      }
    },
    {
      "name": "VpnGatewayId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "增强型 VPN 网关实例的 ID。",
        "en": "The ID of the enhanced VPN gateway."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除指定的增强型VPN网关",
    "en": "Delete a specified enhanced VPN Gateway"
  },
  "descriptions": {
    "zh": "> 无法删除已有 IPsec 连接的增强型 VPN 网关。",
    "en": "> You cannot delete an enhanced VPN gateway that has an existing IPsec connection."
  },
  "example": {
    "unifiedCli": "aliyun vpc delete-enhanced-vpn-gateway --biz-region-id cn-hangzhou --client-token 02fb3da4-130e-11e9-8e44-0016e04115b --vpn-gateway-id vpn-bp1q8bgx4xnkm2ogj0000 --region cn-hangzhou",
    "legacyCli": "aliyun vpc DeleteEnhancedVpnGateway --region cn-hangzhou --VpnGatewayId 'vpn-bp1q8bgx4xnkm2ogj0000' --RegionId 'cn-hangzhou' --ClientToken '02fb3da4-130e-11e9-8e44-0016e04115b'"
  }
}