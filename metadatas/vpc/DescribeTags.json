{
  "name": "DescribeTags",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResult",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小，取值范围：1~50，默认值：50。",
        "en": "The number of entries per page. Valid values: 1 to 50. Default value: 50."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否拥有下一次查询的令牌（Token）。取值：\n- 第一次查询和没有下一次查询时，均无需填写。\n- 如果有下一次查询，取值为上一次API调用返回的NextToken值。",
        "en": "The pagination token. Valid values:\n- If this is the first query or no subsequent query exists, leave this parameter empty.\n- If a subsequent query exists, set this parameter to the NextToken value returned by the previous API call."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源所属的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the resource.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the region ID."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源ID。最多支持输入50个资源ID。",
        "en": "The resource ID. You can specify up to 50 resource IDs."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源类型。取值：\n- **VPC**：表示资源类型为专有网络实例。\n- **VSWITCH**：表示资源类型为交换机实例。\n- **ROUTETABLE**：表示资源类型为路由表实例。\n- **EIP**：表示资源类型为弹性公网IP实例。\n- **VPNGATEWAY**：表示资源类型为VPN网关实例。\n- **NATGATEWAY**：表示资源类型为NAT网关实例。\n- **COMMONBANDWIDTHPACKAGE**：表示资源类型为共享带宽实例。\n- **PREFIXLIST**：表示资源类型为前缀列表实例。\n- **PUBLICIPADDRESSPOOL**：表示资源类型为IP地址池实例。\n- **IPV4GATEWAY**：表示资源类型为IPv4网关实例。\n- **IPV6GATEWAY**：表示资源类型为IPv6网关实例。\n- **NETWORKACL**：表示资源类型为网络ACL实例。\n- **TRAFFICMIRRORFILTER**：表示资源类型为流量镜像筛选条件实例。\n- **TRAFFICMIRRORSESSION**：表示资源类型为流量镜像会话实例。\n- **FLOWLOG**：表示资源类型为流日志实例。\n- **HAVIP**：表示资源类型为高可用虚拟IP实例。\n- **DHCPOPTIONSSET**：表示资源类型为DHCP选项集实例。\n- **GATEWAYENDPOINT**：表示资源类型为网关终端节点实例。\n\n> 输入的资源类型取值不区分大小写。",
        "en": "The resource type. Valid values:\n- **VPC**: virtual private cloud (VPC) instance.\n- **VSWITCH**: virtual switch instance.\n- **ROUTETABLE**: route table instance.\n- **EIP**: elastic IP address (EIP) instance.\n- **VPNGATEWAY**: VPN gateway instance.\n- **NATGATEWAY**: NAT gateway instance.\n- **COMMONBANDWIDTHPACKAGE**: Internet Shared Bandwidth instance.\n- **PREFIXLIST**: prefix list instance.\n- **PUBLICIPADDRESSPOOL**: IP address pool instance.\n- **IPV4GATEWAY**: IPv4 gateway instance.\n- **IPV6GATEWAY**: IPv6 gateway instance.\n- **NETWORKACL**: network ACL instance.\n- **TRAFFICMIRRORFILTER**: traffic mirror filter instance.\n- **TRAFFICMIRRORSESSION**: traffic mirror session instance.\n- **FLOWLOG**: flow log instance.\n- **HAVIP**: high-availability virtual IP address instance.\n- **DHCPOPTIONSSET**: DHCP options set instance.\n- **GATEWAYENDPOINT**: gateway endpoint instance.\n\n> The resource type value is case-insensitive."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源的标签集合。",
        "en": "The tags of the resource."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。最多支持输入20个标签键。\n\n一个标签键最多支持128个字符，不能是空字符串，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag key of the resource. You can specify up to 20 tag keys.\n\nA tag key can be up to 128 characters in length. It cannot be an empty string or start with `aliyun` or `acs:`, and cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。最多支持输入20个标签值。\n\n\n一个标签值最多支持128个字符，可以是空字符串，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag value of the resource. You can specify up to 20 tag values.\n\nA tag value can be up to 128 characters in length. It can be an empty string but cannot start with `aliyun` or `acs:`, and cannot contain `http://` or `https://`."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询满足筛选条件的标签列表",
    "en": "Query tags that meet filter conditions"
  },
  "descriptions": {
    "zh": "- 请求中至少指定参数**ResourceId.N**或**Tag.N**（**Tag.N.Key**与**Tag.N.Value**），以确定检索对象。\n\n- **Tag.N**是资源的标签，由一个键值对组成。仅指定**Tag.N.Key**时，则返回该标签键关联的所有标签值。仅指定**Tag.N.Value**会报错。\n\n- 如果您同时指定**Tag.N**和**ResourceId.N**筛选标签，则**ResourceId.N**须满足任意输入的标签键值对。\n\n- 如果您同时指定多个标签键值对，返回结果为所有满足任意标签值对的标签。",
    "en": "- Specify at least **ResourceId.N** or **Tag.N** (**Tag.N.Key** and **Tag.N.Value**) in the request to determine the objects to query.\n\n- **Tag.N** is a tag of a resource and consists of a key-value pair. If you specify only **Tag.N.Key**, all tag values associated with the tag key are returned. If you specify only **Tag.N.Value**, an error is returned.\n\n- If you specify both **Tag.N** and **ResourceId.N** to filter tags, **ResourceId.N** must match any of the specified tag key-value pairs.\n\n- If you specify multiple tag key-value pairs, the results contain all tags that match any of the tag key-value pairs."
  },
  "example": {
    "unifiedCli": "aliyun vpc describe-tags --resource-type VPC --biz-region-id cn-hangzhou --next-token FFmyTO70tTpLG6I3FmYAXGKPd0000 --max-result 50 --resource-id vpc-bp16qjewdsunr41m10000 --tag Value=FinanceJoshua Key=FinanceDept --region cn-hangzhou",
    "legacyCli": "aliyun vpc DescribeTags --region cn-hangzhou --ResourceId.1 'vpc-bp16qjewdsunr41m10000' --Tag.1.Key FinanceDept --NextToken FFmyTO70tTpLG6I3FmYAXGKPd0000 --MaxResult 50 --Tag.1.Value FinanceJoshua --ResourceType VPC --RegionId 'cn-hangzhou'"
  }
}