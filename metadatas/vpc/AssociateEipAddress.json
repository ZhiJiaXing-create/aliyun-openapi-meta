{
  "name": "AssociateEipAddress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllocationId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "绑定云产品实例的EIP的ID。",
        "en": "The ID of the EIP to be associated with a cloud resource instance."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要绑定EIP的实例ID。\n\n支持输入NAT网关实例ID、传统型负载均衡CLB实例ID、云服务器ECS实例ID、辅助弹性网卡实例ID、高可用虚拟IP实例ID、IP地址。",
        "en": "The instance ID of the instance to be associated with the EIP.\n\nYou can enter the instance ID of an Internet NAT gateway, a Classic Load Balancer (CLB) instance, an ECS instance, a secondary elastic network interface (ENI), a high-availability virtual IP address (HAVIP), or an IP address."
      }
    },
    {
      "name": "InstanceRegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要绑定EIP的实例所属地域的ID。\n\n>仅在EIP加入到带宽共享型全球加速实例后，才需要填写该参数。",
        "en": "The region ID of the instance to be associated with the EIP.\n\n> This parameter is required only after the EIP is added to a shared-bandwidth Alibaba Cloud Global Accelerator (GA) instance."
      }
    },
    {
      "name": "InstanceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要绑定EIP的实例的类型，取值：\n\n- **Nat**：NAT网关。\n\n- **SlbInstance**：传统型负载均衡CLB。\n\n- **EcsInstance**（默认值）：云服务器ECS。\n\n- **NetworkInterface**：辅助弹性网卡。\n\n- **HaVip**：高可用虚拟IP。 \n\n- **IpAddress**：IP地址。\n\n> 默认要绑定EIP的实例类型为**EcsInstance**，如果您需要绑定EIP的实例类型不为**EcsInstance**，则该值必填。",
        "en": "The type of the instance to be associated with the EIP. Valid values:\n\n- **Nat**: Internet NAT gateway.\n\n- **SlbInstance**: Classic Load Balancer (CLB).\n\n- **EcsInstance** (default): Elastic Compute Service (ECS).\n\n- **NetworkInterface**: secondary elastic network interface (ENI).\n\n- **HaVip**: high-availability virtual IP address.\n\n- **IpAddress**: IP address.\n\n> The default instance type is **EcsInstance**. If the instance type is not **EcsInstance**, this parameter is required."
      }
    },
    {
      "name": "Mode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "绑定模式，取值：\n\n- **NAT**（默认值）：NAT模式（普通模式）。\n\n- **MULTI_BINDED**：多EIP网卡可见模式。\n\n- **BINDED**：EIP网卡可见模式。\n\n\n> 仅**InstanceType**配置为**NetworkInterface**时，才需要配置该参数。",
        "en": "The association mode. Valid values:\n\n- **NAT** (default): NAT mode (standard mode).\n\n- **MULTI_BINDED**: multi-EIP-to-ENI mode.\n\n- **BINDED**: EIP-to-ENI mode.\n\n\n> This parameter is required only when **InstanceType** is set to **NetworkInterface**."
      }
    },
    {
      "name": "PrivateIpAddress",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "交换机网段内的一个IP地址。\n\n如果不输入该参数，系统根据VPC ID和交换机ID自动分配一个私网IP地址。\n> 当**InstanceType**配置为**NetworkInterface**时，该参数必填，需输入要绑定的私网IP。",
        "en": "An IP address in the vSwitch CIDR block.\n\nIf you do not specify this parameter, the system automatically assigns a private IP address based on the VPC ID and vSwitch ID.\n> If **InstanceType** is set to **NetworkInterface**, this parameter is required. Enter the private IP address to be associated."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要绑定云产品实例的EIP所在的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the EIP to be associated with a cloud resource instance.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the region ID."
      }
    },
    {
      "name": "VpcId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "开通了IPv4网关功能且与EIP同地域的VPC ID。\n\nEIP绑定IP地址时，系统可以根据该VPC的路由配置，使绑定的IP地址具备公网访问能力。\n\n> **InstanceType**取值为**IpAddress**时，即要绑定EIP的实例类型为IP地址时，该参数必填。",
        "en": "The ID of the VPC that has the IPv4 gateway feature enabled and is in the same region as the EIP.\n\nWhen the EIP is associated with an IP address, the system uses the routing configuration of this VPC to enable public network access for the associated IP address.\n\n> This parameter is required when **InstanceType** is set to **IpAddress**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "将EIP绑定到云产品实例",
    "en": "Associate an EIP with a cloud resource instance"
  },
  "descriptions": {
    "zh": "- EIP实例可绑定到同地域的专有网络类型的ECS实例、专有网络类型的CLB实例、辅助弹性网卡、NAT网关和高可用虚拟IP上。\n\n- **AssociateEipAddress**接口属于异步接口，即系统会先返回一个请求ID，但EIP实例与云产品实例并未绑定完成，系统后台的绑定任务仍在进行。您可以调用[DescribeEipAddresses](~~120193~~)查询EIP实例的状态：\n    - 当EIP实例处于**Associating**状态时，表示EIP实例正在绑定中，在该状态下，您只能执行查询操作，不能执行其他操作。\n    - 当EIP实例处于**InUse**状态时，表示EIP实例绑定完成。\n- **AssociateEipAddress**接口不支持同一个EIP实例并发绑定云产品实例。",
    "en": "- An EIP can be associated with a virtual private cloud (VPC)-type ECS instance, a VPC-type CLB instance, a secondary elastic network interface (ENI), an Internet NAT gateway, or a high-availability virtual IP address (HAVIP) in the same region.\n\n- The **AssociateEipAddress** operation is asynchronous. After you send a request, the system returns a request ID, but the association between the EIP and the cloud resource instance is not yet complete. The association task continues to run in the background. You can invoke [DescribeEipAddresses](~~120193~~) to query the status of the EIP:\n    - If the EIP is in the **Associating** state, the EIP is being associated. In this state, you can only execute query operations.\n    - If the EIP is in the **InUse** state, the EIP is associated.\n- The **AssociateEipAddress** operation does not support concurrent requests to associate the same EIP with cloud resource instances."
  },
  "example": {
    "unifiedCli": "aliyun vpc associate-eip-address --biz-region-id cn-hangzhou --allocation-id eip-2zeerraiwb7ujsxdc0000 --instance-id i-2zebb08phyczzawe0000 --instance-type EcsInstance --instance-region-id cn-hangzhou --private-ip-address 192.168.XX.XX --biz-mode NAT --vpc-id vpc-257gqcdfvx6n0000 --client-token 0c593ea1-3bea-11e9-b96b-88e9fe630000 --region cn-hangzhou",
    "legacyCli": "aliyun vpc AssociateEipAddress --region cn-hangzhou --PrivateIpAddress '192.168.XX.XX' --InstanceRegionId 'cn-hangzhou' --VpcId 'vpc-257gqcdfvx6n0000' --InstanceId 'i-2zebb08phyczzawe0000' --AllocationId 'eip-2zeerraiwb7ujsxdc0000' --Mode NAT --RegionId 'cn-hangzhou' --InstanceType EcsInstance --ClientToken '0c593ea1-3bea-11e9-b96b-88e9fe630000'"
  }
}