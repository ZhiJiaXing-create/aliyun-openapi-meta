{
  "name": "AttachDhcpOptionsSetToVpc",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the value, but you must make sure that it is unique among different requests. The client token can contain only ASCII characters.\n\n>  If you do not set this parameter, the system uses **RequestId** as **ClientToken**. **RequestId** may be different for each API request."
      }
    },
    {
      "name": "DhcpOptionsSetId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "DHCP选项集的ID。",
        "en": "The ID of the DHCP options set."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n**true**：发送检查请求，不会将DHCP选项集关联VPC。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码`DryRunOperation`。\n\n**false**（默认值）：发送正常请求，通过检查后返回2XX HTTP状态码并直接将DHCP选项集关联VPC。",
        "en": "Specifies whether to check the request without performing the operation. Valid values:\n\n**true**: checks the request without performing the operation. The system checks whether your AccessKey pair is valid, whether the Resource Access Management (RAM) user is authorized, and whether the required parameters are set. If the request fails to pass the check, the corresponding error message is returned. If the request passes the check, the `DryRunOperation` error code is returned.\n\n**false** (default): sends the request. If the request passes the check, a 2XX HTTP status code is returned and the operation is performed."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "DHCP选项集所在的地域。您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region to which the DHCP options set belongs. You can call the [DescribeRegions](~~36063~~) operation to query the most recent region list."
      }
    },
    {
      "name": "VpcId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "DHCP选项集关联的VPC的ID。",
        "en": "The ID of the VPC to be associated with the DHCP options set."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "将DHCP选项集关联到VPC",
    "en": "AttachDhcpOptionsSetToVpc"
  },
  "descriptions": {
    "zh": "- **AttachDhcpOptionsSetToVpc**接口属于异步接口，即系统返回一个请求ID，但该DHCP选项集尚未关联到VPC，系统后台的关联任务仍在进行。您可以调用[DescribeVpcAttribute](~~94565~~)查询DHCP选项集的关联状态：\n    - 当DHCP选项集处于**Pending**状态时，表示DHCP选项集正在关联中。\n    - 当DHCP选项集处于**InUse**状态时，表示DHCP选项集关联成功。\n- **AttachDhcpOptionsSetToVpc**接口不支持在同一个VPC下并发关联DHCP选项集。",
    "en": "*   The **AttachDhcpOptionsSetToVpc** operation is asynchronous. After you send the request, the system returns a request ID. However, the operation is still being performed in the system background. You can call the [DescribeVpcAttribute](~~94565~~) operation to query the status of a DHCP options set:\n\n    *   If the DHCP options set is in the **Pending** state, the DHCP options set is being associated.\n    *   If the DHCP options set is in the **InUse** state, the DHCP options set is associated.\n\n*   You cannot repeatedly call the **AttachDhcpOptionsSetToVpc** operation to associate DHCP options sets with a VPC within the specified period of time."
  },
  "example": {
    "unifiedCli": "aliyun vpc attach-dhcp-options-set-to-vpc --biz-region-id cn-hangzhou --dhcp-options-set-id dopt-o6w0df4epg9zo8isy0000 --vpc-id vpc-sfdkfdjkdf0000 --client-token 0c593ea1-3bea-11e9-b96b-88e9fe637760 --dry-run false --region cn-hangzhou",
    "legacyCli": "aliyun vpc AttachDhcpOptionsSetToVpc --region cn-hangzhou --DhcpOptionsSetId 'dopt-o6w0df4epg9zo8isy0000' --VpcId 'vpc-sfdkfdjkdf0000' --DryRun false --RegionId 'cn-hangzhou' --ClientToken '0c593ea1-3bea-11e9-b96b-88e9fe637760'"
  }
}