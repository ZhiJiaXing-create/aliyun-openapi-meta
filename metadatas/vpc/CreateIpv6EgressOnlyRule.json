{
  "name": "CreateIpv6EgressOnlyRule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.\n\n> If you do not specify this parameter, the system automatically uses the RequestId of the API request as the ClientToken. The RequestId may be different for each API request."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "仅主动出规则的描述。\n\n长度为0-256个字符，不能以`http://`或`https://`开头。",
        "en": "The description of the egress-only rule.\n\nThe description must be 0 to 256 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "参数预检，不实际运行",
        "en": "Specifies whether to perform a dry run without performing the actual request."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要设置仅主动出规则的IPv6地址ID。",
        "en": "The ID of the IPv6 address for which you want to create an egress-only rule."
      }
    },
    {
      "name": "InstanceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要设置仅主动出规则的实例类型。\n\n仅取值：**Ipv6Address**（默认值），表示为IPv6地址类型。",
        "en": "The type of the instance for which you want to create an egress-only rule.\n\nSet the value to **Ipv6Address** (default), which indicates the IPv6 address type."
      }
    },
    {
      "name": "Ipv6GatewayId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPv6网关实例ID。",
        "en": "The instance ID of the IPv6 gateway."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "仅主动出规则的名称。\n\n长度为0-128个字符，不能以`http://`或`https://`开头。",
        "en": "The name of the egress-only rule.\n\nThe name must be 0 to 128 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPv6网关的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the IPv6 gateway.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为IPv6地址添加仅主动出规则",
    "en": "Create an iPv6 egress-only rule for an iPv6 address"
  },
  "descriptions": {
    "zh": "- **CreateIpv6EgressOnlyRule**接口属于异步接口，即系统返回一个实例ID，但该IPv6地址的仅主动出规则尚未创建成功，系统后台的创建任务仍在进行。您可以调用[DescribeIpv6EgressOnlyRules](~~102208~~)查询IPv6地址的仅主动出规则的创建状态：\n    - 当IPv6地址的仅主动出规则处于**Creating**状态时，表示IPv6地址仅主动出规则正在创建中。\n    - 当IPv6地址的仅主动出规则处于**Created**状态时，表示IPv6地址仅主动出规则创建完成。\n- **CreateIpv6EgressOnlyRule**接口不支持为同一个IPv6地址并发添加仅主动出规则。",
    "en": "- The **CreateIpv6EgressOnlyRule** operation is asynchronous. After you send a request, the system returns an instance ID, but the IPv6 egress-only rule has not been created yet. The creation task is still running in the background. You can call [DescribeIpv6EgressOnlyRules](~~102208~~) to query the creation status of the IPv6 egress-only rule:\n    - If the IPv6 egress-only rule is in the **Creating** state, the rule is being created.\n    - If the IPv6 egress-only rule is in the **Created** state, the rule has been created.\n- The **CreateIpv6EgressOnlyRule** operation does not support concurrent creation of egress-only rules for the same IPv6 address."
  },
  "example": {
    "unifiedCli": "aliyun vpc create-ipv6-egress-only-rule --biz-region-id cn-hangzhou --ipv6-gateway-id ipv6gw-hp3c2paq0ywauasza0000 --instance-id ipv6-hp3nxjkfxn5pnhgl50000 --instance-type Ipv6Address --name rulename --description ruledescription --client-token 0c593ea1-3bea-11e9-b96b-88e9fe637760 --dry-run true --region cn-hangzhou",
    "legacyCli": "aliyun vpc CreateIpv6EgressOnlyRule --region cn-hangzhou --Description ruledescription --InstanceId 'ipv6-hp3nxjkfxn5pnhgl50000' --DryRun true --Ipv6GatewayId 'ipv6gw-hp3c2paq0ywauasza0000' --RegionId 'cn-hangzhou' --InstanceType Ipv6Address --ClientToken '0c593ea1-3bea-11e9-b96b-88e9fe637760' --Name rulename"
  }
}