{
  "name": "ListPublicIpAddressPools",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n- **true**：发送检查请求，不会查询可使用的IP地址池信息。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作。",
        "en": "Specifies whether to perform a dry run, without performing the actual request. Valid values:\n\n*   **true**: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n*   **false**(default): performs a dry run and performs the actual request. If the request passes the dry run, a 2xx HTTP status code is returned and the operation is performed."
      }
    },
    {
      "name": "Isp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "线路类型。取值：\n\n- **BGP**（默认值）：BGP（多线）线路。\n\n- **BGP_PRO**：BGP（多线）\\_精品线路。\n\n关于BGP（多线）线路和BGP（多线）_精品线路的更多信息，请参见[EIP线路类型](~~32321~~)。\n\n如果您是单线带宽的白名单用户，您还可以选择以下类型：\n-  **ChinaTelecom**：中国电信\n- **ChinaUnicom**：中国联通\n- **ChinaMobile**：中国移动\n- **ChinaTelecom_L2**：中国电信L2\n- **ChinaUnicom_L2**：中国联通L2\n- **ChinaMobile_L2**：中国移动L2\n\n如果您是杭州金融云用户，该字段必填，取值：**BGP_FinanceCloud**。",
        "en": "The line type. Valid values:\n\n*   **BGP** (default): BGP (Multi-ISP) line\n*   **BGP_PRO**: BGP (Multi-ISP) Pro line\n\nFor more information about the BGP (Multi-ISP) line and BGP (Multi-ISP) Pro line, see the \"Line types\" section of [What is EIP?](~~32321~~)\n\nIf you are allowed to use single-ISP bandwidth, you can also choose one of the following values:\n\n*   **ChinaTelecom**\n*   **ChinaUnicom**\n*   **ChinaMobile**\n*   **ChinaTelecom_L2**\n*   **ChinaUnicom_L2**\n*   **ChinaMobile_L2**\n\nIf your services are deployed in China East 1 Finance, this parameter is required and you must set the parameter to **BGP_FinanceCloud**."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "本次读取的最大数据记录数量。取值范围：**10**~**100**。默认值：**10**。",
        "en": "The maximum number of entries to return. Valid values: **10** to **100**. Default value: **10**."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地址池实例的名称。\n\n名称长度为0-128个字符，不能以`http://`或`https://`开头。",
        "en": "The name of the IP address pool.\n\nIf you enter a name, the name must be 1 to 128 characters in length and can contain digits, periods (.), underscores (\\_), and hyphens (-). The name must start with a letter but cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否拥有下一次查询的令牌（Token）。取值：\n- 第一次查询和没有下一次查询时，均无需填写。\n- 如果有下一次查询，取值为上一次API调用返回的NextToken值。",
        "en": "The pagination token that is used in the next request to retrieve a new page of results.\n\n*   You do not need to specify this parameter for the first request.\n*   You must specify the token that is obtained from the previous query as the value of NextToken."
      }
    },
    {
      "name": "PublicIpAddressPoolIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "IP地址池的实例ID列表。\n\n您最多可输入100个IP地址池的实例ID。",
        "en": "The IDs of the IP address pool.\n\nYou can enter up to 100 IDs."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要查询的IP地址池所在的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region in which the IP address pool that you want to query resides.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the region ID."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IP地址池所属的资源组ID。",
        "en": "The ID of the resource group to which the IP address pool belongs."
      }
    },
    {
      "name": "SecurityProtectionEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启DDoS防护（增强版）。取值：\n- **false**：不开启。\n- **true**：开启。",
        "en": "Specifies whether to enable Anti-DDoS Pro/Premium. Valid values:\n\n*   **false**\n*   **true**"
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IP地址池实例状态。取值：\n- **Created**：可用。\n- **Deleting**：删除中。\n- **Modifying**：修改中。",
        "en": "The status of the IP address pool. Valid values:\n\n*   **Created**\n*   **Deleting**\n*   **Modifying**"
      }
    },
    {
      "name": "Tags",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表。",
        "en": "The tags to add to the resource."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。最多支持输入20个标签键。如需传入该值，则不能输入空字符串。\n\n一个标签键最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag key to add to the resource. You can specify up to 20 tag keys. The tag key cannot be an empty string.\n\nThe tag key can be up to 128 characters in length. It cannot start with `aliyun` or `acs:`, and cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。最多支持输入20个标签值。如需传入该值，可以输入空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag value to add to the resource. You can specify up to 20 tag values. The tag value can be an empty string.\n\nThe tag key can be up to 128 characters in length. It cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询IP地址池信息",
    "en": "ListPublicIpAddressPools"
  },
  "example": {
    "unifiedCli": "aliyun vpc list-public-ip-address-pools --public-ip-address-pool-ids pippool-6wetvn6fumkgycssx0000 --status Created --max-results 10 --next-token FFmyTO70tTpLG6I3FmYAXGKPd0000 --isp BGP --name AddressPoolName --dry-run false --resource-group-id rg-acfmxazb4pcdvf0000 --tags Value=FinanceJoshua Key=FinanceDept --biz-region-id cn-hangzhou --security-protection-enabled true --region cn-hangzhou",
    "legacyCli": "aliyun vpc ListPublicIpAddressPools --region cn-hangzhou --Status Created --Tags.1.Value FinanceJoshua --NextToken FFmyTO70tTpLG6I3FmYAXGKPd0000 --ResourceGroupId 'rg-acfmxazb4pcdvf0000' --MaxResults 10 --Isp BGP --DryRun false --SecurityProtectionEnabled true --PublicIpAddressPoolIds.1 'pippool-6wetvn6fumkgycssx0000' --Tags.1.Key FinanceDept --RegionId 'cn-hangzhou' --Name AddressPoolName"
  }
}