{
  "name": "DescribeBgpGroups",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BgpGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "BGP组的ID。",
        "en": "The ID of the BGP group."
      }
    },
    {
      "name": "IsDefault",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否为默认BGP组。取值：\n\n- **false**：不是默认BGP组。\n- **true**：是默认BGP组。",
        "en": "Specifies whether the BGP group is the default one. Valid values:\n\n*   **false**\n*   **true**"
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "列表的页码。默认值：**1**。",
        "en": "The page number. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页的行数。最大值：**50**。默认值：**10**。",
        "en": "The number of entries per page. The maximum value is **50**. Default value: **10**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "VBR所在的地域ID。 \n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region in which the VBR is deployed.\n\nYou can call the [DescribeRegions](~~36063~~) operation to obtain the region ID."
      }
    },
    {
      "name": "RouterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "BGP组关联的VBR的ID。",
        "en": "The ID of the virtual border router (VBR) that is associated with the BGP group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询指定地域下的BGP组",
    "en": "DescribeBgpGroups"
  },
  "example": {
    "unifiedCli": "aliyun vpc describe-bgp-groups --router-id vbr-bp1ctxy813985gkuk0000 --bgp-group-id bgpg-bp1k25cyp26cllath0000 --biz-region-id cn-hangzhou --is-default false --page-number 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun vpc DescribeBgpGroups --region cn-hangzhou --IsDefault false --PageSize 10 --PageNumber 1 --RouterId 'vbr-bp1ctxy813985gkuk0000' --RegionId 'cn-hangzhou' --BgpGroupId 'bgpg-bp1k25cyp26cllath0000'"
  }
}