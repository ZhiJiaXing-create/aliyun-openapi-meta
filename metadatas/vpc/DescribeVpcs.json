{
  "name": "DescribeVpcs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DhcpOptionsSetId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "DHCP选项集的ID。",
        "en": "The ID of the DHCP options set."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n- **true**：发送检查请求，不会查询资源状况。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码`DryRunOperation`。\n\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接查询资源状况。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: performs a dry run. The system checks the request for potential issues, including invalid AccessKey pairs, unauthorized RAM users, and missing parameter values. If the check fails, the corresponding error is returned. If the check succeeds, the `DryRunOperation` error code is returned.\n\n- **false** (default): sends a normal request. If the check succeeds, an HTTP 2xx status code is returned and the resource is queried."
      }
    },
    {
      "name": "EnableIpv6",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否查询指定地域下开启IPv6网段的VPC，默认为空值（空值则不根据是否开启IPv6网段做过滤），取值：\n\n- **false**：不开启。\n- **true**：开启。",
        "en": "Specifies whether to query VPCs that have IPv6 CIDR blocks enabled in the specified region. The default value is empty, which means no filtering is performed based on IPv6 enablement. Valid values:\n\n- **false**: IPv6 is not enabled.\n- **true**: IPv6 is enabled."
      }
    },
    {
      "name": "IsDefault",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否查询指定地域下的默认VPC，取值： \n\n- **true**（默认值）：查询指定地域下的默认VPC。  \n\n- **false**：不查询默认VPC。",
        "en": "Specifies whether to query the default VPC in the specified region. Valid values: \n\n- **true** (default): Queries the default VPC in the specified region.  \n\n- **false**: Does not query the default VPC."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "列表的页码，默认值为**1**。",
        "en": "The page number. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页的行数，最大值为**50**，默认值为**10**。",
        "en": "The number of entries per page for paging. Maximum value: **50**. Default value: **10**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "VPC所在的地域ID。 \n\n您可以通过调用[DescribeRegions](~~448570~~)接口获取地域ID。",
        "en": "The region ID of the VPC. \n\nYou can call the [DescribeRegions](~~448570~~) operation to query the region ID."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的VPC所属的资源组ID。",
        "en": "The ID of the resource group to which the VPC belongs."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源的标签。",
        "en": "The tags of the resource."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。最多支持输入20个标签键。如需传入该值，则不能输入空字符串。\n\n一个标签键最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag key of the resource. You can specify up to 20 tag keys. The tag key cannot be an empty string.\n\nThe tag key can be up to 128 characters in length. It cannot start with `aliyun` or `acs:`, and cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。最多支持输入20个标签值。如需传入该值，可以输入空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag value of the resource. You can specify up to 20 tag values. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length. It cannot start with `aliyun` or `acs:`, and cannot contain `http://` or `https://`."
          }
        }
      ]
    },
    {
      "name": "VpcId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "VPC的ID。 \n\n最多支持指定20个VPC ID，多个VPC的ID之间用半角逗号（,）隔开。",
        "en": "The ID of the VPC. \n\nYou can specify up to 20 VPC IDs. Separate multiple IDs with commas (,)."
      }
    },
    {
      "name": "VpcName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "VPC的名称。",
        "en": "The name of the VPC."
      }
    },
    {
      "name": "VpcOwnerId",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "VPC所属的阿里云账号ID。",
        "en": "The Alibaba Cloud account ID of the VPC owner."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询已创建的VPC",
    "en": "Query created VPCs"
  },
  "example": {
    "unifiedCli": "aliyun vpc describe-vpcs --vpc-id vpc-bp1b1xjllp3ve5yze0000 --biz-region-id cn-hangzhou --vpc-name Vpc-1 --is-default false --dry-run false --resource-group-id rg-acfmxvfvazb4p0000 --page-number 1 --page-size 10 --vpc-owner-id 253460731706911258 --dhcp-options-set-id dopt-o6w0df4epg9zo8isy0000 --enable-ipv6 false --tag Value=FinanceJoshua Key=FinanceDept --region cn-hangzhou",
    "legacyCli": "aliyun vpc DescribeVpcs --region cn-hangzhou --IsDefault false --Tag.1.Key FinanceDept --ResourceGroupId 'rg-acfmxvfvazb4p0000' --PageSize 10 --DryRun false --VpcOwnerId 253460731706911258 --EnableIpv6 false --DhcpOptionsSetId 'dopt-o6w0df4epg9zo8isy0000' --VpcId 'vpc-bp1b1xjllp3ve5yze0000' --PageNumber 1 --Tag.1.Value FinanceJoshua --VpcName 'Vpc-1' --RegionId 'cn-hangzhou'"
  }
}