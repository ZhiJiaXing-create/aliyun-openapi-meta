{
  "name": "TerminateVirtualBorderRouter",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "VBR所在的地域。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the VBR.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list."
      }
    },
    {
      "name": "VbrId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "VBR的ID。",
        "en": "The ID of the VBR."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "终止边界路由器（VBR）",
    "en": "TerminateVirtualBorderRouter"
  },
  "descriptions": {
    "zh": "调用该接口后VBR从**active**状态进入**terminating**状态，终止成功后进入**terminated**状态。 \n\n> 只有物理专线的所有者可以调用该接口。",
    "en": "After you call this operation, the VBR enters the **terminating** state from the **active** state. After the VBR is terminated, the VBR enters the **terminated** state.\n\n>  Only the owner of an Express Connect circuit can call this operation."
  },
  "example": {
    "unifiedCli": "aliyun vpc terminate-virtual-border-router --biz-region-id cn-hangzhou --vbr-id vbr-bp15zckdt37pq720000 --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun vpc TerminateVirtualBorderRouter --region cn-hangzhou --RegionId 'cn-hangzhou' --VbrId 'vbr-bp15zckdt37pq720000' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}