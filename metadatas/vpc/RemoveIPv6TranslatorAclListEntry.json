{
  "name": "RemoveIPv6TranslatorAclListEntry",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AclEntryId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要删除的访问控制策略条目ID。",
        "en": "The ID of the ACL entry to be deleted."
      }
    },
    {
      "name": "AclId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制策略条目所属的访问控制策略组ID。",
        "en": "The ID of the ACL to which the ACL entry belongs."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制策略组的地域。",
        "en": "The region of the ACL."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除访问控制策略组中的IP条目",
    "en": "RemoveIPv6TranslatorAclListEntry"
  },
  "example": {
    "unifiedCli": "aliyun vpc remove-ipv6-translator-acl-list-entry --client-token 123e4567-e89b-12d3-a456-426655440000 --biz-region-id cn-hangzhou --acl-id ipv6transacl-bp1de20000 --acl-entry-id ipv6transaclentry-bp105jrs0000 --region cn-hangzhou",
    "legacyCli": "aliyun vpc RemoveIPv6TranslatorAclListEntry --region cn-hangzhou --AclEntryId 'ipv6transaclentry-bp105jrs0000' --AclId 'ipv6transacl-bp1de20000' --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}