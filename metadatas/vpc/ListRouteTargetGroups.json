{
  "name": "ListRouteTargetGroups",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。说明 若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样。",
        "en": "Client token used to ensure idempotence of the request. Generate a unique parameter value from your client to ensure uniqueness across different requests. ClientToken only supports ASCII characters. Note: If you do not specify this, the system will automatically use the RequestId of the API request as the ClientToken identifier. The RequestId is different for each API request."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小，取值范围：**1**~**50**，默认值：**50**。",
        "en": "Page size, with a range of **1** to **50**. Default value: **50**."
      }
    },
    {
      "name": "MemberId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "路由目标组成员实例ID。\n筛选成员包含传入成员实例ID的路由目标组。",
        "en": "Route target group member instance ID.\nFilters the route target groups that contain the specified member instance ID."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否拥有下一次查询的令牌（Token）。取值：第一次查询和没有下一次查询时，均无需填写。如果有下一次查询，取值为上一次API调用返回的NextToken值。",
        "en": "Token for the next query. Value: If it is the first query or there is no next query, this field does not need to be filled. If there is a next query, the value should be the NextToken returned from the previous API call."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "路由目标组所属的VPC的地域ID。您可以通过调用DescribeRegions接口获取地域ID。",
        "en": "The region ID of the VPC to which the route target group belongs. You can obtain the region ID by calling the DescribeRegions interface."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。关于资源组的更多信息，请参见什么是资源组。",
        "en": "Resource group ID. For more information about resource groups, see What is a Resource Group?"
      }
    },
    {
      "name": "RouteTargetGroupIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "路由目标组实例ID列表。\n\n单次查询最多支持传入50个实例ID。",
        "en": "List of route target group instance IDs.\n\nUp to 50 instance IDs can be queried at a time."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表。",
        "en": "List of tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。最多支持输入20个标签键。如需传入该值，则不能输入空字符串。\n\n一个标签键最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "Resource tag key. Up to 20 tag keys are supported. If you need to pass this value, you cannot input an empty string.\n\nA tag key can have up to 128 characters and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。最多支持输入20个标签值。如需传入该值，可以输入空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "Resource tag value. Up to 20 tag values are supported. If you need to pass this value, you can input an empty string.\n\nA tag value can have up to 128 characters and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`."
          }
        }
      ]
    },
    {
      "name": "VpcId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "路由目标组所属的VPC的ID。",
        "en": "The ID of the VPC to which the route target group belongs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询路由目标组列表",
    "en": "ListRouteTargetGroups"
  },
  "descriptions": {
    "zh": "查询路由目标组列表。",
    "en": "Lists the route target groups."
  },
  "example": {
    "unifiedCli": "aliyun vpc list-route-target-groups --vpc-id vpc-xxxx --route-target-group-ids rtg-xxxx --member-id ep-xxxx --max-results 50 --next-token FFmyTO70tTpLG6I3FmYAXGKPd0000 --biz-region-id cn-hangzhou --resource-group-id rg-acfmxazdjdhd0000 --client-token 123e4567-e89b-12d3-a456-426655440000 --tag Value=FinanceJoshua Key=FinanceJoshua --region cn-hangzhou",
    "legacyCli": "aliyun vpc ListRouteTargetGroups --region cn-hangzhou --MemberId 'ep-xxxx' --Tag.1.Key FinanceJoshua --VpcId 'vpc-xxxx' --NextToken FFmyTO70tTpLG6I3FmYAXGKPd0000 --ResourceGroupId 'rg-acfmxazdjdhd0000' --RouteTargetGroupIds.1 'rtg-xxxx' --MaxResults 50 --Tag.1.Value FinanceJoshua --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}