{
  "name": "ModifySslVpnClientCert",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.\n\n>  If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "SSL-VPN客户端证书的新名称。该参数不能为空。\n\n长度为1～100个字符，不能以`http://`或`https://`开头。",
        "en": "The new name of the SSL client certificate. This parameter cannot be left empty.\n\nThe name must be 1 to 100 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "SSL-VPN客户端证书所在的地域ID。 \n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region where the SSL client certificate is created.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list."
      }
    },
    {
      "name": "SslVpnClientCertId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "SSL-VPN客户端证书的ID。",
        "en": "The ID of the SSL client certificate."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改SSL-VPN客户端证书的名称",
    "en": "ModifySslVpnClientCert"
  },
  "example": {
    "unifiedCli": "aliyun vpc modify-ssl-vpn-client-cert --client-token 02fb3da4-130e-11e9-8e44-0016e04115b --biz-region-id cn-hangzhou --ssl-vpn-client-cert-id vsc-bp1n8wcf134yl0osrc0000 --name cert2 --region cn-hangzhou",
    "legacyCli": "aliyun vpc ModifySslVpnClientCert --region cn-hangzhou --SslVpnClientCertId 'vsc-bp1n8wcf134yl0osrc0000' --RegionId 'cn-hangzhou' --ClientToken '02fb3da4-130e-11e9-8e44-0016e04115b' --Name cert2"
  }
}