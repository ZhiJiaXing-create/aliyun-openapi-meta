{
  "name": "AllocateIpv6Address",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AddressType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IPv6地址的类型，取值：\n\n* IPv6Address（默认值）：代表当前实例是单个IPv6 IP。\n* IPv6Prefix：代表当前实例是IPv6 Cidr。",
        "en": "The type of the IPv6 address. Valid values:\n\n* IPv6Address (default): The instance is a single IPv6 address.\n* IPv6Prefix: The instance is an IPv6 CIDR block."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The ClientToken value can contain only ASCII characters.\n\n> If you do not specify this parameter, the system uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n- true：发送检查请求，不会查询资源状况。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码DryRunOperation。\n\n- false（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- true: performs a dry run. The system checks the required parameters, request syntax, and business restrictions. If the check fails, the corresponding error is returned. If the check succeeds, the DryRunOperation error code is returned.\n\n- false (default): performs a dry run and sends the request. If the check succeeds, an HTTP 2xx status code is returned and the operation is performed."
      }
    },
    {
      "name": "Ipv6Address",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定要申请的IPv6地址。指定的IPv6地址必须是在所属交换机的地址段内的空闲地址。",
        "en": "The IPv6 address to allocate. The specified IPv6 address must be an idle address within the CIDR block of the vSwitch."
      }
    },
    {
      "name": "Ipv6AddressDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IPv6地址的实例描述。\n\n长度为0～256个字符，不能以`http://`或`https://`开头。",
        "en": "The description of the IPv6 address instance.\n\nThe description must be 0 to 256 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "Ipv6AddressName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IPv6地址的实例名称。\n\n长度为0～128个字符，不能以`http://`或`https://`开头。",
        "en": "The name of the IPv6 address instance.\n\nThe name must be 0 to 128 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。关于资源组的更多信息，请参见[什么是资源组](~~2381067~~)。",
        "en": "The resource group ID. For more information about resource groups, see [What is a resource group?](~~2381067~~)."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源的标签列表。",
        "en": "The list of tags for the resource."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。最多支持输入20个标签键。如需传入该值，请勿输入空字符串。\n\n一个标签键最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag key of the resource. You can specify up to 20 tag keys. Do not set this parameter to an empty string.\n\nA tag key can be up to 128 characters in length and cannot start with `aliyun` or `acs:`, or contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。最多支持输入20个标签值。如需传入该值，可以输入空字符串。\n\n最多支持128个字符，不能以aliyun和acs:开头，不能包含http://或者https://。",
            "en": "The tag value of the resource. You can specify up to 20 tag values. The value can be an empty string.\n\nThe tag value can be up to 128 characters in length and cannot start with aliyun or acs:, or contain http:// or https://."
          }
        }
      ]
    },
    {
      "name": "VSwitchId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IPv6地址所在的交换机的ID。",
        "en": "The ID of the vSwitch to which the IPv6 address belongs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建IPv6地址",
    "en": "Create an iPv6 address"
  },
  "descriptions": {
    "zh": "使用场景：为游离 IPv6 地址开通公网带宽后，在 IPv6 网关路由表中配置入方向路由，将公网 IPv6 流量通过专线导向 IDC 侧，实现 IDC 资源通过阿里云 IPv6 地址对外提供服务。\n\n> 如果需要为 ECS 实例的弹性网卡分配 IPv6 地址，请调用 ECS 的 [AssignIpv6Addresses](https://help.aliyun.com/zh/ecs/developer-reference/api-ecs-2014-05-26-assignipv6addresses)",
    "en": "Scenarios: After you enable Internet bandwidth for a free IPv6 address, configure an inbound routing rule in the IPv6 gateway route table to direct public IPv6 traffic to an Internet Data Center (IDC) through an Express Connect circuit. This way, IDC resources can provide services over the Internet by using an Alibaba Cloud IPv6 address.\n\n> To allocate an IPv6 address to an elastic network interface (ENI) of an ECS instance, invoke the [AssignIpv6Addresses](https://www.alibabacloud.com/help/en/ecs/developer-reference/api-ecs-2014-05-26-assignipv6addresses) operation of ECS."
  },
  "example": {
    "unifiedCli": "aliyun vpc allocate-ipv6-address --biz-region-id cn-hangzhou --vswitch-id vsw-asdfjlnaue4g0000 --ipv6-address 2408:XXXX:153:3921:851c:c435:7b12:1c5f --ipv6-address-name ipv6-name --ipv6-address-description ipv6-description --address-type IPv6Address --resource-group-id rg-bp67acfmxazb4ph0000 --tag Value=FinanceJoshua Key=FinanceDept --client-token 02fb3da4-130e-11e9-8e44-0016e040000 --dry-run false --region cn-hangzhou",
    "legacyCli": "aliyun vpc AllocateIpv6Address --region cn-hangzhou --Tag.1.Key FinanceDept --ResourceGroupId 'rg-bp67acfmxazb4ph0000' --VSwitchId 'vsw-asdfjlnaue4g0000' --Tag.1.Value FinanceJoshua --DryRun false --Ipv6AddressDescription 'ipv6-description' --RegionId 'cn-hangzhou' --AddressType IPv6Address --ClientToken '02fb3da4-130e-11e9-8e44-0016e040000' --Ipv6Address '2408:XXXX:153:3921:851c:c435:7b12:1c5f' --Ipv6AddressName 'ipv6-name'"
  }
}