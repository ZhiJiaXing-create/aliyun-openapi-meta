{
  "name": "ModifyRouteEntry",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "路由条目的描述。\n\n描述长度为1～256个字符，不能以`http://`或`https://`开头。",
        "en": "The description of the route entry.\n\nThe description must be 1 to 256 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "DestinationCidrBlock",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "路由条目的目标网段，支持IPv4网段、IPv6网段。\n> 当**RouteEntryId**参数未输入时，**DestinationCidrBlock**和**RouteTableId**参数必填。\n> 当需要修改路由的目标网段为 **前缀列表** 时，必须传入 **RouteEntryId** 参数，且 **DestinationCidrBlock** 参数不支持传入前缀列表网段或前缀列表实例ID。",
        "en": "The IPv4 CIDR block of the route entry. IPv4 and IPv6 CIDR blocks are supported.\n> If the **RouteEntryId** parameter is not specified, the **DestinationCidrBlock** and **RouteTableId** parameters are required.\n> To change the IPv4 CIDR block of a route to a **prefix list**, specify the **RouteEntryId** parameter. The **DestinationCidrBlock** parameter does not support prefix list CIDR blocks or prefix list instance IDs."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n**true**：发送请求，不会修改路由条目。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码`DryRunOperation`。\n\n**false**（默认值）：发送正常请求，通过检查后返回2xx HTTP状态码并直接修改路由条目。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n**true**: sends the request without modifying the route. The system checks whether the AccessKey pair is valid, the authorization of the Resource Access Management (RAM) user, and whether the required parameters are specified. If the check fails, the corresponding error is returned. If the check succeeds, the `DryRunOperation` error code is returned.\n\n**false** (default): sends a Normal request. After the request passes the check, a 2xx HTTP status code is returned and the route is modified."
      }
    },
    {
      "name": "NewNextHopId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "新路由条目下一跳实例ID。",
        "en": "The new next hop instance ID of the route."
      }
    },
    {
      "name": "NewNextHopType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "新路由条目下一跳类型。取值如下：\n\n- **Instance**：ECS实例。\n\n- **HaVip**：高可用虚拟IP。  \n\n- **RouterInterface**：路由器接口。\n\n- **NetworkInterface**：弹性网卡。\n\n- **VpnGateway**：VPN网关。\n\n- **IPv6Gateway**：IPv6网关。\n\n- **NatGateway**：NAT网关。\n\n- **Attachment**：转发路由器。\n\n- **VpcPeer**：VPC对等连接。\n- **Ipv4Gateway**：IPv4网关。\n- **GatewayEndpoint**：网关终端节点。\n- **Ecr**：专线网关。\n- **GatewayLoadBalancerEndpoint**：网关型负载均衡终端节点。",
        "en": "The new next hop type of the route. Valid values:\n\n- **Instance**: ECS instance.\n\n- **HaVip**: high-availability virtual IP address.  \n\n- **RouterInterface**: vRouter interface.\n\n- **NetworkInterface**: elastic network interface (ENI).\n\n- **VpnGateway**: VPN gateway.\n\n- **IPv6Gateway**: IPv6 gateway.\n\n- **NatGateway**: NAT gateway.\n\n- **Attachment**: transit router.\n\n- **VpcPeer**: VPC peering connection.\n- **Ipv4Gateway**: IPv4 gateway.\n- **GatewayEndpoint**: gateway endpoint.\n- **Ecr**: Express Connect Router (ECR).\n- **GatewayLoadBalancerEndpoint**: Gateway Load Balancer endpoint (GWLBe)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "路由条目所在的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the route entry.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the region ID."
      }
    },
    {
      "name": "RouteEntryId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自定义路由条目ID。",
        "en": "The ID of the custom route entry."
      }
    },
    {
      "name": "RouteEntryName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "路由条目的名称。\n\n名称长度为1～128个字符，不能以`http://`或`https://`开头。",
        "en": "The name of the route entry.\n\nThe name must be 1 to 128 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "RouteTableId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "路由表ID。",
        "en": "The route table ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改自定义路由条目的名称描述或者路由下一跳",
    "en": "Modify the name, description, or next hop of a custom route entry"
  },
  "descriptions": {
    "zh": "- **ModifyRouteEntry**接口不支持并发修改同一个自定义路由条目的名称和描述。\n- **ModifyRouteEntry**接口在并发情况下可能返回**TaskConflict**错误码，请酌情重试。",
    "en": "- The **ModifyRouteEntry** operation does not support concurrent modifications to the name and description of the same custom route entry.\n- The **ModifyRouteEntry** operation may return the **TaskConflict** error code in concurrent scenarios. Retry as appropriate."
  },
  "example": {
    "unifiedCli": "aliyun vpc modify-route-entry --biz-region-id cn-hangzhou --route-entry-name EntryName --route-entry-id rte-acfvgfsghfdd0000 --description EntryDescription --new-next-hop-type NetworkInterface --new-next-hop-id eni-bp17y37ytsenqyim0000 --route-table-id vtb-bp1nk7zk65du3pni8z9td --destination-cidr-block 192.168.0.0/24 --dry-run true --region cn-hangzhou",
    "legacyCli": "aliyun vpc ModifyRouteEntry --region cn-hangzhou --RouteEntryId 'rte-acfvgfsghfdd0000' --Description EntryDescription --RouteTableId 'vtb-bp1nk7zk65du3pni8z9td' --RouteEntryName EntryName --NewNextHopId 'eni-bp17y37ytsenqyim0000' --DryRun true --NewNextHopType NetworkInterface --RegionId 'cn-hangzhou' --DestinationCidrBlock '192.168.0.0/24'"
  }
}