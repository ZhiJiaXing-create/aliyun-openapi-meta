{
  "name": "DescribePhysicalConnectionLOA",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "A client token that ensures the idempotence of the request.\n\nYour client generates this value, which must be unique for each request and have a maximum length of 64 ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. Each API request has a unique **RequestId**."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "物理专线实例ID。",
        "en": "The ID of the physical connection instance."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "部署物理专线的地域ID。\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region where the physical connection is located. You can call the [DescribeRegions](~~36063~~) operation to get a list of available region IDs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询物理专线LOA信息",
    "en": "DescribePhysicalConnectionLOA"
  },
  "example": {
    "unifiedCli": "aliyun vpc describe-physical-connection-loa --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-426655440000 --instance-id pc-bp1ca4wca27ex0000 --region cn-hangzhou",
    "legacyCli": "aliyun vpc DescribePhysicalConnectionLOA --region cn-hangzhou --InstanceId 'pc-bp1ca4wca27ex0000' --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}