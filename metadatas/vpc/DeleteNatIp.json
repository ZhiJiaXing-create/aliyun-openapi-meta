{
  "name": "DeleteNatIp",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The ClientToken value can contain only ASCII characters.\n\n> If you do not specify this parameter, the system uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may differ for each API request."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n- **true**：发送检查请求，不会删除NAT IP地址。检查项包括AccessKey是否有效、RAM用户的授权情况和是否填写了必需参数。如果检查不通过，则返回对应错误。如果检查通过，会返回错误码`DryRunOperation`。\n\n- **false**（默认值）：发送正常请求，通过检查后返回2xx HTTP状态码并删除NAT IP地址。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: performs a dry run without deleting the NAT IP address. The system checks the AccessKey pair, the authorization of the Resource Access Management (RAM) user, and the required parameters. If the check fails, the corresponding error is returned. If the check succeeds, the error code `DryRunOperation` is returned.\n\n- **false** (default): sends a Normal request. If the check succeeds, a 2xx HTTP status code is returned and the NAT IP address is deleted."
      }
    },
    {
      "name": "Ipv4Prefix",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "删除的IP前缀地址。",
        "en": "The IP prefix to delete."
      }
    },
    {
      "name": "NatGatewayId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要删除的IP前缀所属的NAT网关实例ID。  当删除IP前缀时需要指定。",
        "en": "The instance ID of the NAT gateway to which the IP prefix belongs. This parameter is required when you delete an IP prefix."
      }
    },
    {
      "name": "NatIpId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要删除的NAT IP地址实例ID。",
        "en": "The instance ID of the NAT IP address that you want to delete."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要删除的NAT IP地址所属NAT网关实例的地域ID。\n\n您可以通过调用[DescribeRegions](~~448570~~)接口获取地域ID。",
        "en": "The region ID of the NAT gateway to which the NAT IP address belongs.\n\nYou can call [DescribeRegions](~~448570~~) to query the region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除NAT IP地址",
    "en": "Delete a NAT IP address"
  },
  "descriptions": {
    "zh": "**DeleteNatIp**接口属于异步接口，即系统会先返回一个请求ID，但该NAT IP地址并未删除完成，系统后台的删除任务仍在进行。您可以调用[ListNatIps](~~2522486~~)查询NAT IP地址的删除状态：\n\n- 当NAT IP地址处于**Deleting**状态时，表示NAT IP地址正在删除中，在该状态下，您只能执行查询操作，不能执行其他操作。\n\n- 当查询不到该NAT IP地址时，表示NAT IP地址已删除。",
    "en": "**DeleteNatIp** is an asynchronous operation. After you send a request, the system returns a request ID, but the NAT IP address is not yet deleted. The deletion task continues to run in the background. You can call [ListNatIps](~~2522486~~) to query the deletion status of the NAT IP address:\n\n- If the NAT IP address is in the **Deleting** state, the NAT IP address is being deleted. In this state, you can only perform query operations.\n\n- If the NAT IP address cannot be found, the NAT IP address is deleted."
  },
  "example": {
    "unifiedCli": "aliyun vpc delete-nat-ip --biz-region-id cn-hangzhou --nat-ip-id vpcnatip-gw8y7q3cpk3fggs870000 --nat-gateway-id ngw-gw8v16wgvtq26vh590000 --ipv4-prefix 192.168.0.0/28 --dry-run false --client-token 5A2CFF0E-5718-45B5-9D4D-70B3FF3898 --region cn-hangzhou",
    "legacyCli": "aliyun vpc DeleteNatIp --region cn-hangzhou --NatIpId 'vpcnatip-gw8y7q3cpk3fggs870000' --DryRun false --RegionId 'cn-hangzhou' --ClientToken '5A2CFF0E-5718-45B5-9D4D-70B3FF3898' --NatGatewayId 'ngw-gw8v16wgvtq26vh590000' --Ipv4Prefix '192.168.0.0/28'"
  }
}