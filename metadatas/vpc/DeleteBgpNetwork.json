{
  "name": "DeleteBgpNetwork",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request."
      }
    },
    {
      "name": "DstCidrBlock",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要和IDC互联的VPC或交换机的网段。",
        "en": "The CIDR block of the virtual private cloud (VPC) or vSwitch that you want to connect to a data center."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "BGP组所在的地域。 \n\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the BGP group.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list."
      }
    },
    {
      "name": "RouterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "边界路由器ID。",
        "en": "The ID of the VBR."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除已宣告的BGP网络",
    "en": "DeleteBgpNetwork"
  },
  "example": {
    "unifiedCli": "aliyun vpc delete-bgp-network --biz-region-id cn-hangzhou --dst-cidr-block 10.110.192.12/32 --router-id vrt-bp1lhl0taikrteen80000 --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun vpc DeleteBgpNetwork --region cn-hangzhou --DstCidrBlock '10.110.192.12/32' --RouterId 'vrt-bp1lhl0taikrteen80000' --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}