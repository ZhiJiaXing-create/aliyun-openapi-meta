{
  "name": "DescribeVpnRouteEntries",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "列表的页码。默认值：**1**。",
        "en": "The page number of the list. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页的行数。最大值：**50**。默认值：**10**。",
        "en": "The number of entries per page in a paged query. Maximum value: **50**. Default value: **10**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "VPN网关实例所在的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the VPN gateway instance.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the region ID."
      }
    },
    {
      "name": "RouteEntryType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "路由条目的类型。取值：\n\n- **custom**（默认值）：表示查询VPN网关实例的目的路由。\n- **bgp**：表示查询VPN网关实例的BGP路由。",
        "en": "The destination type of the routing entry. Valid values:\n\n- **custom** (default): queries the destination routes of the VPN gateway instance.\n- **bgp**: queries the BGP routes of the VPN gateway instance."
      }
    },
    {
      "name": "VpnGatewayId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "VPN网关的实例ID。",
        "en": "The instance ID of the VPN gateway."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询目的路由和BGP路由",
    "en": "Query destination routes and BGP routes"
  },
  "example": {
    "unifiedCli": "aliyun vpc describe-vpn-route-entries --biz-region-id cn-hangzhou --vpn-gateway-id vpn-bp1cmw7jh1nfe43m90000 --page-number 1 --page-size 10 --route-entry-type custom --region cn-hangzhou",
    "legacyCli": "aliyun vpc DescribeVpnRouteEntries --region cn-hangzhou --PageSize 10 --VpnGatewayId 'vpn-bp1cmw7jh1nfe43m90000' --PageNumber 1 --RouteEntryType custom --RegionId 'cn-hangzhou'"
  }
}