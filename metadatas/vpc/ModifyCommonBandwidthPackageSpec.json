{
  "name": "ModifyCommonBandwidthPackageSpec",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Bandwidth",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "共享带宽实例的带宽峰值。单位：Mbps。 \n\n<props=\"china\">\n\n - 当共享带宽的计费方式为按带宽计费时，**Bandwidth**的默认取值范围为**2**~**20000**。\n-  当共享带宽的计费方式为按增强型95计费时，**Bandwidth**的默认取值范围为**200**~**20000**。\n- 当共享带宽的计费方式为按主流量计费时，**Bandwidth**的默认取值范围为**1**~**2000**。\n\n</props>\n<props=\"intl\">取值范围：**1**~**1000**。</props>",
        "en": "The peak bandwidth of the Internet Shared Bandwidth instance. Unit: Mbit/s. \n\n<props=\"china\">\n\n - If the billing method of the Internet Shared Bandwidth instance is pay-by-bandwidth, the default value range of **Bandwidth** is **2** to **20000**.\n-  If the billing method of the Internet Shared Bandwidth instance is pay-by-95th-percentile-bandwidth, the default value range of **Bandwidth** is **200** to **20000**.\n- If the billing method of the Internet Shared Bandwidth instance is pay-by-traffic, the default value range of **Bandwidth** is **1** to **2000**.\n\n</props>\n<props=\"intl\">Valid values: **1** to **1000**.</props>"
      }
    },
    {
      "name": "BandwidthPackageId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "共享带宽实例的ID。",
        "en": "The ID of the Internet Shared Bandwidth instance."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "共享带宽实例所在的地域ID。 \n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the Internet Shared Bandwidth instance. \n\nYou can call [DescribeRegions](~~36063~~) to query the region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改共享带宽的带宽峰值",
    "en": "Modify the peak bandwidth of an internet shared bandwidth instance"
  },
  "descriptions": {
    "zh": "在调用本接口时，请了解以下内容：\n- **ModifyCommonBandwidthPackageSpec**接口属于异步接口，即系统会先返回一个请求ID，但共享带宽的带宽峰值并未修改完成，系统后台的修改任务仍在进行。您可以调用[DescribeCommonBandwidthPackages](~~120309~~)查询共享带宽实例的状态。\n    - 当共享带宽实例处于**Modifying**状态时，表示共享带宽的带宽峰值正在修改中，在该状态下，您只能执行查询操作，不能执行其他操作。\n    - 当共享带宽实例处于**Available**状态时，表示共享带宽的带宽峰值修改完成。\n- **ModifyCommonBandwidthPackageSpec**接口不支持并发修改同一个共享带宽的带宽峰值。",
    "en": "Before you call this operation, take note of the following information:\n- **ModifyCommonBandwidthPackageSpec** is an asynchronous operation. After you send a request, the system returns a request ID, but the peak bandwidth of the Internet Shared Bandwidth instance has not been modified yet. The modification task is still running in the background. You can call [DescribeCommonBandwidthPackages](~~120309~~) to query the status of the Internet Shared Bandwidth instance.\n    - If the Internet Shared Bandwidth instance is in the **Modifying** state, the peak bandwidth is being modified. In this state, you can only perform query operations and cannot perform other operations.\n    - If the Internet Shared Bandwidth instance is in the **Available** state, the peak bandwidth has been modified.\n- **ModifyCommonBandwidthPackageSpec** does not support concurrent modifications to the peak bandwidth of the same Internet Shared Bandwidth instance."
  },
  "example": {
    "unifiedCli": "aliyun vpc modify-common-bandwidth-package-spec --biz-region-id cn-hangzhou --bandwidth-package-id cbwp-2ze2ic1xd2qeqk1450000 --bandwidth 1000 --region cn-hangzhou",
    "legacyCli": "aliyun vpc ModifyCommonBandwidthPackageSpec --region cn-hangzhou --Bandwidth 1000 --BandwidthPackageId 'cbwp-2ze2ic1xd2qeqk1450000' --RegionId 'cn-hangzhou'"
  }
}