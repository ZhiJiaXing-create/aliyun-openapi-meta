{
  "name": "CreateNatGateway",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccessMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "反向访问VPC NAT网关的访问模式。",
        "en": "The access mode of the VPC NAT gateway for reverse endpoint access."
      }
    },
    {
      "name": "AutoPay",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "包年包月公网NAT网关已停止新购，该参数不再使用。",
        "en": "Subscription-based public NAT gateways are no longer available for purchase. This parameter is deprecated."
      }
    },
    {
      "name": "AvailabilityMode",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can create the token, but you must make sure that the token is unique among different requests.\n\n> If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "NAT网关的描述。\n\n描述可以为空；或填写2~256个字符，不能以`http://`或`https://`开头。",
        "en": "The description of the NAT gateway.\n\nThe description must be 2 to 256 characters in length. It cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "Duration",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "包年包月公网NAT网关已停止新购，该参数不再使用。",
        "en": "Subscription-based public NAT gateways are no longer available for purchase. This parameter is deprecated."
      }
    },
    {
      "name": "EipBindMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "NAT网关的EIP绑定模式，取值：\n- **MULTI_BINDED**（默认值）：多EIP网卡可见模式。\n- **NAT**：EIP普通模式，兼容IPv4网关。\n  > NAT网关的EIP绑定模式为EIP普通模式时，EIP将占用NAT网关所在交换机的一个私网IP，请确保NAT网关所在交换机内私网IP地址充足，如果NAT网关所在的交换机没有可用的空闲私网地址时，将无法绑定新的EIP。NAT网关的EIP绑定模式为EIP普通模式时，NAT网关支持绑定的EIP数量上限为50个。",
        "en": "The mode in which the EIP is associated with the NAT gateway. Valid values:\n\n- **MULTI\\_BINDED** (default): the multi-EIP-to-ENI mode.\n\n- **NAT**: the EIP-to-NAT gateway mode. This mode is compatible with IPv4 gateways.\n\n  > If the EIP is associated with the NAT gateway in EIP-to-NAT gateway mode, the EIP occupies a private IP address of the vSwitch to which the NAT gateway belongs. Make sure that the vSwitch has sufficient private IP addresses. Otherwise, the EIP fails to be associated. In EIP-to-NAT gateway mode, a NAT gateway can be associated with up to 50 EIPs."
      }
    },
    {
      "name": "IcmpReplyEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启ICMP代回。取值：\n\n- **true**（默认值）：开启。\n- **false**：不开启。",
        "en": "Specifies whether to enable ICMP reply. Valid values:\n\n- **true** (default): enables ICMP reply.\n\n- **false**: disables ICMP reply."
      }
    },
    {
      "name": "InstanceChargeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "NAT网关的付费模式，取值：\n\n**PostPaid**（默认值）：按量付费。\n          \n更多信息，请参见[公网NAT网关计费](~~48126~~)和[VPC NAT网关计费](~~270913~~)。",
        "en": "The billing method of the NAT gateway. Set the value to:\n\n**PostPaid** (default): pay-as-you-go.\n\nFor more information, see [Billing of public NAT gateways](~~48126~~) and [Billing of VPC NAT gateways](~~270913~~)."
      }
    },
    {
      "name": "InternetChargeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "NAT网关的计费类型，取值：**PayByLcu**，表示按使用量计费。",
        "en": "The billing method of the NAT gateway. Set the value to **PayByLcu**, which indicates that the NAT gateway is a pay-as-you-go NAT gateway and is measured in LCUs."
      }
    },
    {
      "name": "Ipv4Prefix",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "创建IP前缀地址段，用于批量创建NAT IP。请使用NAT所在交换机中未分配的预留网段。",
        "en": "The IP address prefix. NAT IP addresses are created from the prefix. Use a reserved CIDR block that is not allocated in the vSwitch to which the NAT gateway belongs."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "NAT网关的名称。\n\n名称长度为2~128个字符，以大小写字母或中文开头，可包含数字、下划线（_）和短划线（-）。\n\n如果没有指定该参数，系统会为NAT网关创建默认的名称。",
        "en": "The name of the NAT gateway.\n\nMust be 2 to 128 characters in length, start with a letter or a Chinese character, and can contain digits, underscores (\\_), and hyphens (-).\n\nIf you do not specify this parameter, the system automatically specifies a name for the NAT gateway."
      }
    },
    {
      "name": "NatIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "NAT网关占用的私网IP地址。请使用NAT所在交换机网段中未分配的IP，当为空时会随机分配IP地址。",
        "en": "The private IP address of the NAT gateway. Use an idle IP address from the CIDR block of the vSwitch to which the NAT gateway belongs. If this parameter is left empty, an IP address is randomly assigned."
      }
    },
    {
      "name": "NatType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "NAT网关的类型，取值：**Enhanced**，增强型NAT网关。",
        "en": "The type of NAT gateway. Set the value to **Enhanced**, which specifies an enhanced NAT gateway."
      }
    },
    {
      "name": "NetworkType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "创建的NAT网关类型，取值：\n\n- **internet**：公网NAT网关。\n\n- **intranet**：VPC NAT网关。",
        "en": "The type of the NAT gateway to be created. Valid values:\n\n- **internet**: a public NAT gateway\n\n- **intranet**: a VPC NAT gateway"
      }
    },
    {
      "name": "PricingCycle",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "包年包月公网NAT网关已停止新购，该参数不再使用。",
        "en": "Subscription-based public NAT gateways are no longer available for purchase. This parameter is no longer used."
      }
    },
    {
      "name": "PrivateLinkEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启私网连接。取值：\n\n- true：开启私网连接。\n\n- false（默认值）：不开启私网连接。",
        "en": "Specifies whether to enable PrivateLink. Valid values:\n\n- true: enables PrivateLink.\n\n- false (default): disables PrivateLink."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "NAT网关所在的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region in which to create the NAT gateway.\n\nCall the [DescribeRegions](~~36063~~) operation to obtain the region ID."
      }
    },
    {
      "name": "SecurityProtectionEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启防火墙功能，取值：\n\n- **false**（默认值）：不开启。\n><notice> 该参数已弃用。></notice>",
        "en": "Specifies whether to enable the firewall feature. Valid values:\n\n- **false** (default): disables the firewall feature.\n\n  ><notice>\n\n  This parameter is deprecated.\n\n  ></notice>"
      }
    },
    {
      "name": "Spec",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "包年包月公网NAT网关已停止新购，该参数不再使用。",
        "en": "Subscription-based public NAT gateways are no longer available for purchase. This parameter is deprecated."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表。",
        "en": "The tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签键。API调用时使用Tag.N.Key的形式，N的取值范围：1~20。一旦传入该值，则不允许为空字符串。最多支持128个字符，不能以aliyun或acs:开头，不能包含http://或https://。",
            "en": "The tag key. You can specify up to 20 tag keys. The tag key cannot be an empty string. The tag key must be 1 to 128 characters in length and cannot start with `aliyun` or `acs:`. The tag key cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签值。API调用时使用Tag.N.Value的形式，N的取值范围：1~20。一旦传入该值，则不允许为空字符串。最多支持128个字符，不能以aliyun或acs:开头，不能包含http://或https://。",
            "en": "The tag value. You can specify up to 20 tag values. The tag value can be an empty string. The tag value must be 0 to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`."
          }
        }
      ]
    },
    {
      "name": "VSwitchId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "NAT网关所属的交换机的ID。\n\n创建NAT网关时，您必须指定NAT网关所属的交换机，系统会为NAT网关分配一个交换机内的空闲私网IP地址。\n\n- 如果您要在存量交换机中创建NAT网关，请确保交换机所属的可用区支持创建NAT网关，且交换机有可用的IP。\n\n- 如果您还未创建交换机，请先在支持创建NAT网关的可用区创建交换机，然后再指定NAT网关所属的交换机。\n\n> 您可以通过[ListEnhanhcedNatGatewayAvailableZones](~~182292~~)接口查询NAT网关的资源可用区，通过[DescribeVSwitches](~~35748~~)接口查询交换机中的可用IP数。",
        "en": "The ID of the vSwitch to which the NAT gateway belongs.\n\nWhen you create a NAT gateway, you must specify a vSwitch to which the NAT gateway belongs. The system then assigns a private IP address to the NAT gateway from the vSwitch.\n\n- To create a NAT gateway in an existing vSwitch, make sure that the zone to which the vSwitch belongs supports NAT gateways and that the vSwitch has idle IP addresses.\n\n- If you have not created a vSwitch, create a vSwitch in a zone that supports NAT gateways and then specify the vSwitch.\n\n> Call the [ListEnhancedNatGatewayAvailableZones](~~182292~~) operation to query available zones and [DescribeVSwitches](~~35748~~) to query the number of idle IP addresses in a vSwitch."
      }
    },
    {
      "name": "VpcId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要创建NAT网关的VPC的ID。",
        "en": "The ID of the virtual private cloud (VPC) where you want to create the NAT gateway."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建NAT网关",
    "en": "CreateNatGateway"
  },
  "descriptions": {
    "zh": "在调用本接口时，请了解以下内容：\n\n- 首次创建NAT网关时，系统会自动创建一个名称为AliyunServiceRoleForNatgw的服务关联角色，并且为该角色添加名称为AliyunServiceRolePolicyForNatgw的权限策略，授予NAT网关拥有访问其他云资源的权限。更多信息，请参见[服务关联角色](~~174251~~)。\n\n- 增强型公网NAT网关创建后，系统会在VPC的路由表中自动添加一条目标网段为0.0.0.0/0，下一跳为NAT网关的路由条目，用于将流量路由到NAT网关。\n\n- **CreateNatGateway**接口属于异步接口，即系统会先返回一个NAT网关（公网NAT网关或VPC NAT网关）实例ID，但该NAT网关实例并未创建完成，系统后台的创建任务仍在进行。您可以调用[DescribeNatGateways](~~36054~~)查询NAT网关的状态。\n\n    - 当NAT网关处于**Creating**状态时，表示NAT网关正在创建中，在该状态下，您只能执行查询操作，不能执行其他操作。\n\n    - 当NAT网关处于**Available**状态时，表示NAT网关创建完成。\n \n NAT网关创建一般需要1~3分钟，请您耐心等待。",
    "en": "Before you call this operation, note the following:\n\n- When you create a NAT gateway for the first time, a service-linked role named AliyunServiceRoleForNatgw is automatically created. A permission policy named AliyunServiceRolePolicyForNatgw is attached to this role to allow the NAT gateway to access other cloud resources. For more information, see [Service-linked roles](~~174251~~).\n\n- After an enhanced public NAT gateway is created, the system automatically adds a route entry to the VPC's route table to route traffic to the NAT gateway. This entry sets the destination CIDR block to 0.0.0.0/0 and the next hop to the NAT gateway.\n\n- The **CreateNatGateway** operation is asynchronous. After you send a request, the system returns a NAT gateway (public or VPC) instance ID while the creation task runs in the background. Call the [DescribeNatGateways](~~36054~~) operation to query the status of the NAT gateway.\n\n  - A status of **Creating** indicates that the NAT gateway is being created. You can perform only query operations.\n\n  - A status of **Available** indicates that the NAT gateway has been created.\n\nCreating a NAT gateway takes about 1 to 3 minutes."
  },
  "example": {
    "unifiedCli": "aliyun vpc create-nat-gateway --biz-region-id cn-hangzhou --vpc-id vpc-bp1di7uewzmtvfuq80000 --name fortest --description testnat --client-token 5A2CFF0E-5718-45B5-9D4D-70B3FF3898 --spec '无效参数' --instance-charge-type PostPaid --pricing-cycle '无效参数' --duration '无效参数' --auto-pay false --vswitch-id vsw-bp1e3se98n9fq8hle0000 --nat-type Enhanced --internet-charge-type PayByLcu --network-type internet --security-protection-enabled false --icmp-reply-enabled true --private-link-enabled false --eip-bind-mode MULTI_BINDED --tag Value=TestValue Key=TestKey --access-mode MULTI_BINDED --nat-ip 192.168.0.2 --ipv4-prefix 192.168.0.0/28 --availability-mode example-string --region cn-hangzhou",
    "legacyCli": "aliyun vpc CreateNatGateway --region cn-hangzhou --Tag.1.Key TestKey --Description testnat --InstanceChargeType PostPaid --PricingCycle '无效参数' --EipBindMode MULTI_BINDED --VSwitchId 'vsw-bp1e3se98n9fq8hle0000' --PrivateLinkEnabled false --SecurityProtectionEnabled false --Duration '无效参数' --AutoPay false --NatType Enhanced --Name fortest --InternetChargeType PayByLcu --NatIp '192.168.0.2' --VpcId 'vpc-bp1di7uewzmtvfuq80000' --NetworkType internet --AvailabilityMode 'example-string' --Tag.1.Value TestValue --IcmpReplyEnabled true --RegionId 'cn-hangzhou' --AccessMode MULTI_BINDED --ClientToken '5A2CFF0E-5718-45B5-9D4D-70B3FF3898' --Spec '无效参数' --Ipv4Prefix '192.168.0.0/28'"
  }
}