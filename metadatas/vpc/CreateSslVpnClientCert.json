{
  "name": "CreateSslVpnClientCert",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端证书的名称。\n\n长度为1～100个字符，不能以`http://`或`https://`开头。",
        "en": "The name of the client certificate.\n\nThe name must be 1 to 100 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "VPN网关所在的地域ID。 \n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the VPN gateway. \n\nYou can call the [DescribeRegions](~~36063~~) operation to query the region ID."
      }
    },
    {
      "name": "SslVpnServerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "SSL-VPN服务端的ID。",
        "en": "The ID of the SSL-VPN server."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建SSL-VPN客户端证书",
    "en": "Create an SSL-VPN client certificate"
  },
  "descriptions": {
    "zh": "创建SSL-VPN客户端证书前，请确保VPN网关实例下已经创建了SSL-VPN服务端。具体操作，请参见[CreateSslVpnServer](~~2794075~~)。",
    "en": "Before you create an SSL-VPN client certificate, make sure that an SSL-VPN server has been created for the VPN gateway instance. For more information, see [CreateSslVpnServer](~~2794075~~)."
  },
  "example": {
    "unifiedCli": "aliyun vpc create-ssl-vpn-client-cert --client-token 02fb3da4-130e-11e9-8e44-0016e04115b --biz-region-id cn-hangzhou --ssl-vpn-server-id vss-m5et0q3iy1qex328w0000 --name SslVpnClientCert1 --region cn-hangzhou",
    "legacyCli": "aliyun vpc CreateSslVpnClientCert --region cn-hangzhou --SslVpnServerId 'vss-m5et0q3iy1qex328w0000' --RegionId 'cn-hangzhou' --ClientToken '02fb3da4-130e-11e9-8e44-0016e04115b' --Name SslVpnClientCert1"
  }
}