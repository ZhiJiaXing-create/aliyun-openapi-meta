{
  "name": "ModifyVpnGatewayAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPropagate",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启VPN网关的路由自动传播功能。取值：\n\n- **true**：开启。\n\n    开启路由自动传播后，VPN网关实例会自动学习VPC实例系统路由表中的系统路由，并向VPC实例系统路由表自动传播本地数据中心的路由。\n- **false**：关闭。\n    \n    关闭路由自动传播前，请确保VPN网关下的所有IPsec连接已关闭BGP动态路由功能。",
        "en": "Specifies whether to enable routing automatic propagation for the VPN gateway. Valid values:\n\n- **true**: enabled.\n\n    After routing automatic propagation is enabled, the VPN gateway instance performs automatic learning of system routes from the system route table of the VPC-connected instance and automatically propagates routes of the on-premises data center to the system route table of the VPC-connected instance.\n- **false**: disabled.\n    \n    Before you disable routing automatic propagation, make sure that the BGP dynamic route feature is disabled for all IPsec-VPN connections under the VPN gateway."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "VPN网关新的描述信息。  \n \n描述信息长度为1～100个字符。",
        "en": "The new description of the VPN gateway.  \n \nThe description must be 1 to 100 characters in length."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "VPN网关的新名称。  \n\n长度为2～100个字符，不能以`http://`或`https://`开头，需要以大写或小写字母开头，后面可包含大小写字母、数字、下划线（_）、短划线（-）和半角句号（.），不支持其他特殊字符。",
        "en": "The new name of the VPN gateway.  \n\nThe name must be 2 to 100 characters in length and cannot start with `http://` or `https://`. It must start with an uppercase or lowercase letter and can contain uppercase and lowercase letters, digits, underscores (\\_), hyphens (-), and periods (.). Other special characters are not supported."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "VPN网关实例所在的地域ID。您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the VPN gateway instance. You can call [DescribeRegions](~~36063~~) to query the most recent region list."
      }
    },
    {
      "name": "VpnGatewayId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "VPN网关实例的ID。",
        "en": "The ID of the VPN gateway instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改VPN网关的名称、描述信息或路由自动传播功能",
    "en": "Modify the name, description, or routing automatic propagation of a VPN gateway"
  },
  "descriptions": {
    "zh": "- **ModifyVpnGatewayAttribute**接口属于异步接口，即系统先返回VPN网关的信息，但该VPN网关的配置尚未修改成功，系统后台的修改任务仍在进行。您可以调用[DescribeVpnGateway](~~73720~~)查询VPN网关的配置的修改状态：\n    - 当VPN网关处于**updating**状态时，表示VPN网关的配置正在修改中。\n    - 当VPN网关处于**active**状态时，表示VPN网关的配置修改成功。\n- **ModifyVpnGatewayAttribute**接口不支持并发修改同一个VPN网关的配置。",
    "en": "- **ModifyVpnGatewayAttribute** is an asynchronous operation. After you call this operation, the system returns the VPN gateway information, but the configuration has not been modified yet. The modification task is still running in the background. You can call [DescribeVpnGateway](~~73720~~) to query the modification status of the VPN gateway configuration:\n    - If the VPN gateway is in the **updating** state, the configuration is being modified.\n    - If the VPN gateway is in the **active** state, the configuration has been modified.\n- **ModifyVpnGatewayAttribute** does not support concurrent modifications to the configuration of the same VPN gateway."
  },
  "example": {
    "unifiedCli": "aliyun vpc modify-vpn-gateway-attribute --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-42660000 --vpn-gateway-id vpn-bp1q8bgx4xnkm2ogj0000 --name myvpn --description test --auto-propagate true --region cn-hangzhou",
    "legacyCli": "aliyun vpc ModifyVpnGatewayAttribute --region cn-hangzhou --Description test --VpnGatewayId 'vpn-bp1q8bgx4xnkm2ogj0000' --AutoPropagate true --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-42660000' --Name myvpn"
  }
}