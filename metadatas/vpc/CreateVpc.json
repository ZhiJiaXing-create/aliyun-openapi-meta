{
  "name": "CreateVpc",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CidrBlock",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "VPC的网段。\n\n- 建议您使用RFC 1918标准中指定的私有IPv4地址作为VPC的主IPv4网段，网段掩码推荐使用16~28位。例如：10.0.0.0/16、172.16.0.0/16、192.168.0.0/16。\n- 您也可以使用除100.64.0.0/10、224.0.0.0/4、127.0.0.0/8或169.254.0.0/16及其子网外的自定义地址段作为专有网络的主IPv4网段。",
        "en": "The CIDR block of the VPC.\n\n- We recommend that you use an IPv4 address specified in RFC 1918 as the primary IPv4 CIDR block of the virtual private cloud (VPC). The subnet mask must be 16 to 28 bits in length. Examples: 10.0.0.0/16, 172.16.0.0/16, and 192.168.0.0/16.\n- You can also use a custom CIDR block other than 100.64.0.0/10, 224.0.0.0/4, 127.0.0.0/8, 169.254.0.0/16, or their subnets as the primary IPv4 CIDR block of the virtual private cloud (VPC)."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "VPC的描述信息。\n\n长度为1～256个字符，不能以`http://`或`https://`开头。",
        "en": "The description of the VPC.\n\nThe description must be 1 to 256 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n- **true**：发送检查请求，不会创建VPC。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接创建VPC。",
        "en": "Specifies whether to perform a dry run. Valid values:\n- **true**: performs a dry run without creating the VPC. The system checks the required parameters, request format, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n- **false** (default): performs a Normal request and sends the request. If the request passes the dry run, an HTTP 2xx status code is returned and the system proceeds to create a VPC."
      }
    },
    {
      "name": "EnableDnsHostname",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否启用DNS主机名功能。取值：\n\n- **false**（默认值）：不开启。\n- **true**：开启。",
        "en": "Specifies whether to enable the DNS hostname feature. Valid values:\n\n- **false** (default): disabled.\n- **true**: enabled."
      }
    },
    {
      "name": "EnableIpv6",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启IPv6。取值：\n\n- **false**（默认值）：不开启。\n- **true**：开启。",
        "en": "Specifies whether to enable IPv6. Valid values:\n\n- **false** (default): disabled.\n- **true**: enabled."
      }
    },
    {
      "name": "Ipv4CidrMask",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "通过输入掩码的方式从IPAM地址池分配VPC。\n\n> 指定IPAM地址池创建VPC时，参数CidrBlock或Ipv4CidrMask至少输入一个。",
        "en": "The subnet mask used to allocate a CIDR block from the IPAM pool to the VPC.\n\n> When you create a VPC by specifying an IPAM pool, you must specify at least one of CidrBlock or Ipv4CidrMask."
      }
    },
    {
      "name": "Ipv4IpamPoolId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IPv4类型的IPAM地址池实例ID。",
        "en": "The instance ID of the IPv4 IPAM pool instance."
      }
    },
    {
      "name": "Ipv6CidrBlock",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "VPC的IPv6网段。为VPC开启IPv6时，系统将分配IPv6网段。如需指定IPv6网段，您需要先调用[AllocateVpcIpv6Cidr](~~448916~~)接口预留指定的IPv6地址段，并将其传入。",
        "en": "The IPv6 CIDR block of the VPC. When you enable IPv6 for the VPC, the system will assign an IPv6 CIDR block. To specify an IPv6 CIDR block, you must first invoke [AllocateVpcIpv6Cidr](~~448916~~) to reserve the IPv6 CIDR block, and then pass it in."
      }
    },
    {
      "name": "Ipv6CidrMask",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "通过输入掩码的方式从IPAM地址池为VPC添加IPv6网段。",
        "en": "The subnet mask used to allocate an IPv6 CIDR block from the IPAM pool to the VPC."
      }
    },
    {
      "name": "Ipv6IpamPoolId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IPv6类型的IPAM地址池实例ID。",
        "en": "The instance ID of the IPv6 IPAM pool instance."
      }
    },
    {
      "name": "Ipv6Isp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "VPC的IPv6地址段类型。取值：\n\n- **BGP**（默认值）：阿里云BGP IPv6。\n- **ChinaMobile**：中国移动（单线）。\n- **ChinaUnicom**：中国联通（单线）。\n- **ChinaTelecom**：中国电信（单线）。\n\n> 如果是开通了单线带宽白名单的用户，该字段可以设置为**ChinaTelecom**（中国电信）、**ChinaUnicom**（中国联通）或**ChinaMobile**（中国移动）。",
        "en": "The type of the IPv6 CIDR block of the VPC. Valid values:\n\n- **BGP** (default): Alibaba Cloud BGP IPv6.\n- **ChinaMobile**: China Mobile (single ISP).\n- **ChinaUnicom**: China Unicom (single ISP).\n- **ChinaTelecom**: China Telecom (single ISP).\n\n> If your account is included in the China Telecom single-ISP bandwidth whitelist, you can set this parameter to **ChinaTelecom** (China Telecom), **ChinaUnicom** (China Unicom), or **ChinaMobile** (China Mobile)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "VPC所在的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the VPC.\n\nYou can call [DescribeRegions](~~36063~~) to query the most recent region list."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。\n\n关于资源组的更多信息，请参见[什么是资源组](~~2381067~~)。",
        "en": "The resource group ID.\n\nFor more information about resource groups, see [What is a resource group?](~~2381067~~)."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源的标签。",
        "en": "The tags of the resource."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。最多支持输入20个标签键。如需传入该值，则不能输入空字符串。\n\n一个标签键最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag key of the resource. You can specify up to 20 tag keys. The tag key cannot be an empty string.\n\nThe tag key can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。最多支持输入20个标签值。如需传入该值，可以输入空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag value of the resource. You can specify up to 20 tag values. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`."
          }
        }
      ]
    },
    {
      "name": "UserCidr",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户网段，如需定义多个网段请使用半角逗号（,）隔开，最多支持3个网段。\n\n关于用户网段的详细信息，请参见[专有网络FAQ](~~185311~~)的`什么是用户网段`。",
        "en": "The user CIDR block. Separate multiple CIDR blocks with commas (,). You can specify up to three CIDR blocks.\n\nFor more information about user CIDR blocks, see the `What is a user CIDR block?` section in [virtual private cloud (VPC) FAQ](~~185311~~)."
      }
    },
    {
      "name": "VpcName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "VPC的名称。\n\n长度为1～128个字符，不能以`http://`或`https://`开头。",
        "en": "The name of the VPC.\n\nThe name must be 1 to 128 characters in length and cannot start with `http://` or `https://`."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建一个VPC",
    "en": "Create a VPC"
  },
  "descriptions": {
    "zh": "调用该接口创建VPC时，请注意：\n\n- 一个 VPC 只能指定一个 IPv4 主网段，您可以添加 IPv4 附加网段来扩充网段。\n\n- VPC 创建后，您可以在原 IPv4 网段内放大或缩小网段。当您缩小 VPC 的 IPv4 网段时，如果有已经使用的 IP 地址在目标网段外，则会修改失败。\n\n- 每个 VPC 支持云资源使用的私网网络地址数量为 300,000 个，且无法提升配额。\n  \n- VPC 创建后，会自动创建一个路由器和一个路由表。\n  \n- 每个 VPC 支持三个用户侧网段。如果多个用户侧网段之间存在包含关系，掩码较短的网段实际生效。例如10.0.0.0/16和10.0.0.0/24中，10.0.0.0/16实际生效。  \n\n- **CreateVpc**接口属于异步接口，即系统会返回一个实例ID，但该VPC尚未创建完成，系统后台的创建任务仍在进行。您可以调用[DescribeVpcAttribute](~~94565~~)查询VPC的创建状态：\n    - 当VPC处于**Creating**状态时，表示VPC正在创建中。\n    - 当VPC处于**Created**状态时，表示VPC创建完成。\n- **CreateVpc**接口支持在同一地域内并发创建非默认VPC，受[配额](~~27750~~)约束。",
    "en": "When you call this operation to create a VPC, take note of the following items:\n\n- A VPC can have only one primary IPv4 CIDR block. You can add IPv4 secondary CIDR blocks to expand the address space.\n\n- After a VPC is created, you can expand or shrink the original IPv4 CIDR block. If IP addresses within the current CIDR block are already in use and fall outside the target CIDR block, the modification fails.\n\n- Each VPC supports up to 300,000 private network IP addresses for cloud resources. This quota cannot be upgraded.\n  \n- After a VPC is created, a vRouter and a route table are automatically created.\n  \n- Each VPC supports up to three user CIDR blocks. If user CIDR blocks overlap, the CIDR block with the shorter mask takes effect. For example, if both 10.0.0.0/16 and 10.0.0.0/24 are specified, 10.0.0.0/16 takes effect.\n\n- **CreateVpc** is an asynchronous operation. After you invoke a request, the system returns an instance ID but the VPC is not yet created. The node for creating the VPC runs in the background. You can invoke [DescribeVpcAttribute](~~94565~~) to query the creation status of the VPC:\n    - If the VPC is in the **Creating** state, the VPC is being created.\n    - If the VPC is in the **Created** state, the VPC is created.\n- **CreateVpc** supports concurrent creation of non-default VPCs in the same region, subject to [quota](~~27750~~) limits."
  },
  "example": {
    "unifiedCli": "aliyun vpc create-vpc --biz-region-id cn-hangzhou --cidr-block 172.16.0.0/12 --ipv6-cidr-block 2408:XXXX:0:6a::/56 --vpc-name abc --enable-ipv6 false --description This_is_my_first_Vpc --resource-group-id rg-acfmxazb4ph6aiy0000 --tag Value=FinanceJoshua Key=FinanceDept --dry-run false --user-cidr 192.168.0.0/12 --client-token 123e4567-e89b-12d3-a456-426655440000 --ipv6-isp BGP --ipv4-ipam-pool-id ipam-pool-sycmt3p2a9v63i0000 --ipv4-cidr-mask 12 --enable-dns-hostname false --ipv6-ipam-pool-id ipam-pool-bp1aq51kkfh477z030000 --ipv6-cidr-mask 56 --region cn-hangzhou",
    "legacyCli": "aliyun vpc CreateVpc --region cn-hangzhou --Ipv6Isp BGP --Tag.1.Key FinanceDept --Description This_is_my_first_Vpc --ResourceGroupId 'rg-acfmxazb4ph6aiy0000' --Ipv4CidrMask 12 --DryRun false --CidrBlock '172.16.0.0/12' --Ipv6CidrMask 56 --EnableIpv6 false --UserCidr '192.168.0.0/12' --Ipv6IpamPoolId 'ipam-pool-bp1aq51kkfh477z030000' --Tag.1.Value FinanceJoshua --Ipv4IpamPoolId 'ipam-pool-sycmt3p2a9v63i0000' --VpcName abc --RegionId 'cn-hangzhou' --Ipv6CidrBlock '2408:XXXX:0:6a::/56' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --EnableDnsHostname false"
  }
}