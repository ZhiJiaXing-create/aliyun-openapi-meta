{
  "name": "CreateDefaultVpc",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The ClientToken value can contain only ASCII characters.\n\n> If you do not specify this parameter, the system uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request."
      }
    },
    {
      "name": "EnableIpv6",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启IPv6网段。取值：\n\n- **false**（默认值）：不开启。\n- **true**：开启。",
        "en": "Specifies whether to enable the IPv6 CIDR block. Valid values:\n\n- **false** (default): does not enable the IPv6 CIDR block.\n- **true**: enables the IPv6 CIDR block."
      }
    },
    {
      "name": "Ipv6CidrBlock",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "默认VPC的IPv6网段。\n\n> 当**EnableIpv6**取值为**true**时，需要输入该参数。",
        "en": "The IPv6 CIDR block of the default VPC.\n\n> This parameter is required when **EnableIpv6** is set to **true**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "默认VPC所在的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the default VPC.\n\nYou can call [DescribeRegions](~~36063~~) to query the most recent region list."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建一个默认的VPC",
    "en": "Create a default VPC"
  },
  "descriptions": {
    "zh": "调用该接口创建默认VPC时，请注意：\n\n- 默认VPC创建后无法修改网段，但可以添加附加IPv4网段。  \n\n- 每个默认VPC支持云资源使用的私网网络地址数量为300,000个，且无法提升配额。\n  \n- 创建默认VPC后，会自动创建一个路由器和一个路由表。\n  \n- 每个默认VPC支持三个用户侧网段。如果多个用户侧网段之间存在包含关系，掩码较短的网段实际生效。例如10.0.0.0/16和10.0.0.0/24中，10.0.0.0/16实际生效。\n\n- **CreateDefaultVpc**接口属于异步接口，即系统会返回一个实例ID，但该默认VPC尚未创建完成，系统后台的创建任务仍在进行。您可以调用[DescribeVpcAttribute](~~94565~~)查询默认VPC的创建状态：\n    - 当默认VPC处于**Pending**状态时，表示默认VPC正在配置中。\n    - 当默认VPC处于**Available**状态时，表示默认VPC可用。\n- **CreateDefaultVpc**接口不支持在同一地域内并发创建默认VPC。\n- 一个地域当前只能有一个默认VPC。",
    "en": "When you call this operation to create a default VPC, note the following items:\n\n- After a default VPC is created, you cannot modify its CIDR block, but you can add secondary IPv4 CIDR blocks.\n\n- Each default VPC supports up to 300,000 private network IP addresses for cloud resources, and this quota cannot be upgraded.\n  \n- After a default VPC is created, a vRouter and a route table are automatically created.\n  \n- Each default VPC supports up to three user CIDR blocks. If multiple user CIDR blocks overlap, the CIDR block with the shorter mask takes effect. For example, if both 10.0.0.0/16 and 10.0.0.0/24 exist, 10.0.0.0/16 takes effect.\n\n- The **CreateDefaultVpc** operation is asynchronous. After you send a request, the system returns an instance ID while the default VPC is still being created in the background. You can call [DescribeVpcAttribute](~~94565~~) to query the creation status of the default VPC:\n    - If the default VPC is in the **Pending** state, the default VPC is being configured.\n    - If the default VPC is in the **Available** state, the default VPC is available.\n- The **CreateDefaultVpc** operation does not support concurrent creation of default VPCs in the same region.\n- Only one default VPC can exist in a region."
  },
  "example": {
    "unifiedCli": "aliyun vpc create-default-vpc --biz-region-id cn-hangzhou --ipv6-cidr-block 2408:XXXX:346:b600::/56 --enable-ipv6 false --resource-group-id rg-acfmystnjq40000 --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun vpc CreateDefaultVpc --region cn-hangzhou --ResourceGroupId 'rg-acfmystnjq40000' --RegionId 'cn-hangzhou' --Ipv6CidrBlock '2408:XXXX:346:b600::/56' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --EnableIpv6 false"
  }
}