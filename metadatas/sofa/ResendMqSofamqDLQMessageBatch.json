{
  "name": "ResendMqSofamqDLQMessageBatch",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Cell",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要查询的单元",
        "en": "The cell to query."
      }
    },
    {
      "name": "GroupId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需查询的消费端 Group ID",
        "en": "The Group ID of the consumer to query."
      }
    },
    {
      "name": "InstanceId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需查询消息所对应的实例 ID。",
        "en": "The instance ID that corresponds to the messages to query."
      }
    },
    {
      "name": "MsgIds",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需重发的消息的 ID，即 Message ID，多个消息使用英文逗号分隔",
        "en": "The IDs of the messages to resend, that is, the message IDs. Separate multiple message IDs with commas (,)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量重发死信消息",
    "en": "Batch resend dead-letter messages"
  }
}