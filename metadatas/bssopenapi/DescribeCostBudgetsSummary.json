{
  "name": "DescribeCostBudgetsSummary",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BudgetName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预算名称。模糊匹配",
        "en": "The name of the budget. Supports fuzzy search."
      }
    },
    {
      "name": "BudgetStatus",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预算状态，枚举值：overdue（已过期）,notOverdue（未过期）。不填写则默认取全部",
        "en": "The status of the budget. If you do not specify this parameter, budgets in all statuses are returned."
      }
    },
    {
      "name": "BudgetType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预算类型。枚举值：cost(费用预算), byquantity（按量编制预算）, asset（使用率及覆盖率预算）。\n不填写则默认取全部",
        "en": "The type of the budget. If you do not specify this parameter, budgets of all types are returned."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "读取的最大数据记录数量。默认值：10，最大值：10，最小值：1。",
        "en": "The maximum number of items to return. Valid values: 1 to 10. Default value: 10."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "当前调用开始读取的位置，参数值必须为空或者使用返回结果中的NextToken设值，否则会报错，空则代表从头开始读取。默认：空",
        "en": "The token that is used to retrieve the next page of results. Set this parameter to the NextToken value that is returned in the response to the previous request. If you do not specify this parameter, the query starts from the first page."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询成本管理预算概要信息",
    "en": "DescribeCostBudgetsSummary"
  },
  "descriptions": {
    "zh": "本OpenAPI灰度中，仅对部分白名单用户开放，调用过多时不排除出现响应超时等性能问题。",
    "en": "This OpenAPI is in beta and available only to whitelisted users. Excessive calls may cause performance issues, such as response timeouts."
  },
  "example": {
    "unifiedCli": "aliyun bssopenapi describe-cost-budgets-summary --budget-name '全年预算' --budget-status notOverdue --budget-type cost --next-token eyJwYWdlTnVtIjoyLCJwYWdlU2l6ZSI6NH0= --max-results 5 --api-version 2017-12-14 --region cn-hangzhou",
    "legacyCli": "aliyun bssopenapi DescribeCostBudgetsSummary --region cn-hangzhou --BudgetStatus notOverdue --NextToken 'eyJwYWdlTnVtIjoyLCJwYWdlU2l6ZSI6NH0=' --MaxResults 5 --BudgetName '全年预算' --BudgetType cost"
  }
}