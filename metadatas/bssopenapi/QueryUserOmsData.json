{
  "name": "QueryUserOmsData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DataType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "计量数据时间类型，参考具体产品文档描述选用。\n- Raw：小时。\n- Hour：小时。\n- Day：天。\n- Month：月。",
        "en": "The time granularity of the metering data. For more information, see the documentation for the specific product.\n\n- `Raw`: by hour.\n\n- `Hour`: by hour.\n\n- `Day`: by day.\n\n- `Month`: by month."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "请求的结束时间。\n\n日期格式按照ISO8601标准标示，并需要使用UTC时间。格式：yyyy-mm-ddThh:mm:ssZ。",
        "en": "The end of the time range to query.\n\nThe time must be in UTC and in the `yyyy-mm-ddThh:mm:ssZ` format, which complies with the ISO 8601 standard."
      }
    },
    {
      "name": "Marker",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设定结果从NextToken之后按字母排序的第一个开始返回。默认从头返回。",
        "en": "The position from which to start reading the query results. The results are returned in alphabetical order, starting from the entry that follows the `NextToken` value. If you do not specify this parameter, the query starts from the beginning."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询每页行数，1~200，默认值100。",
        "en": "The number of entries to return on each page for a paged query. Valid values: 1 to 200. Default value: 100."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "请求的开始时间。\n\n日期格式按照ISO8601标准标示，并需要使用UTC时间。格式：yyyy-mm-ddThh:mm:ssZ。",
        "en": "The start of the time range to query.\n\nThe time must be in UTC and in the `yyyy-mm-ddThh:mm:ssZ` format, which complies with the ISO 8601 standard."
      }
    },
    {
      "name": "Table",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "用量数据产品和明细信息，一般为ProductCode，不同产品存在多种计量模型。",
        "en": "The product and details of the usage data. This is typically the `ProductCode`. Different products may have multiple metering models."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "根据传入的云产品查询相应的计量记录",
    "en": "QueryUserOmsData"
  },
  "descriptions": {
    "zh": "## API描述\n- 传入云产品参数必须在以下云产品code中，计量数据类型有小时、天类型。\n- 传入时间格式按照ISO8601标准，时间规范约束：\n\t-\t由于数据推送存在一定延时，按小时类型能保证24小时前计量数据完整性，按天类型能保证2天前计量数据完整性。\n\t-\t时间跨度上，可取最近一个季度的计量数据。",
    "en": "## API description\n\n- The cloud product parameter must specify a supported product code. You can aggregate metering data by hour or by day.\n\n- The time must be in ISO 8601 format. Note the following time constraints:\n\n  - Because of data push latency, data integrity is guaranteed for hourly data that is at least 24 hours old and for daily data that is at least 2 days old.\n\n  - You can query metering data from the previous quarter."
  },
  "example": {
    "unifiedCli": "aliyun bssopenapi query-user-oms-data --table rds --data-type Hour --start-time 2020-02-20T12:00:00Z --end-time 2020-02-21T12:00:00Z --marker NextToken --page-size 1 --api-version 2017-12-14 --region cn-hangzhou",
    "legacyCli": "aliyun bssopenapi QueryUserOmsData --region cn-hangzhou --Table rds --EndTime '2020-02-21T12:00:00Z' --PageSize 1 --StartTime '2020-02-20T12:00:00Z' --DataType Hour --Marker NextToken"
  }
}