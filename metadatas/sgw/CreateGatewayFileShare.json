{
  "name": "CreateGatewayFileShare",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccessBasedEnumeration",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "文件共享SMB协议下，是否启用Windows ABE，需要windowsAcl参数设置为true情况下生效。取值范围如下：\n\n- false（默认）：否\n- true：是\n\n>网关版本大于等于1.0.45支持。",
        "en": "For the SMB protocol, indicates whether to enable Windows access-based enumeration (ABE). This parameter takes effect only if \\`WindowsAcl\\` is set to \\`true\\`. Valid values:\n\n- false (default): disables Windows ABE.\n\n- true: enables Windows ABE.\n\n> This feature is supported by gateway versions 1.0.45 and later."
      }
    },
    {
      "name": "BackendLimit",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "文件共享最大上传速度，单位为MB/s。取值范围为0~1280，默认值为0，表示无限制。\n>同时如果已限制最大写入速度，最大上传速度不能小于最大写入速度。",
        "en": "The maximum upload speed of the file share, in MB/s. Valid values are 0 to 1,280. A value of 0 indicates no limit. The default value is 0.\n\n> If you set a maximum write speed, the maximum upload speed cannot be less than the maximum write speed."
      }
    },
    {
      "name": "BindIPAddr",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "仅高可用网关使用，设置共享绑定的虚拟挂载IP。",
        "en": "The virtual mount IP address to attach to the share. This parameter is used only for high availability (HA) gateways."
      }
    },
    {
      "name": "Browsable",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "文件共享SMB协议下是否可浏览（即网络邻居中是否可发现），取值范围如下：\n\n- true（默认）：是\n\n- false：否\n\n> \n> - 该参数在NFS协议下无效。",
        "en": "For a file share that uses the SMB protocol, this parameter indicates whether the share is discoverable in Network Neighborhood. Valid values:\n\n- true (default): The file share is discoverable.\n\n- false: The file share is not discoverable.\n\n> * This parameter is not valid for the NFS protocol."
      }
    },
    {
      "name": "BucketInfos",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "废弃。",
        "en": "Deprecated."
      },
      "sub_parameters": [
        {
          "name": "Endpoint",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "废弃。",
            "en": "Deprecated."
          }
        },
        {
          "name": "Name",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "废弃。",
            "en": "Deprecated."
          }
        },
        {
          "name": "PathPrefix",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "废弃。",
            "en": "Deprecated."
          }
        }
      ]
    },
    {
      "name": "BucketStub",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "BypassCacheRead",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否直读OSS。取值范围如下：\n\n- true：是\n\n- false（默认）：否",
        "en": "Indicates whether to read directly from OSS. Valid values:\n\n- true: reads directly from OSS.\n\n- false (default): does not read directly from OSS."
      }
    },
    {
      "name": "CacheMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件共享缓存模式。取值范围如下：\n\n- Cache：缓存模式\n\n- Sync：复制模式",
        "en": "The cache mode of the file share. Valid values:\n\n- Cache: cache mode\n\n- Sync: replication mode"
      }
    },
    {
      "name": "ClientSideCmk",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "废弃。",
        "en": "Deprecated."
      }
    },
    {
      "name": "ClientSideEncryption",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "废弃。",
        "en": "Deprecated."
      }
    },
    {
      "name": "DirectIO",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "废弃。",
        "en": "Deprecated."
      }
    },
    {
      "name": "DownloadLimit",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "文件共享最大下载速度，单位为MB/s。取值范围为0~1280，0表示无限制。\n>- 只在复制模式且开启下载文件数据的情况下才可设置。- 只有当该共享打开了反向同步或者加入极速同步组时候，此参数才生效。- 网关版本大于等于1.3.0支持。",
        "en": "The maximum download speed for the file share in MB/s. Valid values range from 0 to 1280. A value of 0 indicates an unlimited speed.\n\n> - This parameter can be set only when the file share is in replication mode and file data download is enabled. It takes effect only if remote sync is enabled for the share or the share is added to an express synchronization group. This parameter is supported in gateway versions 1.3.0 and later."
      }
    },
    {
      "name": "FastReclaim",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "文件共享是否开启上传优化，适合数据纯备份上云场景。取值范围如下：\n\n- false（默认）：否\n- true：是\n\n>网关版本大于等于1.0.39支持。",
        "en": "Indicates whether to enable upload optimization for the file share. This is suitable for scenarios where you only back up data to the cloud. Valid values:\n\n- false (default): disables upload optimization.\n\n- true: enables upload optimization.\n\n> This feature is supported by gateway versions 1.0.39 and later."
      }
    },
    {
      "name": "FrontendLimit",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "文件共享最大写入速度，单位为MB/s。取值范围为0~1280，默认值为0，表示无限制。",
        "en": "The maximum write speed of the file share, in MB/s. Valid values are 0 to 1,280. A value of 0 indicates no limit. The default value is 0."
      }
    },
    {
      "name": "GatewayId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网关ID。",
        "en": "The gateway ID."
      }
    },
    {
      "name": "IgnoreDelete",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "文件共享是否忽略删除。启用后网关侧删除文件不会删除云上（OSS）对应文件。\n\n- false（默认）：否\n- true：是\n\n>网关版本大于等于1.0.40支持。",
        "en": "Indicates whether to ignore deletions. If this feature is enabled, deleting a file on the gateway does not delete the corresponding file in the OSS bucket.\n\n- false (default): does not ignore deletions.\n\n- true: ignores deletions.\n\n> This feature is supported by gateway versions 1.0.40 and later."
      }
    },
    {
      "name": "InPlace",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "已废弃，不建议设置。",
        "en": "This parameter is deprecated. Do not set this parameter."
      }
    },
    {
      "name": "KmsRotatePeriod",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "废弃。",
        "en": "Deprecated."
      }
    },
    {
      "name": "LagPeriod",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "同步延迟，即网关本地缓存同步到云上OSS的延迟时间。单位为秒。取值范围为5~120，默认值为5。\n>网关版本大于等于1.0.40支持。",
        "en": "The synchronization latency in seconds. This is the delay when synchronizing the local cache of the gateway to the OSS bucket. Valid values are 5 to 120. The default value is 5.\n\n> This feature is supported by gateway versions 1.0.40 and later."
      }
    },
    {
      "name": "LocalFilePath",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件共享使用的缓存盘内部设备名，可以通过DescribeGatewayCaches接口获取。",
        "en": "The internal device name of the cache disk that the file share uses. To get this name, call the \\`DescribeGatewayCaches\\` operation."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件共享名称。长度限制1~255个字符，可以包含大小写字母，数字，“.”，“_”或“-”。",
        "en": "The name of the file share. The name must be 1 to 255 characters in length. It can contain uppercase and lowercase letters, digits, periods (.), underscores (\\_), and hyphens (-)."
      }
    },
    {
      "name": "NfsV4Optimization",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "文件共享NFS协议下，是否启用NFS v4优化，提升挂载上传效率。取值范围如下：\n\n- false（默认）：否\n\n- true：是\n\n>开启后将不支持NFS v3形式挂载。网关版本大于等于1.2.0支持。",
        "en": "For the NFS protocol, indicates whether to enable NFSv4 optimization to improve mount and upload efficiency. Valid values:\n\n- false (default): disables NFSv4 optimization.\n\n- true: enables NFSv4 optimization.\n\n> If you enable this feature, you cannot mount the share with NFSv3. This feature is supported by gateway versions 1.2.0 and later."
      }
    },
    {
      "name": "OssBucketName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件共享对应的对象存储（OSS）Bucket名称。\n\n>\n> - 文件共享不支持“冷归档类型”和“回源特性”的OSS桶。\n> - 开启多Bucket聚合功能时，多个OssBucketName请用逗号（,）分隔。",
        "en": "The name of the OSS bucket that the file share uses.\n\n> - File shares do not support OSS buckets of the Cold Archive storage class or buckets that have the origin fetch feature enabled.\n>\n> - If you enable multi-bucket aggregation, separate multiple OSS bucket names with a comma (,)."
      }
    },
    {
      "name": "OssBucketRegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件共享对应的对象存储（OSS）Bucket所在地域。\n> \n> -  当网关版本大于等于2.0.0，并正确传入该参数，网关会使用OSS v4签名算法访问OSS桶中的数据。\n> -  当网关版本小于2.0.0，或未正确传入该参数，网关会使用OSS v1签名算法访问OSS桶中的数据。",
        "en": "The region where the OSS bucket is located.\n\n> - If the gateway version is 2.0.0 or later and you pass this parameter correctly, the gateway uses the OSS v4 signature algorithm to access data in the OSS bucket.\n>\n> - If the gateway version is earlier than 2.0.0 or you do not pass this parameter correctly, the gateway uses the OSS v1 signature algorithm to access data in the OSS bucket."
      }
    },
    {
      "name": "OssBucketSsl",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "文件共享是否启用SSL访问OSS Bucket。取值范围如下：\n\n- true（默认）：是\n\n- false：否",
        "en": "Indicates whether to use Secure Sockets Layer (SSL) to access the OSS bucket. Valid values:\n\n- true (default): enables SSL.\n\n- false: disables SSL."
      }
    },
    {
      "name": "OssEndpoint",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件共享对应的对象存储（OSS）Bucket所在地域节点。\n>区分内网及外网Endpoint。建议如果OSS Bucket和网关处于相同Region则使用内网Endpoint：oss-cn-hangzhou-internal.aliyuncs.com。",
        "en": "The endpoint of the region where the OSS bucket is located.\n\n> Use an internal endpoint if the OSS bucket and the gateway are in the same region. For example, \\`oss-cn-hangzhou-internal.aliyuncs.com\\`. Otherwise, use a public endpoint."
      }
    },
    {
      "name": "PartialSyncPaths",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "复制模式下，通过设置目录路径集合，指定仅有这些目录采用复制模式。\n>需要联系我们开启白名单后才可进行相关设置。",
        "en": "In replication mode, lets you specify a collection of directory paths. Only these directories use replication mode.\n\n> Before you can configure this setting, contact us to add your account to the whitelist."
      }
    },
    {
      "name": "PathPrefix",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件共享对应的对象存储（OSS）Bucket下的子目录路径。为空则代表对应的是Bucket的根目录。",
        "en": "The path of the subdirectory in the OSS bucket that the file share uses. If you leave this parameter empty, the file share uses the root directory of the bucket."
      }
    },
    {
      "name": "PollingInterval",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "文件共享反向同步时间间隔。取值范围为15~36000。\n\n>在复制模式+反向同步启用下载文件数据时，取值范围为3600~36000。",
        "en": "The remote sync interval, in seconds. The value must be an integer from 15 to 36,000.\n\n> If you use replication mode and enable both remote sync and file data download, the value must be an integer from 3,600 to 36,000."
      }
    },
    {
      "name": "ReadOnlyClientList",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件共享NFS协议下只读客户端列表（可以是IP地址或者IP地址范围）。多个客户端使用“,”分隔。",
        "en": "A list of clients with read-only permissions for the NFS file share. The list can contain IP addresses or IP address ranges. Separate multiple clients with a comma (,)."
      }
    },
    {
      "name": "ReadOnlyUserList",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件共享SMB协议下只读用户列表。多个用户使用逗号“,”分隔。\n\n> \n> - 该参数在NFS协议下无效。",
        "en": "A list of users with read-only permissions for the SMB file share. Separate multiple users with a comma (,).\n\n> - This parameter is not valid for the NFS protocol."
      }
    },
    {
      "name": "ReadWriteClientList",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件共享NFS协议下读写客户端列表（可以是IP地址或者IP地址范围）。多个客户端使用逗号“,”分隔。",
        "en": "A list of clients with read and write permissions for the NFS file share. The list can contain IP addresses or IP address ranges. Separate multiple clients with a comma (,)."
      }
    },
    {
      "name": "ReadWriteUserList",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件共享SMB协议下读写用户列表。多个用户使用逗号“,”分隔。\n>\n> - 该参数在NFS协议下无效。",
        "en": "A list of users with read and write permissions for the SMB file share. Separate multiple users with a comma (,).\n\n> - This parameter is not valid for the NFS protocol."
      }
    },
    {
      "name": "RemoteSync",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "文件共享是否打开反向同步。取值范围如下：\n\n- false（默认）：否\n\n- true：是",
        "en": "Indicates whether to enable remote sync for the file share. Valid values:\n\n- false (default): disables remote sync.\n\n- true: enables remote sync."
      }
    },
    {
      "name": "RemoteSyncDownload",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "复制模式情况下，是否下载文件数据。取值范围如下：\n\n- false（默认）：否\n- true：是\n\n>只有当该共享打开了反向同步或者加入极速同步组时候，此参数才生效。",
        "en": "In replication mode, indicates whether to download file data. Valid values:\n\n- false (default): does not download file data.\n\n- true: downloads file data.\n\n> This parameter takes effect only if remote sync is enabled for the share or the share is added to an express synchronization group."
      }
    },
    {
      "name": "ServerSideAlgorithm",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "废弃。",
        "en": "Deprecated."
      }
    },
    {
      "name": "ServerSideCmk",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "废弃。",
        "en": "Deprecated."
      }
    },
    {
      "name": "ServerSideEncryption",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "废弃。",
        "en": "Deprecated."
      }
    },
    {
      "name": "ShareProtocol",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件共享协议。取值范围：\n\n- NFS。\n\n- SMB。",
        "en": "The file sharing protocol. Valid values:\n\n- NFS: Network File System (NFS)\n\n- SMB: Server Message Block (SMB)"
      }
    },
    {
      "name": "Squash",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件共享NFS协议下用户映射。取值范围如下：\n\n- none（默认）\n\n- root_squash\n\n- all_squash\n\n- all_anonymous",
        "en": "The user mapping for the file share that uses the NFS protocol. Valid values:\n\n- none (default)\n\n- root\\_squash\n\n- all\\_squash\n\n- all\\_anonymous"
      }
    },
    {
      "name": "SupportArchive",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "已废弃，不建议设置。",
        "en": "This parameter is deprecated. Do not set this parameter."
      }
    },
    {
      "name": "TransferAcceleration",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "文件共享是否开启传输加速功能，需要对应OSS Bucket已开启传输加速。\n>网关版本大于等于1.3.0支持。",
        "en": "Indicates whether to enable transfer acceleration for the file share. The corresponding OSS bucket must also have transfer acceleration enabled.\n\n> This feature is supported by gateway versions 1.3.0 and later."
      }
    },
    {
      "name": "WindowsAcl",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "文件共享SMB协议下，是否开启通过Windows访问列表（需要AD域）进行权限控制。取值范围如下：\n\n- false（默认）：否\n- true：是\n\n>网关版本大于等于1.0.45支持。",
        "en": "For the SMB protocol, indicates whether to enable access control with Windows Access Control Lists (ACLs). This requires an Active Directory (AD) domain. Valid values:\n\n- false (default): disables Windows ACLs.\n\n- true: enables Windows ACLs.\n\n> This feature is supported by gateway versions 1.0.45 and later."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建文件共享",
    "en": "CreateGatewayFileShare"
  },
  "descriptions": {
    "zh": "使用该接口时，需要注意如下事项：\n\n- 您已经创建部署完成文件网关，并且该网关拥有未被使用的缓存盘。\n\n- 您拥有对象存储(OSS)的Bucket。",
    "en": "Before you call this operation, ensure that the following requirements are met:\n\n- You have created and deployed a file gateway that has an unused cache disk.\n\n- You have an Object Storage Service (OSS) bucket."
  },
  "example": {
    "unifiedCli": "aliyun sgw create-gateway-file-share --gateway-id gw-000eg44nmxbsfwbvq000 --name alex000 --share-protocol NFS --remote-sync false --polling-interval 4500 --ignore-delete false --frontend-limit 1234 --backend-limit 1234 --in-place false --cache-mode Cache --browsable true --squash none --read-write-user-list 'user1，user2' --read-only-user-list 'userA，userB' --read-write-client-list 12.12.12.12 --read-only-client-list 12.12.12.12 --oss-bucket-name testbucket --oss-endpoint oss-cn-hangzhou-internal.aliyuncs.com --oss-bucket-ssl true --lag-period 5 --direct-io false --local-file-path /dev/vdb --server-side-encryption false --server-side-cmk xxxxx --client-side-encryption false --client-side-cmk xxxxx --kms-rotate-period 0 --path-prefix test1 --fast-reclaim false --support-archive false --windows-acl false --access-based-enumeration false --nfs-v4-optimization false --transfer-acceleration false --remote-sync-download false --download-limit 0 --partial-sync-paths test1 --server-side-algorithm AES256 --bypass-cache-read false --bind-ip-addr 192.168.0.10 --oss-bucket-region-id cn-hangzhou --bucket-infos Endpoint=oss-cn-hangzhou.aliyuncs.com PathPrefix=test1 Name=test-aliyun --bucket-stub true --region cn-hangzhou",
    "legacyCli": "aliyun sgw CreateGatewayFileShare --region cn-hangzhou --BucketInfos '[{\"Endpoint\":\"oss-cn-hangzhou.aliyuncs.com\",\"PathPrefix\":\"test1\",\"Name\":\"test-aliyun\"}]' --RemoteSync false --AccessBasedEnumeration false --WindowsAcl false --OssBucketRegionId 'cn-hangzhou' --DownloadLimit 0 --CacheMode Cache --ReadOnlyUserList 'userA，userB' --ClientSideCmk xxxxx --ServerSideCmk xxxxx --TransferAcceleration false --Squash none --Name alex000 --ShareProtocol NFS --RemoteSyncDownload false --OssEndpoint 'oss-cn-hangzhou-internal.aliyuncs.com' --ReadWriteUserList 'user1，user2' --NfsV4Optimization false --BackendLimit 1234 --ServerSideEncryption false --FrontendLimit 1234 --ClientSideEncryption false --PathPrefix test1 --BindIPAddr '192.168.0.10' --LagPeriod 5 --PartialSyncPaths test1 --SupportArchive false --Browsable true --BypassCacheRead false --DirectIO false --GatewayId 'gw-000eg44nmxbsfwbvq000' --OssBucketSsl true --FastReclaim false --ReadOnlyClientList '12.12.12.12' --IgnoreDelete false --BucketStub true --KmsRotatePeriod 0 --InPlace false --OssBucketName testbucket --PollingInterval 4500 --ReadWriteClientList '12.12.12.12' --LocalFilePath '/dev/vdb' --ServerSideAlgorithm AES256"
  }
}