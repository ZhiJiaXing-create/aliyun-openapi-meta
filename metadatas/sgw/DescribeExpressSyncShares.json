{
  "name": "DescribeExpressSyncShares",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ExpressSyncIds",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "同步组的ID列表。同步组ID之间以逗号进行分割。",
        "en": "A list of express synchronization group IDs. The IDs are separated by commas."
      }
    },
    {
      "name": "IsExternal",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否查询已加入的共享。取值范围：\n\n- true：查询可加入该同步组的共享信息。\n\n- false：查询已在同步组的共享信息。\n\n默认值：false。",
        "en": "Specifies whether to query the shares that have been added to the express synchronization group. Valid values:\n\n- true: queries the information about shares that can be added to the express synchronization group.\n- false: queries the information about shares that are in the express synchronization group.\n\nDefault value: false."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询极速同步组的共享信息或可加入的共享信息",
    "en": "DescribeExpressSyncShares"
  },
  "example": {
    "unifiedCli": "aliyun sgw describe-express-sync-shares --express-sync-ids sync-0002xetjdlxne2sd000,sync-0004agfrbke1l39p000 --is-external true --region cn-hangzhou",
    "legacyCli": "aliyun sgw DescribeExpressSyncShares --region cn-hangzhou --ExpressSyncIds 'sync-0002xetjdlxne2sd000,sync-0004agfrbke1l39p000' --IsExternal true"
  }
}