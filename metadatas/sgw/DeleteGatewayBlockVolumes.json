{
  "name": "DeleteGatewayBlockVolumes",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GatewayId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网关ID。",
        "en": "The ID of the gateway."
      }
    },
    {
      "name": "IndexId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "索引ID。可以通过DescribeGatewayBlockVolumes接口获得。",
        "en": "The ID of the index. You can call the DescribeGatewayBlockVolumes operation to query the ID."
      }
    },
    {
      "name": "IsSourceDeletion",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否删除源数据。可能值：\n\n- true：删除块网关卷时同时删除云上OSS Bucket中的数据，请谨慎操作。\n\n- false：删除块网关卷时不删除云上OSS Bucket中的数据。\n默认false。",
        "en": "Specifies whether to delete the source data. Valid values:\n\n- true: When the iSCSI gateway volume is deleted, the data in the OSS bucket is also deleted. Proceed with caution.\n- false: When the iSCSI gateway volume is deleted, the data in the OSS bucket is not deleted. Default value: false."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除块卷",
    "en": "DeleteGatewayBlockVolumes"
  },
  "example": {
    "unifiedCli": "aliyun sgw delete-gateway-block-volumes --gateway-id gw-000eg44nmxbsfwb000 --index-id NFSalex-gx000 --is-source-deletion true --region cn-hangzhou",
    "legacyCli": "aliyun sgw DeleteGatewayBlockVolumes --region cn-hangzhou --IndexId 'NFSalex-gx000' --IsSourceDeletion true --GatewayId 'gw-000eg44nmxbsfwb000'"
  }
}