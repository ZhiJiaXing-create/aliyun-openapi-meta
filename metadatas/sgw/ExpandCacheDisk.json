{
  "name": "ExpandCacheDisk",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GatewayId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网关ID。",
        "en": "The ID of the gateway."
      }
    },
    {
      "name": "LocalFilePath",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "缓存盘内部设备名。您可以通过DescribeGatewayCaches接口获得缓存盘内部设备名。",
        "en": "The internal device name of the cache disk. You can call the DescribeGatewayCaches operation to query the internal device name of the cache disk."
      }
    },
    {
      "name": "NewSizeInGB",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "扩容后的缓存容量。单位为GB。扩容要求如下：\n\n- 扩容后的缓存盘容量不能比原先缓存盘的容量小，即不支持缩减缓存盘。\n\n- 基础型网关扩容缓存盘上限为4 TB，标准型网关扩容缓存盘上限为8 TB，增强型和性能型网关扩容缓存盘上限为32 TB。\n\n- 文件网关缓存盘扩容下限为40 GB，块网关缓存盘扩容下限为20 GB。",
        "en": "The capacity of the cache disk after it is scaled out. Unit: GB. The following requirements must be met:\n\n- The capacity of the cache disk after it is scaled out cannot be smaller than the original capacity. You cannot scale in a cache disk.\n- The maximum capacity of a cache disk for a Basic gateway is 4 TB. The maximum capacity of a cache disk for a Medium gateway is 8 TB. The maximum capacity of a cache disk for an Enhanced or Performance gateway is 32 TB.\n- The minimum capacity of a cache disk for a file gateway is 40 GB. The minimum capacity of a cache disk for an iSCSI gateway is 20 GB."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "扩容缓存盘",
    "en": "ExpandCacheDisk"
  },
  "descriptions": {
    "zh": "调用该接口时，您需要注意：\n\n <props=\"china\"> 请确保在使用该接口前，已充分了解云存储网关产品的收费方式和[价格](https://www.aliyun.com/price/product?spm=5176.144914.752642.btn2.1fea7d70I6QIr5&aly_as=C81M1m5N#/hcs_sgw/detail)。</props>\n<props=\"intl\"> 请确保在使用该接口前，已充分了解云存储网关产品的收费方式和价格。</props>\n\n- 请确认您已创建缓存盘，并通过接口返回的购买链接进行购买，完成缓存盘的扩容。\n\n - 基础型网关扩容缓存盘上限为4 TB，标准型网关扩容缓存盘上限为8 TB，增强型和性能型网关扩容缓存盘上限为32 TB。\n\n - 云存储网关支持扩容缓存盘，但不支持缩减缓存盘。",
    "en": "Before you call this operation, note the following:\n\n<props=\"china\">Before you call this operation, make sure that you are familiar with the billing and [pricing](https://www.aliyun.com/price/product?spm=5176.144914.752642.btn2.1fea7d70I6QIr5\\&aly_as=C81M1m5N#/hcs_sgw/detail) of Cloud Storage Gateway.</props> <props=\"intl\">Before you call this operation, make sure that you are familiar with the billing and pricing of Cloud Storage Gateway.</props>\n\n- Make sure that a cache disk is created. You can use the purchase URL returned by this operation to complete the payment and expand the cache disk.\n- The maximum cache disk capacity is 4 TB for a Basic gateway, 8 TB for a Medium gateway, and 32 TB for an Enhanced or Performance gateway.\n- Cloud Storage Gateway lets you scale out cache disks, but you cannot scale them in."
  },
  "example": {
    "unifiedCli": "aliyun sgw expand-cache-disk --gateway-id gw-000eg44nmxbsfwbvq000 --new-size-in-gb 101 --local-file-path /dev/vdb --region cn-hangzhou",
    "legacyCli": "aliyun sgw ExpandCacheDisk --region cn-hangzhou --NewSizeInGB 101 --GatewayId 'gw-000eg44nmxbsfwbvq000' --LocalFilePath '/dev/vdb'"
  }
}