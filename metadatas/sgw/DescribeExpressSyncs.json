{
  "name": "DescribeExpressSyncs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BucketName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Bucket 名称。未传递参数时，默认查询该用户下的所有极速同步组。",
        "en": "The name of the bucket. If you do not specify this parameter, all express synchronization groups that belong to the current user are queried."
      }
    },
    {
      "name": "BucketPrefix",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Bucket 子目录。未传递参数时，查询该Bucket的极速同步组；传入空字符串时，默认查询该Bucket根目录的极速同步组。\n> 不支持Bucket名称为空，只查询Bucket子目录的场景。",
        "en": "The subdirectory of the bucket. If you do not specify this parameter, the express synchronization groups of the bucket are queried. If you specify an empty string, the express synchronization groups in the root directory of the bucket are queried by default.\n\n> You cannot leave the bucket name empty and query only the bucket subdirectory."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询极速同步组",
    "en": "DescribeExpressSyncs"
  },
  "example": {
    "unifiedCli": "aliyun sgw describe-express-syncs --bucket-name sgw0000 --bucket-prefix test0000 --region cn-hangzhou",
    "legacyCli": "aliyun sgw DescribeExpressSyncs --region cn-hangzhou --BucketName sgw0000 --BucketPrefix test0000"
  }
}