{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID，可以通过接口DescribeRegions查看可用的地域ID。",
        "en": "The region ID. You can call the DescribeRegions operation to query region IDs."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "网关ID列表。可以同时对N个实例绑定标签，N的取值范围为1~50。",
        "en": "The list of gateway IDs. You can attach tags to N gateways at a time. Valid values of N: 1 to 50."
      }
    },
    {
      "name": "ResourceRegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源地域ID。",
        "en": "The region ID of the resource."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型定义。唯一取值：GATEWAY。",
        "en": "The type of the resource. Valid value: GATEWAY."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "标签信息。",
        "en": "The tag information."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "标签键。可以同时创建N个标签键，N的取值范围为1~20。不允许传入空字符串。",
            "en": "The key of the tag. You can create N tag keys for a gateway. Valid values of N: 1 to 20. This parameter cannot be an empty string."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签键对应的标签值。可以同时创建N个标签值，N的取值范围为1~20。允许传入空字符串。",
            "en": "The tag value that is associated with the specified tag key. You can create N tag values for a gateway. Valid values of N: 1 to 20. This parameter can be an empty string."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建并绑定标签",
    "en": "TagResources"
  },
  "descriptions": {
    "zh": "在实例数量较多的情况下，您可以创建多个标签，为实例绑定不同的标签对其进行分类，之后通过标签进行实例筛选。\n- 标签由一对键（key）值（value）组成，键在同账号同地域下唯一，值无此限制。\n- 若设置的标签不存在，则自动创建该标签并绑定到目标实例。\n- 若实例已经绑定了有相同键的标签，则进行覆盖绑定。\n- 每个实例最多可以绑定20个标签。\n- 每次调用最多设置50个实例进行批量标签绑定。",
    "en": "If you have a large number of instances, you can create multiple tags and add these tags to the instances. Then, you can query instances by tag.\n\n*   A tag consists of a key and a value. Each key must be unique in a region within an Alibaba Cloud account. Different keys can be mapped to the same value.\n*   If you specify a tag that does not exist, the tag is automatically created and added to the specified gateway.\n*   If the key of the tag that you want to add to a gateway is the same as the key of an existing tag that is added to the gateway, the tag that you want to add overwrites the existing tag.\n*   You can attach up to 20 tags to each gateway.\n*   You can attach tags to up to 50 gateways in an API request."
  },
  "example": {
    "unifiedCli": "aliyun sgw tag-resources --biz-region-id cn-hangzhou --resource-region-id cn-hangzhou --resource-type GATEWAY --tag Value=testvalue1 Key=testkey1 --resource-id gw-uf6wjk5xxxxxxx --region cn-hangzhou",
    "legacyCli": "aliyun sgw TagResources --region cn-hangzhou --ResourceId.1 'gw-uf6wjk5xxxxxxx' --Tag.1.Key testkey1 --ResourceRegionId 'cn-hangzhou' --Tag.1.Value testvalue1 --ResourceType GATEWAY --RegionId 'cn-hangzhou'"
  }
}