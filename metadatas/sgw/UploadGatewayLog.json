{
  "name": "UploadGatewayLog",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GatewayId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网关ID。",
        "en": "The ID of the gateway."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "上传网关支持信息",
    "en": "UploadGatewayLog"
  },
  "descriptions": {
    "zh": "调用该接口时，您需要注意：\n\n- 仅支持网关状态为运行中（Running）、已激活（Activated）时调用此接口。",
    "en": "Note:\n\n- This operation is available only when the gateway is in the Running or Activated state."
  },
  "example": {
    "unifiedCli": "aliyun sgw upload-gateway-log --gateway-id gw-0001xv7je3031yzun000 --region cn-hangzhou",
    "legacyCli": "aliyun sgw UploadGatewayLog --region cn-hangzhou --GatewayId 'gw-0001xv7je3031yzun000'"
  }
}