{
  "name": "DescribeStorageBundles",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackendBucketRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网关集群所在的地域。可以通过DescribeRegions接口获得有效的regionId。",
        "en": "The region where the gateway cluster resides. You can call the DescribeRegions operation to obtain a valid region ID."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "网关集群列表信息的页码。\n\n起始值：1。\n\n默认值：1。",
        "en": "The page number of the gateway cluster list.\n\nStart value: 1.\n\nDefault value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数。\n\n最大值：50。\n\n默认值：10。",
        "en": "The number of entries to return on each page for a paged query.\n\nMaximum value: 50.\n\nDefault value: 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询集群列表信息",
    "en": "DescribeStorageBundles"
  },
  "example": {
    "unifiedCli": "aliyun sgw describe-storage-bundles --backend-bucket-region-id cn-hangzhou --page-number 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun sgw DescribeStorageBundles --region cn-hangzhou --PageSize 10 --PageNumber 1 --BackendBucketRegionId 'cn-hangzhou'"
  }
}