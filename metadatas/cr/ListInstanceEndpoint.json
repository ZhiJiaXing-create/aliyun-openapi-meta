{
  "name": "ListInstanceEndpoint",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "仓库实例ID",
        "en": "The ID of the instance."
      }
    },
    {
      "name": "ModuleName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置访问的模块，取值：\n\n- `Registry`：访问镜像仓库\n\n- `Chart`：访问Helm Chart",
        "en": "The name of the module that you want to access. Valid values:\n\n*   `Registry`: image repositories.\n*   `Chart`: Helm charts."
      }
    },
    {
      "name": "Summary",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否为简要模式，简要模式不返回ACL信息",
        "en": "Specify whether to return endpoints in simple mode. If yes, no access control information about the endpoint is returned."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询实例网络访问入口列表",
    "en": "ListInstanceEndpoint"
  },
  "example": {
    "unifiedCli": "aliyun cr list-instance-endpoint --instance-id cri-kmsiwlxxdcva0000 --module-name Registry --summary false --region cn-hangzhou",
    "legacyCli": "aliyun cr ListInstanceEndpoint --region cn-hangzhou --InstanceId 'cri-kmsiwlxxdcva0000' --ModuleName Registry --Summary false"
  }
}