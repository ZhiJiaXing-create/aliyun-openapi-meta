{
  "name": "UpdateInstanceEndpointStatus",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Enable",
      "position": "Query",
      "type": "Boolean",
      "required": true,
      "description": {
        "zh": "是否开启实例访问入口，取值：\n\n- `true`：开启实例访问入口\n\n- `false`：关闭实例访问入口",
        "en": "Specifies whether to enable the instance endpoint. Valid values:\n\n*   `true`: enables the instance endpoint.\n*   `false`: disables the instance endpoint"
      }
    },
    {
      "name": "EndpointType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "端点类型，只支持Internet",
        "en": "The type of the endpoint. Set the value to Internet."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID",
        "en": "The ID of the instance."
      }
    },
    {
      "name": "ModuleName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置访问的模块，取值：\n\n- `Registry`：访问镜像仓库\n\n- `Chart`：访问Helm Chart",
        "en": "The name of the module that you want to access. Valid values:\n\n*   `Registry`: the image repository.\n*   `Chart`: a Helm chart."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新实例访问入口状态",
    "en": "UpdateInstanceEndpointStatus"
  },
  "example": {
    "unifiedCli": "aliyun cr update-instance-endpoint-status --instance-id cri-kmsiwlxxdcva0000 --biz-endpoint-type internet --enable false --module-name Chart --region cn-hangzhou",
    "legacyCli": "aliyun cr UpdateInstanceEndpointStatus --region cn-hangzhou --InstanceId 'cri-kmsiwlxxdcva0000' --ModuleName Chart --EndpointType internet --Enable false"
  }
}