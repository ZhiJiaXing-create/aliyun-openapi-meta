{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解绑资源上全部的标签，取值：\n\n- **true**：解绑资源上的全部标签。\n\n- **false**：不解绑全部标签。\n\n> 如果同时设置了**TagKey.n**和本参数，则本参数不生效。",
        "en": "Specifies whether to remove all tags from the resource. Valid values:\n\n*   **true**\n*   **false**\n\n>If you specify both this parameter and the TagKey parameter, this parameter does not take effect."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The ID of the region in which the resources are deployed."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源 ID。最多支持添加20个资源。",
        "en": "The resource IDs. You can specify a maximum of 20 resource IDs."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。当前支持实例Instance资源。",
        "en": "The type of the resources. Instance resources are supported."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签键。N的取值范围为1~20。",
        "en": "The key of tag N added to the resource. Valid values of N: 1 to 20."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除资源标签",
    "en": "UntagResources"
  },
  "example": {
    "unifiedCli": "aliyun cr untag-resources --resource-type Instance --resource-id cri-8qong6ve5p3m0000 --biz-region-id cn-hangzhou --tag-key test-key --all true --region cn-hangzhou",
    "legacyCli": "aliyun cr UntagResources --region cn-hangzhou --All true --ResourceId.1 'cri-8qong6ve5p3m0000' --TagKey.1 'test-key' --ResourceType Instance --RegionId 'cn-hangzhou'"
  }
}