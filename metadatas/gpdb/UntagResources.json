{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解绑实例上全部的标签。当请求中未设置TagKey.N时，该参数才有效。取值范围：\n\n- true\n- false\n\n默认值：false",
        "en": "Whether to untag all tags on the instance. This parameter is only effective when TagKey.N is not set in the request. The value range is:\n\n- true\n- false\n\nDefault value: false"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID，您可通过接口[DescribeRegions](~~86912~~)查看可用的地域ID。",
        "en": "Region ID, you can view available region IDs through the [DescribeRegions](~~86912~~) interface."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "实例ID。N的取值范围：1~50",
        "en": "Instance ID. N's value range: 1~50"
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。取值范围：\n- `instance`：预留模式实例。\n- `ALIYUN::GPDB::INSTANCE`：弹性模式实例。",
        "en": "Resource type. The value range is:\n- `instance`: Reserved mode instance.\n- `ALIYUN::GPDB::INSTANCE`: Elastic mode instance."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源的标签键。N的取值范围：1~20",
        "en": "Resource tag key. N's value range: 1~20"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "解绑标签",
    "en": "UntagResources"
  },
  "example": {
    "unifiedCli": "aliyun gpdb untag-resources --biz-region-id cn-hangzhou --resource-type instance --all false --resource-id gp-j6cdjo680513l0000 --tag-key TestKey --region cn-hangzhou",
    "legacyCli": "aliyun gpdb UntagResources --region cn-hangzhou --All false --ResourceId.1 'gp-j6cdjo680513l0000' --TagKey.1 TestKey --ResourceType instance --RegionId 'cn-hangzhou'"
  }
}