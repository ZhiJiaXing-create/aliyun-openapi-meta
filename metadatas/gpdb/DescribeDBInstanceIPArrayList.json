{
  "name": "DescribeDBInstanceIPArrayList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceIPArrayName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要查询的IP白名单分组的名称。\n> 如果不传入该参数，默认返回所有IP白名单分组。",
        "en": "The name of the IP address whitelist. If you do not specify this parameter, the default whitelist is queried.\n\n>  Each instance supports up to 50 IP address whitelists."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。\n\n> 您可以调用[DescribeDBInstances](~~86911~~)接口查看目标地域下所有的AnalyticDB PostgreSQL实例的详情，包括实例ID。",
        "en": "The instance ID.\n\n> You can call the [DescribeDBInstances](~~86911~~) operation to query details about all AnalyticDB for PostgreSQL instances within a region, including instance IDs."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "该参数已废弃，无需传入该参数。",
        "en": "The ID of the resource group to which the instance belongs. For information about how to obtain the ID of a resource group, see [View basic information of a resource group](~~151181~~)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查看实例IP白名单",
    "en": "DescribeDBInstanceIPArrayList"
  },
  "descriptions": {
    "en": "You can call this operation to query the whitelists of IP addresses that are allowed to access an AnalyticDB for PostgreSQL instance.\n\n## Limits\n\nYou can call this operation up to 1,000 times per second per account. If the number of the calls per second exceeds the limit, throttling is triggered. As a result, your business may be affected. We recommend that you take note of the limit when you call this operation."
  },
  "example": {
    "unifiedCli": "aliyun gpdb describe-db-instance-ip-array-list --resource-group-id example-string --db-instance-id gp-bp000000000000000 --db-instance-ip-array-name Default --region cn-hangzhou",
    "legacyCli": "aliyun gpdb DescribeDBInstanceIPArrayList --region cn-hangzhou --ResourceGroupId 'example-string' --DBInstanceId 'gp-bp000000000000000' --DBInstanceIPArrayName Default"
  }
}