{
  "name": "DescribeTags",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。\n\n> 您可以调用[DescribeRegions](~~86912~~)接口查看可用的地域ID。",
        "en": "The ID of the region.\n\n>  You can call the [DescribeRegions](~~86912~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "该参数已废弃，无需传入该参数。",
        "en": "This parameter is no longer used."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。取值为：**instance**。",
        "en": "The type of the resource. Set the value to **instance**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询标签列表",
    "en": "DescribeTags"
  },
  "example": {
    "unifiedCli": "aliyun gpdb describe-tags --resource-group-id example-string --biz-region-id cn-hangzhou --resource-type instance --region cn-hangzhou",
    "legacyCli": "aliyun gpdb DescribeTags --region cn-hangzhou --ResourceGroupId 'example-string' --ResourceType instance --RegionId 'cn-hangzhou'"
  }
}