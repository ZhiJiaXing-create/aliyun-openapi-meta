{
  "name": "ModifyParameters",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。\n\n> 您可以调用[DescribeDBInstances](~~86911~~)接口查看目标地域下所有的AnalyticDB PostgreSQL实例的详情，包括实例ID。",
        "en": "The ID of the instance.\n\n>  You can call the [DescribeDBInstances](~~86911~~) operation to query the details of all AnalyticDB for PostgreSQL instances in a specific region, including instance IDs."
      }
    },
    {
      "name": "ForceRestartInstance",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否强制重启实例。取值说明：\n\n- **true**：强制重启。\n- **false**（默认）：不强制重启。",
        "en": "Specifies whether to forcibly restart the instance. Valid values:\n\n*   **true**\n*   **false**"
      }
    },
    {
      "name": "Parameters",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要修改的参数名称和参数值。格式为`<参数名称>:<参数值>`。\n\n您可以通过[DescribeParameters](~~208310~~)接口查看支持修改的配置参数。",
        "en": "The name and value of the parameter to be modified. Specify the parameter in the `<Parameter name>:<Parameter value>` format.\n\nYou can call the [DescribeParameters](~~208310~~) operation to query the parameters that can be modified."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改配置参数",
    "en": "ModifyParameters"
  },
  "descriptions": {
    "zh": "本接口仅支持修改存储弹性模式和Serverless版本的AnalyticDB PostgreSQL版实例的配置参数。",
    "en": "This operation can be called to modify parameters of an AnalyticDB for PostgreSQL instance in elastic storage mode or Serverless mode.\n\n## Limits\n\nYou can call this operation up to 1,000 times per second per account. If the number of the calls per second exceeds the limit, throttling is triggered and may affect your business. We recommend that you take note of the limit when you call this operation."
  },
  "example": {
    "unifiedCli": "aliyun gpdb modify-parameters --db-instance-id gp-bp000000000000000 --parameters statement_timeout_:_11800010 --force-restart-instance false --region cn-hangzhou",
    "legacyCli": "aliyun gpdb ModifyParameters --region cn-hangzhou --Parameters 'statement_timeout_:_11800010' --DBInstanceId 'gp-bp000000000000000' --ForceRestartInstance false"
  }
}