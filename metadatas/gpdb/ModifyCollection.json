{
  "name": "ModifyCollection",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Collection",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集合名。\n\n> 您可以通过[ListCollections](~~2401503~~)接口查看列表。",
        "en": "The collection name.\n\n> You can call the [ListCollections](~~2401503~~) operation to list all collections."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。\n\n> 您可以调用[DescribeDBInstances](~~86911~~)接口查看目标地域下所有的AnalyticDB PostgreSQL实例的详情，包括实例ID。",
        "en": "The instance ID.\n\n> You can call the [DescribeDBInstances](~~86911~~) operation to query details for all AnalyticDB for PostgreSQL instances in a region, including their instance IDs."
      }
    },
    {
      "name": "Metadata",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "向量metadata新增和修改操作的元信息，为JSON字符串，其中JSON字符串的格式如示例所示。\n\n通过指定该参数可以实现增加新的metadata定义，也可对已有metadata的定义执行重命名和隐式数据类型转换操作。\n\n具体来说\n\n当指定 `operations[*].operator = add` 时，为增加新metadata定义的操作，此时 `operations[*].newMetaName` 代表新metadata定义的名称， `operations[*].newMetaType` 代表新metadata定义的数据类型，`operations[*].fullTextRetrieval`代表是否需要添加到全文字段。\n\n当指定 `operations[*].operator = replace` 时，为修改已有metadata定义的操作，此时 `operations[*].oldMetaName` 代表当前metadata定义的名称， `operations[*].newMetaName` 代表metadata定义重命名后的名称， `operations[*].newMetaType` 代表metadata定义新的数据类型。如果仅需要重命名，则无需指定 `operations[*].newMetaType` 字段，如果仅需隐式数据类型转换，则无需指定 `operations[*].newMetaName` 字段\n\n\n> \n> - 支持的数据类型列表详见：[数据类型](~~424383~~)。其中money类型暂不支持。\n> - 仅支持add操作时将字段添加到全文字段，replace不支持。\n\n><warning>id、vector、to_tsvector、source字段为保留字段，禁止使用。></warning>",
        "en": "A JSON string that specifies the operations to add or modify metadata fields. For the required format, see the example.\n\nUse this parameter to add new metadata fields, rename existing metadata fields, or perform implicit data type conversion on existing fields.\n\nDetails:\n\nTo add a new metadata field, set `operations[*].operator = add`. Then, use `operations[*].newMetaName` to specify the field's name, `operations[*].newMetaType` for its data type, and `operations[*].fullTextRetrieval` to enable full-text retrieval for it.\n\nTo modify an existing metadata field, set `operations[*].operator = replace`. You must specify the current field name in `operations[*].oldMetaName`. To rename the field, provide the new name in `operations[*].newMetaName`. To change its data type, provide the new type in `operations[*].newMetaType`.\n\n> - For a list of supported data types, see [Data types](~~424383~~). The money data type is not supported.\n>\n> - Full-text retrieval can be enabled for a field only during an `add` operation, not a `replace` operation.\n\n><warning>\n\nThe field names `id`, `vector`, `to_tsvector`, and `source` are reserved.\n\n></warning>"
      }
    },
    {
      "name": "Namespace",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "命名空间，默认为public。\n\n> 您可以通过CreateNamespace接口创建，通过ListNamespaces接口查看列表。",
        "en": "The namespace. The default value is `public`.\n\n> You can call the CreateNamespace operation to create a namespace and the ListNamespaces operation to list existing namespaces."
      }
    },
    {
      "name": "NamespacePassword",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "命名空间对应的密码。",
        "en": "The password for the namespace."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例所在的地域ID。",
        "en": "The region ID of the instance."
      }
    },
    {
      "name": "WorkspaceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "多数据库实例组成的Workspace的Id。此参数和DBInstanceId参数不能同时为空，当和DBInstanceId同时指定时以此参数为准。",
        "en": "The ID of the workspace that contains multiple database instances. You must specify either this parameter or `DBInstanceId`. If you specify both, this parameter takes precedence."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改向量数据集",
    "en": "ModifyCollection"
  },
  "example": {
    "unifiedCli": "aliyun gpdb modify-collection --db-instance-id gp-xxxxxxxxx --namespace-password testpassword --namespace mynamespace --collection document --metadata operations_:_operator_:_add_,_newMetaType_:_int_,_newMetaName_:_ext1_,_fullTextRetrieval_:true_,_operator_:_replace_,_oldMetaName_:_ext2_,_newMetaName_:_ext3_,_operator_:_replace_,_newMetaType_:_bigint_,_oldMetaName_:_ext4_,_operator_:_replace_,_newMetaType_:_int_,_oldMetaName_:_ext5_,_newMetaName_:_ext6 --biz-region-id cn-hangzhou --workspace-id gp-ws-00000 --region cn-hangzhou",
    "legacyCli": "aliyun gpdb ModifyCollection --region cn-hangzhou --DBInstanceId 'gp-xxxxxxxxx' --Metadata 'operations_:_operator_:_add_,_newMetaType_:_int_,_newMetaName_:_ext1_,_fullTextRetrieval_:true_,_operator_:_replace_,_oldMetaName_:_ext2_,_newMetaName_:_ext3_,_operator_:_replace_,_newMetaType_:_bigint_,_oldMetaName_:_ext4_,_operator_:_replace_,_newMetaType_:_int_,_oldMetaName_:_ext5_,_newMetaName_:_ext6' --WorkspaceId 'gp-ws-00000' --Collection document --RegionId 'cn-hangzhou' --NamespacePassword testpassword --Namespace mynamespace"
  }
}