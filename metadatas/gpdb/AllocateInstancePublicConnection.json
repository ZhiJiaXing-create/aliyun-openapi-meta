{
  "name": "AllocateInstancePublicConnection",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AddressType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "网络类型。取值：\n\n- **primary**：主地址。\n- **cluster**：集群地址，仅多协调节点实例支持创建集群地址。\n\n> 默认为主地址。",
        "en": "The network type of the endpoint. Valid values:\n\n*   **primary**: primary endpoint\n*   **cluster**: instance endpoint. This value is supported only for an instance that contains multiple coordinator nodes.\n\n>  The default value is primary."
      }
    },
    {
      "name": "ConnectionStringPrefix",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "连接地址前缀。\n\n您可以自定义连接地址前缀，例如连接地址前缀为`gp-bp12ga6v69h86****`时，连接地址为`gp-bp12ga6v69h86****.gpdb.rds.aliyuncs.com`。",
        "en": "The prefix of the endpoint.\n\nSpecify a prefix for the endpoint. Example: `gp-bp12ga6v69h86****`. In this example, the endpoint is `gp-bp12ga6v69h86****.gpdb.rds.aliyuncs.com`."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。\n\n> 您可以调用[DescribeDBInstances](~~86911~~)接口查看目标地域下所有的AnalyticDB PostgreSQL实例的详情，包括实例ID。",
        "en": "The ID of the instance.\n\n>  You can call the [DescribeDBInstances](~~86911~~) operation to query the details of all AnalyticDB for PostgreSQL instances in a specific region, including instance IDs."
      }
    },
    {
      "name": "Port",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "端口号，例如5432。",
        "en": "The port number. Example: 5432."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "申请外网地址",
    "en": "AllocateInstancePublicConnection"
  },
  "descriptions": {
    "zh": "本接口用于申请AnalyticDB PostgreSQL版实例的外网地址。AnalyticDB PostgreSQL版实例的主地址和集群地址均支持创建外网地址，更多介绍，请参见[主地址和集群地址](~~204879~~)。",
    "en": "You can call this operation to apply for a public endpoint for an AnalyticDB for PostgreSQL instance. Both the primary and instance endpoints of an AnalyticDB for PostgreSQL instance can be public endpoints. For more information, see [Endpoints of an instance and its primary coordinator node](~~204879~~).\n\n## Limits\n\nYou can call this operation up to 1,000 times per second per account. If the number of the calls per second exceeds the limit, throttling is triggered. As a result, your business may be affected. We recommend that you take note of the limit when you call this operation."
  },
  "example": {
    "unifiedCli": "aliyun gpdb allocate-instance-public-connection --db-instance-id gp-bp12ga6v69h860000 --connection-string-prefix gp-bp12ga6v69h860000 --port 5432 --address-type primary --region cn-hangzhou",
    "legacyCli": "aliyun gpdb AllocateInstancePublicConnection --region cn-hangzhou --DBInstanceId 'gp-bp12ga6v69h860000' --Port 5432 --AddressType primary --ConnectionStringPrefix 'gp-bp12ga6v69h860000'"
  }
}