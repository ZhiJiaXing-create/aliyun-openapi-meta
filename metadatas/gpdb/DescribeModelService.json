{
  "name": "DescribeModelService",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。\n\n> 您可以调用[DescribeDBInstances](~~86911~~)接口查看目标地域下所有的AnalyticDB PostgreSQL实例的详情，包括实例ID。",
        "en": "The instance ID.\n\n> You can call the [DescribeDBInstances](~~86911~~) operation to query the details of all AnalyticDB for PostgreSQL instances in a region, including the instance ID."
      }
    },
    {
      "name": "ModelServiceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "模型服务id",
        "en": "The model service ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询模型服务",
    "en": "Query a model service"
  },
  "descriptions": {
    "zh": "## 使用说明\n本接口用于查看模型服务的详细信息。\n\n## QPS限制\n本接口的单用户QPS限制默认为1000次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "## Operation description\nYou can call this operation to query the details of a model service.\n\n## Rate limit\nThe maximum number of queries per second (QPS) per user for this operation is 1,000. If the number of calls exceeds the limit, throttling is triggered. Throttling may affect your business. Call this operation as needed."
  },
  "example": {
    "unifiedCli": "aliyun gpdb describe-model-service --db-instance-id gp-xxxxxxxxx --model-service-id mx-xxxxxxxxx --region cn-hangzhou",
    "legacyCli": "aliyun gpdb DescribeModelService --region cn-hangzhou --DBInstanceId 'gp-xxxxxxxxx' --ModelServiceId 'mx-xxxxxxxxx'"
  }
}