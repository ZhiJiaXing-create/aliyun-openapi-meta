{
  "name": "DescribeWaitingSQLInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。\n\n> 您可以调用[DescribeDBInstances](~~86911~~)接口查看目标地域下所有的AnalyticDB PostgreSQL实例的实例ID。",
        "en": "The ID of the instance.\n\n>  You can call the [DescribeDBInstances](~~86911~~) operation to query the instance IDs of all AnalyticDB for PostgreSQL instances in a specific region."
      }
    },
    {
      "name": "Database",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据库名称。",
        "en": "The name of the database."
      }
    },
    {
      "name": "PID",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "进程ID，即等待查询SQL的唯一标识。\n\n> 您可以调用[DescribeWaitingSQLRecords](~~461735~~)接口获取锁等待的查询的进程ID。",
        "en": "The ID of the process that uniquely identifies the query.\n\n>  You can call the [DescribeWaitingSQLRecords](~~461735~~) operation to obtain the process IDs of lock-waiting queries."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取锁诊断详细信息",
    "en": "DescribeWaitingSQLInfo"
  },
  "descriptions": {
    "zh": "本接口仅支持查询6.0版本存储弹性模式实例的锁等待查询的详细信息。",
    "en": "You can call this operation to query the details of a lock-waiting query only for an AnalyticDB for PostgreSQL V6.0 instance in elastic storage mode.\n\n## Limits\n\nYou can call this operation up to 1,000 times per second per account. If the number of the calls per second exceeds the limit, throttling is triggered. As a result, your business may be affected. We recommend that you take note of the limit when you call this operation."
  },
  "example": {
    "unifiedCli": "aliyun gpdb describe-waiting-sql-info --db-instance-id gp-bp12ga6v69h860000 --pid 100 --database test --region cn-hangzhou",
    "legacyCli": "aliyun gpdb DescribeWaitingSQLInfo --region cn-hangzhou --DBInstanceId 'gp-bp12ga6v69h860000' --Database test --PID 100"
  }
}