{
  "name": "DescribeDBInstanceIndexUsage",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。\n\n> 您可以调用[DescribeDBInstances](~~86911~~)接口查看目标地域下所有的AnalyticDB PostgreSQL实例的详情，包括实例ID。",
        "en": "The instance ID.\n\n> You can call the [DescribeDBInstances](~~86911~~) operation to query the information about all AnalyticDB for PostgreSQL instances within a region, including instance IDs."
      }
    },
    {
      "name": "Database",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据库名称",
        "en": "The name of the database."
      }
    },
    {
      "name": "OrderBy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序条件",
        "en": "order by search condition"
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，大于0且不超过Integer的最大值，默认值：**1**。",
        "en": "The page number. Pages start from page 1. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，取值说明：\n\n- **30**\n- **50**\n- **100**\n\n默认值：**30**。",
        "en": "The number of entries per page. Valid values:\n\n*   **30**\n*   **50**\n*   **100**\n\nDefault value: **30**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查看索引的使用情况",
    "en": "DescribeDBInstanceIndexUsage"
  },
  "descriptions": {
    "zh": "合理的使用索引可以提升数据库的查询速度，本接口用于查看AnalyticDB PostgreSQL版实例的索引使用情况。\n\n目前该功能仅支持存储弹性模式实例，且内核版本为V6.3.10.1及以上版本。如何查看和升级内核小版本，请参见[查看内核小版本](~~277424~~)和[版本升级](~~139271~~)。",
    "en": "Appropriate indexes can accelerate database queries. You can call this operation to query the index usage of an AnalyticDB for PostgreSQL instance.\n\nThis operation is available only for instances of V6.3.10.1 or later in elastic storage mode. For information about how to view and update the minor version of an instance, see [View the minor engine version](~~277424~~) and [Update the minor engine version](~~139271~~)."
  },
  "example": {
    "unifiedCli": "aliyun gpdb describe-db-instance-index-usage --db-instance-id gp-bp12ga6v69h860000 --page-size 30 --page-number 1 --database test --order-by Field:_SchemaName,_Type:_Desc --region cn-hangzhou",
    "legacyCli": "aliyun gpdb DescribeDBInstanceIndexUsage --region cn-hangzhou --OrderBy 'Field:_SchemaName,_Type:_Desc' --PageSize 30 --DBInstanceId 'gp-bp12ga6v69h860000' --PageNumber 1 --Database test"
  }
}