{
  "name": "ListModelOperatorServices",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "当前页码。",
        "en": "The current page number."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，取值说明：\n- **20**\n- **50**\n- **100**\n\n默认值：**20**。",
        "en": "The number of entries per page. Valid values:\n- **20**\n- **50**\n- **100**\n\nDefault value: **20**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取模型算子服务列表",
    "en": "Retrieve model operator service list"
  },
  "example": {
    "unifiedCli": "aliyun gpdb list-model-operator-services --page-size 20 --page-number 1 --region cn-hangzhou",
    "legacyCli": "aliyun gpdb ListModelOperatorServices --region cn-hangzhou --PageSize 20 --PageNumber 1"
  }
}