{
  "name": "GetBranchSchema",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BranchId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "分支 ID，用于唯一标识一个 Supabase 分支。",
        "en": "The branch ID that uniquely identifies a Supabase branch."
      }
    },
    {
      "name": "DBName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据库名称。系统库 postgres、template0、template1 不支持查询 Schema。",
        "en": "The database name. The system databases postgres, template0, and template1 do not support schema queries."
      }
    },
    {
      "name": "ProjectId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "主分支对应的 Supabase 项目 ID。",
        "en": "The Supabase project ID that corresponds to the primary branch."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域 ID。创建主分支时必须指定，创建子分支时默认继承主分支所在地域。",
        "en": "The region ID. This parameter is required when you create a primary branch. When you create a sub-branch, the region of the primary branch is inherited by default."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取分支 Schema",
    "en": "Get branch schema"
  },
  "descriptions": {
    "zh": "该接口用于查询指定分支中指定数据库的 Schema SQL 内容。系统库 postgres、template0、template1 不支持查询。",
    "en": "Queries the schema SQL content of a specified database in a specified branch. The system databases postgres, template0, and template1 are not supported."
  },
  "example": {
    "unifiedCli": "aliyun gpdb get-branch-schema --biz-region-id cn-hangzhou --project-id spb-xxxx --branch-id br-xxxx --db-name neondb --region cn-hangzhou",
    "legacyCli": "aliyun gpdb GetBranchSchema --region cn-hangzhou --ProjectId 'spb-xxxx' --DBName neondb --RegionId 'cn-hangzhou' --BranchId 'br-xxxx'"
  }
}