{
  "name": "CreateAIService",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。\n\n> 您可以调用[DescribeDBInstances](~~86911~~)接口查看目标地域下所有的AnalyticDB PostgreSQL实例的详情，包括实例ID。",
        "en": "The instance ID.\n\n> You can call the [DescribeDBInstances](~~86911~~) operation to query the details of all AnalyticDB for PostgreSQL instances in a region, including instance IDs."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "描述信息。",
        "en": "The description."
      }
    },
    {
      "name": "SecurityIPList",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IP 白名单分组下的 IP 列表，最多 1000 个，以逗号隔开。127.0.0.1 表示禁止任何外部 IP 访问。格式如下：\n- 10.23.12.24（IP）\n- 10.23.12.24/24（CIDR 模式，无类域间路由，/24表示地址中前缀的长度，范围为[1,32]）\n\n> 您可以在服务创建完成后调用 ModifyAIServiceSecurityIps 接口修改 IP 白名单。",
        "en": "The list of IP addresses in IP address whitelist group. You can specify up to 1,000 IP addresses, separated by commas (,). The value 127.0.0.1 indicates that no external IP addresses are allowed to access the instance. The following formats are supported:\n- 10.23.12.24 (IP address)\n- 10.23.12.24/24 (CIDR block. The value /24 indicates the length of the prefix in the address, which ranges from 1 to 32.)\n\n> After the service is created, you can call the ModifyAIServiceSecurityIps operation to modify IP address whitelist."
      }
    },
    {
      "name": "ServiceAccount",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "服务账号，限制如下：\n- 由小写字母，数字，下划线组成。\n- 以小写字母开头，小写字母或数字结尾。\n- 不能以 gp 开头。\n- 长度为 2~16 个字符。",
        "en": "The service account. The following limits apply:\n- The account name can contain lowercase letters, digits, and underscores (_).\n- The account name must start with a lowercase letter and end with a lowercase letter or digit.\n- The account name cannot start with gp.\n- The account name must be 2 to 16 characters in length."
      }
    },
    {
      "name": "ServiceAccountPassword",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "服务账号对应的密码，限制如下：\n- 由大写字母、小写字母、数字、特殊字符其中三种及以上组成。\n- 支持的特殊字符：!@#$%^&*()_+-=\n- 长度为 8~32 个字符。",
        "en": "The password of the service account. The following limits apply:\n- The password must contain at least three of the following character types: uppercase letters, lowercase letters, digits, and special characters.\n- Supported special characters: !@#$%^&*()_+-=\n- The password must be 8 to 32 characters in length."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "服务类型。当前仅支持 drama。",
        "en": "The service type. Currently, only drama is supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建 AI 服务",
    "en": "Create an AI service"
  },
  "example": {
    "unifiedCli": "aliyun gpdb create-ai-service --db-instance-id gp-xxxxxxxxx --type drama --service-account dramauser --service-account-password '123456Aa!' --description dramatest --security-ip-list 127.0.0.1 --region cn-hangzhou",
    "legacyCli": "aliyun gpdb CreateAIService --region cn-hangzhou --ServiceAccount dramauser --Type drama --Description dramatest --DBInstanceId 'gp-xxxxxxxxx' --SecurityIPList '127.0.0.1' --ServiceAccountPassword '123456Aa!'"
  }
}