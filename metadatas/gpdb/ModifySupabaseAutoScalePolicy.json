{
  "name": "ModifySupabaseAutoScalePolicy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoScale",
      "position": "Query",
      "type": "Boolean",
      "required": true,
      "description": {
        "zh": "是否启用**自动启停**。\n取值说明：\n- true：启用。启用后，supabase会根据流量情况自动暂停和自动恢复。\n- false：关闭，关闭后，会关闭supabase自动启停功能。",
        "en": "Specifies whether to enable **automatic start and stop**. Valid values:\n- true: Enabled. After this feature is enabled, Supabase automatically pauses and resumes based on traffic conditions.\n- false: Disabled. After this feature is disabled, the automatic start and stop feature of Supabase is turned off."
      }
    },
    {
      "name": "ProjectId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Supabase项目的ID。您可以登录控制台Supabase页面，获取工作空间ID。",
        "en": "The ID of the Supabase project. You can obtain the workspace ID from the Supabase page in the console."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例所在的地域ID。",
        "en": "The region ID of the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改Supabase自动启停策略",
    "en": "Modify supabase automatic start and stop policy"
  },
  "descriptions": {
    "zh": "- 仅支持ADBPG Supabase 实例。",
    "en": "- Only ADBPG Supabase instances are supported."
  },
  "example": {
    "unifiedCli": "aliyun gpdb modify-supabase-auto-scale-policy --biz-region-id cn-hangzhou --project-id sbp-tyarplz0000 --auto-scale false --region cn-hangzhou",
    "legacyCli": "aliyun gpdb ModifySupabaseAutoScalePolicy --region cn-hangzhou --ProjectId 'sbp-tyarplz0000' --RegionId 'cn-hangzhou' --AutoScale false"
  }
}