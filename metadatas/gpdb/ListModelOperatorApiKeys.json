{
  "name": "ListModelOperatorApiKeys",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，大于0且不超过Integer的最大值；默认值：1。",
        "en": "The page number. The value must be greater than 0 and cannot exceed the maximum value of Integer. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，取值说明：\n\n- **20**\n- **50**\n- **100**\n\n默认值：**20**。",
        "en": "The number of records per page. Valid values:\n\n- **20**\n- **50**\n- **100**\n\nDefault value: **20**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取模型算子 API KEY 列表",
    "en": "Retrieve model operator API key list"
  },
  "example": {
    "unifiedCli": "aliyun gpdb list-model-operator-api-keys --page-size 20 --page-number 1 --region cn-hangzhou",
    "legacyCli": "aliyun gpdb ListModelOperatorApiKeys --region cn-hangzhou --PageSize 20 --PageNumber 1"
  }
}