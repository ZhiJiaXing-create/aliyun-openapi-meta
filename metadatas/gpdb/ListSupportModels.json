{
  "name": "ListSupportModels",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例所在地域ID。",
        "en": "The region ID of the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询支持的模型列表",
    "en": "ListSupportModels"
  },
  "descriptions": {
    "zh": "- 本接口用于查询支持的模型列表。",
    "en": "*   This API is used to query the list of supported models."
  },
  "example": {
    "unifiedCli": "aliyun gpdb list-support-models --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun gpdb ListSupportModels --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}