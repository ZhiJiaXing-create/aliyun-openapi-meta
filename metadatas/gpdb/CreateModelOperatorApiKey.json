{
  "name": "CreateModelOperatorApiKey",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "描述信息。不允许空格，且不能超过256字符。",
        "en": "The description. Spaces are not allowed, and the value cannot exceed 256 characters."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建模型算子 API KEY",
    "en": "Create a model operator API key"
  },
  "example": {
    "unifiedCli": "aliyun gpdb create-model-operator-api-key --description test-apikey --region cn-hangzhou",
    "legacyCli": "aliyun gpdb CreateModelOperatorApiKey --region cn-hangzhou --Description 'test-apikey'"
  }
}