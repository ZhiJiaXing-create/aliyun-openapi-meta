{
  "name": "QueryContent",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Collection",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文档库名称。\n\n> 由[CreateDocumentCollection](~~2618448~~)接口创建。您可以调用[ListDocumentCollections](~~2618452~~)接口查看已经创建的文档库。",
        "en": "The name of the document collection.\n\n> The document collection is created by calling the [CreateDocumentCollection](~~2618448~~) operation. You can call the [ListDocumentCollections](~~2618452~~) operation to query existing document collections."
      }
    },
    {
      "name": "Content",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于检索的文本内容。",
        "en": "The text content used for retrieval."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。\n\n> 您可以调用[DescribeDBInstances](~~86911~~)接口查看目标地域下所有的AnalyticDB PostgreSQL实例的详情，包括实例ID。",
        "en": "The instance ID.\n\n> You can call the [DescribeDBInstances](~~86911~~) operation to query the details of all AnalyticDB for PostgreSQL instances in a region, including instance IDs."
      }
    },
    {
      "name": "FileName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "以图搜图场景中，待搜索的图片源文件名。\n\n> 图片文件必须带有文件后缀，当前支持的图片后缀：bmp、jpg、jpeg、png、 tiff。",
        "en": "The name of the source image file to search in image-to-image search scenarios.\n\n> The image file must have a file extension. Supported image extensions: bmp, jpg, jpeg, png, and tiff."
      }
    },
    {
      "name": "FileUrl",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "以图搜图场景中，公网可访问的图片文件URL地址。\n\n> 图片文件必须带有文件后缀，当前支持的图片后缀：bmp、jpg、jpeg、png、tiff。",
        "en": "The publicly accessible URL of the image file in image-to-image search scenarios.\n\n> The image file must have a file extension. Supported image extensions: bmp, jpg, jpeg, png, and tiff."
      }
    },
    {
      "name": "Filter",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的数据的过滤条件，格式为SQL的WHERE格式。是一个返回布尔值（真或假）的表达式，条件可以是简单的比较运算符，如等于(=)、不等于(<>或!=)、大于(>), 小于(<)、大于等于(>=)、小于等于(<=)，也可以是逻辑运算符（AND, OR, NOT）组合的更复杂的表达式，以及使用IN、BETWEEN、LIKE等关键字的条件。\n\n> \n> - 详细语法可参考：https://www.postgresqltutorial.com/postgresql-tutorial/postgresql-where/",
        "en": "The filter condition for the data to query, in SQL WHERE clause format. The filter is an expression that returns a Boolean value (true or false). Conditions can be simple comparison operators such as equal to (=), not equal to (<> or !=), greater than (>), less than (<), greater than or equal to (>=), and less than or equal to (<=). Conditions can also be more complex expressions combined with logical operators (AND, OR, NOT), as well as conditions using the IN, BETWEEN, and LIKE keywords.\n\n> \n> - For detailed syntax, refer to: https://www.postgresqltutorial.com/postgresql-tutorial/postgresql-where/."
      }
    },
    {
      "name": "GraphEnhance",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启知识图谱增强。默认值：false。",
        "en": "Specifies whether to enable knowledge graph enhancement. Default value: false."
      }
    },
    {
      "name": "GraphSearchArgs",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "知识图谱检索参数。",
        "en": "The knowledge graph retrieval parameters."
      }
    },
    {
      "name": "HybridSearch",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "多路召回算法，默认为空(即直接将稠密向量和全文的分数比较并排序)。\n\n可选值：\n\n- RRF：倒数排序融合(Reciprocal rank fusion)，有一个参数k控制融合效果，详见HybridSearchArgs配置；\n- Weight：比重排序，采用参数控制向量和全文的分数比重，然后再排序，参数详见 HybridSearchArgs 配置；\n- Cascaded：先全文检索再在其基础上进行向量检索；",
        "en": "The multi-channel recall algorithm. Default value: empty, which indicates that the dense vector and full-text index scores are directly compared and sorted.\n\nValid values:\n\n- RRF: Reciprocal Rank Fusion. A parameter k controls the fusion effect. For more information, see the HybridSearchArgs configuration.\n- Weight: Weighted sorting. Parameters control the score weights of AISearch retrieve and full-text index results before sorting. For more information, see the HybridSearchArgs configuration.\n- Cascaded: Full-text index retrieve is performed first, followed by AISearch retrieve based on the full-text index results."
      }
    },
    {
      "name": "HybridSearchArgs",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "多路召回的算法参数。目前支持RRF和Weight两种。HybridPathsSetting可以指定召回稠密向量（dense）、稀疏向量（sparse）和全文检索（fulltext），如果值为空，默认召回稠密向量（dense）和全文检索（fulltext）。\n\n- RRF：指定计算分数的算法的`1/(k+rank_i)`中的k常数，范围大于1的正整数，格式为：\n```\n{\n  \"HybridPathsSetting\": {\n    \"paths\": \"dense,fulltext\"\n  },\n  \"RRF\": {\n    \"k\": 60\n  }\n}\n```\n\n- Weight: \n   - 双路召回（不指定HybridPathsSetting，仅指定 alpha）：\n      - 计算公式：alpha * dense_score + (1-alpha) * fulltext_score，参数 alpha 表示稠密向量和全文的检索分数比重，范围为 0～1，其中 0 表示只全文，1 表示只稠密向量：\n```\n{ \n   \"Weight\": {\n    \"alpha\": 0.5\n   }\n}\n```\n  - 三路召回模式：\n     - 计算公式：normalized_dense * dense_score + normalized_sparse * sparse_score + normalized_fulltext * fulltext_score。其中dense、sparse、fulltext 分别代表稠密向量、稀疏向量、全文检索的权重，取值范围大于等于 0。系统会自动将权重归一化到 0～1（即 normalized_x = x / (dense + sparse + fulltext)）。\n```\n{\n  \"HybridPathsSetting\": {\n     \"paths\": \"dense,sparse,fulltext\"\n   },\n  \"Weight\": {\n    \"dense\": 0.5,\n    \"sparse\": 0.3,\n    \"fulltext\": 0.2\n  }\n}\n```",
        "en": "The algorithm parameters for multi-channel recall. RRF and Weight are supported. HybridPathsSetting specifies the recall paths: dense vectors (dense), sparse vectors (sparse), and full-text index (fulltext). If this value is empty, dense vectors (dense) and full-text index (fulltext) are used by default.\n\n- RRF: Specifies the constant k in the score calculation formula `1/(k+rank_i)`. The value must be a positive integer greater than 1. Format:\n```\n{\n  \"HybridPathsSetting\": {\n    \"paths\": \"dense,fulltext\"\n  },\n  \"RRF\": {\n    \"k\": 60\n  }\n}\n```\n\n- Weight: \n   - Dual-path recall (without specifying HybridPathsSetting, only specifying alpha):\n      - Formula: alpha * dense_score + (1-alpha) * fulltext_score. The alpha parameter specifies the score weight between dense vectors and full-text index retrieve. Valid values: 0 to 1, where 0 indicates full-text index only and 1 indicates dense vector only:\n```\n{ \n   \"Weight\": {\n    \"alpha\": 0.5\n   }\n}\n```\n  - Three-path recall pattern:\n     - Formula: normalized_dense * dense_score + normalized_sparse * sparse_score + normalized_fulltext * fulltext_score. The dense, sparse, and fulltext values represent the weights for dense vectors, sparse vectors, and full-text index retrieve respectively. Valid values: greater than or equal to 0. The system automatically performs normalization of the weights to 0 to 1 (normalized_x = x / (dense + sparse + fulltext)).\n```\n{\n  \"HybridPathsSetting\": {\n     \"paths\": \"dense,sparse,fulltext\"\n   },\n  \"Weight\": {\n    \"dense\": 0.5,\n    \"sparse\": 0.3,\n    \"fulltext\": 0.2\n  }\n}\n```"
      }
    },
    {
      "name": "IncludeFileUrl",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否同步返回文档的链接地址，默认不返回。",
        "en": "Specifies whether to synchronously return the URL of the document. By default, the URL is not returned."
      }
    },
    {
      "name": "IncludeMetadataFields",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "默认为空，表示要返回的metadata字段，多个字段用逗号分隔。",
        "en": "The metadata fields to return. Default value: empty. Separate multiple fields with commas."
      }
    },
    {
      "name": "IncludeVector",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否返回向量。默认为false。\n> - **false**：不返回向量。\n> - **true**：返回向量。",
        "en": "Specifies whether to return vectors. Default value: false.\n> - **false**: Does not return vectors.\n> - **true**: Returns vectors."
      }
    },
    {
      "name": "Metrics",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "检索时的相似度算法。此值为空时则采用创建知识库时指定的算法，建议如无特殊需求不需设置。\n\n> 取值说明：\n> - **l2**：欧氏距离。\n> - **ip**：点积（内积）距离。\n> - **cosine**：余弦相似度。",
        "en": "The similarity algorithm used for retrieval. If this value is empty, the algorithm specified when the knowledge base was created is used. Leave this parameter empty unless you have specific requirements.\n\n> Valid values:\n> - **l2**: Euclidean distance.\n> - **ip**: inner product distance.\n> - **cosine**: cosine similarity."
      }
    },
    {
      "name": "Namespace",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "命名空间，默认为public。\n\n> 您可以通过[CreateNamespace](~~2401495~~)接口创建，通过[ListNamespaces](~~2401502~~)接口查看列表。",
        "en": "The namespace. Default value: public.\n\n> You can create a namespace by calling the [CreateNamespace](~~2401495~~) operation and query namespaces by calling the [ListNamespaces](~~2401502~~) operation."
      }
    },
    {
      "name": "NamespacePassword",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "命名空间对应的密码。\n\n> 本值为[CreateNamespace](~~2401495~~)接口指定。",
        "en": "The password of the namespace.\n\n> This value is specified by the [CreateNamespace](~~2401495~~) operation."
      }
    },
    {
      "name": "Offset",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "偏移量，用于分页查询",
        "en": "The offset for paged query. Used for paging through results."
      }
    },
    {
      "name": "OrderBy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "默认为空，表示排序的依据字段。\n\n字段必须属于 metadata 或表里的默认字段比如 id，格式支持：\n\n单个字段，如 chunk_id；\n多个字段，用逗号连接，如 block_id, chunk_id；\n支持反序，如: block_id DESC, chunk_id DESC；",
        "en": "The field used for sorting. Default value: empty.\n\nThe field must belong to metadata or a default field in the table, such as id. Supported formats:\n\nA single field, such as chunk_id.\nMultiple fields separated by commas, such as block_id, chunk_id.\nDescending order, such as block_id DESC, chunk_id DESC."
      }
    },
    {
      "name": "RecallWindow",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "召回窗口。当该值不为空时，增加返回检索结果的上下文。格式为2个元素的数组：List<A, B>，其中-10<=A<=0，0<=B<=10。\n> - 推荐当文档切分过碎、检索可能会丢失上下文信息时使用该参数。\n> - 重排优先窗口化，即先rerank，再窗口化处理。",
        "en": "The recall window. When this value is not empty, additional context around the retrieval results is returned. The format is a two-element array: List<A, B>, where -10<=A<=0 and 0<=B<=10.\n> - Use this parameter when documents are split into overly small chunks and retrieval may lose contextual information.\n> - Reranking takes priority over windowing. Reranking is performed first, followed by windowing."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例所在的地域ID。",
        "en": "The region ID of the instance."
      }
    },
    {
      "name": "RerankFactor",
      "position": "Query",
      "type": "Double",
      "required": false,
      "description": {
        "zh": "重排因子。当该值不为空时，会对向量检索结果再做一次重排。取值范围：1<RerankFactor<=5。\n> - 当文档切分稀疏时，重排效率慢。\n> - 建议重排个数（TopK*Factor（向上取整））不超过50。",
        "en": "The reranking factor. When this value is not empty, the AISearch retrieve results are reranked. Valid values: 1 < RerankFactor <= 5.\n> - Reranking is slow when documents are sparsely chunked.\n> - The total number of reranked results (TopK × Factor, rounded up) should not exceed 50."
      }
    },
    {
      "name": "RerankModel",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "重排模型参数",
        "en": "The rerank model parameters."
      }
    },
    {
      "name": "TopK",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "返回top数量的结果。",
        "en": "The number of top results to return."
      }
    },
    {
      "name": "UrlExpiration",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回图片URL的有效期。\n\n> 取值说明\n> - 支持以秒（s）和日（d）为单位。例如300s代表链接有效期为300秒，60d代表链接有效期为60天。\n> - 取值范围在60s ~ 365d之间。\n> - 默认值：7200s，即2小时",
        "en": "The validity period of the returned image URL.\n\n> Valid values:\n> - Supports seconds (s) and days (d) as units. For example, 300s indicates a validity period of 300 seconds, and 60d indicates a validity period of 60 days.\n> - Valid values: 60s to 365d.\n> - Default value: 7200s (2 hours)."
      }
    },
    {
      "name": "UseFullTextRetrieval",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "（参数废弃）是否使用全文检索（双路召回）。默认为false，仅采用向量检索。",
        "en": "(Deprecated) Specifies whether to use full-text retrieval (dual-path recall). Default value: false, which indicates that only vector retrieval is used."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询文档内容",
    "en": "Query document content"
  },
  "example": {
    "unifiedCli": "aliyun gpdb query-content --db-instance-id gp-xxxxxxxxx --namespace mynamespace --collection document --biz-region-id cn-hangzhou --namespace-password testpassword --content What_is_AnalyticDB_for_PostgreSQL0 --filter title_=_test_AND_name_like_test% --recall-window -5 --topk 10 --rerank-factor 2.0 --use-full-text-retrieval true --metrics cosine --file-name test.jpg --file-url https://xx/myImage.jpg --include-vector true --hybrid-search RRF --hybrid-search-args key=example-string --include-metadata-fields title,page --include-file-url false --url-expiration 7200s --graph-enhance false --graph-search-args GraphTopK=60 --rerank-model Instruct=Given_a_web_search_query,_retrieve_relevant_passages_that_answer_the_query RerankMetadataFields=example-string Name=qwen3-rerank --order-by created_at --offset 0 --region cn-hangzhou",
    "legacyCli": "aliyun gpdb QueryContent --region cn-hangzhou --IncludeVector true --RecallWindow '[-5]' --FileName 'test.jpg' --NamespacePassword testpassword --IncludeMetadataFields 'title,page' --TopK 10 --RerankModel '{\"Instruct\":\"Given_a_web_search_query,_retrieve_relevant_passages_that_answer_the_query\",\"RerankMetadataFields\":\"example-string\",\"Name\":\"qwen3-rerank\"}' --Metrics cosine --DBInstanceId 'gp-xxxxxxxxx' --RerankFactor '2.0' --OrderBy created_at --UrlExpiration 7200s --GraphEnhance false --UseFullTextRetrieval true --GraphSearchArgs '{\"GraphTopK\":60}' --HybridSearch RRF --Namespace mynamespace --Offset 0 --FileUrl 'https://xx/myImage.jpg' --Filter 'title_=_test_AND_name_like_test%' --IncludeFileUrl false --Content What_is_AnalyticDB_for_PostgreSQL0 --Collection document --RegionId 'cn-hangzhou' --HybridSearchArgs '{\"key\":\"example-string\"}'"
  }
}