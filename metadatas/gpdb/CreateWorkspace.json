{
  "name": "CreateWorkspace",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。\n\n> 目前只支持4个单元。",
        "en": "The region ID.\n\n> Currently, only four units are supported."
      }
    },
    {
      "name": "WorkspaceName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "工作空间名称。",
        "en": "The workspace name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建工作空间",
    "en": "Create a workspace"
  },
  "descriptions": {
    "zh": "此接口为创建上下文服务工作空间",
    "en": "Creates a context service workspace."
  },
  "example": {
    "unifiedCli": "aliyun gpdb create-workspace --workspace-name my-first-workspace --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun gpdb CreateWorkspace --region cn-hangzhou --WorkspaceName 'my-first-workspace' --RegionId 'cn-hangzhou'"
  }
}