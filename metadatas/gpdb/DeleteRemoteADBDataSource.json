{
  "name": "DeleteRemoteADBDataSource",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DataSourceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "service id。",
        "en": "The service ID."
      }
    },
    {
      "name": "LocalDBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "本地使用数据的实例 ID。",
        "en": "The ID of the instance that uses the data provided by another instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除同构数据源",
    "en": "DeleteRemoteADBDataSource"
  },
  "example": {
    "unifiedCli": "aliyun gpdb delete-remote-adb-data-source --local-db-instance-id gp-test --data-source-id 1 --region cn-hangzhou",
    "legacyCli": "aliyun gpdb DeleteRemoteADBDataSource --region cn-hangzhou --LocalDBInstanceId 'gp-test' --DataSourceId 1"
  }
}