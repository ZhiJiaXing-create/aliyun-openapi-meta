{
  "name": "ModifyDBInstanceNetworkType",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。\n\n> 您可以调用[DescribeDBInstances](~~86911~~)接口查看目标地域下所有的AnalyticDB PostgreSQL实例的详情，包括实例ID。",
        "en": "The instance ID.\n\n> You can call the [DescribeDBInstances](~~86911~~) operation to query the information about all AnalyticDB for PostgreSQL instances within a region, including instance IDs."
      }
    },
    {
      "name": "InstanceNetworkType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定网络类型：\n- VPC：专有网络类型\n- Classic：经典网络类型",
        "en": "The new network type of the instance. Valid values:\n\n*   VPC\n*   Classic"
      }
    },
    {
      "name": "PrivateIpAddress",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "内网IP地址。",
        "en": "The internal IP address of the instance."
      }
    },
    {
      "name": "VPCId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "VPC ID。",
        "en": "The virtual private cloud (VPC) ID of the instance."
      }
    },
    {
      "name": "VSwitchId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "vSwitch ID，若传入VPC ID的值，则该参数必传。",
        "en": "The vSwitch ID of the instance. This parameter must be specified when VPCId is specified."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "切换实例网络类型",
    "en": "ModifyDBInstanceNetworkType"
  },
  "descriptions": {
    "zh": "## 使用说明\n本接口仅支持修改存储预留模式的AnalyticDB PostgreSQL版实例的网络类型。\n\n## QPS限制\n本接口的单用户QPS限制默认为1000次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。\n\n<props=\"china\">本文档中的QPS仅为默认参考值，准确信息请参见[API速率配额列表](https://quotas.console.aliyun.com/flow-control-products/gpdb/quotas)。</props>",
    "en": "##\n\nThis operation is available only for AnalyticDB for PostgreSQL instances in reserved storage mode.\n\n## QPS limits\n\nYou can call this operation up to 1,000 times per second per account. If the number of the calls per second exceeds the limit, throttling is triggered. As a result, your business may be affected. We recommend that you take note of the limit when you call this operation."
  },
  "example": {
    "unifiedCli": "aliyun gpdb modify-db-instance-network-type --db-instance-id gp-bp12ga6v69h860000 --instance-network-type VPC --vpc-id vpc-bp19ame5m1r3oejns0000 --vswitch-id vsw-bp1cpq8mr64paltkb0000 --private-ip-address 10.10.XX.XX --region cn-hangzhou",
    "legacyCli": "aliyun gpdb ModifyDBInstanceNetworkType --region cn-hangzhou --PrivateIpAddress '10.10.XX.XX' --DBInstanceId 'gp-bp12ga6v69h860000' --VPCId 'vpc-bp19ame5m1r3oejns0000' --VSwitchId 'vsw-bp1cpq8mr64paltkb0000' --InstanceNetworkType VPC"
  }
}