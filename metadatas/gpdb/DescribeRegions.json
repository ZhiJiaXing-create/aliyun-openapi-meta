{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Region",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。\n\n> 您可以调用[DescribeRegions](~~86912~~)接口查看可用的地域ID。",
        "en": "The region ID.\n\n> You can call the [DescribeRegions](~~86912~~) operation to query available region IDs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查看可选的地域和可用区",
    "en": "DescribeRegions"
  },
  "descriptions": {
    "zh": "创建AnalyticDB PostgreSQL版实例前，请先调用本接口查询可用地域和可用区。",
    "en": "Before you create an AnalyticDB for PostgreSQL instance, call this operation to query the available regions and zones."
  },
  "example": {
    "unifiedCli": "aliyun gpdb describe-regions --biz-region cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun gpdb DescribeRegions --region cn-hangzhou --Region 'cn-hangzhou'"
  }
}