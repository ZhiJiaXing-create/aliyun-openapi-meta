{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。\n\n> 您可以调用[DescribeRegions](~~86912~~)接口查看可用的地域ID。",
        "en": "The ID of the region. You can call the [DescribeRegions](~~86912~~) operation to query region IDs."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "第N个实例。N的取值范围：1~50。",
        "en": "The ID of an instance. Valid values of N: 1 to 50."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。取值固定为`instance`。",
        "en": "The mode of the instance. Valid values:\n\n*   `instance`: reserved storage mode\n*   `ALIYUN::GPDB::INSTANCE`: elastic storage mode"
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "第N个标签。N的取值范围：1~20。",
        "en": "The list of tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "标签键。该值不允许为空字符串。最多支持64个字符，不能以`aliyun`和`acs:`开头，不能包`含http://`或者`https://`。",
            "en": "The key of a tag. Valid values of N: 1 to 20. This parameter value cannot be an empty string. A tag key can contain a maximum of 128 characters. It cannot start with `aliyun` or`  acs: ` and cannot contain `http://` or`  https:// `."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "标签值。该值可以为空字符串。最多支持128个字符，不能包含`http://`或者`https://`。",
            "en": "The value of a tag. Valid values of N: 1 to 20. This parameter value can be an empty string. A tag value can contain a maximum of 128 characters. It cannot start with `acs:` and cannot contain `http://` or `https://`."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建并绑定标签",
    "en": "Create and attach tags"
  },
  "example": {
    "unifiedCli": "aliyun gpdb tag-resources --biz-region-id cn-hangzhou --resource-type instance --resource-id gp-bp000000000000000 --tag Value=TestValue Key=TestKey --region cn-hangzhou",
    "legacyCli": "aliyun gpdb TagResources --region cn-hangzhou --ResourceId.1 'gp-bp000000000000000' --Tag.1.Key TestKey --Tag.1.Value TestValue --ResourceType instance --RegionId 'cn-hangzhou'"
  }
}