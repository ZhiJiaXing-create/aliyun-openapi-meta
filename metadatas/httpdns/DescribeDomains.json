{
  "name": "DescribeDomains",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccountId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "HTTPDNS账户ID。",
        "en": "The HTTPDNS account ID."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "当前页数，起始值为1，默认为1。",
        "en": "The current page number. Minimum value: 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数，最大值100，默认为20。",
        "en": "The number of entries per page for a paged query. Maximum value: 100. Default value: 20."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取用户的域名列表",
    "en": "List domain names of a user"
  },
  "example": {
    "unifiedCli": "aliyun httpdns describe-domains --account-id 123456 --page-number 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun httpdns DescribeDomains --region cn-hangzhou --AccountId 123456 --PageSize 10 --PageNumber 1"
  }
}