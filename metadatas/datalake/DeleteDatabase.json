{
  "name": "DeleteDatabase",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/metastore/catalogs/databases",
  "parameters": [
    {
      "name": "Async",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否异步执行。",
        "en": "Specifies whether to execute the operation asynchronously."
      }
    },
    {
      "name": "Cascade",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否级联删除数据。取值如下：\n\n- true：删除数据库下的表。\n\n- false：不删数据库下的表。如果数据库下还存在表，则删除失败。",
        "en": "Specifies whether to perform a cascade delete. Valid values:\n\n- true: Deletes the tables in the database.\n\n- false: Does not delete the tables in the database. If the database contains tables, the operation fails."
      }
    },
    {
      "name": "CatalogId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据目录ID。",
        "en": "The catalog ID."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据库名称。",
        "en": "The database name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除数据库",
    "en": "DeleteDatabase"
  }
}