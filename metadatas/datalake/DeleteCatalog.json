{
  "name": "DeleteCatalog",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/metastore/catalogs",
  "parameters": [
    {
      "name": "CatalogId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据目录ID。",
        "en": "The ID of the catalog."
      }
    },
    {
      "name": "IsAsync",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否异步，默认同步。",
        "en": "Specifies whether the deletion is asynchronous. By default, the operation is synchronous."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除数据湖数据目录",
    "en": "DeleteCatalog"
  }
}