{
  "name": "DescribePlaybookReleases",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置请求和接收消息的语言类型，默认为**zh**。取值：\n\n- **zh**：中文\n- **en**：英文",
        "en": "The language of the content within the request and response. Default value: **zh**. Valid values:\n\n- **zh**: Chinese\n\n- **en**: English"
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "设置从返回结果的第几页开始显示查询结果。默认值为1，表示从第1页开始显示。",
        "en": "The page number of the page to return. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定分页查询时，每页显示的数据最大条数。每页默认显示的数据条数为20条，PageSize参数值为空时，将默认返回10条数据。\n> 建议PageSize取值不要为空。",
        "en": "The number of entries per page. Default value: 20. If you do not specify this parameter, 10 entries are returned.\n\n> Specify a value for this parameter."
      }
    },
    {
      "name": "PlaybookUuid",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "剧本的UUID。",
        "en": "The UUID of the playbook."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取剧本发布的版本列表",
    "en": "DescribePlaybookReleases"
  },
  "example": {
    "unifiedCli": "aliyun sophonsoar describe-playbook-releases --playbook-uuid ac343acc-1a61-4084-9a1c-xxxx --lang zh --page-size 10 --page-number 1 --api-version 2022-07-28 --region cn-hangzhou",
    "legacyCli": "aliyun sophonsoar DescribePlaybookReleases --region cn-hangzhou --PlaybookUuid 'ac343acc-1a61-4084-9a1c-xxxx' --PageSize 10 --PageNumber 1 --Lang zh"
  }
}