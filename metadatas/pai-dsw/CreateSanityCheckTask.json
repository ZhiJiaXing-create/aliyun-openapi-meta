{
  "name": "CreateSanityCheckTask",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/api/v2/sanitychecks/[CheckType]",
  "parameters": [
    {
      "name": "CheckType",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "检查项类型。取值：\n\n- **SSH**：用于触发实例SSH联通性健康检查\n- **CustomService**：用于触发实例自定义服务的联通性健康检查",
        "en": "The type of the check item. Valid values:\n\n- **SSH**: triggers an SSH health check for the instance."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求body。",
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "CreateSanityCheckTask",
    "en": "Create a sanity check task"
  },
  "example": {
    "unifiedCli": "aliyun pai-dsw create-sanity-check-task --check-type SSH --instance-id dsw-730xxxxxxxxxx --api-version 2022-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun pai-dsw POST /api/v2/sanitychecks/SSH --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"InstanceId\":\"dsw-730xxxxxxxxxx\",\"CheckArgs\":{\"key\":\"example-string\"}}"
  }
}