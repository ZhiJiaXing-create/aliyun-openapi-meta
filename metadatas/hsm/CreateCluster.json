{
  "name": "CreateCluster",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterName",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群名称。",
        "en": "The name of the cluster."
      }
    },
    {
      "name": "MasterInstanceId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "主密码机实例ID。",
        "en": "The ID of the master HSM instance."
      }
    },
    {
      "name": "RegionId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建集群",
    "en": "CreateCluster"
  },
  "descriptions": {
    "zh": "创建集群选择的密码机实例状态必须为ACTIVE（已启用）。",
    "en": "The HSM instance that you select to create the cluster must be in the Active state."
  }
}