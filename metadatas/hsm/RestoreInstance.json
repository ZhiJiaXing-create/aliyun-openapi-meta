{
  "name": "RestoreInstance",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ImageId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要恢复的镜像ID。",
        "en": "The ID of the image from which to restore the instance."
      }
    },
    {
      "name": "InstanceId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "密码机实例ID。",
        "en": "The ID of the HSM instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "恢复实例数据",
    "en": "RestoreInstance"
  },
  "descriptions": {
    "zh": "只有已停用或未启用状态的实例才能进行镜像导入。",
    "en": "You can import an image to an instance only if the instance is stopped or disabled."
  }
}