{
  "name": "ConfigClusterWhitelist",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The ID of the cluster."
      }
    },
    {
      "name": "Whitelist",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群访问白名单列表。",
        "en": "The IP address whitelist of the cluster."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "配置集群白名单",
    "en": "ConfigClusterWhitelist"
  },
  "descriptions": {
    "zh": "集群的白名单优先级高于实例的白名单。集群模式下建议您创建集群白名单，不需要再创建密码机白名单。",
    "en": "The IP address whitelist of a cluster has a higher priority than the IP address whitelist of a hardware security module (HSM) in the cluster. In cluster mode, we recommend that you create an IP address whitelist for your cluster. You do not need to create an IP address for the HSM in the cluster."
  }
}