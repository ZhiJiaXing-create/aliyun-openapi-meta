{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "SMC资源ID。SMC资源包括迁移源和迁移任务。N的取值范围：1~50",
        "en": "The ID of the SMC resource. SMC resources include migration sources and migration tasks. You can specify 1 to 50 resource IDs."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "SMC资源类型。取值范围：\n\n- sourceserver：迁移源\n\n- replicationjob：迁移任务",
        "en": "The type of the SMC resource. Valid values:\n\n- sourceserver: migration source\n\n- replicationjob: migration task"
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "标签信息列表。",
        "en": "The list of tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "为SMC资源指定的标签键。N的取值范围：1~20\n\n一旦传入该值，则不允许为空字符串。最多支持64个字符，不能以aliyun和acs:开头，不能包含http:\\//或者https:\\//。",
            "en": "The key of tag N to add to the SMC resource. Valid values of N: 1 to 20.\n\nThe tag key cannot be an empty string. The key can be up to 64 characters in length. It cannot start with aliyun or acs:. It also cannot contain http\\:// or https\\://."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "为SMC资源指定的标签值。N的取值范围：1~20。\n\n一旦传入该值，可以为空字符串。最多支持64个字符，不能包含http://或者https://。",
            "en": "The value of tag N to add to the SMC resource. Valid values of N: 1 to 20.\n\nThe tag value can be an empty string. The value can be up to 64 characters in length and cannot contain http\\:// or https\\://."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "绑定标签",
    "en": "TagResources"
  },
  "descriptions": {
    "zh": "## 接口说明\n\n每个SMC资源可绑定的标签限额为20。\n\n绑定标签前，阿里云会校验资源已有标签数量，超过限额后返回报错信息。",
    "en": "## Description\n\nEach SMC resource can have a maximum of 20 tags.\n\nBefore you attach tags, Alibaba Cloud checks the number of existing tags on a resource. If the number exceeds the limit, an error message is returned."
  },
  "example": {
    "unifiedCli": "aliyun smc tag-resources --resource-type sourceserver --resource-id s-bw526m1vi6x20c6g0000 --tag Value=TestValue Key=TestKey --region cn-hangzhou",
    "legacyCli": "aliyun smc TagResources --region cn-hangzhou --ResourceId.1 's-bw526m1vi6x20c6g0000' --Tag.1.Key TestKey --Tag.1.Value TestValue --ResourceType sourceserver"
  }
}