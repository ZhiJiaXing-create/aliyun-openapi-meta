{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否删除SMC资源绑定的全部标签。仅当请求中未设置`TagKey.N`时该参数有效。取值范围：\n\n- true：删除指定SMC资源绑定的全部标签。若指定的SMC资源未绑定任何标签，则不做处理。\n- false：不删除指定SMC资源绑定的任何标签。\n\n默认值：false。",
        "en": "Specifies whether to remove all tags from the SMC resources. This parameter takes effect only if you do not specify the `TagKey.N` parameter in the request. Valid values:\n\n- true: Removes all tags from the specified SMC resources. If no tags are attached to the SMC resources, no operation is performed.\n\n- false: Does not remove any tags from the specified SMC resources.\n\nDefault value: false."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "SMC资源ID。SMC资源包括迁移源和迁移任务。N的取值范围：1~50。",
        "en": "The IDs of the SMC resources. You can specify 1 to 50 resource IDs. SMC resources include migration sources and migration tasks."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "SMC资源类型。取值范围：\n\n- sourceserver：迁移源。\n\n- replicationjob：迁移任务。",
        "en": "The type of the SMC resource. Valid values:\n\n- sourceserver: a migration source.\n\n- replicationjob: a migration task."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "SMC资源的标签键。标签键大小写敏感。N的取值范围：1~20。",
        "en": "The keys of the tags to remove. You can specify 1 to 20 tag keys. Tag keys are case-sensitive."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "解绑并删除标签",
    "en": "UntagResources"
  },
  "descriptions": {
    "zh": "如果标签不再使用，您可以使用此接口为迁移源和迁移任务统一解绑并删除标签。",
    "en": "If a tag is no longer needed, you can call this operation to detach and delete the tag from migration sources and migration tasks."
  },
  "example": {
    "unifiedCli": "aliyun smc untag-resources --resource-type sourceserver --all false --resource-id s-bp12tueadp5ndleg0000 --tag-key smc --region cn-hangzhou",
    "legacyCli": "aliyun smc UntagResources --region cn-hangzhou --All false --ResourceId.1 's-bp12tueadp5ndleg0000' --TagKey.1 smc --ResourceType sourceserver"
  }
}