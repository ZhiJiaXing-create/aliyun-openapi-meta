{
  "name": "GetToken",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/tokens",
  "parameters": [
    {
      "name": "ExpireTime",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分享链接过期时间（默认为604800秒），最小值为0。",
        "en": "The time when the share link expires. Default value: 604800. Minimum value: 0. Unit: seconds."
      }
    },
    {
      "name": "TargetId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "等待分享的任务的ID。",
        "en": "The ID of the job to be shared."
      }
    },
    {
      "name": "TargetType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分享的任务类型，只可指定job或者tensorboard。",
        "en": "The type of the job that you want to share. Valid values: job and tensorboard."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取任务分享令牌",
    "en": "GetToken"
  },
  "example": {
    "unifiedCli": "aliyun pai-dlc get-token --target-type job --target-id dlc0000000 --expire-time 60 --region cn-hangzhou",
    "legacyCli": "aliyun pai-dlc GET /api/v1/tokens --region cn-hangzhou --TargetType job --ExpireTime 60 --TargetId dlc0000000 --header \"Content-Type=application/json\""
  }
}