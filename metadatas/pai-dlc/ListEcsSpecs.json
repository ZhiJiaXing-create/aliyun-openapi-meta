{
  "name": "ListEcsSpecs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/ecsspecs",
  "parameters": [
    {
      "name": "AcceleratorType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按加速器类型过滤。取值如下：\n- CPU\n- GPU",
        "en": "Filter by accelerator type. Valid values:\n\n*   CPU\n*   GPU"
      }
    },
    {
      "name": "InstanceTypes",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要查询的机器规格列表，以半角逗号（,）分隔。",
        "en": "The instance types to query. Separate the types with commas (,)."
      }
    },
    {
      "name": "Order",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序顺序，取值如下：\n- desc：逆序排列。\n- asc：升序排列。",
        "en": "The sorting order. Valid values:\n\n*   desc: descending order.\n*   asc: ascending order."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询第几页数据，起始值为1。",
        "en": "The number of the page to query. The start value is 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页显示的数量。",
        "en": "The number of entries returned per page."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源类型，取值如下：\n- ECS\n- Lingjun",
        "en": "The type of the resource. Valid values:\n\n*   ECS\n*   Lingjun"
      }
    },
    {
      "name": "SortBy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按返回字段排序。取值如下：\n- CPU\n- GPU\n- Memory\n- GmtCreateTime",
        "en": "The field based on which the results are sorted. Valid values:\n\n*   CPU\n*   GPU\n*   Memory\n*   GmtCreateTime"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询机器资源配置列表",
    "en": "ListEcsSpecs"
  },
  "example": {
    "unifiedCli": "aliyun pai-dlc list-ecs-specs --accelerator-type GPU --sort-by GPU --order desc --page-number 1 --page-size 10 --instance-types ecs.g6.large,ecs.g6.xlarge --resource-type ECS --region cn-hangzhou",
    "legacyCli": "aliyun pai-dlc GET /api/v1/ecsspecs --region cn-hangzhou --Order desc --PageSize 10 --PageNumber 1 --SortBy GPU --ResourceType ECS --InstanceTypes 'ecs.g6.large,ecs.g6.xlarge' --AcceleratorType GPU --header \"Content-Type=application/json\""
  }
}