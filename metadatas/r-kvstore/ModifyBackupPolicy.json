{
  "name": "ModifyBackupPolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupRetentionPeriod",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "数据备份保留天数。默认7天，取值范围为7~730。",
        "en": "The number of days for which you want to retain data backup files. Valid values: 7 to 730. Default value: 7."
      }
    },
    {
      "name": "EnableBackupLog",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "开启或关闭数据闪回功能，取值：\n* **1**：开启。同时，您还需确保已在实例的参数设置中开启AOF持久化（`appendonly`为`yes`），开启后，才能使用数据闪回功能。\n* **0**：关闭，默认值。\n> 仅Tair（企业版）内存型和持久内存型支持该参数，更多信息请参见[数据闪回](~~148479~~)。",
        "en": "Specifies whether to enable the data flashback feature for the instance. Valid values:\n\n*   **1**: enables the data flashback feature. You must also enable append-only file (AOF) persistence by setting `appendonly` to `yes` in the parameter settings of the instance. Then, you can use the data flashback feature.\n*   **0** (default): disables the data flashback feature.\n\n>  This parameter is available only for Tair (Enterprise Edition) DRAM-based and persistent memory-optimized instances. For more information, see [Data flashback](~~148479~~)."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The ID of the instance."
      }
    },
    {
      "name": "PreferredBackupPeriod",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "备份周期，取值：\n* **Monday**：周一。\n* **Tuesday**：周二。\n* **Wednesday**：周三。\n* **Thursday**：周四。\n* **Friday**：周五。\n* **Saturday**：周六。\n* **Sunday**：周日。\n\n> 支持多选，请使用英文逗号（,）分隔。",
        "en": "The days of the week to back up data. Valid values:\n\n*   **Monday**\n*   **Tuesday**\n*   **Wednesday**\n*   **Thursday**\n*   **Friday**\n*   **Saturday**\n*   **Sunday**\n\n> Separate multiple options with commas (,)."
      }
    },
    {
      "name": "PreferredBackupTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "备份时间，格式为<i>HH:mm</i>Z-<i>HH:mm</i>Z（UTC时间）。\n> 必须为整点，且间隔时间为1小时。",
        "en": "The time range to back up data. Specify the time in the *HH:mm*Z-*HH:mm*Z format. The time is displayed in UTC.\n\n> The beginning and end of the time range must be on the hour. The duration must be an hour."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改备份策略设置",
    "en": "ModifyBackupPolicy"
  },
  "example": {
    "unifiedCli": "aliyun r-kvstore modify-backup-policy --instance-id r-bp1zxszhcgatnx0000 --preferred-backup-time 07:00Z-08:00Z --preferred-backup-period Tuesday --enable-backup-log 1 --backup-retention-period 7 --region cn-hangzhou",
    "legacyCli": "aliyun r-kvstore ModifyBackupPolicy --region cn-hangzhou --PreferredBackupPeriod Tuesday --InstanceId 'r-bp1zxszhcgatnx0000' --PreferredBackupTime '07:00Z-08:00Z' --EnableBackupLog 1 --BackupRetentionPeriod 7"
  }
}