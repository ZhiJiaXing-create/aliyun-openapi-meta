{
  "name": "DescribeInstanceAutoRenewalAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符，且该参数值中不能包含非ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must ensure that it is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "DBInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例的ID。\n> 默认查询所有实例是否开通自动续费。",
        "en": "The ID of the instance.\n\n> By default, the system checks whether auto-renewal is enabled for all instances."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，大于**0**且不超过Integer的最大值，默认值：**1**。",
        "en": "The number of the page to return. The value must be an integer greater than **0**. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页记录数，取值：**30**、**50**、**100**。\n> 默认值：**30**。",
        "en": "The number of entries to return on each page. Valid values: **30**, **50**, and **100**.\n\n> The default value is **30**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例所属的地域ID。",
        "en": "The region ID of the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询实例是否开通自动续费",
    "en": "DescribeInstanceAutoRenewalAttribute"
  },
  "example": {
    "unifiedCli": "aliyun r-kvstore describe-instance-auto-renewal-attribute --client-token ETnLKlblzczshOTUbOCz0000 --biz-region-id cn-hangzhou --db-instance-id r-bp1zxszhcgatnx0000 --page-size 30 --page-number 1 --region cn-hangzhou",
    "legacyCli": "aliyun r-kvstore DescribeInstanceAutoRenewalAttribute --region cn-hangzhou --PageSize 30 --DBInstanceId 'r-bp1zxszhcgatnx0000' --PageNumber 1 --RegionId 'cn-hangzhou' --ClientToken ETnLKlblzczshOTUbOCz0000"
  }
}