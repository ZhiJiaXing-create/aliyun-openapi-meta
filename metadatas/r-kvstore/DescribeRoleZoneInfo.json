{
  "name": "DescribeRoleZoneInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID，可调用[DescribeInstances](~~473778~~)接口获取。",
        "en": "The ID of the instance. You can call the [DescribeInstances](~~473778~~) operation to query instance IDs."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，大于**0**且不超过Integer数据类型的最大值，默认值为**1**。",
        "en": "The page number. The value must be an integer that is greater than **0** and less than or equal to the maximum value supported by the integer data type. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页可展示的记录数，取值：**10**、**20**、**50**，默认值为**10**.",
        "en": "The number of entries per page. Valid values: **10**, **20**, and **50**. Default value: **10**."
      }
    },
    {
      "name": "QueryType",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询的目标节点类型，取值：\n* **0**：Proxy（代理）节点。\n  > 该参数仅支持集群与读写分离架构实例。\n\n\n* **1**：数据节点，默认值。",
        "en": "The type of the node to query. Default value: 1. Valid values:\n\n*   **0**: proxy node\n\n    **\n\n    **Note** This parameter is supported only for cluster instances and read/write splitting instances.\n\n*   **1**: data node"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询节点和所属可用区信息",
    "en": "DescribeRoleZoneInfo"
  },
  "example": {
    "unifiedCli": "aliyun r-kvstore describe-role-zone-info --instance-id r-t4nlenc2p04uvb0000 --query-type 0 --page-number 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun r-kvstore DescribeRoleZoneInfo --region cn-hangzhou --PageSize 10 --InstanceId 'r-t4nlenc2p04uvb0000' --PageNumber 1 --QueryType 0"
  }
}