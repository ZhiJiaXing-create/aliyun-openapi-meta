{
  "name": "ModifySecurityIps",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The ID of the instance."
      }
    },
    {
      "name": "ModifyMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "修改方式，取值：\n* **Cover**（默认）：覆盖原白名单。\n* **Append**：追加白名单。\n* **Delete**：删除该白名单。",
        "en": "The method that is used to modify the whitelist. Valid values:\n\n*   **Cover** (default): overwrites the original whitelist.\n*   **Append**: appends data to the whitelist.\n*   **Delete**: deletes the whitelist."
      }
    },
    {
      "name": "SecurityIpGroupAttribute",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "默认为空。用于区分不同的属性值，控制台将不显示该值为**hidden**的白名单分组。",
        "en": "By default, this parameter is left empty. The attribute of the whitelist. The console does not display the whitelist whose value of this parameter is **hidden**."
      }
    },
    {
      "name": "SecurityIpGroupName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IP白名单分组的名称。\n<warning>不可修改系统生成白名单分组。如果不传此字段，将默认修改 default 分组的白名单</warning>",
        "en": "The name of the IP address whitelist.\n><warning>You cannot modify the whitelist that is generated by the system. If you do not specify this parameter, the default whitelist is modified by default.></warning>"
      }
    },
    {
      "name": "SecurityIps",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "IP白名单分组下的IP列表，最多1000个。IP之间以逗号隔开，格式如下：0.0.0.0/0,10.23.12.24，或者10.23.12.24/24（CIDR模式，无类域间路由，/24表示地址中前缀的长度，范围1-32）。",
        "en": "The IP addresses in the whitelist. Up to 1,000 IP addresses can be specified in a whitelist. Separate multiple IP addresses with a comma (,). Specify an IP address in the 0.0.0.0/0, 10.23.12.24, or 10.23.12.24/24 format. In CIDR block 10.23.12.24/24, /24 specifies the length of the prefix of an IP address. The prefix length ranges from 1 to 32."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改IP白名单",
    "en": "ModifySecurityIps"
  },
  "descriptions": {
    "zh": "该API对应的控制台操作请参见[设置IP白名单](~~56464~~)。",
    "en": "You can also modify the whitelists of an instance in the Tair (Redis OSS-compatible) console. For more information, see [Configure a whitelist for an instance](~~56464~~)."
  },
  "example": {
    "unifiedCli": "aliyun r-kvstore modify-security-ips --instance-id r-bp1zxszhcgatnx0000 --security-ips 100.64.000.0/24,10.101.11.000 --security-ip-group-name default --security-ip-group-attribute hidden --modify-mode Append --region cn-hangzhou",
    "legacyCli": "aliyun r-kvstore ModifySecurityIps --region cn-hangzhou --SecurityIpGroupAttribute hidden --SecurityIps '100.64.000.0/24,10.101.11.000' --InstanceId 'r-bp1zxszhcgatnx0000' --SecurityIpGroupName default --ModifyMode Append"
  }
}