{
  "name": "DescribeZones",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置返回后可用区名称的语言，取值：\n* **zh-CN**（默认）：中文。\n* **en-US**：英文。",
        "en": "The display language of the zone names to return. Valid values:\n\n*   **zh-CN** (default): Chinese\n*   **en-US**: English"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询支持的可用区",
    "en": "DescribeZones"
  },
  "example": {
    "unifiedCli": "aliyun r-kvstore describe-zones --biz-region-id cn-hangzhou --accept-language en-US --region cn-hangzhou",
    "legacyCli": "aliyun r-kvstore DescribeZones --region cn-hangzhou --AcceptLanguage 'en-US' --RegionId 'cn-hangzhou'"
  }
}