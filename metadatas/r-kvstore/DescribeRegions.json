{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定返回参数**LocalName**对应的值的语言，取值：\n* **zh-CN**：中文。\n* **en-US**：英文。\n\n> 默认值：**zh-CN**。",
        "en": "The display language of the **LocalName** parameter value. Valid values:\n\n*   **zh-CN**: Chinese\n*   **en-US**: English\n\n> The default value is **zh-CN**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询可用的地域",
    "en": "DescribeRegions"
  },
  "example": {
    "unifiedCli": "aliyun r-kvstore describe-regions --accept-language zh-CN --region cn-hangzhou",
    "legacyCli": "aliyun r-kvstore DescribeRegions --region cn-hangzhou --AcceptLanguage 'zh-CN'"
  }
}