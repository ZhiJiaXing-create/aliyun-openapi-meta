{
  "name": "FlushInstanceForDB",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DbIndex",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "指定DB的索引号，取值为[0, 255]。",
        "en": "The index number of the database. Valid values: 0 to 255."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID，可调用 [DescribeInstances](~~473778~~) 获取。",
        "en": "The instance ID. You can call the [DescribeInstances](~~473778~~) operation to query the ID of the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "清理实例指定DB的数据",
    "en": "FlushInstanceForDB"
  },
  "descriptions": {
    "zh": "云数据库 Tair（兼容 Redis）实例支持256个DB，从DB0到DB255。每个DB没有单独的内存占用量限制，DB可使用的内存容量受该实例的总内存限制。您可以通过`SELECT`命令切换DB，更多信息请参见[Tair DB](~~38688~~)。\n> 本接口仅支持云原生实例（原云盘）集群架构。",
    "en": "Each Tair (Redis OSS-compatible) instance can contain up to 256 databases named from DB0 to DB255. Each database does not have a separate memory usage limit. The memory capacity that a database can use is subject to the total memory limit of the instance. You can execute the `SELECT` statement to switch between databases. For more information, see [What is the size of each database on a Tair (Redis OSS-compatible) instance, and how can I choose databases?](~~38688~~)\n\n>  This operation is available only for cloud-native instances that use cloud disks."
  },
  "example": {
    "unifiedCli": "aliyun r-kvstore flush-instance-for-db --instance-id r-bp1zxszhcgatnx0000 --db-index 1 --region cn-hangzhou",
    "legacyCli": "aliyun r-kvstore FlushInstanceForDB --region cn-hangzhou --InstanceId 'r-bp1zxszhcgatnx0000' --DbIndex 1"
  }
}