{
  "name": "ListZones",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/v1/zones",
  "parameters": [
    {
      "name": "gatewayEdition",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询可用区的目标网关版本。可选值：Professional（普通网关，默认）、ServerlessV2（API 多租 Serverless V2）。不传时按 Professional 处理。",
        "en": "The target gateway edition for querying zones. Valid values:\n- Professional: standard gateway. This is the default value.\n- ServerlessV2: API multi-tenant Serverless V2.\n\nIf this parameter is not specified, Professional is used."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取可用区",
    "en": "Get zones"
  },
  "example": {
    "unifiedCli": "aliyun apig list-zones --gateway-edition ServerlessV2 --region cn-hangzhou",
    "legacyCli": "aliyun apig GET /v1/zones --region cn-hangzhou --gatewayEdition ServerlessV2 --header \"Content-Type=application/json\""
  }
}