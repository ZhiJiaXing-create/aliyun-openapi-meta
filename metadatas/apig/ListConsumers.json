{
  "name": "ListConsumers",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/v1/consumers",
  "parameters": [
    {
      "name": "gatewayType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "网关类型过滤，目前支持**AI**、**API**两种网关类型。",
        "en": "Filters by gateway type. Valid values: **AI** and **API**."
      }
    },
    {
      "name": "nameLike",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按接口名称模糊搜索接口。",
        "en": "Fuzzy matches APIs by API name."
      }
    },
    {
      "name": "pageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页页码，从1开始，不填默认1。",
        "en": "The page number, starting from 1. Default value: 1."
      }
    },
    {
      "name": "pageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页大小，默认10",
        "en": "The number of entries per page. Default value: 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取消费者列表",
    "en": "Retrieve consumer list"
  },
  "example": {
    "unifiedCli": "aliyun apig list-consumers --name-like UI-test --page-number 1 --page-size 10 --gateway-type AI --region cn-hangzhou",
    "legacyCli": "aliyun apig GET /v1/consumers --region cn-hangzhou --nameLike 'UI-test' --pageNumber 1 --gatewayType AI --pageSize 10 --header \"Content-Type=application/json\""
  }
}