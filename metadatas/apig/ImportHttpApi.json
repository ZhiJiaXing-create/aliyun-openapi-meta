{
  "name": "ImportHttpApi",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/v1/http-apis/import",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求体参数。",
        "en": "The request body parameters."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "导入HTTP API",
    "en": "Import HTTP API"
  },
  "example": {
    "unifiedCli": "aliyun apig import-http-api --region cn-hangzhou",
    "legacyCli": "aliyun apig POST /v1/http-apis/import --region cn-hangzhou --header \"Content-Type=application/json\" --body \"key_:_value\""
  }
}