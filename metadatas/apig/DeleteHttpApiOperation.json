{
  "name": "DeleteHttpApiOperation",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/v1/http-apis/[httpApiId]/operations/[operationId]",
  "parameters": [
    {
      "name": "httpApiId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待删除的接口所属HTTP API ID。",
        "en": "The ID of the HTTP API to which the operation to be deleted belongs."
      }
    },
    {
      "name": "operationId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "接口ID。",
        "en": "The operation ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除接口",
    "en": "Delete an operation"
  },
  "example": {
    "unifiedCli": "aliyun apig delete-http-api-operation --http-api-id api-xxx --operation-id op-xxx --region cn-hangzhou",
    "legacyCli": "aliyun apig DELETE /v1/http-apis/api-xxx/operations/op-xxx --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}