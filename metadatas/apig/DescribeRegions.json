{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "/v1/regions",
  "parameters": [
    {
      "name": "language",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回结果语言，取值：zh、en。",
        "en": "The language of the response. Valid values: zh, en."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询APIG的Region列表",
    "en": "Query APIG region list"
  },
  "example": {
    "unifiedCli": "aliyun apig describe-regions --biz-language zh --region cn-hangzhou",
    "legacyCli": "aliyun apig POST /v1/regions --region cn-hangzhou --language zh --header \"Content-Type=application/json\""
  }
}