{
  "name": "UpdateHttpApi",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "PUT",
  "pathPattern": "/v1/http-apis/[httpApiId]",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求体参数。",
        "en": "The request body parameters."
      }
    },
    {
      "name": "httpApiId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待更新的HTTP API ID。",
        "en": "The ID of the HTTP API to update."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新HTTP API",
    "en": "Update an HTTP API"
  },
  "example": {
    "unifiedCli": "aliyun apig update-http-api --http-api-id api-xxx --region cn-hangzhou",
    "legacyCli": "aliyun apig PUT /v1/http-apis/api-xxx --region cn-hangzhou --header \"Content-Type=application/json\" --body \"key_:_value\""
  }
}