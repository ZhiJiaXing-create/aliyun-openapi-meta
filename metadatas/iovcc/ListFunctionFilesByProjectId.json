{
  "name": "ListFunctionFilesByProjectId",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageIndex",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "目标页号，默认为1",
        "en": "The page number. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页所显示的项数，默认为10",
        "en": "The number of items per page. Default value: 10."
      }
    },
    {
      "name": "ProjectId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "项目ID",
        "en": "The project ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "根据项目ID查询函数文件",
    "en": "Query function files by project ID"
  },
  "descriptions": {
    "zh": "> 如在创建触发器时，需要先列出当前项目关联的函数文件，然后进行函数的选择。",
    "en": "> For example, when creating a trigger, list the function files associated with the current project first, and then select a function."
  }
}