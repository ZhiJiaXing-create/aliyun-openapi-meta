{
  "name": "DeleteVersionGroupDevice",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DeviceGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "设备所属测试设备分组ID",
        "en": "The ID of the test device group to which the device belongs."
      }
    },
    {
      "name": "DeviceIdType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "用于判断的DeviceIds的类型，取值有：**device_id**，**sn**，**vin**，**hardware_id**，**uuid**。",
        "en": "The type of DeviceIds used for identification. Valid values: **device_id**, **sn**, **vin**, **hardware_id**, **uuid**."
      }
    },
    {
      "name": "DeviceIds",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "原始设备ID",
        "en": "The original device ID."
      }
    },
    {
      "name": "ProjectId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "所属项目ID",
        "en": "The ID of the project to which the device belongs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "从测试设备分组中删除一个特定设备",
    "en": "Remove a specific device from a test device group"
  }
}