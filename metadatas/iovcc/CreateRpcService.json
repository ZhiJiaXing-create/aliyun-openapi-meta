{
  "name": "CreateRpcService",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppKey",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "iovcc cmns应用appKey，应用唯一标识。",
        "en": "The AppKey of the IoVCC CMNS application. This is the unique identifier of the application."
      }
    },
    {
      "name": "GroupName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "服务端接口分组名称。",
        "en": "The group name of the server-side interface."
      }
    },
    {
      "name": "InterfaceName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "服务接口名称。\n\nhttp接口为不包含参数的请求地址。例如：`https://www.aliyun.com/rpcServiceTest`。",
        "en": "The name of the service interface.\n\nFor HTTP interfaces, specify the request URL without parameters. Example: `https://www.aliyun.com/rpcServiceTest`."
      }
    },
    {
      "name": "InvokeType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "接口调用类型。\n\n支持http（含https）和hsf。其中，hsf接口需与iovcc服务部署在同一vpc内，方能支持。",
        "en": "The invocation type of the interface.\n\nValid values: http (including https) and hsf. HSF interfaces must be deployed in the same VPC as the IoVCC service."
      }
    },
    {
      "name": "MethodName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "接口调用方法名称，由字母、数字和下划线(_)组成。\n\n- 调用类型为**http**时，名称为服务方自定义。\n- 调用类型为**hsf**时，名称为hsf接口方法名称。",
        "en": "The method name of the interface invocation. The name consists of letters, digits, and underscores (_).\n\n- If the invocation type is **http**, the name is custom-defined by the service provider.\n- If the invocation type is **hsf**, the name is the HSF interface method name."
      }
    },
    {
      "name": "Params",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "- 调用为http方式时，参数key以英文逗号隔开。例如：`param1,param2`。\n- 调用为hsf方式时，参数类型以英文逗号隔开。参数基本类型仅支持string，int，long，bytes，double，float，bool7种。例如：`string,int,long`。",
        "en": "- For HTTP invokes, separate parameter keys with commas (,). Example: `param1,param2`.\n- For HSF invokes, separate parameter types with commas (,). Only the following seven primitive data types are supported: string, int, long, bytes, double, float, and bool. Example: `string,int,long`."
      }
    },
    {
      "name": "ProjectId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "iovcc的项目工程ID。",
        "en": "The project ID of IoVCC."
      }
    },
    {
      "name": "VersionCode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "接口版本号，由服务方自定义设置。",
        "en": "The version number of the interface. This value is custom-defined by the service provider."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "注册cmns rpc服务接口",
    "en": "Register a CMNS RPC service interface"
  },
  "descriptions": {
    "zh": "> 支持服务接口以http、hsf两种方式暴露。",
    "en": "> Service interfaces can be exposed through HTTP or HSF."
  }
}