{
  "name": "UnTagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否删除全部标签。如果传入了TagKey，则只按TagKey删除。为true/false，为true时切未传入TagKey时删除全部标签。",
        "en": "Specifies whether to delete all labels. If TagKey is specified, labels are deleted only by TagKey. Valid values: true and false. If this parameter is set to true and TagKey is not specified, all labels are deleted."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域id。",
        "en": "The region ID."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "要删除的第N个资源Id，此处是为集群Id。如果有多个，可以传参数名为ResourceId.2、ResourceId.3等等。N为正整数。",
        "en": "The ID of the Nth resource to delete. The resource ID is the cluster ID. If you want to specify multiple resource IDs, pass parameter names such as ResourceId.2 and ResourceId.3. N is a positive integer."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "第N个要删除的标签的键，可传多个，TagKey.2、TagKey.3等待。N为正整数。",
        "en": "The key of the Nth label to delete. You can specify multiple keys, such as TagKey.2 and TagKey.3. N is a positive integer."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除资源标签",
    "en": "Delete resource labels"
  }
}