{
  "name": "DescribeBackups",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "备份类型。取值：\n\n- full：全量备份\n- Incremental：增量备份",
        "en": "The backup type. Valid values:\n\n- full: full backup\n- Incremental: incremental backup."
      }
    },
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "备份集群ID。",
        "en": "The ID of the backup cluster."
      }
    },
    {
      "name": "DataCenterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "备份集群数据中心ID。",
        "en": "The ID of the data center in the backup cluster."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取集群备份列表",
    "en": "Obtain cluster backup list"
  }
}