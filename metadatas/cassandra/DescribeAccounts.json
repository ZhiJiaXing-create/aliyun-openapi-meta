{
  "name": "DescribeAccounts",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "账号所属实例的ID。",
        "en": "The ID of the instance to which the accounts belong."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查找实例的帐户列表信息",
    "en": "Query account list of an instance"
  }
}