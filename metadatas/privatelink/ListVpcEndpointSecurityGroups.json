{
  "name": "ListVpcEndpointSecurityGroups",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndpointId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要查询的终端节点的ID。",
        "en": "The ID of the endpoint that you want to query."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小，取值范围：**1**~**50**，默认值：**50**。",
        "en": "The number of entries to return on each page. Valid values:**1** to **50**. Default value: **50**."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否拥有下一次查询的令牌（Token）。取值：\n- 第一次查询和没有下一次查询时，均无需填写。\n- 如果有下一次查询，取值为上一次API调用返回的**NextToken**值。",
        "en": "The pagination token that is used in the next request to retrieve a new page of results. Valid values:\n\n- If this is your first request and no next requests are to be performed, you do not need to specify this parameter.\n\n- If a next request is to be performed, set the parameter to the value of **NextToken** that is returned from the last call."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要查询的终端节点所属的地域ID。\n\n您可以通过调用[DescribeRegions](~~120468~~)接口获取地域ID。",
        "en": "The region ID of the endpoint that you want to query.\n\nYou can call the [DescribeRegions](~~120468~~) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询终端节点已加入的安全组",
    "en": "ListVpcEndpointSecurityGroups"
  },
  "example": {
    "unifiedCli": "aliyun privatelink list-vpc-endpoint-security-groups --biz-region-id cn-hangzhou --next-token caeba0bbb2be03f84eb48b699f0a4883 --max-results 50 --endpoint-id ep-hp33b2e43fays7s80000 --region cn-hangzhou",
    "legacyCli": "aliyun privatelink ListVpcEndpointSecurityGroups --region cn-hangzhou --NextToken caeba0bbb2be03f84eb48b699f0a4883 --MaxResults 50 --EndpointId 'ep-hp33b2e43fays7s80000' --RegionId 'cn-hangzhou'"
  }
}