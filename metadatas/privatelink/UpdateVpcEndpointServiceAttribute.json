{
  "name": "UpdateVpcEndpointServiceAttribute",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AddSupportedRegionSet",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "需要添加的终端节点服务支持的远程地域列表。",
        "en": "The list of remote regions to add for the endpoint service."
      }
    },
    {
      "name": "AddressIpVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "协议版本。取值：\n\n- **IPv4**：IPv4类型。\n- **DualStack**：双栈类型。\n> 目前仅后端资源类型为nlb、gwlb的终端节点服务支持指定IP地址协议为DualStack。",
        "en": "The protocol version. Valid values:\n\n- **IPv4**: IPv4.\n- **DualStack**: dual-stack.\n> Currently, only endpoint services whose backend resource type is NLB or GWLB support setting the IP address protocol to DualStack."
      }
    },
    {
      "name": "AutoAcceptEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否自动接受终端节点连接，取值：\n\n- **true**：自动接受终端节点连接。\n\n- **false**：不自动接受终端节点连接。",
        "en": "Specifies whether to automatically accept endpoint connections. Valid values:\n\n- **true**: automatically accepts endpoint connections.\n\n- **false**: does not automatically accept endpoint connections."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。**ClientToken**只支持ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The **ClientToken** parameter supports only ASCII characters."
      }
    },
    {
      "name": "ConnectBandwidth",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "默认连接带宽峰值。默认值：**3072**。单位：Mbps。\n\n取值范围：**100**~**10240**。\n\n> 当服务资源为传统型负载均衡CLB或应用型负载均衡ALB时，支持设置默认连接带宽峰值；当服务资源为网络型负载均衡NLB时，不支持设置连接带宽。",
        "en": "The default bandwidth limit. Default value: **3072**. Unit: Mbit/s.\n\nValid values: **100** to **10240**.\n\n> Settings for the default bandwidth limit are supported when the service resource is a Classic Load Balancer (CLB) instance or an Application Load Balancer (ALB) instance. When the service resource is a Network Load Balancer (NLB) instance, the connection bandwidth cannot be configured."
      }
    },
    {
      "name": "DeleteSupportedRegionSet",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "需要关闭的终端节点服务支持的远程地域列表。",
        "en": "The list of remote regions to remove from the endpoint service."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n- **true**：发送检查请求，不会修改资源属性。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP\n 2xx状态码并直接修改资源属性。",
        "en": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\n\n- **true**: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error code is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\n- **false** (default): performs a dry run and performs the actual request. If the request passes the dry run, an HTTP 2xx status code is returned and the resource attributes are modified."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "终端节点服务所在的地域ID。\n\n您可以通过调用[DescribeRegions](~~120468~~)接口获取地域ID。",
        "en": "The region ID of the endpoint service.\n\nYou can call the [DescribeRegions](~~120468~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ServiceDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "终端节点服务的描述信息。",
        "en": "The description of the endpoint service."
      }
    },
    {
      "name": "ServiceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "终端节点服务的ID。",
        "en": "The ID of the endpoint service."
      }
    },
    {
      "name": "ServiceSupportIPv6",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "终端节点服务是否支持IPv6功能。取值：\n\n- **true**：是。\n\n- **false**（默认值）：否。",
        "en": "Specifies whether the endpoint service supports IPv6. Valid values:\n\n- **true**: yes.\n\n- **false** (default): no."
      }
    },
    {
      "name": "ZoneAffinityEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否支持连接服务的终端节点域名就近解析。取值：\n\n- **true**（默认值）：是。\n- **false**：否。",
        "en": "Specifies whether to resolve the domain name of the endpoint that is connected to the endpoint service to the nearest access point. Valid values:\n\n- **true** (default): yes.\n- **false**: no."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改终端节点服务的属性",
    "en": "Modify the attributes of an endpoint service"
  },
  "descriptions": {
    "zh": "- 修改终端节点服务的属性之前，请确保终端节点服务处于**Active**状态，您可以通过调用[GetVpcEndpointServiceAttribute](~~469330~~)接口，查询终端节点服务状态。\n- **UpdateVpcEndpointServiceAttribute**接口不支持在同一个阿里云账号（主账号）下并发修改终端节点服务的属性。",
    "en": "- Before you modify the attributes of an endpoint service, make sure that the endpoint service is in the **Active** state. You can call the [GetVpcEndpointServiceAttribute](~~469330~~) operation to query the endpoint service status.\n- The **UpdateVpcEndpointServiceAttribute** operation does not support concurrent modification of endpoint service attributes within the same Alibaba Cloud account."
  },
  "example": {
    "unifiedCli": "aliyun privatelink update-vpc-endpoint-service-attribute --biz-region-id cn-hangzhou --client-token 0c593ea1-3bea-11e9-b96b-88e9fe637760 --dry-run false --service-id epsrv-hp3vpx8yqxblby3i0000 --service-description This_is_my_EndpointService. --auto-accept-enabled false --connect-bandwidth 3072 --zone-affinity-enabled true --service-support-ipv6 false --address-ip-version IPv4 --add-supported-region-set cn-huhehaote --delete-supported-region-set cn-beijing --region cn-hangzhou",
    "legacyCli": "aliyun privatelink UpdateVpcEndpointServiceAttribute --region cn-hangzhou --AddressIpVersion IPv4 --ServiceDescription 'This_is_my_EndpointService.' --AddSupportedRegionSet.1 'cn-huhehaote' --DryRun false --ConnectBandwidth 3072 --RegionId 'cn-hangzhou' --ClientToken '0c593ea1-3bea-11e9-b96b-88e9fe637760' --ZoneAffinityEnabled true --DeleteSupportedRegionSet.1 'cn-beijing' --ServiceId 'epsrv-hp3vpx8yqxblby3i0000' --AutoAcceptEnabled false --ServiceSupportIPv6 false --method POST --force"
  }
}