{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "ServiceResourceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "PrivateLink支持的开服地域，与服务资源类型有关，不同的服务类型支持的地域存在差异。在查询PrivateLink开服地域时可指定服务资源类型，取值如下：\n\n- **slb**（默认）：表示服务资源类型为传统型负载均衡CLB（Classic Load Balancer）。\n-  **alb**：表示服务资源类型为应用型负载均衡ALB（Application Load Balancer）。\n- **nlb**：表示服务资源类型为网络型负载均衡NLB（Network Load Balancer ）。\n- **gwlb**：表示服务资源类型为网关型负载均衡 GWLB（Gateway Load Balancer）。\n\n- **ALL**：上述全部。",
        "en": "The EPS resource type supported by PrivateLink. The available regions vary based on the service resource type. You can specify a service resource type when querying the regions where PrivateLink is available. Valid values:\n\n- **slb** (default): the service resource type is Classic Load Balancer (CLB).\n- **alb**: the service resource type is Application Load Balancer (ALB).\n- **nlb**: the service resource type is Network Load Balancer (NLB).\n- **gwlb**: the service resource type is Gateway Load Balancer (GWLB).\n\n- **ALL**: all of the preceding types."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询私网连接支持的地域列表",
    "en": "Query regions supported by privateLink"
  },
  "example": {
    "unifiedCli": "aliyun privatelink describe-regions --biz-region-id cn-hangzhou --service-resource-type slb --region cn-hangzhou",
    "legacyCli": "aliyun privatelink DescribeRegions --region cn-hangzhou --ServiceResourceType slb --RegionId 'cn-hangzhou'"
  }
}