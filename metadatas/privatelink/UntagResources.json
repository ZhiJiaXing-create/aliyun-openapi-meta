{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解绑资源上全部的标签，取值：\n\n- **true**：解绑资源上的全部标签。\n\n- **false**：不解绑全部标签。\n\n> 如果同时设置了**TagKey**和本参数，则本参数不生效。",
        "en": "Specifies whether to remove all tags from the resource. Valid values:\n\n- **true**\n\n- **false**\n\n> If you specify both the All and TagKey.N parameters, the All parameter does not take effect."
      }
    },
    {
      "name": "ClientToken",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。**ClientToken**只支持ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the request ID as the client token.\\*\\*\\*\\*\\*\\*\\*\\* The request ID may be different for each request."
      }
    },
    {
      "name": "DryRun",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n\n- **true**：发送检查请求，不会解绑标签。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n\n- **false**（默认值）：发送正常请求，通过检查后返回`HTTP 2xx`状态码并直接进行操作。",
        "en": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\n\n- **true**: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\n- **false** (default): performs a dry run and performs the actual request. If the request passes the dry run, a `2xx HTTP` status code is returned and the operation is performed."
      }
    },
    {
      "name": "RegionId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。您可以通过调用[describeregions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the PrivateLink instance.\n\nYou can call the [DescribeRegions](~~120468~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceId",
      "position": "Body",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "资源ID列表。最多支持50个资源ID。",
        "en": "The resource IDs. You can specify up to 50 resource IDs."
      }
    },
    {
      "name": "ResourceType",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。取值：\n\n- **vpcendpoint**：终端节点。                                \n\n- **vpcendpointservice**：终端节点服务。",
        "en": "The resource type. Valid values:\n\n- **vpcendpoint**: endpoint\n\n- **vpcendpointservice**: endpoint service"
      }
    },
    {
      "name": "TagKey",
      "position": "Body",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "要解绑的标签键列表。最多支持20个标签键。",
        "en": "The keys of the tags that you want to remove from the resources. You can specify up to 20 tag keys."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为指定的资源列表统一解绑标签",
    "en": "UntagResources"
  },
  "example": {
    "unifiedCli": "aliyun privatelink untag-resources --resource-id ep-hp3i05294c20000 --tag-key FinanceDept --resource-type vpcendpoint --all true --dry-run false --client-token 0ED8D006-F706-4D23-88ED-E11ED28DCAC0 --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun privatelink UntagResources --region cn-hangzhou --All true --ResourceId.1 'ep-hp3i05294c20000' --DryRun false --TagKey.1 FinanceDept --ResourceType vpcendpoint --RegionId 'cn-hangzhou' --ClientToken '0ED8D006-F706-4D23-88ED-E11ED28DCAC0' --method POST --force"
  }
}