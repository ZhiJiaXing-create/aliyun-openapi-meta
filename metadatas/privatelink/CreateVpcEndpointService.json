{
  "name": "CreateVpcEndpointService",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AddressIpVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "协议版本。取值：\n\n- **IPv4**：IPv4类型（默认）。\n- **DualStack**：双栈类型。\n> 目前仅后端资源类型为nlb、gwlb的终端节点服务支持指定IP地址协议为DualStack。终端节点服务支持双栈，要求其后端资源也需支持双栈。",
        "en": "The protocol version. Valid values:\n\n- **IPv4**: IPv4 (default).\n- **DualStack**: dual-stack.\n> Only endpoint services whose backend resource type is nlb or gwlb support the DualStack IP address protocol. If the endpoint service supports dual-stack, the backend resources must also support dual-stack."
      }
    },
    {
      "name": "AutoAcceptEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否自动接受终端节点连接，取值：\n\n- **true**：自动接受终端节点连接。\n\n- **false**（默认值）：不自动接受终端节点连接。",
        "en": "Specifies whether to automatically accept endpoint connections. Valid values:\n\n- **true**: automatically accept endpoint connections.\n\n- **false** (default): do not automatically accept endpoint connections."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。**ClientToken**只支持ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n- **true**：发送检查请求，不会创建资源。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作。",
        "en": "Specifies whether to perform only a dry run, without performing the actual request. Valid values:\n\n- **true**: performs only a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error code is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\n- **false** (default): performs a dry run and performs the actual request. If the request passes the dry run, an HTTP 2xx status code is returned and the operation is performed."
      }
    },
    {
      "name": "Payer",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "付费方，取值：\n\n- **Endpoint**：服务使用方。\n\n- **EndpointService**：服务提供方。",
        "en": "The payer. Valid values:\n\n- **Endpoint**: service consumer.\n\n- **EndpointService**: service provider."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "终端节点服务所在的地域ID。\n\n您可以通过调用[DescribeRegions](~~469325~~)接口获取地域ID。",
        "en": "The region ID of the endpoint service.\n\nYou can call the [DescribeRegions](~~469325~~) operation to query the most recent region list."
      }
    },
    {
      "name": "Resource",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "终端节点服务资源合集。创建时最多支持10个资源。创建后可通过为终端节点添加服务资源继续添加。",
        "en": "The service resources of the endpoint service. You can add up to 10 resources during creation. After creation, you can add more resources by adding service resources to the endpoint."
      },
      "sub_parameters": [
        {
          "name": "ResourceId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "添加到终端节点服务中的服务资源ID。",
            "en": "The ID of the service resource to add to the endpoint service."
          }
        },
        {
          "name": "ResourceType",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "添加到终端节点服务中的服务资源的类型，一个终端节点服务最多支持添加20个服务资源。取值：\n\n- **slb** ：表示服务资源类型为传统型负载均衡CLB（Classic Load Balancer）。\n\n- **alb** ：表示服务资源类型为应用型负载均衡ALB（Application Load Balancer）。\n- **nlb** ：表示服务资源类型为网络型负载均衡NLB（Network Load Balancer ）。\n- **gwlb** : 表示服务资源类型为网关型负载均衡 GWLB（Gateway Load Balancer）。",
            "en": "The EPS resource type to add to the endpoint service. You can add up to 20 service resources to an endpoint service. Valid values:\n\n- **slb**: Classic Load Balancer (CLB).\n\n- **alb**: Application Load Balancer (ALB).\n- **nlb**: Network Load Balancer (NLB).\n- **gwlb**: Gateway Load Balancer (GWLB)."
          }
        },
        {
          "name": "ZoneId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "可用区ID。",
            "en": "The zone ID."
          }
        }
      ]
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The resource group ID."
      }
    },
    {
      "name": "ServiceDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "终端节点服务的描述信息。",
        "en": "The description of the endpoint service."
      }
    },
    {
      "name": "ServiceResourceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "服务资源类型。取值：\n\n- **slb** ：表示服务资源类型为传统型负载均衡CLB（Classic Load Balancer）。\n\n- **alb** ：表示服务资源类型为应用型负载均衡ALB（Application Load Balancer）。\n- **nlb** ：表示服务资源类型为网络型负载均衡NLB（Network Load Balancer ）。\n- **gwlb** : 表示服务资源类型为网关型负载均衡 GWLB（Gateway Load Balancer）。\n\n> 不支持访问NLB的TCPSSL类型监听。",
        "en": "The EPS resource type. Valid values:\n\n- **slb**: Classic Load Balancer (CLB).\n\n- **alb**: Application Load Balancer (ALB).\n- **nlb**: Network Load Balancer (NLB).\n- **gwlb**: Gateway Load Balancer (GWLB).\n\n> Access to TCPSSL listeners of NLB is not supported."
      }
    },
    {
      "name": "ServiceSupportIPv6",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "终端节点服务是否支持IPv6功能。取值：\n\n- **true**：是。\n\n- **false**（默认值）：否。",
        "en": "Specifies whether the endpoint service supports IPv6. Valid values:\n\n- **true**: supported.\n\n- **false** (default): not supported."
      }
    },
    {
      "name": "SupportedRegionList",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "终端节点服务支持的地域列表，服务使用方可从列表中的地域发起终端节点连接。",
        "en": "The list of regions supported by the endpoint service. Service consumers can initiate endpoint connections from the regions in the list."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表。",
        "en": "The list of tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "实例的标签键。最多支持20个标签键。一旦传入该值，则不允许为空字符串。\n\n最多支持64个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag key of the instance. You can specify up to 20 tag keys. The tag key cannot be an empty string.\n\nThe tag key can be up to 64 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "实例的标签值。最多支持20个标签值。一旦传入该值，可以为空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag value of the instance. You can specify up to 20 tag values. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`."
          }
        }
      ]
    },
    {
      "name": "ZoneAffinityEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否支持连接服务的终端节点域名就近解析，取值：\n\n- **true**：是。\n\n- **false**（默认值）：否。",
        "en": "Specifies whether to support zone affinity for the endpoint domain name of the endpoint service. Valid values:\n\n- **true**: supported.\n\n- **false** (default): not supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建终端节点服务",
    "en": "Create an endpoint service"
  },
  "descriptions": {
    "zh": "- 创建终端节点服务之前，请确保您已创建支持私网连接的负载均衡资源。具体操作，请参见[创建负载均衡实例](~~174064~~)。\n- **CreateVpcEndpointService**接口属于异步接口，即系统会返回一个实例ID，但该终端节点服务尚未创建完成，系统后台任务仍在进行中。您可以调用[GetVpcEndpointServiceAttribute](~~183542~~)查询终端节点服务的创建状态：\n\n     - 当终端节点服务处于**Creating**状态时，表示终端节点服务正在创建。\n\n     - 当终端节点服务处于**Active**状态时，表示终端节点服务创建完成。",
    "en": "- Before you create an endpoint service, make sure that you have created a load balancing resource that supports PrivateLink. For more information, see [Create a load balancer instance](~~174064~~).\n- **CreateVpcEndpointService** is an asynchronous operation. After a request is sent, the system returns an instance ID, but the endpoint service is not yet created. The background node is still running. You can invoke [GetVpcEndpointServiceAttribute](~~183542~~) to query the creation status of the endpoint service:\n\n     - If the endpoint service is in the **Creating** state, the endpoint service is being created.\n\n     - If the endpoint service is in the **Active** state, the endpoint service is created."
  },
  "example": {
    "unifiedCli": "aliyun privatelink create-vpc-endpoint-service --biz-region-id cn-hangzhou --client-token 0c593ea1-3bea-11e9-b96b-88e9fe637760 --dry-run false --service-description This_is_my_EndpointService. --auto-accept-enabled false --payer Endpoint --zone-affinity-enabled false --service-resource-type slb --resource ZoneId=cn-huhehaote-a ResourceId=lb-hp32z1wp5peaoox2q0000 ResourceType=slb --service-support-ipv6 false --resource-group-id rg-acfmy00000 --tag Value=prod Key=env --address-ip-version IPv4 --supported-region-list cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun privatelink CreateVpcEndpointService --region cn-hangzhou --Payer Endpoint --Tag.1.Key env --AddressIpVersion IPv4 --SupportedRegionList.1 'cn-hangzhou' --ResourceGroupId 'rg-acfmy00000' --Resource.1.ZoneId 'cn-huhehaote-a' --ServiceDescription 'This_is_my_EndpointService.' --DryRun false --Resource.1.ResourceType slb --AutoAcceptEnabled false --ServiceSupportIPv6 false --Resource.1.ResourceId 'lb-hp32z1wp5peaoox2q0000' --Tag.1.Value prod --ServiceResourceType slb --RegionId 'cn-hangzhou' --ClientToken '0c593ea1-3bea-11e9-b96b-88e9fe637760' --ZoneAffinityEnabled false --method POST --force"
  }
}