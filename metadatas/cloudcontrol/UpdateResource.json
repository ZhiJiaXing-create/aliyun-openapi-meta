{
  "name": "UpdateResource",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/api/v1/providers/[provider]/products/[product]/resources/*",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。需要更新的资源属性，以JSON的形式传入。",
        "en": "The request body. The property of the resource to be updated is specified in JSON format."
      }
    },
    {
      "name": "clientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "幂等参数。若云产品支持幂等能力，则传入生效。",
        "en": "The client token that is used to ensure the idempotence of the request. If a cloud service supports idempotence, the parameter takes effect."
      }
    },
    {
      "name": "regionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。若云产品是region化产品，则此参数为必填。",
        "en": "The region ID. This parameter is required if a cloud service is a regionalized."
      }
    },
    {
      "name": "requestPath",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求路径。格式为：\n/api/v1/providers/{provider}/products/{product}/resources/{resourceType}/{resourceId}\n\n请求路径中变量说明：\n\nprovider: 云厂商，目前只支持Aliyun。\n\nproduct: 产品Code。\n\nresourceType: 资源类型。有父资源时，格式为{父资源类型code}/父资源ID/{资源类型code}。\n\nresourceId: 资源ID。",
        "en": "The request path. Specify this parameter in the following format: /api/v1/providers/{provider}/products/{product}/resources/{resourceType}/{resourceId}\n\nVariables in the request path:\n\nprovider: the Cloud service provider. Only Aliyun is supported.\n\nproduct: the code of the service.\n\nresourceType: the type of resources. If parent resource is specified, the format is {parent resource type code}/parent resource ID/{resource type code}.\n\nresourceId: the resource ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新资源",
    "en": "UpdateResource"
  },
  "descriptions": {
    "zh": "用户可前往[在线调试API门户](https://next.api.aliyun.com/cloudcontrol)查看资源文档和体验Cloud Control API。\n\n如果更新资源在任何时候失败，云控制 API 不会将资源回滚到先前的状态。\n\n由于资源API目前不支持回滚，当用户遇到API部分失败时，需手动调用GetResources API查看资源最新的状态，然后调用UpdateResource或DeleteResource进行手动补偿（如果有必要）。",
    "en": "You can go to [OpenAPI Explorer](https://next.api.aliyun.com/cloudcontrol) to view the documentation and try out Cloud Control API.\n\nIf resources fail to be updated at any time, the Cloud Control API does not roll the resource back to the original status.\n\nThe resource APIs cannot be rolled back. If the API operation is partially failed to be called, you can call the GetResource operation to view the latest status of the resource. If necessary, you can call the UpdateResource or DeleteResource operation to manually compensate for the failure."
  }
}