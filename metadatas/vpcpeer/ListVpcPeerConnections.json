{
  "name": "ListVpcPeerConnections",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要查询详细信息的VPC对等连接实例ID。",
        "en": "The ID of the VPC peering connection."
      }
    },
    {
      "name": "MaxResults",
      "position": "Body",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分批次查询时每次显示的条目数。取值范围：**1**~**100**，默认值为**20**。",
        "en": "The number of entries to return on each page. Valid values: **1** to **100**. Default value: **20**."
      }
    },
    {
      "name": "Name",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要查询详细信息的VPC对等连接的名称。",
        "en": "The name of the VPC peering connection."
      }
    },
    {
      "name": "NextToken",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否拥有下一次查询的令牌（Token）。取值：\n- 第一次查询和没有下一次查询时，均无需填写。\n- 如果有下一次查询，取值为上一次API调用返回的NextToken值。",
        "en": "The token that is used for the next query. Valid values:\n\n- Do not specify this parameter for the first request.\n\n- You must specify the token that is obtained from the previous query as the value of NextToken."
      }
    },
    {
      "name": "RegionId",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要查询详细信息的VPC对等连接的地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The region ID of the VPC peering connection.\n\nYou can call the [DescribeRegions](~~36063~~) operation to obtain the region ID."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。\n\n关于资源组的更多信息，请参见[什么是资源组](~~94475~~)。",
        "en": "The ID of the resource group.\n\nFor more information about resource groups, see [What is a resource group?](~~94475~~)."
      }
    },
    {
      "name": "Tags",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表。",
        "en": "The tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签键。最多支持输入20个标签键。一旦传入该值，则不允许为空字符串。\n\n一个标签键最多支持 128 个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag key. You can specify up to 20 tag keys. The tag key cannot be an empty string.\n\nThe key can be up to 128 characters in length. It cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签值。最多支持输入20个标签值。一旦传入该值，可以为空字符串。\n\n一个标签值最多支持 128 个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag value. You can specify up to 20 tag values. The tag value can be an empty string.\n\nThe value can be up to 128 characters in length. It cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`."
          }
        }
      ]
    },
    {
      "name": "VpcId",
      "position": "Body",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "根据两端vpcid过滤，不区分发起端和接收端。如果只传入一个，则根据一端过滤",
        "en": "The ID of a VPC in the peering connection. You can specify the ID of the requester or accepter VPC. If you specify only one VPC ID, the query returns all peering connections that involve the specified VPC."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询VPC对等连接列表",
    "en": "ListVpcPeerConnections"
  },
  "example": {
    "unifiedCli": "aliyun vpcpeer list-vpc-peer-connections --biz-region-id cn-hangzhou --instance-id pcc-lnk0m24khwvtkm0000 --vpc-id vpc-bp1gsk7h12ew7oegk0000 --name vpcpeer --next-token FFmyTO70tTpLG6I3FmYAXGKPd0000 --max-results 20 --tags Value=FinanceJoshua Key=FinanceDept --resource-group-id rg-acfmxazb4ph6aiy0000 --region cn-hangzhou",
    "legacyCli": "aliyun vpcpeer ListVpcPeerConnections --region cn-hangzhou --Tags.1.Value FinanceJoshua --VpcId 'vpc-bp1gsk7h12ew7oegk0000' --NextToken FFmyTO70tTpLG6I3FmYAXGKPd0000 --ResourceGroupId 'rg-acfmxazb4ph6aiy0000' --InstanceId 'pcc-lnk0m24khwvtkm0000' --MaxResults 20 --Tags.1.Key FinanceDept --RegionId 'cn-hangzhou' --Name vpcpeer"
  }
}