{
  "name": "DeleteBackups",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupIds",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待删除的备份集ID，多个备份集ID以英文逗号（,）分隔。",
        "en": "The ID of the backup set that you want to delete. Separate multiple backup set IDs with commas (,)."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "AnalyticDB for MySQL集群ID。",
        "en": "The ID of the AnalyticDB for MySQL cluster."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID of the cluster."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除备份集",
    "en": "DeleteBackups"
  },
  "descriptions": {
    "zh": "- 删除备份集是异步操作，前端显示会有延迟，需要等待约10-20分钟才会生效。\n- 单次最大允许删除100个备份集。如果要删超过100个备份集，请分2次调用。\n- 为保证数据安全性，系统会强制保留1个有效备份集。当要删除仅有的备份集时，接口会禁止。",
    "en": "*   Deleting backup sets is an asynchronous operation and may require 10 to 20 minutes to complete.\n*   You can delete up to 100 backup sets at a time. If you want to delete more than 100 backup sets, call this operation twice.\n*   To ensure data security, the system forcibly retains one valid backup set. If you want to delete the last backup set, the system prohibits your operation."
  },
  "example": {
    "unifiedCli": "aliyun adb delete-backups --db-cluster-id am-bp1ub9grke10000 --backup-ids 766,767 --biz-region-id cn-hangzhou --api-version 2019-03-15 --region cn-hangzhou",
    "legacyCli": "aliyun adb DeleteBackups --region cn-hangzhou --DBClusterId 'am-bp1ub9grke10000' --RegionId 'cn-hangzhou' --BackupIds '766,767'"
  }
}