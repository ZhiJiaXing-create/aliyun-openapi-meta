{
  "name": "CreateDBCluster",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupSetID",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预留参数，不涉及。",
        "en": "This parameter is reserved."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性。由客户端生成该参数值，保证在不同请求间唯一。大小写敏感、不超过64个ASCII 字符。",
        "en": "A client-generated token to ensure request idempotence. The token must be unique across requests, case-sensitive, and up to 64 ASCII characters long."
      }
    },
    {
      "name": "ComputeResource",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "计算资源用于数据计算，增加计算资源可以提高数据查询速度。计算资源分为集群版和单机版，其中：\n- 集群版为16核64 GB（集群版）、24核96 GB（集群版）和32核及以上规格。集群版支持资源池隔离，分时弹性功能和数据冷热分层。\n- 单机版为8核32 GB和16核64 GB（单机版）。单机版不提供SLA承诺，故障恢复耗时长（大约为4~8小时），不建议用于生产环境。\n\n> - 您可以调用[DescribeAvailableResource](~~190632~~)接口查询指定地域中可用的计算资源。\n> - Mode取值为**Flexible**（即弹性模式）时，本参数必填。",
        "en": "The compute resources for the cluster. Compute resources are used for data queries. A larger amount of compute resources can provide better query performance. Compute resources are available in cluster and single-node editions:\n\n- Cluster edition: includes specifications such as 16 cores/64 GB, 24 cores/96 GB, and 32 cores or more. The cluster edition supports resource pool isolation, scheduled scaling, and tiered storage of hot and cold data.\n\n- Single-node edition: includes specifications such as 8 cores/32 GB and 16 cores/64 GB. The single-node edition does not provide an SLA guarantee and has a long recovery time from failures (4 to 8 hours). We do not recommend that you use the single-node edition in production environments.\n\n> * You can call the [DescribeAvailableResource](~~190632~~) operation to query the compute resources that are available in a specific region.\n>\n> * This parameter is required when `Mode` is set to `Flexible` (flexible mode)."
      }
    },
    {
      "name": "DBClusterCategory",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "系列。取值说明：\n- **Cluster**：预留模式集群版。\n\n<props=\"china\">\n> 仅中国内地地域和新加坡地域支持购买预留模式集群版。其中，新加坡地域仅支持在按量付费模式下购买预留模式集群版。</props>\n\n- **MixedStorage**：弹性模式集群版（新版）。\n\n> 当DBClusterCategory为Cluster时，Mode参数必须填写Reserver；当DBClusterCategory为MixedStorage时，Mode参数必须填写Flexible，否则会导致创建集群失败。",
        "en": "The cluster edition. Valid values:\n\n- **Cluster**: reserved cluster.\n\n<props=\"china\">\n\n> Reserved clusters are available only in the Chinese mainland and Singapore regions. In the Singapore region, you can purchase reserved clusters only with the pay-as-you-go billing method.\n\n</props>\n\n- **MixedStorage**: elastic cluster (new).\n\n> If you set `DBClusterCategory` to `Cluster`, you must set the `Mode` parameter to `Reserved`. If you set `DBClusterCategory` to `MixedStorage`, you must set the `Mode` parameter to `Flexible`. Otherwise, cluster creation will fail."
      }
    },
    {
      "name": "DBClusterClass",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "集群规格，取值：\n\n- **C8**\n- **C32**\n\n>Mode取值为Reserver（即预留模式）时，本参数必填。",
        "en": "The cluster specification. Valid values:\n\n- **C8**\n\n- **C32**\n\n> This parameter is required when `Mode` is set to `Reserved` (reserved mode)."
      }
    },
    {
      "name": "DBClusterDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "集群描述。\n- 不能以`http://`或者`https`开头。\n- 长度为2~256个字符。",
        "en": "The description of the cluster.\n\n- The description cannot start with `http://` or `https://`.\n\n- The description must be 2 to 256 characters long."
      }
    },
    {
      "name": "DBClusterNetworkType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网络类型：**VPC**。",
        "en": "The network type of the cluster. Set the value to **VPC**."
      }
    },
    {
      "name": "DBClusterVersion",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "AnalyticDB MySQL数仓版集群版本。取值：**3.0**。",
        "en": "The version of the AnalyticDB for MySQL cluster. Set the value to **3.0**."
      }
    },
    {
      "name": "DBNodeGroupCount",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "节点组数量，取值范围为：1~200（整数）。\n>Mode取值为Reserver（即预留模式）时，本参数必填。",
        "en": "The number of node groups. The value must be an integer from 1 to 200.\n\n> This parameter is required when `Mode` is set to `Reserved` (reserved mode)."
      }
    },
    {
      "name": "DBNodeStorage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "节点存储空间，单位：GB。取值说明：\n\n- 规格为C8时，取值范围为：100~1000。\n- 规格为C32时，取值范围为：100~8000。\n\n> - Mode取值为Reserver（即预留模式）时，本参数必填。\n> - 1000 GB以下步长为100 GB，1000 GB以上步长为1000 GB。",
        "en": "The node storage capacity. Unit: GB. The valid values vary based on the cluster specification:\n\n- For the `C8` specification, the value ranges from 100 to 1,000.\n\n- For the `C32` specification, the value ranges from 100 to 8,000.\n\n> * This parameter is required when `Mode` is set to `Reserved` (reserved mode).\n>\n> * The value must be a multiple of 100 for storage sizes under 1,000 GB, and a multiple of 1,000 for storage sizes of 1,000 GB or more."
      }
    },
    {
      "name": "DiskEncryption",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启云盘加密。取值：\n\n- true：是。\n- false：否。",
        "en": "Specifies whether to enable disk encryption. Valid values:\n\n- `true`: enables disk encryption.\n\n- `false`: disables disk encryption."
      }
    },
    {
      "name": "ElasticIOResource",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "弹性IO资源（Elastic IO Unit，简称EIU），更多详情，请参见[EIU详解](~~189505~~)。",
        "en": "The number of Elastic IO Units (EIUs). For more information, see [EIU details](~~189505~~)."
      }
    },
    {
      "name": "EnableSSL",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启SSL链路加密功能，取值：\n\n- **true**：开启。\n- **false**：关闭。",
        "en": "Specifies whether to enable SSL encryption. Valid values:\n\n- **true**: enables SSL encryption.\n\n- **false**: disables SSL encryption."
      }
    },
    {
      "name": "ExecutorCount",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预留参数，不涉及。",
        "en": "This parameter is reserved."
      }
    },
    {
      "name": "KmsId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于云盘加密的kmsId，仅当DiskEncryption为true时有效。",
        "en": "The ID of the Key Management Service (KMS) key that is used for disk encryption. This parameter is valid only when `DiskEncryption` is set to `true`."
      }
    },
    {
      "name": "Mode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模式。取值说明：\n- **Reserver**：预留模式。\n- **Flexible**：弹性模式。",
        "en": "The cluster mode. Valid values:\n\n- **Reserved**: reserved mode.\n\n- **Flexible**: flexible mode."
      }
    },
    {
      "name": "PayType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "付费类型。取值说明：\n\n- **Postpaid**：按量付费。\n- **Prepaid**：预付费（包年包月）。",
        "en": "The billing method of the cluster. Valid values:\n\n- **Postpaid**: pay-as-you-go.\n\n- **Prepaid**: subscription."
      }
    },
    {
      "name": "Period",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定预付费集群为包年或包月类型。取值说明：\n- **Year**：包年类型。\n- **Month**：包月类型。\n\n>PayType为Prepaid（即预付费（包年包月））时，本参数为必填参数。",
        "en": "The subscription period unit. Valid values:\n\n- **Year**\n\n- **Month**\n\n> This parameter is required when `PayType` is set to `Prepaid` (subscription)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。\n>您可以调用[DescribeRegions](~~143074~~)接口查看地域ID。",
        "en": "The region ID.\n\n> You can call the [DescribeRegions](~~143074~~) operation to query the IDs of available regions."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The resource group ID."
      }
    },
    {
      "name": "RestoreTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预留参数，不涉及。",
        "en": "This parameter is reserved."
      }
    },
    {
      "name": "RestoreType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预留参数，不涉及。",
        "en": "This parameter is reserved."
      }
    },
    {
      "name": "SourceDBInstanceName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预留参数，不涉及。",
        "en": "This parameter is reserved."
      }
    },
    {
      "name": "StorageResource",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预留参数，不涉及。",
        "en": "This parameter is reserved."
      }
    },
    {
      "name": "StorageType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预留参数，不涉及。",
        "en": "This parameter is reserved."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签信息列表。",
        "en": "The list of tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签键，可以根据标签过滤集群列表。最多20对标签，各个标签对的数字n必须不同，且必须是从1开始的连续整数。`Tag.N.Key`对应的值为`Tag.N.Value`。\n\n> 最多支持64个字符，不能以`aliyun`、`acs:`、`http://`或者`https://`开头。",
            "en": "The tag key. You can use tags to filter clusters. You can specify up to 20 tags. N in `Tag.N.Key` must be a unique and consecutive integer that starts from 1. `Tag.N.Key` is paired with `Tag.N.Value`.\n\n> The tag key can be up to 64 characters in length and cannot start with `aliyun`, `acs:`, `http://`, or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签值，可以根据标签过滤集群列表。最多20对标签，各个标签对的数字n必须不同，且必须是从1开始的连续整数。`Tag.N.Key`对应的值为`Tag.N.Value`。\n\n> 最多支持64个字符，不能以`aliyun`、`acs:`、`http://`或者`https://`开头。",
            "en": "The tag value. You can use tags to filter clusters. You can specify up to 20 tags. N in `Tag.N.Value` must be a unique and consecutive integer that starts from 1. `Tag.N.Value` is paired with `Tag.N.Key`.\n\n> The tag value can be up to 64 characters in length and cannot start with `aliyun`, `acs:`, `http://`, or `https://`."
          }
        }
      ]
    },
    {
      "name": "UsedTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预付费集群的购买时长。取值说明：\n\n- 当Period为Year时，UsedTime取值范围为：1、2、3、5（整数）。\n- 当Period为Month时，UsedTime取值范围为：1~11（整数）。\n\n> - PayType为Prepaid（即预付费（包年包月））时，本参数为必填参数。\n> - 购买的时间越长价格越优惠，购买1年的实际价格会比购买10个月或11个月更划算。",
        "en": "The subscription duration. Valid values:\n\n- If `Period` is set to `Year`, the value can be 1, 2, 3, or 5.\n\n- If `Period` is set to `Month`, the value can be an integer from 1 to 11.\n\n> * This parameter is required when `PayType` is set to `Prepaid` (subscription).\n>\n> * The longer the subscription duration, the greater the savings. For example, a one-year subscription is more cost-effective than a 10- or 11-month subscription."
      }
    },
    {
      "name": "VPCId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "专有网络ID。",
        "en": "The Virtual Private Cloud (VPC) ID."
      }
    },
    {
      "name": "VSwitchId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "虚拟交换机ID。",
        "en": "The vSwitch ID."
      }
    },
    {
      "name": "ZoneId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "可用区ID。\n>您可以调用[DescribeRegions](~~143074~~)接口查看可用区ID。",
        "en": "The availability zone ID.\n\n> You can call the [DescribeRegions](~~143074~~) operation to query the IDs of available availability zones."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建集群",
    "en": "CreateDBCluster"
  },
  "descriptions": {
    "zh": "### 注意事项\n- 集群创建成功后，将按照您选的集群规格计费。数仓版集群的计费项与定价，详情请参见[数仓版计费项](~~303131~~)和[数仓版产品定价](~~135229~~)。\n- <props=\"china\">数仓版目前已停止新购。您可以购买企业版或基础版。企业版、基础版与湖仓版的特性相同，详细介绍请参见[产品系列](~~205001~~)。</props>",
    "en": "### Considerations\n\n- After creating a cluster, you are charged based on the cluster specifications you selected. For more information on the billable items and pricing for the Data Warehouse Edition, see [Billable items for Data Warehouse Edition](~~303131~~) and [Pricing for Data Warehouse Edition](~~135229~~).\n\n<props=\"china\">\n\n- The Data Warehouse Edition is no longer available for new purchases; you can purchase Enterprise Edition or Basic Edition instead. The Enterprise, Basic, and Lakehouse editions all share the same features. For more information, see [product editions](~~205001~~).\n\n</props>"
  },
  "example": {
    "unifiedCli": "aliyun adb create-db-cluster --biz-region-id cn-hangzhou --zone-id cn-hangzhou-h --db-cluster-version 3.0 --db-cluster-category Cluster --db-cluster-class C8 --db-node-group-count 2 --db-node-storage 200 --db-cluster-network-type VPC --db-cluster-description test --pay-type Postpaid --period Year --used-time 1 --vpc-id vpc-bp1at5ze0t5u3xtqn0000 --vswitch-id vsw-bp1aadw9k19x6cis90000 --client-token 123e4567-e89b-12d3-a456-t72410000 --executor-count '无' --resource-group-id rg-4690g379290000 --tag Value=test1 Key=tag1 --biz-mode Reserver --storage-resource '无' --storage-type '无' --compute-resource 32Core128GBNEW --restore-type '无' --source-db-instance-name '无' --backup-set-id '无' --restore-time '无' --elastic-io-resource 0 --disk-encryption true --kms-id xxxxxxxx-xxxx-xxxx-xxxx-xxxx --enable-ssl true --api-version 2019-03-15 --region cn-hangzhou",
    "legacyCli": "aliyun adb CreateDBCluster --region cn-hangzhou --StorageResource '无' --ResourceGroupId 'rg-4690g379290000' --DBClusterNetworkType VPC --BackupSetID '无' --RestoreTime '无' --ElasticIOResource 0 --DiskEncryption true --DBClusterVersion '3.0' --DBNodeGroupCount 2 --ExecutorCount '无' --RestoreType '无' --DBNodeStorage 200 --Tag.1.Key tag1 --DBClusterCategory Cluster --EnableSSL true --StorageType '无' --ZoneId 'cn-hangzhou-h' --VPCId 'vpc-bp1at5ze0t5u3xtqn0000' --VSwitchId 'vsw-bp1aadw9k19x6cis90000' --Mode Reserver --DBClusterDescription test --Period Year --ComputeResource 32Core128GBNEW --PayType Postpaid --Tag.1.Value test1 --UsedTime 1 --KmsId 'xxxxxxxx-xxxx-xxxx-xxxx-xxxx' --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-t72410000' --SourceDBInstanceName '无' --DBClusterClass C8"
  }
}