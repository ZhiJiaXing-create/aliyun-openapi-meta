{
  "name": "BindDBResourceGroupWithUser",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性。由客户端生成该参数值，保证在不同请求间唯一。大小写敏感、不超过64个ASCII 字符。",
        "en": "A client-generated token that is used to ensure the idempotence of the request. Make sure that the token is unique among different requests. The token is case-sensitive and can be up to 64 ASCII characters in length."
      }
    },
    {
      "name": "DBClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数仓版集群ID。\n> 您可以调用[DescribeDBClusters](~~129857~~)接口查询目标地域中所有数仓版集群的集群ID。",
        "en": "The ID of the Data Warehouse Edition (V3.0) cluster.\n\n> Call the [DescribeDBClusters](~~129857~~) operation to query the IDs of all Data Warehouse Edition (V3.0) clusters in the destination region."
      }
    },
    {
      "name": "GroupName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源组名称。",
        "en": "The name of the resource group."
      }
    },
    {
      "name": "GroupUser",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要绑定的数据库账号。既可以绑定数据库普通账号，也可以绑定数据库高权限账号。",
        "en": "The database account to attach. You can attach a standard account or a privileged account."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "将资源组与数据库用户进行绑定",
    "en": "BindDBResourceGroupWithUser"
  },
  "descriptions": {
    "zh": "### 注意事项\n- 该接口仅适用于32核及以上规格的弹性模式集群版（新版）集群。\n- 默认资源组USER_DEFAULT不支持绑定数据库账号。",
    "en": "### Notes\n\n- This operation is available only for Data Warehouse Edition (V3.0) clusters in elastic mode that have 32 or more cores.\n\n- You cannot attach database accounts to the default resource group USER\\_DEFAULT."
  },
  "example": {
    "unifiedCli": "aliyun adb bind-db-resource-group-with-user --db-cluster-id am-bp1ub9grke10000 --group-name test --group-user accout --client-token 123e4567-e89b-12d3-a456-t72410000 --api-version 2019-03-15 --region cn-hangzhou",
    "legacyCli": "aliyun adb BindDBResourceGroupWithUser --region cn-hangzhou --GroupName test --GroupUser accout --DBClusterId 'am-bp1ub9grke10000' --ClientToken '123e4567-e89b-12d3-a456-t72410000'"
  }
}