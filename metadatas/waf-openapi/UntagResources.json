{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解绑指定资源组或资源目录成员的全部标签。取值：\n- false（默认值）：否\n- true：是",
        "en": "Specifies whether to remove all tags from the specified resource groups or members. Valid values:\n\n- false (default)\n\n- true"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "WAF实例所属地域。取值：\n\n- **cn-hangzhou**：表示中国内地。\n\n- **ap-southeast-1**：表示非中国内地。",
        "en": "The region in which the Web Application Firewall (WAF) instance is deployed. Valid values:\n\n- **cn-hangzhou**: Chinese mainland.\n\n- **ap-southeast-1**: outside the Chinese mainland."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID列表，最多50个子项",
        "en": "The resource IDs. You can specify up to 50 resource IDs."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型定义。取值：ALIYUN::WAF::DEFENSERESOURCE",
        "en": "The type of the resource. Set the value to ALIYUN::WAF::DEFENSERESOURCE."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签键列表，最多20个子项。",
        "en": "The tag keys. You can specify up to 20 tag keys."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "解绑并删除标签",
    "en": "UntagResources"
  },
  "example": {
    "unifiedCli": "aliyun waf-openapi untag-resources --biz-region-id cn-hangzhou --resource-type ALIYUN::WAF::DEFENSERESOURCE --all false --resource-id demo.xxxxaliyundoc.com-waf --tag-key demoTagKey --api-version 2021-10-01 --region cn-hangzhou",
    "legacyCli": "aliyun waf-openapi UntagResources --region cn-hangzhou --All false --ResourceId.1 'demo.xxxxaliyundoc.com-waf' --TagKey.1 demoTagKey --ResourceType 'ALIYUN::WAF::DEFENSERESOURCE' --RegionId 'cn-hangzhou'"
  }
}