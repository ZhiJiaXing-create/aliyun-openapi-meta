{
  "name": "DeleteAtiAgentRegisterInfo",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AgentRegisterInfoId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "AgentRegisterInfoId 由阿里云 ATS 平台自动生成，具有全局唯一性。需要调用查询接口获取，再基于此ID删除该Agent",
        "en": "The AgentRegisterInfoId is automatically generated by the Alibaba Cloud ATS platform and is globally unique. Call the query operation to obtain this ID, and then use it to delete the Agent."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。\n\n> 若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样。",
        "en": "Ensures the idempotency of the request. Generate a parameter value from your client to ensure uniqueness across different requests. ClientToken supports only ASCII characters and cannot exceed 64 characters in length.\n\n> If you do not specify this parameter, the system automatically uses the RequestId of the API request as the ClientToken. The RequestId may be different for each API request."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除Agent",
    "en": "Delete an agent"
  },
  "example": {
    "unifiedCli": "aliyun alidns delete-ati-agent-register-info --client-token eyJhbGciOiJIUzI1NiIsInR5cC..... --agent-register-info-id 2074031103110331392 --region cn-hangzhou",
    "legacyCli": "aliyun alidns DeleteAtiAgentRegisterInfo --region cn-hangzhou --AgentRegisterInfoId 2074031103110331392 --ClientToken 'eyJhbGciOiJIUzI1NiIsInR5cC.....'"
  }
}