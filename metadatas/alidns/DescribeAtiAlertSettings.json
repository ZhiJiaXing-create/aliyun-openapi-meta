{
  "name": "DescribeAtiAlertSettings",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。",
        "en": "The token that ensures the idempotence of the request. Generate a unique value from your client. The ClientToken value can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询ATS告警设置",
    "en": "Query ATS alert settings"
  },
  "example": {
    "unifiedCli": "aliyun alidns describe-ati-alert-settings --client-token eyJhbGciOiJIUzI1NiIsInR5cC..... --region cn-hangzhou",
    "legacyCli": "aliyun alidns DescribeAtiAlertSettings --region cn-hangzhou --ClientToken 'eyJhbGciOiJIUzI1NiIsInR5cC.....'"
  }
}