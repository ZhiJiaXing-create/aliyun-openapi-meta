{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置请求和接收消息的语言类型，默认为**zh**。取值：\n- **zh**：中文\n- **en**：英文",
        "en": "The language of the request and response. Default value: **zh**. Valid values:\n\n- **zh**: Chinese\n\n- **en**: English"
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源id。",
        "en": "The IDs of the resources."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。取值：\n- **DOMAIN**：域名",
        "en": "The resource type. Valid value:\n\n- **DOMAIN**: domain name"
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "标签键值对数组。",
        "en": "An array of tag key-value pairs."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签键。不能以`acs:`或`aliyun`开头，长度不能超过20字节。",
            "en": "The tag key. The key cannot start with `acs:` or `aliyun` and cannot be longer than 20 bytes."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签值。长度不能超过20字节。",
            "en": "The tag value. The value cannot be longer than 20 bytes."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "添加修改资源标签",
    "en": "TagResources"
  },
  "example": {
    "unifiedCli": "aliyun alidns tag-resources --lang en --resource-type DOMAIN --tag Value=abcd Key=abcd --resource-id example.com --region cn-hangzhou",
    "legacyCli": "aliyun alidns TagResources --region cn-hangzhou --ResourceId.1 'example.com' --Tag.1.Key abcd --Tag.1.Value abcd --Lang en --ResourceType DOMAIN"
  }
}