{
  "name": "RevokeAtiRegistrant",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken 只支持 ASCII 字符，且不能超过 64 个字符。\n\n若您未指定，则系统自动使用 API 请求的 RequestId 作为 ClientToken 标识。每次 API 请求的 RequestId 可能不一样。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.\n\nIf you do not specify this parameter, the system automatically uses the value of RequestId as the value of ClientToken. The value of RequestId may be different for each API request."
      }
    },
    {
      "name": "RegistrantId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实名认证注册人 ID",
        "en": "The ID of the real-name registrant."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "撤销实名认证申请",
    "en": "Revoke a real-name verification application"
  },
  "example": {
    "unifiedCli": "aliyun alidns revoke-ati-registrant --client-token eyJhbGciOiJIUzI1NiIsInR5cC..... --registrant-id 2072277378616354816 --region cn-hangzhou",
    "legacyCli": "aliyun alidns RevokeAtiRegistrant --region cn-hangzhou --RegistrantId 2072277378616354816 --ClientToken 'eyJhbGciOiJIUzI1NiIsInR5cC.....'"
  }
}