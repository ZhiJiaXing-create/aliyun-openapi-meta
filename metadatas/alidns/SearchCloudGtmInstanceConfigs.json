{
  "name": "SearchCloudGtmInstanceConfigs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回结果显示的语言。取值：\n\n- zh-CN：中文\n\n- en-US（默认值）：英文",
        "en": "The language of the response. Valid values:\n\n- `zh-CN`: Chinese\n\n- `en-US` (default): English"
      }
    },
    {
      "name": "AvailableStatus",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "域名实例服务可用状态：\n\n- available：可用，域名**启用**且健康状态为**正常**，接入域名的服务可用状态为**可用**。\n\n- unavailable：不可用，域名**禁用**或健康状态为**异常**，接入域名的服务可用状态为**不可用**。",
        "en": "The service availability status of the instance configuration. Valid values:\n\n- `available`: The service for the GTM access domain is available when the instance configuration is **enabled** and its health status is **Normal** or Alert.\n\n- `unavailable`: The service for the GTM access domain is unavailable when the instance configuration is **disabled** or its health status is **Exceptional**."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户生成的请求token。用于保证请求的幂等性。该参数值可自定义，但是要保证在不同请求间唯一，最大值不超过64个ASCII字符。",
        "en": "A client-generated token to ensure the idempotence of the request. The token must be unique across requests and can contain up to 64 ASCII characters."
      }
    },
    {
      "name": "EnableStatus",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "域名实例的启用状态：\n\n- enable：启用状态，GTM实例智能调度策略生效。\n\n- disable：禁用状态，GTM实例智能调度策略不可用。",
        "en": "The status of the instance configuration. Valid values:\n\n- `enable`: Enabled. The intelligent scheduling policy of the GTM instance is in effect.\n\n- `disable`: Disabled. The intelligent scheduling policy of the GTM instance is unavailable."
      }
    },
    {
      "name": "HealthStatus",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "域名实例健康状态：\n\n- ok：正常，接入域名引用的所有地址池全部可用。\n\n- ok_alert：警告，接入域名引用的所有地址池部分不可用。警告状态下，可用的地址池正常解析，不可用的地址池停止解析。\n\n- exceptional：异常，接入域名引用的所有地址池全部不可用。此时，使用实例所引用的顺序号较小的非空地址池中的地址进行兜底解析，尽可能帮助客户端获得解析结果。",
        "en": "The health status of the instance configuration. Valid values:\n\n- `ok`: Normal. All address pools referenced by the GTM access domain are available.\n\n- `ok_alert`: Alert. Some address pools referenced by the GTM access domain are unavailable. In this state, DNS resolution continues for available address pools but stops for unavailable ones.\n\n- `exceptional`: Exceptional. All address pools referenced by the GTM access domain are unavailable. In this case, failover resolution uses the addresses from the non-empty address pool with the smallest sequence number to ensure clients receive a resolution result."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "全局流量管理GTM 3.0实例ID。",
        "en": "The ID of the Global Traffic Manager (GTM) 3.0 instance."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "当前页数，起始值为1，默认为1。",
        "en": "The page number. Pages start from 1. The default value is 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "分页查询时设置的每页行数，最大值**100**，默认为**20**。",
        "en": "The number of entries per page for a paged query. The maximum value is **100** and the default value is **20**."
      }
    },
    {
      "name": "Remark",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "域名实例的备注。",
        "en": "A note for the instance configuration."
      }
    },
    {
      "name": "ScheduleDomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "GTM接入域名。格式为接入域名主机记录（ScheduleHostname参数值）+主域或子域（ScheduleZoneName参数值）",
        "en": "The GTM access domain. It is formed by combining the host record (`ScheduleHostname`) with the primary or subdomain name (`ScheduleZoneName`)."
      }
    },
    {
      "name": "ScheduleZoneName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "GTM接入域名的主域（example.com）或子域（a.example.com）。一般为GTM实例所在账号下，在云解析DNS控制台权威解析中托管的域名。",
        "en": "The primary domain name (for example, `example.com`) or subdomain name (for example, `a.example.com`) of the GTM access domain. This is typically a domain name managed by Alibaba Cloud DNS under the same account that owns the GTM instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "搜索实例配置列表",
    "en": "SearchCloudGtmInstanceConfigs"
  },
  "example": {
    "unifiedCli": "aliyun alidns search-cloud-gtm-instance-configs --accept-language zh-CN --client-token 1ae05db4-10e7-11ef-b126-00163e240000 --page-number 1 --page-size 20 --instance-id gtm-cn-wwo3a3h0000 --schedule-zone-name example.com --schedule-domain-name www.example.com --enable-status enable --remark test --health-status ok --available-status available --region cn-hangzhou",
    "legacyCli": "aliyun alidns SearchCloudGtmInstanceConfigs --region cn-hangzhou --ScheduleZoneName 'example.com' --PageSize 20 --PageNumber 1 --InstanceId 'gtm-cn-wwo3a3h0000' --EnableStatus enable --AvailableStatus available --HealthStatus ok --AcceptLanguage 'zh-CN' --ClientToken '1ae05db4-10e7-11ef-b126-00163e240000' --ScheduleDomainName 'www.example.com' --Remark test"
  }
}