{
  "name": "ListRecursionZones",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "本次请求所返回的最大记录条数。",
        "en": "The maximum number of entries to return for this request."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "下一个查询开始Token。",
        "en": "The token to start the next query."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "当前页数，起始值为**1**，默认为**1**。",
        "en": "The current page number. The value starts from **1**. The default value is **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "分页查询时设置的每页行数，最大值100，默认为20。",
        "en": "The number of entries to return on each page for a paged query. The maximum value is 100. The default value is 20."
      }
    },
    {
      "name": "Remark",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备注信息。",
        "en": "The remarks."
      }
    },
    {
      "name": "ZoneName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "域名Zone名称。",
        "en": "The name of the zone."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询HTTPDNS内置权威域名zone",
    "en": "ListRecursionZones"
  },
  "descriptions": {
    "zh": "- 请求中至少指定参数ResourceId.N或Tag.N（Tag.N.Key与Tag.N.Value），以确定检索对象。\n- Tag.N是资源的标签，由一个键值对组成。仅指定Tag.N.Key时，则返回该标签键关联的所有标签值。仅指定Tag.N.Value会报错。\n- 如果您同时指定Tag.N和ResourceId.N筛选标签，则ResourceId.N必须满足所有输入的标签键值对。\n- 如果您同时指定多个标签键值对，返回结果为同时包含被指定的多个键值对的资源。",
    "en": "- Specify at least ResourceId.N or Tag.N (Tag.N.Key and Tag.N.Value) in your request to identify the resources to retrieve.\n\n- Tag.N is a resource tag that consists of a key-value pair. If you specify only Tag.N.Key, all tag values associated with the tag key are returned. An error is returned if you specify only Tag.N.Value.\n\n- If you specify both Tag.N and ResourceId.N to filter resources, only the resources that are specified by ResourceId.N and match all the specified key-value pairs are returned.\n\n- If you specify multiple tag key-value pairs, the resources that match all of them are returned."
  },
  "example": {
    "unifiedCli": "aliyun alidns list-recursion-zones --zone-name lisheng999.com --remark test --page-number 1 --page-size 10 --max-results 100 --next-token 4698691 --region cn-hangzhou",
    "legacyCli": "aliyun alidns ListRecursionZones --region cn-hangzhou --ZoneName 'lisheng999.com' --PageSize 10 --NextToken 4698691 --PageNumber 1 --MaxResults 100 --Remark test"
  }
}