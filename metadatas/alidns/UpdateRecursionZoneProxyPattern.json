{
  "name": "UpdateRecursionZoneProxyPattern",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。",
        "en": "A client token that ensures the request is idempotent. This token is generated by the client and must be unique for each request. The token can be up to 64 ASCII characters long."
      }
    },
    {
      "name": "ProxyPattern",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启子域名递归代理。取值：\n\nzone: 关闭（当解析本域名下不存在的子域名时，直接返回NXDOMAIN，表示子域名不存在）\nrecord: 开启（当解析本域名下不存在的子域名时，会依次查询“转发”和“递归”模块，并以最终查询到的解析结果来应答解析请求）",
        "en": "Specifies whether to enable recursive proxy for subdomains. Valid values:\n\nzone: Disables recursive proxy. If a request is made for a subdomain that does not exist, the system returns an NXDOMAIN response.\nrecord: Enables recursive proxy. If a request is made for a subdomain that does not exist, the system queries the forwarding and recursion modules in order. The final result is used to respond to the request."
      }
    },
    {
      "name": "ZoneId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Zone ID，Zone的唯一识别码。",
        "en": "The unique ID of the zone."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改HTTPDNS内置权威域名zone递归代理模式",
    "en": "UpdateRecursionZoneProxyPattern"
  },
  "descriptions": {
    "zh": "每个IP段的IP地址必须满足： EndIp >=StartIp。<br>\n域名下所有自定义线路中的所有IP段，IP范围都不能交叉重叠。",
    "en": "The end IP address of each IP segment must be greater than or equal to the start IP address.<br>The IP address ranges of all IP segments for the domain name cannot overlap across custom lines.<br>"
  },
  "example": {
    "unifiedCli": "aliyun alidns update-recursion-zone-proxy-pattern --client-token 21079fa016944979537637959d09bc --zone-id 173671468000011 --proxy-pattern record --region cn-hangzhou",
    "legacyCli": "aliyun alidns UpdateRecursionZoneProxyPattern --region cn-hangzhou --ZoneId 173671468000011 --ProxyPattern record --ClientToken 21079fa016944979537637959d09bc"
  }
}