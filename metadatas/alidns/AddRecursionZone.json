{
  "name": "AddRecursionZone",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户生成的请求token。用于保证请求的幂等性。该参数值可自定义，但是要保证在不同请求间唯一，最大值不超过64个ASCII字符。",
        "en": "A client token that ensures the idempotence of the request. You can specify a custom value. Make sure that the value is unique among different requests. The value can contain up to 64 ASCII characters."
      }
    },
    {
      "name": "ProxyPattern",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启子域名递归代理。取值：\n\nzone: 关闭（当解析本域名下不存在的子域名时，直接返回NXDOMAIN，表示子域名不存在）\nrecord: 开启（当解析本域名下不存在的子域名时，会依次查询“转发”和“递归”模块，并以最终查询到的解析结果来应答解析请求）",
        "en": "Specifies whether to enable recursive proxy for subdomains. Valid values:\n\nzone: Disables recursive proxy. An NXDOMAIN response is returned for non-existent subdomains. record: Enables recursive proxy. For non-existent subdomains, the system queries the forwarding and recursion modules in sequence and returns the final result."
      }
    },
    {
      "name": "ZoneName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "域名（Zone）名称。",
        "en": "The name of the zone."
      }
    }
  ],
  "security": [
    "AK",
    "BearerToken"
  ],
  "title": {
    "zh": "新增HTTPDNS内置权威域名zone",
    "en": "AddRecursionZone"
  },
  "descriptions": {
    "zh": "每个IP段的IP地址必须满足： EndIp >=StartIp。<br>\n域名下所有自定义线路中的所有IP段，IP范围都不能交叉重叠。",
    "en": "The end IP address of each IP range must be greater than or equal to the start IP address.<br>The IP address ranges of all IP ranges in all custom lines for a domain name cannot overlap.<br>"
  },
  "example": {
    "unifiedCli": "aliyun alidns add-recursion-zone --zone-name example.com --proxy-pattern record --client-token 1ae05db4-10e7-11ef-b126-00163e240022 --region cn-hangzhou",
    "legacyCli": "aliyun alidns AddRecursionZone --region cn-hangzhou --ZoneName 'example.com' --ProxyPattern record --ClientToken '1ae05db4-10e7-11ef-b126-00163e240022'"
  }
}