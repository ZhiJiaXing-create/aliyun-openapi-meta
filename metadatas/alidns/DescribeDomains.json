{
  "name": "DescribeDomains",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指域名分组ID。\n\n如不填或传空字符，将在全部域名下查询。\n\n如传defaultGroup，将在默认分组下查询。",
        "en": "The ID of the domain name group.\n\nIf you leave this parameter empty or pass an empty string, all domain names are queried.\n\nIf you set this parameter to defaultGroup, domain names in the default group are queried."
      }
    },
    {
      "name": "KeyWord",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "关键字，按照“%KeyWord%”模式搜索，不区分大小写。",
        "en": "The keyword. The search is performed in the %KeyWord% pattern and is not case-sensitive."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回值语言，取值：\n- zh：中文\n- en：英文\n\n默认：zh",
        "en": "The language of the response. Valid values:\n\n- zh: Chinese\n\n- en: English\n\nDefault value: zh."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "当前页数，起始值为**1**，默认为**1**。",
        "en": "The page number. The value starts from **1**. The default value is **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数，最大值**100**，默认为**20**。",
        "en": "The number of entries per page. The maximum value is **100**. The default value is **20**."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group."
      }
    },
    {
      "name": "SearchMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "搜索模式。取值：\n\n- **LIKE**：模糊搜索\n- **EXACT**：精确搜索\n\n默认：LIKE",
        "en": "The search mode. Valid values:\n\n- **LIKE**: fuzzy search\n\n- **EXACT**: exact search\n\nDefault value: LIKE"
      }
    },
    {
      "name": "Starmark",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否查询域名星标。取值：\n\n- **true**：是\n\n- **false**：**否**\n\n默认：true",
        "en": "Specifies whether to query starred domain names. Valid values:\n\n- **true**\n\n- **false**\n\nDefault value: true"
      }
    }
  ],
  "security": [
    "AK",
    "BearerToken"
  ],
  "title": {
    "zh": "获取域名列表",
    "en": "DescribeDomains"
  },
  "descriptions": {
    "zh": "1. 可以指定页码（PageNumber）和每页的数量（PageSize）来获取域名列表。\n2. 可以指定域名的关键字（KeyWord）来查询含有该关键字的域名列表。\n3. 域名列表的默认排序方式是按照域名添加的时间从新到旧排序的。\n4. 可以指定域名分组的ID（GroupId），全部域名代表所有域名、默认分组代表未设置分组的域名。",
    "en": "1. You can specify a page number (PageNumber) and page size (PageSize) to retrieve a paginated list of domain names.\n\n2. You can specify a keyword (KeyWord) to query for domain names that contain the specified keyword.\n\n3. By default, domain names are sorted in descending order of their creation time.\n\n4. You can specify a domain name group ID (GroupId) to query for domain names in a specific group. This lets you retrieve all domain names or only the domain names that are not assigned to a group."
  },
  "example": {
    "unifiedCli": "aliyun alidns describe-domains --lang en --key-word com --group-id defaultGroup --page-number 1 --page-size 20 --search-mode LIKE --resource-group-id rg-re00000000 --starmark true --region cn-hangzhou",
    "legacyCli": "aliyun alidns DescribeDomains --region cn-hangzhou --PageSize 20 --ResourceGroupId 'rg-re00000000' --PageNumber 1 --KeyWord com --Lang en --Starmark true --SearchMode LIKE --GroupId defaultGroup"
  }
}