{
  "name": "DescribeAtiRegistrant",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。\n\n> 若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样。",
        "en": "Ensures the idempotence of the request. Generate a parameter value from your client to ensure uniqueness across different requests. ClientToken supports only ASCII characters and cannot exceed 64 characters in length.\n\n> If you do not specify this parameter, the system automatically uses the RequestId of the API request as the ClientToken. The RequestId may be different for each API request."
      }
    },
    {
      "name": "RegistrantId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实名认证注册人 ID",
        "en": "The ID of the real-name verified registrant."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "实名认证 - 查看注册人详情",
    "en": "Real-name verification - view registrant details"
  },
  "example": {
    "unifiedCli": "aliyun alidns describe-ati-registrant --client-token eyJhbGciOiJIUzI1NiIsInR5cC..... --registrant-id 2072277378616354816 --region cn-hangzhou",
    "legacyCli": "aliyun alidns DescribeAtiRegistrant --region cn-hangzhou --RegistrantId 2072277378616354816 --ClientToken 'eyJhbGciOiJIUzI1NiIsInR5cC.....'"
  }
}