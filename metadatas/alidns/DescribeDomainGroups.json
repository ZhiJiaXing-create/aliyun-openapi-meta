{
  "name": "DescribeDomainGroups",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "KeyWord",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "组名关键字，按照“%KeyWord%”模式搜索，不区分大小写。",
        "en": "The keyword for the group name. The search uses the %KeyWord% pattern and is case-insensitive."
      }
    },
    {
      "name": "Lang",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回值语言，取值：\n- zh：中文\n- en：英文\n\n默认：zh",
        "en": "The language of the response. Valid values:\n\n- zh: Chinese\n\n- en: English\n\nDefault value: zh"
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "当前页数，起始值为**1**，默认为**1**。",
        "en": "The page number. The start value is **1**. The default value is **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数，最大值**100**，默认为**20**。",
        "en": "The number of entries to return on each page. The maximum value is **100**. The default value is **20**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取域名分组列表",
    "en": "DescribeDomainGroups"
  },
  "example": {
    "unifiedCli": "aliyun alidns describe-domain-groups --lang en --key-word Group --page-number 1 --page-size 20 --region cn-hangzhou",
    "legacyCli": "aliyun alidns DescribeDomainGroups --region cn-hangzhou --PageSize 20 --PageNumber 1 --KeyWord Group --Lang en"
  }
}