{
  "name": "DescribeDeductionStatistic",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndTime",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "结束时间（时间戳）",
        "en": "The end time, specified as a Unix timestamp."
      }
    },
    {
      "name": "InstanceIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "实例 ID 列表（JSON 数组）",
        "en": "The list of instance IDs, specified as a JSON array."
      }
    },
    {
      "name": "Periods",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "时间周期列表，JSON 数组格式，元素为 `PeriodParam` 对象",
        "en": "The list of periods, formatted as a JSON array of `PeriodParam` objects."
      },
      "sub_parameters": [
        {
          "name": "BaseTime",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "基准时间",
            "en": "The base time."
          }
        },
        {
          "name": "PeriodUnit",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "时间周期单位（参考 `PeriodUnit` 枚举）",
            "en": "The period unit. For valid values, see the `PeriodUnit` enumeration."
          }
        }
      ]
    },
    {
      "name": "ResourceTypes",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源类型列表，JSON 数组格式",
        "en": "The list of resource types, specified as a JSON array."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "开始时间（时间戳）",
        "en": "The start time, specified as a Unix timestamp."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询抵扣详情",
    "en": "DescribeDeductionStatistic"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n- 该接口支持 GET 和 POST 方法。\n- `periods` 参数为 JSON 数组格式，每个元素为一个 `PeriodParam` 对象，包含 `periodUnit` 和 `baseTime` 字段。\n- `resourceTypes` 参数为 JSON 数组格式，包含多个资源类型字符串。\n- `startTime` 和 `endTime` 参数为时间戳格式，表示查询的时间范围。\n- 分页参数包括 `nextToken`、`maxResults`、`pageNo` 和 `pageSize`，用于控制返回结果的数量和分页。\n- 当 `resourceTypes` 参数包含无效值或 `periods` 参数 JSON 解析失败时，会抛出 `SalesClientException` 异常。",
    "en": "## Request\n\n- This API supports GET and POST methods.\n\n- The `periods` parameter is a JSON array of `PeriodParam` objects, each containing the `periodUnit` and `baseTime` fields.\n\n- The `resourceTypes` parameter is a JSON array of resource type strings.\n\n- The `startTime` and `endTime` parameters are timestamps that define the query's time range.\n\n- The `nextToken`, `maxResults`, `pageNo`, and `pageSize` parameters control pagination and the number of results to return.\n\n- The API throws a `SalesClientException` if the `resourceTypes` parameter contains an invalid value or if the `periods` parameter fails JSON parsing."
  }
}