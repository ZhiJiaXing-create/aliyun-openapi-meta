{
  "name": "DescribeCreditPackageAgents",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AgentIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "AgentId 列表的 JSON 字符串，例：`[\"agent-1\",\"agent-2\"]`",
        "en": "An array of agent IDs to query. Example: `[\"agent-1\",\"agent-2\"]`."
      }
    },
    {
      "name": "AgentType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Agent 类型：`CREDIT_PACKAGE` / `JVS_CLAW` / `OPEN_CLAW` / `JVS_COPILOT`",
        "en": "The agent type. Valid values: `CREDIT_PACKAGE`, `JVS_CLAW`, `OPEN_CLAW`, and `JVS_COPILOT`."
      }
    },
    {
      "name": "BizType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "业务类型",
        "en": "The business type."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页数量",
        "en": "The maximum number of results to return per page."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页 Token；首次请求传空，后续取上一次响应的 `nextToken`",
        "en": "The token to retrieve the next page of results. Obtain this value from the `NextToken` parameter of the previous response. For the first request, set this parameter to an empty string."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询积分包Agent列表",
    "en": "DescribeCreditPackageAgents"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n- **分页支持**：通过 `NextToken` 和 `MaxResults` 参数实现分页，首次请求时 `NextToken` 应为空字符串。\n- **过滤条件**：可以使用 `AgentType` 和 `AgentIds` 参数来过滤返回的 Agent 列表。\n- **状态过滤**：通过 `Status` 参数（0-已删除，1-生效中）来过滤 Agent 的状态。\n- **排序规则**：默认按 `id` 升序排列。\n- **匿名版额外参数**：`FillInstance` 参数用于自动填充当前登录用户的已绑定 JVS_COPILOT AgentId。",
    "en": "## Usage notes\n\n- **Pagination**: This operation supports pagination by using the `NextToken` and `MaxResults` parameters. For the first request, set `NextToken` to an empty string.\n\n- **Filtering**: Use the `AgentType` and `AgentIds` parameters to filter the results.\n\n- **Status filtering**: Use the `Status` parameter to filter agents by status. Valid values are 0 (deleted) and 1 (active).\n\n- **Sorting**: By default, the results are sorted by `id` in ascending order.\n\n- **Additional parameter for anonymous edition**: The `FillInstance` parameter automatically populates the ID of the JVS\\_COPILOT agent that is associated with the current user."
  }
}