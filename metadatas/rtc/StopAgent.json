{
  "name": "StopAgent",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID，仅支持传单个ID。您可以在控制台创建和查询。",
        "en": "The application ID. Only a single ID is supported. You can create and query applications in the console."
      }
    },
    {
      "name": "ChannelId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加入的频道，仅支持传单个ID。",
        "en": "The channel to join. Only a single ID is supported."
      }
    },
    {
      "name": "TaskId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "任务ID，仅支持传单个ID。",
        "en": "The task ID. Only a single ID is supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "关闭智能体",
    "en": "Shutdown Agent"
  }
}