{
  "name": "DescribeUsageDistributionStatData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "APP ID。",
        "en": "APP ID"
      }
    },
    {
      "name": "EndDate",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "查询的结束时间，使用UNIX时间戳表示，单位：秒。",
        "en": "The end time of the query, expressed as a UNIX timestamp in seconds."
      }
    },
    {
      "name": "StartDate",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "查询的开始时间，使用UNIX时间戳表示，单位：秒。",
        "en": "The start time of the query, expressed as a UNIX timestamp in seconds."
      }
    },
    {
      "name": "StatDim",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "统计维度。取值：\n- **CHANNEL_ONLINE**：按照频道在线人数统计。\n- **NETWORK**：按照网络类型统计。\n- **OS**：按照系统类型统计。",
        "en": "The statistics dimension. Valid values:  \n- **CHANNEL_ONLINE**: Statistics by channel online user count.  \n- **NETWORK**: Statistics by network type.  \n- **OS**: Statistics by operating system type."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取用量统计的分布数据",
    "en": "Obtain distribution data of usage statistics"
  },
  "descriptions": {
    "zh": "### 使用说明\n本接口用于获取用量统计的分布数据。使用该接口时，限制如下：\n- 支持查询最近365天（不包含查询当天）任意范围的数据。\n- 如果查询范围小于24小时，则按小时统计查询的数据；如果查询范围大于或等于24小时，则按天统计查询的数据。\n### QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "### How-To  \nThis API is used to retrieve the distribution data of usage statistics. When using this API, the following restrictions apply:  \n- You can query data for any range within the last 365 days (excluding the current day).  \n- If the query range is less than 24 hours, the data is aggregated by hour. If the query range is greater than or equal to 24 hours, the data is aggregated by day.  \n\n### Rate Limiting  \nThe queries per second (QPS) limit for this API is 10 per user. If you exceed this limit, your API calls will be rate-limited, which may impact your business. Please invoke the API appropriately."
  }
}