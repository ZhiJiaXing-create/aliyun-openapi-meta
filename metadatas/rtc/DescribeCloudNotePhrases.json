{
  "name": "DescribeCloudNotePhrases",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID。通过控制台创建和查询，仅支持传单个ID。",
        "en": "The application ID. Obtain the ID from the console. Only a single ID can be specified."
      }
    },
    {
      "name": "Condition",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询条件。",
        "en": "The query condition."
      }
    },
    {
      "name": "PageNum",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "第几页，默认查询第1页。",
        "en": "The page number. The default value is 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页显示个数，默认为10。",
        "en": "The number of entries to return on each page. The default value is 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "热词表列表",
    "en": "Hot word List"
  }
}