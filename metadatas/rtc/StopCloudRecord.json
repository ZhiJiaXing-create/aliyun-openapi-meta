{
  "name": "StopCloudRecord",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID，通过控制台创建和查询，仅支持传单个ID。",
        "en": "The application ID, created and queried via the console. Only a single ID is supported."
      }
    },
    {
      "name": "ChannelId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "已存在的频道ID，仅支持传单个ID。",
        "en": "The existing channel ID. Only a single ID is supported."
      }
    },
    {
      "name": "TaskId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "已存在的云端录制的任务ID，仅支持传单个ID。",
        "en": "The existing cloud recording job ID. Only a single ID is supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "停止云端录制任务",
    "en": "Stop Cloud Recording Job"
  },
  "descriptions": {
    "zh": "### 使用说明\n本接口用于停止在线频道云端录制任务。\n### QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "### How-To  \nThis API is used to stop an online channel cloud recording job.  \n### queries per second (QPS) Limit  \nThe QPS limit for this API is 10 queries per second (QPS) per user. If the limit is exceeded, API calls will be subject to rate limiting, which may impact your business. Please invoke the API appropriately."
  }
}