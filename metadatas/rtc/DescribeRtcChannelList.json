{
  "name": "DescribeRtcChannelList",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用ID，可通过控制台创建和查询，仅支持传单个ID。",
        "en": "Application ID. You can create and view this ID in the console. Only one ID is supported."
      }
    },
    {
      "name": "ChannelId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "频道ID，仅支持传单个ID。",
        "en": "Channel ID. Only one ID is supported."
      }
    },
    {
      "name": "PageNo",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "第几页，取值：大于**0**。\n\n默认为1。",
        "en": "Page number. Value must be greater than **0**.\n\nDefault is 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "显示数量，取值：大于**0**。\n\n默认为10。",
        "en": "Number of items to return. Value must be greater than **0**.\n\nDefault is 10."
      }
    },
    {
      "name": "ServiceArea",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "服务区域，默认查询所有区域。\n\n- **cn**：中国。\n\n- **us**：美国。",
        "en": "Service area. Default is all areas.\n\n- **cn**: China.\n\n- **us**: United States."
      }
    },
    {
      "name": "SortType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序顺序，默认按StartTime逆序排序。",
        "en": "Sort order. Default is descending by StartTime."
      }
    },
    {
      "name": "TimePoint",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "查询时间点，UTC格式，格式为yyyy-MM-ddTHH:mm:ssZ。\n\n时间粒度为天。",
        "en": "Query time point in UTC format, yyyy-MM-ddTHH:mm:ssZ.\n\nTime granularity is day."
      }
    },
    {
      "name": "UserId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户ID，仅支持传单个ID。",
        "en": "User ID. Only one ID is supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取频道通信记录列表",
    "en": "Obtain the list of channel communication records"
  }
}