{
  "name": "RemoveUsers",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID，通过控制台创建和查询，仅支持传单个ID。",
        "en": "Application ID. You can create and query it in the console. Only a single ID is supported."
      }
    },
    {
      "name": "ChannelId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "已加入的频道ID，仅支持传单个ID。",
        "en": "The ID of the joined channel. Only a single ID is supported."
      }
    },
    {
      "name": "Users",
      "position": "Query",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "用户列表，N的取值：1～30。",
        "en": "User List. Valid values of N: 1 to 30."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "踢出用户",
    "en": "Kick Out User"
  },
  "descriptions": {
    "zh": "### 使用说明\n本接口用于踢出在线频道内的用户。\n### QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "### How-To  \nThis API is used to kick users out of an online channel.  \n### queries per second (QPS) Limit  \nThe QPS limit for this API is 10 requests per second per user. If the limit is exceeded, API calls will be subject to Rate Limiting, which may Impact your business. Please invoke the API appropriately."
  }
}