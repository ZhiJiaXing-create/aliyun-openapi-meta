{
  "name": "StartAgent",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID，可通过控制台创建和查询，仅支持传单个ID。",
        "en": "The application ID. You can create and query this ID in the console. Only one application ID is supported."
      }
    },
    {
      "name": "ChannelId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "加入的频道，仅支持传单个ID。",
        "en": "The ID of the channel to join. Only one channel ID is supported."
      }
    },
    {
      "name": "RtcConfig",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "智能体加入rtc频道的配置",
        "en": "The configuration for the agent to join an RTC channel."
      }
    },
    {
      "name": "TaskId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "任务id",
        "en": "The task ID."
      }
    },
    {
      "name": "TemplateId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "模版ID。",
        "en": "The template ID."
      }
    },
    {
      "name": "VoiceChatConfig",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "AI语音对话的配置",
        "en": "The configuration for AI voice chat."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "启动智能体",
    "en": "Start Agent"
  }
}