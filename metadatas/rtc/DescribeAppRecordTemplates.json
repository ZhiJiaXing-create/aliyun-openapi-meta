{
  "name": "DescribeAppRecordTemplates",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID。通过控制台创建和查询，仅支持传单个ID。",
        "en": "Application ID. Find or create this ID in the console. You can specify only one ID."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "幂等参数",
        "en": "Idempotency token"
      }
    },
    {
      "name": "Condition",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询条件。",
        "en": "Query conditions."
      }
    },
    {
      "name": "PageNum",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "第几页，默认查询第1页。",
        "en": "Page number. Default is 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页显示个数，默认为10。",
        "en": "Number of items per page. Default is 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询录制模板列表",
    "en": "Query recording template list"
  },
  "descriptions": {
    "zh": "### QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "### QPS limit\n\nThe per-user query per second (QPS) limit for this operation is 10 calls per second. Exceeding this limit triggers rate limiting, which may affect your business. Call this operation only as needed."
  }
}