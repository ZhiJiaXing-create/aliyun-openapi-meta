{
  "name": "DescribeRtcChannelMetric",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID，可通过控制台创建和查询，仅支持传单个ID。",
        "en": "The application ID. You can create and query the application ID in the console. Only a single ID is supported."
      }
    },
    {
      "name": "ChannelId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "频道ID，仅支持传单个ID。",
        "en": "The channel ID. Only a single ID is supported."
      }
    },
    {
      "name": "TimePoint",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "查询记录时间，UTC格式，格式为yyyy-MM-ddTHH:mm:ssZ。\n\n时间粒度为天。",
        "en": "The time when the record is queried, in UTC. Specify the time in the yyyy-MM-ddTHH:mm:ssZ format.\n\nThe time granularity is day."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取频道通信记录详情",
    "en": "Queries the details of communication records in a channel"
  }
}