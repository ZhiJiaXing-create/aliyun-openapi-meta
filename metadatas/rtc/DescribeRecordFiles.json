{
  "name": "DescribeRecordFiles",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID。仅支持传单个ID，您可以在控制台创建和查询。",
        "en": "The application ID. Only a single ID can be passed. You can create and query it in the console."
      }
    },
    {
      "name": "ChannelId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "频道ID。仅支持传单个ID。",
        "en": "The channel ID. Only a single ID can be passed."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "结束时间。格式为：<i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z（UTC时间）。",
        "en": "The end time. Format: <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z (UTC time)."
      }
    },
    {
      "name": "PageNum",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "第几页，默认查询第1页。",
        "en": "The page number. By default, the first page is queried."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页显示个数，默认为**10**，每页最多显示**100**个数据。",
        "en": "The number of entries to return on each page. Default value: **10**. Maximum value: **100**."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "开始时间。格式为：<i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z（UTC时间）。",
        "en": "The start time. Format: <i>yyyy-MM-dd</i>T<i>HH:mm:ss</i>Z (UTC time)."
      }
    },
    {
      "name": "TaskIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "录制任务ID。",
        "en": "Recording task ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询录制生成的文件列表",
    "en": "Query the list of files generated by recording"
  },
  "descriptions": {
    "zh": "# 使用说明\n本接口用于查询录制生成的文件列表。例如：您可以查询录制生成文件的创建时间、录制时长等参数。\n# QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。更多信息，请参见[QPS限制](~~341645~~)。",
    "en": "# Usage notes\nThis operation is used to query the list of files generated by recording. For example, you can query parameters such as the creation time and recording duration of files generated by recording.\n# QPS limit\nThe per-user QPS limit of this operation is 10 calls per second. If the limit is exceeded, API calls will be throttled, which may affect your business. Please call this operation reasonably. For more information, see [QPS limit](~~341645~~)."
  }
}