{
  "name": "DescribeAppLiveStreamStatus",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID。通过控制台创建和查询，仅支持传单个ID。",
        "en": "The ID of the application. You can create and query the application ID in the console. You can specify only one application ID."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "幂等参数",
        "en": "A parameter that ensures the idempotence of the request."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询旁路推流功能状态",
    "en": "Query the status of the bypass stream ingest feature"
  },
  "descriptions": {
    "zh": "### QPS限制\n本接口的单用户QPS限制为10次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。",
    "en": "### QPS limit\n\nThe queries per second (QPS) limit for this API is 10 for each user. API calls that exceed this limit are subject to rate limiting, which may impact your business. We recommend that you call this API at a reasonable rate."
  }
}