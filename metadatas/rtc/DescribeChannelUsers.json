{
  "name": "DescribeChannelUsers",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID，通过控制台创建和查询，仅支持传单个ID。",
        "en": "The application ID. You can create and query the ID in the console. Only a single ID is supported."
      }
    },
    {
      "name": "ChannelId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要查询的频道ID，仅支持传单个ID。",
        "en": "The ID of the channel to query. Only a single ID is supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询实时在线用户列表",
    "en": "Query real-time online users"
  },
  "descriptions": {
    "zh": "# 使用说明\n本接口用于查询频道内在线用户列表的详细信息。例如：您可以查询通信模式和直播模式下的用户总数。\n# QPS限制\n本接口的单用户QPS限制为100次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。更多信息，请参见[QPS限制](~~341645~~)。",
    "en": "# 使用说明\n本接口用于查询频道内在线用户列表的详细信息。例如：您可以查询通信模式和直播模式下的用户总数。\n# QPS限制\n本接口的单用户QPS限制为100次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。更多信息，请参见[QPS限制](~~341645~~)。"
  }
}