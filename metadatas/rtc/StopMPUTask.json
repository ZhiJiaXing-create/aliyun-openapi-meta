{
  "name": "StopMPUTask",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID，仅支持传单个ID。\n\n> 您可以在[控制台](https://rtc.console.aliyun.com/manage/list#/manage/list)创建和查询。",
        "en": "The application ID. Only a single ID can be passed.\n\n> You can create and query the application ID in the [console](https://rtc.console.aliyun.com/manage/list#/manage/list)."
      }
    },
    {
      "name": "TaskId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "任务ID，仅支持传单个ID，和[StartMpuTask](~~93183~~)入参TaskId一致。",
        "en": "The task ID. Only a single ID can be passed. The value must be the same as the TaskId parameter of [StartMpuTask](~~93183~~)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "停止任务",
    "en": "Stop a task"
  },
  "descriptions": {
    "zh": "# 使用说明\n\n- 调用本接口前，您必须已经调用[StartMpuTask](~~93183~~)开始旁路转推任务。\n- 如果需要停止旁路转推任务时，此时旁路转推任务异常（未调用StopMPUTask停止任务），本次旁路转推任务会在最后一个人离开频道2分钟后自动停止任务。停止后如果需要恢复旁路转推，您需要重新调用[StartMpuTask](~~93183~~)接口。\n\n# QPS限制\n本接口的单用户QPS限制为500次/秒。超过限制，API调用会被限流，这可能会影响您的业务，请合理调用。更多信息，请参见[QPS限制](~~341645~~)。",
    "en": "# Usage notes\n\n- Before you call this operation, you must have called [StartMpuTask](~~93183~~) to start a stream relay task.\n- If you need to stop a stream relay task and the stream relay task is abnormal (the StopMPUTask operation is not called to stop the task), the stream relay task is automatically stopped 2 minutes after the last user leaves the channel. After the task is stopped, if you need to resume stream relay, you need to call the [StartMpuTask](~~93183~~) operation again.\n\n# QPS limit\nThe QPS limit per user for this operation is 500 times per second. If the limit is exceeded, API calls are throttled, which may affect your business. Call the operation properly. For more information, see [QPS limits](~~341645~~)."
  }
}