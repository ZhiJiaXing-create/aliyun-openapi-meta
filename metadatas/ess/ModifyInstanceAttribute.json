{
  "name": "ModifyInstanceAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Entrusted",
      "position": "Query",
      "type": "Boolean",
      "required": true,
      "description": {
        "zh": "对于手动添加到伸缩组的ECS实例，是否将该实例的生命周期托管给伸缩组。取值范围：\n\n- true：托管。该实例的生命周期由弹性伸缩管理，与伸缩组自动创建的实例一致。实例被移出伸缩组时（不包括通过调用DetachInstances参数移出的实例）会自动释放。\n- false：不托管。该实例在被移出伸缩组时不会被释放。\n\n> 该参数仅支持手动添加到伸缩组的实例，而不支持在伸缩组内创建的实例。",
        "en": "Specifies whether to entrust the lifecycle of a manually added ECS instance to the scaling group. Valid values:\n\n- true: Entrusted. The lifecycle of the instance is managed by Auto Scaling, the same as instances of automatic creation in the scaling group. The instance is subject to automatic release when removed from the scaling group (excluding instances removed by invoking the DetachInstances operation).\n- false: Not entrusted. The instance is not released when removed from the scaling group.\n\n> This parameter is supported only for instances manually added to the scaling group, not for instances created within the scaling group."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "伸缩组的ID。",
        "en": "The ID of the scaling group."
      }
    },
    {
      "name": "InstanceIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "伸缩组所属的地域ID。",
        "en": "The region ID of the scaling group."
      }
    },
    {
      "name": "ScalingGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "伸缩组内ECS实例的ID。",
        "en": "The ID of the ECS instance in the scaling group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改伸缩组中一台ECS实例的部分信息",
    "en": "Modify partial information of an ECS instance in a scaling group"
  },
  "example": {
    "unifiedCli": "aliyun ess modify-instance-attribute --scaling-group-id asg-bp18p2yfxow2dloq0000 --instance-id i-bp109k5j3dum1ce60000 --entrusted true --biz-region-id cn-hangzhou --instance-ids i-bp109k5j3dum1ce60000 --api-version 2014-08-28 --region cn-hangzhou",
    "legacyCli": "aliyun ess ModifyInstanceAttribute --region cn-hangzhou --InstanceId 'i-bp109k5j3dum1ce60000' --ScalingGroupId 'asg-bp18p2yfxow2dloq0000' --Entrusted true --RegionId 'cn-hangzhou' --InstanceIds.1 'i-bp109k5j3dum1ce60000'"
  }
}