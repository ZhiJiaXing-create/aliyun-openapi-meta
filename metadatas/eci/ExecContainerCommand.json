{
  "name": "ExecContainerCommand",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Command",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要在容器内执行的命令序列。最多20个命令，单个命令最长256个字符。    \n执行多个命令时，字符串需按照JSON格式传入，例如：`[\"/bin/sh\", \"-c\", \"ls -a\"]`。",
        "en": "The commands to execute in the container. You can specify up to 20 commands. Each command can be up to 256 characters long.<br>\nTo execute multiple commands, pass the string in JSON format. For example, `[\"/bin/sh\", \"-c\", \"ls -a\"]`.<br>"
      }
    },
    {
      "name": "ContainerGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID，即容器组ID。",
        "en": "The instance ID, which is the container group ID."
      }
    },
    {
      "name": "ContainerName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "容器名称。",
        "en": "The name of the container."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例所属的地域ID。",
        "en": "The region ID of the instance."
      }
    },
    {
      "name": "Stdin",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否标准输入。默认为true。",
        "en": "Specifies whether to enable standard input. The default value is true."
      }
    },
    {
      "name": "Sync",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否立即执行命令并同步返回结果。默认为false。    \n如果设置为true，TTY只能设置为false（即Command不是`/bin/bash`命令）。",
        "en": "Specifies whether to execute the command immediately and return the result synchronously. The default value is false.<br>\nIf you set this parameter to true, you must set TTY to false. This means Command cannot be `/bin/bash`.<br>"
      }
    },
    {
      "name": "TTY",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启交互。默认为false。    \n当Command为`/bin/bash`命令时，需要设置为true。",
        "en": "Specifies whether to enable interaction. The default value is false.<br>\nIf you set Command to `/bin/bash`, set this parameter to true.<br>"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "在容器内部执行命令",
    "en": "ExecContainerCommand"
  },
  "example": {
    "unifiedCli": "aliyun eci exec-container-command --biz-region-id cn-hangzhou --container-group-id eci-2zebxkiifulhl0000 --container-name nginx --command /bin/sh_,_-c_,_ls_-a --tty false --stdin true --sync false --region cn-hangzhou",
    "legacyCli": "aliyun eci ExecContainerCommand --region cn-hangzhou --ContainerName nginx --Command '/bin/sh_,_-c_,_ls_-a' --TTY false --ContainerGroupId 'eci-2zebxkiifulhl0000' --RegionId 'cn-hangzhou' --Sync false --Stdin true"
  }
}