{
  "name": "DescribeContainerGroupMetric",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ContainerGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "ECI实例ID，即容器组ID。",
        "en": "The ID of the ECI instance. This is the container group ID."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询某个时间段内的监控数据时，设置的结束时间。默认为当前时间。\n\n格式为RFC3339时间格式。",
        "en": "The end of the time range to query. The default value is the current time.\n\nThe time is in the RFC3339 format."
      }
    },
    {
      "name": "Period",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据聚合周期，单位为秒。目前只支持15、30、60和600秒。默认为60秒。\n\n> 如果StartTime和EndTime均没有设置，则默认获取最新5分钟的监控数据，默认聚合周期为15S，传入的Period将被忽略。",
        "en": "The aggregation period. Unit: seconds. Valid values: 15, 30, 60, and 600. Default value: 60.\n\n> If you do not set StartTime and EndTime, the monitoring data of the last 5 minutes is queried. The default aggregation period is 15s, and the Period parameter is ignored."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询某个时间段内的监控数据时，设置的开始时间。取值必须为30天之内，默认为EndTime减去5分钟。\n\n格式为RFC3339时间格式。例如：北京时间2019年03月12号上午9点整可以表示为2019-03-12T09:00:00.000+08:00或者2019-03-12T01:00:00.000Z",
        "en": "The start of the time range to query. The value must be within the last 30 days. The default value is 5 minutes before the value of EndTime.\n\nThe time is in the RFC3339 format. For example, 09:00:00 on March 12, 2019 (UTC+8) can be represented as 2019-03-12T09:00:00.000+08:00 or 2019-03-12T01:00:00.000Z."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询一个ECI实例的监控数据",
    "en": "DescribeContainerGroupMetric"
  },
  "descriptions": {
    "zh": "- 最多支持返回50条监控数据，超出50条将会报错。\n- 仅支持分别查询5分钟以内的实时数据，或者5分钟之前的历史数据。如果查询时间段超出了当前时间点，默认只查询5分钟之前的历史数据。\n- 仅支持查询创建时间晚于北京时间2019年04月03日15点的ECI实例的监控数据。",
    "en": "- This operation can return a maximum of 50 monitoring data records. An error is returned if the number of records exceeds 50.\n\n- You can query real-time data from the last 5 minutes or historical data from more than 5 minutes ago. If the specified time range extends beyond the current time, the operation queries only historical data by default.\n\n- Monitoring data is available only for ECI instances created after 15:00 on April 3, 2019 (UTC+8)."
  },
  "example": {
    "unifiedCli": "aliyun eci describe-container-group-metric --biz-region-id cn-hangzhou --container-group-id eci-bp17da5olmkuagfr0000 --start-time 2019-03-12T01:00:00.000Z --end-time 2019-09-12T01:05:00.000Z --period 15 --region cn-hangzhou",
    "legacyCli": "aliyun eci DescribeContainerGroupMetric --region cn-hangzhou --EndTime '2019-09-12T01:05:00.000Z' --StartTime '2019-03-12T01:00:00.000Z' --Period 15 --ContainerGroupId 'eci-bp17da5olmkuagfr0000' --RegionId 'cn-hangzhou'"
  }
}