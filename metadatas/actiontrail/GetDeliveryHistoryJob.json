{
  "name": "GetDeliveryHistoryJob",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "JobId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "任务ID。",
        "en": "The ID of the historical event delivery task."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询数据回补投递任务详情",
    "en": "GetDeliveryHistoryJob"
  },
  "descriptions": {
    "zh": "本文将提供一个示例，查询投递任务ID为`16602`的数据回补投递任务详情。返回结果显示，该任务将跟踪`trail-name`的历史事件投递到日志服务SLS，且任务已经投递完成。",
    "en": "This topic provides an example on how to query the details of a data backfill task whose ID is `16602`. The return result shows that historical events for a trail named `trail-name` are delivered to Simple Log Service and the task is complete."
  },
  "example": {
    "unifiedCli": "aliyun actiontrail get-delivery-history-job --job-id 16602 --api-version 2020-07-06 --region cn-hangzhou",
    "legacyCli": "aliyun actiontrail GetDeliveryHistoryJob --region cn-hangzhou --JobId 16602"
  }
}