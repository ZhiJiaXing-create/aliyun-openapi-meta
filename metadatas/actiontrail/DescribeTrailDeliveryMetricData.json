{
  "name": "DescribeTrailDeliveryMetricData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "结束时间。格式同上。",
        "en": "The end of the time window for the query. Specify the time in ISO 8601 format: 'YYYY-MM-DDThh:mm:ssZ'. The 'Z' indicates UTC."
      }
    },
    {
      "name": "MetricName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "监控指标名称：\n- delivery_sls_success_count：投递SLS成功日志数量\n- delivery_sls_fail_count：投递SLS失败日志数量\n- delivery_oss_success_count：投递OSS成功日志数量\n- delivery_oss_fail_count：投递OSS失败日志数量",
        "en": "The name of the delivery monitoring metric. Valid values:\n\n- `delivery_sls_success_count`: The number of logs successfully delivered to SLS.\n\n- `delivery_sls_fail_count`: The number of logs that failed to be delivered to SLS.\n\n- `delivery_oss_success_count`: The number of logs successfully delivered to OSS.\n\n- `delivery_oss_fail_count`: The number of logs that failed to be delivered to OSS."
      }
    },
    {
      "name": "Period",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "监控数据的统计周期。最小值60，支持60的倍数。\n\n推荐取值：60、900 和 3600。单位：秒。",
        "en": "The statistical period for the metric data, in seconds. The value must be 60 or a multiple of 60.\n\nRecommended values: 60, 900, and 3600."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "开始时间。格式：2026-02-25T00:35:00Z。",
        "en": "The start of the time window for the query. Specify the time in ISO 8601 format: 'YYYY-MM-DDThh:mm:ssZ'. The 'Z' indicates UTC."
      }
    },
    {
      "name": "TrailName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "跟踪名称。",
        "en": "The name of the trail."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取投递监控指标",
    "en": "DescribeTrailDeliveryMetricData"
  },
  "example": {
    "unifiedCli": "aliyun actiontrail describe-trail-delivery-metric-data --metric-name delivery_sls_success_count --trail-name trail-name --start-time 2026-04-09T01:00:00Z --end-time 2026-04-10T01:00:00Z --period 3600 --api-version 2020-07-06 --region cn-hangzhou",
    "legacyCli": "aliyun actiontrail DescribeTrailDeliveryMetricData --region cn-hangzhou --MetricName delivery_sls_success_count --EndTime '2026-04-10T01:00:00Z' --StartTime '2026-04-09T01:00:00Z' --Period 3600 --TrailName 'trail-name'"
  }
}