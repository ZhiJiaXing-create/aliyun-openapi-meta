{
  "name": "ModifyFullLogTtl",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "DDoS高防实例在资源管理产品中所属的资源组ID。默认为空，即属于默认资源组。",
        "en": "The ID of the resource group to which the instance belongs in Resource Management. This parameter is empty by default, which indicates that the instance belongs to the default resource group."
      }
    },
    {
      "name": "Ttl",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "DDoS高防网站业务日志的存储时长。取值范围：**7**~**180**，单位：天。",
        "en": "The log storage duration of a website. Valid values: **7** to **180**. Unit: days."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改日志存储时长",
    "en": "ModifyFullLogTtl"
  },
  "example": {
    "unifiedCli": "aliyun ddoscoo modify-full-log-ttl --ttl 30 --resource-group-id default --api-version 2020-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun ddoscoo ModifyFullLogTtl --region cn-hangzhou --ResourceGroupId default --Ttl 30"
  }
}