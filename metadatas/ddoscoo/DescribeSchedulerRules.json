{
  "name": "DescribeSchedulerRules",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询请求时返回的页码。例如，查询第一页的返回结果，则填写**1**。",
        "en": "The page number of the returned page. For example, to query the results on the first page, enter **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "页面显示的记录数量。",
        "en": "The number of records displayed on each page."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "DDoS高防实例在资源管理产品中所属的资源组ID。默认为空，即属于默认资源组。",
        "en": "The resource group ID of the Anti-DDoS Proxy instance in Resource Management. The default value is empty, which indicates that the instance belongs to the default resource group."
      }
    },
    {
      "name": "RuleName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "规则名称。",
        "en": "The rule name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询流量调度器的调度规则",
    "en": "DescribeSchedulerRules"
  },
  "example": {
    "unifiedCli": "aliyun ddoscoo describe-scheduler-rules --resource-group-id default --rule-name testrule --page-number 1 --page-size 10 --api-version 2020-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun ddoscoo DescribeSchedulerRules --region cn-hangzhou --ResourceGroupId default --PageSize 10 --PageNumber 1 --RuleName testrule"
  }
}