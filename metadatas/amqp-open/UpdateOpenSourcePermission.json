{
  "name": "UpdateOpenSourcePermission",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大不超过64个ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "Configure",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "配置权限正则表达式",
        "en": "The regular expression for configure permissions."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "Read",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "读权限正则",
        "en": "The regular expression for read permissions."
      }
    },
    {
      "name": "UserName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户名",
        "en": "The username."
      }
    },
    {
      "name": "Vhost",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "vhost 名称",
        "en": "The vhost name."
      }
    },
    {
      "name": "Write",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "写权限正则",
        "en": "The regular expression for write permissions."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新开源权限",
    "en": "Update open source permissions"
  },
  "example": {
    "unifiedCli": "aliyun amqp-open update-open-source-permission --instance-id amqp-cn-v0h1kb9nu000 --user-name MyName --vhost production --configure '^' --write order_exchange --read '^' --client-token f6af6bb123988d191bfe01c9a9b --region cn-hangzhou",
    "legacyCli": "aliyun amqp-open UpdateOpenSourcePermission --region cn-hangzhou --Write order_exchange --Read '^' --Vhost production --UserName MyName --InstanceId 'amqp-cn-v0h1kb9nu000' --Configure '^' --ClientToken f6af6bb123988d191bfe01c9a9b"
  }
}