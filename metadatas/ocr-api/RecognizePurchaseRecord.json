{
  "name": "RecognizePurchaseRecord",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "OutputMultiOrders",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "* 是否需要识别多条订单，默认不需要。\n* true：需要；false：不需要。\n* 如果需要使用此参数，请更新SDK到1.1.14或更高版本。\n* 注意：如果此参数设置为true，返回结果字段会变化。",
        "en": "* Specifies whether to recognize multiple orders. By default, this feature is disabled.  \n* true: Enable; false: Disable.  \n* To use this parameter, update your software development kit (SDK) to version 1.1.14 or later.  \n* Note: If this parameter is set to true, the structure of the return result changes."
      }
    },
    {
      "name": "Url",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "* 本字段和body字段二选一，不可同时透传或同时为空。\n* 图片链接（长度不超2048字节，不支持base64）。",
        "en": "* This field and the body field are mutually exclusive—you must specify one and only one.  \n* Image URL (must not exceed 2048 bytes in length; base64 encoding is not supported)."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "* 本字段和URL字段二选一，不可同时透传或同时为空。\n* 图片二进制文件，最大10MB。\n* 使用HTTP方式调用，把图片二进制文件放到HTTP body中上传即可。\n* 使用SDK的方式调用，把图片放到SDK的body中即可。",
        "en": "* This field and the URL field are mutually exclusive—you must specify one and only one.  \n* Binary file of the image, up to 10 MB in size.  \n* When invoking via HTTP, upload the binary file in the HTTP body.  \n* When invoking via SDK, place the image in the SDK body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "电商订单页识别",
    "en": "E-commerce Order Page Recognition"
  }
}