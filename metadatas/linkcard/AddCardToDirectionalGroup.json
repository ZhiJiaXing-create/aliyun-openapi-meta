{
  "name": "AddCardToDirectionalGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AddType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "添加的方式：\n\nNEW：仅导入其中待分组的卡\n\nTRANSFER：全量导入（卡从原分组中删除）",
        "en": "The method used to add cards. Valid values:\n\nNEW: imports only the cards that are not assigned to any group.\n\nTRANSFER: imports all cards. The cards are removed from their original groups."
      }
    },
    {
      "name": "ApiProduct",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Linkcard",
        "en": "Linkcard."
      }
    },
    {
      "name": "GroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "分组ID。",
        "en": "The group ID."
      }
    },
    {
      "name": "IccidList",
      "position": "Query",
      "type": "Json",
      "required": true,
      "description": {
        "zh": "卡号的iccid集合，数量小于等于200",
        "en": "The collection of ICCID values for the cards. The maximum number of ICCIDs is 200."
      }
    },
    {
      "name": "MsgNotify",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "执行成功后是否通过MQ推送，默认false： \n\nfalse：不推送 \n\ntrue： 推送",
        "en": "Specifies whether to push a notification through MQ after successful execution. Default value: false. Valid values: \n\nfalse: does not push a notification. \n\ntrue: pushes a notification."
      }
    },
    {
      "name": "SerialNo",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "请求编号，支持幂等。",
        "en": "The request number. Supports idempotence."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "定向分组添加卡片",
    "en": "Add cards to a specified group"
  }
}