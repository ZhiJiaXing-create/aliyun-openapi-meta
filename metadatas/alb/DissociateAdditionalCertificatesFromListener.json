{
  "name": "DissociateAdditionalCertificatesFromListener",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Certificates",
      "position": "Query",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "扩展证书列表。当前仅支持服务器证书。最多支持20个证书。",
        "en": "A list of additional certificates. Only server certificates are supported. Specify a maximum of 20 certificates."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request. The client token must be a unique value that is generated by the client. The token can contain only ASCII characters.\n\nGenerate a unique value on your client. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system uses the **RequestId** of the request as the **ClientToken**. The **RequestId** of each request is unique."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n- **true**：发送检查请求，不会从HTTPS和QUIC监听移除扩展证书。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n\n- **false**（默认值）：发送正常请求，通过检查后返回**HTTP 2xx**状态码并直接进行操作。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: performs a dry run to check the request. The system checks for missing parameters, invalid formats, and service limits. If the request fails the check, an error is returned. If the request passes the check, the `DryRunOperation` error code is returned. No certificates are dissociated from the listener.\n\n- **false** (default): sends a normal request. After the request passes the check, an HTTP 2xx status code is returned and the operation is performed."
      }
    },
    {
      "name": "ListenerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "监听ID。HTTPS和QUIC监听有效。",
        "en": "The listener ID. This parameter applies to HTTPS and QUIC listeners."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "解除监听和扩展证书关联",
    "en": "DissociateAdditionalCertificatesFromListener"
  },
  "descriptions": {
    "zh": "**DissociateAdditionalCertificatesFromListener**接口属于异步接口，即系统返回一个请求ID，但该扩展证书尚未解绑成功，系统后台的解绑任务仍在进行。您可以调用[ListListenerCertificates](~~214354~~)查询扩展证书的解绑状态：\n\n- 当扩展证书状态处于**Dissociating**状态时，表示扩展证书正在解绑中。\n\n- 当扩展证书状态处于**Dissociated**状态时，表示扩展证书解绑成功。",
    "en": "**DissociateAdditionalCertificatesFromListener** is an asynchronous operation. After you send a request, the system returns a request ID, but the operation is still being performed in the background. You can call [](t2324486.xdita#)to query the status of the dissociation task:\n\n- If an additional certificate is in the **Dissociating** state, the dissociation is in progress.\n\n- If an additional certificate is in the **Dissociated** state, the dissociation is complete."
  },
  "example": {
    "unifiedCli": "aliyun alb dissociate-additional-certificates-from-listener --listener-id lsn-5qnirjhpt7000000 --certificates CertificateId=103705000000 --client-token 5A2CFF0E-5718-45B5-9D4D-70B3000000 --dry-run true --region cn-hangzhou",
    "legacyCli": "aliyun alb DissociateAdditionalCertificatesFromListener --region cn-hangzhou --DryRun true --Certificates.1.CertificateId 103705000000 --ClientToken '5A2CFF0E-5718-45B5-9D4D-70B3000000' --ListenerId 'lsn-5qnirjhpt7000000' --method POST --force"
  }
}