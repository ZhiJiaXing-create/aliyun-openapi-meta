{
  "name": "UpdateAclAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AclId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "ACL实例ID",
        "en": "The ID of the ACL."
      }
    },
    {
      "name": "AclName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "ACL的名称。必须以字母、中文或数字开头，可包含数字、半角句号（.）、下划线（_）、短划线（-）和空格，限制长度为2~128个字符。",
        "en": "The name of the ACL. The name must be 2 to 128 characters long. It must start with a letter, a Chinese character, or a digit, and can contain digits, periods (.), underscores (\\_), hyphens (-), and spaces."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。\n\n> 若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样。",
        "en": "A client-generated token that ensures request idempotence. The token must be unique for each request. The token can contain only ASCII characters and cannot exceed 64 characters in length.\n\n> If you do not specify this parameter, the system automatically uses the request's `RequestId` as the `ClientToken`."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n\n- **true**：发送检查请求，不会创建资源。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP `2xx`状态码并直接进行操作。",
        "en": "Specifies whether to perform a dry run on the request. Valid values:\n\n- **true**: performs a check on the request without modifying the resource. The system checks for required parameters, valid request formats, and service limits. If the check fails, an error is returned. If the check succeeds, the system returns the `DryRunOperation` error code.\n\n- **false** (default): sends a normal request. If the request passes the check, the system performs the operation and returns an HTTP `2xx` status code."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新访问控制的属性",
    "en": "UpdateAclAttribute"
  },
  "example": {
    "unifiedCli": "aliyun alb update-acl-attribute --client-token 5A2CFF0E-5718-45B5-9D4D-70B000000 --acl-id acl-hp34s2h0xx1ht4nwo0000 --acl-name test-acl --dry-run true --region cn-hangzhou",
    "legacyCli": "aliyun alb UpdateAclAttribute --region cn-hangzhou --AclId 'acl-hp34s2h0xx1ht4nwo0000' --DryRun true --ClientToken '5A2CFF0E-5718-45B5-9D4D-70B000000' --AclName 'test-acl'"
  }
}