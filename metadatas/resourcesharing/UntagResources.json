{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解绑指定资源的全部标签。取值：\n- false（默认值）：否。\n- true：是。",
        "en": "Specifies whether to remove all tags from the specified resources. Valid values:\n\n- false\n\n- true\n\nDefault value: false."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "共享单元所属的地域ID。",
        "en": "The ID of the region where the resource share resides."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "共享单元ID。\n\n最多可以添加20个共享单元。",
        "en": "The IDs of the resource shares.\n\nYou can specify up to 20 resource share IDs."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。\n\n取值为`ResourceShare`，表示共享单元。",
        "en": "The resource type.\n\nSet this parameter to `ResourceShare`, which indicates a resource share."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签键。\n\n最多可以解绑20个标签键。\n> 当解绑全部标签的参数`All`取值为`true`时，不需要设置标签键。",
        "en": "The tag keys.\n\nYou can remove up to 20 tag keys.\n\n> You do not need to specify tag keys if you set the `All` parameter to `true`."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为共享单元解绑标签",
    "en": "UntagResources"
  },
  "example": {
    "unifiedCli": "aliyun resourcesharing untag-resources --resource-type ResourceShare --resource-id rs-PqysnzIj0000 --biz-region-id cn-hangzhou --tag-key k1 --all false --region cn-hangzhou",
    "legacyCli": "aliyun resourcesharing UntagResources --region cn-hangzhou --All false --ResourceId.1 'rs-PqysnzIj0000' --TagKey.1 k1 --ResourceType ResourceShare --RegionId 'cn-hangzhou'"
  }
}