{
  "name": "DeleteResourceShare",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ResourceShareId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "共享单元ID。",
        "en": "The ID of the resource share."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除共享单元",
    "en": "DeleteResourceShare"
  },
  "descriptions": {
    "zh": "删除共享单元后，与该共享单元关联的所有资源使用者都将失去对共享资源的访问权限。删除共享单元不会删除共享资源。\n\n已成功删除的共享单元，其状态为`Deleted`，该条记录会在48~96小时内被系统自动删除。\n\n本文将提供一个示例，删除地域`cn-hangzhou`下的共享单元`rs-qSkW1HBY****`。",
    "en": "After a resource share is deleted, all principals in the resource share can no longer access the resources in the resource share. However, the resources are not deleted with the resource share.\n\nA resource share that is deleted is in the `Deleted` state. The system deletes the record of the resource share within 48 hours to 96 hours.\n\nThis topic provides an example on how to call the API operation to delete the resource share `rs-qSkW1HBY****` in the `cn-hangzhou` region."
  },
  "example": {
    "unifiedCli": "aliyun resourcesharing delete-resource-share --resource-share-id rs-qSkW1HBY0000 --region cn-hangzhou",
    "legacyCli": "aliyun resourcesharing DeleteResourceShare --region cn-hangzhou --ResourceShareId 'rs-qSkW1HBY0000'"
  }
}