{
  "name": "SetForecastAgentInstance",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DataRegion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据湖所属的地域。",
        "en": "The region where the data lake is located."
      }
    },
    {
      "name": "InstanceUuid",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "恢复目标服务器数据库备份客户端唯一标识。\n>调用[DescribeUniBackupDatabase](~~DescribeUniBackupDatabase~~)接口可以获取该参数。",
        "en": "The unique ID of the client used to restore database backups on the target server.\n\n> You can call the [DescribeUniBackupDatabase](~~DescribeUniBackupDatabase~~) operation to obtain this parameter."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置预测推演身份",
    "en": "SetForecastAgentInstance"
  },
  "descriptions": {
    "zh": "注意：删除实例是指将实例从DMS实例列表中移除，不会导致您的实例被删除或下线。",
    "en": "Note: This operation removes the instance from the DMS instance list. It does not delete the instance or take it offline."
  },
  "example": {
    "unifiedCli": "aliyun dms-enterprise set-forecast-agent-instance --instance-uuid v1dhf8mn8i298s --data-region cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun dms-enterprise SetForecastAgentInstance --region cn-hangzhou --InstanceUuid v1dhf8mn8i298s --DataRegion 'cn-hangzhou'"
  }
}