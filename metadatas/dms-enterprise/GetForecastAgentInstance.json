{
  "name": "GetForecastAgentInstance",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DataRegion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据湖所属的地域。",
        "en": "The region where the data lake is located."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取推演预测梳理列表",
    "en": "GetForecastAgentInstance"
  },
  "descriptions": {
    "zh": "注意：删除实例是指将实例从DMS实例列表中移除，不会导致您的实例被删除或下线。",
    "en": "Note: Removing an instance only removes it from the Data Management Service (DMS) instance list. This operation does not delete or shut down your instance."
  },
  "example": {
    "unifiedCli": "aliyun dms-enterprise get-forecast-agent-instance --data-region cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun dms-enterprise GetForecastAgentInstance --region cn-hangzhou --DataRegion 'cn-hangzhou'"
  }
}