{
  "name": "ListWorkspaceUsers",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "限定每次返回的最大条数，可以和nextToken搭配使用实现翻页能力",
        "en": "The maximum number of entries to return on each page. You can use this parameter with `NextToken` to implement pagination."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "NextToken暂无用途。",
        "en": "The token used to retrieve the next page of results. For the first request, leave this parameter empty. For subsequent requests, set this parameter to the NextToken value returned in the previous response."
      }
    },
    {
      "name": "PageNum",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "页码",
        "en": "The page number."
      }
    },
    {
      "name": "WorkspaceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "工作空间id",
        "en": "The ID of the workspace."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列出工作空间的用户",
    "en": "ListWorkspaceUsers"
  },
  "descriptions": {
    "zh": "## 前置条件\n* 已创建专有网络VPC。\n* 使用阿里云账号（主账号）或高权限的RAM用户（子账号）登录DMS控制台，并授予DMS访问云资源的权限（AliyunDMSProcessingDataRolePolicy）。",
    "en": "## Prerequisites\n\n- A VPC has been created.\n\n- You have logged on to the DMS console with an Alibaba Cloud account (main account) or a RAM user with administrative privileges, and have granted the `AliyunDMSProcessingDataRolePolicy` permission to DMS to access cloud resources."
  },
  "example": {
    "unifiedCli": "aliyun dms-enterprise list-workspace-users --workspace-id 120000 --page-num 1 --max-results 20 --next-token f056501ada12c1cc --region cn-hangzhou",
    "legacyCli": "aliyun dms-enterprise ListWorkspaceUsers --region cn-hangzhou --PageNum 1 --NextToken f056501ada12c1cc --MaxResults 20 --WorkspaceId 120000"
  }
}