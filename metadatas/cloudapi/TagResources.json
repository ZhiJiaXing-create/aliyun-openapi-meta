{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID。\n\nn的取值范围为`[1, 50]`",
        "en": "Resource ID.\n\nn ranges from 1 to `50`."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型定义，目前分组，插件，应用接入了标签，API相关操作基于分组的标签：\n- **apiGroup**\n- **plugin**\n- **app**\n- **api**",
        "en": "Resource type. Currently, groups, plug-ins, and applications support tags. API-related operations use group-level tags:\n\n- **apiGroup**\n\n- **plugin**\n\n- **app**\n\n- **api**"
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源的标签值。",
        "en": "The tag value of the resource."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "标签键。\n\nn 的取值范围为 `[1, 20]`",
            "en": "Tag key.\n\nn ranges from 1 to `20`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "标签值。\n\nn 的取值范围为 `[1, 20]`",
            "en": "Tag value.\n\nn ranges from 1 to `20`."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建标签资源关系（用户标签）",
    "en": "TagResources"
  },
  "descriptions": {
    "zh": "- 所有的Tag应用于所有的ResoureId的列表，即ResourceId.N是作用于所有的键值对。\n- 对于Tag.N.Key及Tag.N.Value中相同的N是一对键值对。\n- 批量标记资源，接口同成功同失败。\n- 除必填参数，传入Tag.1.Value，则入参必须有Tag.1.Key，否则报InvalidParameter.TagKey。也就是指定某个Tag.N.Value对应N的Key必须存在，但是可以为空字符串。\n- 当传入资源及Tag key，如果自定义Tag已经存在则覆盖之前的Tag value。",
    "en": "- All tags apply to all resource IDs in the list. This means that each ResourceId.N is associated with all specified key-value pairs.\n\n- For Tag.N.Key and Tag.N.Value, the same N value indicates a matching key-value pair.\n\n- Tag multiple resources in one call. The operation either succeeds for all resources or fails for all resources.\n\n- If you specify Tag.1.Value, you must also specify Tag.1.Key. Otherwise, the system returns the error InvalidParameter.TagKey. If you specify a Tag.N.Value, the corresponding Tag.N.Key must exist. The Tag.N.Key value can be an empty string.\n\n- If you specify a resource and a tag key that already exists, the existing tag value is overwritten."
  },
  "example": {
    "unifiedCli": "aliyun cloudapi tag-resources --resource-type apiGroup --tag Value=example-string Key=env --resource-id example-string --api-version 2016-07-14 --region cn-hangzhou",
    "legacyCli": "aliyun cloudapi TagResources --region cn-hangzhou --ResourceId.1 'example-string' --Tag.1.Key env --Tag.1.Value 'example-string' --ResourceType apiGroup"
  }
}