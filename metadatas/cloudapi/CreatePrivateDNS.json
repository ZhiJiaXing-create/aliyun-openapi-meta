{
  "name": "CreatePrivateDNS",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IntranetDomain",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "内网域名",
        "en": "The internal domain name."
      }
    },
    {
      "name": "Records",
      "position": "Body",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "解析记录列表，仅Type取值为A时支持配置",
        "en": "The resolution records. This parameter is valid only when Type is set to A."
      },
      "sub_parameters": [
        {
          "name": "Record",
          "position": "FormData",
          "type": "String",
          "required": false,
          "description": {
            "zh": "解析记录值",
            "en": "The resolution record."
          }
        },
        {
          "name": "Weight",
          "position": "FormData",
          "type": "Integer",
          "required": false,
          "description": {
            "zh": "权重",
            "en": "The weight of the record."
          }
        }
      ]
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "内网域名解析的类型\n- VPC：针对VPC授权解析，仅支持绑定传统类型专享实例；\n- A：支持创建A记录，仅支持绑定VPC融合类型专享实例",
        "en": "The internal domain name resolution type. Valid values:\n\n*   VPC: resolution for VPC access authorizations. A resolution of this type can be bound only to traditional dedicated instances.\n*   A: resolution that supports A records. A resolution of this type can be bound only to VPC integration dedicated instances."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建内网域名解析",
    "en": "CreatePrivateDNS"
  },
  "descriptions": {
    "zh": "内网域名解析的取值为VPC时，仅支持绑定传统类型专享实例；\n内网域名解析的取值为A时，仅支持绑定VPC融合类型专享实例。",
    "en": "An internal domain name resolution of the virtual private cloud (VPC) type can be bound only to traditional dedicated instances. An internal domain name resolution of the A type can be bound only to VPC integration dedicated instances."
  },
  "example": {
    "unifiedCli": "aliyun cloudapi create-private-dns --intranet-domain api.demo.com --type A --records Record=192.168.0.1 Weight=100 --api-version 2016-07-14 --region cn-hangzhou",
    "legacyCli": "aliyun cloudapi CreatePrivateDNS --region cn-hangzhou --Type A --IntranetDomain 'api.demo.com' --Records '[{\"Record\":\"192.168.0.1\",\"Weight\":100}]'"
  }
}