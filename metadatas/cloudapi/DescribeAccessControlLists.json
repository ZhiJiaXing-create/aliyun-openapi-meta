{
  "name": "DescribeAccessControlLists",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AclName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问控制策略名称。",
        "en": "The name of the access control policy."
      }
    },
    {
      "name": "AddressIPVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IP版本，可以设置为**ipv4**或者**ipv6**。",
        "en": "IP Version,You can set it to **ipv4** or **ipv6**."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页参数：当前页码。",
        "en": "The page number of the current page."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页参数：每页显示条数。",
        "en": "The number of entries to return on each page."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询访问控制策略列表",
    "en": "DescribeAccessControlLists"
  },
  "example": {
    "unifiedCli": "aliyun cloudapi describe-access-control-lists --acl-name testAcl --page-size 10 --page-number 1 --address-ip-version ipv6 --api-version 2016-07-14 --region cn-hangzhou",
    "legacyCli": "aliyun cloudapi DescribeAccessControlLists --region cn-hangzhou --PageSize 10 --PageNumber 1 --AddressIPVersion ipv6 --AclName testAcl"
  }
}