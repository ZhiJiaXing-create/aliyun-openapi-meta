{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Language",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定返回结果中管控策略描述的语言。取值：\n\n- zh-CN（默认值）：中文。\n- en：英文。\n- ja：日文。",
        "en": "The language in which you want to return the descriptions of the access control policies. Valid values:\n\n- zh-CN (default value): Chinese\n\n- en: English\n\n- ja: Japanese"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询支持的区域",
    "en": "DescribeRegions"
  },
  "descriptions": {
    "zh": "查询支持的区域列表\n\n* 此功能面向开放API和调用API的用户",
    "en": "This operation queries regions in which API Gateway is available.\n\n- This operation is intended for API providers and callers."
  },
  "example": {
    "unifiedCli": "aliyun cloudapi describe-regions --biz-language zh --api-version 2016-07-14 --region cn-hangzhou",
    "legacyCli": "aliyun cloudapi DescribeRegions --region cn-hangzhou --Language zh"
  }
}