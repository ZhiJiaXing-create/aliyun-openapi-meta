{
  "name": "DeletePrivateDNS",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Force",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否强制删除：\n- true：如果内网域名解析已经被实例关联，强制删除内网域名解析；\n- false：如果内网域名解析已经被实例关联，无法删除。",
        "en": "Specifies whether to force delete the resolution.\n\n*   true: force deletes the resolution if the resolution is associated with an instance.\n*   false: does not force delete the resolution if the resolution is associated with an instance."
      }
    },
    {
      "name": "IntranetDomain",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "内网域名",
        "en": "The internal domain name."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "内网域名解析的类型\n- VPC：针对VPC授权解析，仅支持绑定传统类型专享实例；\n- A：支持创建A记录，仅支持绑定VPC融合类型专享实例",
        "en": "The internal domain name resolution type. Valid values:\n\n*   VPC: resolution for virtual private cloud (VPC) access authorizations. A resolution of this type can be bound only to traditional dedicated instances.\n*   A: resolution that supports A records. A resolution of this type can be bound only to VPC integration dedicated instances."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除内网域名解析",
    "en": "DeletePrivateDNS"
  },
  "example": {
    "unifiedCli": "aliyun cloudapi delete-private-dns --intranet-domain api.demo.com --type A --biz-force false --api-version 2016-07-14 --region cn-hangzhou",
    "legacyCli": "aliyun cloudapi DeletePrivateDNS --region cn-hangzhou --Type A --Force false --IntranetDomain 'api.demo.com'"
  }
}