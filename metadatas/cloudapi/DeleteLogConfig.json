{
  "name": "DeleteLogConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "LogType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "日志类型，可选值：-**log**：日志-**survey**：质检日志",
        "en": "The log type. Valid values: **log** and **survey**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除日志配置",
    "en": "DeleteLogConfig"
  },
  "example": {
    "unifiedCli": "aliyun cloudapi delete-log-config --log-type log --api-version 2016-07-14 --region cn-hangzhou",
    "legacyCli": "aliyun cloudapi DeleteLogConfig --region cn-hangzhou --LogType log"
  }
}