{
  "name": "DescribeApiTrafficControls",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ApiIds",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定要查询的API编号，支持输入多个，“,”分隔，最多支持100个",
        "en": "The IDs of APIs that you want to query. Separate multiple API IDs with commas (,). A maximum of 100 API IDs can be entered."
      }
    },
    {
      "name": "GroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定要查询的分组",
        "en": "The ID of the API group that you want to query."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定要查询的页码，默认是1，起始是1",
        "en": "The number of the page to return. Pages start from page 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定分页查询时每页行数，最大值100，默认值为10",
        "en": "The number of entries to return on each page. Maximum value: 100. Default value: 10."
      }
    },
    {
      "name": "StageName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定要查询的环境。\n\n- **RELEASE**：线上\n- **TEST**：测试",
        "en": "The runtime environment of the API. Valid values:\n\n- **RELEASE**\n\n- **TEST**: the test environment"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询API流控策略绑定概况",
    "en": "DescribeApiTrafficControls"
  },
  "descriptions": {
    "zh": "- 此功能面向开放API的用户\n- 非必填的参数，不传入则默认返回所有，分页返回",
    "en": "- This API is intended for API providers.\n\n- The ApiIds parameter is optional. If this parameter is not specified, all results in the specified environment of an API group are returned."
  },
  "example": {
    "unifiedCli": "aliyun cloudapi describe-api-traffic-controls --stage-name RELEASE --group-id 0009db9c828549768a200320714b8930 --api-ids 123,234 --page-number 1 --page-size 20 --api-version 2016-07-14 --region cn-hangzhou",
    "legacyCli": "aliyun cloudapi DescribeApiTrafficControls --region cn-hangzhou --PageSize 20 --StageName RELEASE --PageNumber 1 --ApiIds '123,234' --GroupId 0009db9c828549768a200320714b8930"
  }
}