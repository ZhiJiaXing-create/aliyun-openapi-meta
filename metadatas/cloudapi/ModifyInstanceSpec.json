{
  "name": "ModifyInstanceSpec",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPay",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "续费时是否自动付费。取值：\n* **True**：自动付费。请确保账号有足够的余额。\n* **False**：控制台手动付费。具体操作为：登录控制台，在右上角选择**费用>进入费用中心**，在**订单管理**找到目标订单进行支付。\n\n默认值：**False**。",
        "en": "Specifies whether payment is automatically made during renewal. Valid values:\n\n- **True**: Automatic payment is enabled. Make sure that your Alibaba Cloud account has adequate balance.\n\n- **False**: Automatic payment is disabled. You have to manually pay in the console. Log on to the console. In the upper-right corner, choose **Expenses > User Center**. In the left-side navigation pane, click **Orders**. On the page that appears, find your order and complete the payment.\n\nDefault value: **False**."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例id",
        "en": "The ID of the instance."
      }
    },
    {
      "name": "InstanceSpec",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例规格",
        "en": "The specifications of the instance."
      }
    },
    {
      "name": "ModifyAction",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "变配参数，取值：\n- 升配：**UPGRADE**。\n- 降配：**DOWNGRADE**。\n\n默认值：**UPGRADE**。",
        "en": "Specifies whether to upgrade or downgrade the instance. Valid values:\n\n- **UPGRADE**\n\n- **DOWNGRADE**\n\nDefault value: **UPGRADE**."
      }
    },
    {
      "name": "SkipWaitSwitch",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否跳过\"待切流\"状态。变配过程中API网关实例可能会增加新的出口IP，\"待切流\"状态是为了提醒用户将新的出口IP添加到IP白名单，将SkipWaitSwitch参数置为true后，实例不会再进入到\"待切流\"状态，若有新的出口IP，系统会发送站内信通知用户。",
        "en": "Specifies whether to skip the Waiting for Traffic Switchover state. During the upgrade or downgrade, a new outbound IP address may be added to the API Gateway instance. The Waiting for Traffic Switchover state is used to remind users of adding the new outbound IP address to the whitelist. If you set the SkipWaitSwitch parameter to true, the instance does not enter the Waiting for Traffic Switchover state when a new outbound IP address is available. Instead, the system sends internal messages to the user."
      }
    },
    {
      "name": "Token",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "口令",
        "en": "The password."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "网关实例变配",
    "en": "ModifyInstanceSpec"
  },
  "example": {
    "unifiedCli": "aliyun cloudapi modify-instance-spec --instance-id apigateway-cn-v6419k43xxxxx --instance-spec api.s1.small --auto-pay true --token b5845042-2f2f-4e96-bd5c-36c6e5c2a68c --modify-action UPGRADE --skip-wait-switch false --api-version 2016-07-14 --region cn-hangzhou",
    "legacyCli": "aliyun cloudapi ModifyInstanceSpec --region cn-hangzhou --SkipWaitSwitch false --InstanceSpec 'api.s1.small' --InstanceId 'apigateway-cn-v6419k43xxxxx' --ModifyAction UPGRADE --Token 'b5845042-2f2f-4e96-bd5c-36c6e5c2a68c' --AutoPay true"
  }
}