{
  "name": "ResetAppSecret",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppKey",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "App的Key，用于调用API时使用",
        "en": "The key of the application that is used to make an API call."
      }
    },
    {
      "name": "NewAppKey",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置的新的AppKey，需要全局唯一",
        "en": "The new AppKey that you set must be globally unique."
      }
    },
    {
      "name": "NewAppSecret",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "新的应用密钥。为提高兼容性，建议您尽量使用其他参数。\n该参数不填，默认系统会自动生成一个新的AppSecret值",
        "en": "The new key of the application. To improve compatibility, we recommend that you use other parameters."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "重置 app 密钥",
    "en": "ResetAppSecret"
  },
  "descriptions": {
    "zh": "- 此功能面向调用API的用户\n- 可以通过该接口重置AppKey和AppSecret\n- NewAppSecret不填写，系统默认会自动生成一个新的AppSecret\n- 该接口不直接返回密码，需要通过查询密码的接口获取",
    "en": "- This operation is intended for API callers.\n\n- A new secret is automatically generated after you have called this operation. This secret cannot be customized.\n\n- The results returned by this operation do not contain the application secret. You can obtain the secret by calling DescribeAppSecurity."
  },
  "example": {
    "unifiedCli": "aliyun cloudapi reset-app-secret --app-key 60030986 --new-app-key testAppKey --new-app-secret test000 --api-version 2016-07-14 --region cn-hangzhou",
    "legacyCli": "aliyun cloudapi ResetAppSecret --region cn-hangzhou --NewAppKey testAppKey --AppKey 60030986 --NewAppSecret test000"
  }
}