{
  "name": "CreateApiDestination",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ApiDestinationName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "API名称，最大长度127个字符。最小长度2个字符。",
        "en": "The name of the API destination. The name must be 2 to 127 characters in length."
      }
    },
    {
      "name": "ConnectionName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "连接配置名称。最大长度127个字符。最小长度2个字符。\n\n<notice>需要先调用Connection接口创建连接配置，ConnectionName填写已存在的连接名称。></notice>",
        "en": "The name of the connection. The name must be 2 to 127 characters in length.\n\n> Before you configure this parameter, you must call the CreateConnection operation to create a connection. Then, set this parameter to the name of the connection that you created."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "API描述，不超过255个字符。",
        "en": "The description of the API destination. The description can be up to 255 characters in length."
      }
    },
    {
      "name": "HttpApiParameters",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "API端点的配置信息。",
        "en": "The parameters that are configured for the API destination."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建API端点信息",
    "en": "CreateApiDestination"
  },
  "descriptions": {
    "zh": "用于创建API 端点信息。",
    "en": "You can call this API operation to create an API destination."
  },
  "example": {
    "unifiedCli": "aliyun eventbridge create-api-destination --api-destination-name api-destination-name --connection-name connection-name --description Description_of_the_connection_configuration --http-api-parameters Endpoint=http://127.0.0.1:8001/api Method=POST --region cn-hangzhou",
    "legacyCli": "aliyun eventbridge CreateApiDestination --region cn-hangzhou --ConnectionName 'connection-name' --Description Description_of_the_connection_configuration --HttpApiParameters '{\"Endpoint\":\"http://127.0.0.1:8001/api\",\"Method\":\"POST\"}' --ApiDestinationName 'api-destination-name'"
  }
}