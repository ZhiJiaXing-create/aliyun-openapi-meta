{
  "name": "CreateLivePackageChannel",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ChannelName",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "频道名称，字符必须为大小写英文字母、数字、短横线、下划线。1 ~ 200个字符。格式：[A-Za-z0-9_-]+",
        "en": "The channel name. It can contain letters, digits, hyphens (-), and underscores (\\_). The name must be 1 to 200 characters in length. Format: \\[A-Za-z0-9\\_-]+"
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "幂等参数",
        "en": "The client token that is used to ensure the idempotence of the request."
      }
    },
    {
      "name": "Description",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "描述，最大1000个字符",
        "en": "The channel description. It can be up to 1,000 characters in length."
      }
    },
    {
      "name": "GroupName",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "频道组名称，字符必须为大小写英文字母、数字、短横线、下划线。1 ~ 200个字符。格式：[A-Za-z0-9_-]+",
        "en": "The channel group name. It can contain letters, digits, hyphens (-), and underscores (\\_). The name must be 1 to 200 characters in length. Format: \\[A-Za-z0-9\\_-]+"
      }
    },
    {
      "name": "Protocol",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "输入协议，目前仅支持HLS",
        "en": "The ingest protocol. Only HLS is supported."
      }
    },
    {
      "name": "SegmentCount",
      "position": "Body",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "输入流m3u8切片个数，2～100",
        "en": "The number of M3U8 segments. Valid values: 2 to 100."
      }
    },
    {
      "name": "SegmentDuration",
      "position": "Body",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "输入流切片时长，1～30",
        "en": "The segment duration. Valid values: 1 to 30."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建实时打包频道",
    "en": "CreateLivePackageChannel"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n该接口用于创建一个实时视频打包服务频道，目前支持HLS协议。系统将自动生成输入端点地址及认证所需的用户名和密码。\n\n### 注意事项\n\n- 频道组名称与频道名称只能包含大小写字母、数字、下划线和短横线。\n- 协议目前仅支持`HLS`。\n- 切片时长需设置在1至30秒之间。\n- m3u8切片个数需在2至100个之间。\n\n成功响应将返回新创建频道的详细信息，包括通道名、创建时间、修改时间、输入端点详情等。",
    "en": "## [](#)Usage notes\n\nAfter you call this operation to create a live package channel, the system will automatically generate the ingest endpoint URL, and username and password required for authentication.\n\n### [](#)Precautions\n\n- Channel group names and channel names can contain only letters, digits, underscores (\\_), and hyphens (-).\n\n- Only `HLS` is supported.\n\n- The segment duration must be from 1 to 30 seconds.\n\n- The number of M3U8 segments must be from 2 to 100.\n\nIf the request succeeds, the system will return the details of the newly created channel, including the channel name, creation time, modification time, and ingest endpoint details."
  }
}