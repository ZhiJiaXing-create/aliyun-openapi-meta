{
  "name": "ListAIAgentInstance",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AIAgentId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "智能体Id。",
        "en": "Agent ID."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询的结束时间（ISO 8601 格式）。默认无。",
        "en": "End time of the query in ISO 8601 format. No default value."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "当前页数，默认值1，取值范围1~100。",
        "en": "Page number. Default is 1. Valid values: 1 to 100."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "每页数量，默认10，取值范围0~100。",
        "en": "Number of results per page. Default is 10. Valid values: 0 to 100."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询的开始时间（ISO 8601 格式）。默认无。",
        "en": "Start time of the query in ISO 8601 format. No default value."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询AI智能体实例列表",
    "en": "Query AI Agent Instance List"
  },
  "descriptions": {
    "zh": "该接口允许用户根据指定的智能体ID（`AIAgentId`），以及可选的时间范围（`StartTime` 和 `EndTime`）、每页记录数（`PageSize`）和当前页码（`PageNumber`）来查询AI智能体实例列表。返回结果包含每个实例的状态、运行时配置、模板配置、用户自定义数据及对话通话记录的下载链接。\n\n><notice>分页参数默认值：PageSize 为 10，PageNumber 为 1。></notice>",
    "en": "This operation lists AI agent instances. Filter results by agent ID (`AIAgentId`). Optionally, specify a time range (`StartTime` and `EndTime`), the number of results per page (`PageSize`), and the page number (`PageNumber`). The response includes each instance’s status, runtime configuration, template configuration, user-defined data, and a download link for the conversation call log.\n\n><notice>\n\nDefault pagination values: PageSize is 10. PageNumber is 1.\n\n></notice>"
  }
}