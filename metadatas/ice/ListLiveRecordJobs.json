{
  "name": "ListLiveRecordJobs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询结束时间，与StartTime相差不超过30天。格式为yyyy-mm-ddthh:mm:ssz（UTC时间）。",
        "en": "The end of the time range to query. The maximum time range between EndTime and StartTime cannot exceed 30 days. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC."
      }
    },
    {
      "name": "Keyword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "搜索关键词，可以根据任务ID和名称搜索。",
        "en": "The search keyword. You can use the job ID or name as the keyword to search for jobs."
      }
    },
    {
      "name": "PageNo",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页的页码。默认值：1。",
        "en": "The page number. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页大小。默认值：10。",
        "en": "The number of entries per page. Default value: 10."
      }
    },
    {
      "name": "SortBy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序参数，默认按创建时间倒序。",
        "en": "The sorting order. By default, the query results are sorted by creation time in descending order.\n\nValid values:\n\n- asc: sorts the query results in ascending order.\n\n- desc: sorts the query results in descending order."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询开始时间。格式为yyyy-mm-ddthh:mm:ssz（UTC时间）。",
        "en": "The beginning of the time range to query. Specify the time in the ISO 8601 standard in the yyyy-MM-ddTHH:mm:ssZ format. The time must be in UTC."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "任务状态。默认不过滤状态。",
        "en": "The state of the job. By default, the state is not filtered.\n\nValid values:\n\n- paused: The job is paused.\n\n- initial: The job is not started.\n\n- started: The job is in progress."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取实时录制任务列表",
    "en": "ListLiveRecordJobs"
  }
}