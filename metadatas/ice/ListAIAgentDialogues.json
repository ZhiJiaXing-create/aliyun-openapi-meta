{
  "name": "ListAIAgentDialogues",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndTime",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "结束Unix时间戳（包含），毫秒。",
        "en": "The end Unix timestamp (inclusive), in milliseconds."
      }
    },
    {
      "name": "Order",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "ASC正序，DESC倒序。默认DESC。",
        "en": "The sort order. Valid values: `ASC` (ascending) and `DESC` (descending). Default value: `DESC`."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页数，默认1。",
        "en": "The page number. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小，最大100，默认20。",
        "en": "The number of items per page. Maximum value: 100. Default value: 20."
      }
    },
    {
      "name": "RoundLimit",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "对话回合限制。指定返回最近N个回合的对话记录，必须为正整数。与分页参数互斥，使用此参数时将忽略分页参数。",
        "en": "The number of most recent dialogue rounds to return. This value must be a positive integer. This parameter is mutually exclusive with pagination parameters; if specified, it overrides them."
      }
    },
    {
      "name": "SessionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "会话ID。",
        "en": "The session ID."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "开始Unix时间戳（包含），毫秒。",
        "en": "The start Unix timestamp (inclusive), in milliseconds."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询对话记录",
    "en": "ListAIAgentDialogues"
  }
}