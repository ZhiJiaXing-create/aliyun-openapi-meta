{
  "name": "DetectAudioForCustomizedVoiceJob",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AudioRecordId",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "RecordUrl 对应的音频序号",
        "en": "The sequence number of the recording file."
      }
    },
    {
      "name": "RecordUrl",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "录音文件 URL 地址\n><notice>必须是您账号下的 OSS 地址，并且 OSS Bucket 的所在区域要与调用本接口时服务所在的区域相同></notice>\n><notice>音频格式要求：音频文件必须是 wav 或 pcm 格式，且为单声道（mono）16 bit 采样位数音频。音频采样率为 48000 Hz></notice>",
        "en": "The URL of the recording file.\n\n> : The URL must be an Object Storage Service (OSS) URL within your Alibaba Cloud account. The OSS bucket must be in the same region in which IMS is activated.\n\n> : The audio file must be in the WAV or PCM format and must be a 16-bit mono audio file at 48000 Hz."
      }
    },
    {
      "name": "VoiceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "人声 Id",
        "en": "The voice ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "提交音频检测（基础版）",
    "en": "DetectAudioForCustomizedVoiceJob"
  }
}