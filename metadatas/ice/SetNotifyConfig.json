{
  "name": "SetNotifyConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AIAgentId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "智能体ID。",
        "en": "The ID of the AI Agent."
      }
    },
    {
      "name": "AudioOssPath",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "CallbackUrl",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户希望接收回调通知的URL。默认不填写。",
        "en": "The callback URL for receiving event notifications. This is not set by default."
      }
    },
    {
      "name": "EnableAudioRecording",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "EnableNotify",
      "position": "Query",
      "type": "Boolean",
      "required": true,
      "description": {
        "zh": "用于开启或关闭事件回调的开关。",
        "en": "Specifies whether to enable or disable event notifications."
      }
    },
    {
      "name": "EventTypes",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "事件类型，不填写则全选。\n- agent_start\n- agent_stop\n- error",
        "en": "The event types. If you do not specify this parameter, all event types are subscribed to by default. Valid values:\n\n- agent\\_start\n\n- agent\\_stop\n\n- error"
      }
    },
    {
      "name": "Token",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "回调的鉴权token。发送回调请求时携带在请求头的 Authorization 字段中。默认不填写。",
        "en": "An authentication token for event callbacks. The service includes this token in the `Authorization` header of each callback request."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置智能体事件回调通知配置",
    "en": "SetNotifyConfig"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n该接口用于设置智能体实例的事件通知配置。用户可以通过此接口来控制是否开启事件通知功能（`EnableNotify`），设置接收通知的回调地址（`CallbackUrl`），以及指定希望接收哪些类型的事件通知（`EventTypes`）。此外，还可以传递一个校验Token（`Token`）以增强安全性。请求成功后，会返回一个唯一的请求ID（`RequestId`）供后续追踪使用。",
    "en": "## Description\n\nThis operation configures event notification settings for an AI Agent instance. You can enable or disable event notifications, specify the Callback URL, and define the Event Types to subscribe to. You can also provide an Authentication Token for enhanced security. A successful request returns a unique Request ID for tracking and troubleshooting."
  }
}