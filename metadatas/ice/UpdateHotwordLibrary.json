{
  "name": "UpdateHotwordLibrary",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "描述，长度上限200",
        "en": "The description of the hotword library. It can be up to 200 characters in length."
      }
    },
    {
      "name": "HotwordLibraryId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "词库ID",
        "en": "The ID of the hotword library."
      }
    },
    {
      "name": "Hotwords",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "热词列表，长度上限300",
        "en": "The hotword list. You can add up to 300 hotword entries to a single library."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "词库名称，长度上限100",
        "en": "The name of the hotword library. It can be up to 100 characters in length."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改热词库",
    "en": "UpdateHotwordLibrary"
  },
  "descriptions": {
    "zh": "## 请求说明\n- 该接口允许用户修改已存在的热词库信息。\n- 修改时需要提供热词库ID（`HotwordLibraryId`），这是必填参数。\n- 可以同时更新热词库的名称（`Name`）、描述（`Description`）以及热词列表（`HotWords`）。\n- 热词列表中的每个热词可以设置其文本内容（`Text`）、权重（`Weight`）、语种（`Language`）及翻译转置结果（`TranspositionResultList`）。\n- 注意：单个账号下最多创建100个词库，单个词库最多添加300个热词；在单个词库内，一个热词的`language`和`text`组合起来必须唯一；在单个词库内，一个热词的`TranspositionResultList`里面的`TranslatedText`和`TargetLanguage`组合也必须唯一。",
    "en": "## [](#)\n\n- Modify a specified hotword library.\n\n- The hotword library ID (`HotwordLibraryId`) is required to identify the library that requires modification.\n\n- You can modify its name (`Name` ), description (`Description` ), and hotword list (`HotWords`).\n\n- Each hotword in the list can also be modified, including its content (`Text`), weight (`Weight`), language (`Language`), and translation results (`TranspositionResultList`).\n\n- A single account supports up to 100 hotword libraries, each containing a maximum of 300 hotword entries. In a library, the combination of `language` and `text` of an entry must be unique. The combination of `TranslatedText` and `TargetLanguage` in `TranspositionResultList` must also be unique."
  }
}