{
  "name": "GetMediaConnectFlow",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FlowId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Flow实例ID",
        "en": "The ID of the MediaConnect Flow instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取MediaConnect Flow实例的信息",
    "en": "Get MediaConnect Flow Instance Information"
  },
  "descriptions": {
    "zh": "- 当输入的Flow实例ID不存在时，接口会返回错误。\n- 当Flow状态为online时，接口返回的StartTime才有效。",
    "en": "- This operation returns an error if the specified `FlowId` does not exist.\n\n- The `StartTime` in the response is valid only when the flow status is `online`."
  }
}