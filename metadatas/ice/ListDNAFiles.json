{
  "name": "ListDNAFiles",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DBId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "DNA库Id。",
        "en": "The ID of the media fingerprint library."
      }
    },
    {
      "name": "NextPageToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页查询。请求第一页时，NextPageToken为空；请求后续文件时需传入前一页查询结果中的NextPageToken值。",
        "en": "The pagination token that is used in the next request to retrieve a new page of results. You do not need to specify this parameter for the first request. You must specify the token that is obtained from the previous query as the value of NextToken."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "单页数据个数，默认为20，最大100。",
        "en": "The number of entries per page. Default value: 20. Valid values: 1 to 100."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询DNA文件",
    "en": "ListDNAFiles"
  },
  "descriptions": {
    "zh": "本接口通过DNA库ID查询文件列表，支持分页查询。",
    "en": "Queries files in a media fingerprint library based on the library ID. The queried results can be paginated."
  }
}