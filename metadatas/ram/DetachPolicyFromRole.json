{
  "name": "DetachPolicyFromRole",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PolicyName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "权限策略名称。",
        "en": "The name of the policy to detach."
      }
    },
    {
      "name": "PolicyType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定权限策略的类型，取值为`System`或`Custom`。",
        "en": "The policy type. Valid values: `System` and `Custom`."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RoleName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定角色名。",
        "en": "The name of the role from which to detach the policy."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为角色撤销指定的权限",
    "en": "DetachPolicyFromRole"
  },
  "example": {
    "unifiedCli": "aliyun ram detach-policy-from-role --policy-type Custom --policy-name OSS-Administrator --role-name OSSAdminRole --resource-group-id example-string --region cn-hangzhou",
    "legacyCli": "aliyun ram DetachPolicyFromRole --region cn-hangzhou --PolicyType Custom --RoleName OSSAdminRole --ResourceGroupId 'example-string' --PolicyName 'OSS-Administrator'"
  }
}