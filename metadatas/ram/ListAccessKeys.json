{
  "name": "ListAccessKeys",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定用户，RAM用户访问时不提供此参数则表示列出自己的访问密钥。",
        "en": "The name of the RAM user. If a RAM user calls this operation and does not specify this parameter, the AccessKey pairs of the RAM user are returned."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列出指定用户的访问密钥",
    "en": "ListAccessKeys"
  },
  "example": {
    "unifiedCli": "aliyun ram list-access-keys --user-name zhangq0000 --region cn-hangzhou",
    "legacyCli": "aliyun ram ListAccessKeys --region cn-hangzhou --UserName zhangq0000"
  }
}