{
  "name": "CreateAccessKey",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定用户名，RAM用户调用此接口时，默认为自己创建访问密钥。",
        "en": "The name of the RAM user. If a RAM user calls this operation and does not specify this parameter, an AccessKey pair is created for the RAM user."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为RAM用户创建访问密钥",
    "en": "CreateAccessKey"
  },
  "example": {
    "unifiedCli": "aliyun ram create-access-key --user-name zhangq0000 --region cn-hangzhou",
    "legacyCli": "aliyun ram CreateAccessKey --region cn-hangzhou --UserName zhangq0000"
  }
}