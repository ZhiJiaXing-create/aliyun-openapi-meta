{
  "name": "SetDefaultPolicyVersion",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PolicyName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定权限策略名称。",
        "en": "The name of the policy."
      }
    },
    {
      "name": "VersionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "新默认版本的ID。",
        "en": "The ID of the policy version that you want to set as the default version."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置权限策略默认版本",
    "en": "SetDefaultPolicyVersion"
  },
  "example": {
    "unifiedCli": "aliyun ram set-default-policy-version --policy-name OSS-Administrator --version-id v2 --region cn-hangzhou",
    "legacyCli": "aliyun ram SetDefaultPolicyVersion --region cn-hangzhou --VersionId v2 --PolicyName 'OSS-Administrator'"
  }
}