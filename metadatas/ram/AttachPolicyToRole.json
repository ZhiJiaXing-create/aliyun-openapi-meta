{
  "name": "AttachPolicyToRole",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PolicyName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定权限策略名称。",
        "en": "The policy name."
      }
    },
    {
      "name": "PolicyType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定权限策略的类型，取值为`System`或`Custom`。",
        "en": "The policy type. Valid values are `System` and `Custom`."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "RoleName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定角色名。",
        "en": "The role name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为指定角色添加权限",
    "en": "AttachPolicyToRole"
  },
  "example": {
    "unifiedCli": "aliyun ram attach-policy-to-role --policy-type Custom --policy-name OSS-Administrator --role-name OSSAdminRole --resource-group-id example-string --region cn-hangzhou",
    "legacyCli": "aliyun ram AttachPolicyToRole --region cn-hangzhou --PolicyType Custom --RoleName OSSAdminRole --ResourceGroupId 'example-string' --PolicyName 'OSS-Administrator'"
  }
}