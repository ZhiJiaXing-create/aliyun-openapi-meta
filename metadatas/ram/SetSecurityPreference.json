{
  "name": "SetSecurityPreference",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllowUserToChangePassword",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否允许RAM用户自主管理密码。取值：\n- true（默认值）：允许。\n- false：不允许。",
        "en": "Specifies whether RAM users can change their passwords. Valid values:\n\n*   true (default)\n*   false"
      }
    },
    {
      "name": "AllowUserToManageAccessKeys",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否允许RAM用户自主管理访问密钥。取值：\n- true：允许。\n- false（默认值）：不允许。",
        "en": "Specifies whether RAM users can manage their AccessKey pairs. Valid values:\n\n*   true\n*   false (default)"
      }
    },
    {
      "name": "AllowUserToManageMFADevices",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否允许RAM用户自主管理多因素认证设备。取值：\n- true（默认值）：允许。\n- false：不允许。",
        "en": "Specifies whether RAM users can manage their MFA devices. Valid values:\n\n*   true (default)\n*   false"
      }
    },
    {
      "name": "AllowUserToManagePublicKeys",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否允许RAM用户自主管理公钥。取值：\n- true：允许。\n- false（默认值）：不允许。\n\n> 该参数仅在日本站有效。",
        "en": "Specifies whether RAM users can manage their public keys. Valid values:\n\n*   true\n*   false (default)\n\n>  This parameter is valid only for the Japan site."
      }
    },
    {
      "name": "EnableSaveMFATicket",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否允许RAM用户在登录时保存多因素设备认证状态，有效期为7天。取值：\n- true：允许。\n- false（默认值）：不允许。",
        "en": "Specifies whether to remember the multi-factor authentication (MFA) devices of Resource Access Management (RAM) users for seven days. Valid values:\n\n*   true\n*   false (default)"
      }
    },
    {
      "name": "LoginNetworkMasks",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "登录掩码。登录掩码决定哪些IP地址会受到登录控制台的影响，包括密码登录和单点登录（SSO），但使用访问密钥发起的API调用并不受影响。\n\n- 如果指定掩码，RAM用户只能从指定的IP地址进行登录。\n- 如果不指定任何掩码，登录控制台功能将适用于整个网络。\n\n当需要配置多个登录掩码时，请使用分号（;）来分隔，例如：192.168.0.0/16;10.0.0.0/8。\n\n最多配置40个登录掩码，总长度最大512个字符。",
        "en": "The subnet mask that specifies the IP addresses from which you can log on to the Alibaba Cloud Management Console. This parameter takes effect on password-based logon and single sign-on (SSO). However, this parameter does not take effect on API calls that are authenticated by using AccessKey pairs.\n\n*   If you specify a subnet mask, RAM users can use only the IP addresses in the subnet mask to log on to the Alibaba Cloud Management Console.\n*   If you do not specify a subnet mask, RAM users can use all IP addresses to log on to the Alibaba Cloud Management Console.\n\nIf you need to specify multiple subnet masks, separate the subnet masks with semicolons (;). Example: 192.168.0.0/16;10.0.0.0/8.\n\nYou can specify up to 40 subnet masks. The total length of the subnet masks can be a maximum of 512 characters."
      }
    },
    {
      "name": "LoginSessionDuration",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "RAM用户登录有效期。\n\n取值范围：1~24。默认值：6。单位：小时。",
        "en": "The validity period of the logon session of RAM users.\n\nValid values: 1 to 24. Default value: 6. Unit: hours."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置全局安全首选项",
    "en": "SetSecurityPreference"
  },
  "example": {
    "unifiedCli": "aliyun ram set-security-preference --enable-save-mfa-ticket true --allow-user-to-change-password true --allow-user-to-manage-access-keys false --allow-user-to-manage-public-keys false --allow-user-to-manage-mfa-devices true --login-session-duration 6 --login-network-masks 10.0.0.0/8 --region cn-hangzhou",
    "legacyCli": "aliyun ram SetSecurityPreference --region cn-hangzhou --LoginSessionDuration 6 --AllowUserToManageMFADevices true --AllowUserToManagePublicKeys false --LoginNetworkMasks '10.0.0.0/8' --AllowUserToChangePassword true --AllowUserToManageAccessKeys false --EnableSaveMFATicket true"
  }
}