{
  "name": "DeletePolicy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CascadingDelete",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否级联删除权限策略的所有版本。取值：\n\n- true：会级联删除所有版本。\n- false：不会级联删除，需要您在删除权限策略前，手动删除权限策略非默认版本。",
        "en": "Specifies whether to delete all versions of the policy. Valid values:\n\n- true: deletes all versions of the policy.\n\n- false: does not delete all versions of the policy. If you set the parameter to false, the non-default versions of the policy are not deleted. Before you delete the policy, you must manually delete all non-default versions of the policy."
      }
    },
    {
      "name": "PolicyName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定权限策略名称。",
        "en": "The name of the policy."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除指定的权限策略",
    "en": "DeletePolicy"
  },
  "example": {
    "unifiedCli": "aliyun ram delete-policy --policy-name OSS-Administrator --cascading-delete true --region cn-hangzhou",
    "legacyCli": "aliyun ram DeletePolicy --region cn-hangzhou --CascadingDelete true --PolicyName 'OSS-Administrator'"
  }
}