{
  "name": "SetPasswordPolicy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "HardExpiry",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "密码是否过期。\n取值为`true`或`false`，如果未指定此参数的值，系统默认为`false`。\n\n- 如果取值为`true`，此时必须由主账号重置密码后，RAM用户才能正常登录。\n- 如果取值为`false`，此时RAM用户可以在密码过期后自行更改密码，并继续以用户身份登录。",
        "en": "Specifies whether a password will expire. Valid values: `true` and `false`. Default value: `false`. If you leave this parameter unspecified, the default value false is used.\n\n- If you set this parameter to `true`, the Alibaba Cloud account to which the RAM users belong must reset the passwords before the RAM users can log on to the Alibaba Cloud Management Console.\n\n- If you set this parameter to `false`, the RAM users can change the passwords after the passwords expire and then log on to the Alibaba Cloud Management Console."
      }
    },
    {
      "name": "MaxLoginAttemps",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "一小时内使用错误密码尝试登录最大次数（重置密码可清除尝试登录计数）。",
        "en": "The maximum number of permitted logon attempts within one hour. The number of logon attempts is reset to zero if a RAM user changes the password."
      }
    },
    {
      "name": "MaxPasswordAge",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "密码有效期，单位是天（重置密码将重置密码过期时间）。0表示不启用密码过期策略，默认不启用。",
        "en": "The number of days for which a password is valid. If you reset a password, the password validity period restarts. Default value: 0. The default value indicates that the password never expires."
      }
    },
    {
      "name": "MinimumPasswordLength",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "最小密码长度。\n\n取值范围：8~32。默认值：8。",
        "en": "The minimum number of characters in a password.\n\nValid values: 8 to 32. Default value: 8."
      }
    },
    {
      "name": "PasswordReusePrevention",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "禁止使用前N次密码。0表示不启用历史密码检查策略，默认不启用。",
        "en": "The number of previous passwords that a RAM user is prevented from reusing. Default value: 0. The default value indicates that the RAM user can reuse previous passwords."
      }
    },
    {
      "name": "RequireLowercaseCharacters",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "必须含有小写字母。",
        "en": "Specifies whether a password must contain one or more lowercase letters."
      }
    },
    {
      "name": "RequireNumbers",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "必须含有数字。",
        "en": "Specifies whether a password must contain one or more digits."
      }
    },
    {
      "name": "RequireSymbols",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "必须含有特殊字符。",
        "en": "Specifies whether a password must contain one or more special characters."
      }
    },
    {
      "name": "RequireUppercaseCharacters",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "必须含有大写字母。",
        "en": "Specifies whether a password must contain one or more uppercase letters."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置RAM用户密码强度等策略信息",
    "en": "SetPasswordPolicy"
  },
  "example": {
    "unifiedCli": "aliyun ram set-password-policy --minimum-password-length 12 --require-lowercase-characters true --require-uppercase-characters true --require-numbers true --require-symbols true --hard-expiry false --max-password-age 0 --password-reuse-prevention 0 --max-login-attemps 5 --region cn-hangzhou",
    "legacyCli": "aliyun ram SetPasswordPolicy --region cn-hangzhou --MinimumPasswordLength 12 --RequireLowercaseCharacters true --RequireNumbers true --MaxPasswordAge 0 --PasswordReusePrevention 0 --MaxLoginAttemps 5 --HardExpiry false --RequireUppercaseCharacters true --RequireSymbols true"
  }
}