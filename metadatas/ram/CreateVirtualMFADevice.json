{
  "name": "CreateVirtualMFADevice",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "VirtualMFADeviceName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "多因素认证设备名称。\n\n长度为1~64个字符，可包含英文字母、数字、半角句号（.）和短划线（-）。",
        "en": "The name of the MFA device.\n\nThe name must be 1 to 64 characters in length, and can contain letters, digits, periods (.), and hyphens (-)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建多因素认证设备",
    "en": "CreateVirtualMFADevice"
  },
  "example": {
    "unifiedCli": "aliyun ram create-virtual-mfa-device --virtual-mfa-device-name device001 --region cn-hangzhou",
    "legacyCli": "aliyun ram CreateVirtualMFADevice --region cn-hangzhou --VirtualMFADeviceName device001"
  }
}