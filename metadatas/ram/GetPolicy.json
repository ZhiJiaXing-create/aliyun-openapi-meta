{
  "name": "GetPolicy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PolicyName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定权限策略名称。",
        "en": "The name of the policy."
      }
    },
    {
      "name": "PolicyType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定权限策略的类型，取值为`System`或`Custom`。",
        "en": "The type of the policy. Valid values: `System` and `Custom`."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取指定的权限策略信息",
    "en": "GetPolicy"
  },
  "example": {
    "unifiedCli": "aliyun ram get-policy --policy-type Custom --policy-name OSS-Administrator --region cn-hangzhou",
    "legacyCli": "aliyun ram GetPolicy --region cn-hangzhou --PolicyType Custom --PolicyName 'OSS-Administrator'"
  }
}