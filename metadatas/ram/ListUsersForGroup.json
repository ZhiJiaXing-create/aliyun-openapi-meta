{
  "name": "ListUsersForGroup",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GroupName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户组名称。",
        "en": "The name of the RAM user group."
      }
    },
    {
      "name": "Marker",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "当请求的返回结果被截断时，可以使用`Marker`获取从当前截断位置之后的内容。",
        "en": "The `marker`. If part of a previous response is truncated, you can use this parameter to obtain the truncated part."
      }
    },
    {
      "name": "MaxItems",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定返回结果的条数。当返回结果达到`MaxItems`限制被截断时，返回参数`IsTruncated`将等于`true`。\n\n取值范围：1 ~ 1000，默认值：100。",
        "en": "The number of entries to return. If a response is truncated because it reaches the value of `MaxItems`, the value of `IsTruncated` will be `true`.\n\nValid values: 1 to 1000. Default value: 100."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列出指定用户组所包含的RAM用户",
    "en": "ListUsersForGroup"
  },
  "example": {
    "unifiedCli": "aliyun ram list-users-for-group --group-name Dev-Team --marker EXAMPLE --max-items 100 --region cn-hangzhou",
    "legacyCli": "aliyun ram ListUsersForGroup --region cn-hangzhou --GroupName 'Dev-Team' --Marker EXAMPLE --MaxItems 100"
  }
}