{
  "name": "CreatePool",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PoolName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源池名称。\n- 长度不超过15个字符。\n- 可包含数字、大写字母、小写字母、下划线（_）和点（.）。",
        "en": "The name of the pool.\n\n- The name can be up to 15 characters long.\n\n- The name can contain digits, uppercase and lowercase letters, underscores (\\_), and periods (.)."
      }
    },
    {
      "name": "Priority",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "资源池的优先级。\n\n- 优先级可设置范围：1~99。默认值为1，即最低优先级。\n- 提交到具有较高优先级层级值的资源池的作业将在较低优先级层级值的资源池中的挂起作业之前进行调度，资源池的优先级层级优先于作业的优先级。",
        "en": "The priority of the pool.\n\n- Valid values: 1 to 99. The default value is 1, which indicates the lowest priority.\n\n- Jobs in higher-priority pools are scheduled before pending jobs in lower-priority pools. The pool's priority overrides an individual job's priority."
      }
    },
    {
      "name": "ResourceLimits",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户在一个资源池内允许并发使用的资源配额限制。",
        "en": "The limits on the resources that a single user can use concurrently in the pool."
      }
    },
    {
      "name": "SchedulingPolicyId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "调度策略。"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建资源池",
    "en": "CreatePool"
  },
  "example": {
    "unifiedCli": "aliyun ehpcinstant create-pool --pool-name PoolTest --priority 1 --resource-limits MaxExecutorNum=100 --scheduling-policy-id policy-xxx --region cn-hangzhou",
    "legacyCli": "aliyun ehpcinstant CreatePool --region cn-hangzhou --PoolName PoolTest --Priority 1 --ResourceLimits '{\"MaxExecutorNum\":100}' --SchedulingPolicyId 'policy-xxx'"
  }
}