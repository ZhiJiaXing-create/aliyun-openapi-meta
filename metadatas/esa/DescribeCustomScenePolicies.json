{
  "name": "DescribeCustomScenePolicies",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码。取值范围为：**1~100000**。",
        "en": "The page number. Valid values: **1** to **100000**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小。默认值：**10**，可选值：**5**，**10**，**20**。",
        "en": "The number of entries per page. Default value: **10**. Valid values: **5**, **10**, and **20**."
      }
    },
    {
      "name": "PolicyId",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "策略ID。",
        "en": "The policy ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询定制场景策略",
    "en": "Query scenario-specific policies"
  },
  "example": {
    "unifiedCli": "aliyun esa describe-custom-scene-policies --page-size 10 --page-number 1 --policy-id 12340000 --region cn-hangzhou",
    "legacyCli": "aliyun esa DescribeCustomScenePolicies --region cn-hangzhou --PageSize 10 --PageNumber 1 --PolicyId 12340000"
  }
}