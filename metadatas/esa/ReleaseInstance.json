{
  "name": "ReleaseInstance",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "安全实例 ID（须为 DDoS 安全实例 ID（格式 esa-ddos-*），通过 ListDDoSInstances API 获取；站点实例 ID（格式 esa-site-*）不适用）。",
        "en": "The ID of the security instance. This must be a DDoS security instance ID in the format of esa-ddos-*. You can call the ListDDoSInstances operation to obtain the ID. Site instance IDs in the format of esa-site-* are not supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "预约释放",
    "en": "Schedule release"
  },
  "example": {
    "unifiedCli": "aliyun esa release-instance --instance-id esa-ddos-2sdj003s --region cn-hangzhou",
    "legacyCli": "aliyun esa ReleaseInstance --region cn-hangzhou --InstanceId 'esa-ddos-2sdj003s'"
  }
}