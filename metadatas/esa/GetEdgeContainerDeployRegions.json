{
  "name": "GetEdgeContainerDeployRegions",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用ID，可使用[ListEdgeContainerApps](~~ListEdgeContainerApps~~)接口获取应用ID。><notice>注意：本参数实际为必填项，未传入时服务返回 InvalidParameter.appid(400)；有效格式为 app-{18位数字}，可通过 ListEdgeContainerApps 接口获取」，同时将 example 修正为真实示例值（如 app-880****75783794688。\n如未开通，需要开通 Edge Container 服务并通过 CreateEdgeContainerApp 创建应用并获取 AppId。\n</notice>",
        "en": "The application ID. You can call the [ListEdgeContainerApps](~~ListEdgeContainerApps~~) operation to obtain the application ID.><notice>This parameter is required. If this parameter is not specified, the service returns InvalidParameter.appid (400). The valid format is app-{18-digit number}. You can call the ListEdgeContainerApps operation to obtain the application ID. Example: app-880****75783794688. If you have not activated the Edge Container service, activate it first and then call the CreateEdgeContainerApp operation to create an application and obtain the AppId.</notice>."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取边缘容器应用部署区域",
    "en": "Get edge container application deployment regions"
  },
  "example": {
    "unifiedCli": "aliyun esa get-edge-container-deploy-regions --app-id GetEdgeContainerDeployRegions --region cn-hangzhou",
    "legacyCli": "aliyun esa GetEdgeContainerDeployRegions --region cn-hangzhou --AppId GetEdgeContainerDeployRegions"
  }
}