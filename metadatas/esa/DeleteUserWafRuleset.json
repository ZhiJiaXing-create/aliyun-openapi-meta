{
  "name": "DeleteUserWafRuleset",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Id",
      "position": "Body",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "WAF规则集ID。",
        "en": "The ID of the WAF ruleset."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The ID of the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除用户WAF规则集",
    "en": "DeleteUserWafRuleset"
  },
  "descriptions": {
    "zh": "## 请求说明\n- `InstanceId` 和 `Id` 是必需参数，分别指定了要操作的WAF实例ID和具体的规则集ID。",
    "en": "## Request description\n\n- The `InstanceId` and `Id` parameters are required. These parameters specify the ID of the WAF instance and the ID of the ruleset to delete."
  },
  "example": {
    "unifiedCli": "aliyun esa delete-user-waf-ruleset --instance-id esa-xxxxxxx --id 10000001 --region cn-hangzhou",
    "legacyCli": "aliyun esa DeleteUserWafRuleset --region cn-hangzhou --InstanceId 'esa-xxxxxxx' --Id 10000001"
  }
}