{
  "name": "CreateCacheRule",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AdditionalCacheablePorts",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "- 在指定端口上开启缓存功能。\n- 取值范围：8880、2052、2082、2086、2095、2053、2083、2087、2096\n- 支持配置多个端口，不同端口间使用英文逗号分隔。",
        "en": "- Specifies additional ports on which caching is enabled.\n\n- Valid values: 8880, 2052, 2082, 2086, 2095, 2053, 2083, 2087, and 2096.\n\n- You can specify multiple ports, separated by commas (,)."
      }
    },
    {
      "name": "BrowserCacheMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "浏览器缓存模式。取值范围：\n- no_cache：不缓存。\n- follow_origin：遵循源站缓存策略。\n- override_origin：代替源站缓存策略。",
        "en": "The browser cache mode. Valid values:\n\n- `no_cache`: Disables browser caching.\n\n- `follow_origin`: Follows the origin server cache policy.\n\n- `override_origin`: Overrides the origin server cache policy."
      }
    },
    {
      "name": "BrowserCacheTtl",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "浏览器缓存过期时间，单位是秒。",
        "en": "The browser cache TTL, in seconds."
      }
    },
    {
      "name": "BypassCache",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置绕过缓存模式。取值范围：\n- cache_all：所有请求都缓存。\n- bypass_all：所有请求都绕过缓存。",
        "en": "The bypass cache mode. Valid values:\n\n- `cache_all`: Caches all requests.\n\n- `bypass_all`: Bypasses the cache for all requests."
      }
    },
    {
      "name": "CacheDeceptionArmor",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "缓存欺骗防御。用于防御Web缓存欺骗攻击，通过校验的缓存内容才会被缓存下来。取值范围：\n- on：开启。\n- off：关闭。",
        "en": "Specifies whether to enable cache deception defense. This feature helps defend against web cache deception attacks. When this feature is enabled, only content that passes validation is cached. Valid values:\n\n- `on`: Enables the defense.\n\n- `off`: Disables the defense."
      }
    },
    {
      "name": "CacheReserveEligibility",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "缓存保持资格。用于控制用户请求在回源的时候是否绕过缓存保持节点。取值范围：\n- bypass_cache_reserve：请求绕过缓存保持。\n- eligible_for_cache_reserve：符合缓存保持资格。",
        "en": "Specifies whether requests bypass cache reservation nodes during an origin fetch. Valid values:\n\n- `bypass_cache_reserve`: The request bypasses cache reservation.\n\n- `eligible_for_cache_reserve`: The request is eligible for cache reservation."
      }
    },
    {
      "name": "CheckPresenceCookie",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "生成缓存键的时候，检查指定的cookie是否存在，如果存在，则在缓存键中加入cookie名称（cookie名称不区分大小写）。支持输入多个cookie名称，多个值之间用空格分隔。cookie名称支持以下字符类型：\n- 符号：! # $ % & ' * + - . ^ _ ` | ~\n- 数字：0-9\n- 字母：小写英文字母a-z",
        "en": "Specifies the cookies to check for presence when generating a cache key. If a specified cookie is present in the request, its name (case-insensitive) is included in the cache key. To specify multiple cookies, separate their names with spaces. The cookie names can contain the following characters:\n\n- Symbols: ! # $ % & ' \\* + - . ^ \\_ | \\~\n\n- Digits: 0-9\n\n- Letters: a-z (lowercase)"
      }
    },
    {
      "name": "CheckPresenceHeader",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "生成缓存键的时候，检查指定的标头是否存在，如果存在，则在缓存键中加入标头名称（标头名称不区分大小写）。支持输入多个标头名称，多个值之间用空格分隔。标头名称支持以下字符类型：\n- 符号：! # $ % & ' * + - . ^ _ ` | ~\n- 数字：0-9\n- 字母：小写英文字母a-z",
        "en": "Specifies the headers to check for presence when generating a cache key. If a specified header is present in the request, its name (case-insensitive) is included in the cache key. To specify multiple headers, separate their names with spaces. The header names can contain the following characters:\n\n- Symbols: ! # $ % & ' \\* + - . ^ \\_ | \\~\n\n- Digits: 0-9\n\n- Letters: a-z (lowercase)"
      }
    },
    {
      "name": "EdgeCacheMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "节点缓存模式。取值范围：\n- follow_origin：遵循源站缓存策略（如果存在），否则使用默认缓存策略。\n- no_cache：不缓存。\n- override_origin：代替源站缓存策略。\n- follow_origin_bypass：遵循源站缓存策略（如果存在），否则不缓存。\n- follow_origin_override：遵循源站缓存策略（如果存在），否则使用自定义缓存TTL。",
        "en": "The edge cache mode. Valid values:\n\n- `follow_origin`: Follows the origin server cache policy if one exists; otherwise, uses the default cache policy.\n\n- `no_cache`: Disables caching on the edge node.\n\n- `override_origin`: Overrides the origin server cache policy.\n\n- `follow_origin_bypass`: Follows the origin server cache policy if one exists; otherwise, the content is not cached.\n\n- `follow_origin_override`: Follows the origin server cache policy if one exists; otherwise, uses a custom edge cache TTL."
      }
    },
    {
      "name": "EdgeCacheTtl",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "节点缓存过期时间，单位是秒。",
        "en": "The edge cache TTL, in seconds."
      }
    },
    {
      "name": "EdgeStatusCodeCacheTtl",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "状态码缓存过期时间，单位是秒。\n- 支持对指定状态码设置缓存过期时间，例如：404=10，表示对404状态码缓存10秒。\n- 支持对4xx、5xx系列状态码设置缓存过期时间，例如：4xx=10，表示对400系列的所有状态码都缓存10秒。\n- 支持对多个状态码设置缓存过期时间，状态码之间用英文逗号分隔。",
        "en": "The status code cache TTL, in seconds.\n\n- You can set the cache TTL for a specific status code. For example, `404=10` caches responses with a 404 status code for 10 seconds.\n\n- You can set the cache TTL for a series of status codes, such as 4xx or 5xx. For example, `4xx=10` caches all responses that have a status code in the 4xx series for 10 seconds.\n\n- You can specify multiple status code TTLs, separated by commas (,)."
      }
    },
    {
      "name": "IncludeCookie",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "生成缓存键的时候，加入指定的cookie名称（cookie名称不区分大小写）及其值，多个值用空格分隔。cookie名称支持以下字符类型：\n- 符号：! # $ % & ' * + - . ^ _ ` | ~\n- 数字：0-9\n- 字母：小写英文字母a-z",
        "en": "The cookies to include in the cache key. Both the cookie names (case-insensitive) and their values are included. Separate multiple cookie names with spaces. The cookie names can contain the following characters:\n\n- Symbols: ! # $ % & ' \\* + - . ^ \\_ | \\~\n\n- Digits: 0-9\n\n- Letters: a-z (lowercase)"
      }
    },
    {
      "name": "IncludeHeader",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "生成缓存键的时候，加入指定的标头名称（标头名称不区分大小写）及其值，支持输入多个值，多个值之间用空格分隔。标头名称支持以下字符类型：\n- 符号：! # $ % & ' * + - . ^ _ ` | ~\n- 数字：0-9\n- 字母：小写英文字母a-z",
        "en": "The headers to include in the cache key. Both the header names (case-insensitive) and their values are included. Separate multiple header names with spaces. The header names can contain the following characters:\n\n- Symbols: ! # $ % & ' \\* + - . ^ \\_ | \\~\n\n- Digits: 0-9\n\n- Letters: a-z (lowercase)"
      }
    },
    {
      "name": "PostBodyCacheKey",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Post缓存对缓存键的处理模式，支持以下两种模式：\n- md5：先对 Body 内容取 MD5，然后将 MD5 值加入缓存键\n- ignore：在缓存键中忽略 Body 内容",
        "en": "Specifies how to process the request body when generating a cache key for POST requests. The following modes are supported:\n\n- `md5`: Calculates the MD5 hash of the request body and adds the hash value to the cache key.\n\n- `ignore`: Ignores the request body when the cache key is generated."
      }
    },
    {
      "name": "PostBodySizeLimit",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Post缓存对Body大小限制，值为数字，单位是KB，支持1～8KB大小的Body，值为空的时候，默认按8KB生效。",
        "en": "The size limit for the request body when using POST request caching, in KB. Supported values range from 1 to 8. If unspecified, the default is 8 KB."
      }
    },
    {
      "name": "PostCache",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Post缓存功能开关。",
        "en": "Specifies whether to enable POST request caching."
      }
    },
    {
      "name": "QueryString",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "生成缓存键的时候，需要保留或者删除的查询字符串，支持输入多个值，多个值之间用空格分隔。",
        "en": "The query strings to include in or exclude from the cache key. Separate multiple query strings with spaces."
      }
    },
    {
      "name": "QueryStringMode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "在生成缓存键的时候，对查询字符串的处理模式。取值范围：\n- ignore_all：全部忽略。\n- exclude_query_string：删除指定查询字符串。\n- reserve_all：默认值，全部保留。\n- include_query_string：保留指定查询字符串。",
        "en": "The mode for processing query strings when generating a cache key. Valid values:\n\n- `ignore_all`: Ignores all query strings.\n\n- `exclude_query_string`: Excludes specified query strings.\n\n- `reserve_all`: Includes all query strings (the default).\n\n- `include_query_string`: Includes only specified query strings."
      }
    },
    {
      "name": "Rule",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "规则内容，使用条件表达式来匹配用户请求。添加全局配置的时候不需要设置该参数。存在两种使用场景：\n- 匹配所有传入请求：值设置为true\n- 匹配指定请求：值设置为自定义表达式，例如：(http.host eq \\\"video.example.com\\\")",
        "en": "The content of the rule, which is a conditional expression used to match user requests. This parameter is not required for a global configuration.\n\n- To match all requests, set the value to `true`.\n\n- To match specific requests, set the value to a custom expression, such as `(http.host eq \"video.example.com\")`."
      }
    },
    {
      "name": "RuleEnable",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "规则开关。添加全局配置的时候不需要设置该参数。取值范围：\n- on：开启。\n- off：关闭。",
        "en": "Specifies whether to enable the rule. This parameter is not required for a global configuration. Valid values:\n\n- `on`: Enables the rule.\n\n- `off`: Disables the rule."
      }
    },
    {
      "name": "RuleName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "规则名称。添加全局配置的时候不需要设置该参数。",
        "en": "The rule name. This parameter is not required for a global configuration."
      }
    },
    {
      "name": "Sequence",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "规则执行顺序，数值越小越优先执行。",
        "en": "The execution order of the rule. A smaller number indicates a higher priority."
      }
    },
    {
      "name": "ServeStale",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "响应过期缓存。开启之后，在源服务器不可用的情况下，节点依然可以使用已缓存的过期文件响应用户请求。取值范围：\n- on：开启。\n- off：关闭。",
        "en": "Specifies whether to serve stale content. If enabled, an edge node can serve stale (expired) content when the origin server is unavailable. Valid values:\n\n- `on`: Enables serving stale content.\n\n- `off`: Disables serving stale content."
      }
    },
    {
      "name": "SiteId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "站点ID，可通过调用[ListSites](~~ListSites~~)接口获取。",
        "en": "The site ID. You can call the [ListSites](~~ListSites~~) operation to get this ID."
      }
    },
    {
      "name": "SiteVersion",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "站点配置的版本号。开启了配置版本管理的站点，可以通过该参数指定配置生效的站点版本，默认为版本0。",
        "en": "The site configuration version. For sites with version management enabled, this parameter specifies the site version to which the configuration applies."
      }
    },
    {
      "name": "SortQueryStringForCache",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询字符串排序，默认关闭。取值范围：\n- on：开启。\n- off：关闭。",
        "en": "Specifies whether to sort query strings. This feature is disabled by default. Valid values:\n\n- `on`: Enables sorting.\n\n- `off`: Disables sorting."
      }
    },
    {
      "name": "UserDeviceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "生成缓存键的时候，加入客户端设备类型。取值范围：\n- on：开启。\n- off：关闭。",
        "en": "Specifies whether to include the client device type in the cache key. Valid values:\n\n- `on`: Includes the client device type.\n\n- `off`: Does not include the client device type."
      }
    },
    {
      "name": "UserGeo",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "生成缓存键的时候，加入客户端地理位置。取值范围：\n- on：开启。\n- off：关闭。",
        "en": "Specifies whether to include the client's geographic location in the cache key. Valid values:\n\n- `on`: Includes the geographic location.\n\n- `off`: Does not include the geographic location."
      }
    },
    {
      "name": "UserLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "生成缓存键的时候，加入客户端语言类型。取值范围：\n- on：开启。\n- off：关闭。",
        "en": "Specifies whether to include the client's language in the cache key. Valid values:\n\n- `on`: Includes the language.\n\n- `off`: Does not include the language."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "添加站点缓存配置",
    "en": "Add Site Cache Configuration"
  },
  "example": {
    "unifiedCli": "aliyun esa create-cache-rule --site-id 340035003106221 --site-version 1 --rule-name rule_example --rule-enable on --rule http.host_eq_video.example.com --bypass-cache cache_all --browser-cache-mode follow_origin --browser-cache-ttl 300 --edge-cache-mode follow_origin --edge-cache-ttl 300 --edge-status-code-cache-ttl 5xx=0,404=10 --sort-query-string-for-cache on --query-string-mode reserve_all --query-string example --include-header headername1_headername2 --include-cookie cookiename1_cookiename2 --cache-reserve-eligibility bypass_cache_reserve --check-presence-header headername1_headername2 --check-presence-cookie cookiename1_cookiename2 --user-device-type on --user-geo on --user-language on --serve-stale on --additional-cacheable-ports 8880,2052,2086 --cache-deception-armor on --sequence 1 --post-cache on --post-body-size-limit 1 --post-body-cache-key ignore --region cn-hangzhou",
    "legacyCli": "aliyun esa CreateCacheRule --region cn-hangzhou --EdgeCacheTtl 300 --SiteId 340035003106221 --EdgeCacheMode follow_origin --EdgeStatusCodeCacheTtl '5xx=0,404=10' --BrowserCacheMode follow_origin --UserGeo on --BrowserCacheTtl 300 --IncludeCookie cookiename1_cookiename2 --RuleName rule_example --PostCache on --RuleEnable on --PostBodySizeLimit 1 --PostBodyCacheKey ignore --QueryStringMode reserve_all --QueryString example --Sequence 1 --BypassCache cache_all --CheckPresenceHeader headername1_headername2 --SortQueryStringForCache on --CheckPresenceCookie cookiename1_cookiename2 --UserDeviceType on --CacheReserveEligibility bypass_cache_reserve --AdditionalCacheablePorts '8880,2052,2086' --Rule 'http.host_eq_video.example.com' --UserLanguage on --ServeStale on --CacheDeceptionArmor on --SiteVersion 1 --IncludeHeader headername1_headername2"
  }
}