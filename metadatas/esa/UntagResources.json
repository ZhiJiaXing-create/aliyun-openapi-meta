{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "删除所有标签。取值：\n- **true**：是。\n- **false**：否。\n\n默认取值为**false**。",
        "en": "Specifies whether to remove all tags. Valid values:\n\n*   **true**\n*   **false**\n\nDefault value: **false**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID，取值范围如下：\n\n- 中国站：cn-hangzhou\n- 国际站：ap-southeast-1",
        "en": "The ID of the region where the resources reside. Set the value to **cn-hangzhou**."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID。请输入站点ID或者DNS记录ID。",
        "en": "The ID of resource N. Valid values of N: **1** to **50**."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。取值范围如下：\n\n- 站点：site\n- DNS记录：record",
        "en": "The resource type."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签键。N的取值范围：**1**~**20**。",
        "en": "The key of tag N to remove from the resource. Valid values of N: **1** to **20**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除资源标签",
    "en": "UntagResources"
  },
  "example": {
    "unifiedCli": "aliyun esa untag-resources --biz-region-id cn-hangzhou --resource-id example.com --tag-key env --resource-type site --all false --region cn-hangzhou",
    "legacyCli": "aliyun esa UntagResources --region cn-hangzhou --All false --ResourceId.1 'example.com' --TagKey.1 env --ResourceType site --RegionId 'cn-hangzhou'"
  }
}