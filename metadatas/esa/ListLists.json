{
  "name": "ListLists",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页面号码，用于指定分页查询的页码。",
        "en": "The page number for paging."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页面大小，用于指定分页查询的每页数量。",
        "en": "The number of entries per page for paging."
      }
    },
    {
      "name": "QueryArgs",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询参数，以JSON格式传递，包含各种筛选条件。",
        "en": "The query parameters passed in JSON format, which contain various filter conditions."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列举自定义列表",
    "en": "List custom lists"
  },
  "example": {
    "unifiedCli": "aliyun esa list-lists --query-args ListLists --page-number 1 --page-size 20 --region cn-hangzhou",
    "legacyCli": "aliyun esa ListLists --region cn-hangzhou --QueryArgs ListLists --PageSize 20 --PageNumber 1"
  }
}