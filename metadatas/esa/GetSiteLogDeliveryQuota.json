{
  "name": "GetSiteLogDeliveryQuota",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BusinessType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实时日志类型，取值：\n- **dcdn_log_access_l1（默认值）**：访问日志。\n- **dcdn_log_er**：边缘函数日志。\n- **dcdn_log_waf**：安全防护日志。\n- **dcdn_log_ipa**：四层加速日志。",
        "en": "The log category. Valid values:\n\n1. dcdn\\_log\\_access\\_l1 (default): access logs.\n\n2. dcdn\\_log\\_er: Edge Routine logs.\n\n3. dcdn\\_log\\_waf: firewall logs.\n\n4. dcdn\\_log\\_ipa: TCP/UDP proxy logs."
      }
    },
    {
      "name": "SiteId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "站点ID，可通过调用[ListSites](~~2850189~~)接口获取。",
        "en": "The website ID, which can be obtained by calling the [ListSites](~~2850189~~) operation."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取站点日志投递配额",
    "en": "GetSiteLogDeliveryQuota"
  },
  "descriptions": {
    "zh": "使用此接口可以查询指定阿里云用户下，特定站点与业务类型的实时日志投递服务剩余配额数量。这对于监控与管理您的日志投递容量至关重要，确保日志能够顺利推送至目标存储端，避免因配额不足导致的数据丢失或延迟。\n\n**参数注意事项：**\n- `BusinessType` 是必填项，需指定业务类型以获取相应的配额信息。\n- `SiteId` 需要是有效的站点ID，且格式应为整数，确保与您在阿里云上配置的站点相对应。\n\n**响应内容：**\n- 接口成功响应将返回投递配额数量（`FreeQuota`），以及请求标识（`RequestId`），站点ID（`SiteId`）和业务类型（`BusinessType`）作为确认和记录使用。",
    "en": "Use this operation to query the remaining quota for delivering a specific category of real-time logs in a website within an Alibaba Cloud account. This is essential for monitoring and managing your log delivery capacity to ensure that logs can be delivered to the destination and prevent data loss or latency caused by insufficient quota.\n\n**Take note of the following parameters:**\n\n- \\`\\`\n\n- `BusinessType` is required. You must specify a log category to obtain the corresponding quota information.\n\n- `SiteId` specifies the ID of a website, which must be a valid integer that corresponds to a website that you configured on Alibaba Cloud.\n\n**Response:**\n\n- If a request is successful, the system returns the remaining log delivery quota (`FreeQuota`), request ID (`RequestId`), website ID (`SiteId`), and log category (`BusinessType`). You can confirm and record the returned data."
  },
  "example": {
    "unifiedCli": "aliyun esa get-site-log-delivery-quota --business-type dcdn_log_access_l1 --site-id 1234560000 --region cn-hangzhou",
    "legacyCli": "aliyun esa GetSiteLogDeliveryQuota --region cn-hangzhou --SiteId 1234560000 --BusinessType dcdn_log_access_l1"
  }
}