{
  "name": "UpdateApplicationMonitor",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Address",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要探测的URL或IP地址。",
        "en": "The URL or IP address to be probed."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** value as the **ClientToken** value. The **RequestId** value of each API request is different."
      }
    },
    {
      "name": "DetectEnable",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启自动诊断功能。取值：\n\n- **true**：是。\n\n- **false**（默认值）：否。",
        "en": "Specifies whether to enable the automatic diagnostics feature. Valid values:\n\n- **true**: Enabled.\n\n- **false** (default): Disabled."
      }
    },
    {
      "name": "DetectThreshold",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "设置自动诊断触发阈值，当源站可用率低于设置的触发阈值，则自动触发诊断。\n\n取值范围：**0**~**100**。",
        "en": "The threshold that triggers automatic diagnostics. When the origin availability rate falls below this threshold, automatic diagnostics is triggered.\n\nValid values: **0** to **100**."
      }
    },
    {
      "name": "DetectTimes",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "触发自动诊断需要达到触发阈值的次数。\n\n取值范围：**1**~**20**。",
        "en": "The number of consecutive times that the availability rate must fall below the threshold before automatic diagnostics is triggered.\n\nValid values: **1** to **20**."
      }
    },
    {
      "name": "ListenerId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要修改源站探测的监听实例ID。",
        "en": "The instance ID of the listener associated with the origin probing task that you want to modify."
      }
    },
    {
      "name": "OptionsJson",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "源站探测任务对应监听协议类型的高级扩展选项。不同源站探测任务的监听协议类型对应不同的扩展选项。",
        "en": "The advanced extension options for the listener protocol type of the origin probing task. Different listener protocol types correspond to different extension options."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "全球加速实例所属的地域ID，仅取值：**cn-hangzhou**。",
        "en": "The region ID of the Alibaba Cloud Global Accelerator (GA) instance. Set the value to **cn-hangzhou**."
      }
    },
    {
      "name": "SilenceTime",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "自动诊断触发的沉默时间。表示自动诊断触发后，未恢复到正常，间隔多久触发一次自动诊断。\n\n可用率连续多次（即设置的**DetectTimes**）低于自动诊断触发阈值时，触发自动诊断，如果可用率在自动诊断触发的沉默时间内持续低于自动诊断触发阈值，在自动诊断触发的沉默时间内不会重复出发自动诊断；如果可用率在自动诊断触发的沉默时间后仍未恢复正常，则再次触发自动诊断。\n\n单位：秒。取值范围：**300**~**86400**。",
        "en": "The silence period for automatic diagnostics. This specifies the interval between automatic diagnostics triggers when the availability rate does not recover to normal after diagnostics is triggered.\n\nWhen the availability rate falls below the automatic diagnostics threshold for consecutive times (as specified by **DetectTimes**), automatic diagnostics is triggered. If the availability rate remains below the threshold during the silence period, automatic diagnostics is not triggered again within the silence period. If the availability rate has not recovered after the silence period expires, automatic diagnostics is triggered again.\n\nUnit: seconds. Valid values: **300** to **86400**."
      }
    },
    {
      "name": "TaskId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要修改源站探测的任务ID。",
        "en": "The ID of the origin probing task that you want to modify."
      }
    },
    {
      "name": "TaskName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "源站探测的任务名称。\n\n名称长度为 1~128 个字符，以大小写字母或中文开头，可包含数字、半角句号（.）下划线（_）和短划线（-）。",
        "en": "The name of the origin probing task.\n\nThe name must be 1 to 128 characters in length and must start with a letter or a Chinese character. It can contain digits, periods (.), underscores (_), and hyphens (-)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改源站探测任务配置",
    "en": "Modify origin probing task configuration"
  },
  "descriptions": {
    "zh": "**UpdateApplicationMonitor**接口属于异步接口，即系统会先返回一个请求ID，但源站探测任务并未修改完成，系统后台的修改任务仍在进行。您可以调用[DescribeApplicationMonitor](~~408463~~)或[ListApplicationMonitor](~~408462~~)查询源站探测任务的配置是否完成修改：\n\n- 当修改的参数值未发生变更，表示源站探测任务正在修改中，在该情况下，您只能执行查询操作，不能执行其他操作。\n\n- 当修改的参数值已发生变更，表示源站探测任务修改完成。",
    "en": "**UpdateApplicationMonitor** is an asynchronous operation. After you call this operation, the system returns a request ID, but the origin probing task is not yet modified. The modification task continues to run in the background. You can call [DescribeApplicationMonitor](~~408463~~) or [ListApplicationMonitor](~~408462~~) to check whether the origin probing task configuration has been modified:\n\n- If the modified parameter values have not changed, the origin probing task is still being modified. In this case, you can only perform query operations and cannot perform other operations.\n\n- If the modified parameter values have changed, the origin probing task has been modified."
  },
  "example": {
    "unifiedCli": "aliyun ga update-application-monitor --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-426655440000 --task-id sm-bp1fpdjfju9k8yr1y0000 --listener-id lsr-bp1bpn0kn908w4nbw0000 --task-name task1 --address https://www.aliyun.com --options-json http_method_:_get_,_header_:_key:asd_,_acceptable_response_code_:_500_,_cert_verify_:_true --detect-enable false --detect-threshold 0 --detect-times 1 --silence-time 300 --region cn-hangzhou",
    "legacyCli": "aliyun ga UpdateApplicationMonitor --region cn-hangzhou --SilenceTime 300 --DetectThreshold 0 --TaskId 'sm-bp1fpdjfju9k8yr1y0000' --Address 'https://www.aliyun.com' --DetectEnable false --TaskName task1 --OptionsJson 'http_method_:_get_,_header_:_key:asd_,_acceptable_response_code_:_500_,_cert_verify_:_true' --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --DetectTimes 1 --ListenerId 'lsr-bp1bpn0kn908w4nbw0000'"
  }
}