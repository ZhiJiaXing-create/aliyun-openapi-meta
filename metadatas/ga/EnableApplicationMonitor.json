{
  "name": "EnableApplicationMonitor",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length.\n\n>  If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "全球加速实例所属的地域，仅取值：**cn-hangzhou**。",
        "en": "The region ID of the Global Accelerator (GA) instance. Set the value to **cn-hangzhou**."
      }
    },
    {
      "name": "TaskId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要开启的源站探测任务ID。",
        "en": "The ID of the origin probing task that you want to enable."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "开启源站探测",
    "en": "EnableApplicationMonitor"
  },
  "example": {
    "unifiedCli": "aliyun ga enable-application-monitor --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-426655440000 --task-id sm-bp1fpdjfju9k8yr1y0000 --region cn-hangzhou",
    "legacyCli": "aliyun ga EnableApplicationMonitor --region cn-hangzhou --TaskId 'sm-bp1fpdjfju9k8yr1y0000' --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}