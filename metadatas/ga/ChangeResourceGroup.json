{
  "name": "ChangeResourceGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。**ClientToken**只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of a request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The **ClientToken** value can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request."
      }
    },
    {
      "name": "NewResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要替换的新的资源组ID。",
        "en": "The ID of the new resource group."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速实例所属的地域ID，仅取值：**cn-hangzhou**。",
        "en": "The region ID of the Alibaba Cloud Global Accelerator (GA) instance. Set the value to **ap-southeast-1**."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待修改资源组的全球加速资源ID。\n- 当**ResourceType**取值为**accelerator**时，取值为标准型全球加速实例ID。\n- 当**ResourceType**取值为**basicaccelerator**时，取值为基础型全球加速实例ID。\n-  当**ResourceType**取值为**bandwidthpackage**时，取值为带宽包ID。\n- 当**ResourceType**取值为**acl**时，取值为访问控制策略组ID。",
        "en": "The instance ID of the Global Accelerator resource for which you want to modify the resource group.\n- If **ResourceType** is set to **accelerator**, set this parameter to the instance ID of a standard Global Accelerator instance.\n- If **ResourceType** is set to **basicaccelerator**, set this parameter to the instance ID of a basic Global Accelerator instance.\n- If **ResourceType** is set to **bandwidthpackage**, set this parameter to the ID of a bandwidth plan.\n- If **ResourceType** is set to **acl**, set this parameter to the ID of an access control policy group."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待修改资源组的全球加速资源类型。取值：\n- **accelerator**：标准型全球加速实例。\n-  **basicaccelerator**：基础型全球加速实例。\n- **bandwidthpackage**：带宽包。\n- **acl**：访问控制策略组。",
        "en": "The type of the Global Accelerator resource for which you want to modify the resource group. Valid values:\n- **accelerator**: a standard Alibaba Cloud Global Accelerator (GA) instance.\n- **basicaccelerator**: a basic Alibaba Cloud Global Accelerator (GA) instance.\n- **bandwidthpackage**: a bandwidth plan.\n- **acl**: an access control policy group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改全球加速资源所属的资源组",
    "en": "Modify the resource group of a global accelerator resource"
  },
  "descriptions": {
    "zh": "**ChangeResourceGroup**接口不支持在同一个全球加速实例内并发修改全球加速资源所属的资源组。",
    "en": "The **ChangeResourceGroup** operation does not support concurrent modifications to the resource group of Global Accelerator resources within the same Alibaba Cloud Global Accelerator (GA) instance."
  },
  "example": {
    "unifiedCli": "aliyun ga change-resource-group --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-426655440000 --resource-id ga-bp149u6o36qt1as9b0000 --new-resource-group-id rg-aekzrnd67gq0000 --resource-type accelerator --region cn-hangzhou",
    "legacyCli": "aliyun ga ChangeResourceGroup --region cn-hangzhou --NewResourceGroupId 'rg-aekzrnd67gq0000' --ResourceId 'ga-bp149u6o36qt1as9b0000' --ResourceType accelerator --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}