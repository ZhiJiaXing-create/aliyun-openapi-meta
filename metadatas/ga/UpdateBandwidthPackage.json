{
  "name": "UpdateBandwidthPackage",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPay",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否自动付费。取值：\n\n - **false**（默认值）：不开启自动付费。生成订单后需要到<props=\"china\">[订单中心](https://usercenter2.aliyun.com/order/list)</props><props=\"intl\">[订单中心](https://usercenter2-intl.aliyun.com/order/list)</props>完成支付。\n- **true**：开启自动付费，自动支付订单。\n\n> 本参数仅对升配订单生效。",
        "en": "Specifies whether to enable automatic payment. Valid values:\n\n- **false** (default): disables automatic payment. After an order is generated, you must go to the <props=\"china\">[Order Hub](https://usercenter2.aliyun.com/order/list)</props><props=\"intl\">[Order Hub](https://usercenter2-intl.aliyun.com/order/list)</props> to complete the payment.\n\n- **true**: enables automatic payment. The system automatically pays the bill.\n\n> This parameter is required only for upgrade orders."
      }
    },
    {
      "name": "AutoUseCoupon",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否使用代金券。取值：\n\n- **true**：使用。\n\n- **false**（默认值）：不使用。",
        "en": "Specifies whether to use coupons. Valid values:\n\n- **true**: yes.\n\n- **false** (default): no."
      }
    },
    {
      "name": "Bandwidth",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "带宽包的带宽值。单位：Mbps。\n\n取值范围：**2**~**2000**。",
        "en": "The bandwidth of the bandwidth plan. Unit: Mbit/s.\n\nValid values: **2** to **2000**."
      }
    },
    {
      "name": "BandwidthPackageId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要修改的带宽包的ID。",
        "en": "The ID of the bandwidth plan that you want to modify."
      }
    },
    {
      "name": "BandwidthType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "带宽类型。取值：\n\n- **Basic**：标准加速带宽。\n\n- **Enhanced**：增强加速带宽。\n\n- **Advanced**：精品加速带宽。\n\n> 目前，仅支持**Basic**与**Enhanced**间变配，不支持**Advanced**与其他类型间进行变配。",
        "en": "The bandwidth type. Valid values:\n\n- **Basic**: standard bandwidth.\n\n- **Enhanced**: enhanced bandwidth.\n\n- **Advanced**: premium bandwidth.\n\n> You can upgrade a Basic bandwidth plan to Enhanced, or downgrade an **Enhanced** bandwidth plan to **Basic**. You cannot change the bandwidth type of an **Advanced** bandwidth plan."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "带宽包的描述信息。\n\n描述长度最大为256个字符。",
        "en": "The description of the bandwidth plan.\n\nThe description can be up to 256 characters in length."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "带宽包的名称。\n名称长度为1~128个英文或中文字符，以大小字母或中文开头，可包含数字、半角句号（.）、下划线（_）和短划线（-）。",
        "en": "The name of the bandwidth plan.\nThe name must be 1 to 128 characters in length, start with a letter or a Chinese character, and can contain letters, digits, periods (.), underscores (\\_), and hyphens (-)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速实例所在的地域ID，仅取值**cn-hangzhou**。",
        "en": "The ID of the region where the Global Accelerator instance is deployed. Set the value to **cn-hangzhou**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改带宽包的配置",
    "en": "UpdateBandwidthPackage"
  },
  "descriptions": {
    "zh": "在调用本接口时，请注意以下事项：\n- 不涉及带宽值修改时，**UpdateBandwidthPackage**接口属于同步接口，修改完成后即时生效。\n- 涉及带宽值修改，且该带宽包未绑定全球加速实例时，**UpdateBandwidthPackage**接口属于异步接口，即系统会先返回一个请求ID，但带宽包并未修改完成。系统后台的修改任务仍在进行。您可以调用[DescribeBandwidthPackage](~~153241~~)查询带宽包配置是否完成修改：\n    - 当修改的参数值未发生变更，表示带宽包正在修改中，在该情况下，您只能执行查询操作，不能执行其他操作。\n    - 当修改的参数值已发生变更，表示修改完成。\n- 涉及带宽值修改，且该带宽包已绑定全球加速实例时，**UpdateBandwidthPackage**接口属于异步接口，即系统会先返回一个请求ID，但带宽包并未修改完成。系统后台的修改任务仍在进行。您可以调用[DescribeAccelerator](~~153235~~)，通过查询全球加速实例的状态，了解带宽包是否修改完成：\n    - 当全球加速实例处于**configuring**状态时，表示绑定全球加速实例的带宽包正在修改中，在该状态下，您只能执行查询操作，不能执行其他操作。\n    - 当全球加速实例处于**active**状态时，表示绑定全球加速实例的带宽包修改完成。\n- **UpdateBandwidthPackage**接口不支持并发修改同一个带宽包的配置。",
    "en": "Before you call this operation, note the following:\n\n- If you do not change the bandwidth value, **UpdateBandwidthPackage** is a synchronous operation. The modification takes effect immediately.\n\n- If you change the bandwidth value of a bandwidth plan that is not associated with a Global Accelerator (GA) instance, **UpdateBandwidthPackage** is an asynchronous operation. The system returns a request ID, but the modification is not complete. The system performs the task in the background. You can call the [DescribeBandwidthPackage](~~153241~~) operation to query whether the configuration of the bandwidth plan is modified:\n\n  - If the values of the parameters that you want to modify remain unchanged, the bandwidth plan is being modified. In this case, you can only perform query operations.\n\n  - If the values of the parameters that you want to modify have changed, the modification is complete.\n\n- If you change the bandwidth value of a bandwidth plan that is associated with a GA instance, **UpdateBandwidthPackage** is an asynchronous operation. The system returns a request ID, but the modification is not complete. The system performs the task in the background. You can call the [DescribeAccelerator](~~153235~~) operation and query the status of the GA instance to check whether the bandwidth plan is modified:\n\n  - If the GA instance is in the **configuring** state, the bandwidth plan that is associated with the GA instance is being modified. In this case, you can only perform query operations.\n\n  - If the GA instance is in the **active** state, the bandwidth plan that is associated with the GA instance has been modified.\n\n- You cannot repeatedly call the **UpdateBandwidthPackage** operation to modify the configuration of the same bandwidth plan."
  },
  "example": {
    "unifiedCli": "aliyun ga update-bandwidth-package --biz-region-id cn-hangzhou --bandwidth-package-id gbwp-bp1sgzldyj6b4q7cx0000 --name testName --description testDescription --bandwidth 2 --bandwidth-type Basic --auto-pay false --auto-use-coupon false --region cn-hangzhou",
    "legacyCli": "aliyun ga UpdateBandwidthPackage --region cn-hangzhou --BandwidthType Basic --Description testDescription --AutoUseCoupon false --Bandwidth 2 --BandwidthPackageId 'gbwp-bp1sgzldyj6b4q7cx0000' --RegionId 'cn-hangzhou' --AutoPay false --Name testName"
  }
}