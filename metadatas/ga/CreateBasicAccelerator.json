{
  "name": "CreateBasicAccelerator",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPay",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否自动付费，取值：\n\n- **false**（默认值）：不开启自动付费，生成订单后需要到订单中心完成支付。\n\n- **true**：开启自动付费，自动支付订单。",
        "en": "Specifies whether to enable automatic payment. Valid values:\n\n- **false** (default): disables automatic payment. After an order is generated, go to the Order Center to complete the payment.\n\n- **true**: enables automatic payment. The order is automatically paid."
      }
    },
    {
      "name": "AutoRenew",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启自动续费。\n\n- **true**：是。\n\n- **false**（默认值）：否。",
        "en": "Specifies whether to enable auto-renewal.\n\n- **true**: enables auto-renewal.\n\n- **false** (default): disables auto-renewal."
      }
    },
    {
      "name": "AutoRenewDuration",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "自动续费时长。单位：月。\n\n取值范围：**1**~**12**。默认值：**1**。\n\n> 仅**AutoRenew**的为**true**时，该项才生效。",
        "en": "The auto-renewal duration. Unit: months.\n\nValid values: **1** to **12**. Default value: **1**.\n\n> This parameter takes effect only when **AutoRenew** is set to **true**."
      }
    },
    {
      "name": "AutoUseCoupon",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否使用优惠券自动支付账单。取值：\n\n- **true**：使用。\n\n- **false**（默认值）：不使用。\n\n> 仅**AutoPay**为**true**时，该项才生效。",
        "en": "Specifies whether to use coupons for automatic payment of the bill. Valid values:\n\n- **true**: uses coupons.\n\n- **false** (default): does not use coupons.\n\n> This parameter takes effect only when **AutoPay** is set to **true**."
      }
    },
    {
      "name": "BandwidthBillingType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "带宽计费方式。取值：\n- **BandwidthPackage**：按带宽包计费。\n- **CDT**：按流量计费并由云数据传输（Cloud DataTransfer，简称CDT）统一结算。\n- **CDT95**：按95计费并由CDT统一结算。该带宽计费方式仅白名单用户可用。",
        "en": "The bandwidth billing method. Valid values:\n- **BandwidthPackage**: billed by bandwidth plan.\n- **CDT**: billed by traffic and settled through unified settlement by Cloud Data Transfer (CDT).\n- **CDT95**: billed by the 95th percentile bandwidth and settled through unified settlement by CDT. This bandwidth billing method is available only to users in the whitelist."
      }
    },
    {
      "name": "ChargeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "付费类型。取值：\n- **PREPAY（默认值）**：代表预付费，即包年包月方式付费。\n- **POSTPAY**：代表后付费，即按使用时长方式付费。",
        "en": "The billing method. Valid values:\n- **PREPAY (default)**: subscription.\n- **POSTPAY**: pay-as-you-go."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n- **true**：发送检查请求，不会创建资源。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作。",
        "en": "Specifies whether to perform a dry run. Valid values:\n- **true**: performs a dry run without creating the resource. The system checks the required parameters, request syntax, and business limitations. If the check fails, the corresponding error is returned. If the check succeeds, the `DryRunOperation` error code is returned.\n- **false** (default): performs a dry run and sends the request. If the check succeeds, an HTTP 2xx status code is returned and the operation is performed."
      }
    },
    {
      "name": "Duration",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "购买时长。\n\n- 当**PricingCycle**取值**Month**时，**Duration**取值范围为**1**~**9**。\n\n- 当**PricingCycle**取值**Year**时，**Duration**取值范围为**1**~**3**。",
        "en": "The subscription duration.\n\n- If **PricingCycle** is set to **Month**, valid values of **Duration** are **1** to **9**.\n\n- If **PricingCycle** is set to **Year**, valid values of **Duration** are **1** to **3**."
      }
    },
    {
      "name": "PricingCycle",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "计费周期。取值：\n\n- **Month**：按月计费。\n\n- **Year**：按年计费。",
        "en": "The billing cycle. Valid values:\n\n- **Month**: billed on a monthly basis.\n\n- **Year**: billed on a yearly basis."
      }
    },
    {
      "name": "PromotionOptionNo",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "优惠券号码。\n> 仅国际站涉及该参数。",
        "en": "The coupon number.\n> This parameter is applicable only to the China site (aliyun.com)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "基础型全球加速实例所属的地域ID，仅取值：**cn-hangzhou**。",
        "en": "The region ID of the basic Alibaba Cloud Global Accelerator (GA) instance. Set the value to **cn-hangzhou**."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "基础型全球加速实例所属资源组ID。",
        "en": "The ID of the resource group to which the basic Alibaba Cloud Global Accelerator (GA) instance belongs."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "基础型全球加速实例的标签。",
        "en": "The labels of the basic Alibaba Cloud Global Accelerator (GA) instance."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "基础型全球加速实例的标签键。一旦输入该值，则不允许为空字符串。\n\n最多支持64个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n\n您最多可输入20个标签键。",
            "en": "The label key of the basic Alibaba Cloud Global Accelerator (GA) instance. If you specify this parameter, the value cannot be an empty string.\n\nThe label key can be up to 64 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.\n\nYou can specify up to 20 label keys."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "基础型全球加速实例的标签值。一旦输入该值，不可以为空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n\n您最多可输入20个标签值。",
            "en": "The label value of the basic Alibaba Cloud Global Accelerator (GA) instance. If you specify this parameter, the value cannot be an empty string.\n\nThe label value can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.\n\nYou can specify up to 20 label values."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建基础型全球加速实例",
    "en": "Create a basic global accelerator instance"
  },
  "descriptions": {
    "zh": "**CreateBasicAccelerator**接口属于异步接口，即系统会先返回一个基础型全球加速实例ID，但基础型全球加速实例并未创建完成，系统后台的创建任务仍在进行。您可以调用[GetBasicAccelerator](~~353188~~)或[ListBasicAccelerators](~~353189~~)查询基础型全球加速实例的状态：\n\n- 当基础型全球加速实例处于**init**状态时，表示基础型全球加速实例正在创建中，在该状态下，您只能执行查询操作，不能执行其他操作。\n- 当基础型全球加速实例处于**active**状态时，表示基础型全球加速实例创建完成。",
    "en": "**CreateBasicAccelerator** is an asynchronous operation. After you invoke this operation, the system returns a basic Alibaba Cloud Global Accelerator (GA) instance ID, but the instance is not yet created. The creation node continues to execute in the background. You can invoke [GetBasicAccelerator](~~353188~~) or [ListBasicAccelerators](~~353189~~) to query the status of the basic GA instance:\n\n- If the basic GA instance is in the **init** state, the instance is being created. In this state, you can only perform query operations.\n- If the basic GA instance is in the **active** state, the instance is created."
  },
  "example": {
    "unifiedCli": "aliyun ga create-basic-accelerator --biz-region-id cn-hangzhou --client-token 123e45670000 --duration 1 --pricing-cycle Month --bandwidth-billing-type CDT --auto-pay false --auto-use-coupon false --promotion-option-no 500032980140000 --auto-renew false --auto-renew-duration 1 --resource-group-id rg-acfmxshhcsn0000 --charge-type PREPAY --dry-run false --tag Value=tag-value Key=tag-key --region cn-hangzhou",
    "legacyCli": "aliyun ga CreateBasicAccelerator --region cn-hangzhou --Tag.1.Key 'tag-key' --ResourceGroupId 'rg-acfmxshhcsn0000' --PromotionOptionNo 500032980140000 --PricingCycle Month --AutoRenew false --DryRun false --Duration 1 --AutoPay false --AutoRenewDuration 1 --BandwidthBillingType CDT --AutoUseCoupon false --ChargeType PREPAY --Tag.1.Value 'tag-value' --RegionId 'cn-hangzhou' --ClientToken 123e45670000"
  }
}