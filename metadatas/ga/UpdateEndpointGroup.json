{
  "name": "UpdateEndpointGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "A client-generated token to ensure the idempotence of the request.\n\nThe token must be unique across requests and can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** is unique for each API request."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "终端节点组描述信息。\n\n描述长度最大200字符，不能以`http://`和`https://`开头。",
        "en": "The description of the endpoint group.\n\nThe description can be up to 200 characters long and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "EndpointConfigurations",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "终端节点配置信息。",
        "en": "The configurations of the endpoints."
      }
    },
    {
      "name": "EndpointGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "终端节点组ID。",
        "en": "The ID of the endpoint group."
      }
    },
    {
      "name": "EndpointGroupRegion",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "终端节点组所属的地域ID。",
        "en": "The ID of the region where the endpoint group is deployed."
      }
    },
    {
      "name": "EndpointIpVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "后端IP地址协议。取值：\n● IPv4（默认值）：GA仅使用IPv4地址与终端节点服务通信。\n● IPv6：GA仅使用IPv6地址与终端节点服务通信。\n● ProtocolAffinity：GA与终端节点服务通信的IP协议与客户端请求使用的IP协议相同。",
        "en": "Specifies the IP protocol that GA uses to communicate with endpoints. Valid values: ● **IPv4** (default): Use IPv4. ● **IPv6**: Use IPv6. ● **ProtocolAffinity**: Use the same IP protocol as the client request."
      }
    },
    {
      "name": "EndpointProtocolVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "后端服务协议版本。取值：\n\n- **HTTP1.1**\n\n- **HTTP2**\n\n> 仅当EndpointRequestProtocol（后端服务协议）值为HTTPS时，支持配置终端节点后端服务协议版本。",
        "en": "The version of the backend service protocol. Valid values:\n\n- **HTTP1.1**\n\n- **HTTP2**\n\n> You can configure this parameter only when `EndpointRequestProtocol` is set to HTTPS."
      }
    },
    {
      "name": "EndpointRequestProtocol",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "后端服务协议。取值：\n\n- **HTTP**\n- **HTTPS**\n\n> - 仅为HTTP或HTTPS协议的监听实例创建终端节点组时才支持配置该项。\n> - 对于HTTP协议的监听实例，后端服务协议支持且仅支持HTTP。",
        "en": "The backend service protocol. Valid values:\n\n- **HTTP**\n\n- **HTTPS**\n\n> * You can configure this parameter only for endpoint groups of HTTP or HTTPS listeners.\n>\n> * For an HTTP listener, the backend service protocol must be HTTP."
      }
    },
    {
      "name": "HealthCheckEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启健康检查。取值：\n- **true**：开启健康检查。\n- **false**（默认值）：关闭健康检查。",
        "en": "Specifies whether to enable health checks. Valid values:\n\n- **true**: Enables health checks.\n\n- **false** (default): Disables health checks."
      }
    },
    {
      "name": "HealthCheckHost",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "健康域名检查",
        "en": "The domain name for the health check."
      }
    },
    {
      "name": "HealthCheckIntervalSeconds",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查的时间间隔。单位：秒。取值范围：**1**-**50**。",
        "en": "The interval between health checks, in seconds. Valid values: **1** to **50**."
      }
    },
    {
      "name": "HealthCheckPath",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "健康检查路径。",
        "en": "The path for health checks."
      }
    },
    {
      "name": "HealthCheckPort",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查的端口。取值范围：**1**~**65535**。",
        "en": "The port used for health checks. Valid values: **1** to **65535**."
      }
    },
    {
      "name": "HealthCheckProtocol",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "健康检查的协议。取值：\n\n- **tcp**或**TCP**：TCP协议。\n\n- **http**或**HTTP**：HTTP协议。\n\n- **https**或**HTTPS**：HTTPS协议。",
        "en": "The protocol for health checks. Valid values:\n\n- **tcp** or **TCP**\n\n- **http** or **HTTP**\n\n- **https** or **HTTPS**"
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "终端节点组的名称。\n\n名称长度为1~128个字符，以大小写字母或中文开头，可包含数字、半角句号（.）下划线（_）和短划线（-）。",
        "en": "The name of the endpoint group.\n\nThe name must be 1 to 128 characters long, start with a letter or a Chinese character, and can contain digits, periods (.), underscores (\\_), and hyphens (-)."
      }
    },
    {
      "name": "PortOverrides",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "端口映射关系。",
        "en": "The port override settings."
      },
      "sub_parameters": [
        {
          "name": "EndpointPort",
          "position": "Query",
          "type": "Integer",
          "required": false,
          "description": {
            "zh": "配置端口映射中的终端节点端口。",
            "en": "The endpoint port in the port override settings."
          }
        },
        {
          "name": "ListenerPort",
          "position": "Query",
          "type": "Integer",
          "required": false,
          "description": {
            "zh": "配置端口映射中的监听端口。\n\n> - 对于TCP监听：虚拟终端节点组不支持配置端口映射。如果监听下已存在虚拟终端节点组，则默认终端节点组无法配置端口映射。如果默认终端节点组已配置端口映射，则无法添加虚拟终端节点组。\n>- 配置端口映射后，后续监听变更时有以下限制：除HTTP和HTTPS之间的协议切换以外，不支持修改监听协议。\n>- 监听端口：您修改的监听端口范围必须包含当前所配置端口映射的全量监听端口。例如，当监听端口范围为80-82，并配置端口映射至终端节点端口100-102时，后续修改监听端口范围时，不支持修改为80-81。",
            "en": "The listener port in the port override settings.\n\n> - For TCP listeners, virtual endpoint groups do not support port overrides. If a listener is already associated with a virtual endpoint group, you cannot configure port overrides for the default endpoint group. If the default endpoint group has port overrides configured, you cannot add a virtual endpoint group.\n>\n> - After you configure port overrides, you can change the listener protocol only between HTTP and HTTPS.\n>\n> - The updated listener port range must include all listener ports in the configured port overrides. For example, if the listener port range is 80-82 and port overrides are configured to map the ports to endpoint ports 100-102, you cannot update the listener port range to 80-81."
          }
        }
      ]
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速实例所在的地域ID，仅取值**cn-hangzhou**。",
        "en": "The ID of the region where the GA instance is deployed. Set the value to **cn-hangzhou**."
      }
    },
    {
      "name": "ThresholdCount",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "针对健康检查状态变化所需要的连续健康检查次数，即从成功到失败的连续健康检查失败次数或从失败到成功的连续健康检查成功次数。\n\n取值范围：**2**~**10**。",
        "en": "The number of consecutive successful or failed health checks required to change an endpoint's health status.\n\nValid values: **2** to **10**."
      }
    },
    {
      "name": "TrafficPercentage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "监听有多个终端节点组时的权重。",
        "en": "The weight of the endpoint group when the listener is associated with multiple endpoint groups."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改终端节点组的业务配置",
    "en": "UpdateEndpointGroup"
  },
  "descriptions": {
    "zh": "- **UpdateEndpointGroup**接口属于异步接口，即系统会先返回一个请求ID，但终端节点组配置信息并未修改完成，系统后台的修改任务仍在进行。您可以调用[DescribeEndpointGroup](~~153260~~)查询终端节点组的状态：\n    - 当终端节点组处于**updating**状态时，表示终端节点组配置信息正在修改中，在该状态下，您只能执行查询操作，不能执行其他操作。\n    - 当终端节点组处于**active**状态时，表示终端节点组配置信息修改完成。\n- **UpdateEndpointGroup**接口不支持在同一个全球加速实例内并发修改终端节点组配置信息。",
    "en": "- The **UpdateEndpointGroup** API is an asynchronous API. After you call this API, the system returns a request ID and starts the update in the background; the configuration is not modified immediately. Call [DescribeEndpointGroup](~~153260~~) to check the status of the endpoint group:\n\n  - If an endpoint group is in the **updating** status, its configuration is being modified, and you can only perform queries.\n\n  - If an endpoint group is in the **active** status, the update is complete.\n\n- The **UpdateEndpointGroup** API does not support concurrent updates to endpoint groups in the same Global Accelerator (GA) instance."
  },
  "example": {
    "unifiedCli": "aliyun ga update-endpoint-group --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-426655440000 --endpoint-group-id epg-bp1dmlohjjz4kqaun0000 --name group1 --description EndpointGroup --endpoint-group-region cn-hangzhou --traffic-percentage 20 --health-check-interval-seconds 3 --health-check-path /healthcheck --health-check-port 20 --health-check-protocol HTTPS --threshold-count 3 --endpoint-configurations '[{\"EnableProxyProtocol\":false,\"ApiKeys\":[\"sk-000000\"],\"Type\":\"Ip\",\"EnableClientIPPreservation\":false,\"Endpoint\":\"120.XX.XX.21\",\"VpcId\":\"vpc-2zen6t0u7xhm0k5iz0000\",\"VSwitchIds\":[\"vsw-2ze2dbtkxabpvpqxc0000\"],\"SubAddress\":\"172.168.XX.XX\",\"Weight\":20,\"Provider\":\"BAILIAN\"}]' --endpoint-request-protocol HTTP --endpoint-protocol-version HTTP1.1 --port-overrides ListenerPort=443 EndpointPort=80 --health-check-enabled true --health-check-host www.taobao.com --endpoint-ip-version IPv4 --region cn-hangzhou",
    "legacyCli": "aliyun ga UpdateEndpointGroup --region cn-hangzhou --EndpointConfigurations.1.Endpoint '120.XX.XX.21' --EndpointConfigurations.1.VpcId 'vpc-2zen6t0u7xhm0k5iz0000' --EndpointConfigurations.1.SubAddress '172.168.XX.XX' --EndpointConfigurations.1.Provider BAILIAN --TrafficPercentage 20 --EndpointGroupId 'epg-bp1dmlohjjz4kqaun0000' --Description EndpointGroup --EndpointConfigurations.1.Weight 20 --HealthCheckPath '/healthcheck' --HealthCheckHost 'www.taobao.com' --PortOverrides.1.EndpointPort 80 --EndpointRequestProtocol HTTP --Name group1 --EndpointConfigurations.1.ApiKeys.1 'sk-000000' --EndpointProtocolVersion 'HTTP1.1' --EndpointGroupRegion 'cn-hangzhou' --HealthCheckProtocol HTTPS --EndpointIpVersion IPv4 --EndpointConfigurations.1.Type Ip --HealthCheckIntervalSeconds 3 --EndpointConfigurations.1.EnableProxyProtocol false --ThresholdCount 3 --HealthCheckEnabled true --PortOverrides.1.ListenerPort 443 --RegionId 'cn-hangzhou' --HealthCheckPort 20 --EndpointConfigurations.1.VSwitchIds.1 'vsw-2ze2dbtkxabpvpqxc0000' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --EndpointConfigurations.1.EnableClientIPPreservation false --method POST --force"
  }
}