{
  "name": "CreateEndpointGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceleratorId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速实例ID。",
        "en": "The ID of the GA instance."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token used to ensure request idempotence.\n\nYou can generate this token, but you must ensure it is unique for each request. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the request as the **ClientToken**. Each request has a unique **RequestId**."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "终端节点组描述信息。\n\n描述长度最大200字符，不能以`http://`和`https://`开头。",
        "en": "The description of the endpoint group.\n\nThe description can be up to 200 characters long and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n- **true**：发送检查请求，不会创建终端节点组。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回HTTP 2xx状态码。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接创建终端节点组。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: performs a dry run. The system checks the required parameters, request format, and service limits. If the request fails the dry run, the system returns an error message. If the request passes the dry run, the system returns an HTTP 2xx status code.\n\n- **false** (default): sends a normal request. If the request passes the check, the system returns an HTTP 2xx status code and creates the endpoint group."
      }
    },
    {
      "name": "EndpointConfigurations",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "终端节点配置信息。",
        "en": "The endpoint configurations."
      }
    },
    {
      "name": "EndpointGroupRegion",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "终端节点组所属的地域ID。",
        "en": "The ID of the region where the endpoint group is deployed."
      }
    },
    {
      "name": "EndpointGroupType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "终端节点组类型。取值：\n\n- **default**（默认值）：默认终端节点组。\n- **virtual**：虚拟终端节点组。\n\n> 为四层监听创建虚拟终端节点组时，请确保已创建默认终端节点组。",
        "en": "The type of the endpoint group. Valid values:\n\n- **default** (default): a default endpoint group.\n\n- **virtual**: a virtual endpoint group.\n\n> Before you create a virtual endpoint group for a Layer 4 listener, make sure that you have created a default endpoint group."
      }
    },
    {
      "name": "EndpointIpVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "后端IP地址协议。取值：  \n- **IPv4**：（默认值）：GA仅使用IPv4地址与终端节点服务通信。  \n- **IPv6**：GA仅使用IPv6地址与终端节点服务通信。  \n- **ProtocolAffinity**：GA与终端节点服务通信的IP协议与客户端请求使用的IP协议相同。",
        "en": "The IP version used to communicate with the backend service. Valid values:\n\n- **IPv4** (default): GA uses only IPv4 to communicate with the backend service.\n\n- **IPv6**: GA uses only IPv6 to communicate with the backend service.\n\n- **ProtocolAffinity**: GA uses the same IP version as the client request to communicate with the backend service."
      }
    },
    {
      "name": "EndpointProtocolVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "后端服务协议版本。取值：\n\n- **HTTP1.1**（默认值）：HTTP1.1版本。\n\n- **HTTP2**：HTTP2协议。\n\n> 仅当EndpointRequestProtocol（后端服务协议）值为HTTPS时，支持配置终端节点后端服务协议版本。",
        "en": "The version of the backend service protocol. Valid values:\n\n- **HTTP1.1** (default): HTTP/1.1.\n\n- **HTTP2**: HTTP/2.\n\n> This parameter is available only when `EndpointRequestProtocol` is set to HTTPS."
      }
    },
    {
      "name": "EndpointRequestProtocol",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "后端服务协议。取值：\n\n- **HTTP**（默认值）\n- **HTTPS**\n\n> - 仅为**HTTP**或**HTTPS**协议的监听实例创建终端节点组时才支持配置该项。\n> - 对于**HTTP**协议的监听实例，后端服务协议支持且仅支持**HTTP**。",
        "en": "The protocol used by the backend service. Valid values:\n\n- **HTTP** (default)\n\n- **HTTPS**\n\n> * This parameter is available only for endpoint groups of **HTTP** or **HTTPS** listeners.\n>\n> * For an **HTTP** listener, the backend service protocol must be **HTTP**."
      }
    },
    {
      "name": "HealthCheckEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启健康检查。取值：\n- **true**：开启健康检查。\n- **false**：关闭健康检查。",
        "en": "Specifies whether to enable health checks. Valid values:\n\n- **true**: enables health checks.\n\n- **false**: disables health checks."
      }
    },
    {
      "name": "HealthCheckHost",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "健康检查域名。",
        "en": "The domain name used for health checks."
      }
    },
    {
      "name": "HealthCheckIntervalSeconds",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查的时间间隔，单位：秒。",
        "en": "The health check interval, in seconds."
      }
    },
    {
      "name": "HealthCheckPath",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "健康检查路径。",
        "en": "The path used for health checks."
      }
    },
    {
      "name": "HealthCheckPort",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查的端口。",
        "en": "The port used for health checks."
      }
    },
    {
      "name": "HealthCheckProtocol",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "健康检查的协议。取值：\n\n- **tcp**或**TCP**：TCP协议。\n\n- **http**或**HTTP**：HTTP协议。\n\n- **https**或**HTTPS**：HTTPS协议。",
        "en": "The protocol used for health checks. Valid values:\n\n- **tcp** or **TCP**: TCP\n\n- **http** or **HTTP**: HTTP\n\n- **https** or **HTTPS**: HTTPS"
      }
    },
    {
      "name": "ListenerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "监听实例ID。",
        "en": "The ID of the listener."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "终端节点组的名称。\n\n名称长度为1~128个字符，以大小写字母或中文开头，可包含数字、半角句号（.）下划线（_）和短划线（-）。",
        "en": "The name of the endpoint group.\n\nThe name must be 1 to 128 characters long, start with a letter or a Chinese character, and can contain digits, periods (.), underscores (\\_), and hyphens (-)."
      }
    },
    {
      "name": "PortOverrides",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "端口映射关系。",
        "en": "The listener-to-endpoint port mappings."
      },
      "sub_parameters": [
        {
          "name": "EndpointPort",
          "position": "Query",
          "type": "Integer",
          "required": false,
          "description": {
            "zh": "配置端口映射时的终端节点端口。",
            "en": "The endpoint port for the port mapping."
          }
        },
        {
          "name": "ListenerPort",
          "position": "Query",
          "type": "Integer",
          "required": false,
          "description": {
            "zh": "配置端口映射时的监听端口。\n\n> - 对于TCP监听：虚拟终端节点组不支持配置端口映射。如果监听下已存在虚拟终端节点组，则默认终端节点组无法配置端口映射。如果默认终端节点组已配置端口映射，则无法添加虚拟终端节点组。\n>- 配置端口映射后，后续监听变更时有以下限制：除HTTP和HTTPS之间的协议切换以外，不支持修改监听协议。\n>- 监听端口：您修改的监听端口范围必须包含当前所配置端口映射的全量监听端口。例如，当监听端口范围为80-82，并配置端口映射至终端节点端口100-102时，后续修改监听端口范围时，不支持修改为80-81。",
            "en": "The listener port for the port mapping.\n\n> - For TCP listeners, you cannot configure port mappings for virtual endpoint groups. If a listener is associated with a virtual endpoint group, you cannot configure port mappings for the default endpoint group. If a default endpoint group has port mappings configured, you cannot add a virtual endpoint group.\n>\n> - After you configure port mappings, you cannot change the listener protocol, except for switching between HTTP and HTTPS.\n>\n> - When you modify the listener port range, the new range must include all listener ports used in port mappings. For example, if the listener port range is 80-82 and the listener ports are mapped to endpoint ports 100-102, you cannot change the listener port range to 80-81."
          }
        }
      ]
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速实例所在的地域ID，仅取值**cn-hangzhou**。",
        "en": "The ID of the region where the Global Accelerator (GA) instance is deployed. Set the value to **cn-hangzhou**."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "终端节点组的标签。",
        "en": "The tags of the endpoint group."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "终端节点组的标签键。一旦输入该值，则不允许为空字符串。\n\n最多支持64个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n\n最多支持输入20个标签键。",
            "en": "The tag key. The tag key cannot be an empty string.\n\nThe tag key can be up to 64 characters long and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.\n\nYou can specify up to 20 tag keys."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "终端节点组的标签值。一旦输入该值，可以为空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n\n最多支持输入20个标签值。",
            "en": "The tag value. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters long and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.\n\nYou can specify up to 20 tag values."
          }
        }
      ]
    },
    {
      "name": "ThresholdCount",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "针对健康检查状态变化所需要的连续健康检查次数，即从成功到失败的连续健康检查失败次数或从失败到成功的连续健康检查成功次数。\n取值范围：**2**~**10**。默认值：**3**。",
        "en": "The number of consecutive health checks that must succeed or fail before an endpoint's status changes between healthy and unhealthy. Valid values: **2** to **10**. Default value: **3**."
      }
    },
    {
      "name": "TrafficPercentage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "监听实例有多个终端节点组时，配置到不同终端节点组的流量比例。\n取值范围：**1**~**100**。",
        "en": "The percentage of traffic distributed to the endpoint group when the listener is associated with multiple endpoint groups. Valid values: **1** to **100**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建终端节点组",
    "en": "CreateEndpointGroup"
  },
  "descriptions": {
    "zh": "- 调用本接口为四层协议监听创建虚拟终端节点组时，请确保已创建默认终端节点组。\n- **CreateEndpointGroup**接口属于异步接口，即系统会先返回一个终端节点组ID，但终端节点组并未创建完成，系统后台的创建任务仍在进行。您可以调用[DescribeEndpointGroup](~~153260~~)查询终端节点组的状态：\n\n    - 当终端节点组处于**init**状态时，表示终端节点组正在创建中，在该状态下，您只能执行查询操作，不能执行其他操作。\n\n    - 当终端节点组处于**active**状态时，表示终端节点组创建完成。\n\n- **CreateEndpointGroup**接口不支持在同一个全球加速实例内并发创建终端节点组。",
    "en": "- Before you create a virtual endpoint group for a Layer 4 listener, you must first create a default endpoint group.\n\n- **CreateEndpointGroup** is an asynchronous operation. After you send a request, the system returns an endpoint group ID and begins creating the endpoint group in the background. You can call [DescribeEndpointGroup](~~153260~~) to query the status of the endpoint group:\n\n  - If the endpoint group is in the **init** state, it is being created. In this state, you can only perform query operations.\n\n  - If the endpoint group is in the **active** state, it has been created.\n\n- You cannot make concurrent calls to the **CreateEndpointGroup** operation for the same Global Accelerator instance."
  },
  "example": {
    "unifiedCli": "aliyun ga create-endpoint-group --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-426655440000 --accelerator-id ga-bp1odcab8tmno0hdq0000 --name group1 --description EndpointGroup --endpoint-group-region cn-hangzhou --listener-id lsr-bp1bpn0kn908w4nbw0000 --traffic-percentage 20 --health-check-interval-seconds 3 --health-check-path /healthcheck --health-check-port 20 --health-check-protocol tcp --threshold-count 3 --endpoint-configurations '[{\"EnableProxyProtocol\":false,\"ApiKeys\":[\"sk-00000000000\"],\"Type\":\"Ip\",\"EnableClientIPPreservation\":false,\"Endpoint\":\"120.1.XX.XX\",\"VpcId\":\"vpc-bp1quce3451z5b2hv0000\",\"VSwitchIds\":[\"vsw-bp12mho4ze51ezagm0000\"],\"SubAddress\":\"172.168.X.X\",\"Weight\":20,\"Provider\":\"BAILIAN\"}]' --endpoint-request-protocol HTTP --endpoint-protocol-version HTTP1.1 --endpoint-group-type default --port-overrides ListenerPort=443 EndpointPort=80 --health-check-enabled true --tag Value=test-value Key=test-key --dry-run false --health-check-host www.taobao.com --endpoint-ip-version IPv4 --region cn-hangzhou",
    "legacyCli": "aliyun ga CreateEndpointGroup --region cn-hangzhou --EndpointConfigurations.1.Endpoint '120.1.XX.XX' --EndpointConfigurations.1.VpcId 'vpc-bp1quce3451z5b2hv0000' --EndpointConfigurations.1.SubAddress '172.168.X.X' --EndpointConfigurations.1.Provider BAILIAN --TrafficPercentage 20 --Description EndpointGroup --EndpointConfigurations.1.Weight 20 --HealthCheckPath '/healthcheck' --HealthCheckHost 'www.taobao.com' --DryRun false --PortOverrides.1.EndpointPort 80 --EndpointRequestProtocol HTTP --Name group1 --EndpointConfigurations.1.ApiKeys.1 'sk-00000000000' --EndpointProtocolVersion 'HTTP1.1' --EndpointGroupRegion 'cn-hangzhou' --HealthCheckProtocol tcp --EndpointIpVersion IPv4 --EndpointConfigurations.1.Type Ip --EndpointGroupType default --HealthCheckIntervalSeconds 3 --Tag.1.Key 'test-key' --EndpointConfigurations.1.EnableProxyProtocol false --ThresholdCount 3 --HealthCheckEnabled true --PortOverrides.1.ListenerPort 443 --Tag.1.Value 'test-value' --RegionId 'cn-hangzhou' --HealthCheckPort 20 --EndpointConfigurations.1.VSwitchIds.1 'vsw-bp12mho4ze51ezagm0000' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --EndpointConfigurations.1.EnableClientIPPreservation false --AcceleratorId 'ga-bp1odcab8tmno0hdq0000' --ListenerId 'lsr-bp1bpn0kn908w4nbw0000' --method POST --force"
  }
}