{
  "name": "ListApplicationMonitor",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "列表的页码。默认值：**1**。",
        "en": "The page number. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页的行数。最大值：**100**。默认值：**10**。",
        "en": "The number of entries per page. Maximum value: **100**. Default value: **10**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "全球加速实例所属的地域，仅取值：**cn-hangzhou**。",
        "en": "The ID of the region where the Global Accelerator (GA) instance is deployed. Set the value to **cn-hangzhou**."
      }
    },
    {
      "name": "SearchValue",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "源站探测任务搜索关键字。支持按照URL、IP地址、全球加速实例ID和监听ID模糊搜索。",
        "en": "The keyword that is used to search for origin probing tasks. You can enter a URL, an IP address, a GA instance ID, or a listener ID to perform a fuzzy match."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询源站探测任务列表",
    "en": "ListApplicationMonitor"
  },
  "example": {
    "unifiedCli": "aliyun ga list-application-monitor --biz-region-id cn-hangzhou --page-number 1 --page-size 10 --search-value aliyun --region cn-hangzhou",
    "legacyCli": "aliyun ga ListApplicationMonitor --region cn-hangzhou --PageSize 10 --PageNumber 1 --SearchValue aliyun --RegionId 'cn-hangzhou'"
  }
}