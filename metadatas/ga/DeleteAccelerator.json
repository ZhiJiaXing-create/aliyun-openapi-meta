{
  "name": "DeleteAccelerator",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceleratorId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要删除的全球加速实例ID。",
        "en": "The ID of the GA instance that you want to delete."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速实例所属地域ID，仅取值**cn-hangzhou**。",
        "en": "The ID of the region where your GA instance is deployed. Set the value to **cn-hangzhou**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除全球加速实例",
    "en": "DeleteAccelerator"
  },
  "descriptions": {
    "zh": "- 不支持删除预付费（包年包月）全球加速实例。\n- **DeleteAccelerator**接口属于异步接口，即系统会先返回一个请求ID，但全球加速实例并未删除完成，系统后台的删除任务仍在进行。您可以调用[DescribeAccelerator](~~153235~~)查询全球加速实例的状态：\n    - 当全球加速实例处于**deleting**状态时，表示全球加速实例正在删除中，在该状态下，您只能执行查询操作，不能执行其他操作。\n    - 当查询不到该全球加速实例时，表示全球加速实例删除完成。",
    "en": "- You cannot delete subscription GA instances.\n\n- **DeleteAccelerator** is an asynchronous operation. After a request is sent, the system returns a request ID and runs the task in the background. You can call the [DescribeAccelerator](~~153235~~) operation to query the status of the task.\n\n  - If the GA instance is in the **deleting** state, the GA instance is being deleted. In this case, you can perform only query operations.\n\n  - If the GA instance cannot be queried, the GA instance is deleted."
  },
  "example": {
    "unifiedCli": "aliyun ga delete-accelerator --biz-region-id cn-hangzhou --accelerator-id ga-bp1odcab8tmno0hdq0000 --region cn-hangzhou",
    "legacyCli": "aliyun ga DeleteAccelerator --region cn-hangzhou --RegionId 'cn-hangzhou' --AcceleratorId 'ga-bp1odcab8tmno0hdq0000'"
  }
}