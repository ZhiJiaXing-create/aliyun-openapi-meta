{
  "name": "ListListeners",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceleratorId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速实例ID。",
        "en": "The ID of the Global Accelerator instance."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "列表的页码，默认值为**1**。",
        "en": "The page number. The default value is **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页的条目数，最大值为**50**，默认值为**10**。",
        "en": "The number of entries to return on each page. The maximum value is **50**. The default value is **10**."
      }
    },
    {
      "name": "Protocol",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "监听网络的传输协议类型",
        "en": "The transport layer protocol used by the listener."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速实例所在的地域ID，仅取值**cn-hangzhou**。",
        "en": "The ID of the region where the Global Accelerator instance is deployed. Set the value to **cn-hangzhou**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询监听列表",
    "en": "ListListeners"
  },
  "descriptions": {
    "zh": "本接口用于查询全球加速实例下的监听列表，包括各监听的路由类型、状态、创建的时间戳、端口信息等。",
    "en": "This operation retrieves the listeners of a Global Accelerator instance. The response includes the routing type, status, creation timestamp, and port details for each listener."
  },
  "example": {
    "unifiedCli": "aliyun ga list-listeners --biz-region-id cn-hangzhou --page-number 1 --page-size 10 --accelerator-id ga-bp1odcab8tmno0hdq0000 --protocol HTTP --region cn-hangzhou",
    "legacyCli": "aliyun ga ListListeners --region cn-hangzhou --PageSize 10 --PageNumber 1 --RegionId 'cn-hangzhou' --Protocol HTTP --AcceleratorId 'ga-bp1odcab8tmno0hdq0000'"
  }
}