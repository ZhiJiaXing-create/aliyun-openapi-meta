{
  "name": "UpdateAcceleratorAutoRenewAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceleratorId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速实例ID。",
        "en": "The ID of the Global Accelerator instance."
      }
    },
    {
      "name": "AutoRenew",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启了自动续费。取值：\n\n- **true**：是。\n\n- **false**（默认值）：否。\n\n> **AutoRenew**和**RenewalStatus**不能同时为空。",
        "en": "Specifies whether to enable auto-renewal for the instance. Valid values:\n\n- **true**: Auto-renewal is enabled.\n\n- **false** (default): Auto-renewal is disabled.\n\n> You must specify at least one of **AutoRenew** and **RenewalStatus**."
      }
    },
    {
      "name": "AutoRenewDuration",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "自动续费时长。单位：月。\n\n取值范围：**1**~**12**。\n\n> 仅**AutoRenew**的为**true**时，该项才生效。",
        "en": "The auto-renewal duration. Unit: month.\n\nValid values: **1** to **12**.\n\n> This parameter takes effect only when **AutoRenew** is set to **true**."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，ClientToken仅支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the request as the **ClientToken**. The **RequestId** may be different for each request."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "全球加速实例的名称。\n\n名称长度为1~128个字符，以大小写字母或中文开头，可包含数字、下划线（_）或短划线（-）。",
        "en": "The name of the Global Accelerator instance.\n\nThe name must be 1 to 128 characters in length, start with a letter or a Chinese character, and can contain letters, digits, underscores (\\_), and hyphens (-)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "全球加速实例所属的地域ID，仅取值：**cn-hangzhou**。",
        "en": "The region ID of the Global Accelerator instance. Set the value to **cn-hangzhou**."
      }
    },
    {
      "name": "RenewalStatus",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "全球加速实例的自动续费状态。取值：\n\n- **AutoRenewal**：设置为自动续费。\n\n- **Normal**：手动续费。\n\n- **NotRenewal**：不再续费，系统不再发送到期提醒，只在到期前第三天发送不续费提醒。不再续费的全球加速实例可以修改成手动续费（**Normal**）后，再自行续费或配置为自动续费（**AutoRenewal**）。\n\n> - **AutoRenew**和**RenewalStatus**不能同时为空。\n> - 参数**RenewalStatus**的优先级高于参数**AutoRenew**。如果不配置参数**RenewalStatus**，则默认以参数**AutoRenew**为准。",
        "en": "The auto-renewal status of the Global Accelerator instance. Valid values:\n\n- **AutoRenewal**: The instance is configured for auto-renewal.\n\n- **Normal**: The instance is configured for manual renewal.\n\n- NotRenewal: The instance is not renewed. The system does not send expiration reminders, but sends a non-renewal reminder three days before the expiration date. You can change the renewal status from **NotRenewal** to **Normal** to manually renew the instance, or change the renewal status to **AutoRenewal**.\n\n> * You must specify at least one of **AutoRenew** and **RenewalStatus**.\n>\n> * The **RenewalStatus** parameter takes precedence over the **AutoRenew** parameter. If you do not specify **RenewalStatus**, the value of **AutoRenew** is used."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改全球加速实例的自动续费属性",
    "en": "UpdateAcceleratorAutoRenewAttribute"
  },
  "descriptions": {
    "zh": "**UpdateAcceleratorAutoRenewAttribute**接口不支持并发修改同一个全球加速实例的自动续费属性。",
    "en": "The **UpdateAcceleratorAutoRenewAttribute** operation does not support concurrent modifications to the auto-renewal property of the same Global Accelerator instance."
  },
  "example": {
    "unifiedCli": "aliyun ga update-accelerator-auto-renew-attribute --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-426655440000 --name test --accelerator-id ga-bp17frjjh0udz4qz0000 --auto-renew false --renewal-status Normal --auto-renew-duration 1 --region cn-hangzhou",
    "legacyCli": "aliyun ga UpdateAcceleratorAutoRenewAttribute --region cn-hangzhou --AutoRenew false --RenewalStatus Normal --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --AcceleratorId 'ga-bp17frjjh0udz4qz0000' --Name test --AutoRenewDuration 1"
  }
}