{
  "name": "ListAcls",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AclIds",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "访问控制策略组ID，一次最多支持查询10个访问控制策略组ID。",
        "en": "The ACL IDs. You can specify a maximum of 20 ACL IDs in each request."
      }
    },
    {
      "name": "AclName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问控制策略组名称。一次最多支持查询10个访问控制策略组名称。\n\n名称长度为 1~128 个字符，以大小写字母或中文开头，可包含数字、半角句号（.）下划线（_）和短划线（-）。",
        "en": "The ACL name. You can specify a maximum of ACL names in each request.\n\nThe name must be 1 to 128 characters in length and can contain letters, digits, periods (.), underscores (\\_), and hyphens (-). The name must start with a letter."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** may be different for each request."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页显示的条目数。取值范围：**1**~**50**。默认值：**10**。",
        "en": "The number of entries per page. Valid values: **1** to **50**. Default value: **10**."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否拥有下一次查询的令牌（Token）。取值：\n- 第一次查询和没有下一次查询时，均无需填写。\n- 如果有下一次查询，取值为上一次API调用返回的NextToken值。",
        "en": "The token that is used for the next query. Valid values:\n\n*   If this is your first and only query, ignore this parameter.\n*   If a subsequent query is to be performed, set the parameter to the value of NextToken that is returned from the last call."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速实例所属的地域ID，仅取值：**cn-hangzhou**。",
        "en": "The ID of the region where the Global Accelerator (GA) instance is deployed. Set the value to **cn-hangzhou**."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "访问控制策略组的标签。",
        "en": "The tag of the ACL."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "访问控制策略组的标签键。一旦输入该值，则不允许为空字符串。\n\n最多支持64个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n\n最多支持输入20个标签键。",
            "en": "The tag key of the ACL. The value of this parameter cannot be an empty string.\n\nThe tag key can be up to 64 characters in length, and cannot contain `http://` or `https://`. The tag key cannot start with `aliyun` or `acs:`.\n\nYou can specify a maximum of 20 tag keys."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "访问控制策略组的标签值。一旦输入该值，可以为空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n\n最多支持输入20个标签值。",
            "en": "The tag value of the ACL. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length, and cannot contain `http://` or `https://`. The tag value cannot start with `aliyun` or `acs:`.\n\nYou can specify a maximum of 20 tag values."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询ACL列表",
    "en": "ListAcls"
  },
  "example": {
    "unifiedCli": "aliyun ga list-acls --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-426655440000 --acl-ids nacl-hp34s2h0xx1ht4nwo0000 --acl-name test-acl --next-token caeba0bbb2be03f84eb48b699f0a0000 --max-results 10 --resource-group-id rg-aek2lgw4evw0000 --tag Value=tag-value Key=tag-key --region cn-hangzhou",
    "legacyCli": "aliyun ga ListAcls --region cn-hangzhou --Tag.1.Key 'tag-key' --NextToken caeba0bbb2be03f84eb48b699f0a0000 --ResourceGroupId 'rg-aek2lgw4evw0000' --AclIds.1 'nacl-hp34s2h0xx1ht4nwo0000' --MaxResults 10 --Tag.1.Value 'tag-value' --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --AclName 'test-acl' --method POST --force"
  }
}