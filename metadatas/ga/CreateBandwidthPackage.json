{
  "name": "CreateBandwidthPackage",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPay",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否自动付费。取值：\n- **false**（默认值）：不开启自动付费，生成订单后需要到订单中心完成支付。\n- **true**：开启自动付费，自动支付订单。",
        "en": "Specifies whether to enable automatic payment. Valid values:\n\n- **false** (default): Disables automatic payment. After an order is generated, go to the Order Hub to complete the payment.\n\n- **true**: Enables automatic payment. Payments are automatically completed."
      }
    },
    {
      "name": "AutoRenew",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启自动续费。取值：\n\n- **true**：是。\n\n- **false**（默认值）：否。",
        "en": "Specifies whether to enable auto-renewal. Valid values:\n\n- **true**: Yes.\n\n- **false** (default): No."
      }
    },
    {
      "name": "AutoRenewDuration",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "自动续费时长。单位：月。\n\n取值范围：**1**~**12**。默认值：**1**。\n\n> 仅**AutoRenew**为**true**时，该项才生效。",
        "en": "The auto-renewal duration. Unit: months.\n\nValid values: **1** to **12**. Default value: **1**.\n\n> This parameter takes effect only if **AutoRenew** is set to **true**."
      }
    },
    {
      "name": "AutoUseCoupon",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否使用优惠券自动支付账单。取值：\n- **true**：是。\n- **false**（默认值）：否。\n\n> 仅**AutoPay**为**true**时，该项才生效。",
        "en": "Specifies whether to use a coupon to automatically pay for the bill. Valid values:\n\n- **true**: Yes.\n\n- **false** (default): No.\n\n> This parameter takes effect only if **AutoPay** is set to **true**."
      }
    },
    {
      "name": "Bandwidth",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "带宽包的带宽值。单位：Mbps。\n取值范围：**2**~**2000**。",
        "en": "The bandwidth of the bandwidth plan. Unit: Mbps.\nValid values: **2** to **2000**."
      }
    },
    {
      "name": "BandwidthType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "带宽类型。取值：\n- **Basic**：标准加速带宽。\n- **Enhanced**：增强加速带宽。\n- **Advanced**：精品加速带宽。\n\n在带宽包类型**Type**取值为**Basic**时，该项不能为空。",
        "en": "The type of bandwidth. Valid values:\n\n- **Basic**: Basic bandwidth.\n\n- **Enhanced**: Enhanced bandwidth.\n\n- **Advanced**: Advanced bandwidth.\n\nThis parameter is required if you set **Type** to **Basic**."
      }
    },
    {
      "name": "BillingType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "后付费计费方式。取值：\n- **PayByTraffic**（默认值）：流量计费。\n- **PayBY95**：95计费。按95计费方式默认不开放，如需使用，请联系您的商务经理申请。\n> 仅**ChargeType**为**POSTPAY**时，该项才生效。",
        "en": "The billing method for a pay-as-you-go bandwidth plan. Valid values:\n\n- **PayByTraffic** (default): pay-by-traffic.\n\n- **PayBY95**: pay-by-95th-percentile. This billing method is not available by default. Contact your account manager to use this billing method.\n\n> This parameter takes effect only if **ChargeType** is set to **POSTPAY**."
      }
    },
    {
      "name": "CbnGeographicRegionIdA",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "跨地域加速包的互通区域A，仅取值为**China-mainland**（中国内地）。\n\n仅国际站支持传入该参数。",
        "en": "Connected area A of the cross-region acceleration bandwidth plan. Set the value to **China-mainland**.\n\nThis parameter is available only on the Alibaba Cloud International Website (www\\.alibabacloud.com)."
      }
    },
    {
      "name": "CbnGeographicRegionIdB",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "跨地域加速包的互通区域B，仅取值为**Global**（全球）。\n\n仅国际站支持传入该参数。",
        "en": "Connected area B of the cross-region acceleration bandwidth plan. Set the value to **Global**.\n\nThis parameter is available only on the Alibaba Cloud International Website (www\\.alibabacloud.com)."
      }
    },
    {
      "name": "ChargeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "付费类型。取值：\n- **PREPAY**（默认值）：预付费，即包年包月。\n- **POSTPAY**：后付费，即按量付费。后付费类型默认不开放，如需使用，请联系您的商务经理申请。",
        "en": "The billing method. Valid values:\n\n- **PREPAY** (default): subscription.\n\n- **POSTPAY**: pay-as-you-go. The pay-as-you-go billing method is not available by default. Contact your account manager to use this billing method."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nGenerate a client token from your client to make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the request as the **ClientToken**. The **RequestId** may be different for each request."
      }
    },
    {
      "name": "Duration",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "购买时长。\n- 当**PricingCycle**取值**Month**时，**Duration**取值范围为**1**~**9**。\n- 当**PricingCycle**取值**Year**时，**Duration**取值范围为**1**~**3**。\n\n在**ChargeType**取值为**PREPAY**时，该项不能为空。",
        "en": "The subscription duration.\n\n- If you set **PricingCycle** to **Month**, valid values for **Duration** are **1** to **9**.\n\n- If you set **PricingCycle** to **Year**, valid values for **Duration** are **1** to **3**.\n\nThis parameter is required if you set **ChargeType** to **PREPAY**."
      }
    },
    {
      "name": "PricingCycle",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "计费周期。取值：\n- **Month**：按月计费。\n- **Year**：按年计费。\n\n在**ChargeType**取值为**PREPAY**时，该项不能为空。",
        "en": "The billing cycle. Valid values:\n\n- **Month**: monthly billing.\n\n- **Year**: yearly billing.\n\nThis parameter is required if you set **ChargeType** to **PREPAY**."
      }
    },
    {
      "name": "PromotionOptionNo",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "优惠券号码。\n\n> 仅国际站涉及该参数。",
        "en": "The coupon code.\n\n> This parameter is available only on the Alibaba Cloud International Website (www\\.alibabacloud.com)."
      }
    },
    {
      "name": "Ratio",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "95计费保底比例。取值范围：**30**~**100**。\n> 仅**BillingType**为**PayBY95**时，该项才生效。",
        "en": "The percentage of the guaranteed minimum bandwidth if you use the pay-by-95th-percentile metering method. Valid values: **30** to **100**.\n\n> This parameter takes effect only if **BillingType** is set to **PayBY95**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速实例所在的地域ID，仅取值**cn-hangzhou**。",
        "en": "The region ID of the GA instance. Set the value to **cn-hangzhou**."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The resource group ID."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "带宽包的标签。",
        "en": "The tags of the bandwidth plan."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "带宽包的标签键。一旦输入该值，则不允许为空字符串。\n\n最多支持64个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n\n最多支持输入20个标签键。",
            "en": "The tag key of the bandwidth plan. The tag key cannot be an empty string.\n\nThe tag key can be up to 64 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.\n\nYou can specify up to 20 tag keys."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "带宽包的标签值。一旦输入该值，可以为空字符串。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n\n最多支持输入20个标签值。",
            "en": "The tag value of the bandwidth plan. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.\n\nYou can specify up to 20 tag values."
          }
        }
      ]
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "带宽包的类型。取值：\n- **Basic**：基础带宽包。\n- **CrossDomain**：跨地域加速包。\n\n中国站仅支持**Basic**。",
        "en": "The type of the bandwidth plan. Valid values:\n\n- **Basic**: a basic bandwidth plan.\n\n- **CrossDomain**: a cross-region acceleration bandwidth plan.\n\nOnly **Basic** is supported on the Alibaba Cloud China Website (www\\.aliyun.com)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建指定带宽包",
    "en": "CreateBandwidthPackage"
  },
  "descriptions": {
    "zh": "使用全球加速GA（Global Accelerator）服务实现网络加速，您需要创建基础带宽包。基础带宽包支持以下带宽类型：\n- **标准加速带宽**：默认的加速区域和服务区域都位于中国内地，被加速的服务部署在阿里云上。\n- **增强加速带宽**：默认的加速区域和服务区域都位于中国内地，可同时加速阿里云和非阿里云的公网服务。\n- **精品加速带宽**：默认的加速区域和服务区域都位于海外，可同时加速阿里云和非阿里云的公网服务。如果要实现中国内地用户的网络加速，需选择中国香港作为加速地域。\n\n调用本接口时，请注意以下事项：\n- **CreateBandwidthPackage**接口属于异步接口，即系统会先返回一个带宽包ID，但带宽包并未创建完成，系统后台的创建任务仍在进行。您可以调用[DescribeBandwidthPackage](~~153241~~)查询带宽包的状态：\n\n    - 当带宽包处于**init**状态时，表示带宽包正在创建中，在该状态下，您只能执行查询操作，不能执行其他操作。\n    - 当带宽包处于**active**状态时，表示带宽包创建完成。\n\n- **CreateBandwidthPackage**接口不支持在同一个全球加速实例内并发创建带宽包。",
    "en": "You must create a basic bandwidth plan to use Global Accelerator (GA) for network acceleration. A basic bandwidth plan supports the following bandwidth types:\n\n- **Basic bandwidth**: The acceleration area and the area where the endpoint is deployed are in the Chinese mainland. The accelerated service is deployed on Alibaba Cloud.\n\n- **Enhanced bandwidth**: The acceleration area and the area where the endpoint is deployed are in the Chinese mainland. This bandwidth type can accelerate services on both Alibaba Cloud and public networks outside Alibaba Cloud.\n\n- **Advanced bandwidth**: The acceleration area and the area where the endpoint is deployed are outside the Chinese mainland. This bandwidth type can accelerate services on both Alibaba Cloud and public networks outside Alibaba Cloud. To accelerate access for users in the Chinese mainland, you can select China (Hong Kong) as the acceleration area.\n\nNote the following when you call this operation:\n\n- The **CreateBandwidthPackage** operation is asynchronous. After you send a request, the system returns a bandwidth plan ID, but the bandwidth plan is not created immediately. The system creates the bandwidth plan in the background. You can call the [DescribeBandwidthPackage](~~153241~~) operation to query the status of the bandwidth plan:\n\n  - If a bandwidth plan is in the **init** state, the bandwidth plan is being created. In this state, you can only query the bandwidth plan and cannot perform other operations.\n\n  - If a bandwidth plan is in the **active** state, the bandwidth plan is created.\n\n- The **CreateBandwidthPackage** operation does not support concurrent requests to create bandwidth plans for the same Global Accelerator instance."
  },
  "example": {
    "unifiedCli": "aliyun ga create-bandwidth-package --biz-region-id cn-hangzhou --bandwidth 2 --duration 1 --pricing-cycle Month --auto-pay false --client-token 123e45670000 --type Basic --bandwidth-type Basic --auto-use-coupon false --ratio 30 --billing-type PayByTraffic --charge-type PREPAY --cbn-geographic-region-id-a China-mainland --cbn-geographic-region-id-b Global --promotion-option-no 500032980140000 --auto-renew false --auto-renew-duration 1 --resource-group-id rg-aekzrnd67gq0000 --tag Value=tag-value Key=tag-key --region cn-hangzhou",
    "legacyCli": "aliyun ga CreateBandwidthPackage --region cn-hangzhou --Tag.1.Key 'tag-key' --CbnGeographicRegionIdB Global --CbnGeographicRegionIdA 'China-mainland' --ResourceGroupId 'rg-aekzrnd67gq0000' --PromotionOptionNo 500032980140000 --PricingCycle Month --AutoRenew false --Duration 1 --AutoPay false --AutoRenewDuration 1 --BandwidthType Basic --Type Basic --AutoUseCoupon false --ChargeType PREPAY --Bandwidth 2 --Tag.1.Value 'tag-value' --Ratio 30 --RegionId 'cn-hangzhou' --ClientToken 123e45670000 --BillingType PayByTraffic"
  }
}