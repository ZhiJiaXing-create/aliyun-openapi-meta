{
  "name": "CreateBasicEndpoint",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceleratorId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "基础型全球加速实例ID。",
        "en": "The instance ID of the basic Alibaba Cloud Global Accelerator (GA)."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request."
      }
    },
    {
      "name": "EndpointAddress",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "终端节点的地址。",
        "en": "The address of the endpoint."
      }
    },
    {
      "name": "EndpointGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "基础型全球加速实例的终端节点组ID。",
        "en": "The endpoint group ID of the basic Alibaba Cloud Global Accelerator (GA) instance."
      }
    },
    {
      "name": "EndpointSubAddress",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "终端节点辅助地址。\n\n当终端节点类型为**ECS**、**ENI**或**NLB**时，需配置该参数。\n- 当终端节点类型为**ECS**时，**EndpointSubAddress**可以为主网卡的辅助私网IP，为空表示主网卡的主私网IP。\n- 当终端节点类型为**ENI**时，**EndpointSubAddress**可以为辅助网卡的辅助私网IP，为空表示辅助网卡的主私网IP。\n- 当终端节点类型为**NLB**时，该参数必填，**EndpointSubAddress**为NLB后端服务器的主私网IP。",
        "en": "The secondary address of the endpoint.\n\nThis parameter is required when the endpoint type is **ECS**, **ENI**, or **NLB**.\n- If the endpoint type is **ECS**, EndpointSubAddress can be set to a secondary private IP of the primary network interface controller (NIC). If you leave this parameter empty, the primary private IP of the primary network interface controller (NIC) is used.\n- If the endpoint type is **ENI**, EndpointSubAddress can be set to a secondary private IP of the secondary network interface controller (NIC). If you leave this parameter empty, the primary private IP of the secondary network interface controller (NIC) is used.\n- If the endpoint type is **NLB**, this parameter is required. Set EndpointSubAddress to the primary private IP of the NLB backend server."
      }
    },
    {
      "name": "EndpointSubAddressType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "终端节点辅助地址的类型。取值：\n- **primary**：终端节点辅助地址的类型为主私网IP。\n- **secondary**：终端节点辅助地址的类型为辅助私网IP。\n\n当终端节点类型为**ECS**、**ENI**或**NLB**时，需配置该参数。其中，终端节点类型为**NLB**时，仅支持**primary**类型。",
        "en": "The type of the secondary address of the endpoint. Valid values:\n- **primary**: The secondary address type is the primary private IP address.\n- **secondary**: The secondary address type is a secondary private IP address.\n\nThis parameter is required when the endpoint type is **ECS**, **ENI**, or **NLB**. If the endpoint type is **NLB**, only **primary** is supported."
      }
    },
    {
      "name": "EndpointType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "终端节点类型。取值：\n- **ENI**：阿里云弹性网卡。\n- **SLB**：阿里云CLB实例。\n- **ECS**：阿里云ECS实例。\n- **NLB**：阿里云NLB实例。",
        "en": "The endpoint type. Valid values:\n- **ENI**: Alibaba Cloud elastic network interface (ENI).\n- **SLB**: Alibaba Cloud Classic Load Balancer (CLB) instance.\n- **ECS**: Alibaba Cloud ECS instance.\n- **NLB**: Alibaba Cloud Network Load Balancer (NLB) instance."
      }
    },
    {
      "name": "EndpointZoneId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "终端节点所在可用区ID。\n\n目前，仅当终端节点类型为**NLB**时，需要输入该参数。",
        "en": "The zone ID of the endpoint.\n\nCurrently, this parameter is required only when the endpoint type is **NLB**."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "基础型全球加速实例终端节点的名称。\n\n名称长度为1~128个字符，以大小写字母或中文开头，可包含数字、半角句号（.）下划线（_）和短划线（-）。",
        "en": "The name of the endpoint for the basic Alibaba Cloud Global Accelerator (GA) instance.\n\nThe name must be 1 to 128 characters in length and must start with a letter or a Chinese character. The name can contain digits, periods (.), underscores (\\_), and hyphens (-)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速所在的地域ID，仅取值**cn-hangzhou**。",
        "en": "The region ID of the Global Accelerator instance. Set the value to **ap-southeast-1**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为基础型全球加速实例创建终端节点",
    "en": "Create an endpoint for a basic global accelerator instance"
  },
  "descriptions": {
    "zh": "- **CreateBasicEndpoint**接口属于异步接口，即系统会先返回一个基础型全球加速实例终端节点ID，但终端节点未创建完成，系统后台的创建任务仍在进行。您可以调用[ListBasicEndpoints](~~466831~~)查询终端节点的状态： \n    - 当终端节点状态为**init**时，表示终端节点正在创建中，在该状态下，您只能执行查询操作，不能执行其他操作。\n    - 当终端节点状态为**active**时，表示终端节点已创建完成。\n- **CreateBasicEndpoint**接口不支持在同一个基础型全球加速实例内并发创建终端节点。",
    "en": "- **CreateBasicEndpoint** is an asynchronous operation. After you invoke this operation, the system returns an endpoint ID for the basic Alibaba Cloud Global Accelerator (GA) instance, but the endpoint is not yet created. The creation task continues to execute in the background. You can invoke [ListBasicEndpoints](~~466831~~) to query the endpoint status: \n    - When the endpoint is in the **init** state, the endpoint is being created. In this state, you can only execute query operations.\n    - When the endpoint is in the **active** state, the endpoint is created.\n- **CreateBasicEndpoint** does not support concurrent endpoint creation within the same basic Alibaba Cloud Global Accelerator (GA) instance."
  },
  "example": {
    "unifiedCli": "aliyun ga create-basic-endpoint --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-426655440000 --accelerator-id ga-bp17frjjh0udz4qz0000 --endpoint-group-id epg-bp1dmlohjjz4kqaun0000 --name ep01 --biz-endpoint-type ENI --endpoint-address eni-bp1a05txelswuj8g0000 --endpoint-sub-address 172.16.XX.XX --endpoint-sub-address-type primary --endpoint-zone-id cn-hangzhou-g --region cn-hangzhou",
    "legacyCli": "aliyun ga CreateBasicEndpoint --region cn-hangzhou --EndpointGroupId 'epg-bp1dmlohjjz4kqaun0000' --EndpointZoneId 'cn-hangzhou-g' --EndpointType ENI --EndpointSubAddressType primary --EndpointSubAddress '172.16.XX.XX' --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --AcceleratorId 'ga-bp17frjjh0udz4qz0000' --Name ep01 --EndpointAddress 'eni-bp1a05txelswuj8g0000'"
  }
}