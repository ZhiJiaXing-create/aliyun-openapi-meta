{
  "name": "CreateApplicationMonitor",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceleratorId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要探测的全球加速实例ID。",
        "en": "The instance ID of the Alibaba Cloud Global Accelerator (GA) instance to be probed."
      }
    },
    {
      "name": "Address",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要探测的URL或IP地址。",
        "en": "The URL or IP address to be probed."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of a request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may be different for each API request."
      }
    },
    {
      "name": "DetectEnable",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启自动诊断功能。取值：\n\n- **true**：是。\n\n- **false**（默认值）：否。",
        "en": "Specifies whether to enable the automatic diagnostics feature. Valid values:\n\n- **true**\n\n- **false** (default)"
      }
    },
    {
      "name": "DetectThreshold",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "设置自动诊断触发阈值。单位：%。\n\n取值范围：**0**~**100**。\n\n默认值：**0**，表示不触发自动诊断。",
        "en": "The threshold that triggers automatic diagnostics. Unit: %.\n\nValid values: **0** to **100**.\n\nDefault value: **0**, which indicates that automatic diagnostics is not triggered."
      }
    },
    {
      "name": "DetectTimes",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "触发自动诊断需要达到触发阈值的次数。\n\n取值范围：**1**~**20**。默认值：**1**。",
        "en": "The number of times that the threshold must be reached to trigger automatic diagnostics.\n\nValid values: **1** to **20**. Default value: **1**."
      }
    },
    {
      "name": "ListenerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要探测的监听实例ID。",
        "en": "The instance ID of the listener to be probed."
      }
    },
    {
      "name": "OptionsJson",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "源站探测任务对应监听协议类型的高级扩展选项。不同源站探测任务的监听协议类型对应不同的扩展选项。",
        "en": "The advanced extension options for the listener protocol type of the origin probing task. Different listener protocol types correspond to different extension options."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速实例所属的地域ID，仅取值：**cn-hangzhou**。",
        "en": "The region ID of the Alibaba Cloud Global Accelerator (GA) instance. Set the value to **cn-hangzhou**."
      }
    },
    {
      "name": "SilenceTime",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "自动诊断触发沉默时间。表示自动诊断触发后，未恢复到正常，间隔多久触发一次自动诊断。\n\n可用率连续多次（即设置的**DetectTimes**）低于自动诊断触发阈值时，触发自动诊断，如果可用率在自动诊断触发沉默时间内持续低于自动诊断触发阈值，在自动诊断触发沉默时间内不会重复出发自动诊断；如果可用率在自动诊断触发沉默时间后仍未恢复正常，则再次触发自动诊断。\n\n单位：秒。取值范围：**300**~**86400**。默认值：**300**。",
        "en": "The silence period for automatic diagnostics. This parameter specifies the interval between two consecutive automatic diagnostics when the availability does not recover to normal after automatic diagnostics is triggered.\n\nWhen the availability is consecutively below the automatic diagnostics threshold for the specified number of times (the value of **DetectTimes**), automatic diagnostics is triggered. If the availability remains below the threshold during the silence period, automatic diagnostics is not triggered again within the silence period. If the availability has not recovered after the silence period, automatic diagnostics is triggered again.\n\nUnit: seconds. Valid values: **300** to **86400**. Default value: **300**."
      }
    },
    {
      "name": "TaskName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "源站探测的任务名称。\n\n名称长度为 1~128 个字符，以大小写字母或中文开头，可包含数字、半角句号（.）下划线（_）和短划线（-）。",
        "en": "The name of the origin probing task.\n\nThe name must be 1 to 128 characters in length and must start with a letter or a Chinese character. It can contain digits, periods (.), underscores (\\_), and hyphens (-)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建源站探测任务",
    "en": "Create an origin probing task"
  },
  "descriptions": {
    "zh": "您可以使用**CreateApplicationMonitor**接口创建源站探测任务，实时监测从探测点经过全球加速至源站的全链路网络质量，从而快速定位网络故障，针对性的进行网络优化。\n\n调用本接口，请注意以下事项：\n\n- 仅包年包月、中型Ⅰ及以上规格的标准型全球加速实例支持创建源站探测任务。\n- 不支持为UDP协议监听创建源站探测任务。\n- 监控地址的服务端口需在监听端口范围内。\n- **CreateApplicationMonitor**接口属于异步接口，即系统会先返回一个源站探测的任务ID，但源站探测任务并未创建完成，系统后台的创建任务仍在进行。您可以调用[DescribeApplicationMonitor](~~408463~~)或[ListApplicationMonitor](~~408462~~)查询源站探测任务的状态：\n\n    - 当源站探测任务处于**init**状态时，表示源站探测任务正在创建中，在该状态下，您只能执行查询操作，不能执行其他操作。\n\n    - 当源站探测任务处于**active**状态时，表示源站探测任务创建完成。\n\n- **CreateApplicationMonitor**接口不支持在同一个全球加速实例内并发创建源站探测任务。",
    "en": "You can call the **CreateApplicationMonitor** operation to create an origin probing task. This task monitors the end-to-end network quality from the probing point through Global Accelerator (GA) to the origin server in real time, helping you quickly locate network faults and perform targeted network optimization.\n\nBefore you begin:\n\n- Only subscription Alibaba Cloud Global Accelerator (GA) instances of Medium Ⅰ or higher specifications support origin probing tasks.\n- Origin probing tasks cannot be created for UDP protocol listeners.\n- The service port of the monitoring address must be within the listener port range.\n- The **CreateApplicationMonitor** operation is asynchronous. After you invoke this operation, the system returns a node ID for the origin probing task, but the node is not yet created. The node creation continues in the background. You can invoke [DescribeApplicationMonitor](~~408463~~) or [ListApplicationMonitor](~~408462~~) to query the status of the origin probing task:\n\n    - If the origin probing task is in the **init** state, the task is being created. In this state, you can only perform query operations.\n\n    - If the origin probing task is in the **active** state, the task is created.\n\n- The **CreateApplicationMonitor** operation does not support concurrent creation of origin probing nodes within the same Alibaba Cloud Global Accelerator (GA) instance."
  },
  "example": {
    "unifiedCli": "aliyun ga create-application-monitor --biz-region-id cn-hangzhou --client-token 123e4567-e89b-12d3-a456-426655440000 --accelerator-id ga-bp1odcab8tmno0hdq0000 --listener-id lsr-bp1bpn0kn908w4nbw0000 --task-name task1 --address https://www.aliyun.com --options-json http_method_:_get_,_header_:_key:asd_,_acceptable_response_code_:_500_,_cert_verify_:_true --detect-enable false --detect-threshold 0 --detect-times 1 --silence-time 300 --region cn-hangzhou",
    "legacyCli": "aliyun ga CreateApplicationMonitor --region cn-hangzhou --SilenceTime 300 --DetectThreshold 0 --Address 'https://www.aliyun.com' --DetectEnable false --TaskName task1 --OptionsJson 'http_method_:_get_,_header_:_key:asd_,_acceptable_response_code_:_500_,_cert_verify_:_true' --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --AcceleratorId 'ga-bp1odcab8tmno0hdq0000' --DetectTimes 1 --ListenerId 'lsr-bp1bpn0kn908w4nbw0000'"
  }
}