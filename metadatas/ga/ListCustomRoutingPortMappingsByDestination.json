{
  "name": "ListCustomRoutingPortMappingsByDestination",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DestinationAddress",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待查询后端实例服务IP地址。",
        "en": "The service IP address of the backend instance."
      }
    },
    {
      "name": "EndpointId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待查询后端实例所属的终端节点ID。",
        "en": "The ID of the endpoint to which the backend instance belongs."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "列表的页码，默认值：**1**。",
        "en": "The page number. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页的条目数。最大值：**50**。默认值：**10**。",
        "en": "The number of entries per page. Maximum value: **50**. Default value: **10**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全球加速实例所在的地域ID，仅取值**cn-hangzhou**。",
        "en": "The ID of the region where the Global Accelerator instance is deployed. Set the value to **cn-hangzhou**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询指定后端实例的端口映射表",
    "en": "ListCustomRoutingPortMappingsByDestination"
  },
  "example": {
    "unifiedCli": "aliyun ga list-custom-routing-port-mappings-by-destination --biz-region-id cn-hangzhou --page-number 1 --page-size 10 --endpoint-id ep-bp16jdc00bhe97sr50000 --destination-address 10.0.XX.XX --region cn-hangzhou",
    "legacyCli": "aliyun ga ListCustomRoutingPortMappingsByDestination --region cn-hangzhou --PageSize 10 --PageNumber 1 --EndpointId 'ep-bp16jdc00bhe97sr50000' --RegionId 'cn-hangzhou' --DestinationAddress '10.0.XX.XX'"
  }
}