{
  "name": "ListDatasets",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DatasetQuery",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求体",
        "en": "The request body."
      }
    },
    {
      "name": "OpTenantId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "租户ID",
        "en": "The tenant ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "数据集列表查询",
    "en": "List datasets"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n该 API 允许用户通过提供租户 ID、项目 ID 以及其他可选参数（如关键词、类型列表等）来获取特定项目下的数据集信息。支持分页查询，返回的数据包括数据集的基本信息及其版本详情。请注意，`ProjectId` 是必填项，而其他参数则为可选项，可以根据需要进行设置以过滤结果。",
    "en": "## Operation description\n\nThis API allows you to retrieve dataset information for a specific project by providing a tenant ID, project ID, and other optional parameters such as keywords and type lists. Paging is supported. The returned data includes basic dataset information and version details. ProjectId is required. Other parameters are optional and can be configured as needed to filter results."
  }
}