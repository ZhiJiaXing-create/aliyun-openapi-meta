{
  "name": "CreateDataset",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CreateCommand",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "创建请求",
        "en": "The creation request."
      }
    },
    {
      "name": "OpTenantId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "租户ID",
        "en": "The tenant ID."
      }
    },
    {
      "name": "ProjectId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "项目 ID",
        "en": "The project ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建数据集",
    "en": "Create dataset"
  },
  "descriptions": {
    "zh": "## 请求说明\n- 该 API 用于在指定的项目中创建一个新的数据集。\n- `ProjectId` 是必填参数，表示要创建数据集的项目 ID。\n- `CreateCommand` 是一个复杂的对象，包含了创建数据集所需的各种配置信息。\n- `Name`、`Type`、`ContentType` 和 `Scenario` 是必填字段，分别表示数据集名称、类型、内容类型和使用场景。\n- `VersionConfig` 中的 `FileStorageConfig` 和 `MetadataStorageConfig` 可以根据实际需求进行配置。\n- 如果需要实时元表配置，请提供 `RealtimeMetaTableConfig` 相关信息。\n- 确保所有必填字段都已正确填写，否则请求将失败。",
    "en": "## Operation description\n- This API creates a new dataset in a specified project.\n- `ProjectId` is a required parameter that specifies the ID of the project in which to create the dataset.\n- `CreateCommand` is a complex object that contains the configuration information required to create the dataset.\n- `Name`, `Type`, `ContentType`, and `Scenario` are required fields that specify the dataset name, type, content type, and scenarios respectively.\n- `FileStorageConfig` and `MetadataStorageConfig` in `VersionConfig` can be configured as needed.\n- If you need a real-time meta table configuration, provide the `RealtimeMetaTableConfig` information.\n- Ensure that all required fields are correctly specified. Otherwise, the request failed."
  }
}