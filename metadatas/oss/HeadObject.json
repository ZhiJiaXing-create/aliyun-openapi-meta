{
  "name": "HeadObject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "HEAD",
  "pathPattern": "/[key]",
  "parameters": [
    {
      "name": "If-Match",
      "position": "Header",
      "type": "String",
      "required": false,
      "description": {
        "zh": "如果传入期望的ETag和Object的 ETag匹配，则返回200 OK和Object Meta；否则返回412 precondition failed。\n默认值：无",
        "en": "If the specified ETag matches the ETag of the object, 200 OK and the object metadata are returned. Otherwise, 412 Precondition Failed is returned.\nDefault value: null."
      }
    },
    {
      "name": "If-Modified-Since",
      "position": "Header",
      "type": "String",
      "required": false,
      "description": {
        "zh": "如果传入参数中的时间早于实际修改时间，则返回200 OK和Object Meta；否则返回304 not modified。\n默认值：无",
        "en": "If the time specified in this parameter is earlier than the actual modification time of the object, 200 OK and the object metadata are returned. Otherwise, 304 Not Modified is returned.\nDefault value: null."
      }
    },
    {
      "name": "If-None-Match",
      "position": "Header",
      "type": "String",
      "required": false,
      "description": {
        "zh": "如果传入期望的ETag值和Object的ETag不匹配，则返回200 OK和Object Meta；否则返回304 Not Modified。\n默认值：无",
        "en": "If the specified ETag does not match the ETag of the object, 200 OK and the object metadata are returned. Otherwise, 304 Not Modified is returned.\nDefault value: null."
      }
    },
    {
      "name": "If-Unmodified-Since",
      "position": "Header",
      "type": "String",
      "required": false,
      "description": {
        "zh": "如果传入参数中的时间等于或者晚于文件实际修改时间，则返回200 OK和Object Meta；否则返回412 precondition failed。\n默认值：无",
        "en": "If the time specified in this parameter is equal to or later than the actual modification time of the object, 200 OK and the object metadata are returned. Otherwise, 412 Precondition Failed is returned.\nDefault value: null."
      }
    },
    {
      "name": "bucket",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "The bucket name."
      }
    },
    {
      "name": "key",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Object的完整路径。",
        "en": "The full path of the object."
      }
    },
    {
      "name": "versionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Object的版本号。",
        "en": "The version ID of the object."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取文件的元信息",
    "en": "Retrieve object metadata"
  },
  "descriptions": {
    "zh": "- 使用此接口不会返回文件内容。\n\n- HeadObject操作默认获取Object当前版本的元信息。如果Object的当前版本为删除标记，则返回404 Not Found。请求参数中指定versionId则返回指定版本Object的元信息。",
    "en": "- This operation does not return the object content.\n\n- The HeadObject operation retrieves the metadata of the current version of an object by default. If the current version of the object is a delete marker, 404 Not Found is returned. If you specify versionId in the request parameters, the metadata of the specified object version is returned."
  }
}