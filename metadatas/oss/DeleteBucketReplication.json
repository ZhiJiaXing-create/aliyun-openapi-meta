{
  "name": "DeleteBucketReplication",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/?replication&comp=delete",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "Struct",
      "required": false,
      "description": {
        "zh": "保存需要删除的数据复制规则的请求体。",
        "en": "The request body that contains the data replication rule to delete."
      }
    },
    {
      "name": "bucket",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "The bucket name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "停止存储空间的数据复制并删除其复制配置",
    "en": "Stop data replication for a bucket and delete its replication configuration"
  },
  "descriptions": {
    "zh": "- 当请求的Bucket没有配置数据复制规则时，调用此接口将返回200 HTTP OK。\n\n- 调用此接口删除某个数据复制规则时，该复制规则不会立刻被删除。OSS需要一定的时间来执行清理操作，此时复制规则的状态为closing。当清理工作完成后，该复制规则才被删除。\n\n- 当请求的Bucket的数据复制规则处于closing状态时，调用此接口将返回204 NoContent。",
    "en": "- If the requested bucket does not have a data replication rule configured, this operation returns 200 HTTP OK.\n\n- When you call this operation to delete a data replication rule, the rule is not immediately deleted. Object Storage Service (OSS) requires some time to perform cleanup operations, during which the replication rule status is closing. The replication rule is deleted only after the cleanup is complete.\n\n- If the data replication rule of the requested bucket is in the closing state, this operation returns 204 NoContent."
  }
}