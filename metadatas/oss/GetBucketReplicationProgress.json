{
  "name": "GetBucketReplicationProgress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/?replicationProgress",
  "parameters": [
    {
      "name": "bucket",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "The bucket name."
      }
    },
    {
      "name": "rule-id",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "复制规则对应的ID。此ID可从GetBucketReplication中获取。",
        "en": "The ID of the replication rule. You can obtain this ID from the GetBucketReplication operation."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取存储空间的数据复制进度",
    "en": "Retrieve data replication progress of a bucket"
  }
}