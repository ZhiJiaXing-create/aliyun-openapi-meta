{
  "name": "PutBucketTags",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/?tagging",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "Struct",
      "required": false,
      "description": {
        "zh": "设置Bucket TagSet的请求体。",
        "en": "The request body for setting the bucket TagSet."
      }
    },
    {
      "name": "bucket",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "The bucket name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为存储空间添加或修改标签",
    "en": "Add or modify tags for a bucket"
  },
  "descriptions": {
    "zh": "- 只有Bucket的拥有者及授权RAM账户才能为Bucket设置用户标签，否则返回403 Forbidden错误，错误码为AccessDenied。\n\n- 最多可设置20对Bucket用户标签（Key-Value对）。\n\n- PutBucketTags是覆盖语义，即新添加的标签会完全覆盖已有的标签。",
    "en": "- Only the bucket owner and authorized RAM users can set tags for a bucket. Otherwise, a 403 Forbidden error with the AccessDenied error code is returned.\n\n- You can set up to 20 tags (key-value pairs) for a bucket.\n\n- PutBucketTags uses overwrite semantics. New tags completely overwrite existing tags."
  }
}