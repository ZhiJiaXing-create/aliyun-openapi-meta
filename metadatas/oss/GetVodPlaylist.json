{
  "name": "GetVodPlaylist",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/[channel]?vod",
  "parameters": [
    {
      "name": "bucket",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "The bucket name."
      }
    },
    {
      "name": "channel",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定LiveChannel名称。该LiveChannel必须存在。",
        "en": "The name of the LiveChannel. The LiveChannel must exist."
      }
    },
    {
      "name": "endTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定查询ts文件的终止时间，格式为Unix timestamp。\n> EndTime必须大于 StartTime，且时间跨度不能大于1天。",
        "en": "The end time for querying TS files. The value is a UNIX timestamp.\n> EndTime must be greater than StartTime, and the time span cannot exceed one day."
      }
    },
    {
      "name": "startTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定查询ts文件的起始时间，格式为Unix timestamp。",
        "en": "The start time for querying TS files. The value is a UNIX timestamp."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查看直播推流的播放列表",
    "en": "Query the playlist of a live stream"
  }
}