{
  "name": "OptionObject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "OPTIONS",
  "pathPattern": "/[key]",
  "parameters": [
    {
      "name": "Access-Control-Request-Headers",
      "position": "Header",
      "type": "String",
      "required": false,
      "description": {
        "zh": "在实际请求中会用到的除了简单头部之外的header。\n在实际请求中可以为该请求头设置多个header，多个header之间使用英文逗号(,)隔开。\n默认值：无",
        "en": "The headers other than simple headers that will be used in the actual request.\nYou can specify multiple headers for this request header in the actual request. Separate multiple headers with commas (,).\nDefault value: null."
      }
    },
    {
      "name": "Access-Control-Request-Method",
      "position": "Header",
      "type": "String",
      "required": false,
      "description": {
        "zh": "在实际请求中会用到的方法。\n在实际请求中只能设置一个该请求头。\n默认值：无",
        "en": "The method that will be used in the actual request.\nYou can specify only one method for this request header in the actual request.\nDefault value: null."
      }
    },
    {
      "name": "Origin",
      "position": "Header",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求来源域，用于标识跨域请求。\n在实际请求中只能设置一个该请求头。\n默认值：无",
        "en": "The origin domain of the request, which identifies a cross-origin request.\nYou can specify only one origin domain for this request header in the actual request.\nDefault value: null."
      }
    },
    {
      "name": "bucket",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "The bucket name."
      }
    },
    {
      "name": "key",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Object完整路径。",
        "en": "The full path of the object."
      }
    }
  ],
  "security": [
    "Anonymous"
  ],
  "title": {
    "zh": "发送预检请求",
    "en": "Send a preflight request"
  },
  "descriptions": {
    "zh": "Options请求是由浏览器自动根据是否跨域来决定是否发送。",
    "en": "The Options request is automatically sent by the browser based on whether the request is cross-origin."
  }
}