{
  "name": "DeleteBucket",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/",
  "parameters": [
    {
      "name": "bucket",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "The bucket name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除存储空间",
    "en": "Delete a bucket"
  },
  "descriptions": {
    "zh": "- 只有Bucket的拥有者才有权限删除该Bucket。\n- 为了防止误删除的发生，OSS不支持删除一个非空的Bucket。",
    "en": "- Only the bucket owner can delete the bucket.\n- To prevent accidental deletion, OSS does not allow you to delete a non-empty bucket."
  }
}