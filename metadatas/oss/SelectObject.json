{
  "name": "SelectObject",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/[key]",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "Struct",
      "required": false,
      "description": {
        "zh": "保存SelectObject请求的容器。",
        "en": "The container that stores the SelectObject request."
      }
    },
    {
      "name": "bucket",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "The name of the bucket."
      }
    },
    {
      "name": "key",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Object的完整路径。",
        "en": "The full path of the object."
      }
    },
    {
      "name": "x-oss-process",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "如果是csv文件，该值需要设置为csv/select；如果是json文件，则需要设置为json/select。",
        "en": "Set this parameter to csv/select for CSV files or json/select for JSON files."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "对文件执行SQL语句并返回结果",
    "en": "Execute an SQL statement on an object and return the result"
  },
  "descriptions": {
    "zh": "- 此操作要求您对该Object有读权限。\n- 正确执行时，该API返回206。如果SQL语句不正确，或者和文件不匹配，则会返回400错误。\n- 关于SelectObject的功能介绍请参见[使用SelectObject查询文件](~~106082~~)。",
    "en": "- This operation requires read permissions on the object.\n- If the operation is successful, the API returns HTTP status code 206. If the SQL statement is incorrect or does not match the object, HTTP status code 400 is returned.\n- For more information about SelectObject, see [Use SelectObject to query objects](~~106082~~)."
  }
}