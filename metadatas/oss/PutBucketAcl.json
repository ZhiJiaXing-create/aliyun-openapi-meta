{
  "name": "PutBucketAcl",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/?acl",
  "parameters": [
    {
      "name": "bucket",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "The bucket name."
      }
    },
    {
      "name": "x-oss-acl",
      "position": "Header",
      "type": "Json",
      "required": true,
      "description": {
        "zh": "设置Bucket的访问权限ACL。PutBucketAcl接口通过Put请求中的x-oss-acl请求头来设置访问权限，如果没有该请求头，则访问权限设置不生效。\n<br>取值如下：\n\n- public-read-write（公共读写）：所有用户都有该Bucket内的文件的读写权限。请谨慎使用该访问权限。\n- public-read（公共读）：Bucket的拥有者和授权用户有该Bucket内的文件的读写权限，其他用户只有该Bucket内的文件的读权限。请谨慎使用该访问权限。\n- private：Bucket的拥有者和授权用户有该Bucket内的文件的读写权限，其他用户没有权限操作该Bucket内的文件。",
        "en": "Sets the access permissions (ACL) of the bucket. The PutBucketAcl operation sets the ACL by using the x-oss-acl request header in the PUT request. If this header is not included, the ACL settings do not take effect.\n<br>Valid values:\n\n- public-read-write: All users have read and write access permissions on the files in the bucket. Exercise caution when you set this ACL.\n- public-read: The bucket owner and authorized users have read and write access permissions on the files in the bucket. Other users have only read permissions on the files in the bucket. Exercise caution when you set this ACL.\n- private: The bucket owner and authorized users have read and write access permissions on the files in the bucket. Other users have no permissions on the files in the bucket."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置或修改存储空间的访问权限",
    "en": "Set or modify the access permissions of a bucket"
  },
  "descriptions": {
    "zh": "- 请求者需要对Bucket拥有写入ACL的权限`oss:PutBucketAcl`。\n\n- PutBucketAcl为覆盖语义，即新传入的ACL将覆盖原有的ACL。\n\n- 如果指定要设置ACL的Bucket不存在，调用该接口时将新建Bucket。",
    "en": "- The requester must have the `oss:PutBucketAcl` permission to write the ACL of the bucket.\n\n- PutBucketAcl uses overwrite semantics. The new ACL overwrites the existing ACL.\n\n- If the specified bucket does not exist, calling this operation creates a new bucket."
  }
}