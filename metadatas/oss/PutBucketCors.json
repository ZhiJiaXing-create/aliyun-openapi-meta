{
  "name": "PutBucketCors",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/?cors",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "Struct",
      "required": false,
      "description": {
        "zh": "设置跨域资源共享规则的请求体。",
        "en": "The request body for configuring CORS rules."
      }
    },
    {
      "name": "bucket",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "The bucket name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置存储空间的跨域资源共享规则",
    "en": "Configure CORS rules for a bucket"
  },
  "descriptions": {
    "zh": "- 默认不开启CORS\n<br>Bucket默认不开启CORS功能，所有跨域请求的Origin都不被允许。\n\n- 覆盖语义\n <br>PutBucketCors为覆盖语义，即新配置的CORS规则将覆盖已有的CORS规则。\n\n- 应用程序中使用CORS\n<br>在应用程序中使用CORS功能时，需通过PutBucketCors接口手动上传CORS规则来开启CORS功能。\n<br>例如从`example.com`通过浏览器的`XMLHttpRequest`功能来访问OSS，需要通过本接口手动上传CORS规则，且CORS规则需由XML文档进行描述。\n\n- CORS规则匹配\n<br>当OSS收到一个跨域请求或OPTIONS请求，会先读取Bucket对应的CORS规则，然后进行相应的权限检查。OSS会依次检查每一条规则，使用第一条匹配的规则来允许请求并返回对应的Header。如果所有规则都匹配失败，则不附加任何CORS相关的Header。\n<br>CORS规则匹配成功必须满足以下三个条件：\n    - 请求的Origin必须匹配一个`AllowedOrigin`项。\n\n    - 请求的方法（例如GET、PUT等）或者OPTIONS请求的`Access-Control-Request-Method`头对应的方法必须匹配一个`AllowedMethod`项。\n\n    - OPTIONS请求的`Access-Control-Request-Headers`头包含的每个header都必须匹配一个`AllowedHeader`项。",
    "en": "- CORS is disabled by default\n<br>CORS is disabled for a bucket by default. Origins of all cross-origin requests are not allowed.\n\n- Overwrite semantics\n <br>PutBucketCors uses overwrite semantics. A new CORS rule configuration overwrites the existing CORS rule configuration.\n\n- Use CORS in applications\n<br>To use CORS in an application, manually upload CORS rules by calling the PutBucketCors operation to enable CORS.\n<br>For example, to access OSS from `example.com` by using the `XMLHttpRequest` function of a browser, manually upload CORS rules by calling this operation. The CORS rules must be described in an XML document.\n\n- CORS rule matching\n<br>When OSS receives a cross-origin request or an OPTIONS request, it reads the CORS rules configured for the bucket and then performs permission checks. OSS checks each rule in sequence and uses the first matching rule to allow the request and return the corresponding headers. If no rule matches, no CORS-related headers are included in the response.\n<br>A CORS rule match must meet all three of the following conditions:\n    - The Origin of the request must match an `AllowedOrigin` entry.\n\n    - The request method (such as GET or PUT) or the method that corresponds to the `Access-Control-Request-Method` header of the OPTIONS request must match an `AllowedMethod` entry.\n\n    - Each header included in the `Access-Control-Request-Headers` header of the OPTIONS request must match an `AllowedHeader` entry."
  }
}