{
  "name": "GetBucketAcl",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/?acl",
  "parameters": [
    {
      "name": "bucket",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "The bucket name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取存储空间的访问权限",
    "en": "Configure lifecycle rules for a bucket"
  },
  "descriptions": {
    "en": "- Only the bucket owner and Resource Access Management (RAM) users who have been granted the PutBucketLifecycle permission can initiate a request to configure lifecycle rules.\n\n- If no lifecycle rules have been configured for the bucket, this operation creates a new lifecycle rule. If lifecycle rules have been configured for the bucket, this operation overwrites the existing rule configurations.\n\n- PutBucketLifecycle uses overwrite semantics. To append lifecycle rules, invoke the GetBucketLifecycle operation to obtain the current lifecycle rule configurations, append the new lifecycle rule configurations, and then invoke the PutBucketLifecycle operation to update the lifecycle rule configurations.\n\n- The PutBucketLifecycle operation can configure expire times for objects and parts (parts that are uploaded by using multipart upload but are not committed in the final epoch)."
  }
}