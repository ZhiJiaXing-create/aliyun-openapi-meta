{
  "name": "DescribeTasks",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v1/tasks",
  "parameters": [
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "返回的最大数。取值范围：10~500。默认值：当不设置值或者设置的值小于10，默认值为10。当设置的值大于500时，默认值为500。",
        "en": "The maximum number of entries to return per page. The valid range is 10 to 500. If this parameter is omitted or its value is less than 10, a default value of 10 is used. Values greater than 500 are treated as 500."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询凭证（Token），取值为上一次API调用返回的NextToken参数值。有关本接口查看返回数据的设置方式，请参见上文接口说明。",
        "en": "A pagination token. To retrieve the next page of results, set this parameter to the `NextToken` value from the response of the previous API call. For more information, see the API description above."
      }
    },
    {
      "name": "TaskStatus",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "CREATED: 创建, RUNNING: 正在执行, COMPLETE: 任务成功完成, FAILED: 任务执行失败, EXPIRED: 任务已过期, CANCELED: 任务已被取消",
        "en": "Specifies the status of tasks to query. If this parameter is omitted, the API returns tasks in all states. Valid values: `CREATED`, `RUNNING`, `COMPLETE`, `FAILED`, `EXPIRED`, and `CANCELED`."
      }
    },
    {
      "name": "TaskType",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询异步任务列表",
    "en": "DescribeTasks"
  },
  "example": {
    "unifiedCli": "aliyun bdrc describe-tasks --task-status RUNNING --next-token cae0000000000699 --max-results 10 --task-type UPDATE_RESOURCES --region cn-hangzhou",
    "legacyCli": "aliyun bdrc GET /api/v1/tasks --region cn-hangzhou --NextToken cae0000000000699 --MaxResults 10 --TaskType UPDATE_RESOURCES --TaskStatus RUNNING --header \"Content-Type=application/json\""
  }
}