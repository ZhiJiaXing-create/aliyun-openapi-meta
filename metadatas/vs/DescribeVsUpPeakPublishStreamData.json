{
  "name": "DescribeVsUpPeakPublishStreamData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "输入域名，查域名粒度数据",
        "en": "Domain name to query. Returns data at the domain granularity."
      }
    },
    {
      "name": "DomainSwitch",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "域名统计开关，取值：on或者off；on：开启域名统计级别，off：用户级别数据。默认统计用户级别数据",
        "en": "Enables or disables domain-level statistics. Valid values: on or off. When set to on, the response shows domain-level data. When set to off or omitted, the response shows user-level data. Default is user-level data."
      }
    },
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "拉流结束时间，UTC格式, StartTime和EndTime时间间隔在30天内,且EndTime超过当前时间",
        "en": "End time of stream ingest, in UTC format. The time range between StartTime and EndTime must be within 30 days. EndTime must be later than the current time."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "拉流开始时间，UTC格式, StartTime和EndTime时间间隔在30天内",
        "en": "Start time of stream ingest, in UTC format. The time range between StartTime and EndTime must be within 30 days."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询用户每日上行并发推流数峰值",
    "en": "Query the daily peak concurrent stream ingest count for a user"
  }
}