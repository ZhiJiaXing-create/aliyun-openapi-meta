{
  "name": "BatchCaptureRenderingInstanceScreenshot",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/",
  "parameters": [
    {
      "name": "Quality",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "图片质量，范围 1~100",
        "en": "The image quality. Valid values: 1 to 100."
      }
    },
    {
      "name": "RenderingInstanceIds",
      "position": "Query",
      "type": "Json",
      "required": true,
      "description": {
        "zh": "实例 ID 列表，最多 100 个",
        "en": "The list of instance IDs. A maximum of 100 instance IDs can be specified."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "云应用服务实例批量截图",
    "en": "Batch capture screenshots of cloud application service instances"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n- **认证**：请求需要携带 `AliUid` 参数进行身份验证。\n- **实例指定方式**：可以通过 `RenderingInstanceIds`指定要截图的实例。\n- **截图质量**：通过 `Quality` 参数可以设置截图图片的质量，默认值为75（如果未配置），取值范围是1到100。\n- **响应处理**：返回结果中包含成功和失败的实例列表及其相关信息，包括下载链接以及截图完成时间等。",
    "en": "## Request description\n\n- **Authentication**: Requests must include the `AliUid` parameter for identity verification.\n- **Instance specification**: Use `RenderingInstanceIds` to specify the instances to capture screenshots from.\n- **Screenshot quality**: Use the `Quality` parameter to set the image quality of screenshots. The default value is 75 (if not configured). Valid values: 1 to 100.\n- **Response handling**: The response contains lists of successful and failed instances with related information, including download URLs and screenshot completion times."
  }
}