{
  "name": "UploadFile",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件描述信息",
        "en": "The description of the file."
      }
    },
    {
      "name": "FileName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "自定义文件名，不可重复，将作为文件的唯一标记之一。\n默认取值规范：\n1. 长度8-255\n2. 小写字母、数字、下划线'_'、中划线'-'、点分段'.'\n3. 首、尾字符必须为字母或数字",
        "en": "A custom file name. The name must be unique and serves as a unique identifier for the file. The name must meet the following requirements:\n\n1. It must be 8 to 255 characters in length.\n\n2. It can contain lowercase letters, digits, underscores (\\_), hyphens (-), and periods (.).\n\n3. The first and last characters must be a letter or a digit."
      }
    },
    {
      "name": "Md5",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件md5，用于检查文件完整性",
        "en": "The MD5 hash of the file. This is used to verify the integrity of the file."
      }
    },
    {
      "name": "OriginUrl",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件下载地址",
        "en": "The download URL of the file."
      }
    },
    {
      "name": "TargetPath",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "向云应用服务实例推送的目标路径，必须是绝对路径，不支持输入目录。目标路径所在上级目录有范围限制，取值范围：\n1. /data/local\n2. /data/user\n3. /data/data\n4. /data/cache\n5. /data/tmp\n6. /data/storage\n7. /data/media/0",
        "en": "The destination path on the service instance. This must be an absolute path to a file. You cannot specify only a folder. The parent folder of the destination path is restricted to the following locations:\n\n1. /data/local\n\n2. /data/user\n\n3. /data/data\n\n4. /data/cache\n\n5. /data/tmp\n\n6. /data/storage\n\n7. /data/media/0"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "上传文件",
    "en": "Upload File"
  }
}