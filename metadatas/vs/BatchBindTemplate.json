{
  "name": "BatchBindTemplate",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ApplyAll",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否强制应用到范围内的所有流，默认false。",
        "en": "Apply to all streams within the scope. Default value: false."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID，多值用逗号分隔。",
        "en": "Instance ID. Separate multiple values with commas."
      }
    },
    {
      "name": "InstanceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例类型。取值：\n- group（空间）\n- stream（流）",
        "en": "Instance type. Valid values:\n\n- group (space)\n\n- stream (stream)"
      }
    },
    {
      "name": "Replace",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否强制替换已有绑定，默认false。",
        "en": "Replace existing bindings. Default value: false."
      }
    },
    {
      "name": "TemplateId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "模板ID。",
        "en": "Template ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量绑定模板到多个实例",
    "en": "Batch attach template to multiple instances"
  }
}