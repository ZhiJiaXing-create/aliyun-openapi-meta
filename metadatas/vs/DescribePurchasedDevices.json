{
  "name": "DescribePurchasedDevices",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按设备所属空间ID查询。",
        "en": "Queries by the ID of the group to which the device belongs."
      }
    },
    {
      "name": "Id",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按设备ID查询。",
        "en": "Queries by device ID."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按设备名称查询。",
        "en": "Queries by device name."
      }
    },
    {
      "name": "PageNum",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "第几个分页，默认为1。",
        "en": "The page number. The default is 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页大小，默认为20。",
        "en": "The number of entries per page. The default is 20."
      }
    },
    {
      "name": "SortBy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按给定字段排序。取值：\n- id（默认）",
        "en": "The field to sort by. Valid value:\n\n- id (default)"
      }
    },
    {
      "name": "SortDirection",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序方式，默认升序。取值：\n- asc（升序）\n- desc（降序）",
        "en": "The sorting order. The default is ascending. Valid values:\n\n- asc (ascending)\n\n- desc (descending)"
      }
    },
    {
      "name": "SubType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按设备子类型查询。取值：\n- bullet（枪机）\n- dome（球机）\n- ptz（PTZ）",
        "en": "Queries by device subtype. Valid values:\n\n- bullet (bullet camera)\n\n- dome (dome camera)\n\n- ptz (PTZ camera)"
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按设备类型查询。取值：\n- ipc（摄像头）\n- platform（平台）\n- ied（智能设备）",
        "en": "Queries by device type. Valid values:\n\n- ipc (camera)\n\n- platform (platform)\n\n- ied (intelligent edge device)"
      }
    },
    {
      "name": "Vendor",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按设备厂商查询。",
        "en": "Queries by device vendor."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询已购买设备列表",
    "en": "Query purchased device list"
  }
}