{
  "name": "UninstallCloudApp",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云应用ID",
        "en": "Cloud application ID"
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "项目下实例关联关系分页页码（分页查询默认按实例关联时间倒序）。仅ProjectId非空场景有效，限制项目下执行动作的最大实例数，以控制动作的影响面。默认1。\n1. 关于PageNumber取值范围：\n  a. 【推荐】方法一：根据项目关联实例总数（接口ListRenderingProjectInstances可获取），换算出上限值。\n  b. 方法二：可以通过接口返回值判断PageNumber是否达到Project上限值，【优点：不用换算取值范围】。接口返回满足以下任意一个条件时，就可以认为PageNumber达到上限值：\n    ⅰ. 接口返回403+错误码Code 200301\n    ⅱ. 接口的返回值SuccessInstanceCount+FailedInstanceCount<PageSize\n2. 场景举例：\n  a. 项目下实例全量卸载场景：项目实例数大于PageSize（默认100）时，可以多次调用unInstall接口(+1递增分页PageNumber)以实现全量卸载。项目实例卸载进度可通过ListCloudAppInstallations接口获取。",
        "en": "Page number for paged queries of instance associations in the project. Results are sorted by association time in descending order. This parameter applies only when ProjectId is not empty. It limits the maximum number of instances affected by this operation to control impact scope. Default value: 1.\n\n1. Valid PageNumber range:\n   a. Recommended method: Calculate the upper limit based on the total number of instances associated with the project. You can get this count using the ListRenderingProjectInstances API.\n   b. Alternative method: Check the API response to determine whether PageNumber has reached the upper limit. This avoids manual calculation. PageNumber has reached the upper limit if either of the following occurs:\n   ⅰ. The API returns HTTP status 403 and error code 200301.\n   ⅱ. The sum of SuccessInstanceCount and FailedInstanceCount in the response is less than PageSize.\n\n2. Example scenario:\n   a. Full uninstall across all project instances: If the project has more instances than PageSize (default 100), call UninstallCloudApp multiple times, incrementing PageNumber by 1 each time. Track uninstall progress using the ListCloudAppInstallations API."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "项目下被选中的实例最大个数。仅ProjectId非空场景有效，限制项目下执行动作的最大实例数，以控制动作的影响面。默认100。取值1-100。",
        "en": "Maximum number of instances selected in the project. This parameter applies only when ProjectId is not empty. It limits the maximum number of instances affected by this operation to control impact scope. Default value: 100. Valid values: 1 to 100."
      }
    },
    {
      "name": "PatchId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要卸载的Patch包ID，仅win场景支持。\n1. 默认卸载StablePatchId\n2. 输入值origin，表示卸载原始版本\n3. 输入值all，表示卸载所有已安装版本",
        "en": "ID of the patch package to uninstall. Supported only on Windows.\n\n1. Default: uninstall the StablePatchId.\n\n2. Set to origin to uninstall the original version.\n\n3. Set to all to uninstall all installed versions."
      }
    },
    {
      "name": "ProjectId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "项目ID",
        "en": "Project ID"
      }
    },
    {
      "name": "RenderingInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云应用服务实例ID",
        "en": "Cloud application instance ID"
      }
    },
    {
      "name": "RenderingInstanceIds",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "云应用服务实例ID列表",
        "en": "List of cloud application instance IDs"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "卸载应用",
    "en": "Uninstall application"
  }
}