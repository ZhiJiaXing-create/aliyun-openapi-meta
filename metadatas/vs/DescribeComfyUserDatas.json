{
  "name": "DescribeComfyUserDatas",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FileName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询的文件的文件名。或要查询的目录（传入为路径格式，以/结尾）",
        "en": "The name of the file to query, or the folder to query. For a folder, provide the path ending with a forward slash (/)."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询列表的页码。",
        "en": "The page number of the list to query."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小。",
        "en": "The number of entries per page."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询用户数据集",
    "en": "DescribeComfyUserDatas"
  },
  "descriptions": {
    "zh": "不指定 StartTime 和 EndTime 时，默认读取过去24小时的数据，同时支持按指定的起止时间查询，两者需要同时指定。\n最大查询范围 31 天。\n\n* 支持批量域名查询，多个域名用英文逗号（,）分隔。\n* 最多可获取最近 90 天的数据。\n* 时间粒度为小时。",
    "en": "If StartTime and EndTime are not specified, data from the last 24 hours is read by default. To query a specific time range, you must specify both StartTime and EndTime. The maximum time range for a query is 31 days.\n\n- You can query multiple domain names in a batch. Separate the domain names with a comma (,).\n\n- You can retrieve data from the last 90 days.\n\n- The time granularity is one hour."
  }
}