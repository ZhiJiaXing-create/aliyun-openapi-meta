{
  "name": "UpdateCloudAppInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云应用ID，对应唯一的应用包",
        "en": "The ID of the cloud application, which corresponds to a unique application package."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "应用描述信息",
        "en": "The description of the application."
      }
    },
    {
      "name": "Patch",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "待上传的Patch包信息。\n1. PkgType=android时不支持;\n2. 同AppId同时只允许一个Patch上传中（即同AppId同时只允许一个Patch非终态)",
        "en": "Information about the patch package to upload.\n\n1. This parameter is not supported when PkgType is android.\n\n2. For the same AppId, only one patch can be in the process of uploading at a time. This means only one patch can be in a state other than its desired state."
      }
    },
    {
      "name": "PkgLabels",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "云应用标签，可多选，将重置云应用标签，\n1. 取值范围：\n  a. hot\n  b. game\n  c. app\n2. 特殊情况：\n  a. 删除所有标签：输入[\"NULL\"]",
        "en": "The tags for the cloud application. You can select multiple tags. This action resets all existing tags for the cloud application.\n\n1. Valid values:\n   hot, game, and app.\n\n2. Special case:\n   To delete all tags, enter \\[\"NULL\"]."
      }
    },
    {
      "name": "StablePatchId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "稳定版PatchId。客户业务使用中(如会话启动)未指定PatchId情况，默认使用的PatchId。PkgType=android时不支持。\n特殊取值说明：\n1. 输入值origin：表示取消patch版本，默认使用初始版本",
        "en": "The ID of the stable patch. This patch is used by default if you do not specify a PatchId when the application is in use, such as during a session startup. This parameter is not supported when PkgType is android.\nSpecial value:\n\n1. If you set this parameter to origin, the patch version is removed and the initial version is used."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新应用基本信息",
    "en": "Update Basic Application Information"
  }
}