{
  "name": "ListEdgeWorkers",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "续费场景必传",
        "en": "This parameter is required for renewal scenarios."
      }
    },
    {
      "name": "HiveIds",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "集群ID列表",
        "en": "The list of cluster IDs."
      }
    },
    {
      "name": "InstanceIds",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "负载ID列表",
        "en": "The list of payload IDs."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询列表的页码。起始值为1。默认值：1。",
        "en": "The page number of the query. The value starts from 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数，最大值为100行。默认值：10。",
        "en": "The number of entries to return on each page for a paged query. The maximum value is 100. Default value: 10."
      }
    },
    {
      "name": "PlanIds",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "套餐ID列表",
        "en": "The list of package IDs."
      }
    },
    {
      "name": "Spec",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "规格",
        "en": "Specification"
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "时间区间筛选参数。按照ISO8601标准表示，并需要使用UTC时间，格式为yyyy-MM-ddTHH:mm:ssZ。",
        "en": "The parameter for filtering by time range. The time must be in ISO 8601 format and use UTC. The format is yyyy-MM-ddTHH:mm:ssZ."
      }
    },
    {
      "name": "Statuses",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "状态列表",
        "en": "The list of statuses."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询负载信息",
    "en": "ListEdgeWorkers"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n- 该 API 用于查询云应用服务的负载信息，支持通过多种参数进行过滤和分页查询。\n- 可选参数包括 `Spec`（规格）、`Statuses`（状态列表）、`InstanceIds`（实例ID列表）、`PlanIds`（套餐ID列表）、`HiveIds`（集群ID列表）等。\n- 分页查询时，可以通过 `PageNumber` 和 `PageSize` 参数来控制返回的数据量，默认每页10条记录，最大支持100条记录。\n- 时间范围查询可通过 `StartTime` 和 `EndTime` 参数指定。",
    "en": "## Request description\n\n- This API queries payload information for cloud application services and supports filtering and paged queries using various parameters.\n\n- Optional parameters include `Spec`, `Statuses`, `InstanceIds`, `PlanIds`, and `HiveIds`.\n\n- For paged queries, you can use the `PageNumber` and `PageSize` parameters to control the amount of data returned. The default page size is 10 records, and the maximum is 100 records.\n\n- You can specify a time range for the query using the `StartTime` and `EndTime` parameters."
  }
}