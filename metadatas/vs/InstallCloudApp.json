{
  "name": "InstallCloudApp",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云应用ID",
        "en": "Cloud application ID"
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "项目下实例关联关系分页页码（分页查询默认按实例关联时间倒序）。仅ProjectId非空场景有效，限制项目下执行动作的最大实例数，以控制动作的影响面。默认1。\n1. 关于PageNumber取值范围：\n  a. 【推荐】方法一：根据项目关联实例总数（接口ListRenderingProjectInstances可获取），换算出上限值。\n  b. 方法二：可以通过接口返回值判断PageNumber是否达到Project上限值，【优点：不用换算取值范围】。接口返回满足以下任意一个条件时，就可以认为PageNumber达到上限值：\n    ⅰ. 接口返回403+错误码Code 200301\n    ⅱ. 接口的返回值SuccessInstanceCount+FailedInstanceCount<PageSize\n2. 场景举例：\n  a. 项目下实例全量安装场景：项目实例数大于PageSize（默认100）时，可以多次调用Install(+1递增分页PageNumber)以实现全量安装。项目实例安装进度可通过ListCloudAppInstallations接口获取。\n  b. 项目下新增实例安装场景：用PageNumber=1开始即可。分页查询默认按实例关联时间倒序，PageNumber=1分页下是最新新增实例。",
        "en": "Page number for paged queries of instance associations under the project. Paged queries default to reverse order by instance association time. This applies only when ProjectId is not empty. It limits the maximum number of instances for actions within the project, controlling the impact scope. Default is 1.\n\n1. PageNumber value range:\n   a. Method one (recommended): Calculate the upper limit using the total number of instances associated with the project. The ListRenderingProjectInstances interface provides this count.\n   b. Method two: Determine if PageNumber reaches the project's upper limit by checking the interface return value. This avoids calculating the range. PageNumber reaches the upper limit if the interface returns any of these conditions:\n   ⅰ. A 403 status code and error code 200301.\n   ⅱ. The sum of \\`SuccessInstanceCount\\` and \\`FailedInstanceCount\\` is less than \\`PageSize\\`.\n\n2. Scenario examples:\n   a. Full installation for project instances: If the number of project instances exceeds \\`PageSize\\` (default 100), invoke Install multiple times. Increment PageNumber by 1 for each call to complete the full installation. Get project instance installation progress using the ListCloudAppInstallations interface.\n   b. New instance installation for a project: Start with \\`PageNumber=1\\`. Paged queries default to reverse order by instance association time. The \\`PageNumber=1\\` page shows the latest new instances."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "项目下被选中的实例最大个数。仅ProjectId非空场景有效，限制项目下执行动作的最大实例数，以控制动作的影响面。默认100。取值1-100。",
        "en": "Maximum number of instances selected for the project. This applies only when ProjectId is not empty. It limits the maximum number of instances for actions within the project, controlling the impact scope. Default is 100. The value range is 1-100."
      }
    },
    {
      "name": "PatchId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要安装的Patch包ID，仅win场景支持。\n1. 默认安装StablePatchId\n2. 输入值origin，表示安装原始版本",
        "en": "Patch package ID to install. This is only for Windows scenarios.\n\n1. Install \\`StablePatchId\\` by default.\n\n2. Enter \\`origin\\` to install the original version."
      }
    },
    {
      "name": "ProjectId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "项目ID",
        "en": "Project ID"
      }
    },
    {
      "name": "RenderingInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云应用服务实例ID",
        "en": "Cloud application service instance ID"
      }
    },
    {
      "name": "RenderingInstanceIds",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "云应用服务实例ID列表",
        "en": "List of cloud application service instance IDs"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "安装应用",
    "en": "Install application"
  }
}