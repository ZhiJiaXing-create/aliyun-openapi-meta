{
  "name": "SendRenderingInstanceCommands",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Commands",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "shell命令字符串。支持输入多条命令，以\";\"/换行符分隔，\n\n- 禁用rm/reboot等危险命令",
        "en": "A shell command string. Enter multiple commands separated by semicolons (;) or line feeds.\n\n- Dangerous commands such as rm and reboot are disabled."
      }
    },
    {
      "name": "Mode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "命令响应模式，取值范围：\n1. Sync：同步响应。默认值。\n2. Async：异步响应",
        "en": "The response pattern for the command. Valid values:\n\n1. Sync: The response is returned synchronously. This is the default value.\n\n2. Async: The response is returned asynchronously."
      }
    },
    {
      "name": "RenderingInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云应用服务实例ID",
        "en": "The ID of the cloud application service instance."
      }
    },
    {
      "name": "Timeout",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "命令执行的超时时间，单位秒。取值范围与Mode响应模式取值有关：\n1. Mode=Sync，0-30，默认30\n2. Mode=Async，0-3600，默认300",
        "en": "The timeout period for command execution, in seconds. The value range depends on the Mode parameter:\n\n1. If Mode is set to Sync, the value range is 0 to 30. The default value is 30.\n\n2. If Mode is set to Async, the value range is 0 to 3600. The default value is 300."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "下发云应用服务实例控制指令",
    "en": "Send Control Command to Cloud Application Service Instance"
  }
}