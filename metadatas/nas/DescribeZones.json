{
  "name": "DescribeZones",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FileSystemType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件系统类型。\n\n取值：\n\n- standard（默认值）：通用型NAS。\n- extreme：极速型NAS。\n- cpfs：文件存储CPFS。",
        "en": "The type of the file system.\n\nValid value:\n\n*   standard: General-purpose Apsara File Storage NAS (NAS) file system\n*   extreme: Extreme NAS file system.\n*   cpfs: CPFS file system."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "可用区所在的地域ID。\n\n您可以调用DescribeRegions查看最新地域列表。",
        "en": "The ID of the region where you want to query zones.\n\nYou can call the DescribeRegions operation to query the latest region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询可用区",
    "en": "DescribeZones"
  },
  "example": {
    "unifiedCli": "aliyun nas describe-zones --biz-region-id cn-hangzhou --file-system-type standard --region cn-hangzhou",
    "legacyCli": "aliyun nas DescribeZones --region cn-hangzhou --FileSystemType standard --RegionId 'cn-hangzhou'"
  }
}