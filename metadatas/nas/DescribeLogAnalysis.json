{
  "name": "DescribeLogAnalysis",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FileSystemType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件系统类型。\n\n取值：\n\n- standard：通用型NAS\n- extreme：极速型NAS\n- all（默认值）：所有类型",
        "en": "The file system type.\n\nValid values:\n\n- standard: General-purpose NAS\n- extreme: Extreme NAS\n- all (default): all types."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "文件系统列表的分页页码。默认值为1。",
        "en": "The page number of the file system list. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，每个分页包含的文件系统个数。\n取值范围：1~100\n默认值：10",
        "en": "The number of file systems on each page in a paging query. Valid values: 1 to 100. Default value: 10."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取日志管理的日志信息",
    "en": "Retrieve log analysis information"
  },
  "example": {
    "unifiedCli": "aliyun nas describe-log-analysis --biz-region-id cn-hangzhou --page-size 1 --page-number 1 --file-system-type all --region cn-hangzhou",
    "legacyCli": "aliyun nas DescribeLogAnalysis --region cn-hangzhou --PageSize 1 --PageNumber 1 --FileSystemType all --RegionId 'cn-hangzhou'"
  }
}