{
  "name": "DeleteAccessGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccessGroupName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待删除的权限组名称。",
        "en": "The name of the access group to delete."
      }
    },
    {
      "name": "FileSystemType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件系统类型。\n\n取值：\n- standard（默认值）：通用型NAS\n- extreme：极速型NAS",
        "en": "The type of the file system.\n\nValid values:\n- standard (default): General-purpose NAS\n- extreme: Extreme NAS"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除权限组",
    "en": "Delete access group"
  },
  "descriptions": {
    "zh": "默认权限组（DEFAULT\\_VPC\\_GROUP\\_NAME）不支持删除。",
    "en": "The default access group (DEFAULT\\_VPC\\_GROUP\\_NAME) cannot be deleted."
  },
  "example": {
    "unifiedCli": "aliyun nas delete-access-group --access-group-name vpc-test --file-system-type standard --region cn-hangzhou",
    "legacyCli": "aliyun nas DeleteAccessGroup --region cn-hangzhou --AccessGroupName 'vpc-test' --FileSystemType standard"
  }
}