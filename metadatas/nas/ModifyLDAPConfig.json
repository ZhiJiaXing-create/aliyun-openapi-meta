{
  "name": "ModifyLDAPConfig",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BindDN",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "绑定LDAP的指定条目。",
        "en": "The LDAP entry."
      }
    },
    {
      "name": "FileSystemId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件系统ID。",
        "en": "The ID of the file system."
      }
    },
    {
      "name": "SearchBase",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "LDAP搜索起始点。",
        "en": "The LDAP search base."
      }
    },
    {
      "name": "URI",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "LDAP服务地址。",
        "en": "The LDAP service address."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改LDAP配置",
    "en": "用于修改LDAP配置"
  },
  "descriptions": {
    "zh": "本接口只支持CPFS并行文件系统。",
    "en": "The API operation is available only for Cloud Parallel File Storage (CPFS) file systems."
  },
  "example": {
    "unifiedCli": "aliyun nas modify-ldap-config --file-system-id 109c042666 --uri ldap://ldap.example.example --bind-dn cn=alibaba,dc=com --search-base dc=example --region cn-hangzhou",
    "legacyCli": "aliyun nas ModifyLDAPConfig --region cn-hangzhou --FileSystemId 109c042666 --SearchBase 'dc=example' --URI 'ldap://ldap.example.example' --BindDN 'cn=alibaba,dc=com'"
  }
}