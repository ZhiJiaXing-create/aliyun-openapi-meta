{
  "name": "DeleteAccessPoint",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccessPointId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "接入点ID。",
        "en": "The ID of the access point."
      }
    },
    {
      "name": "FileSystemId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件系统ID。",
        "en": "The ID of the file system."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除接入点",
    "en": "DeleteAccessPoint"
  },
  "descriptions": {
    "zh": "- 仅通用型NAS NFS协议文件系统支持该功能。\n- 删除接入点后，会立即中断当前正在通过接入点访问此目录的所有I/O，请谨慎操作。",
    "en": "*   Only General-purpose Network File System (NFS) file systems support access points.\n*   After an access point is deleted, all I/O operations that are being performed on the directory accessed over the access point are interrupted immediately. Exercise caution when you perform this operation."
  },
  "example": {
    "unifiedCli": "aliyun nas delete-access-point --file-system-id 1ca4040000 --access-point-id ap-ie15yd0000 --region cn-hangzhou",
    "legacyCli": "aliyun nas DeleteAccessPoint --region cn-hangzhou --AccessPointId 'ap-ie15yd0000' --FileSystemId 1ca4040000"
  }
}