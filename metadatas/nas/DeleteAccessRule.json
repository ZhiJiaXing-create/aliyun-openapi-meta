{
  "name": "DeleteAccessRule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccessGroupName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "权限组名称。",
        "en": "The name of the permission group."
      }
    },
    {
      "name": "AccessRuleId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "权限组规则ID。",
        "en": "The ID of the permission rule."
      }
    },
    {
      "name": "FileSystemType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件系统类型。\n\n取值：\n\n- standard（默认值）：通用型NAS。\n- extreme：极速型NAS。",
        "en": "The type of the file system.\n\nValid values:\n\n- standard (default): General-purpose NAS.\n- extreme: Extreme NAS."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除权限组规则",
    "en": "Delete a permission rule"
  },
  "descriptions": {
    "zh": "默认权限组（DEFAULT_VPC_GROUP_NAME）中的规则不支持删除。",
    "en": "Rules in the default permission group (DEFAULT_VPC_GROUP_NAME) cannot be deleted."
  },
  "example": {
    "unifiedCli": "aliyun nas delete-access-rule --access-group-name vpc-test --access-rule-id 1 --file-system-type standard --region cn-hangzhou",
    "legacyCli": "aliyun nas DeleteAccessRule --region cn-hangzhou --AccessGroupName 'vpc-test' --FileSystemType standard --AccessRuleId 1"
  }
}