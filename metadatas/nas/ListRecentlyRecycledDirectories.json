{
  "name": "ListRecentlyRecycledDirectories",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FileSystemId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件系统ID。",
        "en": "The ID of the file system."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "每次查询返回目录的个数。\n\n取值范围：10~1000\n\n默认值：100",
        "en": "The number of directories to return for each query.\n\nValid values: 10 to 1000.\n\nDefault value: 100."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "下一页标识，首次查询无需传入。\n\n如果一次查询结果并没有完整返回所有的目录，则返回NextToken不为空。您可以在查询中上传正确的NextToken继续查询。",
        "en": "The pagination token that is used in the next request to retrieve a new page of results. You do not need to specify this parameter for the first request.\n\nIf not all directories are returned in a query, the return value of the NextToken parameter is not empty. In this case, you can specify a valid value for the NextToken parameter to continue the query."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询最近删除的目录",
    "en": "ListRecentlyRecycledDirectories"
  },
  "descriptions": {
    "zh": "仅通用型NAS文件系统支持该功能。",
    "en": "Only General-purpose NAS file systems support this operation."
  },
  "example": {
    "unifiedCli": "aliyun nas list-recently-recycled-directories --file-system-id 1ca4040000 --next-token 1256000025 --max-results 100 --region cn-hangzhou",
    "legacyCli": "aliyun nas ListRecentlyRecycledDirectories --region cn-hangzhou --NextToken 1256000025 --MaxResults 100 --FileSystemId 1ca4040000"
  }
}