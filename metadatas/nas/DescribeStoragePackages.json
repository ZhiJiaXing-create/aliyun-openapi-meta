{
  "name": "DescribeStoragePackages",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "列表的分页页码。\n\n起始值（默认值）：1",
        "en": "The number of the page to return.\n\nPages start from page 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，每个分页包含的存储包个数。\n\n取值范围：1~100\n\n默认值：10",
        "en": "The number of storage plans to return on each page.\n\nValid values: 1 to 100.\n\nDefault value: 10."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "UseUTCDateTime",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "返回的时间是否按照UTC标准格式表示。\n\n取值：\n\n- true（默认值）：返回的时间为UTC标准格式。\n- false：返回Unix时间戳。",
        "en": "Specifies whether the time to return is in UTC.\n\nValid values:\n\n*   true (default): returns UTC time.\n*   false: returns UNIX timestamp."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询存储包列表信息",
    "en": "DescribeStoragePackages"
  },
  "example": {
    "unifiedCli": "aliyun nas describe-storage-packages --biz-region-id cn-hangzhou --page-size 10 --use-utc-date-time true --page-number 1 --region cn-hangzhou",
    "legacyCli": "aliyun nas DescribeStoragePackages --region cn-hangzhou --PageSize 10 --UseUTCDateTime true --PageNumber 1 --RegionId 'cn-hangzhou'"
  }
}