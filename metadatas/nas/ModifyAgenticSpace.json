{
  "name": "ModifyAgenticSpace",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AgenticSpaceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "AgenticSpace Id。",
        "en": "AgenticSpace Id。"
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性，从您的客户端生成一个参数值，确保不同请求间该参数值唯一。\n\nClientToken只支持ASCII字符，且不能超过64个字符。更多信息，请参见[如何保证幂等性](~~25693~~)。\n\n> 若用户未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样。",
        "en": "Ensures the idempotence of the request. Generate a unique parameter value from your client to ensure that the value is unique across different requests.\n\nClientToken supports only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~).\n\n> If you do not specify this parameter, the system automatically uses the RequestId of the API request as the ClientToken. The RequestId may differ for each API request."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "AgenticSpace 描述信息。",
        "en": "The description of the AgenticSpace."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否对此次创建请求执行预检。\n\n预检操作会帮助您检查参数有效性、校验库存等，并不会实际创建实例，也不会产生费用。\n\n取值：\n\n- true：发送检查请求，不会创建实例。检查项包括是否填写了必需参数、请求格式、业务限制和NAS库存。如果检查不通过，则返回对应错误。如果检查通过，则返回200 HttpCode，但FileSystemId为空。\n- false（默认）：发送正常请求，通过检查后直接修改实例。",
        "en": "Specifies whether to perform a dry run for this request.\n\nA dry run checks parameter validity and resource availability without actually modifying the instance or incurring charges.\n\nValid values:\n\n- true: Sends a check request without modifying the instance. The check items include whether required parameters are specified, request format, business limits, and NAS resource availability. If the check fails, the corresponding error is returned. If the check passes, HTTP status code 200 is returned, but FileSystemId is empty.\n- false (default): Sends a normal request and directly modifies the instance after the check passes."
      }
    },
    {
      "name": "FileSystemId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件系统ID。",
        "en": "The file system ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改Agentic空间",
    "en": "Modify an agentic space"
  },
  "descriptions": {
    "zh": "适用agentic类型文件系统。",
    "en": "Applicable to agentic file systems."
  },
  "example": {
    "unifiedCli": "aliyun nas modify-agentic-space --file-system-id 31a8e40000 --agentic-space-id agentic-229oypxjgpau20000 --description Agentic_Space_Description --dry-run false --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun nas ModifyAgenticSpace --region cn-hangzhou --Description Agentic_Space_Description --DryRun false --FileSystemId 31a8e40000 --AgenticSpaceId 'agentic-229oypxjgpau20000' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}