{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FileSystemType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件系统类型。\n\n取值范围：\n取值：\n- all：所有类型。\n- standard（默认值）：通用型NAS。\n- extreme：极速型NAS。\n- cpfs：CPFS。",
        "en": "The type of the file system.\n\nValid values:\n\n*   all: all types of file systems\n*   standard (default): General-purpose NAS file system\n*   extreme: Extreme NAS file system\n*   cpfs: Cloud Parallel File Storage (CPFS) file system\n\n> CPFS file systems are available only on the China site (aliyun.com)."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "列表的分页页码。\n\n起始值（默认值）：1",
        "en": "The page number.\n\nPages start from page 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，每页包含的地域数。\n\n取值范围：1~100\n\n默认值：10",
        "en": "The number of entries per page.\n\nValid values: 1 to 100.\n\nDefault value: 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询地域",
    "en": "DescribeRegions"
  },
  "example": {
    "unifiedCli": "aliyun nas describe-regions --page-size 10 --page-number 1 --file-system-type standard --region cn-hangzhou",
    "legacyCli": "aliyun nas DescribeRegions --region cn-hangzhou --PageSize 10 --PageNumber 1 --FileSystemType standard"
  }
}