{
  "name": "CreateDir",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FileSystemId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件系统ID。",
        "en": "The ID of the file system."
      }
    },
    {
      "name": "OwnerGroupId",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "目录的拥有者用户组ID。支持从0至4294967295的值。",
        "en": "The ID of the owner group for the directory. Valid values: 0 to 4294967295."
      }
    },
    {
      "name": "OwnerUserId",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "目录的拥有者用户ID。\n支持从0至4294967295的值。",
        "en": "The owner ID for the directory. Valid values: 0 to 4294967295."
      }
    },
    {
      "name": "Permission",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定应用到根目录路径的POSIX权限。\n格式为合法的八进制数，如0755。",
        "en": "The Portable Operating System Interface (POSIX) permissions applied to the root directory. The value is a valid octal number, such as 0755."
      }
    },
    {
      "name": "Recursion",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否创建多级目录。\n取值：\n\n- true（默认值）：如果多级目录不存在，逐级创建。\n- false：只创建最后一级目录，父目录不存在返回错误信息。",
        "en": "Specifies whether to create a multi-level directory. Valid values:\n\n*   true (default): If no multi-level directory exists, directories are created level by level.\n*   false: Only the last level of directory is created. An error message is returned because the parent directory does not exist."
      }
    },
    {
      "name": "RootDirectory",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目录名称。\n\n- 必须以正斜线（/）开头。\n- 支持数字、大小写字母。\n- 可以包含下划线（_）、短划线（-）或半角句号（.）。\n- 路径中不能包含软链接，如本层目录（.）、上层目录（..）或其他软链接。\n>  - 如果根目录不存在，则需要配置目录创建信息，系统将根据设置自动创建指定的根目录。\n > - 如果根目录已存在，则不需要配置目录创建信息，如果进行配置，目录创建信息中的配置也会被忽略。",
        "en": "The directory name.\n\n*   The directory must start with a forward slash (/).\n*   The directory can contain digits and letters.\n*   The directory can contain underscores (\\_), hyphens (-), and periods (.).\n*   The directory cannot contain symbolic links, such as the current directory (.), the upper-level directory (..), and other symbolic links.\n\n> *   If the root directory does not exist, configure the information for directory creation. The system then automatically creates the specified root directory based on your settings.\n> *  If the root directory exists, you do not need to configure the information for directory creation. The configurations for directory creation are ignored even if you configure the information."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建目录",
    "en": "CreateDir"
  },
  "descriptions": {
    "zh": "仅通用型NAS NFS协议文件系统支持该功能。",
    "en": "Only General-purpose Network File System (NFS) file systems support this operation."
  },
  "example": {
    "unifiedCli": "aliyun nas create-dir --file-system-id 31a8e40000 --root-directory /test --owner-user-id 1 --owner-group-id 123 --permission 0755 --recursion true --region cn-hangzhou",
    "legacyCli": "aliyun nas CreateDir --region cn-hangzhou --OwnerUserId 1 --Permission 0755 --FileSystemId 31a8e40000 --RootDirectory '/test' --OwnerGroupId 123 --Recursion true"
  }
}