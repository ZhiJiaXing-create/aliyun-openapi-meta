{
  "name": "AddClientToBlackList",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientIP",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待逐出的客户端IP地址。",
        "en": "The IP address of the client to add."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。\n\n更多详情，请参见[如何保证幂等性](~~25693~~)。",
        "en": "This parameter ensures the idempotency of each request. A ClientToken is generated for each client. Make sure that each ClientToken is unique between different requests. The parameter can be a maximum of 64 characters in length and contain ASCII characters.\n\nFor more information, see [How to ensure idempotence](https://www.alibabacloud.com/help/doc-detail/25693.htm)."
      }
    },
    {
      "name": "FileSystemId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件系统ID。",
        "en": "The ID of the file system."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件系统所属的地域ID。",
        "en": "The ID of the region where the file system resides."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "将客户端加入黑名单",
    "en": "AddClientToBlackList"
  },
  "descriptions": {
    "zh": "本接口只支持CPFS并行文件系统。",
    "en": "The API operation is available only for CPFS file systems."
  },
  "example": {
    "unifiedCli": "aliyun nas add-client-to-black-list --biz-region-id cn-hangzhou --file-system-id 1ca404a348 --client-ip 192.168.0.0 --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun nas AddClientToBlackList --region cn-hangzhou --FileSystemId 1ca404a348 --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --ClientIP '192.168.0.0'"
  }
}