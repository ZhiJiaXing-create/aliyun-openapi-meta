{
  "name": "DescribeFileSystemStatistics",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "列表的分页页码。\n\n起始值（默认值）：1",
        "en": "The page number.\n\nPages start from page 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，每个分页包含的文件系统统计信息的个数。\n\n取值范围：1~100\n\n默认值：10",
        "en": "The number of entries per page.\n\nValid values: 1 to 100.\n\nDefault value: 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "概览页获取数据",
    "en": "DescribeFileSystemStatistics"
  },
  "example": {
    "unifiedCli": "aliyun nas describe-file-system-statistics --page-size 10 --page-number 1 --region cn-hangzhou",
    "legacyCli": "aliyun nas DescribeFileSystemStatistics --region cn-hangzhou --PageSize 10 --PageNumber 1"
  }
}