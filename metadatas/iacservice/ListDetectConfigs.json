{
  "name": "ListDetectConfigs",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/terraformState/detectConfig",
  "parameters": [
    {
      "name": "detectConfigName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "偏差检测配置名称（模糊匹配）。",
        "en": "Name of the drift detection configuration (fuzzy match)."
      }
    },
    {
      "name": "maxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "一次获取的最大记录数。范围 1～200，默认 100。",
        "en": "Maximum number of records to retrieve in a single request. Valid range: 1–200. Default value: 100."
      }
    },
    {
      "name": "nextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询凭证（Token），取值为上一次API调用返回的NextToken参数值。",
        "en": "Query credential (Token). Set this parameter to the NextToken value returned by the previous API call."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列举偏差检测配置",
    "en": "List drift detection configurations."
  },
  "example": {
    "unifiedCli": "aliyun iacservice list-detect-configs --detect-config-name test --max-results 20 --next-token 30BaZ9ekYWXJdqshYecA++coNg7qT1Zbm3RfLyFIZeY= --api-version 2021-08-06 --region cn-hangzhou",
    "legacyCli": "aliyun iacservice GET /terraformState/detectConfig --region cn-hangzhou --maxResults 20 --nextToken '30BaZ9ekYWXJdqshYecA++coNg7qT1Zbm3RfLyFIZeY=' --detectConfigName test --header \"Content-Type=application/json\""
  }
}