{
  "name": "UploadModule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/modules/upload/[resourceType]",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模板内容",
        "en": "The template content."
      }
    },
    {
      "name": "moduleId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Module Id，当resourceType为Module 时有效并且必填。",
        "en": "The module ID. This parameter is valid and required when resourceType is set to Module."
      }
    },
    {
      "name": "moduleName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Module的名称。当resourceType为RegistryModule 时有效并且必填。",
        "en": "The name of the module. This parameter is valid and required when resourceType is set to RegistryModule."
      }
    },
    {
      "name": "namespaceName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "工作空间名称。当resourceType为RegistryModule 时有效并且必填。",
        "en": "The workspace name. This parameter is valid and required when resourceType is set to RegistryModule."
      }
    },
    {
      "name": "resourceType",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。可选值：\n-  Module\n-  RegistryModule",
        "en": "The type of the resource. Valid values:\n- Module\n- RegistryModule."
      }
    },
    {
      "name": "url",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "本地需要上传的zip文件\n不超过10MB",
        "en": "The local zip file to upload. The file size cannot exceed 10 MB."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "上传模板",
    "en": "Upload template"
  },
  "example": {
    "unifiedCli": "aliyun iacservice upload-module --resource-type RegistryModule --url url --namespace-name test_namespace --module-name RegistryModule-test0ef88 --module-id mod-kw1018ogp2m3qp22b3k31d --code key=example-string --api-version 2021-08-06 --region cn-hangzhou",
    "legacyCli": "aliyun iacservice POST /modules/upload/RegistryModule --region cn-hangzhou --moduleName 'RegistryModule-test0ef88' --moduleId 'mod-kw1018ogp2m3qp22b3k31d' --url url --namespaceName test_namespace --header \"Content-Type=application/json\" --body {\"code\":{\"key\":\"example-string\"}}"
  }
}