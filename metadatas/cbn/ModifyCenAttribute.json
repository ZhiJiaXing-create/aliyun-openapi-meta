{
  "name": "ModifyCenAttribute",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CenId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云企业网实例的ID。",
        "en": "The ID of the CEN instance."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云企业网实例的描述信息。\n\n描述可以为空或长度为1～256个字符，不能以http://或https://开头。",
        "en": "The description of the CEN instance.\n\nThe description can be empty or must be 1 to 256 characters in length. The description cannot start with http\\:// or https\\://."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云企业网实例的名称。\n\n名称可以为空或长度为1～128个字符，不能以http://或https://开头。",
        "en": "The name of the CEN instance.\n\nThe name can be empty or must be 1 to 128 characters in length. The name cannot start with http\\:// or https\\://."
      }
    },
    {
      "name": "ProtectionLevel",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置网段重叠的级别。\n\n取值：**REDUCED**（默认值），表示网段可以有重叠，但是不能完全相同。",
        "en": "The level of CIDR block overlap.\n\nSet the value to **REDUCED**. This is the default value. This value specifies that CIDR blocks can overlap but cannot be identical."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "编辑云企业网实例的名称和描述信息",
    "en": "ModifyCenAttribute"
  },
  "descriptions": {
    "zh": "**ModifyCenAttribute**接口属于异步接口，即系统会先返回一个**RequestId**，但该云企业网实例并未修改完成，系统后台的修改任务仍在进行。您可以调用**DescribeCens**接口查询云企业网实例的状态。 \n\n- 当云企业网实例处于**Modifying**状态时，表示云企业网实例正在修改中，在该状态下，您只能对云企业网实例执行查询操作，不能执行其他操作。\n- 当云企业网实例处于**Active**状态时，表示云企业网实例修改完成。",
    "en": "**ModifyCenAttribute** is an asynchronous operation. After you send a request, the system returns a **RequestId**, but the CEN instance is not modified immediately. The system modifies the instance in the background. You can call the **DescribeCens** operation to query the status of the CEN instance.\n\n- If a CEN instance is in the **Modifying** state, the modification is in progress. In this state, you can only query the instance and cannot perform other operations.\n\n- If a CEN instance is in the **Active** state, the modification is complete."
  },
  "example": {
    "unifiedCli": "aliyun cbn modify-cen-attribute --cen-id cen-7qthudw0ll6jmc0000 --name test --description cen --protection-level REDUCED --region cn-hangzhou",
    "legacyCli": "aliyun cbn ModifyCenAttribute --region cn-hangzhou --ProtectionLevel REDUCED --Description cen --CenId 'cen-7qthudw0ll6jmc0000' --Name test"
  }
}