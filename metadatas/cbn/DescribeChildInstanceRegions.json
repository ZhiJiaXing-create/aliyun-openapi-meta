{
  "name": "DescribeChildInstanceRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "支持的语言。包括以下取值：中文：zh-CN（默认值）                                 英文：en-US                                 日文：ja",
        "en": "The language of the response. Valid values:"
      }
    },
    {
      "name": "ProductType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "网络实例的类型，取值：\n\n- **VPC**：专有网络实例。\n\n- **VBR**：边界路由器实例。\n\n- **CCN**：云连接网实例。",
        "en": "The type of the network instance. Valid values:\n\n- **VPC**: a virtual private cloud (VPC).\n\n- **VBR**: a virtual border router (VBR).\n\n- **CCN**: a Cloud Connect Network (CCN) instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询云企业网支持加载网络实例的地域信息",
    "en": "DescribeChildInstanceRegions"
  },
  "descriptions": {
    "zh": "对于不同的网络实例类型，云企业网支持的地域不同。您可以通过指定`ProductType`参数，查询不同网络实例类型下云企业网支持的地域信息。如果您不指定`ProductType`参数，则系统默认查询所有网络实例类型下云企业网支持的地域信息。",
    "en": "The regions that CEN supports vary based on the network instance type. You can specify the `ProductType` parameter to query the regions that CEN supports for a specific type of network instance. If you do not specify the `ProductType` parameter, the system queries the regions supported for all network instance types by default."
  },
  "example": {
    "unifiedCli": "aliyun cbn describe-child-instance-regions --product-type VPC --accept-language zh-CN --region cn-hangzhou",
    "legacyCli": "aliyun cbn DescribeChildInstanceRegions --region cn-hangzhou --ProductType VPC --AcceptLanguage 'zh-CN'"
  }
}