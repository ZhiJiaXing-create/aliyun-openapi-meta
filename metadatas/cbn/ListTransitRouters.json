{
  "name": "ListTransitRouters",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CenId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云企业网实例ID。",
        "en": "The ID of the CEN instance."
      }
    },
    {
      "name": "FeatureFilter",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "转发路由器功能筛选字段。",
        "en": "The feature to be filtered."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "转发路由器功能筛选字段键值。\n目前支持字段：\n- **Multicast**：组播功能。",
            "en": "The key of the feature to be filtered.\nThe following key is supported:\n\n- **Multicast**: the multicast feature."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "RepeatList",
          "required": false,
          "description": {
            "zh": "转发路由器功能筛选字段值列表。\n当键值为**Multicast**时，只支持填入1个字段，支持字段内容为：\n- **Enabled**：支持组播。\n- **Disabled**：不支持组播。",
            "en": "A list of values for the feature.\nIf you set the key to **Multicast**, you can specify only one value. Valid values:\n\n- **Enabled**: Multicast is supported.\n\n- **Disabled**: Multicast is not supported."
          }
        }
      ]
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "列表的页码。默认值：**1**。",
        "en": "The page number. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页显示的条目数。取值范围：**1**~**50**。默认值：**10**。",
        "en": "The number of entries per page. Valid values: **1** to **50**. Default value: **10**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "转发路由器实例所属地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region where the transit router is deployed.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "转发路由器实例的状态。\n\n- **Creating**：创建中。\n- **Active**：可用。\n- **Modifying**：修改中。\n-  **Deleting**：删除中。\n-  **Upgrading**：升级中。",
        "en": "The status of the transit router.\n\n- **Creating**: The transit router is being created.\n\n- **Active**: The transit router is available.\n\n- **Modifying**: The transit router is being modified.\n\n- **Deleting**: The transit router is being deleted.\n\n- **Upgrading**: The transit router is being upgraded."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签信息。\n\n一次最多支持输入20个标签信息。",
        "en": "The tags.\n\nYou can specify up to 20 tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。\n\n一旦传入该值，则不允许为空字符串。最多支持64个字符，不能以`aliyun`或者`acs:`开头，不能包含`http://`或者`https:// `。\n\n一次最多支持输入20个标签的标签键。",
            "en": "The tag key.\n\nThe tag key cannot be an empty string. The tag key can be up to 64 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`.\n\nYou can specify up to 20 tag keys."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。\n\n标签值可以为空或输入不超过128个字符的字符串，不能以`aliyun`或者`acs:`开头，不能包含`http://`或者`https:// `。\n\n每一个标签键对应一个标签值，一次最多支持输入20个标签键对应的标签值。",
            "en": "The tag value.\n\nThe tag value can be an empty string or a string of up to 128 characters. It cannot start with `aliyun` or `acs:` and cannot contain `http://` or `https://`.\n\nEach tag key must have a unique tag value. You can specify up to 20 tag values."
          }
        }
      ]
    },
    {
      "name": "TransitRouterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "转发路由器实例ID。",
        "en": "The ID of the transit router."
      }
    },
    {
      "name": "TransitRouterName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "转发路由器实例的名称。\n\n名称长度为1~128个字符不能以`http://`或`https://`开头。",
        "en": "The name of the transit router.\n\nThe name must be 1 to 128 characters in length and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "转发路由器实例的类型。\n\n- **Enterprise**：企业版。\n- **Basic**：基础版。",
        "en": "The type of the transit router.\n\n- **Enterprise**: Enterprise Edition.\n\n- **Basic**: Basic Edition."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询云企业网实例下转发路由器实例的信息",
    "en": "ListTransitRouters"
  },
  "descriptions": {
    "zh": "在您调用本接口查询云企业网实例下转发路由器实例的信息时，您可以按需填写参数**RegionId**和**TransitRouterId**，关于这两个参数的关系，请参见以下说明：\n\n- 如果您未输入**RegionId**和**TransitRouterId**的值，则表示您要查询云企业网实例下所有转发路由器实例的信息。\n- 如果您只输入了**RegionId**的值，则表示您要查询云企业网实例指定地域下的转发路由器实例的信息。\n- 如果您只输入了**TransitRouterId**的值，则表示您要查询云企业网实例中指定的转发路由器实例的信息。",
    "en": "When you call this operation to query transit routers in a CEN instance, you can specify the **RegionId** and **TransitRouterId** parameters. Note the following information about these parameters:\n\n- If you do not specify **RegionId** or **TransitRouterId**, all transit routers in the CEN instance are queried.\n\n- If you specify only **RegionId**, the transit routers in the specified region are queried.\n\n- If you specify only **TransitRouterId**, the specified transit router is queried."
  },
  "example": {
    "unifiedCli": "aliyun cbn list-transit-routers --page-number 1 --page-size 10 --cen-id cen-j3jzhw1zpau2km0000 --transit-router-id tr-uf654ttymmljlvh2x0000 --biz-region-id cn-hangzhou --tag Value=TagValue Key=TagKey --status Active --type Enterprise --transit-router-name testname --feature-filter '[{\"Value\":[\"Enabled\"],\"Key\":\"Multicast\"}]' --region cn-hangzhou",
    "legacyCli": "aliyun cbn ListTransitRouters --region cn-hangzhou --Status Active --Tag.1.Key TagKey --Type Enterprise --PageSize 10 --PageNumber 1 --Tag.1.Value TagValue --FeatureFilter.1.Key Multicast --CenId 'cen-j3jzhw1zpau2km0000' --TransitRouterName testname --FeatureFilter.1.Value.1 Enabled --RegionId 'cn-hangzhou' --TransitRouterId 'tr-uf654ttymmljlvh2x0000'"
  }
}