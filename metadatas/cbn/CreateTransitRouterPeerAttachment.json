{
  "name": "CreateTransitRouterPeerAttachment",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPublishRouteEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否使企业版转发路由器自动发布跨地域连接的路由到对端地域。\n\n- **false**（默认值）：否。\n- **true**：是。",
        "en": "Specifies whether to enable the local Enterprise Edition transit router to automatically advertise the routes of the inter-region connection to the peer transit router. Valid values:\n\n- **false** (default): no.\n\n- **true**: yes."
      }
    },
    {
      "name": "Bandwidth",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "跨地域连接的带宽值。单位：Mbps。\n\n- 当**BandwidthType**取值为**BandwidthPackage**时，该参数表示跨地域连接可使用的带宽值。\n\n- 当**BandwidthType**取值为**DataTransfer**时，该参数表示跨地域连接的限速带宽值。",
        "en": "The bandwidth value of the inter-region connection. Unit: Mbps.\n\n- When **BandwidthType** is set to **BandwidthPackage**, this parameter specifies the bandwidth value that the inter-region connection can use.\n\n- When **BandwidthType** is set to **DataTransfer**, this parameter specifies the maximum bandwidth value of the inter-region connection."
      }
    },
    {
      "name": "BandwidthType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "跨地域连接的带宽分配方式。取值：\n\n- **BandwidthPackage**：从带宽包中分配带宽。\n\n- **DataTransfer**：不为跨地域连接分配带宽，按使用流量计费。",
        "en": "The method that is used to allocate bandwidth to the inter-region connection. Valid values:\n\n- **BandwidthPackage**: allocates bandwidth from a bandwidth plan.\n\n- **DataTransfer**: does not allocate bandwidth to the inter-region connection and charges based on pay-by-traffic."
      }
    },
    {
      "name": "CenBandwidthPackageId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "跨地域连接要绑定的带宽包ID。\n\n> 当**BandwidthType**取值为**DataTransfer**时，无需配置该项。",
        "en": "The ID of the bandwidth plan that you want to associate with the inter-region connection.\n\n> You do not need to configure this parameter when **BandwidthType** is set to **DataTransfer**."
      }
    },
    {
      "name": "CenId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云企业网实例ID。",
        "en": "The ID of the Cloud Enterprise Network (CEN) instance."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token used to ensure the idempotence of the request.\n\nYou can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** as the **ClientToken**. The **RequestId** is different for each API request."
      }
    },
    {
      "name": "DefaultLinkType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "默认链路类型。\n\n可选值为Platinum（铂金），Gold（金），默认为Gold（金）。\n\n且仅在带宽分配方式是按流量计费时，才可以配置为Platinum（铂金）。",
        "en": "The default line type.\n\nValid values: Platinum and Gold. Default value: Gold.\n\nYou can set this parameter to Platinum only when the bandwidth allocation method is pay-by-traffic."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否对此次请求执行预检，包括权限、实例状态校验等。取值：\n\n- **false**（默认值）：发送正常请求，通过检查后直接创建跨地域连接。\n- **true**：发送检查请求，只进行校验，不会创建跨地域连接。检查项包括是否填写了必需参数、请求格式等。如果检查不通过，则返回对应错误。如果检查通过，则返回对应请求ID。",
        "en": "Specifies whether to perform a dry run to check information such as the permissions and instance status. Valid values:\n\n- **false** (default): sends a normal request. After the request passes the check, the system creates an inter-region connection.\n\n- **true**: sends a check request. The system checks the required parameters and request syntax. If the request fails the dry run, an error message is returned. If the request passes the dry run, a request ID is returned."
      }
    },
    {
      "name": "PeerTransitRouterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "对端转发路由器实例ID。",
        "en": "The ID of the peer transit router instance."
      }
    },
    {
      "name": "PeerTransitRouterRegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "对端转发路由器实例所属地域ID。",
        "en": "The ID of the region where the peer transit router instance is deployed."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "本端企业版转发路由器实例所属地域ID。\n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region where the local Enterprise Edition transit router instance is deployed.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query region IDs."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签信息。\n\n一次最多支持输入20个标签信息。",
        "en": "The tag information.\n\nYou can specify at most 20 tags in each call."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。\n\n一旦传入该值，则不允许为空字符串。最多支持64个字符，不能以`aliyun`或者`acs:`开头，不能包含`http://`或者`https:// `。\n\n一次最多支持输入20个标签的标签键。",
            "en": "The key of the tag that you want to attach.\n\nYou cannot specify an empty string as a tag key. The tag key can be up to 64 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https:// `.\n\nYou can specify at most 20 tag keys in each call."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。\n\n标签值可以为空或输入不超过128个字符的字符串，不能以`aliyun`或者`acs:`开头，不能包含`http://`或者`https:// `。\n\n每一个标签键对应一个标签值，一次最多支持输入20个标签键对应的标签值。",
            "en": "The value of the tag that you want to attach to the specified resource.\n\nThe tag value can be an empty string or a string of up to 128 characters in length. It cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https:// `.\n\nEach key-value pair must be unique. You can specify at most 20 tag values in each call."
          }
        }
      ]
    },
    {
      "name": "TransitRouterAttachmentDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "跨地域连接的描述信息。\n\n描述可以为空或长度为1～256个字符，不能以http://或https://开头。",
        "en": "The description of the inter-region connection.\n\nThe description is optional. If you enter a description, it must be 1 to 256 characters in length, and cannot start with http\\:// or https\\://."
      }
    },
    {
      "name": "TransitRouterAttachmentName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "跨地域连接的名称。\n\n名称可以为空或长度为1～128个字符，不能以http://或https://开头。",
        "en": "The name of the inter-region connection.\n\nThe name can be empty or 1 to 128 characters in length, and cannot start with http\\:// or https\\://."
      }
    },
    {
      "name": "TransitRouterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "本端企业版转发路由器实例ID。",
        "en": "The ID of the local Enterprise Edition transit router instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为企业版转发路由器实例创建跨地域连接",
    "en": "CreateTransitRouterPeerAttachment"
  },
  "descriptions": {
    "zh": "- 企业版转发路由器支持以下两种带宽分配方式：\n\n  - **从带宽包分配**：\n\n    使用此种分配方式，您需要先购买带宽包，从带宽包中为跨地域连接分配带宽。购买带宽包具体操作，请参见[CreateCenBandwidthPackage](~~65919~~)。\n  - **按流量付费**：\n\n    使用此种分配方式，您需要为跨地域连接设置限速带宽，系统会按照跨地域连接实际使用的流量计费。具体计费信息，请参见[跨地域流量](~~337827~~)。\n\n- **CreateTransitRouterPeerAttachment**接口属于异步接口，即系统会先返回一个跨地域连接ID，但该跨地域连接并未创建完成，系统后台的创建任务仍在进行。您可以调用**ListTransitRouterPeerAttachments**接口查询跨地域连接的状态。 \n\n  - 当跨地域连接处于**Attaching**状态时，表示跨地域连接正在创建中，在该状态下，您只能对跨地域连接执行查询操作，不能执行其他操作。\n  - 当跨地域连接处于**Attached**状态时，表示跨地域连接创建完成。",
    "en": "- Enterprise Edition transit routers allow you to allocate bandwidth resources to inter-region connections using the following methods:\n\n  - **From bandwidth plan**:\n\n    You must purchase a bandwidth plan and then allocate bandwidth resources from the plan to inter-region connections. For more information about how to purchase a bandwidth plan, see [CreateCenBandwidthPackage](~~65919~~).\n\n  - **Pay-by-traffic**:\n\n    You can set a maximum bandwidth value for an inter-region connection. You are then charged based on the amount of data transferred over the connection. For more information about billing, see [Inter-region traffic](~~337827~~).\n\n- The **CreateTransitRouterPeerAttachment** operation is asynchronous. After you call this operation, the system returns an inter-region connection ID. However, the inter-region connection is not created immediately. The creation task runs in the background. You can call the **ListTransitRouterPeerAttachments** operation to query the status of the inter-region connection.\n\n  - When the inter-region connection is in the **Attaching** state, the connection is being created. In this state, you can only query the inter-region connection. You cannot perform other operations.\n\n  - When the inter-region connection is in the **Attached** state, the connection creation is complete."
  },
  "example": {
    "unifiedCli": "aliyun cbn create-transit-router-peer-attachment --client-token 02fb3da4-130e-11e9-8e44-0010000 --cen-id cen-j3jzhw1zpau2km0000 --transit-router-id tr-bp1su1ytdxtataupl0000 --biz-region-id cn-hangzhou --transit-router-attachment-name testname --transit-router-attachment-description testdesc --peer-transit-router-id tr-m5eq27g6bndum7e880000 --peer-transit-router-region-id cn-qingdao --auto-publish-route-enabled false --bandwidth 2 --cen-bandwidth-package-id cenbwp-3xrxupouolw5ou0000 --dry-run false --bandwidth-type BandwidthPackage --tag Value=value_A1 Key=tag_A1 --default-link-type Gold --region cn-hangzhou",
    "legacyCli": "aliyun cbn CreateTransitRouterPeerAttachment --region cn-hangzhou --Tag.1.Key tag_A1 --DryRun false --TransitRouterAttachmentName testname --DefaultLinkType Gold --AutoPublishRouteEnabled false --BandwidthType BandwidthPackage --Bandwidth 2 --Tag.1.Value value_A1 --CenId 'cen-j3jzhw1zpau2km0000' --RegionId 'cn-hangzhou' --PeerTransitRouterId 'tr-m5eq27g6bndum7e880000' --ClientToken '02fb3da4-130e-11e9-8e44-0010000' --CenBandwidthPackageId 'cenbwp-3xrxupouolw5ou0000' --TransitRouterAttachmentDescription testdesc --TransitRouterId 'tr-bp1su1ytdxtataupl0000' --PeerTransitRouterRegionId 'cn-qingdao'"
  }
}