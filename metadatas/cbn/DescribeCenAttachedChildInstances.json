{
  "name": "DescribeCenAttachedChildInstances",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CenId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云企业网实例ID。",
        "en": "The ID of the CEN instance."
      }
    },
    {
      "name": "ChildInstanceRegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "网络实例所属地域ID。                   \n\n您可以通过调用[DescribeRegions](~~36063~~)接口获取地域ID。",
        "en": "The ID of the region where the network instance is deployed.\n\nYou can call the [DescribeRegions](~~36063~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ChildInstanceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "网络实例的类型，取值：\n\n- **VPC**：专有网络实例。\n\n- **VBR**：边界路由器实例。\n\n- **CCN**：云连接网实例。",
        "en": "The type of the network instance. Valid values:\n\n- **VPC**: virtual private cloud (VPC)\n- **VBR**: virtual border router (VBR)\n- **CCN**: Cloud Connect Network (CCN) instance"
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询页码。默认值为**1**。",
        "en": "The number of the page to return. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页显示的条目数。默认值为**10**，取值范围：**1**~**50**。",
        "en": "The number of entries to return on each page. Default value: **10**. Valid values: **1** to **50**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询云企业网实例下已加载的网络实例信息",
    "en": "DescribeCenAttachedChildInstances"
  },
  "descriptions": {
    "zh": "通过本接口您可以使用以下三种方式查询云企业网实例已加载的网络实例的信息：\n\n- 通过指定查询`CenId`，查询云企业网实例下已加载的所有网络实例的信息。\n- 通过指定`CenId`和`ChildInstanceRegionId`，查询云企业网实例在某一个地域下已加载的网络实例的信息。\n- 通过指定`CenId`和`ChildInstanceType`，查询云企业网实例已加载的某一类型的网络实例的信息。",
    "en": "You can use one of the following methods to query the information about the network instances that are attached to a CEN instance:\n\n- You can query all the network instances that are attached to a CEN instance by setting the `CenId` parameter.\n- You can query the network instances that are attached to a CEN instance in a specified region by setting the `CenId` and `ChildInstanceRegionId` parameters.\n- You can query a specified type of network instances that are attached to a CEN instance by setting the `CenId` and `ChildInstanceType` parameters."
  },
  "example": {
    "unifiedCli": "aliyun cbn describe-cen-attached-child-instances --page-number 1 --page-size 10 --cen-id cen-j3jzhw1zpau2km0000 --child-instance-type VPC --child-instance-region-id cn-zhangjiakou --region cn-hangzhou",
    "legacyCli": "aliyun cbn DescribeCenAttachedChildInstances --region cn-hangzhou --ChildInstanceType VPC --PageSize 10 --PageNumber 1 --CenId 'cen-j3jzhw1zpau2km0000' --ChildInstanceRegionId 'cn-zhangjiakou'"
  }
}