{
  "name": "MoveResourceGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nThe client generates this value. Make sure that the value is unique among different requests. The token can contain a maximum of 64 ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the request as the **ClientToken**. The **RequestId** of each API request may be different."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n- **true**：发送检查请求，不会更改云企业网实例或带宽包实例所属的资源组。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: performs a dry run. The system checks the required parameters, the request format, and business limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\n- **false** (default): sends a normal request. If the request passes the check, an HTTP 2xx status code is returned and the operation is performed."
      }
    },
    {
      "name": "NewResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "新资源组ID。",
        "en": "The ID of the new resource group."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源ID。",
        "en": "The ID of the resource."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。取值：\n\n- **cen**：云企业网实例。\n- **bandwidthpackage**：带宽包实例。",
        "en": "The type of the resource. Valid values:\n\n- **cen**: a CEN instance.\n\n- **bandwidthpackage**: a bandwidth plan."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改云企业网实例或带宽包实例所属的资源组",
    "en": "MoveResourceGroup"
  },
  "descriptions": {
    "zh": "云企业网实例和带宽包实例默认归属于默认资源组，您可以通过`MoveResourceGroup`接口修改云企业网实例或带宽包实例所属的资源组。",
    "en": "By default, Cloud Enterprise Network (CEN) instances and bandwidth plans belong to the default resource group. You can call the `MoveResourceGroup` operation to move a CEN instance or a bandwidth plan to a different resource group."
  },
  "example": {
    "unifiedCli": "aliyun cbn move-resource-group --client-token 123e4560000 --dry-run false --new-resource-group-id rg-aekzqqurtfg0000 --resource-type cen --resource-id cen-nye53d7p3hzyu40000 --region cn-hangzhou",
    "legacyCli": "aliyun cbn MoveResourceGroup --region cn-hangzhou --NewResourceGroupId 'rg-aekzqqurtfg0000' --ResourceId 'cen-nye53d7p3hzyu40000' --DryRun false --ResourceType cen --ClientToken 123e4560000"
  }
}