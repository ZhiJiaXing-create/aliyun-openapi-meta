{
  "name": "ModifyTransitRouterMulticastDomain",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。",
        "en": "A client token that ensures the idempotence of the request.\n\nGenerate a unique token on your client for each request. The token can contain only ASCII characters."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n\n- **true**：发送检查请求，不会修改组播域的名称和描述信息。检查项包括是否填写了必需参数、请求格式、业务限制等。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后直接修改组播域的名称和描述信息。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: Performs a dry run. The system checks the required parameters, request format, and service limits. If the check fails, an error message is returned. If the check passes, the `DryRunOperation` error code is returned.\n\n- **false** (default): Sends the request. If the request passes the check, the name and description of the multicast domain are modified."
      }
    },
    {
      "name": "Options",
      "position": "Query",
      "type": "Struct",
      "required": false,
      "description": {
        "zh": "组播域功能选项。",
        "en": "The feature options of the multicast domain."
      }
    },
    {
      "name": "TransitRouterMulticastDomainDescription",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "组播域新的描述信息。\n\n描述可以为空或长度为1～256个字符，不能以http://或https://开头。",
        "en": "The new description of the multicast domain.\n\nThe description can be empty or 1 to 256 characters long. It cannot start with http\\:// or https\\://."
      }
    },
    {
      "name": "TransitRouterMulticastDomainId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "组播域ID。",
        "en": "The ID of the multicast domain."
      }
    },
    {
      "name": "TransitRouterMulticastDomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "组播域的新名称。\n\n名称可以为空或长度为1～128个字符，不能以http://或https://开头。",
        "en": "The new name of the multicast domain.\n\nThe name can be empty or 1 to 128 characters long. It cannot start with http\\:// or https\\://."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改组播域配置",
    "en": "ModifyTransitRouterMulticastDomain"
  },
  "example": {
    "unifiedCli": "aliyun cbn modify-transit-router-multicast-domain --client-token 123e4567-e89b-12d3-a456-42660000 --transit-router-multicast-domain-id tr-mcast-domain-40cwj0rgzgdtam0000 --transit-router-multicast-domain-name nametest --transit-router-multicast-domain-description desctest --dry-run false --options StrictSourceControl=enable Igmpv2Support=enable --region cn-hangzhou",
    "legacyCli": "aliyun cbn ModifyTransitRouterMulticastDomain --region cn-hangzhou --Options.Igmpv2Support enable --TransitRouterMulticastDomainId 'tr-mcast-domain-40cwj0rgzgdtam0000' --DryRun false --TransitRouterMulticastDomainName nametest --TransitRouterMulticastDomainDescription desctest --Options.StrictSourceControl enable --ClientToken '123e4567-e89b-12d3-a456-42660000' --method POST --force"
  }
}