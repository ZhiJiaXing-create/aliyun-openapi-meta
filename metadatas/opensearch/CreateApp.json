{
  "name": "CreateApp",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/v4/openapi/app-groups/[appGroupIdentity]/apps",
  "parameters": [
    {
      "name": "appGroupIdentity",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用名称",
        "en": "The name of the application."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body。",
        "en": "The request body."
      }
    },
    {
      "name": "dryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否dryRun创建（仅校验数据源是否合法）。取值：-true 是 -false 否",
        "en": "Specifies whether to perform a dry run. A dry run checks the validity of the data source but does not create the application. Valid values: true and false."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建一个应用版本",
    "en": "CreateApp"
  },
  "descriptions": {
    "zh": "- 若已存在同名标准版应用，则创建新版本；\n- 在新建版本情况下，autoSwitch 和 realtimeShared 也是必填的；\n- 在新建版本情况下，quota 是自动从上一个版本继承的；\n- 在新建版本情况下，修改 quota 是无效的。",
    "en": "- If a Standard Edition application with the same name already exists, a new version is created.\n\n- The autoSwitch and realtimeShared parameters are required to create a new version.\n\n- The quota for the new version is automatically inherited from the previous version.\n\n- You cannot modify the quota when you create the new version."
  }
}