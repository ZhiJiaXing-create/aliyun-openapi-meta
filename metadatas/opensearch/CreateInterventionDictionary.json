{
  "name": "CreateInterventionDictionary",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/v4/openapi/intervention-dictionaries",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求体",
        "en": "The request body."
      }
    },
    {
      "name": "dryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否校验入参数据合法性，默认false。\n\n取值：\n\n- **true**：只校验参数合法性。\n\n- **false**：校验参数合法性，创建归因配置。",
        "en": "Specifies whether to perform a dry run. The default value is false.\n\nValid values:\n\n- **true**: Validates the request parameters but does not create the intervention dictionary.\n\n- **false**: Validates the request parameters and creates the intervention dictionary."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建干预词典",
    "en": "CreateInterventionDictionary"
  }
}