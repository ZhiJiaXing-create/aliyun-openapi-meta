{
  "name": "SetPasswordPolicy",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DirectoryId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目录ID。",
        "en": "The ID of the directory."
      }
    },
    {
      "name": "MaxLoginAttempts",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "密码重试次数。\n\n连续输入错误密码达到设定次数后，账号将被锁定一小时。\n\n取值范围：0~32，0表示不限制密码重试次数。",
        "en": "The number of password retries.\n\nIf you enter wrong passwords for the specified consecutive times, the account is locked for 1 hour.\n\nValid values: 0 to 32. The value 0 specifies that the number of password retries is not limited."
      }
    },
    {
      "name": "MaxPasswordAge",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "密码有效期。\n\n取值范围：1~120。单位：天。",
        "en": "The validity period of a password.\n\nValid values: 1 to 120. Unit: days."
      }
    },
    {
      "name": "MinPasswordDifferentChars",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "密码中最少包含的不同字符数量。\n\n最小值为0，表示不限制密码中的不同字符数量。最大值为`MinPasswordLength`设置的值。",
        "en": "The minimum number of unique characters in a password.\n\nThe minimum value is 0, which specifies that the minimum number of unique characters in a password is not limited. The maximum value is the value of the `MinPasswordLength` parameter."
      }
    },
    {
      "name": "MinPasswordLength",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "最小密码长度。\n\n取值范围：8~32。",
        "en": "The minimum password length.\n\nValid values: 8 to 32 characters."
      }
    },
    {
      "name": "PasswordNotContainUsername",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "密码中是否不允许包含用户名。取值：\n\n- true：密码中不允许包含用户名。\n- false：密码中允许包含用户名。",
        "en": "Specifies whether a password can contain the username. Valid value:\n\n*   true: A password cannot contain the username.\n*   false: A password can contain the username."
      }
    },
    {
      "name": "PasswordReusePrevention",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "历史密码检查策略。\n\n禁止使用前N次的历史密码，N的取值范围：0~24，0表示不启用历史密码检查策略。\n> 2024年1月5日之前修改的密码不计入历史密码。",
        "en": "The policy for password history check.\n\nThe previous N passwords cannot be reused. Valid values of N: 0 to 24. The value 0 specifies that all historical passwords can be reused.\n\n>  Passwords that are generated before January 5, 2024 are not counted as historical passwords."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置密码策略",
    "en": "SetPasswordPolicy"
  },
  "example": {
    "unifiedCli": "aliyun cloudsso set-password-policy --directory-id d-00fc2p610000 --min-password-length 8 --min-password-different-chars 8 --password-not-contain-username true --max-password-age 90 --password-reuse-prevention 1 --max-login-attempts 5 --region cn-hangzhou",
    "legacyCli": "aliyun cloudsso SetPasswordPolicy --region cn-hangzhou --MinPasswordDifferentChars 8 --DirectoryId 'd-00fc2p610000' --PasswordNotContainUsername true --MaxPasswordAge 90 --PasswordReusePrevention 1 --MinPasswordLength 8 --MaxLoginAttempts 5"
  }
}