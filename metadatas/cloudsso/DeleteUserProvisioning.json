{
  "name": "DeleteUserProvisioning",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DeletionStrategy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "删除策略。删除RAM用户同步时，对已同步的RAM用户的处理策略。取值：\n\n- Delete：删除。删除RAM用户同步时，会删除从云SSO已经同步到RAM中的RAM用户。\n- Keep：保留。删除RAM用户同步时，会保留从云SSO已经同步到RAM中的RAM用户。\n\n> 该参数置空时，将采用创建RAM用户同步时设置的删除策略。",
        "en": "The deletion policy. The policy is used to manage synchronized users when you delete the RAM user provisioning. Valid values:\n\n- Delete: When you delete the RAM user provisioning, the system deletes the synchronized users.\n\n- Keep: When you delete the RAM user provisioning, the system retains the synchronized users.\n\n> If you do not specify this parameter, the deletion policy that is configured when you create the RAM user provisioning is used."
      }
    },
    {
      "name": "DirectoryId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目录ID。",
        "en": "The ID of the resource directory."
      }
    },
    {
      "name": "UserProvisioningId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "RAM用户同步的ID。",
        "en": "The ID of the RAM user provisioning."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除RAM用户同步",
    "en": "DeleteUserProvisioning"
  },
  "example": {
    "unifiedCli": "aliyun cloudsso delete-user-provisioning --directory-id d-003qew840000 --user-provisioning-id up-002axzhapcbz6e630000 --deletion-strategy Delete --region cn-hangzhou",
    "legacyCli": "aliyun cloudsso DeleteUserProvisioning --region cn-hangzhou --DirectoryId 'd-003qew840000' --UserProvisioningId 'up-002axzhapcbz6e630000' --DeletionStrategy Delete"
  }
}