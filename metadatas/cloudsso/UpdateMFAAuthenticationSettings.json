{
  "name": "UpdateMFAAuthenticationSettings",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DirectoryId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目录ID。",
        "en": "The ID of the directory."
      }
    },
    {
      "name": "MFAAuthenticationSettings",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全局MFA配置。取值：\n\n- Enabled：为所有用户启用MFA校验。\n- Byuser：依赖用户的MFA独立配置。关于用户MFA单独配置的详情，请参见[UpdateUserMFAAuthenticationSettings](~~450135~~)。\n- Disabled：为所有用户禁用MFA校验。\n- OnlyRiskyLogin：只在异常登录时验证。",
        "en": "Specifies whether to enable MFA for all users. Valid values:\n\n*   Enabled: enables MFA for all users.\n*   Byuser: uses user-specific settings. For more information about how to configure MFA for a single user, see [UpdateUserMFAAuthenticationSettings](~~450135~~).\n*   Disabled: disables MFA for all users.\n*   OnlyRiskyLogin: MFA is required only for unusual logons."
      }
    },
    {
      "name": "OperationForRiskLogin",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "当登录是否进行多因素配置选项值是只在异常登录时验证时。取值：\n\nAutonomous：异常登录时可以跳过绑定MFA, 但是已经绑定的必须验证MFA。\nEnforceVerify：未绑定MFA时需要强制绑定，已绑定的需验证。",
        "en": "Specifies whether MFA is required for users who initiated unusual logons. Valid values:\n\nAutonomous: MFA is prompted for users who initiated unusual logons. However, the users are allowed to skip MFA. If an MFA device is bound to a user who initiated an unusual logon, the user must pass MFA. EnforceVerify: MFA is required. If no MFA devices are bound to a user who initiated an unusual logon, the user must bind an MFA device. If an MFA device is already bound to a user who initiated an unusual logon, the user must pass MFA."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改全局MFA配置",
    "en": "UpdateMFAAuthenticationSettings"
  },
  "descriptions": {
    "zh": "当启用用户名密码登录时，您可以配置全局用户登录时的MFA校验策略。\n\n本文将提供一个示例，为目录`d-00fc2p61****`下所有云SSO用户启用MFA校验。",
    "en": "If you enable username-password logon for CloudSSO users, you can also configure MFA for the users.\n\nThis topic provides an example on how to enable MFA for all CloudSSO users that belong to the directory named `d-00fc2p61****`."
  },
  "example": {
    "unifiedCli": "aliyun cloudsso update-mfa-authentication-settings --directory-id d-00fc2p610000 --mfa-authentication-settings Enabled --operation-for-risk-login Autonomous --region cn-hangzhou",
    "legacyCli": "aliyun cloudsso UpdateMFAAuthenticationSettings --region cn-hangzhou --OperationForRiskLogin Autonomous --DirectoryId 'd-00fc2p610000' --MFAAuthenticationSettings Enabled"
  }
}