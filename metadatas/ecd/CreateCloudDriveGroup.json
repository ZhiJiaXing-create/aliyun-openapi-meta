{
  "name": "CreateCloudDriveGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AdminUserIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "要设置成团队空间管理员的用户Id列表。",
        "en": "List of user IDs to be set as team space administrators."
      }
    },
    {
      "name": "CdsId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "无影云盘ID。",
        "en": "The ID of the cloud disk in Cloud Drive Service."
      }
    },
    {
      "name": "GroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "组织团队ID。",
        "en": "The ID of the team."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The ID of the region."
      }
    },
    {
      "name": "TotalSize",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "无影云盘团队空间的容量。单位为Byte，默认不限大小。",
        "en": "The size of the cloud disk in Cloud Drive Service. Unit: bytes. The size is unlimited."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "添加团队空间",
    "en": "Add team space"
  },
  "descriptions": {
    "zh": "云盘团队列表是从无影客户端->用户管理->组织结构同步过来的，如果用户想要使用团队空间，需要在用户管理界面将用户移动到指定组织下，用户在云盘客户端可看到团队空间的菜单栏。",
    "en": "The disk team list is synchronized from the EDS client -> User Management -> organization chart. If a user wants to use a team space, you can move the user to the specified organization in the User Management interface. The user can then see the team space menu bar in the disk client."
  }
}