{
  "name": "QueryHistoryMetricDistribution",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndDate",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "统计结束日期。格式“YYYY-MM-DD”默认值T-1。",
        "en": "The end date of the query period. The date must be in the `YYYY-MM-DD` format. The default value is T-1."
      }
    },
    {
      "name": "MetricName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "监控指标类型。",
        "en": "The name of the metric to query."
      }
    },
    {
      "name": "Ranges",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "自定义数值区间",
        "en": "An array of custom value ranges."
      },
      "sub_parameters": [
        {
          "name": "IncludeMax",
          "position": "Query",
          "type": "Boolean",
          "required": false,
          "description": {
            "zh": "是否包含最大值，默认 false",
            "en": "Specifies whether to include the maximum value in the range. The default value is `false`."
          }
        },
        {
          "name": "IncludeMin",
          "position": "Query",
          "type": "Boolean",
          "required": false,
          "description": {
            "zh": "是否包含最小值，默认 true",
            "en": "Specifies whether to include the minimum value in the range. The default value is `true`."
          }
        },
        {
          "name": "Label",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "区间标签（用于返回值）",
            "en": "The label for the value range. This label is returned in the response."
          }
        },
        {
          "name": "Max",
          "position": "Query",
          "type": "Float",
          "required": false,
          "description": {
            "zh": "最大值",
            "en": "The maximum value of the value range."
          }
        },
        {
          "name": "Min",
          "position": "Query",
          "type": "Float",
          "required": false,
          "description": {
            "zh": "最小值",
            "en": "The minimum value of the value range."
          }
        }
      ]
    },
    {
      "name": "StartDate",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "统计起始日期。格式“YYYY-MM-DD”默认值T-1。",
        "en": "The start date of the query period. The date must be in the `YYYY-MM-DD` format. The default value is T-1."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "历史指标分布查询",
    "en": "QueryHistoryMetricDistribution"
  },
  "descriptions": {
    "zh": "## 请求说明\n该API用于查询给定日期范围内，特定监控指标（如CPU使用率、内存使用量等）的数值分布情况。用户可以通过自定义数值区间来获取更详细的统计数据。支持企业版和商业版两种业务渠道，默认为T-1日的数据统计。\n\n- **BusinessChannel**：默认为企业版，可选商业版。\n- **StartDate & EndDate**：默认值为T-1，即昨天的日期。格式必须为“YYYY-MM-DD”。\n- **MetricName**：需要查询的具体指标名称，请参照文档中提供的有效指标列表。\n- **Ranges**：允许用户自定义多个数值区间以进行更细致的数据分析。每个区间可以设置最小值、最大值以及是否包含边界值。",
    "en": "## Request\n\nThis API queries the value distribution for specific metrics, such as CPU usage and memory usage, within a given date range. You can define custom value ranges for more detailed statistics. The API supports both the enterprise edition and commercial edition. By default, it returns statistics for the previous day (T-1).\n\n- **BusinessChannel**: Defaults to the enterprise edition. The commercial edition is also available.\n\n- **StartDate & EndDate**: Both default to T-1 (the previous day). The date must be in the `YYYY-MM-DD` format.\n\n- **MetricName**: The metric to query. For a list of valid metrics, see the parameter description in this topic.\n\n- **Ranges**: Defines multiple value ranges for a more detailed analysis. For each range, you can set a minimum value, a maximum value, and whether to include these boundary values."
  }
}