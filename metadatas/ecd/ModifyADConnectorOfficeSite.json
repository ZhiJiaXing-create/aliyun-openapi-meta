{
  "name": "ModifyADConnectorOfficeSite",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AdHostname",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "域控主机名。\n必须符合Windows主机命名规范。",
        "en": "The hostname of the domain controller. The hostname must comply with the naming conventions for hostnames in Windows."
      }
    },
    {
      "name": "BackupDCHostname",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备用域控主机名。",
        "en": "The hostname of the secondary domain controller."
      }
    },
    {
      "name": "BackupDns",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备用域控DNS地址。",
        "en": "The IP address of the DNS server corresponding to the secondary domain controller."
      }
    },
    {
      "name": "DnsAddress",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "企业AD对应DNS的IP地址。目前仅支持设置一个。",
        "en": "The IP addresses of the DNS servers corresponding to the enterprise ADs. You can specify only one DNS IP address."
      }
    },
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "企业AD的域名。相同的域名只能注册一次。",
        "en": "The domain name of the enterprise AD system. You can register each domain name only once."
      }
    },
    {
      "name": "DomainPassword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "域管理员的用户密码。最长支持64个英文字符。",
        "en": "The password of the domain administrator. The username can be up to 64 characters in length."
      }
    },
    {
      "name": "DomainUserName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "域管理员的用户名。最长支持64个英文字符。\n\n> 用户名请使用sAMAccountName，不能使用userPrincipalName。",
        "en": "The username of the domain administrator. The username can be up to 64 characters in length.\n\n> Specify the value of the sAMAccountName parameter instead of the value of the userPrincipalName parameter as the username."
      }
    },
    {
      "name": "MfaEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启多因素认证MFA。",
        "en": "Specifies whether to enable multi-factor authentication (MFA).\n\nValid values:\n\n*   true\n\n    <!-- -->\n\n    <!-- -->\n\n    <!-- -->\n\n*   false\n\n    <!-- -->\n\n    <!-- -->\n\n    <!-- -->"
      }
    },
    {
      "name": "OUName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "AD域的组织单元OU。您可以调用[ListUserAdOrganizationUnits](~~311259~~)获取。",
        "en": "The name of the organizational unit (OU) in the AD domain. You can call the [ListUserAdOrganizationUnits](~~311259~~) operation to obtain OUs."
      }
    },
    {
      "name": "OfficeSiteId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "办公网络ID。",
        "en": "The office network ID."
      }
    },
    {
      "name": "OfficeSiteName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "办公网络名称。长度为2~255个英文或中文字符。必须以大小字母或中文开头，不能以`http://`和`https://`开头。可以包含数字、半角冒号（:）、下划线（_）或者连字符（-）。",
        "en": "The office network name. The name must be 2 to 255 characters in length. It can contain letters, digits, colons (:), underscores (\\_), and hyphens (-). It must start with a letter and cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。可以调用[DescribeRegions](~~DescribeRegions~~)获取无影云电脑支持的地域列表。",
        "en": "The region ID. You can call the [DescribeRegions](~~DescribeRegions~~) operation to query the list of regions where Elastic Desktop Service (EDS) Enterprise is available."
      }
    },
    {
      "name": "SubDomainDnsAddress",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "企业AD子域的DNS地址。目前仅支持设置一个。如果设置了`SubDomainName`，而未设置此参数，则默认为子域DNS和父域的一致。",
        "en": "The IP addresses of the DNS servers corresponding to the enterprise AD subdomains. You can specify only one DNS IP address. If you specify `SubDomainName` and leave this parameter empty, the value is the same as that of the enterprise AD domain."
      }
    },
    {
      "name": "SubDomainName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "企业AD子域的域名。",
        "en": "The name of the subdomain in the enterprise AD domain."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改AD办公网络",
    "en": "ModifyADConnectorOfficeSite"
  },
  "descriptions": {
    "zh": "仅状态为错误`ERROR`或者注册中`REGISTERING`的AD办公网络支持修改域名和DNS相关参数，包括`DomainName`、`SubDomainName`、`DnsAddress.N`、`SubDomainDnsAddress.N`。",
    "en": "You can modify parameters of domain names and Domain Name System (DNS) for enterprise AD office networks that are in the `ERROR` or `REGISTERED` state. The parameters include `DomainName`, `SubDomainName`, `DnsAddress.N`, and `SubDomainDnsAddress.N`."
  }
}