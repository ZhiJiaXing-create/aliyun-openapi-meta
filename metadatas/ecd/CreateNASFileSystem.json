{
  "name": "CreateNASFileSystem",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "NAS文件系统描述。",
        "en": "Description of the NAS file system."
      }
    },
    {
      "name": "EncryptType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "文件系统是否使用KMS服务托管密钥，对文件系统落盘数据进行加密存储。在读写加密数据时，无需解密。",
        "en": "Whether the file system is encrypted. Uses KMS service-managed keys to encrypt the file system's on-disk data. No decryption is required when reading and writing encrypted data."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "NAS文件系统名称。文件名称需遵循以下规则：长度为2\\~128个英文或中文字符。必须以大小字母或中文开头，不能以`http://`和`https://`开头。可以包含数字、下划线（_）或短划线（-）。",
        "en": "Name of the NAS file system.\nThe file name must follow these rules:\n\n- Length: 2 to 128 English or Chinese characters.\n\n- Must start with an uppercase or lowercase letter or a Chinese character, cannot start with http\\:// or https\\://.\n\n- Can include numbers, underscores (\\_), or hyphens (-)."
      }
    },
    {
      "name": "OfficeSiteId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "办公网络ID。",
        "en": "Workspace ID."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。可以调用[DescribeRegions](~~DescribeRegions~~)获取无影云电脑支持的地域列表。",
        "en": "Region ID."
      }
    },
    {
      "name": "StorageType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "NAS文件系统的存储规格类型。",
        "en": "Storage specification type of the NAS file system."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建NAS文件系统",
    "en": "CreateNASFileSystem"
  },
  "descriptions": {
    "zh": "- 每个标准办公网络可以创建一个NAS文件系统，用于满足办公网络内云电脑之间共享文件的需求。\n- 系统将自动创建一个通用型的NAS文件系统（存储规格为容量型和性能型，容量分别为10 PiB和1 PiB），并生成一个默认的挂载点。\n- NAS文件系统默认采用按量付费，您需要为实际使用的存储用量付费。您也可以搭配购买资源包来抵扣存储用量。\n\n更多信息，请参见[创建共享存储NAS](~~214481~~)。",
    "en": "<props=\"china\">\n\n- Each standard workspace can create one NAS file system to meet the need for sharing files between cloud desktops in the workspace.\n\n- The system will automatically create a general-purpose NAS file system (with storage specifications of Capacity and Performance, with capacities of 10 PiB and 1 PiB respectively) and generate a default mount point.\n\n- The NAS file system uses pay-as-you-go by default. You need to pay for the actual storage usage. You can also purchase resource packages to offset the storage usage.\n\nFor more information, see [Creating Shared Storage NAS](~~214481~~).\n\n</props>\n\n<props=\"intl\">\n\n- Each standard workspace can create one NAS file system to meet the need for sharing files between cloud desktops in the workspace.\n\n- The system will automatically create a general-purpose NAS file system (with storage specifications of Capacity and Performance, with capacities of 10 PiB and 1 PiB respectively) and generate a default mount point.\n\n- The NAS file system uses pay-as-you-go by default. You need to pay for the actual storage usage. You can also purchase storage packages to offset the storage usage.\n\nFor more information, see [Creating Shared Storage NAS](~~214481~~).\n\n</props>"
  }
}