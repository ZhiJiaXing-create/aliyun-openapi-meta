{
  "name": "ResetSnapshot",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "SnapshotId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "用来恢复磁盘数据的快照ID。",
        "en": "The ID of the snapshot used to recover disk data."
      }
    },
    {
      "name": "StopDesktop",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否关机并恢复快照。",
        "en": "Indicates whether to shut down and recover the disk using the snapshot."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "恢复快照对应的磁盘数据",
    "en": "Recover disk data from the snapshot"
  },
  "descriptions": {
    "zh": "调用该接口时，请您确保：\n\n- 磁盘中需要保存的数据已备份。\n    > 恢复数据是不可逆操作。调用该接口后，磁盘将恢复到快照创建时间点对应的状态，从快照创建时间到当前时间之间的数据将会丢失，请务必做好重要数据的备份。\n\n- 磁盘所属的云电脑已关机。",
    "en": "When calling this interface, ensure the following:\n\n- You must back up any data on the disk that you want to save.\n\n  > Data recovery is an irreversible operation. After you call this interface, the disk will revert to its state at the time the snapshot was created. Any data created between the snapshot creation time and the current time will be lost. Therefore, back up important data.\n\n- You must shut down the cloud computer to which the disk belongs."
  }
}