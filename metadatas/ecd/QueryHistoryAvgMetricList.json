{
  "name": "QueryHistoryAvgMetricList",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DataDate",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "统计起始日期。格式“YYYY-MM-DD”，默认值T-1。",
        "en": "The start date of the statistics. The format is `YYYY-MM-DD`. The default value is T-1."
      }
    },
    {
      "name": "DesktopId",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "桌面ID列表，最多支持100个",
        "en": "The desktop ID list. A maximum of 100 IDs are supported."
      }
    },
    {
      "name": "MetricName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "监控指标类型。",
        "en": "The metric to query."
      }
    },
    {
      "name": "PageNum",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，默认值1，>0",
        "en": "The page number, which must be greater than 0. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页大小，默认值20，1-200",
        "en": "The page size. Valid values: 1 to 200. Default value: 20."
      }
    },
    {
      "name": "Range",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自定义数值区间列表",
        "en": "The list of custom numeric ranges."
      }
    },
    {
      "name": "ResourceRegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "桌面Region",
        "en": "The desktop region."
      }
    },
    {
      "name": "SortType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序方式",
        "en": "The sorting method."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "历史平均监控指标查询",
    "en": "QueryHistoryAvgMetricList"
  },
  "descriptions": {
    "zh": "## 请求说明\n- 本接口用于获取特定时间段内，根据给定的条件筛选出的桌面资源的历史平均监控指标。\n- `DataDate` 和 `EndDate` 参数定义了查询的时间范围，默认为前一天。\n- 支持通过桌面ID、名称等进行过滤，并且可以自定义数值区间来进一步细化查询结果。\n- 返回结果包括每个符合条件的桌面资源的详细信息及其对应的平均值。\n- 分页参数`PageNum`和`PageSize`允许用户控制返回的数据量，以适应不同的展示需求。\n- 注意：当使用`Ranges`参数时，请确保提供的区间合理，否则可能影响查询性能或导致无效的结果。",
    "en": "## Request\n\n- This API retrieves historical average monitoring metrics for desktop resources that meet specified criteria within a time range.\n\n- The `DataDate` and `EndDate` parameters specify the time range. If omitted, the query defaults to the previous day.\n\n- You can filter results by criteria such as desktop ID, name, and custom numeric ranges.\n\n- The response contains the details of each matching desktop resource and its corresponding average values.\n\n- Use the `PageNum` and `PageSize` pagination parameters to control the number of results returned.\n\n- Note: When using the `Ranges` parameter, ensure that you provide reasonable intervals. Unreasonable intervals can degrade query performance or lead to invalid results."
  }
}