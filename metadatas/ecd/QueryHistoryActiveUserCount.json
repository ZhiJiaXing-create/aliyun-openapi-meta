{
  "name": "QueryHistoryActiveUserCount",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DataDate",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "统计日期。格式“YYYY-MM-DD”默认值T-1。",
        "en": "The date for which to query data, in YYYY-MM-DD format. Default: the previous day (T-1)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询历史日活月活用户数",
    "en": "QueryHistoryActiveUserCount"
  },
  "descriptions": {
    "zh": "## 请求说明\n- `AliUid` 参数将从AK/SK中自动解析获得，无需手动提供。\n- `BusinessChannel` 默认为企业版，但也可以选择其他业务渠道。\n- `DataDate` 支持自定义统计日期，默认为前一天（T-1）。请确保输入格式符合\"YYYY-MM-DD\"。",
    "en": "## Usage notes\n\n- The `AliUid` parameter is automatically resolved from your AccessKey pair and does not need to be specified in the request.\n\n- The `BusinessChannel` parameter defaults to Enterprise Edition, but you can select other business channels.\n\n- By default, the query returns data for the previous day (T-1). To query for a different day, use the `DataDate` parameter in YYYY-MM-DD format."
  }
}