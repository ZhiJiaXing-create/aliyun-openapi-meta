{
  "name": "CreateSnapshot",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "快照描述。长度不能超过128个英文或中文字符。",
        "en": "The description of the snapshot. The description can be up to 128 characters in length."
      }
    },
    {
      "name": "DesktopId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云电脑ID。",
        "en": "The ID of the cloud computer."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。可以调用[DescribeRegions](~~196646~~)获取无影云电脑支持的地域列表。",
        "en": "The region ID. You can call the [DescribeRegions](~~196646~~) operation to query the most recent region list."
      }
    },
    {
      "name": "SnapshotName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "快照的显示名称。长度为2\\~127个英文或中文字符。必须以大小写字母或中文开头。可以包含数字、下划线（\\_）或者短划线（-）。为防止和自动快照的名称冲突，不能以`auto`开头。",
        "en": "The name of the snapshot. The name must be 2 to 127 characters in length. The name must start with a letter. The name can contain letters, digits, underscores (\\_), and hyphens (-). The name cannot start with `auto` because snapshots whose names start with auto are recognized as automatic snapshots."
      }
    },
    {
      "name": "SourceDiskType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要创建快照的磁盘类型。",
        "en": "The type of the disk for which you want to create a snapshot.\n\nValid values:\n\n- system: system disk\n\n  <!-- -->\n\n  <!-- -->\n\n  <!-- -->\n\n- data: data disk\n\n  <!-- -->\n\n  <!-- -->\n\n  <!-- -->"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为云电脑的磁盘创建一份快照",
    "en": "CreateSnapshot"
  },
  "descriptions": {
    "zh": "云电脑的状态必须为**运行中**或**已停止**。",
    "en": "The cloud computer must be in the **Running** or **Stopped** state."
  }
}