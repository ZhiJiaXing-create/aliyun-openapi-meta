{
  "name": "QueryHistoryUsageDurationRank",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BizType",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "业务类型。",
        "en": "The business type."
      }
    },
    {
      "name": "EndDate",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询结束日期。格式：YYYY-MM-DD。最多支持查询 90 天数据",
        "en": "The end date of the query in `YYYY-MM-DD` format. You can query data within the last 90 days."
      }
    },
    {
      "name": "Limit",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询结果数量。默认值为 5，最大为 200",
        "en": "The number of entries to return. The default value is 5, and the maximum value is 200."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "下次分页查询的 Token，从上一次返回结果中获取",
        "en": "The token that is used to retrieve the next page of results. You can obtain this token from the response to the previous request."
      }
    },
    {
      "name": "StartDate",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询开始日期。格式：YYYY-MM-DD。最多支持查询 90 天数据",
        "en": "The start date of the query in `YYYY-MM-DD` format. You can query data within the last 90 days."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "桌面历史使用时长排行榜查询",
    "en": "QueryHistoryUsageDurationRank"
  },
  "descriptions": {
    "zh": "## 请求说明\n- **日期范围**：支持查询最多90天内的数据。\n- **分页查询**：通过`NextToken`参数实现分页，从上一次返回结果中获取。\n- **默认与最大限制**：默认返回5条记录，最大支持200条。\n- **鉴权方式**：使用AccessKey进行身份验证。\n- **调用方账号信息**：无需手动传入AliUid，系统将自动解析。\n- **计费情况**：此API调用免费。",
    "en": "## Usage notes\n\n- **Date range**: You can query data within the last 90 days.\n\n- **Pagination**: This operation uses the`NextToken` parameter for pagination. To retrieve the next page of results, use the `NextToken` value from the previous response.\n\n- **Default and maximum limits**: This operation returns 5 records by default, with a maximum of 200 records per page.\n\n- **Authentication**: This operation uses an AccessKey for authentication.\n\n- **Caller account information**: You do not need to specify an Alibaba Cloud account ID (AliUid). The system automatically resolves it.\n\n- **Billing**: This API operation is free of charge."
  }
}