{
  "name": "GetScheduledInstances",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "页码，默认为1。",
        "en": "The page number. Pages start from 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "每页数量，默认为20，最大100。",
        "en": "The number of records on each page. Default value: 20. Maximum value: 100."
      }
    },
    {
      "name": "ScheduledId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "定时巡检配置ID。",
        "en": "The ID of the scheduled inspection configuration."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询指定定时巡检配置中包含的所有实例ID列表。",
    "en": "GetScheduledInstances"
  }
}