{
  "name": "GetStandAloneReports",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "结束时间过滤（格式：YYYY-MM-DDTHH:mm:ssZ），不传则不过滤。",
        "en": "The end of the query's time range. The time must be in UTC and in the `YYYY-MM-DDTHH:mm:ssZ` format. If omitted, no end time filter is applied."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "页码，默认为1。",
        "en": "The number of the page to return. The default value is 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "每页数量，默认为20，最大100。",
        "en": "The number of entries to return on each page. Default: 20. Maximum: 100."
      }
    },
    {
      "name": "ReportType",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "开始时间过滤（格式：YYYY-MM-DDTHH:mm:ssZ），不传则不过滤。",
        "en": "The start of the query's time range. The time must be in UTC and in the `YYYY-MM-DDTHH:mm:ssZ` format. If omitted, no start time filter is applied."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询指定用户下所有非定时任务的单独巡检报告列表",
    "en": "GetStandAloneReports"
  }
}