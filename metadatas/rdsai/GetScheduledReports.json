{
  "name": "GetScheduledReports",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "结束时间过滤（格式：YYYY-MM-DDTHH:mm:ssZ），不传则不过滤。",
        "en": "The end time of the query range. The time must be in the YYYY-MM-DDTHH:mm:ssZ format."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "页码，默认为1。",
        "en": "The page number. The default value is 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "每页数量，默认为20，最大100。",
        "en": "The number of entries per page. The default value is 20, and the maximum value is 100."
      }
    },
    {
      "name": "ScheduledId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "巡检任务ID。",
        "en": "The ID of the inspection task."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "开始时间过滤（格式：YYYY-MM-DDTHH:mm:ssZ），不传则不过滤。",
        "en": "The start time of the query range. The time must be in the YYYY-MM-DDTHH:mm:ssZ format."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询指定定时任务下的所有巡检报告列表",
    "en": "GetScheduledReports"
  }
}