{
  "name": "ListStoreViews",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/storeviews",
  "parameters": [
    {
      "name": "name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于模糊搜索的数据集名称。",
        "en": "The dataset name that is used for fuzzy match."
      }
    },
    {
      "name": "offset",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "返回结果在全部数据集中的偏移位置， 默认为 0 。",
        "en": "The offset of the datasets to return. Default value: 0."
      }
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "日志项目名称。",
        "en": "The name of the project."
      }
    },
    {
      "name": "size",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "期望返回的数据集个数， 默认值为 100。",
        "en": "The number of datasets to return. Default value: 100."
      }
    },
    {
      "name": "storeType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据集类型，默认不根据类型过滤。",
        "en": "The type of the datasets to return. By default, datasets are not filtered by type.\n\nValid values:\n\n- metricstore\n\n  <!-- -->\n\n  <!-- -->\n\n  <!-- -->\n\n- logstore\n\n  <!-- -->\n\n  <!-- -->\n\n  <!-- -->"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询数据集列表",
    "en": "ListStoreViews"
  },
  "example": {
    "unifiedCli": "aliyun sls list-store-views --project example-project --size 100 --offset 0 --name my_storeview --store-type logstore --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /storeviews --project example-project --region cn-hangzhou --storeType logstore --size 100 --offset 0 --name my_storeview --header \"Content-Type=application/json\""
  }
}