{
  "name": "ListS3Ingestions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/s3ingestions",
  "parameters": [
    {
      "name": "logstore",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Logstore名称",
        "en": "The name of the Logstore."
      }
    },
    {
      "name": "offset",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "返回结果在全部数据集中的偏移位置， 默认为 0 。",
        "en": "The starting position of the results to return. The default value is 0."
      }
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": false,
      "description": {
        "zh": "project名称。",
        "en": "The name of the project."
      }
    },
    {
      "name": "size",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，设置的每页行数。",
        "en": "The number of rows per page for a paged query."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列出OSS导入任务",
    "en": "ListS3Ingestions"
  },
  "example": {
    "unifiedCli": "aliyun sls list-s3-ingestions --project testproject --offset 0 --size 100 --logstore test --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /s3ingestions --project testproject --region cn-hangzhou --offset 0 --size 100 --logstore test --header \"Content-Type=application/json\""
  }
}