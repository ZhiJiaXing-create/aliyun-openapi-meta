{
  "name": "GetS3Ingestion",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/s3ingestions/[s3IngestionName]",
  "parameters": [
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "project名称",
        "en": "The name of the project."
      }
    },
    {
      "name": "s3IngestionName",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "s3导入任务名称",
        "en": "The name of the S3 import task."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取S3导入任务",
    "en": "GetS3Ingestion"
  },
  "example": {
    "unifiedCli": "aliyun sls get-s3-ingestion --project test --s3-ingestion-name test --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /s3ingestions/test --project test --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}