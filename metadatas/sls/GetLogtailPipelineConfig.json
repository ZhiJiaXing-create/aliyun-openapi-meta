{
  "name": "GetLogtailPipelineConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/pipelineconfigs/[configName]",
  "parameters": [
    {
      "name": "configName",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Logtail流水线配置名称",
        "en": "The name of the Logtail pipeline configuration."
      }
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Project名称",
        "en": "The name of the project."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取Logtail流水线配置",
    "en": "GetLogtailPipelineConfig"
  },
  "descriptions": {
    "en": "The UK (London) region is supported. Supported regions are constantly updated."
  },
  "example": {
    "unifiedCli": "aliyun sls get-logtail-pipeline-config --project test-project --config-name test-config --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /pipelineconfigs/test-config --project test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}