{
  "name": "DeleteLogging",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/logging",
  "parameters": [
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Project名称。",
        "en": "The name of the project."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除指定服务日志",
    "en": "DeleteLogging"
  },
  "descriptions": {
    "zh": "Host由Project名称和日志服务Endpoint构成，您需要在Host中指定Project。",
    "en": "The host must be specified as a combination of the project name and the Simple Log Service endpoint."
  },
  "example": {
    "unifiedCli": "aliyun sls delete-logging --project test-project --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE /logging --project test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}