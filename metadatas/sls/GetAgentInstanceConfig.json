{
  "name": "GetAgentInstanceConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/agentinstanceconfigs/[configType]",
  "parameters": [
    {
      "name": "attributes",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "进程级配置归属",
        "en": "The owner of the process-level configuration."
      }
    },
    {
      "name": "configType",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "进程级配置类型",
        "en": "The type of the process-level configuration."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取进程级配置",
    "en": "GetAgentInstanceConfig"
  },
  "example": {
    "unifiedCli": "aliyun sls get-agent-instance-config --config-type apm_biz_trace --attributes key=example-string --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /agentinstanceconfigs/apm_biz_trace --region cn-hangzhou --attributes '{\"key\":\"example-string\"}' --header \"Content-Type=application/json\""
  }
}