{
  "name": "ListDownloadJobs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/downloadjobs",
  "parameters": [
    {
      "name": "logstore",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Logstore名称",
        "en": "The name of the Logstore."
      }
    },
    {
      "name": "offset",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "查询开始行，默认值为0。",
        "en": "The line from which to start the query. The default value is 0."
      }
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Project名称",
        "en": "The name of the project."
      }
    },
    {
      "name": "size",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "单次查询需要获取的日志下载任务数量",
        "en": "The number of log download jobs to return in a single request."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列出日志下载任务",
    "en": "ListDownloadJobs"
  },
  "example": {
    "unifiedCli": "aliyun sls list-download-jobs --project ali-test-project --offset 0 --size 10 --logstore ali-test-logstore --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /downloadjobs --project ali-test-project --region cn-hangzhou --offset 0 --size 10 --logstore 'ali-test-logstore' --header \"Content-Type=application/json\""
  }
}