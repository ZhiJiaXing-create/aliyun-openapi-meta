{
  "name": "ListElasticsearchIngestions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/elasticsearchingestions",
  "parameters": [
    {
      "name": "logstore",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "logstore 名称。",
        "en": "The name of the Logstore."
      }
    },
    {
      "name": "offset",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询开始行。默认值为 0。",
        "en": "The line from which to start the query. The default value is 0."
      }
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Project 名称",
        "en": "The name of the project."
      }
    },
    {
      "name": "size",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定查询数量，默认值为 10。",
        "en": "The number of entries to return. The default value is 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列出ES/OpenSearch导入任务",
    "en": "ListElasticsearchIngestions"
  },
  "example": {
    "unifiedCli": "aliyun sls list-elasticsearch-ingestions --project ali-test-project --offset 0 --size 10 --logstore ali-test-logstore --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /elasticsearchingestions --project ali-test-project --region cn-hangzhou --offset 0 --size 10 --logstore 'ali-test-logstore' --header \"Content-Type=application/json\""
  }
}