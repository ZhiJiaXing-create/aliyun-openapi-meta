{
  "name": "ListAgentInstanceConfigs",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/agentinstanceconfigs",
  "parameters": [
    {
      "name": "configType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "进程级配置类型",
        "en": "The type of the process-level configuration."
      }
    },
    {
      "name": "offset",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "查询开始行，默认 0",
        "en": "The number of entries to skip. The default value is 0."
      }
    },
    {
      "name": "size",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "数据条数，默认为 500。",
        "en": "The number of entries to return. The default value is 500."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列出进程级配置",
    "en": "ListAgentInstanceConfigs"
  },
  "descriptions": {
    "en": "Retrieves a list of process-level configurations."
  },
  "example": {
    "unifiedCli": "aliyun sls list-agent-instance-configs --offset 0 --size 100 --config-type apm_trace --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /agentinstanceconfigs --region cn-hangzhou --offset 0 --size 100 --configType apm_trace --header \"Content-Type=application/json\""
  }
}