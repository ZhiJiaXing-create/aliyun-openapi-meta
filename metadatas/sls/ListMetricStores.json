{
  "name": "ListMetricStores",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/metricstores",
  "parameters": [
    {
      "name": "mode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的时序库规格类型，例如 standard",
        "en": "The type of the Metricstore. For example, you can set the parameter to standard to query Standard Metricstores."
      }
    },
    {
      "name": "name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要查询的时序库名称，支持模糊查询，此参数为空或者不传递此参数表示查询所有时序库",
        "en": "The name of the Metricstore. Fuzzy search is supported. If you do not specify this parameter, all Metricstores are involved."
      }
    },
    {
      "name": "offset",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询分页的起始位置",
        "en": "The start position of the query."
      }
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "日志项目名称",
        "en": "The name of the project."
      }
    },
    {
      "name": "size",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询分页的页大小，一次请求最多返回的时序库数量",
        "en": "The number of entries per page."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列出时序库",
    "en": "ListMetricStores"
  },
  "descriptions": {
    "zh": "- Host由Project名称和日志服务Endpoint构成，您需要在Host中指定Project。\n- 已创建并获取AccessKey。更多信息，请参见[访问密钥](~~29009~~)。\n\n 阿里云账号AccessKey拥有所有API的访问权限，风险很高。强烈建议您创建并使用RAM用户进行API访问或日常运维。RAM用户需具备操作日志服务资源的权限。具体操作，请参见[创建RAM用户及授权](~~47664~~)。\n- 已明确您查询日志所属的Project名称、所属地域等。如何查询，请参见[管理Project](~~48984~~)。",
    "en": "- Host consists of a project name and a Simple Log Service endpoint. You must specify a project in Host.\n\n- An AccessKey pair is created and obtained. For more information, see [AccessKey pair](~~29009~~).\n\nThe AccessKey pair of an Alibaba Cloud account has permissions on all API operations. Using these credentials to perform operations in Simple Log Service is a high-risk operation. We recommend that you use a Resource Access Management (RAM) user to call API operations or perform routine O\\&M. To create a RAM user, log on to the RAM console. Make sure that the RAM user has the management permissions on Simple Log Service resources. For more information, see [Create a RAM user and authorize the RAM user to access Simple Log Service](~~47664~~).\n\n- The information that is required to query metric data is obtained. The information includes the name of the project to which the metric data belong and the region of the project. For more information, see [Manage a project](~~48984~~)."
  },
  "example": {
    "unifiedCli": "aliyun sls list-metric-stores --project my-project --offset 0 --size 100 --biz-mode standard --name metric_store --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /metricstores --project my-project --region cn-hangzhou --mode standard --offset 0 --size 100 --name metric_store --header \"Content-Type=application/json\""
  }
}