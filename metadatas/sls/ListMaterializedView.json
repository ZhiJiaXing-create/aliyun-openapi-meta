{
  "name": "ListMaterializedView",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/materializedviews",
  "parameters": [
    {
      "name": "name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The name of the materialized view. Use this to query for a specific materialized view."
      }
    },
    {
      "name": "offset",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "en": "The number of entries to skip before returning results. This parameter is used for pagination. Default value: 0."
      }
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": false,
      "description": {
        "en": "The project that contains the materialized views."
      }
    },
    {
      "name": "size",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "en": "The maximum number of materialized views to return per page. Default value: 100. Maximum value: 500."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "ListMaterializedView"
  },
  "example": {
    "unifiedCli": "aliyun sls list-materialized-view --project example-string --name example-string --offset 1 --size 1 --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /materializedviews --project example-string --region cn-hangzhou --offset 1 --size 1 --name 'example-string' --header \"Content-Type=application/json\""
  }
}