{
  "name": "ListGCSIngestions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/gcsingestions",
  "parameters": [
    {
      "name": "logstore",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Logstore 名称",
        "en": "The name of the Logstore."
      }
    },
    {
      "name": "offset",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "返回结果在全部数据集中的偏移位置， 默认为 0 。",
        "en": "The position from which to start returning results. The default value is 0."
      }
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "project 名称。",
        "en": "The name of the project."
      }
    },
    {
      "name": "size",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，设置的每页行数。",
        "en": "The number of results to return on each page."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列出gcs导入任务",
    "en": "ListGCSIngestions"
  },
  "example": {
    "unifiedCli": "aliyun sls list-gcs-ingestions --project testproject --offset 0 --size 100 --logstore test --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /gcsingestions --project testproject --region cn-hangzhou --offset 0 --size 100 --logstore test --header \"Content-Type=application/json\""
  }
}