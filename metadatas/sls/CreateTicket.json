{
  "name": "CreateTicket",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/tickets",
  "parameters": [
    {
      "name": "accessTokenExpirationTime",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "- 访问令牌过期时间（秒），即用户可以访问页面接口的过期时间，默认86400秒（一天），取值0~86400秒（一天）。\n- 访问令牌过期时间是accessTokenExpirationTime、expirationTime的最小值。\n- 若通过STS调用，访问令牌过期时间（即用户可以访问页面接口的时间）是accessTokenExpirationTime、expirationTime、STS过期时间的最小值。",
        "en": "- The validity period of the access token. Unit: seconds. Default value: 86400, which specifies one day. Valid values: 0 to 86400.\n\n- The validity period of the access token is the smaller value between accessTokenExpirationTime and expirationTime.\n\n- If you use a Security Token Service (STS) token to call this operation, the validity period of the access token is the smallest value among accessTokenExpirationTime, expirationTime, and the validity period of the STS token."
      }
    },
    {
      "name": "expirationTime",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "- 服务地址只能是华东2（上海）或新加坡，但获取的Ticket可以在各个地域使用。\n- 过期时间（秒），即内嵌页面URL链接过期时间，默认86400秒（一天），取值0~2592000秒（30天）。",
        "en": "- You must use the Simple Log Service endpoint for the China (Shanghai) or Singapore region to call the CreateTicket operation. After you obtain the ticket, you can use the ticket regardless of the region.\n\n- The validity period for the URL of the console page that you want to embed. Unit: seconds. Default value: 86400 (one day). Valid values: 0 to 2592000 (30 days)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建免登录票据",
    "en": "CreateTicket"
  },
  "example": {
    "unifiedCli": "aliyun sls create-ticket --expiration-time 86400 --access-token-expiration-time 600 --region cn-hangzhou",
    "legacyCli": "aliyun sls POST /tickets --region cn-hangzhou --expirationTime 86400 --accessTokenExpirationTime 600 --header \"Content-Type=application/json\""
  }
}