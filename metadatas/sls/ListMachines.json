{
  "name": "ListMachines",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/machinegroups/[machineGroup]/machines",
  "parameters": [
    {
      "name": "machineGroup",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "机器组名称。",
        "en": "The name of the machine group."
      }
    },
    {
      "name": "offset",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询开始行。默认值为0。",
        "en": "The line from which the query starts. Default value: 0."
      }
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Project名称。",
        "en": "The name of the project."
      }
    },
    {
      "name": "size",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时，设置的每页行数。默认值为100，最大值为500。",
        "en": "The number of entries per page. Default value: 100. Maximum value: 500."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询机器组中的机器列表",
    "en": "ListMachines"
  },
  "descriptions": {
    "zh": "Host由Project名称和日志服务Endpoint构成，您需要在Host中指定Project。",
    "en": "Host consists of a project name and a Simple Log Service endpoint. You must specify a project in Host."
  },
  "example": {
    "unifiedCli": "aliyun sls list-machines --project ali-test-project --machine-group test-group --offset 0 --size 10 --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /machinegroups/test-group/machines --project ali-test-project --region cn-hangzhou --offset 0 --size 10 --header \"Content-Type=application/json\""
  }
}