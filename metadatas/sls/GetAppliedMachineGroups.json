{
  "name": "GetAppliedMachineGroups",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/configs/[configName]/machinegroups",
  "parameters": [
    {
      "name": "configName",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Logtail配置名称。",
        "en": "The name of the Logtail configuration."
      }
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Project名称。",
        "en": "The name of the project."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "通过Logtail配置获取已绑定的机器组",
    "en": "GetAppliedMachineGroups"
  },
  "descriptions": {
    "zh": "请求语法中Host由Project名称和日志服务Endpoint构成，您需要在Host中指定Project名称。",
    "en": "Host consists of a project name and a Simple Log Service endpoint. You must specify a project in Host."
  },
  "example": {
    "unifiedCli": "aliyun sls get-applied-machine-groups --project ali-test-project --config-name logtail-config-sample --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /configs/logtail-config-sample/machinegroups --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}