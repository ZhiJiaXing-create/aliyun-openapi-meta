{
  "name": "GetIndex",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/logstores/[logstore]/index",
  "parameters": [
    {
      "name": "logstore",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Logstore名称。",
        "en": "The name of the Logstore."
      }
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "project 名称。",
        "en": "The name of the project."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取索引",
    "en": "GetIndex"
  },
  "descriptions": {
    "zh": "### 接口说明\n请求语法中Host由Project名称和日志服务Endpoint构成，您需要在Host中指定Project。",
    "en": "### Usage notes\n\nHost consists of a project name and a Simple Log Service endpoint. You must specify a project in Host."
  },
  "example": {
    "unifiedCli": "aliyun sls get-index --project ali-test-project --logstore test-logstore --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /logstores/test-logstore/index --project ali-test-project --region cn-hangzhou --header \"Content-Type=application/json\""
  }
}