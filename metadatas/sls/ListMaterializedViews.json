{
  "name": "ListMaterializedViews",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/materializedviews",
  "parameters": [
    {
      "name": "name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "en": "The name of the materialized view. Fuzzy search is supported."
      }
    },
    {
      "name": "offset",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "en": "The position from which to start returning results for pagination. Default value: 0."
      }
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": false,
      "description": {
        "en": "The name of the Project."
      }
    },
    {
      "name": "size",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "en": "The number of materialized views to return. The actual number of returned materialized views may be less than the specified value. If you do not specify this parameter, a system default is used."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "ListMaterializedViews"
  },
  "example": {
    "unifiedCli": "aliyun sls list-materialized-views --project example-string --name example-string --offset 1 --size 1 --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /materializedviews --project example-string --region cn-hangzhou --offset 1 --size 1 --name 'example-string' --header \"Content-Type=application/json\""
  }
}