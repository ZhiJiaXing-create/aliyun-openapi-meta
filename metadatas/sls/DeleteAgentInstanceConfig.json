{
  "name": "DeleteAgentInstanceConfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/agentinstanceconfigs/[configType]",
  "parameters": [
    {
      "name": "attributes",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "进程级配置归属",
        "en": "The process to which the configuration applies."
      }
    },
    {
      "name": "configType",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "进程级配置类型",
        "en": "The type of the process-level configuration."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除进程级配置",
    "en": "DeleteAgentInstanceConfig"
  },
  "example": {
    "unifiedCli": "aliyun sls delete-agent-instance-config --config-type apm_biz_trace --attributes key=example-string --region cn-hangzhou",
    "legacyCli": "aliyun sls DELETE /agentinstanceconfigs/apm_biz_trace --region cn-hangzhou --attributes '{\"key\":\"example-string\"}' --header \"Content-Type=application/json\""
  }
}