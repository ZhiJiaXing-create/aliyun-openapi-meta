{
  "name": "ListOSSHDFSExports",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/osshdfsexports",
  "parameters": [
    {
      "name": "logstore",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "logstore名称。",
        "en": "The name of the Logstore."
      }
    },
    {
      "name": "offset",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "查询开始行，默认值为0。",
        "en": "The line from which to start the query. Default value: 0."
      }
    },
    {
      "name": "project",
      "position": "Host",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Project名称",
        "en": "The name of the project."
      }
    },
    {
      "name": "size",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定查询数量，默认值为10。",
        "en": "The number of entries to return. Default value: 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列出OSS-HDFS投递任务",
    "en": "ListOSSHDFSExports"
  },
  "example": {
    "unifiedCli": "aliyun sls list-oss-hdfs-exports --project my-project --offset 0 --size 10 --logstore ali-test-logstore --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /osshdfsexports --project my-project --region cn-hangzhou --offset 0 --size 10 --logstore 'ali-test-logstore' --header \"Content-Type=application/json\""
  }
}