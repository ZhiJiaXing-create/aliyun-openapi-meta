{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/regions",
  "parameters": [
    {
      "name": "language",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回参数localName对应的语言。",
        "en": "The language of the returned `localName` value."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询可用区域",
    "en": "DescribeRegions"
  },
  "example": {
    "unifiedCli": "aliyun sls describe-regions --biz-language zh --region cn-hangzhou",
    "legacyCli": "aliyun sls GET /regions --region cn-hangzhou --language zh --header \"Content-Type=application/json\""
  }
}