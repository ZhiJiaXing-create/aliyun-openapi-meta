{
  "name": "RenewServiceInstance",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否对此次续费实例请求执行预检，包括权限、实例状态校验等。可能的值：\n\n- **true**：发送请求，不续费服务实例。\n\n- **false**：发送请求，检查通过后续费服务实例。\n\n默认为false。只有服务实例状态为待续费、续费失败才可执行。",
        "en": "Specifies whether to perform a dry run of the renewal request, including permission and instance status checks. Valid values:\n\n- **true**: Sends the request without renewing the service instance.\n\n- **false**: Sends the request and renews the service instance after the checks pass.\n\nDefault value: false. The operation is allowed only when the service instance is in the Pending Renewal or Renewal Failed state."
      }
    },
    {
      "name": "ServiceInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "服务实例ID。",
        "en": "The service instance ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "续费服务实例",
    "en": "RenewServiceInstance"
  },
  "example": {
    "unifiedCli": "aliyun computenest renew-service-instance --dry-run false --service-instance-id si-70a3b15bb626435b0000 --region cn-hangzhou",
    "legacyCli": "aliyun computenest RenewServiceInstance --region cn-hangzhou --ServiceInstanceId 'si-70a3b15bb626435b0000' --DryRun false"
  }
}