{
  "name": "UpdateStoreConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EnableNotification",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否启用钉钉异常消息通知，true 启用，false不启用。",
        "en": "Specifies whether to enable DingTalk exception message notifications. Set to true to enable or false to disable."
      }
    },
    {
      "name": "ExtraParams",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "系统扩展字段，请忽略；",
        "en": "The system extension field. Ignore this parameter."
      }
    },
    {
      "name": "NotificationSilentTimes",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户配置的静默期，不发通知消息，JSON列表，单位为分钟，每个JSON字段表示一个静默期时间段，里面的值为UTC时间下一天里面的分钟数，from为静默期的起始分钟数，to为结束分钟数。",
        "en": "The silent periods configured by the user during which notification messages are not sent. The value is a JSON list in minutes. Each JSON object represents a silent period time range, where the values are the minute offsets within a day in UTC time. The \"from\" field specifies the start minute of the silent period, and the \"to\" field specifies the end minute."
      }
    },
    {
      "name": "NotificationWebHook",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "钉钉消息的webHook地址。",
        "en": "The webhook URL for DingTalk messages."
      }
    },
    {
      "name": "StoreId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "门店ID或商家自定义门店ID。",
        "en": "The store ID or a custom store ID defined by the merchant."
      }
    },
    {
      "name": "SubscribeContents",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "订阅内容。",
        "en": "The subscription content."
      }
    }
  ],
  "security": [
    "AK",
    "BearerToken"
  ],
  "title": {
    "zh": "修改门店配置",
    "en": "Modify Store Configuration"
  }
}