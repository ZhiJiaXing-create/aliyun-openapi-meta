{
  "name": "ListUserDefineRegion",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/pop/v5/user_region_defs",
  "parameters": [
    {
      "name": "DebugEnable",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否允许远程调试。",
        "en": "Indicates whether remote debugging is allowed."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取用户自定义命名空间列表",
    "en": "ListUserDefineRegion"
  },
  "example": {
    "unifiedCli": "aliyun edas list-user-define-region --debug-enable false --region cn-hangzhou",
    "legacyCli": "aliyun edas POST /pop/v5/user_region_defs --region cn-hangzhou --DebugEnable false --header \"Content-Type=application/json\""
  }
}