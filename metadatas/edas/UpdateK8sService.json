{
  "name": "UpdateK8sService",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "PUT",
  "pathPattern": "/pop/v5/k8s/acs/k8s_service",
  "parameters": [
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID。",
        "en": "The ID of the application."
      }
    },
    {
      "name": "ExternalTrafficPolicy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置外部流量管理策略：\n- Local：Local模式。\n- Cluster：Cluster模式。\n\n默认为Local模式。",
        "en": "The policy used for external traffic management. Valid values:\n\n*   Local: local mode\n*   Cluster: cluster mode\n\nDefault value: Local."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "K8s Service名称，需要遵循以下规范：\n\n- 仅支持小写字母、数字和短划线（-）。\n- 只能以字母开头，并且以字母或数字结尾。\n- 长度范围为2~32字符。",
        "en": "The name of the service in a Kubernetes cluster.\n\n*   The name can contain lowercase letters, digits, and hyphens (-).\n*   It must start with a letter and end with a letter or digit.\n*   The name can be 2 to 32 characters in length."
      }
    },
    {
      "name": "ServicePorts",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "K8s Service端口映射表，需要符合JsonArray格式，支持的参数如下：\n- **protocol**：必填，服务协议，支持TCP和UDP。\n- **port**：必填，前端服务端口，取值范围1~65535。\n- **targetPort**：必填，后端容器端口，取值范围1~65535。\n\n参考示例：\n`[{\"protocol\": \"TCP\", \"port\": 80, \"targetPort\": 8080},{\"protocol\": \"TCP\", \"port\": 81, \"targetPort\": 8081}]`",
        "en": "The mappings between service ports. Set this parameter to a JSON array. The following parameters are included in the configurations:\n\n*   **protocol**: the protocol used by the service. Valid values: TCP and UDP. This parameter is required.\n*   **port**: the frontend service port. Valid values: 1 to 65535. This parameter is required.\n*   **targetPort**: the backend container port. Valid values: 1 to 65535. This parameter is required.\n\nExample: `[{\"protocol\": \"TCP\", \"port\": 80, \"targetPort\": 8080},{\"protocol\": \"TCP\", \"port\": 81, \"targetPort\": 8081}]`"
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "K8s服务类型，当前只支持ClusterIP类型。",
        "en": "The type of the service in a Kubernetes cluster. Set the value to ClusterIP."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新K8s服务",
    "en": "UpdateK8sService"
  },
  "example": {
    "unifiedCli": "aliyun edas update-k8s-service --app-id 5a166fbd-0000-0000-a286-781659d9f54c --name service-http --type ClusterIP --service-ports protocol_:_TCP_,_port_:80,_targetPort_:8080 --external-traffic-policy Local --region cn-hangzhou",
    "legacyCli": "aliyun edas PUT /pop/v5/k8s/acs/k8s_service --region cn-hangzhou --Type ClusterIP --AppId '5a166fbd-0000-0000-a286-781659d9f54c' --ServicePorts 'protocol_:_TCP_,_port_:80,_targetPort_:8080' --ExternalTrafficPolicy Local --Name 'service-http' --header \"Content-Type=application/json\""
  }
}