{
  "name": "GetK8sCluster",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/pop/v5/k8s_clusters",
  "parameters": [
    {
      "name": "ClusterType",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "K8s集群类型：\n\n- 5：容器服务K8s集群。\n- 7：自建K8s集群。",
        "en": "The type of the Kubernetes cluster:\n\n- 5: an ACK cluster.\n\n- 7: a self-managed Kubernetes cluster."
      }
    },
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时当前显示第几页，默认值为1。",
        "en": "The number of the page to return for a paged query. The default value is 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页显示几条，默认值为1000。",
        "en": "The number of entries to return on each page for a paged query. The default value is 1000."
      }
    },
    {
      "name": "RegionTag",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域。",
        "en": "The region."
      }
    },
    {
      "name": "SubClusterType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "集群子类型：\n* Ask：Serverless K8s 集群。\n* ManagedKubernetes：容器服务 K8s 集群。",
        "en": "The subtype of the cluster:\n\n- Ask: an ASK cluster.\n\n- ManagedKubernetes: an ACK cluster."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取K8s集群列表",
    "en": "GetK8sCluster"
  },
  "example": {
    "unifiedCli": "aliyun edas get-k8s-cluster --region-tag cn-hangzhou --current-page 1 --page-size 10 --cluster-type 5 --sub-cluster-type Ask --region cn-hangzhou",
    "legacyCli": "aliyun edas POST /pop/v5/k8s_clusters --region cn-hangzhou --PageSize 10 --CurrentPage 1 --RegionTag 'cn-hangzhou' --ClusterType 5 --SubClusterType Ask --header \"Content-Type=application/json\""
  }
}