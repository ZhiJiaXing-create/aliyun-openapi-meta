{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/pop/v5/tag/tags",
  "parameters": [
    {
      "name": "ResourceIds",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源ID列表， 列表的长度范围为1~20，JSON数组格式。",
        "en": "The IDs of the resources. You can specify up to 20 IDs in the format of a JSON array."
      }
    },
    {
      "name": "ResourceRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源所属地域。",
        "en": "The region in which the resource resides."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型，取值：\n- **application：EDAS**应用。 \n- **cluster**：EDAS集群。",
        "en": "The type of the resource. Valid values:\n\n- **application**: Enterprise Distributed Application Service (EDAS) application\n\n- **cluster**: EDAS cluster"
      }
    },
    {
      "name": "Tags",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "标签键值对。设置时，请注意以下限制：\n- 单个资源最多贴20个标签。\n- key不能以**aliyun**和**acs:**开头，不能包含**http://**或者**https://**。\n- key和value最大不超过128字符，仅支持英文字母、数字和短划线（-）、半角逗号（,）、星号（*）、正斜线（/）、半角问号（?）和半角冒号（:）的组合。\n- JSON格式字符串。",
        "en": "The key-value pairs. When you set this parameter, take note of the following limits:\n\n- You can add up to 20 tags to a resource.\n\n- The tag key cannot start with **aliyun** or **acs:**. It cannot contain **http\\://** or **https\\://**.\n\n- The tag key or tag value can be up to 128 characters in length, and can contain letters, digits, hyphens (-), commas (,), asterisks (\\*), forward slashes (/), question marks (?), and colons (:).\n\n- Set this parameter to a JSON array."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为资源绑定标签",
    "en": "TagResources"
  },
  "example": {
    "unifiedCli": "aliyun edas tag-resources --resource-type application --resource-ids 000e5836-xxxx-xxxx-xxxx-0d6ab2ac4877 --tags key_:_key1_,_value_:_v1_,_key_:_key2_,_value_:_v2 --resource-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun edas POST /pop/v5/tag/tags --region cn-hangzhou --ResourceRegionId 'cn-hangzhou' --ResourceIds '000e5836-xxxx-xxxx-xxxx-0d6ab2ac4877' --ResourceType application --Tags 'key_:_key1_,_value_:_v1_,_key_:_key2_,_value_:_v2' --header \"Content-Type=application/json\""
  }
}