{
  "name": "SynchronizeResource",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/pop/v5/resource/pop_sync_resource",
  "parameters": [
    {
      "name": "ResourceIds",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定的资源ID，仅当Type为`ecs`时支持填写此参数，如有多个ID，需要以逗号分隔，一次最多同步50个资源。",
        "en": "The ID of the resource. This parameter is required only when you set the Type parameter to `ecs`. If you specify multiple IDs, separate them with commas (,). You can synchronize up to 50 resources at a time."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "同步的资源类型，该参数可选的参数值为：`ecs、slb、vpc、all`其中的一个，不支持其他格式的参数值。",
        "en": "The type of the resource to be synchronized. Valid values: `ecs, slb, vpc, and all`. These values are case-sensitive."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "同步阿里云基础资源到EDAS",
    "en": "SynchronizeResource"
  },
  "descriptions": {
    "zh": "调用此接口同步ECS资源信息时，将从ECS同步全量的实例数据，当ECS实例数超过100时，不建议频繁调用此接口。",
    "en": "If you call this operation to synchronize ECS resource information, all instance data is synchronized from ECS. If you have more than 100 ECS instances, we recommend that you do not frequently call this operation."
  },
  "example": {
    "unifiedCli": "aliyun edas synchronize-resource --type ecs --resource-ids i-bp17c0005q8x,i-bp1005q8x --region cn-hangzhou",
    "legacyCli": "aliyun edas GET /pop/v5/resource/pop_sync_resource --region cn-hangzhou --Type ecs --ResourceIds 'i-bp17c0005q8x,i-bp1005q8x' --header \"Content-Type=application/json\""
  }
}