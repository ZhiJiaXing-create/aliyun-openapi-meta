{
  "name": "AuthorizeApplication",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE|POST|PUT",
  "pathPattern": "/pop/v5/account/authorize_app",
  "parameters": [
    {
      "name": "AppIds",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用ID。用半角分号（;）分割，null表示取消授权。",
        "en": "The ID of the application. You can specify multiple IDs. Separate multiple IDs with semicolons (;). If you leave this parameter empty, the permissions on the application are revoked."
      }
    },
    {
      "name": "TargetUserId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待授权子账号或 RAM 用户的 USER ID，格式为子账号名称@主账号 UID。",
        "en": "The ID of the RAM user to be authorized. The value of the parameter is in the `sub-account name@primary account UID` format."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为子账号授权应用",
    "en": "AuthorizeApplication"
  },
  "example": {
    "unifiedCli": "aliyun edas authorize-application --target-user-id test@1334344340000 --app-ids 5fdf50e8-00000_696-000000 --region cn-hangzhou",
    "legacyCli": "aliyun edas POST /pop/v5/account/authorize_app --region cn-hangzhou --AppIds '5fdf50e8-00000_696-000000' --TargetUserId 'test@1334344340000' --header \"Content-Type=application/json\""
  }
}