{
  "name": "ListFlowControlTaskReports",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/v2/openapi/instances/[instanceId]/flowControlTasks/[taskId]/flowTaskReports",
  "parameters": [
    {
      "name": "endTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "s时间戳（格式：yyyymmdd），最大值为t-1。",
        "en": "The end timestamp for the query, in seconds. The format is YYYYMMDD. The latest possible end time is the previous day (t-1)."
      }
    },
    {
      "name": "instanceId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "startTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "s时间戳（格式：yyyymmdd），如果\n不传，默认查询任务开始到t-1天统计数据与指标分析数据。",
        "en": "The start timestamp for the query, in seconds. The format is YYYYMMDD. If you do not specify this parameter, data is queried from the task's start time to the previous day (t-1)."
      }
    },
    {
      "name": "taskId",
      "position": "Path",
      "type": "String",
      "required": false,
      "description": {
        "zh": "流量调控任务ID。",
        "en": "The ID of the traffic control task."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "流量调控任务曝光分析",
    "en": "ListFlowControlTaskReports"
  }
}