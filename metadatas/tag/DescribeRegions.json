{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "支持的语言。取值：\n\n- zh-CN（默认值）：中文\n- en-US：英文\n- ja：日文",
        "en": "The supported natural language. Valid values:\n\n- zh-CN: Chinese (default value)\n\n- en-US: English\n\n- ja: Japanese"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The ID of the region."
      }
    }
  ],
  "security": [
    "AK",
    "APP"
  ],
  "title": {
    "zh": "查询支持标签的地域信息",
    "en": "DescribeRegions"
  },
  "example": {
    "unifiedCli": "aliyun tag describe-regions --biz-region-id cn-hangzhou --accept-language zh-CN --region cn-hangzhou",
    "legacyCli": "aliyun tag DescribeRegions --region cn-hangzhou --AcceptLanguage 'zh-CN' --RegionId 'cn-hangzhou'"
  }
}