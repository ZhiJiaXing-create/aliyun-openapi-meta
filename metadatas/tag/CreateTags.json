{
  "name": "CreateTags",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。\n\n> 仅支持`cn-hangzhou`地域。",
        "en": "The region ID.\n\n> Only `ap-southeast-1` is supported."
      }
    },
    {
      "name": "TagKeyValueParamList",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "标签键值参数列表。",
        "en": "The list of tag key-value parameters."
      },
      "sub_parameters": [
        {
          "name": "Description",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "标签键描述。\n\nN的取值范围：1~10。",
            "en": "The description of the tag key.\n\nValid values of N: 1 to 10."
          }
        },
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "标签键。\n\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n\nN的取值范围：1~10。",
            "en": "The tag key.\n\nThe tag key can be up to 128 characters in length and cannot start with `aliyun` or `acs:`, or contain `http://` or `https://`.\n\nValid values of N: 1 to 10."
          }
        },
        {
          "name": "TagValueParamList",
          "position": "Query",
          "type": "RepeatList",
          "required": false,
          "description": {
            "zh": "标签值参数列表。",
            "en": "The list of tag value parameters."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建预置标签",
    "en": "Create preset tags"
  },
  "descriptions": {
    "zh": "### 使用说明\n预置标签是您预先创建并作用于所有地域的一种标签，非常适合在标签规划阶段使用。您可以在标签规划阶段创建预置标签，然后在标签实施阶段绑定具体的云资源。创建预置标签时，您可以仅设置标签键，而不设置标签值，标签值可以在后续设置。\n\n本文将提供一个示例，创建一个标签键为`Environment`的预置标签，用来标识业务环境。",
    "en": "### Usage notes\nPreset tags are tags that you create in advance and that take effect across all regions. Preset tags are suitable for use during the tag planning phase. You can create preset tags during the tag planning phase and then attach them to specific cloud resources during the tag implementation phase. When you create a preset tag, you can specify only the tag key without specifying a tag value. You can set the tag value later.\n\nThis topic provides an example of how to create a preset tag with the tag key `Environment` to identify business environments."
  },
  "example": {
    "unifiedCli": "aliyun tag create-tags --biz-region-id cn-hangzhou --tag-key-value-param-list '[{\"TagValueParamList\":[{\"Description\":\"Test_environment\",\"Value\":\"test\"}],\"Description\":\"Business_environment\",\"Key\":\"Environment\"}]' --region cn-hangzhou",
    "legacyCli": "aliyun tag CreateTags --region cn-hangzhou --TagKeyValueParamList.1.TagValueParamList.1.Value test --TagKeyValueParamList.1.Description Business_environment --TagKeyValueParamList.1.TagValueParamList.1.Description Test_environment --RegionId 'cn-hangzhou' --TagKeyValueParamList.1.Key Environment"
  }
}