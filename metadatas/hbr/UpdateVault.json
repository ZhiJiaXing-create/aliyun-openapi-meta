{
  "name": "UpdateVault",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "仓库描述信息。长度为0~255个字符。",
        "en": "The description of the backup vault. The description must be 0 to 255 characters in length."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group."
      }
    },
    {
      "name": "VaultId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "备份仓库ID。",
        "en": "The ID of the backup vault."
      }
    },
    {
      "name": "VaultName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份仓库名称。长度为1~64个字符。",
        "en": "The name of the backup vault. The name must be 1 to 64 characters in length."
      }
    },
    {
      "name": "WormEnabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启存储库备份锁定功能。开启备份锁定后，备份库和备份库所有的备份数据在自动到期之前无法删除。开启备份锁定后，不支持关闭备份锁定。备份锁定仅对标准备份库和归档备份库有效。",
        "en": "Specifies whether to enable the immutable backup feature for storage vaults. After the immutable backup feature is enabled, backup vaults and all backup data cannot be deleted until the retention period expires. The immutable backup feature cannot be disabled after it is enabled. Only standard backup vaults and archive vaults support the immutable backup feature."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新备份仓库的配置信息",
    "en": "UpdateVault"
  },
  "example": {
    "unifiedCli": "aliyun hbr update-vault --vault-id v-000000000000000000000 --vault-name vaultname --description vault_description --worm-enabled true --resource-group-id rg-acfm2fa2xeiebyy --region cn-hangzhou",
    "legacyCli": "aliyun hbr UpdateVault --region cn-hangzhou --Description vault_description --ResourceGroupId 'rg-acfm2fa2xeiebyy' --VaultName vaultname --VaultId 'v-000000000000000000000' --WormEnabled true"
  }
}