{
  "name": "DescribeCrossAccounts",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页页码，从1开始，默认值为1。",
        "en": "The page number for paging. Pages start from 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小，最小值为1，最大值为99，默认值为10。",
        "en": "The number of entries per page. Minimum value: 1. Maximum value: 99. Default value: 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询跨账号纳管关系",
    "en": "DescribeCrossAccounts"
  },
  "example": {
    "unifiedCli": "aliyun hbr describe-cross-accounts --page-number 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun hbr DescribeCrossAccounts --region cn-hangzhou --PageSize 10 --PageNumber 1"
  }
}