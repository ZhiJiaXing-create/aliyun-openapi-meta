{
  "name": "CreateBackupJob",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份类型。当SourceType取值为UDM_ECS时，需要配置该参数。\n* **COMPLETE**：全量备份。",
        "en": "The backup type. This parameter is required only if you set the SourceType parameter to UDM_ECS.\n\n*   **COMPLETE**: full backup"
      }
    },
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "无需填写。",
        "en": "You do not need to specify this parameter."
      }
    },
    {
      "name": "ContainerClusterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "无需填写",
        "en": "You do not need to specify this parameter."
      }
    },
    {
      "name": "ContainerResources",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "无需填写。",
        "en": "You do not need to specify this parameter."
      }
    },
    {
      "name": "CrossAccountRoleName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "被当前账号管理的跨账号备份的原账号RAM中创建的角色名。",
        "en": "The name of the RAM role that is created within the source Alibaba Cloud account and assigned to the current Alibaba Cloud account to authorize the current Alibaba Cloud account to back up data across Alibaba Cloud accounts."
      }
    },
    {
      "name": "CrossAccountType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "跨账号备份类型。支持： \n- SELF_ACCOUNT：本账号备份\n- CROSS_ACCOUNT：跨账号备份",
        "en": "Specifies whether data is backed up within the same Alibaba Cloud account or across Alibaba Cloud accounts. Valid values:\n\n*   SELF_ACCOUNT: Data is backed up within the same Alibaba Cloud account.\n*   CROSS_ACCOUNT: Data is backed up across Alibaba Cloud accounts."
      }
    },
    {
      "name": "CrossAccountUserId",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "被当前账号管理的跨账号备份的原账号ID。",
        "en": "The ID of the source Alibaba Cloud account that authorizes the current Alibaba Cloud account to back up data across Alibaba Cloud accounts."
      }
    },
    {
      "name": "Detail",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "当**SourceType**取值为**UDM_ECS**时，需要配置该参数。\n类型为JSON字符串。\n\n* doCopy：是否开启异地复制\n* destinationRegionId：异地复制目标地域\n* destinationRetention：异地复制备份点保留时间\n* diskIdList：备份的云盘ID列表，为空时备份全部云盘。\n* snapshotGroup：是否使用一致性快照组（仅实例所有云盘均为ESSD时有效）。\n* appConsistent：是否使用应用一致性（需要搭配参数preScriptPath和postScriptPath使用）。\n* preScriptPath：冻结脚本路径。\n* postScriptPath：解冻脚本路径。\n* enableWriters：仅当**AppConsistent**为**true**时，需要配置该参数。是否设置应用一致性快照（默认为 true）：\n  * true：创建应用一致性快照\n  * false：创建文件系统一致性快照\n* enableFsFreeze：仅当**AppConsistent**为**true**时，需要配置该参数。是否在创建应用一致性快照前使用 Linux 的 FsFreeze 机制确保文件系统处于只读一致性。默认为 true。\n* timeoutSeconds：仅当**AppConsistent**为**true**时，需要配置该参数。IO 冻结超时时间。默认为 30 秒。",
        "en": "This parameter is required only if you set the **SourceType** parameter to **UDM_ECS**. The value is a JSON string. Valid values:\n\n*   doCopy: specifies whether to enable remote replication.\n\n*   destinationRegionId: the destination region for remote replication.\n\n*   destinationRetention: the retention period of the backup point for remote replication.\n\n*   diskIdList: the IDs of the disks that are to be backed up. If this parameter is left empty, all disks are backed up.\n\n*   snapshotGroup: specifies whether to use a snapshot-consistent group. This parameter is valid only if all disks of the ECS instance are Enterprise SSDs (ESSDs).\n\n*   appConsistent: specifies whether to use the application-consistent backup feature. This parameter must be used with the preScriptPath and postScriptPath parameters.\n\n*   preScriptPath: the path to the pre-freeze scripts.\n\n*   postScriptPath: the path to the post-thaw scripts.\n\n*   enableWriters: This parameter is required only if you set the **AppConsistent** parameter to **true**. This parameter specifies whether to create application-consistent snapshots.\n\n    *   true: creates application-consistent snapshots.\n    *   false: creates file system-consistent snapshots.\n\n*   enableFsFreeze: This parameter is required only if you set the **AppConsistent** parameter to **true**. This parameter specifies whether to enable Linux fsfreeze to put file systems into the read-only state before application-consistent snapshots are created. Default value: true.\n\n*   timeoutSeconds: This parameter is required only if you set the **AppConsistent** parameter to **true**. This parameter specifies the I/O freeze timeout period. Default value: 30. Unit: seconds."
      }
    },
    {
      "name": "Exclude",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "当**SourceType**取值为**UDM_ECS**时，该参数不生效。\n表示不需要进行备份的路径，该路径下所有文件都不备份。最多支持255个字符。",
        "en": "This parameter does not take effect if you set the **SourceType** parameter to **UDM_ECS**. This parameter specifies the paths to the files that are excluded from the backup job. The value can be up to 255 characters in length."
      }
    },
    {
      "name": "Include",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "当**SourceType**取值为**UDM_ECS**时，该参数不生效。\n表示需要备份的路径，该路径下所有文件都备份。最多支持255个字符。",
        "en": "This parameter does not take effect if you set the **SourceType** parameter to **UDM_ECS**. This parameter specifies the paths to the files that are backed up. The value can be up to 255 characters in length."
      }
    },
    {
      "name": "InitiatedByAck",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "false或留空",
        "en": "false or left empty"
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "当**SourceType**取值为**UDM_ECS**时，需要配置该参数。\n表示ECS实例ID。",
        "en": "This parameter is required only if you set the **SourceType** parameter to **UDM_ECS**. This parameter specifies the ID of the ECS instance."
      }
    },
    {
      "name": "JobName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份任务名称。",
        "en": "The name of the backup job."
      }
    },
    {
      "name": "Options",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "无需填写。",
        "en": "You do not need to specify this parameter."
      }
    },
    {
      "name": "Retention",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "备份保留天数。",
        "en": "The retention period of the backup data. Unit: days."
      }
    },
    {
      "name": "SourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据源类型，取值范围：\n  - **UDM_ECS**：表示ECS整机备份。",
        "en": "The type of the data source. Valid values:\n\n*   **UDM_ECS**: Elastic Compute Service (ECS) instance"
      }
    },
    {
      "name": "SpeedLimit",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "当**SourceType**取值为**UDM_ECS**时，该参数不生效。\n表示备份流量控制。格式为`{start}|{end}|{bandwidth}`。多个流量控制配置使用|分隔，并且配置时间不允许有重叠。\n\n- **start**：起始小时。\n- **end**：结束小时。\n- **bandwidth**：限制速率，单位KB/s。",
        "en": "This parameter does not take effect if you set the **SourceType** parameter to **UDM_ECS**. This parameter specifies the throttling rules. Format: `{start}|{end}|{bandwidth}`. Separate multiple throttling rules with vertical bars (|). A specified time range cannot overlap with another time range.\n\n*   **start**: the start hour.\n*   **end**: the end hour.\n*   **bandwidth**: the bandwidth. Unit: KB/s."
      }
    },
    {
      "name": "VaultId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份仓库ID。\n当SourceType取值为UDM_ECS时，不需要配置该参数。",
        "en": "The ID of the backup vault. This parameter is not required if you set the SourceType parameter to UDM_ECS."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建备份任务",
    "en": "CreateBackupJob"
  },
  "example": {
    "unifiedCli": "aliyun hbr create-backup-job --job-name k8s-backup-infra-20220131150046-hbr --source-type UDM_ECS --retention 15 --instance-id i-bp1xxxxxxxxxxxxxxysm --detail doCopy=false appConsistent=false enableWriters=true preScriptPath=example-string postScriptPath=example-string destinationRegionId=example-string enableFsFreeze=true diskIdList=[] destinationRetention=null snapshotGroup=false timeoutInSeconds=60 --backup-type COMPLETE --vault-id v-000xxxxxxxxxxxxxxy1v --speed-limit 0_24_1024 --options example-string --include /home/alice/0.pdf_,_/home/bob/0.txt --exclude /var_,_/proc --cluster-id example-string --container-resources example-string --initiated-by-ack false --container-cluster-id example-string --cross-account-type SELF_ACCOUNT --cross-account-user-id 1589754625 --cross-account-role-name BackupRole --region cn-hangzhou",
    "legacyCli": "aliyun hbr CreateBackupJob --region cn-hangzhou --Options 'example-string' --Exclude '/var_,_/proc' --CrossAccountUserId 1589754625 --InstanceId 'i-bp1xxxxxxxxxxxxxxysm' --SpeedLimit 0_24_1024 --ClusterId 'example-string' --CrossAccountRoleName BackupRole --SourceType UDM_ECS --CrossAccountType SELF_ACCOUNT --JobName 'k8s-backup-infra-20220131150046-hbr' --Include '/home/alice/0.pdf_,_/home/bob/0.txt' --Retention 15 --VaultId 'v-000xxxxxxxxxxxxxxy1v' --InitiatedByAck false --BackupType COMPLETE --ContainerResources 'example-string' --Detail '{\"doCopy\":false,\"appConsistent\":false,\"enableWriters\":true,\"preScriptPath\":\"example-string\",\"postScriptPath\":\"example-string\",\"destinationRegionId\":\"example-string\",\"enableFsFreeze\":true,\"diskIdList\":[],\"destinationRetention\":null,\"snapshotGroup\":false,\"timeoutInSeconds\":60}' --ContainerClusterId 'example-string'"
  }
}