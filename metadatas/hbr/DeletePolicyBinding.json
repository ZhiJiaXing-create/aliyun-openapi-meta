{
  "name": "DeletePolicyBinding",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DataSourceIds",
      "position": "Body",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "需要解除与策略绑定的数据源ID列表。",
        "en": "The IDs of the data sources to detach from the policy."
      }
    },
    {
      "name": "PolicyId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "策略ID。",
        "en": "The policy ID."
      }
    },
    {
      "name": "SourceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据源类型。取值范围：\n* **UDM_ECS**：表示ECS整机备份。",
        "en": "The type of the data source. Valid values:\n\n- **UDM\\_ECS**: indicates an ECS instance backup."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除策略与数据源关联",
    "en": "DeletePolicyBinding"
  },
  "example": {
    "unifiedCli": "aliyun hbr delete-policy-binding --policy-id po-000000000000000hgp --data-source-ids i-2ze000000000000quf --source-type UDM_ECS --region cn-hangzhou",
    "legacyCli": "aliyun hbr DeletePolicyBinding --region cn-hangzhou --SourceType UDM_ECS --DataSourceIds '[\"i-2ze000000000000quf\"]' --PolicyId 'po-000000000000000hgp'"
  }
}