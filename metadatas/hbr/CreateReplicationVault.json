{
  "name": "CreateReplicationVault",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份仓库描述信息。长度为0~255个字符。",
        "en": "The description of the backup vault. The description must be 0 to 255 characters in length."
      }
    },
    {
      "name": "EncryptType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "镜像备份仓库加密类型，仅VaultType为STANDARD时有效，需与源端备份仓库加密类型保持一致，支持\n- **HBR_PRIVATE**：HBR完全托管，使用备份服务自带加密方式。\n- **KMS**：使用阿里云KMS服务自定义密钥加密。",
        "en": "The method that is used to encrypt the source data. This parameter is valid only if you set the VaultType parameter to STANDARD or OTS_BACKUP. Valid values:\n\n*   **HBR_PRIVATE**: The source data is encrypted by using the built-in encryption method of Hybrid Backup Recovery (HBR).\n*   **KMS**: The source data is encrypted by using Key Management Service (KMS)."
      }
    },
    {
      "name": "KmsKeyId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "阿里云KMS服务自定义密钥Key或者Alias，仅当EncryptType=KMS时必填。",
        "en": "The customer master key (CMK) created in KMS or the alias of the key. This parameter is required only if you set the EncryptType parameter to KMS."
      }
    },
    {
      "name": "RedundancyType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份仓库的数据冗余存储方式。取值包括：\n\n- LRS：标准存储-本地冗余（LRS）。采用数据冗余存储机制，将每个对象的不同冗余存储在同一个可用区内多个设施的多个设备上，确保硬件失效时的数据持久性和可用性。\n- ZRS：标准存储-同城冗余（ZRS）。采用多可用区（AZ）机制，将用户的数据分散存放在同一地域（Region）的3个可用区。当某个可用区不可用时，仍然能够保障数据的正常访问。",
        "en": "The data redundancy type of the backup vault. Valid values:\n\n*   LRS: standard locally redundant storage (LRS). Cloud Backup stores the copies of each object on multiple devices of different facilities in the same zone. This way, Cloud Backup ensures data durability and availability even if hardware failures occur.\n*   ZRS: standard zone-redundant storage (ZRS). Cloud Backup uses the multi-zone mechanism to distribute data across three zones within the same region. If a zone fails, the data that is stored in the other two zones is still accessible."
      }
    },
    {
      "name": "ReplicationSourceRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "源仓库所属的地域ID。",
        "en": "The ID of the region where the source vault resides."
      }
    },
    {
      "name": "ReplicationSourceVaultId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "源仓库ID。",
        "en": "The ID of the source vault."
      }
    },
    {
      "name": "VaultName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "备份仓库名称。长度为1~64个字符。",
        "en": "The name of the backup vault. The name must be 1 to 64 characters in length."
      }
    },
    {
      "name": "VaultRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "备份仓库所属的地域ID。",
        "en": "The ID of the region where the backup vault resides."
      }
    },
    {
      "name": "VaultStorageClass",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份仓库存储类型。取值仅为**STANDARD**，表示标准存储。",
        "en": "The storage type of the backup vault. Valid value: **STANDARD**, which indicates standard storage."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建镜像备份仓库",
    "en": "CreateReplicationVault"
  },
  "descriptions": {
    "zh": "备份仓库创建后，状态为INITIALIZING，并自动发起初始化任务。任务成功后，仓库状态为CREATED。\n\n请在镜像备份仓库所在地域（VaultRegionId）调用该方法。",
    "en": "After a backup vault is created, the backup vault is in the INITIALIZING state, and the system automatically runs an initialization task to initialize the backup vault. After the initialization task is completed, the backup vault is in the CREATED state.Call this operation in the region where the mirror vault resides, which is specified by the VaultRegionId parameter."
  },
  "example": {
    "unifiedCli": "aliyun hbr create-replication-vault --vault-region-id cn-shanghai --vault-name mirrorvaultname --description description --vault-storage-class STANDARD --replication-source-vault-id v-000000000000000000000 --replication-source-region-id cn-hangzhou --redundancy-type LRS --encrypt-type HBR_PRIVATE --kms-key-id alias/test --region cn-hangzhou",
    "legacyCli": "aliyun hbr CreateReplicationVault --region cn-hangzhou --Description description --EncryptType HBR_PRIVATE --KmsKeyId 'alias/test' --VaultName mirrorvaultname --VaultStorageClass STANDARD --RedundancyType LRS --ReplicationSourceRegionId 'cn-hangzhou' --ReplicationSourceVaultId 'v-000000000000000000000' --VaultRegionId 'cn-shanghai'"
  }
}