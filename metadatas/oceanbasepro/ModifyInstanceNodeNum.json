{
  "name": "ModifyInstanceNodeNum",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DryRun",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求, 默认为 false。取值范围：\n- true：发送检查请求，不会更改实例节点数。如果检查通过，则返回 DryRunResult=true， 如果检查不通过，则返回相应错误的错误码。\n- false（默认）：发送正常请求，通过检查后直接更改实例节点数。如为 false, 则表示不进行预检查, DryRunResult 默认返回 false。",
        "en": "Specifies whether to perform only a dry run for the request. Default value: false. Valid values:\n- true: Only a dry-run request is sent and the number of nodes in the instance is not modified. If the dry run succeeds, DryRunResult=true is returned. If the dry run fails, an error code is returned.\n- false: The actual request is sent and no dry run is performed. The number of nodes is changed if the requirements are met. By default, the DryRunResult parameter returns false if you set DryRun to false."
      }
    },
    {
      "name": "InstanceId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "OceanBase 集群的 ID。",
        "en": "The ID of the OceanBase cluster."
      }
    },
    {
      "name": "NodeNum",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群节点个数信息。 \n如果部署模式为 n-n-n，则节点个数为 n * 3。",
        "en": "The number of nodes in the cluster. If the cluster is deployed in n-n-n mode, the number of nodes in the cluster equals n × 3."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改集群节点个数",
    "en": "ModifyInstanceNodeNum"
  },
  "example": {
    "unifiedCli": "aliyun oceanbasepro modify-instance-node-num --instance-id ob317v4uif0000 --node-num 6 --dry-run false --region cn-hangzhou",
    "legacyCli": "aliyun oceanbasepro ModifyInstanceNodeNum --region cn-hangzhou --NodeNum 6 --InstanceId ob317v4uif0000 --DryRun false"
  }
}