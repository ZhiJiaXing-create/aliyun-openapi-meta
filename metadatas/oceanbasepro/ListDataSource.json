{
  "name": "ListDataSource",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Order",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据源排序的顺序，包括 `asc`（升序）和 `descend`（降序）。",
        "en": "The sort order for the data sources. Valid values are `asc` (ascending order) and `descend` (descending order)."
      }
    },
    {
      "name": "PageNumber",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "页序号，分页查询时生效。",
        "en": "The page number to return."
      }
    },
    {
      "name": "PageSize",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "页大小，分页查询时生效。",
        "en": "The number of entries to return per page."
      }
    },
    {
      "name": "SearchKey",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据源的名称或 ID。",
        "en": "The name or ID of the data source."
      }
    },
    {
      "name": "SortField",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据源排序的字段，包括 gmtCreate（默认值）和 gmtModified。",
        "en": "The field used to sort the data sources. Valid values include gmtCreate (default) and gmtModified."
      }
    },
    {
      "name": "Types",
      "position": "Body",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "数据源类型的集合。",
        "en": "A collection of data source types."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询数据源列表",
    "en": "Query data source list"
  },
  "example": {
    "unifiedCli": "aliyun oceanbasepro list-data-source --search-key sourcename --types MYSQL_PUBLIC --sort-field gmtCreate --order descend --page-number 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun oceanbasepro ListDataSource --region cn-hangzhou --Order descend --Types '[\"MYSQL_PUBLIC\"]' --PageSize 10 --PageNumber 1 --SearchKey sourcename --SortField gmtCreate"
  }
}