{
  "name": "CreateFullBackupSetDownload",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupSetDataFormat",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "全量备份集下载格式。取值：\n- **Native**\n- **SQL**\n- **CSV** \n- **JSON**\n\n> 默认CSV。",
        "en": "The format of the downloaded full backup set. Valid values:\n\n- **Native**\n\n- **SQL**\n\n- **CSV**\n\n- **JSON**\n\n> The default value is CSV."
      }
    },
    {
      "name": "BackupSetId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "全量备份集ID。",
        "en": "The ID of the full backup set."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。",
        "en": "A client token used to ensure the idempotence of the request. This prevents the same request from being sent multiple times."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建全量备份集下载任务",
    "en": "CreateFullBackupSetDownload"
  },
  "example": {
    "unifiedCli": "aliyun dbs create-full-backup-set-download --backup-set-id dbs1hvXXXXX --backup-set-data-format SQL --client-token ETnLKlblzczshOTUbOCzxxxxxxx --api-version 2019-03-06 --region cn-hangzhou",
    "legacyCli": "aliyun dbs CreateFullBackupSetDownload --region cn-hangzhou --BackupSetDataFormat SQL --BackupSetId dbs1hvXXXXX --ClientToken ETnLKlblzczshOTUbOCzxxxxxxx"
  }
}