{
  "name": "EnableBackupLog",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupPlanId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "备份计划ID。您可以调用[DescribeBackupPlanList](~~2869825~~)查询。",
        "en": "The backup plan ID. Call [DescribeBackupPlanList](~~2869825~~) to obtain this ID."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "任意字符串。",
        "en": "A client-generated token that ensures the idempotency of the request."
      }
    },
    {
      "name": "EnableMysqlPhysicalBackupBinlog",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "开启MySQL物理备份的日志备份",
        "en": "Specifies whether to enable log backup for MySQL physical backups. Set the value to `true` to enable this feature."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "开启增量备份",
    "en": "EnableBackupLog"
  },
  "descriptions": {
    "zh": "## 影响\n开启增量备份不会产生任何费用，但因增量备份会生成对应的备份流量和存储量，该部分收费标准与全量备份的收费标准相同，且都可以用备份计划或存储包的免费额度进行抵扣。",
    "en": "## Impact\n\nEnabling log backup does not incur direct charges. However, log backups generate backup traffic and storage usage. These resources are billed at the same rate as full backups. You can use the free quota from your backup plan or storage plan to offset these charges."
  },
  "example": {
    "unifiedCli": "aliyun dbs enable-backup-log --backup-plan-id dbstooi000000 --client-token dbs --enable-mysql-physical-backup-binlog true --api-version 2019-03-06 --region cn-hangzhou",
    "legacyCli": "aliyun dbs EnableBackupLog --region cn-hangzhou --EnableMysqlPhysicalBackupBinlog true --BackupPlanId dbstooi000000 --ClientToken dbs"
  }
}