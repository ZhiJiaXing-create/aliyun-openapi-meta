{
  "name": "StopBackupPlan",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BackupPlanId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "备份计划ID。",
        "en": "The ID of the backup plan."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性，防止重复提交请求。",
        "en": "A client token to ensure the idempotence of the request. This prevents the same request from being sent repeatedly."
      }
    },
    {
      "name": "StopMethod",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "暂停方式，取值：\n- ALL：暂停备份计划、全量数据备份任务、增量日志备份任务、恢复任务\n- PLAN：只暂停备份计划",
        "en": "The method used to pause the backup plan. Valid values:\n\n- ALL: Pauses the backup schedule, full data backup jobs, incremental log backup jobs, and restore jobs.\n\n- PLAN: Pauses only the backup schedule."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "暂停备份计划",
    "en": "StopBackupPlan"
  },
  "example": {
    "unifiedCli": "aliyun dbs stop-backup-plan --backup-plan-id dbstooi01XXXX --stop-method ALL --client-token ETnLKlblzczshOTUbOCzxxxxxxx --api-version 2019-03-06 --region cn-hangzhou",
    "legacyCli": "aliyun dbs StopBackupPlan --region cn-hangzhou --BackupPlanId dbstooi01XXXX --StopMethod ALL --ClientToken ETnLKlblzczshOTUbOCzxxxxxxx"
  }
}