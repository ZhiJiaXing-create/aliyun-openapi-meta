{
  "name": "DeleteStack",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DeleteOptions",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "用于控制删除资源栈的行为。",
        "en": "Stack deletion options."
      }
    },
    {
      "name": "Parallelism",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "资源最大并发数。\n默认为空，用户可以输入大于等于零的整数。\n- 若设置为大于零的整数，则使用该整数。  \n- 若设置为零，对于ROS类型资源栈不限制，Terraform类型资源栈使用Terraform默认值（通常为10）。\n- 若不设置，则使用上次操作时的设置；若上次操作时未设置，对于ROS类型资源栈不限制，Terraform类型资源栈使用Terraform默认值（通常为10）。\n- 若设置了该值，将与资源栈进行关联，影响资源栈的后续操作。",
        "en": "Maximum number of concurrent resource operations.\n\nDefault: empty. Accepts an integer >= 0.\n\n> - A value greater than 0 is used as-is. A value of 0 or empty imposes no limit on ROS stacks. For Terraform stacks, the Terraform default (typically 10) applies.\n>\n> - This value persists on the stack and affects subsequent operations such as updates."
      }
    },
    {
      "name": "RamRoleName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "RAM角色名称。ROS会扮演该角色创建资源栈，使用角色的凭证代表用户进行接口调用。   \nROS始终将此角色用于资源栈上将进行的操作。只要用户有权在资源栈上进行操作，即使用户无权使用角色，ROS也会使用此角色，确保角色授予最少的权限。  \n该接口与其他接口不同，如果用户未指定该值，ROS不会使用以前与资源栈关联的角色。如果需要使用角色，请显式指定。如果没有可用角色，ROS将使用从您的用户凭证中生成的临时凭证。  \nRAM角色名称最大长度为64个字节。",
        "en": "The RAM role name. ROS assumes this role to create the stack and call Alibaba Cloud service APIs.<br> Even if you have stack operation permissions but lack RAM role permissions, ROS still assumes the role. Ensure the role follows the least-privilege principle.<br> If empty, ROS cannot assume the existing RAM role associated with the stack. To have ROS assume a role, specify this parameter. If no RAM role is available, ROS uses a temporary credential from your account credentials.<br> Maximum length: 64 bytes."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源栈所属的地域ID。您可以调用[DescribeRegions](~~131035~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the stack. Call [DescribeRegions](~~131035~~) to query available regions."
      }
    },
    {
      "name": "RetainAllResources",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否保留该资源栈下的所有资源。\n\n取值：\n\n- true：保留该资源栈下的所有资源。\n\n- false（默认值）：不保留该资源栈下的资源。",
        "en": "Whether to retain all resources in the stack.\n\nValid values:\n\n- true\n\n- false (default)"
      }
    },
    {
      "name": "RetainResources",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "需要保留资源的列表。",
        "en": "Resources to retain."
      }
    },
    {
      "name": "StackId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源栈ID。",
        "en": "The ID of the stack."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除资源栈",
    "en": "DeleteStack"
  },
  "example": {
    "unifiedCli": "aliyun ros delete-stack --stack-id 4a6c9851-3b0f-4f5f-b4ca-a14bf6910000 --retain-all-resources false --biz-region-id cn-hangzhou --biz-ram-role-name test-role --retain-resources my_disk --delete-options ThrowErrorWithSubscriptionResources --parallelism 1 --region cn-hangzhou",
    "legacyCli": "aliyun ros DeleteStack --region cn-hangzhou --RamRoleName 'test-role' --DeleteOptions.1 ThrowErrorWithSubscriptionResources --RetainAllResources false --Parallelism 1 --RegionId 'cn-hangzhou' --RetainResources.1 my_disk --StackId '4a6c9851-3b0f-4f5f-b4ca-a14bf6910000'"
  }
}