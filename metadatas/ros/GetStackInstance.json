{
  "name": "GetStackInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "OutputOption",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否返回Outputs参数（资源栈输出列表）。取值：  \n\n- Enabled：返回Outputs参数。  \n\n- Disabled（默认值）：不返回Outputs参数。  \n> Outputs计算较为耗时。如果不需要获取Outputs信息，建议您将OutputOption指定为Disabled，提高接口响应速度。",
        "en": "Specifies whether to return the Outputs parameter. The Outputs parameter specifies the outputs of the stack. Valid values:\n\n- Enabled: returns the Outputs parameter.\n\n- Disabled (default): does not return the Outputs parameter.\n\n> The Outputs parameter requires a long period of time to calculate. If you do not require the outputs of the stack, we recommend that you set OutputOption to Disabled to improve the response speed of the API operation."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源栈组所属的地域ID。您可以调用[DescribeRegions](~~131035~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the stack group. You can call the [DescribeRegions](~~131035~~) operation to query the most recent region list."
      }
    },
    {
      "name": "StackGroupName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源栈组名称。名称在单个地域内唯一。  \n长度不超过255个字符，必须以数字或英文字母开头，可以包含数字、英文字母、短划线（-）和下划线（_）。",
        "en": "The name of the stack group. The name must be unique within a region.<br>\nThe name can be up to 255 characters in length, and can contain digits, letters, hyphens (-), and underscores (\\_). It must start with a digit or letter."
      }
    },
    {
      "name": "StackInstanceAccountId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源栈所属目标账号ID。\n\n- 自助管理权限模式下，该账号是阿里云账号。\n\n- 服务管理权限模式下，该账号是资源目录的成员账号。\n\n>关于目标账号的更多信息，请参见[概览](~~154578~~)。",
        "en": "The ID of the destination account to which the stack belongs.\n\n- If the stack group is granted self-managed permissions, the stack belongs to an Alibaba Cloud account.\n\n- If the stack group is granted service-managed permissions, the stack belongs to a member in a resource directory.\n\n> For more information about the destination account, see [Overview](~~154578~~)."
      }
    },
    {
      "name": "StackInstanceRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源栈所属地域。",
        "en": "The region ID of the stack."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询资源栈实例信息",
    "en": "GetStackInstance"
  },
  "descriptions": {
    "zh": "本文将提供一个示例，对杭州地域已有的自助管理权限模式的资源栈组`MyStackGroup`，查询其在北京地域和阿里云账号`151266687691****`下的资源栈实例的详细信息。",
    "en": "In this example, the information about a stack instance associated with a stack group named `MyStackGroup` is queried. The stack instance is deployed in the China (Beijing) region within the `151266687691****` Alibaba Cloud account. The stack group is granted self-managed permissions and deployed in the China (Hangzhou) region."
  },
  "example": {
    "unifiedCli": "aliyun ros get-stack-instance --biz-region-id cn-hangzhou --stack-group-name MyStackGroup --stack-instance-account-id 1512666876910000 --stack-instance-region-id cn-beijing --output-option Disabled --region cn-hangzhou",
    "legacyCli": "aliyun ros GetStackInstance --region cn-hangzhou --StackInstanceAccountId 1512666876910000 --StackGroupName MyStackGroup --StackInstanceRegionId 'cn-beijing' --RegionId 'cn-hangzhou' --OutputOption Disabled"
  }
}