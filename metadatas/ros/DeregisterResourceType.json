{
  "name": "DeregisterResourceType",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。",
        "en": "The resource type."
      }
    },
    {
      "name": "VersionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "版本ID。如果要删除资源类型某个版本，请指定该参数。",
        "en": "The version ID. Specify this parameter to delete a specific version of the resource type."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "注销资源类型",
    "en": "DeregisterResourceType"
  },
  "descriptions": {
    "zh": "- 如果删除某个资源类型，那么您不能在ROS中使用它。\n- 如果删除资源类型的某个版本，那么您不能在ROS中使用该版本。\n- 当且仅当资源类型只有一个版本时，才能被删除。否则，您必须手动删除资源类型的其他版本。\n- 当资源类型的版本数量大于1时，不能删除默认版本。\n- 当资源类型的版本数量为1时，删除该版本，将删除资源类型。",
    "en": "- If you delete a resource type, it can no longer be used in ROS.\n\n- If you delete a version of a resource type, that version can no longer be used in ROS.\n\n- You can delete a resource type only if it has a single version. If a resource type has multiple versions, you must delete the other versions first.\n\n- You cannot delete the default version if the resource type has more than one version.\n\n- If a resource type has only one version, deleting that version also deletes the resource type."
  },
  "example": {
    "unifiedCli": "aliyun ros deregister-resource-type --resource-type MODULE::MyOrganization::MyService::MyUsecase --version-id v1 --region cn-hangzhou",
    "legacyCli": "aliyun ros DeregisterResourceType --region cn-hangzhou --VersionId v1 --ResourceType 'MODULE::MyOrganization::MyService::MyUsecase'"
  }
}