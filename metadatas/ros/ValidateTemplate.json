{
  "name": "ValidateTemplate",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求的幂等性。该值由客户端生成，并且必须是全局唯一的。 \n\n长度不超过64个字符，可包含英文字母、数字、短划线（-）和下划线（_）。 \n\n更多信息，请参见[如何保证幂等性](~~134212~~)。",
        "en": "A client token that is used to ensure the idempotence of the request. The client generates the value, which must be unique.\n\nThe token can be up to 64 characters in length and can contain letters, digits, hyphens (-), and underscores (\\_).\n\nFor more information, see [How to ensure idempotence](~~134212~~)."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源栈模板所属的地域ID。您可以调用[DescribeRegions](~~131035~~)查看最新的阿里云地域列表。",
        "en": "The region ID of the stack template. You can call the [DescribeRegions](~~131035~~) operation to query the most recent list of Alibaba Cloud regions."
      }
    },
    {
      "name": "TemplateBody",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模板主体的结构。长度为1~524,288个字节。   \n如果长度较大，建议通过HTTP POST+Body Param的方式，将参数放在请求体中进行传递，避免URL过长而导致请求失败。   \n您可以指定TemplateBody或TemplateURL参数，但不能同时指定。",
        "en": "The structure of the template body. The template body can be 1 to 524,288 bytes in length.<br>If the template body is long, use a POST request and place the parameter in the request body. This prevents a request failure caused by an excessively long URL.<br>You can specify either TemplateBody or TemplateURL, but not both.<br><br>"
      }
    },
    {
      "name": "TemplateURL",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "包含模板主体的文件的位置。URL必须指向位于HTTP Web服务器或阿里云OSS存储空间（例如：oss://ros/template/demo、oss://ros/template/demo?RegionId=cn-hangzhou）中的模板，模板最大为524,288个字节。   \n\n> 如果OSS地域未指定，默认与接口参数RegionId相同。\n   \n您可以指定TemplateBody或TemplateURL参数，但不能同时指定。   \nURL的最大长度为：1024个字节。",
        "en": "The location of the file that contains the template body. The URL must point to a template that is located on an HTTP web server or in an Alibaba Cloud OSS bucket, such as oss\\://ros/template/demo or oss\\://ros/template/demo?RegionId=cn-hangzhou. The template can be up to 524,288 bytes in size.\n\n> If you do not specify the region of the OSS bucket, the value of the RegionId parameter is used.\n\nYou can specify either TemplateBody or TemplateURL, but not both.<br>The URL can be up to 1,024 bytes in length.<br>"
      }
    },
    {
      "name": "UpdateInfoOptions",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "更新信息选项列表。列表最大长度为2。",
        "en": "The list of options for the update information. The list can contain up to two options."
      }
    },
    {
      "name": "ValidationOption",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否对模板开启额外校验。取值：\n\n- None（默认值）：不开启额外校验。\n\n- EnableTerraformValidation：对于Terraform类型模板，使用Terraform CLI的`terraform validate`命令开启额外校验。\n\n- EnableFastTerraformValidation：对于Terraform类型模板，使用一种类似于Terraform CLI的`terraform validate`命令开启额外校验。\n  \n> EnableFastTerraformValidation与EnableTerraformValidation相比速度较快，但校验完整性稍有不足。",
        "en": "Specifies whether to enable additional validation for the template. Valid values:\n\n- None (default): No additional validation is enabled.\n\n- EnableTerraformValidation: For a Terraform template, the `terraform validate` command of the Terraform command-line interface (CLI) is used to enable additional validation.\n\n- EnableFastTerraformValidation: For a Terraform template, a command that is similar to the `terraform validate` command of the Terraform CLI is used to enable additional validation.\n\n> Compared with EnableTerraformValidation, EnableFastTerraformValidation is faster but less comprehensive."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "验证将要创建资源栈的模板",
    "en": "ValidateTemplate"
  },
  "descriptions": {
    "zh": "该接口不涉及用户数据，只对模板进行校验，无需进行RAM鉴权。\n\n本文将提供一个示例，验证将要创建资源栈的模板，包含模板主体的文件的位置`TemplateURL`为`oss://ros/template/demo`。",
    "en": "This API operation validates only templates and does not involve user data. RAM authentication is not required.\n\nThis topic provides an example of how to validate a stack template. In this example, the `TemplateURL` of the template file is `oss://ros/template/demo`."
  },
  "example": {
    "unifiedCli": "aliyun ros validate-template --template-url oss://ros/template/demo --biz-region-id cn-hangzhou --template-body ROSTemplateFormatVersion_:_2015-09-01 --client-token 123e4567-e89b-12d3-a456-426655440000 --validation-option None --update-info-options ConsiderCondition --region cn-hangzhou",
    "legacyCli": "aliyun ros ValidateTemplate --region cn-hangzhou --TemplateURL 'oss://ros/template/demo' --UpdateInfoOptions.1 ConsiderCondition --ValidationOption None --RegionId 'cn-hangzhou' --TemplateBody 'ROSTemplateFormatVersion_:_2015-09-01' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}