{
  "name": "GenerateTemplatePolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "GenerateOptions",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "GenerateOptions 用于控制模板权限策略的生成方式。该参数为列表类型，可指定是否解析模板参数以及生成策略的权限范围： \n**ResolveParameters** 表示根据传入的 Parameters 或参数默认值解析条件和资源后生成策略\n\n**IgnoreParameters**  表示不解析参数并按兼容旧逻辑生成策略\n\n**MandatoryPolicy**  表示仅生成模板执行所需的必要权限\n\n**FullPolicy**  表示生成完整权限策略\n\nResolveParameters 与 IgnoreParameters 互斥，MandatoryPolicy 与 FullPolicy 互斥，且 MandatoryPolicy 必须与 ResolveParameters 一起使用。\n\n未指定时，若传入 Parameters 默认使用 ResolveParameters + FullPolicy，否则默认使用 IgnoreParameters + FullPolicy。",
        "en": "The options that control how the template access policy is generated. This parameter is of the LIST type. You can specify whether to resolve template parameters and the permission scope of the generated policy: \n**ResolveParameters** indicates that the policy is generated after conditions and resources are resolved based on the specified Parameters or parameter default values.\n\n**IgnoreParameters** indicates that the policy is generated without resolving parameters, using the legacy logic.\n\n**MandatoryPolicy** indicates that only the minimum permissions required for template execution are generated.\n\n**FullPolicy** indicates that a full access policy is generated.\n\nResolveParameters and IgnoreParameters are mutually exclusive. MandatoryPolicy and FullPolicy are mutually exclusive. MandatoryPolicy must be used together with ResolveParameters.\n\nIf this parameter is not specified, ResolveParameters + FullPolicy is used by default when Parameters is specified. Otherwise, IgnoreParameters + FullPolicy is used by default."
      }
    },
    {
      "name": "OperationTypes",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "需要生成策略信息的操作类型。",
        "en": "The operation types for which policy information is generated."
      }
    },
    {
      "name": "Parameters",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "输入参数。 输入参数中，定义了通过此模板创建资源栈时需要指定的参数，这些参数用来指定每次资源栈创建的细节，例如：用户名、密码、环境相关的ECS规格等。",
        "en": "The input parameters. The input parameters define the parameters that must be specified when you create a stack by using this template. These parameters specify the details of each stack creation, such as the username, password, and environment-specific ECS instance types."
      },
      "sub_parameters": [
        {
          "name": "ParameterKey",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "参数的名称。如果未指定模板中定义的参数名称和参数值，ROS将使用模板中指定的默认值。  \nN的最大值为：200。\n取值示例：\n\n- Parameters.1.ParameterKey：`Name`。\n\n- Parameters.2.ParameterKey：`Netmode`。\n\n\n> Parameters为可选参数。如果需要指定Parameters，则Parameters.N.ParameterKey和Parameters.N.ParameterValue必须同时指定。",
            "en": "The name of the parameter. If you do not specify the name and value of a parameter defined in the template, Resource Orchestration Service (ROS) uses the default value specified in the template.  \nMaximum value of N: 200.\nExample values:\n\n- Parameters.1.ParameterKey: `Name`.\n\n- Parameters.2.ParameterKey: `Netmode`.\n\n\n> Parameters is optional. If you specify Parameters, you must specify both Parameters.N.ParameterKey and Parameters.N.ParameterValue."
          }
        },
        {
          "name": "ParameterValue",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "模板中已定义的参数的取值。N的最大值为200。  \n\n> Parameters为可选参数。若指定了Parameters，则Parameters.N.ParameterValue为必选参数。",
            "en": "The value of the parameter defined in the template. Maximum value of N: 200.  \n\n> Parameters is optional. If you specify Parameters, Parameters.N.ParameterValue is required."
          }
        }
      ]
    },
    {
      "name": "TemplateBody",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模板的结构。长度为1~524,288个字节。\n如果长度较大，建议通过HTTP POST+Body Param的方式，将参数放在请求体中进行传递，避免URL过长而导致请求失败。  \n\n您仅能指定TemplateBody、TemplateURL、TemplateId其中一个参数。",
        "en": "The structure of the template body. The length is 1 to 524,288 bytes.\nIf the length is large, we recommend that you use the HTTP POST method and include the parameter in the request body to avoid request failures caused by an excessively long URL.  \n\nYou can specify only one of the following parameters: TemplateBody, TemplateURL, and TemplateId."
      }
    },
    {
      "name": "TemplateId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模板ID。支持共享模板和私有模板。  \n您仅能指定TemplateBody、TemplateURL、TemplateId其中一个参数。",
        "en": "The template ID. Shared templates and private templates are supported.  \nYou can specify only one of the following parameters: TemplateBody, TemplateURL, and TemplateId."
      }
    },
    {
      "name": "TemplateURL",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "包含模板主体的文件的位置。URL必须指向位于Web服务器（HTTP或HTTPS）或阿里云OSS存储空间（例如：oss://ros/template/demo、oss://ros/template/demo?RegionId=cn-hangzhou）中的模板，模板最大为524,288个字节。\n\n> 如果OSS地域未指定，默认与接口参数RegionId相同。\n\n您仅能指定TemplateBody、TemplateURL、TemplateId其中一个参数。\nURL的最大长度为：1024个字节。",
        "en": "The URL of the file that contains the template body. The URL must point to a template that is located on a web server (HTTP or HTTPS) or in an OSS bucket, such as oss://ros/template/demo or oss://ros/template/demo?RegionId=ap-southeast-1. The maximum size of the template is 524,288 bytes.\n\n> If the OSS region is not specified, the region specified by the RegionId parameter is used by default.\n\nYou can specify only one of the following parameters: TemplateBody, TemplateURL, and TemplateId.\nMaximum length: 1,024 bytes."
      }
    },
    {
      "name": "TemplateVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模板版本。仅在指定TemplateId时生效。",
        "en": "The template version. This parameter takes effect only when TemplateId is specified."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "生成模板所需的策略信息",
    "en": "Generate policy information required by a template"
  },
  "descriptions": {
    "zh": "如果模板所需的策略中包含企业级分布式应用服务EDAS（Enterprise Distributed Application Service），您需要登录阿里云账号，对需要授权的RAM用户进行RAM权限升级。\n\n本文将提供一个示例，为您介绍如何为模板ID为`5ecd1e10-b0e9-4389-a565-e4c15efc****`的模板生成权限策略。",
    "en": "If the policies required by the template include Enterprise Distributed Application Service (EDAS), log on to your Alibaba Cloud account and upgrade the RAM permissions for the Resource Access Management (RAM) users that require authorization.\n\nThis topic provides an example of how to generate access policies for a template whose ID is `5ecd1e10-b0e9-4389-a565-e4c15efc****`."
  },
  "example": {
    "unifiedCli": "aliyun ros generate-template-policy --template-url oss://ros/template/demo --template-body ROSTemplateFormatVersion_:_2015-09-01 --template-id 5ecd1e10-b0e9-4389-a565-e4c15efc0000 --template-version v1 --operation-types CreateStack --parameters ParameterValue=PostPaid ParameterKey=Domain --generate-options MandatoryPolicy --region cn-hangzhou",
    "legacyCli": "aliyun ros GenerateTemplatePolicy --region cn-hangzhou --TemplateURL 'oss://ros/template/demo' --Parameters.1.ParameterKey Domain --GenerateOptions.1 MandatoryPolicy --TemplateVersion v1 --TemplateBody 'ROSTemplateFormatVersion_:_2015-09-01' --Parameters.1.ParameterValue PostPaid --OperationTypes.1 CreateStack --TemplateId '5ecd1e10-b0e9-4389-a565-e4c15efc0000'"
  }
}