{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回结果显示的语言。\n取值：\n\n- zh-CN（默认值）：中文。  \n\n- en-US：英文。  \n\n- ja：日文。",
        "en": "The language in which you want to display the results. Valid values:\n\n- zh-CN (default): Chinese\n\n- en-US: English\n\n- ja: Japanese"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询地域列表",
    "en": "DescribeRegions"
  },
  "example": {
    "unifiedCli": "aliyun ros describe-regions --accept-language zh-CN --region cn-hangzhou",
    "legacyCli": "aliyun ros DescribeRegions --region cn-hangzhou --AcceptLanguage 'zh-CN'"
  }
}