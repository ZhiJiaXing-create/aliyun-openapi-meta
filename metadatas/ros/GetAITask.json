{
  "name": "GetAITask",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "OutputOption",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否返回TaskOutput参数（任务的输出）。取值：  \n\n- Enabled：返回TaskOutput参数。  \n\n- Disabled（默认值）：不返回TaskOutput参数。  \n> TaskOutput输出较长。如果不需要获取Outputs信息，建议您将OutputOption指定为Disabled，提高接口响应速度。",
        "en": "Specifies whether to return the TaskOutput parameter. The TaskOutput parameter specifies the outputs of the AI task. Valid values:\n\n*   Enabled\n*   Disabled (default)\n\n>  The value of TaskOutput may be excessively long. If you do not require the outputs of the task, we recommend that you set OutputOption to Disabled to improve the response speed of the API operation."
      }
    },
    {
      "name": "TaskId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "AI任务ID。",
        "en": "The ID of the AI task."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询AI任务详情",
    "en": "GetAITask"
  },
  "example": {
    "unifiedCli": "aliyun ros get-ai-task --task-id t-asasas00000 --output-option Disabled --region cn-hangzhou",
    "legacyCli": "aliyun ros GetAITask --region cn-hangzhou --TaskId 't-asasas00000' --OutputOption Disabled"
  }
}