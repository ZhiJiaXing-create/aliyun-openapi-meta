{
  "name": "CreateDomain",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "域名，长度1-50，可以包含数字、大写字母、小写字母、.、-。",
        "en": "Domain name, length 1-50, can include numbers, uppercase letters, lowercase letters, ., and -."
      }
    },
    {
      "name": "dkimSelector",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "DKIM协议中的selector字段，用于标识特定公钥的字段。\n建议为空，系统会根据集群信息自动生成。\n如果用户手工指定，比如发信域名是sub.example.com，dkimSelector填写的是default，那么主机记录就是default._domainkey.sub\n\n约束：\n1. 长度不能大于60\n2. 必须是可见字符\n3. 不能以-打头\n4. 不能以-结尾\n5. 不能包含以下字符_ :;/!*~.@#$%^&()+=[{]}|?<>,\"'\\",
        "en": "The selector field in the DKIM protocol, used to identify a specific public key. It is recommended to leave it blank, as the system will automatically generate it based on cluster information. If the user specifies it manually, for example, if the sending domain is \"sub.example.com\" and dkimSelector is set to \"default\", then the host record will be \"default.\\_domainkey.sub\"\nConstraints:\n\n1. The length must not exceed 60 characters.\n\n2. It must consist of visible characters only.\n\n3. It cannot start with a hyphen (-).\n\n4. It cannot end with a hyphen (-).\n\n5. It cannot contain any of the following characters: \\_ :;/!\\*\\~.@#$%^&()+=\\[{]}|?<>,'\""
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建域名",
    "en": "Create Domain"
  },
  "example": {
    "unifiedCli": "aliyun dm create-domain --domain-name sub.example.com --dkim-selector default --api-version 2015-11-23 --region cn-hangzhou",
    "legacyCli": "aliyun dm CreateDomain --region cn-hangzhou --dkimSelector default --DomainName 'sub.example.com'"
  }
}