{
  "name": "DescDomain",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DomainId",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "域名ID。可以通过QueryDomainByParam获取。",
        "en": "The domain ID. Call `QueryDomainByParam` to obtain it."
      }
    },
    {
      "name": "RequireRealTimeDnsRecords",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "判断是否要实时DNS解析",
        "en": "Specifies whether to perform real-time DNS resolution."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "配置域名",
    "en": "Configure Domain"
  },
  "example": {
    "unifiedCli": "aliyun dm desc-domain --domain-id 13464 --require-real-time-dns-records true --api-version 2015-11-23 --region cn-hangzhou",
    "legacyCli": "aliyun dm DescDomain --region cn-hangzhou --DomainId 13464 --RequireRealTimeDnsRecords true"
  }
}