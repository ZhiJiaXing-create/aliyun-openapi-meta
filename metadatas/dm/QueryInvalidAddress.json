{
  "name": "QueryInvalidAddress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EndTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "结束时间，和起始时间跨度不能超出30天,格式yyyy-MM-dd。",
        "en": "End time, with a span from the start time that cannot exceed 30 days, in the format yyyy-MM-dd."
      }
    },
    {
      "name": "KeyWord",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "关键词。不填，代表所有无效地址。",
        "en": "Keyword. If not provided, it represents all invalid addresses."
      }
    },
    {
      "name": "Length",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每次请求数目",
        "en": "Number of items per request."
      }
    },
    {
      "name": "NextStart",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求开始位置",
        "en": "Request starting position."
      }
    },
    {
      "name": "StartTime",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "开始时间，时间不能早于30日，格式yyyy-MM-dd。",
        "en": "Start time, which cannot be earlier than 30 days ago, in the format yyyy-MM-dd."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取无效地址信息",
    "en": "Get Invalid Address Information"
  },
  "descriptions": {
    "zh": "获取去重的无效地址信息，如果给同一个无效地址发送邮件多次，也会记录第一次，需要根据第一次被判断为无效地址分类的时间来查询",
    "en": "Retrieve deduplicated invalid address information. If an email is sent to the same invalid address multiple times, only the first occurrence will be recorded. The query should be based on the time when the address was first classified as invalid."
  },
  "example": {
    "unifiedCli": "aliyun dm query-invalid-address --start-time 2019-09-29 --end-time 2019-09-29 --key-word info --length 100 --next-start 000 --api-version 2015-11-23 --region cn-hangzhou",
    "legacyCli": "aliyun dm QueryInvalidAddress --region cn-hangzhou --EndTime '2019-09-29' --Length 100 --KeyWord info --StartTime '2019-09-29' --NextStart 000"
  }
}