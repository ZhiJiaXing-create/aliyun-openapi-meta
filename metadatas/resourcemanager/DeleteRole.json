{
  "name": "DeleteRole",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RoleName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "角色名称。\n\n长度为1~64个字符，可包含英文字母、数字、点号（.）和短横线（-）。",
        "en": "The name of the role.\n\nThe name must be 1 to 64 characters in length and can contain letters, digits, periods (.), and hyphens (-)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除角色",
    "en": "DeleteRole"
  },
  "example": {
    "unifiedCli": "aliyun resourcemanager delete-role --role-name ECSAdmin --api-version 2020-03-31 --region cn-hangzhou",
    "legacyCli": "aliyun resourcemanager DeleteRole --region cn-hangzhou --RoleName ECSAdmin"
  }
}