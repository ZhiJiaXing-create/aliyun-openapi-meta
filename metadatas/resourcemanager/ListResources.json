{
  "name": "ListResources",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码。\n\n起始值：1。默认值：1。",
        "en": "The page number.\n\nPages start from page 1. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页时每页显示的数据条数。\n\n取值范围：1~100。默认值：10。",
        "en": "The number of entries per page.\n\nValid values: 1 to 100. Default value: 10."
      }
    },
    {
      "name": "Region",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源ID。",
        "en": "The ID of the resource."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源类型。\n\n您可以从[支持资源组的云服务](~~94479~~)的**资源类型**列获取。",
        "en": "The resource type.\n\nFor more information about the supported resource types, see the **Resource type** column in [Services that work with Resource Group](~~94479~~)."
      }
    },
    {
      "name": "ResourceTypes",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "资源类型列表。最多支持50组。\n\n> 如果您设置了`ResourceTypes`，则请求参数中单独设置的`Service`和`ResourceType`将不生效。",
        "en": "The resource types. A maximum of 50 resource types are supported.\n\n>  If you configure `ResourceTypes`, you must configure both `Service` and `ResourceType`. Otherwise, the configured Service or ResourceType does not take effect."
      }
    },
    {
      "name": "Service",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云服务代码。\n\n您可以从[支持资源组的云服务](~~94479~~)的**云服务代码**列获取。",
        "en": "The ID of the Alibaba Cloud service.\n\nYou can obtain the ID from the **Service code** column in [Services that work with Resource Group](~~94479~~)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查看当前账号可以访问的资源列表",
    "en": "ListResources"
  },
  "descriptions": {
    "zh": "> 该API支持通过RAM角色方式调用，但不支持角色的会话策略。",
    "en": ">  You can use a RAM role that is not associated with a session policy to call this API operation.\n\nThis topic provides an example on how to call the API operation to query resources that can be accessed by the current account in resource groups. The response shows that the current account can access only the Elastic Compute Service (ECS) instance `i-23v38****` in the resource group `rg-uPJpP****`."
  },
  "example": {
    "unifiedCli": "aliyun resourcemanager list-resources --resource-group-id rg-uPJpP0000 --service ecs --biz-region cn-hangzhou --resource-type instance --resource-id i-23v380000 --page-number 1 --page-size 10 --resource-types Service=ecs ResourceType=instance --api-version 2020-03-31 --region cn-hangzhou",
    "legacyCli": "aliyun resourcemanager ListResources --region cn-hangzhou --ResourceTypes.1.Service ecs --ResourceGroupId 'rg-uPJpP0000' --PageSize 10 --ResourceTypes.1.ResourceType instance --ResourceId 'i-23v380000' --PageNumber 1 --Service ecs --Region 'cn-hangzhou' --ResourceType instance --method POST --force"
  }
}