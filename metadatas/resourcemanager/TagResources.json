{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "资源组ID或资源目录成员ID。\n\n最多可以指定50个。",
        "en": "The ID of a resource group or member."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源类型。取值：\n\n- ResourceGroup（默认值）：为资源组绑定标签。\n- Account：为资源目录成员绑定标签。\n\n> 当为资源目录成员绑定标签时，该参数为必选。",
        "en": "The type of the objects to which you want to add tags. Valid values:\n\n*   ResourceGroup : resource group. This is the default value.\n*   Account: member.\n\n>  This parameter is required if you add tags to members in a resource directory."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "标签。\n\n最多可以绑定20个标签。",
        "en": "The tags."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "绑定标签",
    "en": "TagResources"
  },
  "descriptions": {
    "en": "This topic provides an example on how to call the API operation to add the tag `k1:v1` to the resource group with an ID of `rg-aekz6bre2uq****`."
  },
  "example": {
    "unifiedCli": "aliyun resourcemanager tag-resources --resource-id rg-aekz6bre2uq0000 --tag Value=v1 Key=k1 --resource-type ResourceGroup --api-version 2020-03-31 --region cn-hangzhou",
    "legacyCli": "aliyun resourcemanager TagResources --region cn-hangzhou --ResourceId.1 'rg-aekz6bre2uq0000' --Tag.1.Key k1 --Tag.1.Value v1 --ResourceType ResourceGroup --method POST --force"
  }
}