{
  "name": "ListWorkspaces",
  "deprecated": true,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/maas/workspaces",
  "parameters": [
    {
      "name": "maxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小。",
        "en": "The number of entries per page."
      }
    },
    {
      "name": "nextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用来返回更多结果。第一次查询不需要提供这个参数，后续查询所需使用的Token，从返回结果中获取。",
        "en": "The pagination token used to retrieve more results. You do not need to provide this parameter for the first query. For subsequent queries, use the token obtained from the previous response."
      }
    },
    {
      "name": "workspaceName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "按业务空间名称进行关键字模糊检索。",
        "en": "The keyword for fuzzy match by workspace name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取业务空间列表",
    "en": "Retrieve workspace list"
  }
}