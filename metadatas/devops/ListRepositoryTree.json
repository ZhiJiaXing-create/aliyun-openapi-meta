{
  "name": "ListRepositoryTree",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/repository/[repositoryId]/files/tree",
  "parameters": [
    {
      "name": "accessToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "个人访问令牌。\n使用阿里云AK+SK或使用STS临时授权方式不需要传该字段",
        "en": "Personal access token.  \nThis field is not required if you use an Alibaba Cloud AccessKey pair (AK+SK) or temporary credentials from Security Token Service (STS)."
      }
    },
    {
      "name": "organizationId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "企业标识，也称企业id，字符串形式，可在云效访问链接中获取，如https://devops.aliyun.com/organization/【OrganizationId】",
        "en": "The enterprise identifier, also known as the organization ID, is a string that can be obtained from the Alibaba Cloud DevOps access URL, such as https://devops.aliyun.com/organization/【OrganizationId】."
      }
    },
    {
      "name": "path",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "代码仓库内的文件路径，获取该路径下的文件目录，默认获取代码库文件树的全部结构",
        "en": "The file path within the code repository. Specifies the folder whose contents you want to retrieve. By default, the entire structure of the repository's file tree is retrieved."
      }
    },
    {
      "name": "refName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分支名称、标签名称或CommitID，默认为代码库库默认分支",
        "en": "The branch name, tag name, or commit ID. Defaults to the repository's default branch."
      }
    },
    {
      "name": "repositoryId",
      "position": "Path",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "代码库ID",
        "en": "The repository ID."
      }
    },
    {
      "name": "type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查找策略\nDIRECT：仅展示当前目录下的内容\nRECURSIVE：递归查找当前路径下的所有文件\nFLATTEN：扁平化展示（如果是目录，递归查找，直到子目录包含文件或多个目录为止）",
        "en": "The search strategy:  \nDIRECT: Lists only the contents of the current folder.  \nRECURSIVE: Recursively retrieves all files under the current path.  \nFLATTEN: Flattens the structure (if the path is a folder, recursively traverses it until subdirectories contain files or multiple folders)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询文件树",
    "en": "Query File Tree"
  }
}