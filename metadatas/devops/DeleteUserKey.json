{
  "name": "DeleteUserKey",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/api/v3/user/keys/[keyId]",
  "parameters": [
    {
      "name": "accessToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "个人访问令牌。 \n使用阿里云AK+SK或使用STS临时授权方式不需要传该字段",
        "en": "The personal access token. This parameter is not required if you use an AccessKey pair or STS temporary authorization for authentication."
      }
    },
    {
      "name": "keyId",
      "position": "Path",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "SSH密钥的主键ID",
        "en": "The unique ID of the ssh key."
      }
    },
    {
      "name": "organizationId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "企业标识，也称企业id，字符串形式，可在云效访问链接中获取，如https://devops.aliyun.com/organization/【OrganizationId】",
        "en": "The ID of the organization. You can find the organization ID in the URL of the organization page on Alibaba Cloud DevOps. For example, in `https://devops.aliyun.com/organization/{organizationId}`, `{organizationId}` is the organization ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除指定的SSH密钥",
    "en": "Delete the specified Secure Shell key"
  }
}