{
  "name": "AddPipelineRelations",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/organization/[organizationId]/[pipelineId]/pipelineRelations",
  "parameters": [
    {
      "name": "organizationId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "企业标识，也称企业id，字符串形式，可在云效访问链接中获取，如  https:// devops.aliyun.com/organization/【OrgId】",
        "en": "The ID of the organization. You can obtain this ID from the URL of your organization in Alibaba Cloud DevOps, for example, `https://devops.aliyun.com/organization/{OrgId}`."
      }
    },
    {
      "name": "pipelineId",
      "position": "Path",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "流水线ID，可在流水线链接中获取，如https://flow.aliyun.com/pipelines/【PipelineId】/current",
        "en": "The ID of the pipeline. You can obtain this ID from the pipeline URL, for example, `https://flow.aliyun.com/pipelines/{PipelineId}/current`."
      }
    },
    {
      "name": "relObjectIds",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "关联的资源id，暂只支持变量组id，多个逗号分割",
        "en": "The IDs of the related resources. This parameter currently accepts only the IDs of variable groups. To specify multiple IDs, separate them with a comma."
      }
    },
    {
      "name": "relObjectType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "关联的资源类型，暂只支持VARIABLE_GROUP",
        "en": "The type of the related resource. The only value currently supported is `VARIABLE_GROUP`, which represents a variable group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "添加流水线关联",
    "en": "Add Pipeline Association"
  }
}