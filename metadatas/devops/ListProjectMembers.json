{
  "name": "ListProjectMembers",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/organization/[organizationId]/projects/[projectId]/listMembers",
  "parameters": [
    {
      "name": "organizationId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "企业标识，也称企业id，字符串形式，可在云效访问链接中获取，如https://devops.aliyun.com/organization/【OrganizationId】",
        "en": "The ID of the organization. You can get this ID from the Alibaba Cloud DevOps URL, for example, `https://devops.aliyun.com/organization/{organizationId}`."
      }
    },
    {
      "name": "projectId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "项目id，同spaceIdentifier",
        "en": "The ID of the project, which is equivalent to `spaceIdentifier`."
      }
    },
    {
      "name": "targetType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "请求资源的类型，这里填Space即可。",
        "en": "The resource type. The value must be Space."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取项目成员",
    "en": "Obtain Project Members"
  }
}