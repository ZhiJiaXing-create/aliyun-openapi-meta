{
  "name": "ListUserResources",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v4/user/vision/user_resources",
  "parameters": [
    {
      "name": "accessToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "个人访问令牌。\n使用阿里云AK+SK或使用STS临时授权方式不需要传该字段",
        "en": "The personal access token. You do not need to specify this parameter if you use an AccessKey pair or an STS token for authentication."
      }
    },
    {
      "name": "organizationId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "企业标识，也称企业id，字符串形式，可在云效访问链接中获取，如https://devops.aliyun.com/organization/【OrganizationId】",
        "en": "The ID of the enterprise. This parameter is a string. You can obtain the enterprise ID from the URL of your enterprise on the Alibaba Cloud DevOps platform. The URL is in the following format: https\\://devops.aliyun.com/organization/{OrganizationId}."
      }
    },
    {
      "name": "page",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "页码，默认值为1，从第1页开始",
        "en": "The page number of the page to return. The first page is page 1. The default value is 1."
      }
    },
    {
      "name": "pageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "每页大小，默认值为20，取值范围【1,100】",
        "en": "The number of entries to return on each page. The valid values are 1 to 100. The default value is 20."
      }
    },
    {
      "name": "userIds",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用户的阿里云账号ID列表，多个账号以逗号隔开",
        "en": "The comma-separated list of Alibaba Cloud account IDs of users."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取用户有权限的资源",
    "en": "Obtain resources to which the user has permission"
  }
}