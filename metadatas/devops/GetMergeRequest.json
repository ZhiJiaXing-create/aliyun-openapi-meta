{
  "name": "GetMergeRequest",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/api/v4/projects/[repositoryId]/merge_requests/[localId]/detail",
  "parameters": [
    {
      "name": "accessToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "个人访问令牌。\n使用阿里云AK+SK或使用STS临时授权方式不需要传该字段",
        "en": "The personal access token. This parameter is optional if you use an Alibaba Cloud AccessKey or STS temporary authorization."
      }
    },
    {
      "name": "localId",
      "position": "Path",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "合并请求局部ID，表示代码库下的第几个合并请求",
        "en": "The sequential ID of the merge request within the repository."
      }
    },
    {
      "name": "organizationId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "企业标识，也称企业id，字符串形式，可在云效访问链接中获取，如https://devops.aliyun.com/organization/【OrganizationId】",
        "en": "The organization ID. This ID is part of your Alibaba Cloud DevOps URL. For example: https\\://devops.aliyun.com/organization/{organizationId}."
      }
    },
    {
      "name": "repositoryId",
      "position": "Path",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "代码库ID",
        "en": "The ID of the repository."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询合并请求",
    "en": "Query merge requests"
  },
  "descriptions": {
    "zh": "该接口需要确保当前企业已经在使用新版MR，具体可看当前企业的合并请求页面是否已是新版本。",
    "en": "This API is for enterprises using the new MR, which can be confirmed by checking if the merge request page is the new version."
  }
}