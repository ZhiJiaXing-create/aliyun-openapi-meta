{
  "name": "MergeMergeRequest",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/api/v4/projects/[repositoryId]/merge_requests/[localId]/merge",
  "parameters": [
    {
      "name": "accessToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "个人访问令牌。\n使用阿里云AK+SK或使用STS临时授权方式不需要传该字段",
        "en": "The personal access token. This parameter is not required if you use an Alibaba Cloud AK/SK or STS temporary authorization for authentication."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求Body",
        "en": "The request body."
      }
    },
    {
      "name": "localId",
      "position": "Path",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "合并请求局部ID，表示代码库下的第几个合并请求",
        "en": "The local ID of the merge request. This ID is unique within a repository."
      }
    },
    {
      "name": "organizationId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "企业标识，也称企业id，字符串形式，可在云效访问链接中获取，如https://devops.aliyun.com/organization/【OrganizationId】",
        "en": "The ID of your organization. To get this ID, log in to the Alibaba Cloud DevOps console and copy it from your browser's address bar. For example, in the URL `https://devops.aliyun.com/organization/{organizationId}`, `{organizationId}` is the organization ID."
      }
    },
    {
      "name": "repositoryId",
      "position": "Path",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "代码库ID",
        "en": "The ID of the repository."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "合并合并请求",
    "en": "Merge Merge Request"
  },
  "descriptions": {
    "zh": "该接口需要确保当前企业已经在使用新版MR，具体可看当前企业的合并请求页面是否已是新版本。",
    "en": "This operation applies only to organizations that use the new merge request interface. To verify which interface you are using, check the Merge Request page in the Alibaba Cloud DevOps console."
  }
}