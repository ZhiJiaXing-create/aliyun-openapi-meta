{
  "name": "ListMergeRequestFilesReads",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/api/v4/projects/merge_requests/diffs/files_read_infos",
  "parameters": [
    {
      "name": "accessToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "个人访问令牌。\n使用阿里云AK+SK或使用STS临时授权方式不需要传该字段",
        "en": "The personal access token. This parameter is not required if you authenticate using an Alibaba Cloud AccessKey pair (AK/SK) or temporary credentials from the Security Token Service (STS)."
      }
    },
    {
      "name": "fromPatchSetBizId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "合并目标的版本业务ID\n\n> 关于from和to，在合并请求以及比较相关的接口中，from和to与常识是相反的，即 from应该是合并目标，to才是合并源，该顺序符合实际Git执行时的顺序，页面UI的源和目标实际上是经过处理后的，才会符合直觉。",
        "en": "The business ID of the patch set for the target branch.\n\n> In APIs for merge requests and comparisons, the `from` and `to` parameters can be counter-intuitive: `from` is the target branch and `to` is the source branch. This order matches the Git execution sequence. The UI reverses these terms for clarity."
      }
    },
    {
      "name": "localId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "合并请求局部ID，表示代码库下的第几个合并请求",
        "en": "The sequential ID of the merge request within the repository."
      }
    },
    {
      "name": "organizationId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "企业标识，也称企业id，字符串形式，可在云效访问链接中获取，如https://devops.aliyun.com/organization/【OrganizationId】",
        "en": "The ID of the organization. You can find this ID in your Alibaba Cloud DevOps URL, for example, `https://devops.aliyun.com/organization/[organizationId]`."
      }
    },
    {
      "name": "repositoryIdentity",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "代码库ID或者全路径",
        "en": "The ID or full path of the repository."
      }
    },
    {
      "name": "toPatchSetBizId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "合并源的版本业务ID\n\n> 关于from和to，在合并请求以及比较相关的接口中，from和to与常识是相反的，即 from应该是合并目标，to才是合并源，该顺序符合实际Git执行时的顺序，页面UI的源和目标实际上是经过处理后的，才会符合直觉。",
        "en": "The business ID of the patch set for the source branch.\n\n> In APIs for merge requests and comparisons, the `from` and `to` parameters can be counter-intuitive: `from` is the target branch and `to` is the source branch. This order matches the Git execution sequence. The UI reverses these terms for clarity."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询合并请求变更文件已读情况",
    "en": "Query read status of changed files in a merge request"
  }
}