{
  "name": "ListServiceVersions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/2021-04-06/services/[serviceName]/versions",
  "parameters": [
    {
      "name": "X-Fc-Account-Id",
      "position": "Header",
      "type": "String",
      "required": false,
      "description": {
        "zh": "您的阿里云账号（主账号）ID。",
        "en": "The ID of your Alibaba Cloud account."
      }
    },
    {
      "name": "X-Fc-Date",
      "position": "Header",
      "type": "String",
      "required": false,
      "description": {
        "zh": "函数的调用开始日期，格式为**EEE,d MMM yyyy HH:mm:ss GMT**。",
        "en": "The time when the function is invoked. The value is in the **EEE,d MMM yyyy HH:mm:ss GMT** format."
      }
    },
    {
      "name": "X-Fc-Trace-Id",
      "position": "Header",
      "type": "String",
      "required": false,
      "description": {
        "zh": "函数计算调用请求的调用链ID。",
        "en": "The trace ID of the request for Function Compute API."
      }
    },
    {
      "name": "direction",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "限定返回的版本排序方向：\n  - **FORWARD**：从小到大排序。\n  - **BACKWARD**：默认值，从大到小排序。",
        "en": "The order in which the returned versions are sorted. Valid values:\n\n*   **FORWARD**: The results are sorted in ascending order.\n*   **BACKWARD**: The results are sorted in descending order."
      }
    },
    {
      "name": "limit",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "限定此次返回资源的数量。如果不设定，默认返回20，最大不能超过100。返回结果可以小于指定的数量，但不会多于指定的数量。",
        "en": "The maximum number of results to return. Default value: 20. Maximum value: 100. The number of returned configurations is less than or equal to the specified number."
      }
    },
    {
      "name": "nextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用来返回更多结果。第一次查询不需要提供这个参数，后续查询的Token从返回结果中获取。",
        "en": "The token required to obtain more results. If the number of layers exceeds the limit, the nextToken parameter is returned. You can include the parameter in subsequent calls to obtain more results. You do not need to provide this parameter in the first call."
      }
    },
    {
      "name": "serviceName",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "服务的名称。",
        "en": "The name of the service."
      }
    },
    {
      "name": "startKey",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设定结果从startKey之后（包括startKey）按版本号排序的第一个开始返回。",
        "en": "The position from which the list begins. Results are sorted based on the version numbers."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取服务版本列表",
    "en": "ListServiceVersions"
  },
  "example": {
    "unifiedCli": "aliyun fc-open list-service-versions --service-name demo-service --limit 20 --next-token 8bj81uI8n0000 --start-key nextservice --direction BACKWARD --x-fc-account-id 1880770869020000 --x-fc-date Wed,_11_May_2022_09:00:00_GMT --x-fc-trace-id rid281s000000 --api-version 2021-04-06 --region cn-hangzhou",
    "legacyCli": "aliyun fc-open GET /2021-04-06/services/demo-service/versions --region cn-hangzhou --startKey nextservice --nextToken 8bj81uI8n0000 --limit 20 --direction BACKWARD --header \"X-Fc-Date=Wed,_11_May_2022_09:00:00_GMT\" --header \"X-Fc-Trace-Id=rid281s000000\" --header \"X-Fc-Account-Id=1880770869020000\" --header \"Content-Type=application/json\""
  }
}