{
  "name": "UpdateInstanceGroupImage",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ImageId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "镜像ID。",
        "en": "The image ID."
      }
    },
    {
      "name": "InstanceGroupIds",
      "position": "Body",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "实例组ID列表。",
        "en": "A list of instance group IDs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "变更实例组镜像",
    "en": "UpdateInstanceGroupImage"
  },
  "descriptions": {
    "zh": "镜像的状态为可用且可用地域中包含实例组的地域，实例组状态为可用。",
    "en": "The image and the instance group must be in the active state. The image must be available in the same region as the instance group."
  }
}