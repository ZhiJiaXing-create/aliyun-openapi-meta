{
  "name": "RecoveryFile",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AndroidInstanceIdList",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "实例ID列表。",
        "en": "The IDs of the instances."
      }
    },
    {
      "name": "BackupAll",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否整机备份。",
        "en": "Specifies whether to back up the whole instance."
      }
    },
    {
      "name": "BackupFileId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份文件ID。",
        "en": "The ID of the backup file."
      }
    },
    {
      "name": "BackupFilePath",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份文件上传的OSS地址。\n<notice>调用describeBuckets接口获取bucketName，选择ossObjectList获取key，key是备份文件的路径，拼接成“oss://${bucketName}/${key}”。</notice>",
        "en": "The OSS path to which the backup file is uploaded.\n\n> When calling the describeBuckets operation to retrieve a bucket name, you must also call the ossObjectList operation to obtain the object key. Combine these to form the full path: oss\\://${bucketName}/${key}."
      }
    },
    {
      "name": "UploadEndpoint",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份地址的域名。\n<notice>调用DescribeBuckets接口获取Bucket，如果云手机和bucket是同地域，获取IntranetEndpoint字段；如果是不同地域获取ExtranetEndpoint字段。></notice>",
        "en": "The endpoint of the OSS bucket that stores the backup file.\n\n> : When calling the DescribeBuckets operation to query buckets, retrieve the IntranetEndpoint value if the cloud phone and the OSS bucket are in the same region. If they are in different regions, retrieve the ExtranetEndpoint value instead."
      }
    },
    {
      "name": "UploadType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份类型。",
        "en": "The type of the backup."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "恢复备份文件",
    "en": "RecoveryFile"
  },
  "descriptions": {
    "zh": "目前仅支持云手机生成的备份文件保存在OSS对象存储服务。",
    "en": "Currently, this operation allows you to restore only backup files generated by cloud phones that are stored in Object Storage Service (OSS) buckets."
  }
}