{
  "name": "DeleteKeyPairs",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "KeyPairIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "密钥对ID列表。",
        "en": "The IDs of the ADB key pairs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除ADB密钥对",
    "en": "DeleteKeyPairs"
  },
  "descriptions": {
    "zh": "- 如果已经有云手机实例绑定了该ADB 密钥对，则该密钥对不支持被删除。\n- 删除ADB密钥对后，无法通过DescribeKeyPairs 查询到该ADB 密钥对。",
    "en": "*   If a cloud phone instance is currently associated with the ADB key pair you intend to delete, the ADB key pair cannot be deleted.\n*   Once an ADB key pair is deleted, it cannot be retrieved or queried by using the DescribeKeyPairs operation."
  }
}