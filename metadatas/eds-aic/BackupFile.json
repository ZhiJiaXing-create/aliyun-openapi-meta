{
  "name": "BackupFile",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AndroidInstanceIdList",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "实例ID列表。",
        "en": "A list of instance IDs."
      }
    },
    {
      "name": "BackupAll",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否整机备份。",
        "en": "Specifies whether to back up the entire instance."
      }
    },
    {
      "name": "BackupFileName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份文件名称。",
        "en": "The name of the backup file."
      }
    },
    {
      "name": "BackupFilePath",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "备份文件的上传地址。\n> 如果上传到OSS Bucket，调用describeBuckets接口获取bucketName，选择ossObjectList获取key，key是OSS Bucket的文件夹路径，拼接成`oss://${bucketName}/${key}`。",
        "en": "The upload URL for the backup file.\n\n> If you upload the file to an OSS bucket, call the DescribeBuckets operation to get the bucketName. Then, select a key from ossObjectList. The key represents the folder path in the OSS bucket. Combine these values into the format `oss://${bucketName}/${key}`."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份文件描述。",
        "en": "The description of the backup file."
      }
    },
    {
      "name": "ExcludeSourceFilePathList",
      "position": "Query",
      "type": "RepeatList",
      "required": false
    },
    {
      "name": "SourceAppList",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "备份的应用包名列表。",
        "en": "A list of application package names to back up."
      }
    },
    {
      "name": "SourceFilePathList",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "备份的文件路径列表。",
        "en": "A list of file paths to back up."
      }
    },
    {
      "name": "UploadEndpoint",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份地址的域名。\n\n> 如果上传到OSS Bucket，调用describeBuckets接口获取Bucket，如果云手机和Bucket是同地域，获取intranetEndpoint字段；如果是不同地域获取extranetEndpoint字段。",
        "en": "The domain name of the upload URL.\n\n> If you upload the file to an OSS bucket, call the DescribeBuckets operation to obtain the bucket information. If the cloud phone and the bucket are in the same region, use the value of the intranetEndpoint field. If they are in different regions, use the value of the extranetEndpoint field."
      }
    },
    {
      "name": "UploadType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "备份类型。",
        "en": "The backup type."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建备份文件",
    "en": "BackupFile"
  },
  "descriptions": {
    "zh": "目前仅支持云手机生成的备份文件保存在OSS对象存储服务。",
    "en": "You can save backup files generated by cloud phones only to Object Storage Service (OSS)."
  }
}