{
  "name": "RenewMobileAgentPackage",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPay",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否自动支付。取值范围：\n\n- **true**：自动支付。您需要确保账户余额充足。\n- **false**（默认）：只生成订单不扣费。\n\n\n\n\n> 如果您的支付方式余额不足，可以将此参数设置为false，此时会生成未支付订单，您可以登录无影云手机管理控制台自行支付。\n>",
        "en": "Specifies whether to enable automatic payment. Valid values:\n\n- **true**: Enables automatic payment. Make sure that your account balance is sufficient.\n\n- **false** (default): Generates an unpaid order.\n\n> If your account balance is insufficient, set this parameter to `false` to generate an unpaid order. You can then pay for the order in the Wuying Mobile Cloud Phone management console."
      }
    },
    {
      "name": "AutoRenew",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否开启自动续费。默认为false。",
        "en": "Specifies whether to enable auto-renewal. The default value is `false`."
      }
    },
    {
      "name": "MobileAgentPackageIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "套餐包ID列表。",
        "en": "A list of mobile agent package IDs."
      }
    },
    {
      "name": "PaidCallbackUrl",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "支付成功页跳转链接。",
        "en": "The URL to which a user is redirected after a successful payment."
      }
    },
    {
      "name": "Period",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "购买资源的时长，单位由`PeriodUnit`指定。",
        "en": "The renewal period. The `PeriodUnit` parameter specifies the time unit."
      }
    },
    {
      "name": "PeriodUnit",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "购买资源的时长单位。\n取值范围：\n- **Month**：月。\n- **Year**：年。",
        "en": "The unit of the renewal period.\nValid values:\n\n- **Month**: month.\n\n- **Year**: year."
      }
    },
    {
      "name": "PromotionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "优惠活动ID。",
        "en": "The promotion ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "续费套餐包",
    "en": "RenewMobileAgentPackage"
  }
}