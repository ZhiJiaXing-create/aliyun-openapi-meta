{
  "name": "ModifyAndroidInstanceGroup",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例组ID。",
        "en": "The ID of the instance group."
      }
    },
    {
      "name": "NewInstanceGroupName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例组的新名称。\n\n> \n> - 实例组名称长度不超过30个字符；请以大/小写字母或中文开头，不能以http://和https://开头，仅支持中文、英文、数字、半角冒号（:）、下划线（_）、点号（.）或者连字符（-）",
        "en": "The new name of the instance group.\n\n> The name can be up to 30 characters in length. It can contain letters, digits, colons (:), underscores (\\_), periods (.), or hyphens (-). It must start with letters but cannot start with http\\:// or https\\://."
      }
    },
    {
      "name": "PolicyGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "策略ID。",
        "en": "The ID of the policy."
      }
    },
    {
      "name": "StreamMode",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "实例组下实例的串流模式，该参数不传默认为抢占模式。",
        "en": "Specifies the streaming mode for instances within the instance group. If this parameter is omitted, the mode defaults to 1 (Preemptive)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改实例组信息",
    "en": "ModifyAndroidInstanceGroup"
  }
}