{
  "name": "CreateScreenshot",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AndroidInstanceIdList",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "实例ID列表，支持批量截图。",
        "en": "The list of instance IDs. Batch screenshots are supported."
      }
    },
    {
      "name": "OssBucketName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自定义OSS桶，桶名必须“cloudphone-saved-bucket-”为前缀，云手机实例和OSS桶地域需要保持一致。空默认创建桶“cloudphone-saved-bucket-{云手机所在地域}-{AliUid}”。",
        "en": "The custom OSS bucket. The bucket name must start with the cloudphone-saved-bucket- prefix. The cloud phone instance and the OSS bucket must be in the same region. If you leave this parameter empty, a default bucket named cloudphone-saved-bucket-{RegionOfCloudPhone}-{AliUid} is created."
      }
    },
    {
      "name": "ScreenshotId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "截屏ID。生成的截屏图片命名为“ ScreenshotId_AndroidInstanceId.png”。\n\n><notice>ScreenshotId 长度为 2~128 个字符，以英文字母或中文开头，不能以http://和https://开头，可包含数字、下划线（_）和短划线（-）。></notice>",
        "en": "The screenshot ID. The generated screenshot is named \"ScreenshotId\\_AndroidInstanceId.png.\"\n\n><notice>\n\nThe ScreenshotId must be 2 to 128 characters long, beginning with a letter, but cannot start with http\\:// or https\\://. Allowed characters include letters, digits, underscores (\\_), and hyphens (-).\n\n></notice>"
      }
    },
    {
      "name": "SkipCheckPolicyConfig",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否跳过截图策略控制，默认false。",
        "en": "Specifies whether to skip the screenshot policy check. The default value is false."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "生成一张云手机的截屏图片",
    "en": "CreateScreenshot"
  },
  "descriptions": {
    "zh": "创建一张云手机的截屏图片并上传到默认的OSS桶，接口返回TaskId，可以通过DescribeTasks接口查询截图的下载链接。",
    "en": "This operation creates a screenshot of a cloud phone and uploads it to the default Object Storage Service (OSS) bucket. The operation returns a task ID. You can then call the DescribeTasks operation to retrieve the download link for the screenshot."
  }
}