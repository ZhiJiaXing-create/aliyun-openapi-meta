{
  "name": "DescribeClusters",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/clusters",
  "parameters": [
    {
      "name": "clusterType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "集群类型。",
        "en": "The cluster type."
      }
    },
    {
      "name": "name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "根据集群Name进行模糊匹配查询。",
        "en": "Perform a fuzzy match query based on the cluster name."
      }
    },
    {
      "name": "resource_group_id",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "（旧）查询用户所有集群实例",
    "en": "(Deprecated) Query all cluster instances of a user"
  },
  "example": {
    "unifiedCli": "aliyun cs describe-clusters --name test --cluster-type ManagedKubernetes --resource-group-id example-string --region cn-hangzhou",
    "legacyCli": "aliyun cs GET /clusters --region cn-hangzhou --clusterType ManagedKubernetes --resource_group_id 'example-string' --name test --header \"Content-Type=application/json\""
  }
}