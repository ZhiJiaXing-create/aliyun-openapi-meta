{
  "name": "CleanClusterUserPermissions",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "DELETE",
  "pathPattern": "/cluster/[ClusterId]/user/[Uid]/permissions",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    },
    {
      "name": "Force",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否强制删除指定的KubeConfig，取值：\n- false（默认值）：删除KubeConfig前校验是否在七天内存在相关的集群访问记录，如果存在或无法查找集群访问记录，则不允许删除。\n- true：强制删除KubeConfig，不检查集群访问记录。",
        "en": "Specifies whether to force delete the specified KubeConfig. Valid values:\n- false (default): Before deleting the KubeConfig, the system checks whether cluster access records exist within the last seven days. If access records exist or cannot be retrieved, the deletion is not allowed.\n- true: Force deletes the KubeConfig without checking cluster access records."
      }
    },
    {
      "name": "Uid",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "阿里云账号内指定RAM用户或RAM角色ID。",
        "en": "The ID of the specified Resource Access Management (RAM) user or RAM role within the Alibaba Cloud account."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "清理指定用户在指定集群已下发KubeConfig凭据和RBAC权限",
    "en": "Clean up kubeConfig credentials and RBAC permissions for a specified user in a specified cluster"
  },
  "descriptions": {
    "zh": "> 1. 需要拥有管理容器服务（CS）的权限（AliyunCSFullAccess） \n> 2. 不支持清除阿里云账号权限\n> 3. 不支持清除操作用户本身的权限",
    "en": "> 1. You must have the permission to manage Container Service for Kubernetes (ACK) (AliyunCSFullAccess). \n> 2. Cleaning up permissions of an Alibaba Cloud account is not supported.\n> 3. Cleaning up the permissions of the user who performs this operation is not supported."
  },
  "example": {
    "unifiedCli": "aliyun cs clean-cluster-user-permissions --cluster-id c5b5e80b0b64a4bf6939d2d8fbbc50000 --uid 2xxxxxxxxxxxxxxxxxx --biz-force false --region cn-hangzhou",
    "legacyCli": "aliyun cs DELETE /cluster/c5b5e80b0b64a4bf6939d2d8fbbc50000/user/2xxxxxxxxxxxxxxxxxx/permissions --region cn-hangzhou --Force false --header \"Content-Type=application/json\""
  }
}