{
  "name": "UpdateK8sClusterUserConfigExpire",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/k8s/[ClusterId]/user_config/expire",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求体参数。",
        "en": "The request body parameters."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新用户自定义配置的过期时间",
    "en": "Update the expiration time of user-defined configuration"
  },
  "descriptions": {
    "zh": "- 该接口只允许被主账号调用。\n- 如果吊销了您在该集群上使用的KubeConfig凭证，设置在该集群KubeConfig自定义过期时间也会被重置，您需要调用该接口重新设置。",
    "en": "- This operation can be called only by an Alibaba Cloud account.\n- If you revoke the KubeConfig credential used in the cluster, the custom expiration time configured for the KubeConfig of the cluster is also reset. You must call this operation to reconfigure the expiration time."
  },
  "example": {
    "unifiedCli": "aliyun cs update-k8s-cluster-user-config-expire --cluster-id c5b5e80b0b64a4bf6939d2d8fbbc50000 --expire-hour 720 --user 206945240368000000 --region cn-hangzhou",
    "legacyCli": "aliyun cs POST /k8s/c5b5e80b0b64a4bf6939d2d8fbbc50000/user_config/expire --region cn-hangzhou --header \"Content-Type=application/json\" --body {\"expire_hour\":720,\"user\":\"206945240368000000\"}"
  }
}