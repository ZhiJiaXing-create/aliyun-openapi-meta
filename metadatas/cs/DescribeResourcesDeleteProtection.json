{
  "name": "DescribeResourcesDeleteProtection",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/clusters/[ClusterId]/resources/[ResourceType]/protection",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "ACK集群ID。",
        "en": "The ACK cluster ID."
      }
    },
    {
      "name": "ResourceType",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待查询的资源类型。取值如下：\n\n- namespaces：命名空间。\n\n- services：服务。",
        "en": "The type of resource to query. Valid values:\n\n- namespaces: namespace.\n\n- services: service."
      }
    },
    {
      "name": "namespace",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "待查询资源所在的命名空间。\n\nresource_type选择为services时该参数必须填写，若未填写则默认按照命名空间为default处理。",
        "en": "The namespace of the resource to query.\n\nThis parameter is required when resource_type is set to services. If this parameter is not specified, the namespace defaults to default."
      }
    },
    {
      "name": "resources",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询的目标资源名称，多个资源以英文半角逗号（,）分割。\n\n- resource_type选择为namespaces时，本参数需要填写为命名空间名称。若不填写，则查询集群下所有命名空间的删除保护状态。\n\n- resource_type选择为services时，本参数必须填写，需要填写为服务名称。",
        "en": "The name of the resource to query. Separate multiple resources with commas (,).\n\n- If resource_type is set to namespaces, set this parameter to namespace names. If this parameter is not specified, the deletion protection status of all namespaces in the cluster is queried.\n\n- If resource_type is set to services, this parameter is required. Set this parameter to service names."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询指定资源是否开启删除保护",
    "en": "Query whether deletion protection is enabled for a specified resource"
  },
  "example": {
    "unifiedCli": "aliyun cs describe-resources-delete-protection --cluster-id c82e6987e2961451182edacd74faf0000 --resource-type namespaces --namespace default --resources test1,test2 --region cn-hangzhou",
    "legacyCli": "aliyun cs GET /clusters/c82e6987e2961451182edacd74faf0000/resources/namespaces/protection --region cn-hangzhou --namespace default --resources 'test1,test2' --header \"Content-Type=application/json\""
  }
}