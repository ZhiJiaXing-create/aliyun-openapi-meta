{
  "name": "OpenAckService",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/service/open",
  "parameters": [
    {
      "name": "type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "待开通的服务类型。可选值：\n\n- `propayasgo`：容器服务Kubernetes版（包括ACK托管集群和ACK专有集群）、容器服务Serverless版、注册集群。\n- `edgepayasgo`：容器服务Edge版。",
        "en": "The type of service to activate. Valid values:\n\n- `propayasgo`: Container Service for Kubernetes (including ACK managed clusters and ACK dedicated clusters), Container Service Serverless, and registered clusters.\n- `edgepayasgo`: Container Service Edge."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "开通容器服务Kubernetes版",
    "en": "Activate container service for kubernetes"
  },
  "descriptions": {
    "zh": "- 阿里云账号（主账号）可以开通容器服务ACK。\n- RAM用户（子账号）有AdministratorAccess权限的子账号可以开通服务。",
    "en": "- An Alibaba Cloud account can activate ACK.\n- A Resource Access Management (RAM) user that has the AdministratorAccess permission can activate the service."
  },
  "example": {
    "unifiedCli": "aliyun cs open-ack-service --type propayasgo --region cn-hangzhou",
    "legacyCli": "aliyun cs POST /service/open --region cn-hangzhou --type propayasgo --header \"Content-Type=application/json\""
  }
}