{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/regions",
  "parameters": [
    {
      "name": "acceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "根据汉语、英语筛选返回结果。",
        "en": "Filters the results by language (Chinese or English)."
      }
    },
    {
      "name": "clusterType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "集群类型。",
        "en": "The cluster type."
      }
    },
    {
      "name": "profile",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "托管类集群子类型。",
        "en": "The subtype of managed clusters."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询地域列表",
    "en": "Query regions"
  },
  "example": {
    "unifiedCli": "aliyun cs describe-regions --cluster-type ManagedKubernetes --biz-profile Default --accept-language zh-CN --region cn-hangzhou",
    "legacyCli": "aliyun cs GET /regions --region cn-hangzhou --clusterType ManagedKubernetes --acceptLanguage 'zh-CN' --profile Default --header \"Content-Type=application/json\""
  }
}