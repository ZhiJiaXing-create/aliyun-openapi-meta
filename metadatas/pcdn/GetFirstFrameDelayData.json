{
  "name": "GetFirstFrameDelayData",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BusinessType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "业务类型，竖线分隔，全部填all。",
        "en": "The business type, separated by vertical lines (|). Set this parameter to all to query all business types."
      }
    },
    {
      "name": "Domain",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "待查询的域名列表，以竖线分割，不传查询全部。",
        "en": "The list of domain names to query, separated by vertical lines (|). If this parameter is not specified, all domain names are queried."
      }
    },
    {
      "name": "EndDate",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "结束时间。",
        "en": "The end time."
      }
    },
    {
      "name": "IspName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "运营商，竖线分隔，全部填all。",
        "en": "The Internet service provider (ISP), separated by vertical lines (|). Set this parameter to all to query all ISPs."
      }
    },
    {
      "name": "PlatformType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "终端，竖线分隔，全部填all。",
        "en": "The terminal type, separated by vertical lines (|). Set this parameter to all to query all terminal types."
      }
    },
    {
      "name": "Region",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地区，竖线分隔，全部填all。",
        "en": "The region, separated by vertical lines (|). Set this parameter to all to query all regions."
      }
    },
    {
      "name": "StartDate",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "开始时间。",
        "en": "The start time."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取加速域名首帧延时数据",
    "en": "Retrieve first frame latency data for accelerated domain names"
  },
  "descriptions": {
    "zh": "**调用该接口前，请您注意：**\n\n- 不指定StartTime和EndTime时，默认读取过去24小时的数据；指定StartTime和EndTime时，按指定的起止时间查询。\n\n- 支持批量域名查询，多个域名用竖线（|）分隔。\n\n- 最多可获取最近90天的数据。",
    "en": "**Before calling this operation, note the following:**\n\n- If you do not specify StartTime or EndTime, data from the past 24 hours is returned by default. If you specify StartTime and EndTime, data within the specified time range is returned.\n\n- Batch domain name queries are supported. Separate multiple domain names with vertical lines (|).\n\n- You can retrieve data from the last 90 days."
  }
}