{
  "name": "PredictPreTrainModel",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BinaryToText",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "content字段内容为图片URL时：false，\nbody字段内容为图片base64时：true",
        "en": "Set this parameter to `false` when the `Content` parameter contains an image URL. Set this parameter to `true` when the `Body` parameter contains Base64-encoded image data."
      }
    },
    {
      "name": "Body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "图片传递base64编码时的预测内容，格式为json字符串。\nUIE抽取参数格式{\"query\":\"xxx\",\"schema\":[\"字段1\",\"字段2\"]}, query对应图片base64编码，schema对应要抽取的字段数组",
        "en": "The prediction content for a Base64-encoded image, in JSON string format. For UIE extraction, the format is `{\"query\":\"\",\"schema\":[\"\",\"\"]}`, where `query` specifies the Base64-encoded image data and `schema` specifies the fields to extract."
      }
    },
    {
      "name": "Content",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预测内容，格式为JSON字符串。\nUIE抽取参数格式{\"query\":\"xxx\",\"schema\":[\"字段1\",\"字段2\"]}, query对应图片URL，schema对应要抽取的字段数组",
        "en": "The prediction content, in JSON string format. For UIE extraction, the format is `{\"query\":\"\",\"schema\":[\"\",\"\"]}`, where `query` specifies the image URL and `schema` specifies the fields to extract."
      }
    },
    {
      "name": "ServiceName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预置能力服务名称，必填\nUIE抽取对应“FormUIE”",
        "en": "The name of the pre-built capability service. For UIE extraction, set this parameter to `FormUIE`."
      }
    },
    {
      "name": "ServiceVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预置能力服务版本，默认V1",
        "en": "The version of the pre-built capability service. The default value is `V1`."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "预置能力服务预测API",
    "en": "Preset Capability Service Prediction API"
  }
}