{
  "name": "CancelTask",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "/openapi/instances/[InstanceId]/actions/cancel-task",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "clientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大不超过64个ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the value, but you must make sure that the value is unique among different requests. The token can be up to 64 ASCII characters in length."
      }
    },
    {
      "name": "taskType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "任务类型，固定为MigrateData，表示数据迁移任务。",
        "en": "The task type. Set this parameter to MigrateData, which indicates a data migration task."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "取消数据迁移任务",
    "en": "Cancel a data migration task"
  },
  "example": {
    "unifiedCli": "aliyun elasticsearch cancel-task --instance-id es-cn-nif1q9o8r00080000 --task-type MigrateData --client-token 5A2CFF0E-5718-45B5-9D4D-70B3FF0000 --api-version 2017-06-13 --region cn-hangzhou",
    "legacyCli": "aliyun elasticsearch POST /openapi/instances/es-cn-nif1q9o8r00080000/actions/cancel-task --region cn-hangzhou --taskType MigrateData --clientToken '5A2CFF0E-5718-45B5-9D4D-70B3FF0000' --header \"Content-Type=application/json\""
  }
}