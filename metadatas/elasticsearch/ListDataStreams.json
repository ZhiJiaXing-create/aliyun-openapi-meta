{
  "name": "ListDataStreams",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "/openapi/instances/[InstanceId]/data-streams",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The instance ID."
      }
    },
    {
      "name": "isManaged",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只显示托管中的索引，取值含义如下：\n\n- true：只显示托管中的索引。\n\n- false（默认值）：显示全部索引。",
        "en": "Specifies whether to display only managed indexes. Valid values:\n\n- true: displays only managed indexes.\n\n- false (default): displays all indexes."
      }
    },
    {
      "name": "name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "数据流名称。",
        "en": "The data stream name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "ES集群数据流列表",
    "en": "List data streams of an elasticsearch cluster"
  },
  "example": {
    "unifiedCli": "aliyun elasticsearch list-data-streams --instance-id es-cn-nif24adwc00820000 --is-managed false --name Log1 --api-version 2017-06-13 --region cn-hangzhou",
    "legacyCli": "aliyun elasticsearch GET /openapi/instances/es-cn-nif24adwc00820000/data-streams --region cn-hangzhou --name Log1 --isManaged false --header \"Content-Type=application/json\""
  }
}