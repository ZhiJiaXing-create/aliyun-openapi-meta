{
  "name": "DeleteConnectedCluster",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/openapi/instances/[InstanceId]/connected-clusters",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "当前实例ID。",
        "en": "The instance ID of the current instance."
      }
    },
    {
      "name": "clientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大不超过64个ASCII字符。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters and cannot exceed 64 characters in length."
      }
    },
    {
      "name": "connectedInstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "已进行网络互通的远程实例ID。",
        "en": "The instance ID of the remote instance that is connected over the network."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "移除互通实例",
    "en": "Remove a connected instance"
  },
  "example": {
    "unifiedCli": "aliyun elasticsearch delete-connected-cluster --instance-id es-cn-n6w1o1x0w001c0000 --client-token 5A2CFF0E-5718-45B5-9D4D-70B3FF0000 --connected-instance-id es-cn-09k1rgid9000g0000 --api-version 2017-06-13 --region cn-hangzhou",
    "legacyCli": "aliyun elasticsearch DELETE /openapi/instances/es-cn-n6w1o1x0w001c0000/connected-clusters --region cn-hangzhou --clientToken '5A2CFF0E-5718-45B5-9D4D-70B3FF0000' --connectedInstanceId 'es-cn-09k1rgid9000g0000' --header \"Content-Type=application/json\""
  }
}