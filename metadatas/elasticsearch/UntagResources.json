{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "DELETE",
  "pathPattern": "/openapi/tags",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否全部删除，默认为**false**。仅当**TagKeys**为空时有效。",
        "en": "Specifies whether to delete all tags. Default value: **false**. This parameter is valid only when **TagKeys** is empty."
      }
    },
    {
      "name": "ResourceIds",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要删除的资源列表。",
        "en": "The list of resources to delete."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源类型。固定为**INSTANCE**。",
        "en": "The resource type. Set this to **INSTANCE**."
      }
    },
    {
      "name": "TagKeys",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要删除的标签列表，最多包含20个子项。",
        "en": "The list of tags to delete. A maximum of 20 items can be specified."
      }
    },
    {
      "name": "body",
      "position": "Query",
      "type": "String",
      "required": false
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除用户资源标签关系",
    "en": "Delete user resource tag associations"
  },
  "descriptions": {
    "zh": "调用该接口时，请注意：\n- 只能删除用户标签。\n  \n    > 用户标签是指用户手动给实例添加的标签。对应的系统标签是指云产品（即阿里云服务）给用户实例添加的标签。系统标签分为可见标签和不可见标签。\n\n- 对于没有关联任何资源的标签，在删除资源标签关系时，对应标签也要被删除。",
    "en": "When calling this operation, note the following:\n- Only user tags can be deleted.\n  \n    > User tags are tags that users manually add to instances. System tags are tags that Alibaba Cloud services add to user instances. System tags are classified into visible tags and invisible tags.\n\n- If a tag is not associated with any resource, the tag is also deleted when the resource tag association is deleted."
  },
  "example": {
    "unifiedCli": "aliyun elasticsearch untag-resources --resource-ids es-cn-09k1rocex00060000_,_es-cn-oew1rgiev00090000 --resource-type INSTANCE --tag-keys tagKey1_,_tagKey2 --all false --biz-body example-string --api-version 2017-06-13 --region cn-hangzhou",
    "legacyCli": "aliyun elasticsearch DELETE /openapi/tags --region cn-hangzhou --All false --ResourceIds 'es-cn-09k1rocex00060000_,_es-cn-oew1rgiev00090000' --ResourceType INSTANCE --body 'example-string' --TagKeys 'tagKey1_,_tagKey2' --header \"Content-Type=application/json\""
  }
}