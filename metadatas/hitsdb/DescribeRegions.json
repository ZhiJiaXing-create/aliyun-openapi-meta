{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "返回结果中地域名称（LocalName）的显示语言，取值：\n\n- **zh-CN**：中文，默认值。\n- **en-US**：英文。",
        "en": "The language to use for the region name (LocalName) in the response. Valid values:\n\n- **zh-CN**: Chinese. This is the default value.\n\n- **en-US**: English."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取Lindorm产品支持的所有地域",
    "en": "DescribeRegions"
  },
  "example": {
    "unifiedCli": "aliyun hitsdb describe-regions --accept-language en-US --api-version 2020-06-15 --region cn-hangzhou",
    "legacyCli": "aliyun hitsdb DescribeRegions --region cn-hangzhou --AcceptLanguage 'en-US'"
  }
}