{
  "name": "DescribeSDGs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceIds",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "待查询的AIC实例ID。",
        "en": "The AIC instance ID to be queried."
      }
    },
    {
      "name": "SDGIds",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "待查询的SDG列表ID。",
        "en": "The IDs of SDGs that you want to query. By default, all SDGs are queried."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询SDG信息",
    "en": "Query SDG information"
  },
  "example": {
    "unifiedCli": "aliyun ens describe-sdgs --sdg-ids sdg-1 --instance-ids aic-1 --region cn-hangzhou",
    "legacyCli": "aliyun ens DescribeSDGs --region cn-hangzhou --SDGIds '[\"sdg-1\"]' --InstanceIds '[\"aic-1\"]'"
  }
}