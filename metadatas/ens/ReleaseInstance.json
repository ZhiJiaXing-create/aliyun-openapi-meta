{
  "name": "ReleaseInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "EIP实例的ID。",
        "en": "The ID of the EIP."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "释放实例",
    "en": "ReleaseInstance"
  },
  "descriptions": {
    "zh": "- API调用频率：10000次/秒。\n- 单用户调用频率：50次/秒。",
    "en": "- up to 10,000 times per second per account.\n\n- The maximum number of times that each user can call this operation per second is 50."
  },
  "example": {
    "unifiedCli": "aliyun ens release-instance --instance-id eip-0000 --region cn-hangzhou",
    "legacyCli": "aliyun ens ReleaseInstance --region cn-hangzhou --InstanceId 'eip-0000'"
  }
}