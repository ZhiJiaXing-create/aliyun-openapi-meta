{
  "name": "CreateEnsService",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EnsServiceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需求资源ID。仅支持传入单个ID。",
        "en": "The ID of the resource that you want to obtain. You can specify only one ID in a request."
      }
    },
    {
      "name": "OrderType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "创建边缘服务预览后的操作。取值：\n\n- **Buy**：创建。\n\n- **Upgrade**：修改。",
        "en": "The operation to perform after you preview the created edge service. Valid values:\n\n- **Buy**: create\n\n- **Upgrade**: change"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建边缘服务",
    "en": "CreateEnsService"
  },
  "example": {
    "unifiedCli": "aliyun ens create-ens-service --ens-service-id ens-201908060000 --order-type Buy --region cn-hangzhou",
    "legacyCli": "aliyun ens CreateEnsService --region cn-hangzhou --OrderType Buy --EnsServiceId 'ens-201908060000'"
  }
}