{
  "name": "PreloadRegionSDG",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DestinationRegionIds",
      "position": "Query",
      "type": "Json",
      "required": true,
      "description": {
        "zh": "目的节点列表。",
        "en": "The IDs of the destination nodes."
      }
    },
    {
      "name": "DiskType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "生产的共享盘磁盘类型：\n\n- standard（标准云盘）。\n\n- rbd（rbd云盘）。\n\n>\n>- 不指定共享盘类型时默认生产标准云盘。",
        "en": "The disk type of the shared disk to be created:\n\n- standard (standard disk).\n\n- rbd (rbd disk).\n\n> * If no shared disk type is specified, a standard disk is created by default."
      }
    },
    {
      "name": "Namespaces",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "命名空间列表。",
        "en": "An array that consists of queried namespaces."
      }
    },
    {
      "name": "RedundantNum",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "冗余数量，以支持快速部署。",
        "en": "The number of redundant replicas to support rapid deployment."
      }
    },
    {
      "name": "SDGId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "期望预加载数据的SDG ID。",
        "en": "The ID of the SDG for which data is preloaded."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "预加载SDG",
    "en": "Preload SDG"
  },
  "example": {
    "unifiedCli": "aliyun ens preload-region-sdg --sdg-id sdg-xxxx --destination-region-ids cn-foshan-4 --namespaces default --redundant-num 2 --disk-type rbd --region cn-hangzhou",
    "legacyCli": "aliyun ens PreloadRegionSDG --region cn-hangzhou --SDGId 'sdg-xxxx' --DiskType rbd --Namespaces '[\"default\"]' --DestinationRegionIds '[\"cn-foshan-4\"]' --RedundantNum 2"
  }
}