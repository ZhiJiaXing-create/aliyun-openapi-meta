{
  "name": "DescribeNetworkPeerConnections",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EnsRegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "ENS的节点ID。",
        "en": "The ID of the ENS node."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "网络对等连接的实例ID。",
        "en": "The ID of the network peer connection."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "网络对等连接名称。",
        "en": "The name of the network peer connection."
      }
    },
    {
      "name": "NetworkIds",
      "position": "Query",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "根据两端网络ID过滤，不区分发起端和接收端。",
        "en": "The IDs of the networks involved in the connection. The query does not distinguish between the requester and accepter."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "列表的页码，默认值为**1**。",
        "en": "The page number for the returned results. Default: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数。默认值：10，取值：**10**~**100**。",
        "en": "The number of entries to return on each page. Default: **10**. Valid values: **10** to **100**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询网络对等连接列表",
    "en": "DescribeNetworkPeerConnections"
  },
  "example": {
    "unifiedCli": "aliyun ens describe-network-peer-connections --instance-id pcc-5inkeimcipxk26yqtzm4q0000 --ens-region-id cn-xian-unicom --name test0 --network-ids n-50000 --page-number 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun ens DescribeNetworkPeerConnections --region cn-hangzhou --PageSize 10 --InstanceId 'pcc-5inkeimcipxk26yqtzm4q0000' --PageNumber 1 --NetworkIds.1 'n-50000' --EnsRegionId 'cn-xian-unicom' --Name test0 --method POST --force"
  }
}