{
  "name": "StopInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ForceStop",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "停止实例时是否强制关机策略。\n\n- **true**：强制关机。\n\n- **false**（默认）：正常关机流程。",
        "en": "Specifies whether to forcibly stop the instance.\n\n- **true**\n\n- **false** (default)"
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要停止运行的实例的ID，仅支持停止单个。",
        "en": "The ID of the instance that you want to stop. You can specify only one instance ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "停止实例",
    "en": "StopInstance"
  },
  "descriptions": {
    "zh": "- 只有状态为运行中（Running）的实例才可以进行此操作。\n\n- 成功调用接口后实例进入停止中（Stopping）状态。\n\n- 实例成功停止关机后进入已停止（Stopped）状态。\n\n- 支持强制停止，等同于断电处理，可能丢失实例操作系统中未写入磁盘的数据。",
    "en": "- to stop instances that are only in the Running state.\n\n- If the call is successful, the state of the instance becomes Stopping.\n\n- Once the instance is stopped, the state of the instance becomes Stopped.\n\n- Force stop is supported, which is equivalent to power-off. Data that is not written to disks on the instance may be lost."
  },
  "example": {
    "unifiedCli": "aliyun ens stop-instance --instance-id i-instanceid0000 --force-stop true --region cn-hangzhou",
    "legacyCli": "aliyun ens StopInstance --region cn-hangzhou --InstanceId 'i-instanceid0000' --ForceStop true"
  }
}