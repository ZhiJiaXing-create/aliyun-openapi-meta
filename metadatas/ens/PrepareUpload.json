{
  "name": "PrepareUpload",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BucketName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "bucket名称",
        "en": "The bucket name."
      }
    },
    {
      "name": "ClientIp",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定IP，适用于用户IP与接口调用IP不一致的情况，如服务端获取授权后下发给客户端的场景。",
        "en": "The client's IP address. Use this parameter when the client's IP address differs from the IP address used to call the API. For example, a server obtains authorization and then sends it to a client."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "准备上传",
    "en": "PrepareUpload"
  },
  "example": {
    "unifiedCli": "aliyun ens prepare-upload --bucket-name test --client-ip 180.166.XX.XX --region cn-hangzhou",
    "legacyCli": "aliyun ens PrepareUpload --region cn-hangzhou --BucketName test --ClientIp '180.166.XX.XX'"
  }
}