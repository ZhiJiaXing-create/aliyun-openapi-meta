{
  "name": "StartInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID，仅支持启动单个。",
        "en": "The ID of the instance. You can start only one instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "启动实例",
    "en": "StartInstance"
  },
  "descriptions": {
    "zh": "- 实例状态必须为已停止（Stopped），才可以调用该接口。\n- 接口调用成功后实例进入启动中 （Starting） 状态。",
    "en": "- You can call the operation only when the instance is in the Stopped state.\n\n- If the operation is successful, the status of the instance becomes Starting."
  },
  "example": {
    "unifiedCli": "aliyun ens start-instance --instance-id i-instance0000 --region cn-hangzhou",
    "legacyCli": "aliyun ens StartInstance --region cn-hangzhou --InstanceId 'i-instance0000'"
  }
}