{
  "name": "ReleasePrePaidInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定删除的实例ID，仅支持删除单个。",
        "en": "The ID of the instance to be deleted. You can specify only one instance ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除预付费实例",
    "en": "ReleasePrePaidInstance"
  },
  "example": {
    "unifiedCli": "aliyun ens release-pre-paid-instance --instance-id i-instance0000 --region cn-hangzhou",
    "legacyCli": "aliyun ens ReleasePrePaidInstance --region cn-hangzhou --InstanceId 'i-instance0000'"
  }
}