{
  "name": "DeleteBucketLifecycle",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BucketName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "The bucket name."
      }
    },
    {
      "name": "RuleId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "规则ID。不指定时，表示清空所有规则。",
        "en": "The rule ID. If this parameter is not specified, all rules are deleted."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除Object生命周期规则",
    "en": "DeleteBucketLifecycle"
  },
  "example": {
    "unifiedCli": "aliyun ens delete-bucket-lifecycle --bucket-name test --rule-id 1 --region cn-hangzhou",
    "legacyCli": "aliyun ens DeleteBucketLifecycle --region cn-hangzhou --BucketName test --RuleId 1"
  }
}