{
  "name": "DeleteLoadBalancerListener",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ListenerPort",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "负载均衡实例前端使用的端口。取值：**1**~**65535**。",
        "en": "The frontend port that is used by the Edge Load Balance (ELB) instance. Valid values: **1** to **65535**."
      }
    },
    {
      "name": "ListenerProtocol",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "负载均衡实例前端使用的协议。\n\n> 相同端口存在不同协议监听时，此字段必填。",
        "en": "The frontend protocol that is used by the ELB instance.\n\n> This parameter is required if the same port is used by listeners that use different protocols."
      }
    },
    {
      "name": "LoadBalancerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "负载均衡实例的ID。",
        "en": "The ID of the ELB instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除监听",
    "en": "DeleteLoadBalancerListener"
  },
  "descriptions": {
    "zh": "- API调用频率：100次/秒。\n- 单用户调用频率：10次/秒。",
    "en": "- up to 100 times per second per account.\n\n- up to 10 times per second per user."
  },
  "example": {
    "unifiedCli": "aliyun ens delete-load-balancer-listener --listener-port 8080 --load-balancer-id lb-5snthcyu1x10g7tywj7iu0000 --listener-protocol tcp --region cn-hangzhou",
    "legacyCli": "aliyun ens DeleteLoadBalancerListener --region cn-hangzhou --ListenerPort 8080 --LoadBalancerId 'lb-5snthcyu1x10g7tywj7iu0000' --ListenerProtocol tcp"
  }
}