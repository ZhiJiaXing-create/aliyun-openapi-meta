{
  "name": "CreateEnsRouteEntry",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自定义路由条目的描述信息。",
        "en": "The description of the custom route entry."
      }
    },
    {
      "name": "DestinationCidrBlock",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "自定义路由条目的目标网段。需满足以下要求：\n- 目标网段不能指向100.64.0.0/10或被100.64.0.0/10包含。\n- 同一张路由表内的不同路由条目的目标网段不能相同。\n- 0.0.0.0/0表示默认",
        "en": "The destination CIDR block of the custom route entry. Make sure that the following requirements are met:\n\n- The destination CIDR block cannot point or belong to 100.64.0.0/10.\n\n- The destination CIDR blocks of the custom route entries in the same route table cannot overlap.\n\n- 0.0.0.0/0 indicates the default CIDR block."
      }
    },
    {
      "name": "NextHopId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "自定义路由条目的下一跳实例的ID。",
        "en": "The ID of the next hop of the custom route entry."
      }
    },
    {
      "name": "NextHopType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自定义路由条目的下一跳的类型，取值：\n- Instance（默认值）：ENS实例。\n- HaVip：高可用虚拟 IP。\n- NetworkPeer：网络对等连接。",
        "en": "The next hop type of the custom route. Valid values:\n\n- Instance: an ENS instance.\n\n- HaVip: a high-availability virtual IP address (HAVIP).\n\n- NetworkPeer: VPC peering connection."
      }
    },
    {
      "name": "RouteEntryName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "要创建的自定义路由条目的名称。长度为1~128个字符，但不能以http://或https://开头。",
        "en": "The name of the custom route entry that you want to add. The name must be 1 to 128 characters in length. It cannot start with http\\:// or https\\://."
      }
    },
    {
      "name": "RouteTableId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要创建自定义路由条目的路由表ID。",
        "en": "The ID of the route table to which you want to add a custom route entry."
      }
    },
    {
      "name": "SourceCidrBlock",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "源地址网段在配置网关路由表的路由条目时可用，但在配置交换机路由表的路由条目时不受支持。",
        "en": "The source CIDR block is available when you configure a route entry in the gateway route table, but is not unavailable when you configure a route entry in the vSwitch route table."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建自定义路由条目",
    "en": "CreateEnsRouteEntry"
  },
  "example": {
    "unifiedCli": "aliyun ens create-ens-route-entry --route-table-id vtb-bp1cifr72dioje82lse2j --destination-cidr-block 120.XXX.XXX.0/24 --next-hop-type Instance --next-hop-id i-5vb7leks9z4mxy1ay258 --route-entry-name test --description example --source-cidr-block 172.XXX.XXX.0/24 --region cn-hangzhou",
    "legacyCli": "aliyun ens CreateEnsRouteEntry --region cn-hangzhou --NextHopType Instance --RouteTableId 'vtb-bp1cifr72dioje82lse2j' --Description example --RouteEntryName test --NextHopId 'i-5vb7leks9z4mxy1ay258' --SourceCidrBlock '172.XXX.XXX.0/24' --DestinationCidrBlock '120.XXX.XXX.0/24'"
  }
}