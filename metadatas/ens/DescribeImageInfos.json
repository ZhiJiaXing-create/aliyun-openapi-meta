{
  "name": "DescribeImageInfos",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "OsType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "系统类型，仅支持传单个类型。不传值，默认查询全部。\n取值：\n\n- Linux\n\n- windows",
        "en": "The operating system (OS). You can specify only one OS in a request. If you do not specify a value for this parameter, images for all supported OSs are queried. Valid values:\n\n- linux\n\n- windows"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询可以使用的镜像资源",
    "en": "DescribeImageInfos"
  },
  "example": {
    "unifiedCli": "aliyun ens describe-image-infos --os-type linux --region cn-hangzhou",
    "legacyCli": "aliyun ens DescribeImageInfos --region cn-hangzhou --OsType linux"
  }
}