{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID列表。一次调用最多支持添加50个资源ID。",
        "en": "The resource IDs. You can add up to 50 resource IDs in a call."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。取值：instance（实例）。",
        "en": "The type of the resource. Set the value to instance."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "实例所绑定的标签",
        "en": "The tag that is bound to the instance."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "资源的标签键。N 的取值范围：1~20。一旦传入该值，则不允许为空字符串。最多支持 128 个字符，不能以aliyun和acs:开头，不能包含http://或者https://。",
            "en": "The key of tag N that is added to the resource. Valid values of N: 1 to 20. The tag value cannot be an empty string. The tag value can be up to 128 characters in length. It cannot start with aliyun or acs: and cannot contain http\\:// or https\\://."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "资源的标签值。N 的取值范围：1~20。一旦传入该值，可以为空字符串。最多支持 128 个字符，不能以acs:开头，不能包含http://或者https://。",
            "en": "The value of tag N that is added to the resource. Valid values of N: 1 to 20. The tag value can be an empty string. The tag value can be up to 128 characters in length. It cannot start with acs: or contain http\\:// or https\\://."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为指定的ENS资源列表统一创建并绑定标签",
    "en": "TagResources"
  },
  "descriptions": {
    "zh": "绑定标签前，阿里云会校验资源已有标签数量。超过限制值后返回报错信息。当前只支持实例（虚机/祼机等）资源。",
    "en": "Before you add tags to a resource, Alibaba Cloud checks the number of existing tags on the resource. If the number exceeds the upper limit, an error message is returned. Only instance resources, such as virtual machines and bare machines, are supported."
  },
  "example": {
    "unifiedCli": "aliyun ens tag-resources --tag Value=Deep Key=team --resource-id i-50000 --resource-type instance --region cn-hangzhou",
    "legacyCli": "aliyun ens TagResources --region cn-hangzhou --ResourceId.1 'i-50000' --Tag.1.Key team --Tag.1.Value Deep --ResourceType instance"
  }
}