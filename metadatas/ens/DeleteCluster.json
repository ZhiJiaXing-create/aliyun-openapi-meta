{
  "name": "DeleteCluster",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID",
        "en": "The ID of the cluster to delete."
      }
    },
    {
      "name": "RetainResources",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否保留云资源，默认为false",
        "en": "Indicates whether to retain the associated cloud resources. The default is `false`."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除集群",
    "en": "DeleteCluster"
  },
  "descriptions": {
    "zh": "删除集群时支持RetainResources参数，默认为false，如果为true，则会保留worker节点所在的ENS计算实例。\n\n删除集群时会自动删除集群内所有节点以及节点池。对于用户通过 DescribeAttachScript接口获取脚本自行接入的ENS计算实例，保留对应计算实例。\n\n如果当前集群中存在扩容流程或者缩容流程中的节点池，请先等待扩缩容完成再执行删除集群操作。",
    "en": "When you delete a cluster, you can use the `RetainResources` parameter. The default is `false`. Setting this parameter to `true` retains the ENS compute instances that host the worker nodes.\n\nDeleting a cluster also deletes all its nodes and node pools. However, ENS compute instances attached using a script from the `DescribeAttachScript` operation are retained.\n\nIf a node pool in the cluster is scaling out or in, wait for the operation to complete before you delete the cluster."
  },
  "example": {
    "unifiedCli": "aliyun ens delete-cluster --cluster-id eck-xxxxxxxx --retain-resources true --region cn-hangzhou",
    "legacyCli": "aliyun ens DeleteCluster --region cn-hangzhou --RetainResources true --ClusterId 'eck-xxxxxxxx'"
  }
}