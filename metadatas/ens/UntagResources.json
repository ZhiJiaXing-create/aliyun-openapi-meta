{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解绑资源上全部的标签。当请求中未设置 TagKey.N 时，该参数才有效。取值范围：\n\n- **true**：解绑所有标签。\n\n- **false**（默认值）：不解绑所有标签。",
        "en": "Specifies whether to remove all tags from the resource. This parameter takes effect only if you do not specify TagKey.N. Valid values:\n\n- **true**\n\n- **false** (default)"
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID列表，最多50个子项",
        "en": "The resource IDs. You can specify up to 50 resource IDs."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型",
        "en": "The type of the resource."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签键，n 的取值范围为 1, 20。",
        "en": "The keys of the tags. Valid values of N: 1 to 20."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为指定资源列表统一解绑标签",
    "en": "UntagResources"
  },
  "example": {
    "unifiedCli": "aliyun ens untag-resources --resource-type instance --resource-id i-5zy93g7z1hnkdmav84joxyzgn --tag-key test_tag_key-2 --all true --region cn-hangzhou",
    "legacyCli": "aliyun ens UntagResources --region cn-hangzhou --All true --ResourceId.1 'i-5zy93g7z1hnkdmav84joxyzgn' --TagKey.1 'test_tag_key-2' --ResourceType instance"
  }
}