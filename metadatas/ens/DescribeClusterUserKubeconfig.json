{
  "name": "DescribeClusterUserKubeconfig",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    },
    {
      "name": "PrivateIpAddress",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否使用私网访问地址。默认false。",
        "en": "Specifies whether to use the internal access address. The default value is false."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询集群 KubeConfig",
    "en": "DescribeClusterUserKubeconfig"
  },
  "descriptions": {
    "zh": "- 单用户调用频率： 100次/秒。",
    "en": "- A single user is limited to 100 calls per second."
  },
  "example": {
    "unifiedCli": "aliyun ens describe-cluster-user-kubeconfig --cluster-id eck-xxxxxxxxx --private-ip-address false --region cn-hangzhou",
    "legacyCli": "aliyun ens DescribeClusterUserKubeconfig --region cn-hangzhou --PrivateIpAddress false --ClusterId 'eck-xxxxxxxxx'"
  }
}