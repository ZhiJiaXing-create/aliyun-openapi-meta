{
  "name": "PutBucketLifecycle",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AllowSameActionOverlap",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否允许前缀重叠。取值：\n\n- **true**：允许重复。\n\n- **false**（默认值）：不允许重复。",
        "en": "Whether to allow overlapping prefixes. Valid values:\n\n- **true**: Overlapping prefixes are allowed.\n\n- **false** (default): Overlapping prefixes are not allowed."
      }
    },
    {
      "name": "BucketName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "The name of the bucket."
      }
    },
    {
      "name": "CreatedBeforeDate",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定过期⽇期，存储会对最后更新时间早于该⽇期的数据执⾏⽣命周期规则。\n\n⽇期设置需按照ISO8601标准表示，并需要使用UTC时间，格式为yyyy-MM-ddTHH:mm:ssZ。\n\n>ExpirationDays和CreateBeforeDate互斥，必须设置⼀个。",
        "en": "The date before which objects expire. The system applies the lifecycle rule to objects last modified before this date.\n\nUse ISO 8601 format and UTC time. Format: yyyy-MM-ddTHH:mm:ssZ.\n\n> ExpirationDays and CreatedBeforeDate are mutually exclusive. Specify one."
      }
    },
    {
      "name": "ExpirationDays",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "指定⽣命周期规则在距离Object最后更新多少天后⽣效。取值需大于0，为正整数。\n>ExpirationDays和CreateBeforeDate互斥，必须设置⼀个。",
        "en": "The number of days after an object's last modification when the lifecycle rule takes effect. Must be a positive integer greater than zero.\n\n> ExpirationDays and CreatedBeforeDate are mutually exclusive. Specify one."
      }
    },
    {
      "name": "Prefix",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "指定规则所适⽤的前缀（Prefix），不⽀持设置相同的Prefix。\n\n- 如果指定了Prefix，则表示此规则仅适⽤于Bucket中与Prefix匹配的Object。\n\n- 如果Prefix置空，则表示此规则适⽤于Bucket中所有Object。",
        "en": "The prefix that the rule applies to. You cannot set duplicate prefixes.\n\n- If you specify a prefix, the rule applies only to objects in the bucket whose names start with that prefix.\n\n- If you leave Prefix empty, the rule applies to all objects in the bucket."
      }
    },
    {
      "name": "RuleId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "标识规则的唯⼀ID。最多由255个字节组成。\n\n- 创建新规则时无需配置该参数，系统会⾃动⽣成⼀个唯⼀ID。\n\n- 更新已有规则需指定RuleId，且该RuleId对应的规则必须存在，否则会报错。",
        "en": "A unique ID that identifies the rule. Maximum length is 255 bytes.\n\n- Omit this parameter when creating a new rule. The system generates a unique ID automatically.\n\n- Specify RuleId when updating an existing rule. The rule must exist. Otherwise, the call fails."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "规则状态。取值范围：\n\n- **Enabled**：定期执⾏该规则。\n\n- **Disabled**：忽略该规则。",
        "en": "The status of the rule. Valid values:\n\n- **Enabled**: The system runs the rule on schedule.\n\n- **Disabled**: The system ignores the rule."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置Object生命周期规则",
    "en": "PutBucketLifecycle"
  },
  "descriptions": {
    "zh": "- 最多⽀持1000条规则。\n\n- 如果同一文件同时满足多条规则，则以最先过期时间规则为准。",
    "en": "- You can configure up to 1,000 rules.\n\n- If an object matches multiple rules, the rule with the earliest expiration time applies."
  },
  "example": {
    "unifiedCli": "aliyun ens put-bucket-lifecycle --bucket-name test --rule-id b8f93xxxxx4881xxxxxc71d991 --status Enabled --prefix image --created-before-date 2023-10-12T05:45:00Z --expiration-days 5 --allow-same-action-overlap false --region cn-hangzhou",
    "legacyCli": "aliyun ens PutBucketLifecycle --region cn-hangzhou --Status Enabled --BucketName test --CreatedBeforeDate '2023-10-12T05:45:00Z' --RuleId b8f93xxxxx4881xxxxxc71d991 --Prefix image --AllowSameActionOverlap false --ExpirationDays 5"
  }
}