{
  "name": "DescribeSDG",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询的页码。",
        "en": "The page number to return."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数。\n\n默认值：10。",
        "en": "The page size.\n\nDefault value: 10."
      }
    },
    {
      "name": "SDGIds",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "指定查询若干SDG，默认查询全部。",
        "en": "The IDs of the SDGs. If omitted, all SDGs are queried."
      }
    },
    {
      "name": "SameDiskId",
      "position": "Query",
      "type": "Boolean",
      "required": false
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询SDG信息",
    "en": "Query SDG Information"
  },
  "example": {
    "unifiedCli": "aliyun ens describe-sdg --sdg-ids sdg-1 --page-number 1 --page-size 10 --same-disk-id true --region cn-hangzhou",
    "legacyCli": "aliyun ens DescribeSDG --region cn-hangzhou --SDGIds '[\"sdg-1\"]' --PageSize 10 --SameDiskId true --PageNumber 1"
  }
}