{
  "name": "DescribeMountTargets",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "EnsRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The ID of the region."
      }
    },
    {
      "name": "FileSystemId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件系统ID。",
        "en": "The ID of the file system."
      }
    },
    {
      "name": "MountTargetName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "挂载点名称。",
        "en": "The name of the mount target."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，起始值为1。默认值：1。",
        "en": "The number of the page to return. Pages start from page 1. Default value: 1"
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页行数，最大值为100行。默认值：10。",
        "en": "The number of entries returned per page. Maximum value: 100. Default value: 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询挂载点信息",
    "en": "DescribeMountTargets"
  },
  "example": {
    "unifiedCli": "aliyun ens describe-mount-targets --ens-region-id cn-beijing-cmcc --file-system-id c50f800000 --mount-target-name TestMountPath --page-number 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun ens DescribeMountTargets --region cn-hangzhou --MountTargetName TestMountPath --PageSize 10 --PageNumber 1 --FileSystemId c50f800000 --EnsRegionId 'cn-beijing-cmcc'"
  }
}