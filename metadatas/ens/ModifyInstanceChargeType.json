{
  "name": "ModifyInstanceChargeType",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPay",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "实例级后付费转为包年包月的时候是否自动支付。取值：\n\ntrue：自动支付，您需要确保账户余额充足。\n\nfalse：只生成订单不扣费（默认值）。",
        "en": "Specifies whether to enable automatic payment. This parameter is valid only when you change the billing method of an instance from pay-as-you-go to subscription. Valid values:\n\n`true`: Automatic payment is enabled. Make sure that your account balance is sufficient.\n\n`false` (default): An order is generated but no payment is made."
      }
    },
    {
      "name": "AutoRenew",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "实例级后付费转为包年包月的时候是否开启自动续费。取值：\n\ntrue：自动续费。\n\nfalse：不自动续费（默认值）。",
        "en": "Specifies whether to enable auto-renewal for the instance. This parameter is valid only when you change the billing method of an instance from pay-as-you-go to subscription. Valid values:\n\n`true`: Auto-renewal is enabled.\n\n`false` (default): Auto-renewal is disabled."
      }
    },
    {
      "name": "BillingCycle",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "IncludeDataDisks",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "实例级后付费转为包年包月的时候是否将随实例创建的所有按量付费数据盘一起转换为包年包月数据盘。取值：\n\ntrue：是。\n\nfalse：否（默认值）。",
        "en": "Specifies whether to change the billing method of all pay-as-you-go data disks that are created with the instance to subscription. This parameter is valid only when you change the billing method of an instance from pay-as-you-go to subscription. Valid values:\n\n`true`: The billing method of the data disks is changed to subscription.\n\n`false` (default): The billing method of the data disks is not changed."
      }
    },
    {
      "name": "InstanceChargeType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例需要修改的目标计费方式。取值：\n\nPrePaid：将实例级后付费实例转换为包年包月实例。\n\nPostPaid：将包年包月实例转换为实例级后付费实例（默认值）。",
        "en": "The target billing method of the instance. Valid values:\n\n`PrePaid`: Changes the billing method of a pay-as-you-go instance to subscription.\n\n`PostPaid` (default): Changes the billing method of a subscription instance to pay-as-you-go."
      }
    },
    {
      "name": "InstanceIds",
      "position": "Query",
      "type": "Json",
      "required": true,
      "description": {
        "zh": "实例ID列表。",
        "en": "A list of instance IDs."
      }
    },
    {
      "name": "Period",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例级后付费转为包年包月的时候的包年包月时长，如果是此场景下该字段必填。取值：\n\n如果 PeriodUnit 取值为 Day 时，则 Period 仅可设置为 3。\n\n如果 PeriodUnit 取值为 Month 时，则 Period 可设置为1-9,12。",
        "en": "The subscription duration. This parameter is required when you change the billing method from pay-as-you-go to subscription. The valid values of this parameter depend on the `PeriodUnit` parameter.\n\nIf `PeriodUnit` is set to `Day`, the valid value of `Period` is `3`.\n\nIf `PeriodUnit` is set to `Month`, the valid values of `Period` are `1` to `9`, and `12`."
      }
    },
    {
      "name": "PeriodUnit",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例级后付费转为包年包月的时候的购买资源的时长单位，如果是此场景下该字段必填。取值：\n\nMonth：按月购买。\n\nDay：按天购买。",
        "en": "The unit of the subscription duration. This parameter is required when you change the billing method of an instance from pay-as-you-go to subscription. Valid values:\n\n`Month`\n\n`Day`"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "实例计费类型转换",
    "en": "ModifyInstanceChargeType"
  },
  "descriptions": {
    "zh": "请确保在使用该接口前，已充分了解ENS产品的收费方式和价格。\n\n目标实例的状态必须为运行中或者已停止，并且无欠费的情况下才能修改计费方式。",
    "en": "Before calling this API, ensure you understand the Billing Methods and pricing of Edge Node Service (ENS).\n\nTo modify the Billing Method, the target Instance must be Running or Stopped, and your account must not be overdue."
  },
  "example": {
    "unifiedCli": "aliyun ens modify-instance-charge-type --instance-ids i-5xlrc9wdeo3xxxxxx --instance-charge-type PostPaid --auto-renew false --period 1 --period-unit Month --include-data-disks true --auto-pay false --billing-cycle example-string --region cn-hangzhou",
    "legacyCli": "aliyun ens ModifyInstanceChargeType --region cn-hangzhou --BillingCycle 'example-string' --InstanceChargeType PostPaid --AutoRenew false --IncludeDataDisks true --Period 1 --AutoPay false --InstanceIds '[\"i-5xlrc9wdeo3xxxxxx\"]' --PeriodUnit Month"
  }
}