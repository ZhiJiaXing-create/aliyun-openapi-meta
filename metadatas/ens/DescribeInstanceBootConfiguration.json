{
  "name": "DescribeInstanceBootConfiguration",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BootSet",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "启动方式。",
        "en": "The startup method."
      }
    },
    {
      "name": "BootType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "启动类型。",
        "en": "The startup type."
      }
    },
    {
      "name": "DiskSet",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "启动依赖磁盘与否。",
        "en": "Specifies whether the startup depends on the disk."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The ID of the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询实例启动配置",
    "en": "DescribeInstanceBootConfiguration"
  },
  "descriptions": {
    "zh": "查询启动配置，只支持异构实例（PCFarm裸金属）。",
    "en": "Queries the boot configuration of a heterogeneous PC Farm bare metal instance."
  },
  "example": {
    "unifiedCli": "aliyun ens describe-instance-boot-configuration --instance-id i-0000 --boot-type pxe --boot-set legacy --disk-set on --region cn-hangzhou",
    "legacyCli": "aliyun ens DescribeInstanceBootConfiguration --region cn-hangzhou --InstanceId 'i-0000' --BootSet legacy --BootType pxe --DiskSet on"
  }
}