{
  "name": "GetBucketLifecycle",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "BucketName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "Bucket名称。",
        "en": "Bucket name."
      }
    },
    {
      "name": "RuleId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需要获取的规则ID。不设置时，获取所有规则信息。",
        "en": "The ID of the rule to retrieve. If not set, retrieve all rule information."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查看Object生命周期规则",
    "en": "GetBucketLifecycle"
  },
  "example": {
    "unifiedCli": "aliyun ens get-bucket-lifecycle --bucket-name tese --rule-id 1 --region cn-hangzhou",
    "legacyCli": "aliyun ens GetBucketLifecycle --region cn-hangzhou --BucketName tese --RuleId 1"
  }
}