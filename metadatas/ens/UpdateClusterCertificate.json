{
  "name": "UpdateClusterCertificate",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "集群id",
        "en": "The cluster ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新集群证书",
    "en": "UpdateClusterCertificate"
  },
  "descriptions": {
    "zh": "- 单用户调用频率： 10次/秒。\n- 更新集群证书是一个异步的操作，调用接口返回之后，需要等待5~10分钟不等，等待集群证书更新以及管控组件重启。这个过程中可以调用DescribeClustersV1接口查询集群证书更新状态。集群证书更新成功之后，可以调用DescribeClusterUserKubeconfig获取集群证书。",
    "en": "- The request rate for a single user is limited to 10 requests per second.\n\n- Updating the cluster certificate is an asynchronous operation. After you call the API, the system updates the cluster certificate and restarts the management components. This process typically takes 5 to 10 minutes. During this time, you can call the DescribeClustersV1 operation to check the update status. After the update completes, you can call the DescribeClusterUserKubeconfig operation to get the new cluster certificate."
  },
  "example": {
    "unifiedCli": "aliyun ens update-cluster-certificate --cluster-id eck-xxxxxxxx --region cn-hangzhou",
    "legacyCli": "aliyun ens UpdateClusterCertificate --region cn-hangzhou --ClusterId 'eck-xxxxxxxx'"
  }
}