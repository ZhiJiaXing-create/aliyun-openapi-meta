{
  "name": "AttachDisk",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DeleteWithInstance",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "待挂载的云盘是否随实例释放。取值：\n\n- true：释放实例时，该云盘随实例一起释放。\n- false ：释放实例时，保留该云盘，不随实例一起释放。\n- 为空则表示默认为false。",
        "en": "Specifies whether the disk to be attached is released with the instance. Valid values:\n\n- true: The disk will be released when the ECS instance is released.\n\n- false: The disk will be retained when the ECS instance is released.\n\n- If you leave this parameter empty, the default value is used."
      }
    },
    {
      "name": "DiskId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待挂载的云盘ID。云盘（DiskId）和实例（InstanceId）必须在同一个节点。",
        "en": "The ID of the disk to be attached. The cloud disk and the instance must belong to the same node."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "The ID of the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "挂载磁盘",
    "en": "AttachDisk"
  },
  "example": {
    "unifiedCli": "aliyun ens attach-disk --disk-id d-5saf13yy6sopmmg88mzsg0000 --instance-id i-5rr19av7tkpgi9os52ag10000 --delete-with-instance false --region cn-hangzhou",
    "legacyCli": "aliyun ens AttachDisk --region cn-hangzhou --InstanceId 'i-5rr19av7tkpgi9os52ag10000' --DeleteWithInstance false --DiskId 'd-5saf13yy6sopmmg88mzsg0000'"
  }
}