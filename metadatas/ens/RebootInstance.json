{
  "name": "RebootInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ForceStop",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "重启ENS实例前是否强制关机策略。\n\n- **true**：重启ENS实例前强制关机。\n- **false**（默认）：重启ENS实例前正常关机。",
        "en": "Specifies whether to forcefully stop the instance before you restart it.\n\n- **true**\n\n- **false** (default)"
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要重启实例的ID，仅支持重启单个。",
        "en": "The ID of the instance that you want to reboot. You can specify only one instance ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "重启实例",
    "en": "RebootInstance"
  },
  "descriptions": {
    "zh": "- 您只能重启状态为运行中（Running）的ENS实例。\n\n- 接口调用成功后实例进入启动中 （Starting） 状态。",
    "en": "- Only instances that are in the Running state can be restarted.\n\n- If the operation is successful, the status of the instance becomes Starting."
  },
  "example": {
    "unifiedCli": "aliyun ens reboot-instance --instance-id i-instanceid0000 --force-stop true --region cn-hangzhou",
    "legacyCli": "aliyun ens RebootInstance --region cn-hangzhou --InstanceId 'i-instanceid0000' --ForceStop true"
  }
}