{
  "name": "CreateStorageVolume",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AuthPassword",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "CHAP协议密码。",
        "en": "The password of the CHAP protocol."
      }
    },
    {
      "name": "AuthProtocol",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "鉴权协议。取值：**CHAP**。当前仅支持CHAP协议。",
        "en": "The authentication protocol. Set the value to **CHAP**."
      }
    },
    {
      "name": "AuthUser",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "CHAP协议用户名。",
        "en": "The username of the CHAP protocol."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "卷的描述。长度为2~256个英文或中文字符。不能以`http://`或`https://`开头。",
        "en": "The description of the volume. The description must be 2 to 128 characters in length. The description cannot start with `http://` or `https://`."
      }
    },
    {
      "name": "EnsRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "节点ID。",
        "en": "The ID of the node."
      }
    },
    {
      "name": "GatewayId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "网关ID。",
        "en": "The ID of the gateway."
      }
    },
    {
      "name": "IsAuth",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否启用鉴权。取值：\n- **1**：启用。\n- **0**：不启用。（默认）",
        "en": "Specifies whether to enable authentication. Valid values:\n\n- **1**: Authentication is enabled.\n\n- **0** (default): Authentication is disabled."
      }
    },
    {
      "name": "IsEnable",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否启用。取值：\n\n- **1**：启用。（默认）\n- **0**：不启用。",
        "en": "Indicates whether the volume is enabled. Valid values:\n\n- **1** (default): The volume is enabled.\n\n- **0**: The volume is disabled."
      }
    },
    {
      "name": "StorageId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "存储介质ID。",
        "en": "The ID of the storage medium."
      }
    },
    {
      "name": "VolumeName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "卷的显示名称。长度为2~128个英文或中文字符。不能以`http://`或`https://`开头。可以包含数字、半角冒号（:）、下划线（_）或者短划线（-）。",
        "en": "The name of the volume. The name must be 2 to 128 characters in length. The name cannot start with `http://` or `https://`. The name can contain letters, digits, colons (:), underscores (\\_), and hyphens (-)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "CreateStorageVolume",
    "en": "CreateStorageVolume"
  },
  "example": {
    "unifiedCli": "aliyun ens create-storage-volume --gateway-id sgw-0000 --storage-id si-0000 --ens-region-id cn-shenzhen-3 --is-enable 1 --is-auth 0 --auth-protocol CHAP --auth-user User --auth-password Password --volume-name testVolumeName --description testDescription --region cn-hangzhou",
    "legacyCli": "aliyun ens CreateStorageVolume --region cn-hangzhou --IsEnable 1 --VolumeName testVolumeName --Description testDescription --IsAuth 0 --AuthProtocol CHAP --AuthUser User --AuthPassword Password --GatewayId 'sgw-0000' --EnsRegionId 'cn-shenzhen-3' --StorageId 'si-0000'"
  }
}