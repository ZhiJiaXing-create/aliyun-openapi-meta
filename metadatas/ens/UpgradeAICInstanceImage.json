{
  "name": "UpgradeAICInstanceImage",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ImageId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "AIC镜像ID。",
        "en": "The ID of the AIC image."
      }
    },
    {
      "name": "ServerIds",
      "position": "Query",
      "type": "Json",
      "required": true,
      "description": {
        "zh": "服务器实例ID列表。",
        "en": "The IDs of the servers."
      }
    },
    {
      "name": "Timeout",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "升级超时时间，单位：秒。",
        "en": "The timeout period of the update. Unit: seconds."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "升级AIC实例镜像",
    "en": "UpgradeAICInstanceImage"
  },
  "example": {
    "unifiedCli": "aliyun ens upgrade-aic-instance-image --server-ids cas-instance0000 --image-id m-0000 --timeout 1800 --region cn-hangzhou",
    "legacyCli": "aliyun ens UpgradeAICInstanceImage --region cn-hangzhou --Timeout 1800 --ServerIds '[\"cas-instance0000\"]' --ImageId 'm-0000'"
  }
}