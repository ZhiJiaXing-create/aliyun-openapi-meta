{
  "name": "DescribeKeyPairs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "KeyPairId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "密钥对的ID。",
        "en": "The ID of the key pair."
      }
    },
    {
      "name": "KeyPairName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "密钥对名称。长度为2~128个英文或中文字符。必须以大小字母或中文开头，不能以`http://`和`https://`开头。可以包含以下字符：\n- 数字\n- :\n- _\n- .\n\n仅支持传单个名称。默认查询所有密钥对信息。",
        "en": "The name of the key pair that you want to bind to the simple application server. The name must be 2 to 128 characters in length. The name must start with a letter but cannot start with `http://` or `https://`. The name can contain the following characters:\n\n- Numbers.\n\n- :\n\n- \\_\n\n- .\n\nYou can specify only one name. By default, all key pairs are queried."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询列表的页码。取值：>0，默认查询第1页。",
        "en": "The page number of the returned page. Valid values: integers that are greater than 0. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数。取值：>0，默认为10。",
        "en": "The number of entries per page. Valid values: integers that are greater than 0. Default value: 10."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询一个或多个密钥对",
    "en": "DescribeKeyPairs"
  },
  "example": {
    "unifiedCli": "aliyun ens describe-key-pairs --biz-key-pair-name TestKeyPairName --page-number 1 --page-size 10 --key-pair-id ssh-50cynkq42sgj4ej1tn78t4000 --region cn-hangzhou",
    "legacyCli": "aliyun ens DescribeKeyPairs --region cn-hangzhou --KeyPairName TestKeyPairName --PageSize 10 --PageNumber 1 --KeyPairId 'ssh-50cynkq42sgj4ej1tn78t4000'"
  }
}