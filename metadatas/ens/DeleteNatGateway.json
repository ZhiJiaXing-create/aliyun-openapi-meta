{
  "name": "DeleteNatGateway",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ForceDelete",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否强制删除NAT实例。",
        "en": "Specifies whether to forcefully delete the NAT instance."
      }
    },
    {
      "name": "NatGatewayId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要删除的NAT网关的实例ID。",
        "en": "The ID of the NAT gateway that you want to delete."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除NAT网关",
    "en": "DeleteNatGateway"
  },
  "example": {
    "unifiedCli": "aliyun ens delete-nat-gateway --nat-gateway-id nat-5t7nh1cfm6kxiszlttr380000 --force-delete true --region cn-hangzhou",
    "legacyCli": "aliyun ens DeleteNatGateway --region cn-hangzhou --ForceDelete true --NatGatewayId 'nat-5t7nh1cfm6kxiszlttr380000'"
  }
}