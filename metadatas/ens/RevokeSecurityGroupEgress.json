{
  "name": "RevokeSecurityGroupEgress",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DestCidrIp",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目的端IP地址范围。支持CIDR格式和IPv4格式的IP地址范围。\n\n默认值：无。",
        "en": "The destination IP addresses. CIDR blocks and IPv4 addresses are supported.\n\nBy default, this parameter is empty."
      }
    },
    {
      "name": "IpProtocol",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "传输层协议。取值大小写敏感。取值范围：\n- tcp\n- udp\n- icmp\n- gre\n- all：支持所有协议",
        "en": "The transport layer protocol. The value of this parameter is case-sensitive. Valid values:\n\n- tcp\n\n- udp\n\n- icmp\n\n- gre\n\n- all: All protocols are supported."
      }
    },
    {
      "name": "Policy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置访问权限。取值：\n- **accept**：接受访问。\n- **drop**：拒绝访问，不返回拒绝信息。\n\n默认值：**accept**。",
        "en": "The action of the security group rule. Valid values:\n\n- **accept**: allows access.\n\n- **drop**: denies access and returns no responses.\n\nDefault value: **accept**."
      }
    },
    {
      "name": "PortRange",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目的端安全组开放的传输层协议相关的端口范围。取值范围：\n- TCP/UDP协议：取值范围为**1**~**65535**。使用斜线（/）隔开起始端口和终止端口。正确示范：**1/200**；错误示范：**200/1**。\n- ICMP协议：**-1/-1**。\n- GRE协议：**-1/-1**。\n- IpProtocol取值为all：**-1/-1**。",
        "en": "The range of destination ports that correspond to the transport layer protocol for the security group rule. Valid values:\n\n- If you set the IpProtocol parameter to tcp or udp, the port number ranges from **1** to **65535**. The start port number and the end port number are separated by a forward slash (/). Correct example: **1/200**. Incorrect example: **200/1**.\n\n- When the IpProtocol parameter is set to icmp, the port number range is **-1/-1**, which indicates all ports.\n\n- When the IpProtocol parameter is set to gre, the port number range is **-1/-1**, which indicates all ports.\n\n- When the IpProtocol parameter is set to all, the port number range is **-1/-1**, which indicates all ports."
      }
    },
    {
      "name": "Priority",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "安全组规则优先级。取值范围：**1**~**100**，数值越小，代表优先级越高。\n\n默认值：**1**。",
        "en": "The priority of the security group rule. Valid values: **1** to **100**. A smaller value indicates a higher priority.\n\nDefault value: **1**."
      }
    },
    {
      "name": "SecurityGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "安全组ID。",
        "en": "The ID of the security group."
      }
    },
    {
      "name": "SourcePortRange",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "源端安全组开放的传输层协议相关的端口范围。取值范围：\n- TCP/UDP协议：取值范围为**1**~**65535**。使用斜线（/）隔开起始端口和终止端口。正确示范：**1/200**；错误示范：**200/1**。\n- ICMP协议：**-1/-1**。\n- GRE协议：**-1/-1**。\n- IpProtocol取值为all：**-1/-1**。",
        "en": "The range of port numbers that correspond to the transport layer protocol for the source security group. Valid values:\n\n- If you set the IpProtocol parameter to tcp or udp, the port number ranges from **1** to **65535**. The start port number and the end port number are separated by a forward slash (/). Correct example: **1/200**. Incorrect example: **200/1**.\n\n- When the IpProtocol parameter is set to icmp, the port number range is **-1/-1**, which indicates all ports.\n\n- When the IpProtocol parameter is set to gre, the port number range is **-1/-1**, which indicates all ports.\n\n- When the IpProtocol parameter is set to all, the port number range is **-1/-1**, which indicates all ports."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除安全组出方向规则",
    "en": "RevokeSecurityGroupEgress"
  },
  "descriptions": {
    "zh": "> 安全组的API文档中，流量的发起端为源端（Source），数据传输的接收端为目的端（Dest）。",
    "en": "> In the security group-related API documents, outbound traffic refers to the traffic sent by the source and received by the destination."
  },
  "example": {
    "unifiedCli": "aliyun ens revoke-security-group-egress --ip-protocol all --port-range 22/22 --security-group-id sg-bp67acfmxazb4ph000 --policy accept --priority 1 --dest-cidr-ip 10.0.0.0/8 --source-port-range 22/22 --region cn-hangzhou",
    "legacyCli": "aliyun ens RevokeSecurityGroupEgress --region cn-hangzhou --Policy accept --PortRange '22/22' --SourcePortRange '22/22' --Priority 1 --SecurityGroupId 'sg-bp67acfmxazb4ph000' --IpProtocol all --DestCidrIp '10.0.0.0/8'"
  }
}