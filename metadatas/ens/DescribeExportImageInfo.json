{
  "name": "DescribeExportImageInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ImageId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "镜像ID。",
        "en": "The ID of the image."
      }
    },
    {
      "name": "ImageName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "镜像的名称。",
        "en": "The name of the image."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "实例状态列表的页码，起始值为**1**，默认值为**1**。",
        "en": "The page number. Pages start from page **1**. Default value: **1**."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时设置的每页行数，最大值**50**行，默认值为**10**。",
        "en": "The number of entries per page. Maximum value: **50**. Default value: **10**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询导出镜像信息",
    "en": "DescribeExportImageInfo"
  },
  "descriptions": {
    "zh": "- 调用DescribeExportImageInfo会返回用户账号下所有的自定义镜像列表，除镜像本身属性外，增加导出状态以及OSS下载链接的属性。\n-  未导出过的镜像下载链接为空。\n-  如果客户自行删除过OSS中的内容，可能会导致下载链接无效。",
    "en": "- to query information about all custom images in your account. The information include the image properties, image export status, and the Object Storage Service (OSS) download links.\n\n- Empty strings are returned for images that are not exported.\n\n- The download links may become invalid if you delete objects in OSS."
  },
  "example": {
    "unifiedCli": "aliyun ens describe-export-image-info --image-id m-xxxxx --image-name testImageName --page-number 1 --page-size 10 --region cn-hangzhou",
    "legacyCli": "aliyun ens DescribeExportImageInfo --region cn-hangzhou --ImageName testImageName --PageSize 10 --PageNumber 1 --ImageId 'm-xxxxx'"
  }
}