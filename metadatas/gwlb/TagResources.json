{
  "name": "TagResources",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId不一样。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The token can contain only ASCII characters.\n\n> If you do not specify this parameter, the system automatically uses the RequestId of the request as the ClientToken. The RequestId may be different for each request."
      }
    },
    {
      "name": "DryRun",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n- **true**：发送检查请求，不会添加标签。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: performs a dry run. The system checks the required parameters, the request format, and the service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n\n- **false** (default): performs a normal request. If the request passes the check, the system returns a 2xx HTTP status code and performs the operation."
      }
    },
    {
      "name": "ResourceId",
      "position": "Body",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "资源 ID列表。一次调用最多支持添加 50 个资源 ID。",
        "en": "The list of resource IDs. You can specify up to 50 resource IDs in each call."
      }
    },
    {
      "name": "ResourceType",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型，取值：\n\n- **loadbalancer**：网关型负载均衡实例。\n- **listener**：监听。\n- **servergroup**：服务器组。",
        "en": "The resource type. Valid values:\n\n- **loadbalancer**: a Gateway Load Balancer (GWLB) instance.\n\n- **listener**: a listener.\n\n- **servergroup**: a server group."
      }
    },
    {
      "name": "Tag",
      "position": "Body",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "标签列表。一次调用最多支持 20 个标签。",
        "en": "The tags. You can specify up to 20 tags in each call."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "添加资源标签",
    "en": "TagResources"
  },
  "example": {
    "unifiedCli": "aliyun gwlb tag-resources --resource-type loadbalancer --resource-id gwlb-nrnrxwd15en27r0000 --tag Value=testValue Key=testKey --dry-run false --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun gwlb TagResources --region cn-hangzhou --ResourceId.1 'gwlb-nrnrxwd15en27r0000' --Tag.1.Key testKey --Tag.1.Value testValue --DryRun false --ResourceType loadbalancer --ClientToken '123e4567-e89b-12d3-a456-426655440000' --method POST --force"
  }
}