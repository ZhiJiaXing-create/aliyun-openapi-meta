{
  "name": "DescribeHdMonitorRegionConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源所在的地域ID。",
        "en": "The ID of the region where the resources are deployed."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询秒级监控配置",
    "en": "DescribeHdMonitorRegionConfig"
  },
  "descriptions": {
    "zh": "查询当前地域配置的秒级监控存储数仓。"
  },
  "example": {
    "unifiedCli": "aliyun nlb describe-hd-monitor-region-config --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun nlb DescribeHdMonitorRegionConfig --region cn-hangzhou --RegionId 'cn-hangzhou'"
  }
}