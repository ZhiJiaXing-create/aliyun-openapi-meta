{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解绑资源的所有标签。取值：\n\n- **true**：解绑资源的所有标签。\n- **false**（默认值）：不解绑资源的所有标签。",
        "en": "Specifies whether to remove all tags from the specified resource. Valid values:\n\n*   **true**: removes all tags from the specified resource.\n*   **false** (default): does not remove all tags from the specified resource."
      }
    },
    {
      "name": "ClientToken",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token. Ensure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system automatically uses the **request ID** as the **client token**. The **request ID** is different for each request."
      }
    },
    {
      "name": "DryRun",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n- **true**：发送检查请求，不会为资源解绑标签。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作。",
        "en": "Specifies whether to perform a dry run, without sending the actual request. Valid values:\n\n*   **true**: performs a dry run. The system checks the request for potential issues, including missing parameter values, incorrect request syntax, and service limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n*   **false** (default): performs a dry run and sends the actual request. If the request passes the dry run, a 2xx HTTP status code is returned and the operation is performed."
      }
    },
    {
      "name": "RegionId",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源所在的地域ID。 \n\n您可以通过调用[DescribeRegions](~~443657~~)接口获取地域ID。",
        "en": "The region ID of the resource.\n\nYou can call the [DescribeRegions](~~443657~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceId",
      "position": "Body",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "资源ID。一次调用最多支持添加50个资源ID。",
        "en": "The ID of the resource. You can specify at most 50 resource IDs in each call."
      }
    },
    {
      "name": "ResourceType",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。取值：\n\n- **loadbalancer**：网络型负载均衡实例。\n- **securitypolicy**：安全策略。\n- **servergroup**：服务器组。",
        "en": "The type of the resource. Valid values:\n\n*   **loadbalancer**: a Network Load Balancer (NLB) instance\n*   **securitypolicy**: a security policy\n*   **servergroup**: a server group"
      }
    },
    {
      "name": "TagKey",
      "position": "Body",
      "type": "Array",
      "required": false,
      "description": {
        "zh": "要删除标签的标签键。一次调用最多支持删除20个标签。",
        "en": "The keys of the tags that you want to remove. You can remove at most 20 tags in each call."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除资源的标签",
    "en": "UntagResources"
  },
  "example": {
    "unifiedCli": "aliyun nlb untag-resources --resource-id nlb-nrnrxwd15en27r0000 --tag-key env --all false --resource-type loadbalancer --dry-run true --client-token 123e4567-e89b-12d3-a456-426655440000 --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun nlb UntagResources --region cn-hangzhou --All false --ResourceId.1 'nlb-nrnrxwd15en27r0000' --DryRun true --TagKey.1 env --ResourceType loadbalancer --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --method POST --force"
  }
}