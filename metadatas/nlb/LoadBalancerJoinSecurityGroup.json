{
  "name": "LoadBalancerJoinSecurityGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**不一样。",
        "en": "The client token that is used to ensure the idempotence of the request.\n\nYou can use the client to generate the token. Ensure that the token is unique among different requests. The client token can contain only ASCII characters.\n\n>  If you do not specify this parameter, the system uses the **request ID** as the **client token**. The **request ID** is different for each request."
      }
    },
    {
      "name": "DryRun",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n- **true**：发送检查请求，不会为实例绑定安全组。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作。",
        "en": "Specifies whether to perform a dry run, without sending the actual request. Valid values:\n\n*   **true**: prechecks the request without performing the operation. The system checks the required parameters, request syntax, and limits. If the request fails the dry run, an error message is returned. If the request passes the dry run, the `DryRunOperation` error code is returned.\n*   **false** (default): performs a dry run and sends the actual request. If the request passes the dry run, a 2xx HTTP status code is returned and the operation is performed."
      }
    },
    {
      "name": "LoadBalancerId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待绑定安全组的网络型负载均衡实例ID。",
        "en": "The ID of the NLB instance which you want to add to a security group."
      }
    },
    {
      "name": "RegionId",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "网络型负载均衡实例所在的地域ID。\n\n您可以通过调用[DescribeRegions](~~443657~~)接口获取地域ID。",
        "en": "The region ID of the NLB instance.\n\nYou can call the [DescribeRegions](~~443657~~) operation to query the most recent region list."
      }
    },
    {
      "name": "SecurityGroupIds",
      "position": "Body",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "安全组ID。",
        "en": "The security group ID of the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "绑定安全组",
    "en": "LoadBalancerJoinSecurityGroup"
  },
  "descriptions": {
    "zh": "- 请确保您已创建安全组。创建安全组，请参见[CreateSecurityGroup](~~25553~~)。\n- 一个网络型负载均衡实例最多支持加入4个安全组。\n- 您可以通过[GetLoadBalancerAttribute](~~214362~~)接口查询实例绑定的安全组。\n- LoadBalancerJoinSecurityGroup属于异步接口，即系统返回一个请求ID，但该网络型负载均衡实例绑定安全组尚未成功，系统后台的绑定任务仍在进行。您可以调用[GetJobStatus](~~445904~~)查询安全组的绑定状态：\n    - 当任务状态处于**Succeeded**时，表示安全组绑定成功。\n    - 当任务状态处于**Processing**时，表示安全组正在绑定中，在该状态下，您只能执行查询操作，不能执行其他操作。",
    "en": "*   Make sure that you have created a security group. For more information about how to create a security group, see [CreateSecurityGroup](~~25553~~).\n\n*   An NLB instance can be associated with up to four security groups.\n\n*   You can query the security groups that are associated with an NLB instance by calling the [GetLoadBalancerAttribute](~~214362~~) operation.\n\n*   LoadBalancerJoinSecurityGroup is an asynchronous operation. After a request is sent, the system returns a request ID and runs the task in the background. You can call the [GetJobStatus](~~445904~~) operation to query the status of a task.\n\n    *   If the task is in the **Succeeded** state, the security group is associated.\n    *   If the task is in the **Processing** state, the security group is being associated. In this case, you can perform only query operations."
  },
  "example": {
    "unifiedCli": "aliyun nlb load-balancer-join-security-group --security-group-ids sg-bp1fg655nh68xyz90000 --load-balancer-id nlb-83ckzc8d4xlp8o0000 --dry-run true --client-token 123e4567-e89b-12d3-a456-426655440000 --biz-region-id cn-hangzhou --region cn-hangzhou",
    "legacyCli": "aliyun nlb LoadBalancerJoinSecurityGroup --region cn-hangzhou --DryRun true --LoadBalancerId 'nlb-83ckzc8d4xlp8o0000' --RegionId 'cn-hangzhou' --ClientToken '123e4567-e89b-12d3-a456-426655440000' --SecurityGroupIds.1 'sg-bp1fg655nh68xyz90000'"
  }
}