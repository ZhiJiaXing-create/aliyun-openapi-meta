{
  "name": "DescribeRegions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AcceptLanguage",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "支持的语言。取值：\n\n- **zh-CN**：中文\n- **en-US**（默认）：英文\n- **ja**：日文",
        "en": "The supported natural language. Valid values:\n\n*   **zh-CN**: Chinese\n*   **en-US** (default): English\n*   **ja**: Japanese"
      }
    },
    {
      "name": "ClientToken",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token used to ensure the idempotence of the request.\n\nYou can use the client to generate this value. Ensure that the value is unique among all requests. Only ASCII characters are allowed.\n\n>  If you do not specify this parameter, the value of **RequestId** is used.**** **RequestId** of each request is different."
      }
    },
    {
      "name": "ServiceCode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云产品的ServiceCode，仅取值：**nlb**。",
        "en": "The service code. Set the value to **nlb**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询地域",
    "en": "DescribeRegions"
  },
  "example": {
    "unifiedCli": "aliyun nlb describe-regions --service-code nlb --accept-language en-US --client-token 123e4567-e89b-12d3-a456-426655440000 --region cn-hangzhou",
    "legacyCli": "aliyun nlb DescribeRegions --region cn-hangzhou --ServiceCode nlb --AcceptLanguage 'en-US' --ClientToken '123e4567-e89b-12d3-a456-426655440000'"
  }
}