{
  "name": "UpdateAxbBindFixedLine",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Anucode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "主叫侧放音编码，固话AXB隐私号业务时必须设置。放音编码必须包含3个场景的编码。按照“B->X,A->X,其他号码->X”的顺序填写编码，编码之间以逗号分隔。\n\n> “1,2,3”表示B->X放音编号为1，A->X放音编号为2，其他号码->X放音编号为3。",
        "en": "The ringback tone code for the caller side. This parameter must be set for the landline AXB privacy number service. The tone code must contain codes for three scenarios. Enter the codes in the order of \"B->X, A->X, other numbers->X\", separated by commas.\n\n> \"1,2,3\" indicates that the tone code for B->X is 1, the tone code for A->X is 2, and the tone code for other numbers->X is 3."
      }
    },
    {
      "name": "Anucodecalled",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "接通后被叫侧放音编码，被叫放音编码必须包含2个场景的编码。按照“A被叫,B被叫”的顺序填写编码，编码之间以逗号分隔。\n\n> “1,2”表示A号码为被叫侧接听时的放音编号为1，B号码为被叫侧接听时的放音编号为2。",
        "en": "The ringback tone code for the called side after the call is connected. The called-side tone code must contain codes for two scenarios. Enter the codes in the order of \"A as called party, B as called party\", separated by commas.\n\n> \"1,2\" indicates that the tone code when number A is the called party is 1, and the tone code when number B is the called party is 2."
      }
    },
    {
      "name": "AppId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "号池ID。",
        "en": "The number pool ID."
      }
    },
    {
      "name": "Expiration",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "过期时间，单位：秒，取值：\n\n- 0：永不过期。\n\n- 其他数值：传其它数值时，比如60s，分配X号码成功的60秒内，绑定有效，超过60s后，自动解绑。",
        "en": "The expiration time, in seconds. Valid values:\n\n- 0: never expires.\n\n- Other values: when other values are specified, such as 60s, the binding is valid for 60 seconds after number X is successfully assigned. After 60 seconds, the binding is automatically released."
      }
    },
    {
      "name": "Extra",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "扩展参数。",
        "en": "The extension parameters."
      }
    },
    {
      "name": "OrderId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "消息请求唯一标识。",
        "en": "The unique identifier of the message request."
      }
    },
    {
      "name": "Remark",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "外部业务扩展字段，通话记录回执消息中会回传此参数。不能超过100个字符长度。",
        "en": "An external business extension field. This parameter is returned in the call record receipt message. The value cannot exceed 100 characters in length."
      }
    },
    {
      "name": "SubId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "绑定id。",
        "en": "The binding ID."
      }
    },
    {
      "name": "Subts",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "绑定时间，格式为yyyyMMddHHmmss。时间采用北京时间，24小时制。",
        "en": "The binding time in the format of yyyyMMddHHmmss. The time is in UTC+8, in 24-hour format."
      }
    },
    {
      "name": "TAnucodeConnect",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "接通后主叫侧的放音编码，接通后主叫侧放音编码必须包含2个场景的编码。按照“A被叫,B被叫”的顺序填写编码，编码之间以逗号分隔。\n\n> “1,2”表示A号码为被叫时主叫侧的放音编号为1，B号码为被叫时主叫侧的放音编号为2。",
        "en": "The ringback tone code for the caller side after the call is connected. The tone code must contain codes for two scenarios. Enter the codes in the order of \"A as called party, B as called party\", separated by commas.\n\n> \"1,2\" indicates that the tone code for the caller side when number A is the called party is 1, and the tone code for the caller side when number B is the called party is 2."
      }
    },
    {
      "name": "TelA",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置绑定关系中的 A 号码。\n\n\n> - A 号码可设置为手机号码或固定电话，固定电话需要加区号，区号和号码中间不需要加连字符。\n> - TelA，TelB不允许同时更新。",
        "en": "Sets number A in the binding relationship.\n\n\n> - Number A can be set to a mobile phone number or a landline number. Landline numbers must include the area code, with no hyphen between the area code and the number.\n> - TelA and TelB cannot be updated at the same time."
      }
    },
    {
      "name": "TelB",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "对设置绑定关系中的 B 号码。\n\n\n> - B 号码可设置为手机号码或固定电话，固定电话需要加区号，区号和号码中间不需要加连字符。\n> - TelA，TelB不允许同时更新。",
        "en": "Sets number B in the binding relationship.\n\n\n> - Number B can be set to a mobile phone number or a landline number. Landline numbers must include the area code, with no hyphen between the area code and the number.\n> - TelA and TelB cannot be updated at the same time."
      }
    },
    {
      "name": "Ts",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "业务时间戳，格式：yyyyMMddHHmmssSSS，时间采用北京时间，24小时制，精确至毫秒。",
        "en": "The business timestamp in the format of yyyyMMddHHmmssSSS. The time is in UTC+8, in 24-hour format, accurate to milliseconds."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "AXB固话隐私号-更新已有绑定",
    "en": "Update an existing landline AXB privacy number binding"
  },
  "descriptions": {
    "zh": "在修改固话AXB隐私号时，A号码和B号码不能同时进行修改。如原有绑定关系A1、B1可以修改为A1、B2或A2、B1。",
    "en": "When modifying a landline AXB privacy number, number A and number B cannot be modified at the same time. For example, if the existing binding relationship is A1 and B1, you can modify it to A1 and B2, or A2 and B1."
  }
}