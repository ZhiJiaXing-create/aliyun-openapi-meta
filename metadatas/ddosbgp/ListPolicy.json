{
  "name": "ListPolicy",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "策略名称。",
        "en": "The policy name."
      }
    },
    {
      "name": "PageNo",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页查询时，设置当前页面的页码。",
        "en": "The page number of the current page when you use paged query. Set the current page number for paging."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页查询时每页的行数。默认值为**10**。",
        "en": "The number of entries per page for a paged query. This parameter controls paging. Default value: **10**."
      }
    },
    {
      "name": "ProductType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "适用产品类型。取值：\n - **ecs**：表示对于查询适用于ecs的默认策略。\n\n-  **slb**：表示对于查询适用于slb的默认策略。\n\n-  **eip**：表示对于查询适用于eip的默认策略。\n\n - **gf-eip**：表示对于查询适用于高防EIP的默认策略。\n\n> 仅对策略类型为 `default`生效。",
        "en": "The applicable product type. Valid values:\n - **ecs**: queries the default policy applicable to ECS.\n\n-  **slb**: queries the default policy applicable to SLB.\n\n-  **eip**: queries the default policy applicable to EIP.\n\n - **gf-eip**: queries the default policy applicable to elastic IP addresses (EIPs) with Anti-DDoS Proxy Enabled.\n\n> This parameter takes effect only when the policy type is `default`."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "策略类型。取值：\n\n- **default**：表示默认防护策略。\n\n- **l3**：表示IP防护策略。\n\n- **l4**：表示端口防护策略。",
        "en": "The policy type. Valid values:\n\n- **default**: default mitigation policy.\n\n- **l3**: IP-specific mitigation policy.\n\n- **l4**: port-specific mitigation policy."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询防护策略",
    "en": "Query mitigation policies"
  },
  "example": {
    "unifiedCli": "aliyun ddosbgp list-policy --name test00 --type l3 --page-no 1 --page-size 10 --product-type ecs --api-version 2018-07-20 --region cn-hangzhou",
    "legacyCli": "aliyun ddosbgp ListPolicy --region cn-hangzhou --Type l3 --PageSize 10 --ProductType ecs --PageNo 1 --Name test00"
  }
}