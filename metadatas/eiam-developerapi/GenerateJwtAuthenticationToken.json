{
  "name": "GenerateJwtAuthenticationToken",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/v2/[instanceId]/authenticationTokens/_/actions/generateJwt",
  "parameters": [
    {
      "name": "Authorization",
      "position": "Header",
      "type": "String",
      "required": true,
      "description": {
        "zh": "认证信息。 格式为：Bearer ${access_token}。\n\n> 请输入 IDaaS 签发的 Access Token。",
        "en": "Authentication information. Format: Bearer ${access\\_token}.\n\n> Enter the Access Token issued by IDaaS."
      }
    },
    {
      "name": "body",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求 Body。",
        "en": "Request body."
      }
    },
    {
      "name": "instanceId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例ID。",
        "en": "Instance ID."
      }
    }
  ],
  "security": [
    "Anonymous"
  ],
  "title": {
    "zh": "生成一个JWT认证令牌",
    "en": "GenerateJwtAuthenticationToken"
  },
  "descriptions": {
    "zh": "该 API 基于 IDaaS 签发的 Access Token 进行身份认证和鉴权。\n\n请确保传入的 Access Token 具备访问 IDaaS 内置 PAM 应用（Privileged Access Management）的“获取认证令牌”的功能授权。\n\n> 对应的 scope 为`urn:cloud:idaas:pam|authentication_token:obtain`。",
    "en": "This API performs identity authentication and authorization using the Access Token issued by IDaaS.\n\nEnsure that the provided Access Token has the authorization to access the \"Obtain Authentication Token\" feature of the built-in Privileged Access Management (PAM) application in IDaaS.\n\n> The corresponding scope is `urn:cloud:idaas:pam|authentication_token:obtain`."
  }
}