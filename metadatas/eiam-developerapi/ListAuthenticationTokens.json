{
  "name": "ListAuthenticationTokens",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/v2/[instanceId]/authenticationTokens",
  "parameters": [
    {
      "name": "Authorization",
      "position": "Header",
      "type": "String",
      "required": true,
      "description": {
        "zh": "认证信息。 格式为：Bearer ${access_token}。\n\n> 请输入 IDaaS 签发的 Access Token。",
        "en": "Authentication information. Format: Bearer ${access\\_token}.\n\n> Enter an IDaaS-issued Access Token."
      }
    },
    {
      "name": "consumerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "认证令牌使用者 ID。",
        "en": "ID of the authentication token consumer."
      }
    },
    {
      "name": "credentialProviderIdentifier",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "凭据提供商标识。",
        "en": "Credential provider identifier."
      }
    },
    {
      "name": "expired",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "认证令牌是否已过期。",
        "en": "Indicates whether the authentication token is expired."
      }
    },
    {
      "name": "instanceId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例 ID。",
        "en": "Instance ID."
      }
    },
    {
      "name": "maxResults",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "分页查询，本次查询返回的最大记录数。",
        "en": "Maximum number of records to return in this paged query."
      }
    },
    {
      "name": "nextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页查询，下一页的起始位置索引Token。",
        "en": "Token that marks the start of the next page in a paged query."
      }
    },
    {
      "name": "revoked",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "认证令牌是否已吊销。",
        "en": "Indicates whether the authentication token is revoked."
      }
    }
  ],
  "security": [
    "Anonymous"
  ],
  "title": {
    "zh": "列举认证令牌",
    "en": "ListAuthenticationTokens"
  },
  "descriptions": {
    "zh": "该 API 基于 IDaaS 签发的 Access Token 进行身份认证和鉴权。\n\n请确保传入的 Access Token 具备访问 IDaaS 内置 PAM 应用（Privileged Access Management）的“查询认证令牌”的功能授权。\n\n> 对应的 scope 为`urn:cloud:idaas:pam|authentication_token:read`。",
    "en": "This API uses an Access Token issued by IDaaS for identity authentication and authorization.\n\nEnsure that the Access Token you provide has the Query authentication tokens permission for the built-in Privileged Access Management (PAM) application in IDaaS.\n\n> The required scope is `urn:cloud:idaas:pam|authentication_token:read`."
  }
}