{
  "name": "ObtainCredential",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "/v2/[instanceId]/credentials/_/actions/obtain",
  "parameters": [
    {
      "name": "Authorization",
      "position": "Header",
      "type": "String",
      "required": true,
      "description": {
        "zh": "认证信息。 格式为：Bearer ${access_token}。 \n> 请输入IDaaS签发的Access Token。",
        "en": "Authentication information. The format is `Bearer ${access_token}`.\n\n> Use an access token issued by IDaaS."
      }
    },
    {
      "name": "credentialIdentifier",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "凭据标识。",
        "en": "The identifier for the credential to obtain."
      }
    },
    {
      "name": "instanceId",
      "position": "Path",
      "type": "String",
      "required": true,
      "description": {
        "zh": "EIAM 实例 ID。",
        "en": "The ID of the IDaaS instance."
      }
    }
  ],
  "security": [
    "Anonymous"
  ],
  "title": {
    "zh": "查询一个含敏感信息的凭据",
    "en": "ObtainCredential"
  },
  "descriptions": {
    "zh": "该 API 基于 IDaaS 签发的 Access Token 进行身份认证和鉴权。\n\n请确保传入的 Access Token 具备访问 IDaaS 内置 PAM 应用（Privileged Access Management）的“获取静态凭据”的功能授权。\n\n> 对应的 scope 为 `urn:cloud:idaas:pam|credential:obtain`。",
    "en": "This API uses an access token from IDaaS for authentication and authorization.\n\nThe access token must have permissions to obtain static credentials for the built-in privileged access management (PAM) application in IDaaS.\n\n> The required scope is `urn:cloud:idaas:pam|credential:obtain`."
  }
}