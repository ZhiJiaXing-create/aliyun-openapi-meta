{
  "name": "DescribeBackupPlanConfig",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "执行备份计划的集群ID。",
        "en": "The ID of the cluster for which the backup plan is executed."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询备份计划配置",
    "en": "Query backup plan configuration"
  },
  "descriptions": {
    "zh": "该接口目前仅支持HBaseue，请确保在使用该接口前，该HBaseue已开通备份恢复功能。",
    "en": "This operation currently supports only HBaseue. Before calling this operation, make sure that the backup and recovery feature is enabled for the HBaseue instance."
  },
  "example": {
    "unifiedCli": "aliyun hbase describe-backup-plan-config --cluster-id ld-m5eznlga4k5bcxxxx --api-version 2019-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun hbase DescribeBackupPlanConfig --region cn-hangzhou --ClusterId 'ld-m5eznlga4k5bcxxxx'"
  }
}