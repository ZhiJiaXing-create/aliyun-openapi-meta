{
  "name": "DescribeRecoverableTimeRange",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "备份恢复的HBaseue集群Id。",
        "en": "The ID of the HBaseue cluster for which you want to query the recoverable time range."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取备份可恢复的时间范围",
    "en": "Retrieve the recoverable time range for backup"
  },
  "descriptions": {
    "zh": "请确保在使用该接口前，确认HBaseue集群已开通备份恢复功能。",
    "en": "Before calling this operation, make sure that the backup and recovery feature is enabled for the HBaseue cluster."
  },
  "example": {
    "unifiedCli": "aliyun hbase describe-recoverable-time-range --cluster-id ld-m5eznlga4k5bcxxxx --api-version 2019-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun hbase DescribeRecoverableTimeRange --region cn-hangzhou --ClusterId 'ld-m5eznlga4k5bcxxxx'"
  }
}