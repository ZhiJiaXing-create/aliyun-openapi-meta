{
  "name": "DeleteHBaseSlbServer",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    },
    {
      "name": "SlbServer",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "负载均衡服务。目前支持\nthrift\nrest",
        "en": "The load balancing service. Valid values:\nthrift\nrest."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除已开通的负载均衡服务器",
    "en": "Delete an activated load balancing service"
  },
  "example": {
    "unifiedCli": "aliyun hbase delete-hbase-slb-server --cluster-id hb-t4naqsay5gn0000 --slb-server thrift --api-version 2019-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun hbase DeleteHBaseSlbServer --region cn-hangzhou --SlbServer thrift --ClusterId 'hb-t4naqsay5gn0000'"
  }
}