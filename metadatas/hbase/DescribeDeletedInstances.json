{
  "name": "DescribeDeletedInstances",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码。",
        "en": "The page number."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码大小。",
        "en": "The number of entries per page."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求的地域region。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取已删除但未完全释放的集群列表",
    "en": "Retrieve a list of deleted but not fully released clusters"
  },
  "descriptions": {
    "zh": "集群删除默认7日后会完全清理资源。如果已经调用了清理接口PurgeInstance或自己调用接口删除时设置了ImmediateDeleteFlag为true时，则已经完全清理掉。",
    "en": "By default, cluster resources are fully cleaned up 7 days after deletion. If you have called the PurgeInstance operation or set ImmediateDeleteFlag to true when calling the delete operation, the resources have already been fully cleaned up."
  },
  "example": {
    "unifiedCli": "aliyun hbase describe-deleted-instances --biz-region-id cn-hangzhou --page-number 1 --page-size 10 --api-version 2019-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun hbase DescribeDeletedInstances --region cn-hangzhou --PageSize 10 --PageNumber 1 --RegionId 'cn-hangzhou'"
  }
}