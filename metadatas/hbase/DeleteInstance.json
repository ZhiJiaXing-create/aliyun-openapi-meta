{
  "name": "DeleteInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待释放的实例ID。",
        "en": "The instance ID of the instance to be released."
      }
    },
    {
      "name": "ImmediateDeleteFlag",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否立即释放实例。\n- **true**：立即释放。\n- **false**：不立即释放，存储在回收站， 默认值。",
        "en": "Specifies whether to immediately release the instance. Valid values:\n- **true**: The instance is immediately released.\n- **false**: The instance is not immediately released and is stored in the recycle bin. This is the default value."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除实例",
    "en": "Delete an instance"
  },
  "descriptions": {
    "zh": "调用本接口时，实例必须满足以下条件：\n\n- 实例状态为**运行中**。\n- 实例的付费方式为**按量付费**。\n\n> 包年包月实例无法调用此接口主动删除，到期后将自动释放。 如需提前释放，请提交工单申请。",
    "en": "When you call this operation, the instance must meet the following conditions:\n\n- The instance status is **Running**.\n- The billing method of the instance is **pay-as-you-go**.\n\n> Subscription instances cannot be deleted by calling this operation. They are automatically released upon expiration. To release a subscription instance in advance, submit a ticket."
  },
  "example": {
    "unifiedCli": "aliyun hbase delete-instance --cluster-id hb-bp16o0pd52e3y0000 --immediate-delete-flag false --api-version 2019-01-01 --region cn-hangzhou",
    "legacyCli": "aliyun hbase DeleteInstance --region cn-hangzhou --ImmediateDeleteFlag false --ClusterId 'hb-bp16o0pd52e3y0000'"
  }
}