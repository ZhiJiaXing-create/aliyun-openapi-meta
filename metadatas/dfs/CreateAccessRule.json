{
  "name": "CreateAccessRule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccessGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "权限组ID。",
        "en": "Permission group ID."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "权限组规则描述。\n\n长度不超过32个字符。",
        "en": "Description of the permission group rule.\n\nThe length cannot exceed 32 characters."
      }
    },
    {
      "name": "InputRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "Region ID."
      }
    },
    {
      "name": "NetworkSegment",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "授权对象的IP地址或网段。",
        "en": "IP address or network segment of the authorization object."
      }
    },
    {
      "name": "Priority",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "权限组规则优先级。\n\n当同一个授权对象匹配到多条规则时，高优先级规则生效。\n\n取值范围：1~100，1为最高优先级。",
        "en": "Priority of the permission group rule.\n\nWhen the same authorization object matches multiple rules, the rule with the highest priority takes effect.\n\nValid values: 1 to 100. A value of 1 indicates the highest priority."
      }
    },
    {
      "name": "RWAccessType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "授权对象对文件系统的读写权限。\n\n取值：\n\n- RDWR：可读可写\n- RDONLY：只读",
        "en": "Read and write permissions of the authorization object on the file system.\n\nValid values:\n\n- RDWR: read-write\n- RDONLY: read-only"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建权限规则",
    "en": "Create a permission rule"
  }
}