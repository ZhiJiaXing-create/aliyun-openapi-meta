{
  "name": "CreateAccessGroup",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AccessGroupName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "权限组名称。命名规则如下：\n\n- 长度为6~64个字符。\n- 全局唯一且不能为空串。\n- 支持英文字母，可包含数字、下划线（_）和短划线（-）。",
        "en": "Name of the permission group. The naming convention is as follows:\n\n- The length must be 6 to 64 characters.\n- It must be globally unique and cannot be empty.\n- It supports English letters and can contain digits, underscores (_), and hyphens (-)."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "权限组描述。\n\n长度不超过32个字符。",
        "en": "Description of the permission group.\n\nThe length cannot exceed 32 characters."
      }
    },
    {
      "name": "InputRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "Region ID."
      }
    },
    {
      "name": "NetworkType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "权限组类型。\n\n仅支持VPC（专有网络）。",
        "en": "Type of the permission group.\n\nOnly VPC (virtual private cloud) is supported."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建权限组",
    "en": "Create Permission Group"
  }
}