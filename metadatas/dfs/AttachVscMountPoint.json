{
  "name": "AttachVscMountPoint",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "预留字段，无实际意义。",
        "en": "A reserved field. This field has no effect."
      }
    },
    {
      "name": "FileSystemId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件系统ID。",
        "en": "File system ID."
      }
    },
    {
      "name": "InputRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "Region ID."
      }
    },
    {
      "name": "InstanceIds",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "待挂载文件存储 HDFS 版文件系统的ECS实例ID。 添加多个ECS实例ID时，使用半角逗号（,）分隔。最多支持100个ECS实例ID。",
        "en": "ECS instance IDs to attach to the Apsara File Storage for HDFS file system. Separate multiple IDs with commas (,). You can specify up to 100 ECS instance IDs."
      }
    },
    {
      "name": "MountPointId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "挂载点ID。",
        "en": "Mount target ID."
      }
    },
    {
      "name": "UseAssumeRoleChkServerPerm",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否使用扮演者检验计算节点权限。\n\n- true：是\n- false（默认值）：否",
        "en": "Whether to use role assumption to check compute node permissions. Valid values:\n\n- true\n\n- false (default)"
      }
    },
    {
      "name": "VscIds",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "待挂载HDFS文件系统的VSC ID。\n建议添加不超过100个VSC ID。",
        "en": "VSC IDs to attach to the HDFS file system. We recommend that you specify no more than 100 VSC IDs."
      }
    },
    {
      "name": "VscName",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "VscType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "VSC类型。\n取值： \n- Primary（默认值）：ECS实例上的预留默认VSC\n- Secondary：ECS实例上的可重复VSC",
        "en": "VSC type. Valid values:\n\n- Primary (default): A reserved default VSC on an ECS instance.\n\n- Secondary: A reusable VSC on an ECS instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "绑定VSC挂载点",
    "en": "Attach VSC Mount Target"
  },
  "descriptions": {
    "zh": "****",
    "en": "***"
  }
}