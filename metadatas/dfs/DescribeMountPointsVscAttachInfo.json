{
  "name": "DescribeMountPointsVscAttachInfo",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InputRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件系统所在的Region。",
        "en": "The region where the file system is located."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页查询时每页行数。最大值为100。默认值：当不设置值或设置的值小于10时，默认值为10。当设置的值大于100时，默认值为100。",
        "en": "The number of entries to return on each page. The maximum value is 100. If you specify a value greater than 100, the system uses 100. If you do not specify this parameter or specify a value less than 10, the system uses the default value of 10."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "下一页标识。\n\n如果一次查询结果并没有完整返回所有的目录，则返回 NextToken 不为空。您可以在查询中上传正确的 NextToken 继续查询。",
        "en": "A pagination token.\n\nThis token is returned when the response is truncated. To retrieve the next page of results, include this token in your next request."
      }
    },
    {
      "name": "QueryInfos",
      "position": "Query",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "查询信息列表",
        "en": "A list of query conditions."
      },
      "sub_parameters": [
        {
          "name": "InstanceId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "已挂载HDFS文件系统的ECS实例ID。",
            "en": "The ID of the ECS instance to which the HDFS file system is mounted."
          }
        },
        {
          "name": "MountPointId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "挂载点ID。",
            "en": "The ID of the mount point."
          }
        },
        {
          "name": "VscId",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "已挂载HDFS文件系统的VSC ID。VSC ID，InstanceId与VscId只能设置一个，且必须设置一个",
            "en": "The ID of the VSC to which the HDFS file system is mounted. You must specify either this parameter or `InstanceId`."
          }
        }
      ]
    },
    {
      "name": "UseAssumeRoleChkServerPerm",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否使用扮演者检验计算节点权限。\n\n- true：是\n- false（默认值）：否",
        "en": "Specifies whether to use a role to verify the permissions of the compute node.\n\n- true: A role is used to verify the permissions.\n\n- false (default): A role is not used to verify the permissions."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量查询挂载信息",
    "en": "Batch query mount information"
  },
  "descriptions": {
    "zh": "只支持一个存储（MountPointId）对应多个计算（InstanceId、VscId） 和 一个计算（InstanceId、VscId）对应多个存储（MountPointId）",
    "en": "This operation queries the relationships between mount points (`MountPointId`) and compute nodes (`InstanceId` or `VscId`). You can use it to find all compute nodes attached to one mount point, or all mount points attached to one compute node."
  }
}