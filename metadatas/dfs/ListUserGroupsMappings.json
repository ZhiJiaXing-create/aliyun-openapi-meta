{
  "name": "ListUserGroupsMappings",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FilesystemId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件存储 HDFS 版的文件系统实例ID。",
        "en": "Instance ID of the Apsara File Storage for HDFS file system."
      }
    },
    {
      "name": "InputRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件系统所在的Region。",
        "en": "The region where the file system is located."
      }
    },
    {
      "name": "Limit",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "每次查询需要返回的用户个数。",
        "en": "Number of users to return per query."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页查询的下一页起始点，首次查询是为空字符串，如果需要分页查询，则每个请求的返回值中会包含下次请求的NextToken值。",
        "en": "Starting point for the next page in a paged query. It is an empty string for the first query. If paged queries are required, the return value of each request includes the NextToken value for the next request."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查看用户和组的映射关系",
    "en": "View User and Group Mapping"
  },
  "descriptions": {
    "zh": "文件存储 HDFS 版的文件系统权限管理依赖文件的用户和组，用户和组映射关系需要通过OpenAPI进行管理。有关文件存储 HDFS 版文件系统目录和文件访问权限的信息，请参见[目录和文件访问权限](~~430865~~)。\n\n单个文件系统存在用户和组数量的限制，包括：\n- 单个文件系统最多添加 1000 个用户。\n- 单个文件系统最多添加 30 个组。\n- 每个用户最多属于 10 个组。",
    "en": "Permission Management for Apsara File Storage for HDFS file systems depends on users and groups associated with files. The user and group mappings must be managed through OpenAPI. For information about folder and file access permissions in Apsara File Storage for HDFS file systems, see [Folder and File Access Permissions](~~430865~~).\n\nA single file system has limits on the number of users and groups, including:\n- A maximum of 1,000 users can be added to a single file system.\n- A maximum of 30 groups can be added to a single file system.\n- Each user can belong to a maximum of 10 groups."
  }
}