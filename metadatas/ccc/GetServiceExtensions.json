{
  "name": "GetServiceExtensions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "呼叫中心实例ID"
      }
    },
    {
      "name": "ServiceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要获取的服务号码的服务类型，比如满意度调查（SatisfactionSurvey）。"
      }
    }
  ],
  "security": [
    "AK",
    "BearerToken"
  ],
  "title": {
    "zh": "获取服务号码",
    "en": "GetServiceExtensions"
  },
  "descriptions": {
    "zh": "> 目前只可以获取发送语音满意度时所需的分机号码（实际是满意度调查IVR的路由点，当您开启了语音满意度调研功能后，会自动生成）。"
  }
}