{
  "name": "ListUserClusterTypes",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询用户可以使用的集群类型",
    "en": "Query the cluster types available to the user"
  },
  "descriptions": {
    "en": "An interface for creating a session, which returns the front-end EndPoint and initiates a periodic task to track the session status"
  },
  "example": {
    "unifiedCli": "aliyun eflo-controller list-user-cluster-types --region cn-hangzhou",
    "legacyCli": "aliyun eflo-controller ListUserClusterTypes --region cn-hangzhou"
  }
}