{
  "name": "BindJoinPermissionToNodeGroup",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "JoinPermissionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "参与关联的入网凭证ID。",
        "en": "The ID of the join permission to associate."
      }
    },
    {
      "name": "NodeGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "参与关联的节点分组ID。",
        "en": "The ID of the node group to associate."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "建立入网凭证和节点分组的关联",
    "en": "Associate a join permission with a node group"
  }
}