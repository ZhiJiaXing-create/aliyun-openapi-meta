{
  "name": "ListGatewayTransferPackets",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Ascending",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "配合**SortingField**参数一起使用，**true**表示升序，**false**表示降序。",
        "en": "Specifies the sort order. Used together with the **SortingField** parameter. A value of **true** indicates ascending order. A value of **false** indicates descending order."
      }
    },
    {
      "name": "BeginMillis",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "查询开始时间，毫秒为单位的UNIX时间戳。",
        "en": "The start time of the query. The value is a UNIX timestamp in milliseconds."
      }
    },
    {
      "name": "Category",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据包分类。\n\n- DOWNLINK_PACKET：下行包。\n\n- UPLINK_PACKET：上行包。\n\n- INVALID_PACKET：无效包。",
        "en": "The packet category. Valid values:\n\n- DOWNLINK_PACKET: downlink packet.\n\n- UPLINK_PACKET: uplink packet.\n\n- INVALID_PACKET: invalid packet."
      }
    },
    {
      "name": "DevEui",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "根据DevEUI过滤数据包。",
        "en": "The DevEUI used to filter packets."
      }
    },
    {
      "name": "EndMillis",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "查询结束时间，毫秒为单位的UNIX时间戳。",
        "en": "The end time of the query. The value is a UNIX timestamp in milliseconds."
      }
    },
    {
      "name": "GwEui",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要查询的网关的GwEUI。",
        "en": "The GwEUI of the gateway to query."
      }
    },
    {
      "name": "IotInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "物联网平台的实例id，为空表示公共实例。",
        "en": "The ID of the IoT Platform instance. Leave this parameter empty for the public instance."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "分页页码，从1开始。",
        "en": "The page number, starting from 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "分页大小，大于等于1。",
        "en": "The page size. The value must be greater than or equal to 1."
      }
    },
    {
      "name": "SortingField",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序字段，取值：**LOG_MILLIS**，表示按照时间排序。",
        "en": "The sorting field. Set the value to **LOG_MILLIS** to sort by time."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取网关上下行数据包的统计信息",
    "en": "Retrieve gateway uplink and downlink packet statistics"
  },
  "descriptions": {
    "zh": "****",
    "en": "****."
  }
}