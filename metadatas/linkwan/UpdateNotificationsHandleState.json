{
  "name": "UpdateNotificationsHandleState",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "NotificationId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "通知ID列表。",
        "en": "The list of notification IDs."
      }
    },
    {
      "name": "TargetHandleState",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要设置的处理状态，可取值为**HANDLED**（已处理）, **UNHANDLED**（未处理）。",
        "en": "The handling status to set. Valid values:\n- **HANDLED**: handled.\n- **UNHANDLED**: not handled."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新通知的处理状态",
    "en": "Update notification handling status"
  }
}