{
  "name": "UnbindJoinPermissionFromNodeGroup",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "JoinPermissionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "参与关联的入网凭证ID。",
        "en": "The ID of the join permission involved in the association."
      }
    },
    {
      "name": "NodeGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要接触关联的节点分组ID。",
        "en": "The ID of the node group to dissociate."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "接触入网凭证和节点分组的关联",
    "en": "Dissociate a join permission from a node group"
  }
}