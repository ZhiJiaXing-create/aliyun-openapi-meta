{
  "name": "SendUnicastCommand",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CleanUp",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否清空该节点的堆积指令，并下发该指令。默认为**false**。",
        "en": "Specifies whether to clear the queued instructions for the node and send this instruction. Default value: **false**."
      }
    },
    {
      "name": "Confirmed",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否是confirmed指令，默认为**false**。",
        "en": "Specifies whether the instruction is a confirmed instruction. Default value: **false**."
      }
    },
    {
      "name": "Content",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要发送的内容。16进制字符串，最长51个字节（如果换算成16进制字符串，最长为51*2个字符）。",
        "en": "The content to send. The value is a hexadecimal string with a maximum length of 51 bytes (102 hexadecimal characters)."
      }
    },
    {
      "name": "DevEui",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目标节点。",
        "en": "The target node."
      }
    },
    {
      "name": "FPort",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "fPort。",
        "en": "The fPort."
      }
    },
    {
      "name": "IotInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "物联网平台的实例id，为空表示公共实例。",
        "en": "The ID of the IoT Platform instance. An empty value indicates the public instance."
      }
    },
    {
      "name": "MaxRetries",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "最大重发次数，默认为0。",
        "en": "The maximum number of retries. Default value: 0."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "发送单播信息",
    "en": "Send unicast command"
  }
}