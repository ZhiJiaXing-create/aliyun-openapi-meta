{
  "name": "CancelJoinPermissionAuthOrder",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "OrderId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要撤销的授权工单ID。",
        "en": "The ID of the authorization ticket to cancel."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "取消专用入网凭证授权工单",
    "en": "Cancel a dedicated join permission authorization ticket"
  },
  "descriptions": {
    "zh": "网络运营者可以通过该接口取消一个尚未被接受的授权工单。",
    "en": "Network operators can call this operation to cancel an authorization ticket that has not been accepted."
  }
}