{
  "name": "CountNodeGroups",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FuzzyDevEui",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模糊匹配节点分组中节点的 DevEUI。",
        "en": "The DevEUI of nodes in the node group for fuzzy matching."
      }
    },
    {
      "name": "FuzzyJoinEui",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模糊匹配节点分组中节点所使用的 JoinEUI。",
        "en": "The JoinEUI used by nodes in the node group for fuzzy matching."
      }
    },
    {
      "name": "FuzzyName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模糊匹配节点分组名称。",
        "en": "The node group name for fuzzy matching."
      }
    },
    {
      "name": "IotInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。\n您可在物联网平台控制台的**实例概览**页面，查看当前实例的**ID**。若有ID值，必须传入该ID值，否则调用会失败。\n\n> 如果公共实例没有ID值，请参见[如何获取实例ID](~~267533~~)。",
        "en": "The instance ID.\n\nYou can view the **ID** of the current instance on the **Instance Overview** page in the IoT Platform console. If an ID value exists, you must specify this ID value. Otherwise, the call fails.\n\n> If no ID value exists for the public instance, refer to [How to obtain an instance ID](~~267533~~)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "CountNodeGroups",
    "en": "CountNodeGroups"
  }
}