{
  "name": "ReturnJoinPermission",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "JoinPermissionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要归还的入网凭证ID。",
        "en": "The ID of the join permission to return."
      }
    },
    {
      "name": "JoinPermissionType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要归还的入网凭证类型。",
        "en": "The type of the join permission to return."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "归还租用的入网凭证",
    "en": "Return a rented join permission"
  }
}