{
  "name": "GetOwnedJoinPermission",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IotInstanceId",
      "position": "Query",
      "type": "String",
      "required": false
    },
    {
      "name": "JoinPermissionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要获取的入网凭证的ID。",
        "en": "The ID of the join permission to retrieve."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取入网凭证",
    "en": "Retrieve a join permission"
  },
  "descriptions": {
    "zh": "> 通过该接口获取的一定是当前用户账号所拥有的某个专用入网凭证。",
    "en": "> The join permission retrieved through this operation is a dedicated join permission owned by the current user account."
  }
}