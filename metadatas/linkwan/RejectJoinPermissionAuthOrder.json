{
  "name": "RejectJoinPermissionAuthOrder",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "OrderId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要回绝的授权工单ID。",
        "en": "The ID of the authorization ticket to reject."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "回绝专用入网凭证授权工单",
    "en": "Reject an authorization ticket for a dedicated join permission"
  },
  "descriptions": {
    "zh": "> 开发者（解决方案供应商）通过该接口回绝一个由网络运营者授权的专用入网凭证。",
    "en": "> Developers (Solutions providers) call this operation to reject an authorization ticket for a dedicated join permission that is granted by a network operator."
  }
}