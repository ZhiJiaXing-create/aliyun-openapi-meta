{
  "name": "ListGatewayTupleOrders",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Ascending",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "需配合**SortingField**参数一起使用。\n\n- **true**：表示升序。\n\n- **false**：表示降序。",
        "en": "Used together with the **SortingField** parameter.\n\n- **true**: ascending order.\n\n- **false**: descending order."
      }
    },
    {
      "name": "Limit",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "本次查询的工单数量上限，需大于等于1。",
        "en": "The maximum number of tickets to return in this query. The value must be greater than or equal to 1."
      }
    },
    {
      "name": "Offset",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "本次查询的起始位置，从0开始。",
        "en": "The start position of the query, starting from 0."
      }
    },
    {
      "name": "SortingField",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询结果排序字段。可取值为：\n\n- **CREATED_MILLIS**：根据工单发起时间排序。\n\n- **ACCEPTED_MILLIS**：根据工单审批通过时间排序。",
        "en": "The field used to sort the query results. Valid values:\n\n- **CREATED_MILLIS**: sorts by ticket creation time.\n\n- **ACCEPTED_MILLIS**: sorts by ticket approval time."
      }
    },
    {
      "name": "State",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "工单状态过滤列表，若不使用此参数，则表示查询所有状态的工单。可取值为：\n\n- APPLYING：申请中。\n\n- REJECTED：已拒绝。\n\n- ACCEPTING：分发中。\n\n- ACCEPTED：分发成功。\n\n- ACCEPTING_FAILED：分发失败。\n\n- REVOKING：撤回中。\n\n- REVOKED：撤回成功。\n\n- REVOKING_FAILED：撤回失败。",
        "en": "The list of ticket states used to filter results. If you do not specify this parameter, tickets in all states are queried. Valid values:\n\n- APPLYING: pending approval.\n\n- REJECTED: rejected.\n\n- ACCEPTING: being distributed.\n\n- ACCEPTED: distributed.\n\n- ACCEPTING_FAILED: distribution failed.\n\n- REVOKING: being revoked.\n\n- REVOKED: revoked.\n\n- REVOKING_FAILED: revocation failed."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取用户已提交的网关元组工单列表",
    "en": "Retrieve the list of gateway tuple tickets submitted by the user"
  }
}