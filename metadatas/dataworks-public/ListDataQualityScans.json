{
  "name": "ListDataQualityScans",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模糊匹配数据质量校验任务名称。",
        "en": "The data quality scan task name for fuzzy match."
      }
    },
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "列表翻页页码，默认为1。",
        "en": "The page number. Default value: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "列表翻页大小，默认为10。",
        "en": "The number of entries per page. Default value: 10."
      }
    },
    {
      "name": "ProjectId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "项目ID。",
        "en": "The project ID."
      }
    },
    {
      "name": "SortBy",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "排序字段列表，支持修改时间、创建时间等字段，格式为\"排序字段+排序方式(Desc/Asc)\"（默认Asc，可以省略）。排序字段的枚举值如下：\n\n- ModifyTime (Desc/Asc)\n- CreateTime (Desc/Asc)\n- Id (Desc/Asc)",
        "en": "The list of sorting fields. Supports fields such as last modified time and creation time. Format: \"SortField+SortOrder (Desc/Asc)\", where Asc is the default. Valid values:\n\n- ModifyTime (Desc/Asc)\n\n- CreateTime (Desc/Asc)\n\n- Id (Desc/Asc)"
      }
    },
    {
      "name": "Table",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模糊匹配监控的表名。",
        "en": "Fuzzy match for the monitored table name."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "数据质量校验任务列表查询",
    "en": "ListDataQualityScans"
  },
  "descriptions": {
    "zh": "需要购买DataWorks基础版及以上版本才能使用。",
    "en": "DataWorks Basic Edition or a higher edition is required."
  },
  "example": {
    "unifiedCli": "aliyun dataworks-public list-data-quality-scans --name test --sort-by ModifyTime_Desc --page-size 10 --page-number 1 --project-id 10000 --table video_album --api-version 2024-05-18 --region cn-hangzhou",
    "legacyCli": "aliyun dataworks-public ListDataQualityScans --region cn-hangzhou --Table video_album --PageSize 10 --PageNumber 1 --SortBy ModifyTime_Desc --ProjectId 10000 --Name test"
  }
}