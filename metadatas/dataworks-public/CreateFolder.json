{
  "name": "CreateFolder",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "FolderPath",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件夹的路径\n。",
        "en": "The path of the folder."
      }
    },
    {
      "name": "ProjectId",
      "position": "Body",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "DataWorks工作空间的ID。\n您可以登录DataWorks管控台，进入工作空间配置页面获取工作空间ID。该参数与**ProjectIdentifier**参数，二者必须设置其一，用来确定本次API调用操作的DataWorks工作空间。",
        "en": "The ID of the DataWorks workspace. You can obtain the workspace ID from the workspace configuration page in the DataWorks console. Either this parameter or **ProjectIdentifier** must be specified to determine which DataWorks workspace this API call operates on."
      }
    },
    {
      "name": "ProjectIdentifier",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "DataWorks工作空间名称。\n您可以登录DataWorks控制台，进入工作空间配置页面获取工作空间名称。该参数与**ProjectId**参数，二者必须设置其一，用来确定本次API调用操作的DataWorks工作空间。",
        "en": "The name of the DataWorks workspace. You can obtain the workspace name from the workspace configuration page in the DataWorks console. Either this parameter or **ProjectId** must be specified to determine which DataWorks workspace this API call operates on."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建文件夹",
    "en": "CreateFolder"
  },
  "example": {
    "unifiedCli": "aliyun dataworks-public create-folder --project-id 10000 --project-identifier dw_project --folder-path Business_process/System_Data/MaxCompute/import_layer --api-version 2024-05-18 --region cn-hangzhou",
    "legacyCli": "aliyun dataworks-public CreateFolder --region cn-hangzhou --ProjectIdentifier dw_project --FolderPath 'Business_process/System_Data/MaxCompute/import_layer' --ProjectId 10000"
  }
}