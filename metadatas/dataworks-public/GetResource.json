{
  "name": "GetResource",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Id",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "文件资源的唯一标识符。\n\n><notice>该字段在SDK8.0.0版本之前为Long类型，SDK8.0.0版本及之后为String类型。**该变更不影响SDK正常使用，参数仍会按SDK中定义的类型返回**。只有在跨8.0.0版本的SDK升级时，类型变更可能会导致项目编译不通过，需要用户手动修正数据类型。></notice>",
        "en": "The unique identifier of the file resource.\n\n> This field is of type Long in SDK versions prior to 8.0.0, and of type String in SDK version 8.0.0 and later. This change does not affect the normal use of the SDK; parameters are still returned according to the type defined in the SDK. Compilation failures due to the type change may occur only when upgrading the SDK across version 8.0.0, in which case users need to manually correct the data type."
      }
    },
    {
      "name": "ProjectId",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "DataWorks工作空间的ID。您可以登录[DataWorks控制台](https://workbench.data.aliyun.com/console)，进入工作空间管理页面获取ID。\n\n该参数用来确定本次API调用操作使用的DataWorks工作空间。",
        "en": "The DataWorks workspace ID. You can log on to the [DataWorks console](https://workbench.data.aliyun.com/console) and go to the Workspace page to query the ID.\n\nYou must configure this parameter to specify the DataWorks workspace to which the API operation is applied."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取数据开发文件资源详情",
    "en": "GetResource"
  },
  "example": {
    "unifiedCli": "aliyun dataworks-public get-resource --project-id 10000 --id 860438872620110000 --api-version 2024-05-18 --region cn-hangzhou",
    "legacyCli": "aliyun dataworks-public GetResource --region cn-hangzhou --ProjectId 10000 --Id 860438872620110000"
  }
}