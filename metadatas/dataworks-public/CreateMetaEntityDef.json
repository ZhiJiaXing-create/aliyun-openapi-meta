{
  "name": "CreateMetaEntityDef",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AttributeDefs",
      "position": "Body",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "纯自定义实体的属性定义列表。extend=TABLE时不能传入",
        "en": "A list of attribute definitions for the pure custom type. Do not specify this parameter if the `Extend` parameter is set to `TABLE`."
      }
    },
    {
      "name": "Description",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "描述",
        "en": "A description of the entity definition."
      }
    },
    {
      "name": "DisplayName",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "展示名称，最长32个字符",
        "en": "The display name. The maximum length is 32 characters."
      }
    },
    {
      "name": "Extend",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "扩展模式。\n- NONE，默认值，表示纯自定义实体，属性自由定义；\n- TABLE表示扩展表类型，参考数据地图已有表类型的方式介入，不需要提供属性定义，可以创建对应的Database/Table对象",
        "en": "The extension mode. Valid values:\n\n- `NONE`: The default value. Specifies a pure custom type with user-defined attributes.\n\n- `TABLE`: Specifies an extended table type that references an existing table type in Data Map. Attribute definitions are not required for this type. You can create corresponding `Database` and `Table` objects for it."
      }
    },
    {
      "name": "Name",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "类型定义名称。纯自定义类型需匹配 `^[a-z0-9][a-z0-9_]*$`；扩展表类型需匹配 `^[a-z0-9][a-z0-9_]*-table$`",
        "en": "The type definition name. For a pure custom type, the name must match `^[a-z0-9][a-z0-9_]*$`. For an extended table type, the name must match `^[a-z0-9][a-z0-9_]*-table$`."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建元数据实体类型定义",
    "en": "CreateMetaEntityDef"
  },
  "descriptions": {
    "zh": "需要购买 DataWorks 专业版及以上版本才能使用。",
    "en": "This operation requires DataWorks Professional Edition or a higher edition."
  },
  "example": {
    "unifiedCli": "aliyun dataworks-public create-meta-entity-def --name biz_api --display-name Business_API --description description --extend NONE --attribute-defs '[\"example-string\"]' --api-version 2024-05-18 --region cn-hangzhou",
    "legacyCli": "aliyun dataworks-public CreateMetaEntityDef --region cn-hangzhou --AttributeDefs '[\"example-string\"]' --Description description --Extend NONE --DisplayName Business_API --Name biz_api"
  }
}