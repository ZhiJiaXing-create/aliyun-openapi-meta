{
  "name": "DeleteAgent",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "要删除的 Agent 名称。"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除 Agent"
  },
  "descriptions": {
    "zh": "## 请求说明\n该 API 用于从 DataWorks 中删除指定名称的 Agent。调用此接口时，必须提供要删除的 Agent 的名称。",
    "en": "## 请求说明\n该 API 用于从 DataWorks 中删除指定名称的 Agent。调用此接口时，必须提供要删除的 Agent 的名称。"
  },
  "example": {
    "unifiedCli": "aliyun dataworks-public delete-agent --name my-agent --api-version 2024-05-18 --region cn-hangzhou",
    "legacyCli": "aliyun dataworks-public DeleteAgent --region cn-hangzhou --Name 'my-agent'"
  }
}