{
  "name": "ListCustomAgents",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "本次请求返回的最大条数。",
        "en": "The maximum number of entries to return on a single page."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页 Token。首次查询不传；后续查询传上一次返回的 `NextToken` 获取下一页。",
        "en": "The pagination token. Omit this parameter for the first request. For subsequent requests, use the `NextToken` value from the previous response to retrieve the next page."
      }
    },
    {
      "name": "Q",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "搜索关键词，按 Agent 名称模糊匹配。",
        "en": "The search keyword for a fuzzy match by agent name."
      }
    },
    {
      "name": "Visibility",
      "position": "Body",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "按可见性级别过滤，可传多个。",
        "en": "Filters the results by visibility level. You can specify multiple levels."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "分页列出 自定义 Agent",
    "en": "ListCustomAgents"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n- **搜索关键词**：通过 `Q` 参数可以按 Agent 名称进行模糊匹配。\n- **可见性级别过滤**：使用 `Visibility` 参数可以根据可见性级别（如 `TENANT`、`PROJECT`、`USER`）来过滤结果。\n- **分页信息**：通过 `MaxResults` 和 `NextToken` 参数实现分页查询，其中 `NextToken` 用于获取下一页的数据。",
    "en": "## Request\n\n- **Search keyword**: Use the `Q` parameter to perform a fuzzy search by agent name.\n\n- **Visibility level filtering**: Use the `Visibility` parameter to filter results by visibility level, such as `TENANT`, `PROJECT`, or `USER`.\n\n- **Paging information**: Use the `MaxResults` and `NextToken` parameters to implement paginated queries. `NextToken` retrieves the next page of results."
  },
  "example": {
    "unifiedCli": "aliyun dataworks-public list-custom-agents --max-results 20 --next-token 12345 --q analysis --visibility TENANT --api-version 2024-05-18 --region cn-hangzhou",
    "legacyCli": "aliyun dataworks-public ListCustomAgents --region cn-hangzhou --Q analysis --NextToken 12345 --MaxResults 20 --Visibility '[\"TENANT\"]'"
  }
}