{
  "name": "ListMcpServers",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "本次请求返回的最大条数。",
        "en": "The maximum number of results to return per page."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页 Token。首次查询不传；后续查询传上一次返回的 NextToken 获取下一页。",
        "en": "The next page token from a previous response. Use this token to retrieve the next page of results. Leave this parameter empty for the first request."
      }
    },
    {
      "name": "Q",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "搜索关键词，按 MCP Server 名称模糊匹配。",
        "en": "The search keyword for a fuzzy search on MCP Server names."
      }
    },
    {
      "name": "Visibility",
      "position": "Body",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "按可见性级别过滤，可传多个。",
        "en": "The visibility level for filtering the results."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "分页列出 MCP Server",
    "en": "ListMcpServers"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n该API用于分页查询当前账号下的所有MCP Server列表。支持通过搜索关键词、可见性级别过滤以及设置最大返回条数和分页Token来获取特定的MCP Server信息。\n\n- **Q**: 可选参数，用于按MCP Server名称进行模糊匹配搜索。\n- **Visibility**: 可选参数，允许传入多个值以按可见性级别（如`TENANT`、`PROJECT`或`USER`）过滤结果。\n- **MaxResults**: 可选参数，指定本次请求返回的最大条数，默认不限制。\n- **NextToken**: 可选参数，用于分页查询时传递上一次请求返回的NextToken值以获取下一页数据。",
    "en": "## Request\n\nThis operation retrieves a paginated list of all MCP Servers within your account. You can filter the list by search keyword and visibility level, and control pagination by specifying the maximum number of results and a next page token.\n\n- **Q**: Optional. The search keyword for a fuzzy search on MCP Server names.\n\n- **Visibility**: Optional. The visibility level for filtering the results.\n\n- **MaxResults**: Optional. The maximum number of results to return per page. By default, no limit is applied.\n\n- **NextToken**: Optional. The next page token from a previous response. Use this parameter to retrieve the next page of results."
  },
  "example": {
    "unifiedCli": "aliyun dataworks-public list-mcp-servers --q mcp --visibility TENANT --max-results 20 --next-token 12345 --api-version 2024-05-18 --region cn-hangzhou",
    "legacyCli": "aliyun dataworks-public ListMcpServers --region cn-hangzhou --Q mcp --NextToken 12345 --MaxResults 20 --Visibility '[\"TENANT\"]'"
  }
}