{
  "name": "UpdateTableBusinessMetadata",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CustomAttributes",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自定义属性值，key为自定义属性标识，value最多一个值；空列表表示删除该属性值。不传Readme时传入本参数可避免清空使用说明；传空对象表示不更新自定义属性",
        "en": "The values of custom attributes. The key specifies the identifier of a custom attribute, and the value is an array that can contain at most one item. To delete the value for an attribute, pass an empty array. To update only custom attributes, omit the `Readme` parameter to prevent its existing value from being cleared. To leave the custom attributes unchanged, pass an empty object `{}`."
      }
    },
    {
      "name": "Id",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据表ID，格式可参考ListTables接口返回结果。",
        "en": "The table ID. For the required format, see the response of the `ListTables` operation."
      }
    },
    {
      "name": "Readme",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "使用说明，支持富文本格式。",
        "en": "The Readme of the table, which supports rich text format."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新数据地图数据表的业务元数据",
    "en": "UpdateTableBusinessMetadata"
  },
  "descriptions": {
    "zh": "1. 需要购买DataWorks基础版及以上版本才能使用。",
    "en": "1. You must purchase DataWorks Basic Edition or a later version to use this operation."
  },
  "example": {
    "unifiedCli": "aliyun dataworks-public update-table-business-metadata --id maxcompute-table:::project_name:_schema_name_:table_name --readme '##_introduction' --custom-attributes '{\"biz_owner\":[\"张三\"]}' --api-version 2024-05-18 --region cn-hangzhou",
    "legacyCli": "aliyun dataworks-public UpdateTableBusinessMetadata --region cn-hangzhou --CustomAttributes '{\"biz_owner\":[\"张三\"]}' --Readme '##_introduction' --Id 'maxcompute-table:::project_name:_schema_name_:table_name'"
  }
}