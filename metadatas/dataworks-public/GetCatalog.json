{
  "name": "GetCatalog",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Id",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "数据目录实体ID，当前支持dlf与starrocks类型，可参考ListCatalogs接口返回结果，以及[元数据实体相关概念说明](~~2880092~~)。\n\n\n- 对于dlf类型，格式为 `dlf-catalog::catalog_id`\n\n- 对于starrocks类型，格式为`starrocks-catalog:(instance_id|encoded_jdbc_url):catalog_name`\n\n> 其中  \n`catalog_id`: dlf目录id  \n`instance_id`: 实例ID，对应数据源以实例模式注册时需要提供  \n`encoded_jdbc_url`: JDBC连接串的URLEncode编码,对应数据源以连接串方式注册时需要提供  \n`catalog_name`: starrocks目录名称",
        "en": "The catalog entity ID. Currently supports dlf and starrocks types. You can refer to the results returned by the ListCatalogs operation and the [Concepts related to metadata entities](~~2880092~~).\n\n- For the DLF type, the format is `dlf-catalog::catalog_id`.\n\n- For the StarRocks type, the format is `starrocks-catalog:(instance_id|encoded_jdbc_url):catalog_name`.\n\n> <br>`catalog_id`: The ID of the DLF catalog.<br>\n> `instance_id`: The instance ID, required if the data source is registered in instance mode.<br>\n> `encoded_jdbc_url`: The URL-encoded JDBC connection string. Required if the data source is registered in connection string mode.<br>\n> `catalog_name`: The name of the StarRocks catalog."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取数据地图数据目录详情",
    "en": "GetCatalog"
  },
  "descriptions": {
    "zh": "1. 需要购买DataWorks基础版及以上版本才能使用。",
    "en": "1. DataWorks Basic Edition or a higher edition is required."
  },
  "example": {
    "unifiedCli": "aliyun dataworks-public get-catalog --id dlf-catalog::catalog_id --api-version 2024-05-18 --region cn-hangzhou",
    "legacyCli": "aliyun dataworks-public GetCatalog --region cn-hangzhou --Id 'dlf-catalog::catalog_id'"
  }
}