{
  "name": "ListAgentSessions",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Id",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "请求方传入的 id，原样回显",
        "en": "The ID provided by the caller. The value is returned as-is."
      }
    },
    {
      "name": "Jsonrpc",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "JSON-RPC 版本，固定 2.0",
        "en": "The JSON-RPC version. The value must be `2.0`."
      }
    },
    {
      "name": "Params",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "业务参数",
        "en": "Business parameters."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询会话列表",
    "en": "ListAgentSessions"
  },
  "descriptions": {
    "zh": "## 请求说明\n- `agentName` 和 `sessionSourceList` 至少需要提供一个。\n- 支持通过 `tagList`, `sessionId`, `sessionTitle` 进行组合过滤。\n- 返回结果符合阿里云 OpenAPI 分页规范，包括 `totalCount`, `maxResults`, `nextToken` 以及 `sessionList`。\n- 如果 `nextToken` 为非法字符串，则默认处理为 1。\n- 默认每页返回 50 条记录，可以通过 `maxResults` 参数调整。",
    "en": "## Request\n\n- Specify at least one of `agentName` or `sessionSourceList`.\n\n- You can use the `tagList`, `sessionId`, and `sessionTitle` parameters for combined filtering.\n\n- The response follows the Alibaba Cloud OpenAPI pagination specification and includes the `totalCount`, `maxResults`, `nextToken`, and `sessionList` fields.\n\n- If you provide an invalid string for `nextToken`, its value defaults to `1`.\n\n- By default, this operation returns 50 records per page. You can use the `maxResults` parameter to adjust this number."
  },
  "example": {
    "unifiedCli": "aliyun dataworks-public list-agent-sessions --jsonrpc 2.0 --id 676303114031776 --params '{\"NextToken\":\"10\",\"SessionSourceList\":[\"pop-sdk\"],\"SessionTitle\":\"Sales_in_the_last_30_days\",\"MaxResults\":50,\"TagList\":[\"user_123456\"],\"AgentName\":\"chat_cli_chatbi\",\"SessionId\":\"sess_0f12abc34\"}' --api-version 2024-05-18 --region cn-hangzhou",
    "legacyCli": "aliyun dataworks-public ListAgentSessions --region cn-hangzhou --Jsonrpc '2.0' --Params '{\"NextToken\":\"10\",\"SessionSourceList\":[\"pop-sdk\"],\"SessionTitle\":\"Sales_in_the_last_30_days\",\"MaxResults\":50,\"TagList\":[\"user_123456\"],\"AgentName\":\"chat_cli_chatbi\",\"SessionId\":\"sess_0f12abc34\"}' --Id 676303114031776"
  }
}