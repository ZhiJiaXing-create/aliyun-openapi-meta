{
  "name": "ListSkills",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "本次请求返回的最大条数。",
        "en": "The maximum number of results to return per page."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "分页 Token。首次查询不传；后续查询传上一次返回的 `NextToken` 获取下一页。",
        "en": "The pagination token for the next page of results. Omit this for the first request. For subsequent requests, set this to the `NextToken` from the previous response."
      }
    },
    {
      "name": "Q",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "搜索关键词，按 Skill 名称模糊匹配。",
        "en": "The search keyword for a fuzzy match on Skill names."
      }
    },
    {
      "name": "Visibility",
      "position": "Body",
      "type": "Json",
      "required": false,
      "description": {
        "zh": "按可见性级别过滤，可传多个。",
        "en": "Filters the results by visibility level. You can specify multiple values."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "列出Skill",
    "en": "ListSkills"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n该API用于分页查询当前账号下的Skill列表，支持通过搜索关键词、可见性级别等条件进行过滤。\n\n- **Q**：可选参数，用于按Skill名称模糊匹配。\n- **Visibility**：可选参数，用于按可见性级别过滤，可以传递多个值。\n- **MaxResults**：可选参数，指定本次请求返回的最大条数。\n- **NextToken**：可选参数，用于分页查询时获取下一页的数据。首次查询时不传此参数；后续查询时传上一次返回的NextToken以获取下一页数据。",
    "en": "## Request\n\nThis operation lists the Skills in your account. You can filter the results by criteria such as a search keyword and visibility level.\n\n- **Q**: An optional search keyword for a fuzzy match on Skill names.\n\n- **Visibility**: An optional parameter to filter Skills by their visibility level. You can specify multiple values.\n\n- **MaxResults**: An optional parameter that specifies the maximum number of results to return per page.\n\n- **NextToken**: An optional pagination token for retrieving the next page of results. Omit this parameter for the first request. For subsequent requests, pass the `NextToken` value from the previous response to fetch the next page."
  },
  "example": {
    "unifiedCli": "aliyun dataworks-public list-skills --max-results 20 --next-token 5 --q analysis --visibility 123456 --api-version 2024-05-18 --region cn-hangzhou",
    "legacyCli": "aliyun dataworks-public ListSkills --region cn-hangzhou --Q analysis --NextToken 5 --MaxResults 20 --Visibility '[\"123456\"]'"
  }
}