{
  "name": "ListSecurityStrategies",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ControlModule",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "**管控模块**",
        "en": "**The control module.**"
      }
    },
    {
      "name": "ControlSubModule",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "**管控子模块**",
        "en": "**The control submodule.**"
      }
    },
    {
      "name": "PageNumber",
      "position": "Body",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页码，从1开始。默认值为1。",
        "en": "The page number. Starts from 1. Default: 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Body",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "页大小，默认20",
        "en": "The number of entries per page. Default: 20."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "分页检索安全策略",
    "en": "ListSecurityStrategies"
  },
  "descriptions": {
    "zh": "## 请求说明\n\n- 该 API 用于分页检索系统中已配置的安全策略。\n- 可通过 `ControlModule` 和 `ControlSubModule` 参数过滤特定模块或子模块下的策略。\n- 分页参数包括 `PageNum` 和 `PageSize`，分别表示页码和每页大小，默认值分别为 1 和 20。\n- 如果需要进一步的分页控制，可以使用 `MaxResults` 和 `NextToken` 参数（私有参数）。",
    "en": "## Request\n\n- This API retrieves a paginated list of configured security policies.\n\n- The `ControlModule` and `ControlSubModule` parameters filter policies by a specific module or submodule.\n\n- The `PageNum` and `PageSize` parameters control pagination. `PageNum` specifies the page number to retrieve (default: 1), and `PageSize` specifies the number of policies to return per page (default: 20).\n\n- Use the `MaxResults` and `NextToken` private parameters for advanced pagination."
  },
  "example": {
    "unifiedCli": "aliyun dataworks-public list-security-strategies --control-module DataQuery --control-sub-module MyCatalog --page-number 1 --page-size 10 --api-version 2024-05-18 --region cn-hangzhou",
    "legacyCli": "aliyun dataworks-public ListSecurityStrategies --region cn-hangzhou --ControlModule DataQuery --PageSize 10 --PageNumber 1 --ControlSubModule MyCatalog"
  }
}