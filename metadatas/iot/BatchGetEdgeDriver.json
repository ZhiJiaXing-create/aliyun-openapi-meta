{
  "name": "BatchGetEdgeDriver",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DriverIds",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "驱动ID列表。最多可填写30个驱动ID，即一次最多支持查询30个驱动的信息。格式请参见本文下方请求示例。",
        "en": "A list of driver IDs. You can specify up to 30 driver IDs, which means you can query information about up to 30 drivers in a single request. For the format, see the request sample below in this topic."
      }
    },
    {
      "name": "IotInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。公共实例不传此参数，企业版实例需传入。",
        "en": "The ID of the Internet of Things (IoT) service instance. This parameter is not required for public instances. However, this parameter is required for the instances that you have purchased."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量查询驱动信息",
    "en": "Batch query driver information"
  },
  "descriptions": {
    "zh": "## 限制条件\n单阿里云账号调用该接口的每秒请求数（QPS）最大限制为5。\n\n> RAM用户共享主账号配额。",
    "en": "## Condition  \nThe maximum queries per second (QPS) for a single Alibaba Cloud account when calling this API is 5.\n\n> Resource Access Management (RAM) users share the quota of the Alibaba Cloud account."
  },
  "example": {
    "unifiedCli": "aliyun iot batch-get-edge-driver --iot-instance-id ot_instc_pu0000_c0-v6400000000 --driver-ids example-string --api-version 2018-01-20 --region cn-hangzhou",
    "legacyCli": "aliyun iot BatchGetEdgeDriver --region cn-hangzhou --DriverIds.1 'example-string' --IotInstanceId 'ot_instc_pu0000_c0-v6400000000'"
  }
}