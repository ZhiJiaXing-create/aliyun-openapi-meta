{
  "name": "QueryEdgeInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CurrentPage",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "从返回结果中的第几页开始显示。最小取值是1。",
        "en": "The name of the edge instance."
      }
    },
    {
      "name": "IotInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。公共实例不传此参数，企业版实例需传入。",
        "en": "The number of entries to return on each page. Maximum value: 30. Default value: 10. If you specify a value smaller than 1, the system uses 10."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "边缘实例名称。",
        "en": "The error code. Success indicates that the call was successful. Other values indicate that specific errors occurred. For more information, see [Error codes](~~135200~~)."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "返回结果中每页显示的记录数量。最大取值30，最小取值1，默认取值是10。",
        "en": "The number of the page from which the results are displayed. The minimum value is 1. If you specify a value smaller than 1, the system uses 1."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询当前账号下的所有边缘实例",
    "en": "Query all edge instances under the current account"
  },
  "descriptions": {
    "zh": "## 限制条件\n单阿里云账号调用该接口的每秒请求数（QPS）最大限制为10。\n> RAM用户共享主账号配额。",
    "en": "## Condition\nEach Alibaba Cloud account can send a maximum of 10 queries per second (QPS) to call this API.\n> Resource Access Management (RAM) users share the quota of the Alibaba Cloud account."
  },
  "example": {
    "unifiedCli": "aliyun iot query-edge-instance --iot-instance-id iot_instc_pu0000_c0-v6400000000 --page-size 15 --current-page 1 --name '测试实例_test' --api-version 2018-01-20 --region cn-hangzhou",
    "legacyCli": "aliyun iot QueryEdgeInstance --region cn-hangzhou --IotInstanceId 'iot_instc_pu0000_c0-v6400000000' --PageSize 15 --CurrentPage 1 --Name '测试实例_test'"
  }
}