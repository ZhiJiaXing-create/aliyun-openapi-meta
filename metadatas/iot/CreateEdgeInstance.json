{
  "name": "CreateEdgeInstance",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "IotInstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例ID。公共实例不传此参数，企业版实例需传入。",
        "en": "The ID of the Internet of Things (IoT) service instance. This parameter is not required for public instances. However, this parameter is required for the instances that you have purchased."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "边缘实例名称。\n支持中文汉字、英文大小写、数字、下划线（_）和短划线（-），不超过20个字符（一个中文汉字算2个字符）。",
        "en": "The name of the edge instance.\n\nAn instance name cannot exceed 20 characters in length and can contain uppercase letters, lowercase letters, digits, underscores (\\_), and hyphens (-)."
      }
    },
    {
      "name": "Spec",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "产品规格。\n- 10：轻量版。\n- 20：标准版。\n- 30：专业版。\n\n默认值为20（标准版）。",
        "en": "The specification of the edge instance.\n\n*   10: Lite Edition.\n*   20: Standard Edition.\n*   30: Pro Edition.\n\nDefault value: 20."
      }
    },
    {
      "name": "Tags",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "边缘实例标签。每个标签由`key:value`组成，多个标签间以英文逗号隔开。如`k1:v1,k2:v2`。\n- 标签key限制如下：    \n    - 不可为空。\n    - 在该边缘实例中唯一。\n    - 仅支持英文大小写。\n    - 不可超过20个字符。\n- 标签value限制如下：\n    - 不可为空。\n    - 支持中文、英文大小写、数字、下划线（_）和短划线（-）。\n    - 不可超过20个字符（一个中文汉字算2个字符）。",
        "en": "The tags of the edge instance. Syntax of a tag: `key:value`. Multiple tags are separated with commas (,), for example, `k1:v1,k2:v2`.\n\n*   Note the following limits on tag keys:\n\n    *   Tag values cannot be left empty.\n    *   Tag keys must be unique in the edge instance.\n    *   Tag keys only support letters.\n    *   Each tag key cannot exceed 20 characters in length.\n\n*   Note the following limits on tag values:\n\n    *   Tag values cannot be left empty.\n    *   A tag value can contain uppercase letters, lowercase letters, digits, underscores (\\_), and hyphens (-).\n    *   Each tag value cannot exceed 20 characters in length."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建边缘实例",
    "en": "CreateEdgeInstance"
  },
  "descriptions": {
    "zh": "## 限制条件\n单阿里云账号调用该接口的每秒请求数（QPS）最大限制为10。\n> RAM用户共享主账号配额。",
    "en": "## Limits\n\nA single Alibaba Cloud account can run a maximum of 10 queries per second (QPS).\n\n>  RAM users share the quota of the Alibaba Cloud account."
  },
  "example": {
    "unifiedCli": "aliyun iot create-edge-instance --iot-instance-id iot_instc_pu0000_c0-v6400000000 --name LinkIoTEdge_Node --tags k1:v1,k2:v2 --spec 20 --api-version 2018-01-20 --region cn-hangzhou",
    "legacyCli": "aliyun iot CreateEdgeInstance --region cn-hangzhou --IotInstanceId 'iot_instc_pu0000_c0-v6400000000' --Spec 20 --Tags 'k1:v1,k2:v2' --Name LinkIoTEdge_Node"
  }
}