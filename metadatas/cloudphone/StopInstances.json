{
  "name": "StopInstances",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Force",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否强制关机。取值范围：\n\n- true：强制关机。相当于典型的断电操作，所有未写入存储设备的缓存数据会丢失。\n- false：正常关机。\n\n默认值：false",
        "en": "Specifies whether to forcefully stop the instances. Valid values:\n\n- true: Forcefully stops the instances. This is equivalent to a typical power-off operation. All cached data that has not been written to storage devices is lost.\n- false: Normally stops the instances.\n\nDefault value: false."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "实例ID列表，N的取值范围1-100。",
        "en": "The list of instance IDs. Valid values of N: 1 to 100."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云手机实例所属地域",
        "en": "The region to which the cloud phone instances belong."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量停止实例",
    "en": "Stop instances in a batch"
  }
}