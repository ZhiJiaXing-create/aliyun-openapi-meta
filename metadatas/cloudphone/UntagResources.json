{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否全部删除，只针对TagKey.N为空时有效。 取值范围： true  false True False  默认是 false",
        "en": "Specifies whether to delete all tags. This parameter takes effect only when TagKey.N is empty. Valid values: true, false, True, and False. Default value: false."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域",
        "en": "The region ID."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID，最多50个子项",
        "en": "The resource ID. You can specify up to 50 subkeys."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。取值范围：\n- instance：弹性云手机实例",
        "en": "The resource type. Valid values:\n- instance: elastic cloud phone instance."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签键，最多20个子项",
        "en": "The tag key. You can specify up to 20 subkeys."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为云资源统一解绑标签",
    "en": "Unbind tags from cloud resources"
  }
}