{
  "name": "ListImages",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ImageCategory",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "镜像来源。取值范围：\n- system：阿里云提供的公共镜像。\n- self：您创建的自定义镜像。\n- others：其他阿里云用户共享给您的镜像。",
        "en": "The source of the image. Valid values:\n- system: a public image provided by Alibaba Cloud.\n- self: a custom image that you created.\n- others: an image shared to you by another Alibaba Cloud user."
      }
    },
    {
      "name": "ImageId",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "镜像ID，N的取值范围：1~100",
        "en": "The image IDs. Valid values of N: 1 to 100."
      }
    },
    {
      "name": "ImageName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "镜像名称。长度为2~128个英文或中文字符。必须以大小字母或中文开头，不能以`http://`和`https://`开头。可以包含数字、半角冒号（:）、下划线（_）或者短划线（-）。",
        "en": "The image name. The name must be 2 to 128 characters in length. It must start with a letter or a Chinese character and cannot start with `http://` or `https://`. It can contain digits, colons (:), underscores (_), or hyphens (-)."
      }
    },
    {
      "name": "InstanceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "实例规格。",
        "en": "The instance type."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "返回的最大数，取值范围：1~100。",
        "en": "The maximum number of entries to return. Valid values: 1 to 100."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询凭证（Token），取值为上一次API调用返回的NextToken参数值，初次调用无需配置。",
        "en": "The pagination token. Set the value to the NextToken value returned in the previous API call. You do not need to set this parameter for the first request."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "Status",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "查询某种状态下的镜像。取值范围：\n- Waiting：多任务排队中。\n- Creating：镜像正在创建中。\n- Copying：镜像正在复制中。\n- Importing：镜像正在导入中。\n- Available（默认）：您可以使用的镜像。\n- CreateFailed：创建失败的镜像。",
        "en": "The status of the image to query. Valid values:\n- Waiting: The image is waiting in the task queue.\n- Creating: The image is being created.\n- Copying: The image is being copied.\n- Importing: The image is being imported.\n- Available (default): The image is available for use.\n- CreateFailed: The image failed to be created."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询镜像资源",
    "en": "ListImages"
  }
}