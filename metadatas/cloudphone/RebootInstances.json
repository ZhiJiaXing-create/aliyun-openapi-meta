{
  "name": "RebootInstances",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Force",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否强制重启。取值范围：\n\n- true：强制重启。相当于典型的断电操作，所有未写入存储设备的缓存数据会丢失。\n- false：正常重启。\n\n默认值：false",
        "en": "Specifies whether to forcibly restart the instance. Valid values:\n\n- true: forcibly restarts the instance. This is equivalent to a typical power-off operation. All cached data that has not been written to the storage device is lost.\n- false: normally restarts the instance.\n\nDefault value: false."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "云手机实例ID。",
        "en": "The instance ID of the cloud phone."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云手机实例所属地域。",
        "en": "The region to which the cloud phone instance belongs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "重启实例",
    "en": "Restart an instance"
  }
}