{
  "name": "InstallApplication",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "待执行命令的实例ID。N的范围：1~10。",
        "en": "The instance ID of the instance on which to execute the command. Valid values of N: 1 to 10."
      }
    },
    {
      "name": "OssBucket",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用文件所在的OSS Bucket。\n\n> 首次导入应用文件到该OSSBucket前，请添加RAM授权策略，否则会报错NoSetRoletoECSServiceAcount。",
        "en": "The OSS bucket where the application file is stored.\n\n> Before importing an application file to the OSS bucket for the first time, add a RAM authorization policy. Otherwise, the NoSetRoletoECSServiceAcount error is returned."
      }
    },
    {
      "name": "OssObject",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用文件所在的OSS Object的文件名称（key）。",
        "en": "The name (key) of the OSS object where the application file is stored."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "安装应用到云手机",
    "en": "Install an application on a cloud phone"
  }
}