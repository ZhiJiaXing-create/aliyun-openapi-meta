{
  "name": "ImportKeyPair",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "KeyPairName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "密钥对名称。必须保持名称唯一性。 长度为2~128个英文或中文字符。必须以大小字母或中文开头，不能以 http:// 和 https:// 开头。可以包含数字、半角冒号（:）、下划线（_）或者连字符（-）。",
        "en": "The name of the key pair. The name must be unique. The name must be 2 to 128 characters in length and can contain letters, digits, colons (:), underscores (_), and hyphens (-). It must start with a letter. It cannot start with http:// or https://."
      }
    },
    {
      "name": "PublicKeyBody",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "密钥对的公钥内容。",
        "en": "The public key content of the key pair."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "导入云手机密钥对",
    "en": "Import a cloud phone key pair"
  },
  "descriptions": {
    "zh": "接口说明\n调用该接口时，您需要注意：\n\n- 您在每个地域的密钥对数最高为500对。\n- 导入的公钥对需要为ADB生成密钥对的公钥",
    "en": "Operation description\nWhen you call this operation, take note of the following items:\n\n- You can have a maximum of 500 key pairs in each region.\n- The imported public key must be the public key of a key pair generated by ADB."
  }
}