{
  "name": "RenewInstances",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoPay",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "自动支付，默认true",
        "en": "Specifies whether to enable automatic payment. Default value: true."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。更多详情，请参见如何保证幂等性。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. The value of ClientToken can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see How to ensure idempotence."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "续费实例列表。列表最大 20 个元素",
        "en": "The list of instance IDs to renew. The list can contain a maximum of 20 elements."
      }
    },
    {
      "name": "Period",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "周期，默认值1",
        "en": "The renewal period. Default value: 1."
      }
    },
    {
      "name": "PeriodUnit",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "续费周期单位，支持\n- Year 包年包月，支持年：1, 2, 3, 4, 5\n- Month 包年包月，支持月：1, 2, 3, 6\n- Hour 按量，支持小时：1\n\n默认Month",
        "en": "The unit of the renewal period. Valid values:\n- Year: subscription. Valid values for the period: 1, 2, 3, 4, and 5.\n- Month: subscription. Valid values for the period: 1, 2, 3, and 6.\n- Hour: pay-as-you-go. Valid value for the period: 1.\n\nDefault value: Month."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量续费云手机实例",
    "en": "Batch renew Cloud phone instances"
  },
  "descriptions": {
    "zh": "- 批量续费接口",
    "en": "- Batch renewal operation."
  }
}