{
  "name": "CopyImage",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。更多详情，请参见如何保证幂等性。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but make sure that the token is unique among different requests. The value of ClientToken can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see How to ensure idempotence."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "镜像的描述信息。长度为2~256个英文或中文字符，不能以http://和https://开头。",
        "en": "The description of the image. The description must be 2 to 256 characters in length and cannot start with http:// or https://."
      }
    },
    {
      "name": "DestinationRegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "拷贝至目标地域",
        "en": "The destination region to which the image is copied."
      }
    },
    {
      "name": "ImageId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "待拷贝的自定义镜像ID",
        "en": "The ID of the custom image to be copied."
      }
    },
    {
      "name": "ImageName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "镜像名称。长度为2~128个英文或中文字符。必须以大小字母或中文开头，不能以http://和https://开头。可以包含数字、半角冒号（:）、下划线（_）或者连字符（-）。",
        "en": "The name of the image. The name must be 2 to 128 characters in length and can contain letters, digits, colons (:), underscores (_), and hyphens (-). It must start with a letter or a Chinese character and cannot start with http:// or https://."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "发起拷贝的源地域",
        "en": "The source region from which the image is copied."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "复制自定义镜像",
    "en": "Copy a custom image"
  },
  "descriptions": {
    "zh": "调用该接口时，您需要注意：\n- 自定义镜像的状态必须为可用（Available）。\n- 源自定义镜像必须为您账号下的镜像，不能跨账号复制。\n- 同一个地域下同时只能有一个镜像拷贝任务运行，其余任务需要排队等待上一个任务完成后再依次排队执行。",
    "en": "When you call this operation, take note of the following items:\n- The custom image must be in the Available state.\n- The source custom image must belong to your account. Cross-account image copying is not supported.\n- Only one image copy task can run at a time in a region. Other tasks are queued and executed sequentially after the previous task is completed."
  }
}