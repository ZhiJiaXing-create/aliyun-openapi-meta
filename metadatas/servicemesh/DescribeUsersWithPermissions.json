{
  "name": "DescribeUsersWithPermissions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "UserType",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "选择列出RAM用户还是RAM角色的ID，取值：\n\n- `SubUser`：列出RAM用户ID\n\n- `RamRole`：列出RAM角色ID",
        "en": "Specifies whether to list the IDs of RAM users or RAM roles. Valid values:\n\n- `SubUser`: Lists the IDs of RAM users.\n\n- `RamRole`: Lists the IDs of RAM roles."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取RBAC权限的RAM用户",
    "en": "DescribeUsersWithPermissions"
  },
  "example": {
    "unifiedCli": "aliyun servicemesh describe-users-with-permissions --user-type SubUser --region cn-hangzhou",
    "legacyCli": "aliyun servicemesh DescribeUsersWithPermissions --region cn-hangzhou --UserType SubUser"
  }
}