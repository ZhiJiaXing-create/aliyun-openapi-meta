{
  "name": "OnsTopicUpdate",
  "deprecated": true,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "需配置的Topic所对应的实例ID。",
        "en": "The ID of the instance to which the topic that you want to configure belongs."
      }
    },
    {
      "name": "Perm",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "设置该Topic的读写模式。取值说明如下：\n\n- **6**：同时支持读写\n- **4**：禁写\n- **2**：禁读",
        "en": "The read and write mode of the topic. Valid values:\n\n- **6**: Both read and write operations are supported.\n- **4**: Write operations are prohibited.\n- **2**: Read operations are prohibited."
      }
    },
    {
      "name": "Topic",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需配置的Topic名称。",
        "en": "The name of the topic that you want to configure."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "配置Topic的读写模式",
    "en": "Configure the read and write mode of a topic"
  },
  "descriptions": {
    "zh": "><notice>阿里云提供的OpenAPI属于管控类API，用于管理和查询阿里云服务的相关资源。仅推荐在管控链路集成，消息收发的核心数据链路中严禁依赖OpenAPI实现，否则可能会导致链路产生风险。></notice>本接口一般用于禁读或禁写的场景。",
    "en": "><notice>The OpenAPI provided by Alibaba Cloud is a management API used to manage and query resources of Alibaba Cloud services. It is only recommended for integration in management scenarios. Do not rely on OpenAPI in the core data path of message sending and receiving. Otherwise, risks may be introduced to the link.></notice>This operation is generally used in scenarios where read or write operations are prohibited."
  },
  "example": {
    "unifiedCli": "aliyun ons ons-topic-update --topic test --perm 6 --instance-id MQ_INST_111111111111_DOxxxxxx --region cn-hangzhou",
    "legacyCli": "aliyun ons OnsTopicUpdate --region cn-hangzhou --Perm 6 --InstanceId MQ_INST_111111111111_DOxxxxxx --Topic test"
  }
}