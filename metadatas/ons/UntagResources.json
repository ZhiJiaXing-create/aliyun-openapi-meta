{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否删除资源绑定的全部标签。当请求中未设置**TagKey**时该参数有效。默认值**false**。",
        "en": "Specifies whether to delete all tags that are attached to the resource. This parameter is valid only when the **TagKey** parameter is not set in the request. The default value is **false**."
      }
    },
    {
      "name": "InstanceId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "如需解除Topic和Group ID与标签的绑定关系，该参数为必填。",
        "en": "This parameter is required to detach tags from topics and group IDs."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID列表。",
        "en": "A list of resource IDs."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。枚举类型，目前支持的资源类型：\n\n- **INSTANCE**\n- **TOPIC**\n- **GROUP**",
        "en": "The type of the resource. This is an enumeration. Supported resource types:\n\n- **INSTANCE**\n\n- **TOPIC**\n\n- **GROUP**"
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源的标签键列表。",
        "en": "A list of tag keys of the resource."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为资源解绑并删除标签",
    "en": "UntagResources"
  },
  "descriptions": {
    "zh": "><notice>阿里云提供的OpenAPI属于管控类API，用于管理和查询阿里云服务的相关资源。仅推荐在管控链路集成，消息收发的核心数据链路中严禁依赖OpenAPI实现，否则可能会导致链路产生风险。></notice>",
    "en": "><notice>\n\nAlibaba Cloud OpenAPI is a control-plane API. You can use it to manage and query Alibaba Cloud service resources. Integrate OpenAPI on the control plane only. Do not use OpenAPI for core data links that send and receive messages because this can introduce threats to your data links.\n\n></notice>"
  },
  "example": {
    "unifiedCli": "aliyun ons untag-resources --instance-id MQ_INST_1880770869020000_BX4jvZZG --resource-type TOPIC --all false --resource-id TopicA --tag-key CartService --region cn-hangzhou",
    "legacyCli": "aliyun ons UntagResources --region cn-hangzhou --All false --ResourceId.1 TopicA --InstanceId MQ_INST_1880770869020000_BX4jvZZG --TagKey.1 CartService --ResourceType TOPIC"
  }
}