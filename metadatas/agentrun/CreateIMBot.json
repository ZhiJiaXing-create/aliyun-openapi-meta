{
  "name": "CreateIMBot",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "/2025-09-10/agents/im-bots",
  "parameters": [
    {
      "name": "body",
      "position": "Body",
      "type": "Json",
      "required": true,
      "description": {
        "en": "The request body."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "en": "CreateIMBot"
  },
  "descriptions": {
    "en": "A successful request returns an HTTP 201 status code. Once created, an IM Bot's status is always `running`. The response is in a standard format, and its `data` field contains an `IMBotInfo` object."
  }
}