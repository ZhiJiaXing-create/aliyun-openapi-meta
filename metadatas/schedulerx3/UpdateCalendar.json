{
  "name": "UpdateCalendar",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CalendarName",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "日历名称",
        "en": "The calendar name."
      }
    },
    {
      "name": "ClientToken",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大不超过64个ASCII字符。",
        "en": "A client-generated token that ensures request idempotence. This token must be unique for each request and be no more than 64 ASCII characters long."
      }
    },
    {
      "name": "ClusterId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID",
        "en": "The cluster ID."
      }
    },
    {
      "name": "Incremental",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否是增量更新（默认：false）。\n- false  更新指定月份数据的同时会删除其他月份的配置\n- true  仅更新指定月份的数据，保留其他月份的现有配置",
        "en": "Specifies whether to perform an incremental update. Default value: `false`.\n\n- `false`: Replaces all existing calendar data with the data in this request, deleting configurations for any unspecified months.\n\n- `true`: Updates only the data for the specified months, preserving existing configurations for all other months."
      }
    },
    {
      "name": "Months",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "月。",
        "en": "A JSON string that defines the days for one or more months."
      }
    },
    {
      "name": "Year",
      "position": "Body",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "年。",
        "en": "The year."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新日历",
    "en": "Update calendar"
  }
}