{
  "name": "UpdateWorkflow",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AppName",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "应用名称。",
        "en": "The application name."
      }
    },
    {
      "name": "Calendar",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自定义日历",
        "en": "The name of a custom calendar to exclude specific dates from the schedule."
      }
    },
    {
      "name": "ClientToken",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于保证请求的幂等性。由客户端生成该参数值，要保证在不同请求间唯一，最大不超过64个ASCII字符。",
        "en": "A unique token that you provide to ensure the request is idempotent. The token can be up to 64 ASCII characters long."
      }
    },
    {
      "name": "ClusterId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID",
        "en": "The cluster ID."
      }
    },
    {
      "name": "Description",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "工作流描述",
        "en": "The workflow description."
      }
    },
    {
      "name": "MaxConcurrency",
      "position": "Body",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "最大并发数\n>同一个工作流同一时间允许运行的最大实例个数，1表示不允许重复执行。如果超过并发数，会跳过当前调度。",
        "en": "The maximum concurrency for the workflow.\n\n> The maximum number of concurrent instances that can run for the same workflow. A value of `1` prevents overlapping runs. If the number of running instances reaches this limit, subsequent scheduled runs are skipped."
      }
    },
    {
      "name": "Name",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "工作流名称",
        "en": "The workflow name."
      }
    },
    {
      "name": "TimeExpression",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "时间表达式，根据选择的时间类型设置时间表达式。\n- none：无需填写\n- cron：填写标准的cron表达式，支持在线验证。\n- api：无需填写",
        "en": "The cron expression for the schedule. This parameter is required only when `TimeType` is set to `1` (cron).\n\n- If `TimeType` is `-1` (none), this parameter is not required.\n\n- If `TimeType` is `1` (cron), specify a standard cron expression.\n\n- If `TimeType` is `100` (api), this parameter is not required."
      }
    },
    {
      "name": "TimeType",
      "position": "Body",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "调度类型，目前支持以下几种类型：\n- -1: none\n- 1：cron\n- 100:：api",
        "en": "The scheduling type. Valid values:\n\n- `-1` (none): The workflow is not scheduled and must be triggered on demand.\n\n- `1` (cron): The workflow runs based on the cron expression in the `TimeExpression` parameter.\n\n- `100` (api): The workflow is triggered by an API call."
      }
    },
    {
      "name": "Timezone",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "时区\n> 默认使用schedulerx服务器所在时区",
        "en": "The time zone for the schedule.\n\n> Defaults to the time zone of the SchedulerX server."
      }
    },
    {
      "name": "WorkflowId",
      "position": "Body",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "工作流ID",
        "en": "The workflow ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "更新工作流",
    "en": "Update pipeline"
  }
}