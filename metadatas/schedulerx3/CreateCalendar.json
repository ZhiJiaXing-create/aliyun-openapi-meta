{
  "name": "CreateCalendar",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CalendarName",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "日历名称",
        "en": "The name of the calendar."
      }
    },
    {
      "name": "ClientToken",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。说明 若您未指定，则系统自动使用API请求的RequestId作为ClientToken标识。每次API请求的RequestId可能不一样。",
        "en": "A client token to ensure request idempotence. Generate a unique value for this parameter on your client. The token can contain only ASCII characters. Note: If you do not specify this parameter, the system automatically uses the Request ID as the client token. The Request ID may be different for each request."
      }
    },
    {
      "name": "ClusterId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。您可以调用[ListClusters](~~28147~~)查看集群的ID。",
        "en": "The Cluster ID. You can call the [ListClusters](~~28147~~) operation to query Cluster IDs."
      }
    },
    {
      "name": "Months",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "每个月的日历，JSON数组格式",
        "en": "The days of each month, specified in a JSON array."
      }
    },
    {
      "name": "Year",
      "position": "Body",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "年。",
        "en": "The year."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建日历",
    "en": "Create Calendar"
  },
  "descriptions": {
    "zh": "创建自定义日历",
    "en": "Creates a custom calendar for SchedulerX."
  }
}