{
  "name": "ImportJobs",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoCreateApp",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "自动创建应用。",
        "en": "Determines whether to automatically create the Application if it does not exist."
      }
    },
    {
      "name": "ClusterId",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    },
    {
      "name": "Content",
      "position": "Body",
      "type": "String",
      "required": false,
      "description": {
        "zh": "导入内容。",
        "en": "The jobs to import, formatted as a JSON string."
      }
    },
    {
      "name": "Overwrite",
      "position": "Body",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "如果已存在，是否覆盖。默认值为 true。\n- true：覆盖。\n- false：不覆盖。",
        "en": "Determines whether to overwrite jobs if they already exist. The default is **true**.\n\n- **true**: Overwrites existing jobs.\n\n- **false**: Does not overwrite existing jobs."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量导入任务",
    "en": "Batch import job"
  }
}