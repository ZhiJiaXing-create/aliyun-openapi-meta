{
  "name": "GetStsInfoAndOssPath",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符，且不能超过64个字符。",
        "en": "Ensures request idempotence.\n\nYou can generate a parameter value from your client to guarantee that this value is unique across different requests. ClientToken supports only ASCII characters and must not exceed 64 characters."
      }
    },
    {
      "name": "ConnectionPoolId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "连接地址池的ID。",
        "en": "ID of the address pool."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n- **true**：发送检查请求，不会获取信息。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后直接获取信息。",
        "en": "Specifies whether to perform a dry run of the request. Valid values:\n- **true**: Sends a dry run request without retrieving information. The system checks whether required parameters are provided, whether the request format is valid, and whether business limits are met. If the check fails, an error is returned. If the check passes, the error code `DryRunOperation` is returned.\n- **false** (default): Sends a normal request and retrieves information if all checks pass."
      }
    },
    {
      "name": "FileName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模板文件名称。\n\n模板文件仅支持CSV格式。",
        "en": "Template file name.\n\nThe template file supports only the CSV format."
      }
    },
    {
      "name": "IoTCloudConnectorId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云连接器实例ID。",
        "en": "The instance ID of the cloud connector."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云连接器实例所在的地域ID，仅取值：**cn-hangzhou**。",
        "en": "The region ID of the cloud connector instance. Valid value: **cn-hangzhou** only."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取服务账号信息和提交地址",
    "en": "Obtain service account information and submission address"
  }
}