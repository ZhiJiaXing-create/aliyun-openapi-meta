{
  "name": "CreateAuthorizationRules",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AuthorizationRules",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "授权规则列表。",
        "en": "List of authorization rules."
      },
      "sub_parameters": [
        {
          "name": "Description",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "授权规则描述。最多支持20个授权规则描述。\n\n长度为2~256个字符，开头必须为大小写英文字母或中文，但不能为`http://`或`https://`。",
            "en": "Description of the authorization rule. A maximum of 20 descriptions are supported.\n\nThe description must be 2 to 256 characters in length, start with an uppercase or lowercase English letter or a Chinese character, and cannot start with `http://` or `https://`."
          }
        },
        {
          "name": "Destination",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "访问目标地址。最多支持20个访问目标地址。",
            "en": "Destination address for access. A maximum of 20 destination addresses are supported."
          }
        },
        {
          "name": "DestinationPort",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "目标端口范围。最多支持20个访问目标端口。取值范围：\n\n- TCP、UDP协议：**1~65535**。使用正斜线（/）隔开起始端口和终止端口。例如：1/200。\n\n- ICMP协议：-1/-1。\n\n- ALL：-1/-1。",
            "en": "Target port range. You can specify up to 20 destination ports for access. Valid values:  \n\n- For TCP and UDP protocols: **1–65535**. Separate the start and end ports with a forward slash (/). Example: 1/200.  \n\n- For ICMP protocol: -1/-1.  \n\n- For ALL: -1/-1."
          }
        },
        {
          "name": "DestinationType",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "访问目标类型。最多支持20个访问目标类型。取值：\n\n- **Cidr**：CIDR地址块。\n- **Domain**：域名。",
            "en": "Type of access destination. A maximum of 20 destination types are supported. Valid values:\n\n- **Cidr**: CIDR block.\n- **Domain**: Domain name."
          }
        },
        {
          "name": "Name",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "授权规则名称。最多支持20个授权规则名称。\n\n名称长度为2~128个字符，以大小写字母或中文开头，可包含数字、半角句号（.）、下划线（_）或短划线（-）。",
            "en": "Name of the authorization rule. You can specify up to 20 authorization rule names.  \n\nThe name must be 2–128 characters in length, and start with a letter (uppercase or lowercase) or a Chinese character. It can contain digits, periods (.), underscores (_), or hyphens (-)."
          }
        },
        {
          "name": "Policy",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "访问策略。最多支持20个访问策略。取值：\n\n- **Permit**：允许。\n- **Deny**：拒绝。",
            "en": "Access policy. A maximum of 20 access policies are supported. Valid values:\n\n- **Permit**: Allow.\n- **Deny**: Deny."
          }
        },
        {
          "name": "Protocol",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "协议类型。最多支持20个协议类型。取值：\n\n- **TCP**。\n\n- **UDP**。\n\n- **ICMP**。\n\n- **ALL**：支持所有协议。",
            "en": "Protocol type. A maximum of 20 protocol types are supported. Valid values:\n\n- **TCP**.\n\n- **UDP**.\n\n- **ICMP**.\n\n- **ALL**: All protocols are supported."
          }
        },
        {
          "name": "SourceCidr",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "源CIDR地址块。最多支持20个CIDR地址块。",
            "en": "Source CIDR block. A maximum of 20 CIDR blocks are supported."
          }
        }
      ]
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n从您的客户端生成一个参数值，确保不同请求间该参数值唯一。ClientToken只支持ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "Client token used to ensure idempotence of the request.  \n\nGenerate a unique value from your client for this parameter to guarantee uniqueness across different requests. ClientToken supports only ASCII characters.  \n\n> If you do not specify this parameter, the system automatically uses the **RequestId** of the API request as the **ClientToken**. The **RequestId** may differ for each API request."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n- **true**：发送检查请求，不会创建多条授权规则。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行操作。",
        "en": "Indicates whether to perform only a dry run of the request. Valid values:  \n- **true**: Sends a dry run request without creating multiple authorization rules. The system checks whether required parameters are specified, whether the request format is valid, and whether business limits are exceeded. If the check fails, an error is returned. If the check passes, the error code `DryRunOperation` is returned.  \n- **false** (default): Sends a normal request. If the check passes, an HTTP 2xx status code is returned and the operation is performed immediately."
      }
    },
    {
      "name": "IoTCloudConnectorId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云连接器实例ID。",
        "en": "Instance ID of the cloud connector."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "实例所在的地域ID。\n\n您可以通过调用[ListRegions](~~313836~~)接口获取地域ID。",
        "en": "Region ID of the instance.  \n\nYou can call the [ListRegions](~~313836~~) API to obtain the region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建多条授权规则",
    "en": "Create Multiple Authorization Rules"
  }
}