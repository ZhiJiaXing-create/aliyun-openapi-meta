{
  "name": "ListConnectionPoolIps",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConnectionPoolId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "连接地址池的ID。",
        "en": "The ID of the connection address pool."
      }
    },
    {
      "name": "IoTCloudConnectorId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "云连接器实例ID。",
        "en": "The instance ID of the cloud connector."
      }
    },
    {
      "name": "Ip",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "连接地址池中的IP地址。",
        "en": "IP address in the connection address pool."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小，取值：**1**~**2000**。传入为空时，默认值为**10**。",
        "en": "Paging size. Valid values: **1** to **2000**. If left empty, the default value is **10**."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用来标记当前开始读取的位置，置空表示从头开始。\n\n> 第一次调用不用填写，会在返回结果中包含第二次调用时用的NextToken，之后每次调用返回结果中都有下一次调用时的NextToken。",
        "en": "A token used to mark the starting position for the current read operation. An empty value indicates reading from the beginning.\n\n> Do not provide this parameter in the first call. The response will include a NextToken for the second call. Each subsequent response will contain the NextToken for the next call."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云连接器实例所在的地域ID，仅取值：**cn-hangzhou**。",
        "en": "The Region ID where the cloud connector instance is located. Valid value: **cn-hangzhou** only."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询连接地址池中的IP地址状态",
    "en": "Query the IP address status in the connection address pool"
  }
}