{
  "name": "ListGroupDNSServiceRules",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "DNSServiceRuleIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "DNS授权规则ID。",
        "en": "The ID of the DNS authorization rule."
      }
    },
    {
      "name": "DNSServiceRuleName",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "DNS授权规则名称。最多支持输入20个规则名称。",
        "en": "The names of DNS authorization rules. You can specify up to 20 rule names."
      }
    },
    {
      "name": "DNSServiceRuleStatus",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "DNS授权规则的状态。",
        "en": "The status of the DNS authorization rule."
      }
    },
    {
      "name": "Destination",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "目标DNS授权规则IP地址。",
        "en": "The destination IP addresses of the DNS authorization rule."
      }
    },
    {
      "name": "IoTCloudConnectorGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云连接器实例组ID。",
        "en": "The ID of the Cloud Connector instance group."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分批次查询时每次显示的条目数。取值：**1~50**。默认值：**10**。",
        "en": "The number of entries to return on each page. Valid values: **1–50**. Default value: **10**."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否拥有下一次查询的令牌（Token）。取值：\n- 第一次查询和没有下一次查询时，均无需填写。\n- 如果有下一次查询，取值为上一次API调用返回的NextToken值。",
        "en": "The token used to retrieve the next page of results. Valid values:\n\n- Leave this parameter empty for the first query or if no further queries are needed.\n\n- If more results are available, set this parameter to the NextToken value returned in the previous API call."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云连接器实例组所在的地域ID。\n\n您可以通过调用[ListRegions](~~313836~~)接口获取地域ID。",
        "en": "The region ID of the Cloud Connector instance group.\n\nYou can call the [ListRegions](~~313836~~) operation to obtain the region ID."
      }
    },
    {
      "name": "ServiceType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "DNS授权规则的目标类型。取值：\n\n- **Normal**：表示目标类型为DNS普通类型。\n\n- **DNSRedirect**：表示目标类型为DNS重定向类型。",
        "en": "The target type of the DNS authorization rule. Valid values:\n\n- **Normal**: The target type is a standard DNS type.\n\n- **DNSRedirect**: The target type is a DNS redirection type."
      }
    },
    {
      "name": "Source",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "源DNS授权规则IP地址。",
        "en": "The source IP addresses of the DNS authorization rule."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询实例组的DNS授权规则",
    "en": "Query DNS authorization rules of an instance group"
  }
}