{
  "name": "DeleteService",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "The client token. This token ensures request idempotence. Generate this value on the client side. Ensure it is unique across different requests. The maximum length is 64 ASCII characters.\n\nThe client generates this parameter value, which must be unique across requests and have a maximum length of 64 ASCII characters.\n\n> If not specified, the system automatically uses the API request's **RequestId** as the **ClientToken** identifier. The **RequestId** for each API request may differ."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求，取值：\n- **true**：发送检查请求，不会删除服务。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接删除服务。",
        "en": "Whether to only dry run this request. Valid values:\n\n- **true**: Sends a check request. This does not delete the service. The check includes required parameters, request format, and business constraints. If the check fails, an error is returned. If the check passes, the error code `DryRunOperation` is returned.\n\n- **false** (default value): Sends a normal request. If the check passes, it returns an HTTP 2xx status code and deletes the service."
      }
    },
    {
      "name": "IoTCloudConnectorId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云连接器实例ID。",
        "en": "The cloud connector instance ID."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "分应用统计服务所在的地域ID。\n\n您可以通过调用[ListRegions](~~313836~~)接口获取地域ID。",
        "en": "The region ID where the application-specific statistics service is located.\n\nObtain the region ID by calling the [ListRegions](~~313836~~) API."
      }
    },
    {
      "name": "ServiceId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "系统为分应用统计服务生成的ID，全网唯一。",
        "en": "The system-generated ID for the application-specific statistics service. This ID is unique across the network."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除分应用统计服务",
    "en": "Delete the per-application statistics service"
  }
}