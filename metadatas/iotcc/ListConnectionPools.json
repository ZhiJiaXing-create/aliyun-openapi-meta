{
  "name": "ListConnectionPools",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ConnectionPoolIds",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "连接地址池的ID列表。最多支持传入20个连接地址池ID。",
        "en": "A list of connection address pool IDs. You can specify up to 20 connection address pool IDs."
      }
    },
    {
      "name": "ConnectionPoolName",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "连接地址池的名称。最多支持传入20个连接地址池名称。\n\n名称长度为2~128个字符，以大小写字母或中文开头，可包含数字、下划线（_）或短划线（-）。",
        "en": "The name of the connection address pool. You can specify up to 20 connection address pool names.\n\nThe name must be 2 to 128 characters in length, and can contain letters, digits, underscores (_), and hyphens (-). It must start with a letter or a Chinese character."
      }
    },
    {
      "name": "ConnectionPoolStatus",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "连接地址池的状态。最多支持传入20个连接地址池状态。\n- **Creating**：创建中。\n- **Running**：运行中。\n- **Deleting**：删除中。\n- **Deleted**：已删除。\n- **Associating**：IP绑定中。\n- **Dissociating**：IP解绑中。",
        "en": "The status of the connection address pool. You can specify up to 20 connection address pool statuses.\n- **Creating**: Creation in progress.\n- **Running**: Running.\n- **Deleting**: Deletion in progress.\n- **Deleted**: Deleted.\n- **Associating**: IP attaching in progress.\n- **Dissociating**: IP detaching in progress."
      }
    },
    {
      "name": "IoTCloudConnectorId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云连接器实例ID。",
        "en": "The cloud connector instance ID."
      }
    },
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "本次读取的最大数据记录数量，取值：**1**~**50**。传入为空时，默认值为**10**。",
        "en": "The maximum number of data records to read in this request. Valid values: **1** to **50**. If this parameter is left empty, the default value is **10**."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用来标记当前开始读取的位置，置空表示从头开始。\n\n> 第一次调用不用填写，会在返回结果中包含第二次调用时用的NextToken，之后每次调用返回结果中都有下一次调用时的NextToken。",
        "en": "A token used to mark the starting position for the current read operation. An empty value indicates reading from the beginning.\n\n> You do not need to specify this parameter in the first call. The response will include a NextToken for the next call. Each subsequent response will contain the NextToken for the following call."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云连接器实例所在的地域ID，仅取值：**cn-hangzhou**。",
        "en": "The region ID of the cloud connector instance. Valid value: **cn-hangzhou** only."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询云连接地址池",
    "en": "Query cloud connection address pools"
  }
}