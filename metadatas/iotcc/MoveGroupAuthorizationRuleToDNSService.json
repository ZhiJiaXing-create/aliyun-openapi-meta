{
  "name": "MoveGroupAuthorizationRuleToDNSService",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AuthorizationRuleId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "授权规则ID。",
        "en": "The ID of the authorization rule."
      }
    },
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "客户端Token，用于保证请求的幂等性。\n\n由客户端生成该参数值，要保证在不同请求间唯一，最大值不超过64个ASCII字符。\n\n> 若您未指定，则系统自动使用API请求的**RequestId**作为**ClientToken**标识。每次API请求的**RequestId**可能不一样。",
        "en": "A client token that ensures the idempotence of the request.\n\nGenerate a unique value on the client side. The value must be unique across requests and contain no more than 64 ASCII characters.\n\n> If you do not specify this parameter, the system uses the **RequestId** of the API request as the **ClientToken**. Each API request has a different **RequestId**."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否只预检此次请求。取值：\n- **true**：发送检查请求，不会转移授权规则。检查项包括是否填写了必需参数、请求格式、业务限制。如果检查不通过，则返回对应错误。如果检查通过，则返回错误码`DryRunOperation`。\n- **false**（默认值）：发送正常请求，通过检查后返回HTTP 2xx状态码并直接进行转移。",
        "en": "Specifies whether to perform a dry run. Valid values:\n\n- **true**: Performs a check. The authorization rule is not moved. The check includes required parameters, request format, and business limits. If the check fails, the corresponding error is returned. If the check passes, the error code `DryRunOperation` is returned.\n\n- **false** (default): Performs the operation. After the check passes, the HTTP status code 2xx is returned and the authorization rule is moved."
      }
    },
    {
      "name": "IoTCloudConnectorGroupId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云连接器实例组ID。",
        "en": "The ID of the cloud connector instance group."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "云连接器实例组所在的地域ID。\n\n> 目前，支持开通云连接器的地域：华东1（杭州）、华东2（上海）、华北1（青岛）、华北2（北京）、华北5（呼和浩特）、华北6（乌兰察布）、华南1（深圳）、华南3（广州）、西南1（成都）、德国（法兰克福）。",
        "en": "The ID of the region where the cloud connector instance group resides.\n\n> Cloud Connector is available in these regions: China East 1 (Hangzhou), China East 2 (Shanghai), China North 1 (Qingdao), China North 2 (Beijing), China North 5 (Hohhot), China North 6 (Ulanqab), China South 1 (Shenzhen), China South 3 (Guangzhou), China Southwest 1 (Chengdu), and Germany (Frankfurt)."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "实例组规则转移到DNS授权规则",
    "en": "Transfer Instance Group Rules to DNS Authorization Rules"
  },
  "descriptions": {
    "zh": "> 只允许将授权规则中访问目标类型为**Cidr**且目的地址为DNS IP的规则转移到DNS授权规则中，您可以通过调用[ListGroupAuthorizationRules](~~402441~~)接口查看实例组授权规则的访问目标类型和目的地址。",
    "en": "> You can move only authorization rules that meet both conditions: the access destination type is **Cidr** and the destination address is a DNS IP address. To view the access destination type and destination address of an instance group’s authorization rules, call the [ListGroupAuthorizationRules](~~402441~~) operation."
  }
}