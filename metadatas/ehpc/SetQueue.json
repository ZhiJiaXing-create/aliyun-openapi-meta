{
  "name": "SetQueue",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。",
        "en": "The cluster ID."
      }
    },
    {
      "name": "Node",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "队列中节点信息列表。",
        "en": "The list of nodes to move."
      },
      "sub_parameters": [
        {
          "name": "Name",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "需要移动的计算节点主机名称。",
            "en": "The hostname of the compute node to move."
          }
        }
      ]
    },
    {
      "name": "QueueName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "目标队列名称。",
        "en": "The name of the destination queue."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "移动计算节点到指定队列",
    "en": "SetQueue"
  },
  "example": {
    "unifiedCli": "aliyun ehpc set-queue --cluster-id ehpc-hz-FYUr320000 --queue-name work --node Name=compute1 --api-version 2018-04-12 --region cn-hangzhou",
    "legacyCli": "aliyun ehpc SetQueue --region cn-hangzhou --ClusterId 'ehpc-hz-FYUr320000' --Node.1.Name compute1 --QueueName work"
  }
}