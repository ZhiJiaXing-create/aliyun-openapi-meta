{
  "name": "AddUsers",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Async",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否采用异步消息链路新增用户。\n\n默认值：false。",
        "en": "Specifies whether to use asynchronous message links to add the users.\n\nDefault value: false."
      }
    },
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。\n\n您可以通过调用[ListClusters](~~87116~~)获取集群ID。",
        "en": "The cluster ID.\n\nYou can call the [ListClusters](~~87116~~) operation to obtain the cluster ID."
      }
    },
    {
      "name": "User",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "用户信息。可以传入1~100个用户信息。",
        "en": "The users. You can specify 1 to 100 users."
      },
      "sub_parameters": [
        {
          "name": "Group",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "新增用户的用户组。取值范围：\n- users：普通权限组，适用于只有提交、调试作业需求的普通用户。 \n- wheel：sudo权限组，适用于需要管理集群的管理员，除提交、调试作业外，还可以执行sudo命令进行安装软件、重启节点等操作。",
            "en": "The permission group of the new user. Valid values:\n\n*   users: ordinary permissions, which are suitable for ordinary users that need only to submit and debug jobs.\n*   wheel: sudo permissions, which are suitable for administrators who need to manage clusters. In addition to submitting and debugging jobs, you can also run sudo commands to install software and restart nodes."
          }
        },
        {
          "name": "Name",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "新增用户的用户名。长度为6~30个字符，必须以英文字母开头，可以包含数字、特殊字符（.）。",
            "en": "The username of the new user. The username must be 6 to 30 characters in length, and can contain letters, digits and periods (.). The username must start with a letter."
          }
        },
        {
          "name": "Password",
          "position": "Query",
          "type": "String",
          "required": true,
          "description": {
            "zh": "新增用户的密码。长度为8~30个字符，必须同时包含以下四项中的三项：\n\n- 大写英文字母\n- 小写英文字母\n- 数字\n- 特殊字符：`()~!@#$%^&*-_+=|{}[]:;'/<>,.?/`\n\n> 建议使用HTTPS协议调用API以避免可能发生的密码泄露。",
            "en": "The password of the new user. The password must be 8 to 30 characters in length and must contain at least three of the following character types:\n\n*   Uppercase letter\n*   Lowercase letter\n*   Digit\n*   Special character: `()~!@#$%^&*-_+=|{}[]:;'/<>,.?/`\n\n>  We recommend that you use HTTPS to call this operation to avoid password leaks."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量新增集群用户",
    "en": "AddUsers"
  },
  "example": {
    "unifiedCli": "aliyun ehpc add-users --cluster-id ehpc-hz-FYUr320000 --user Group=users Password=1@a20000 Name=user1 --async false --api-version 2018-04-12 --region cn-hangzhou",
    "legacyCli": "aliyun ehpc AddUsers --region cn-hangzhou --User.1.Group users --User.1.Password '1@a20000' --ClusterId 'ehpc-hz-FYUr320000' --Async false --User.1.Name user1"
  }
}