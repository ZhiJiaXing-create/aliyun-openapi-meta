{
  "name": "ListClustersMeta",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "PageNumber",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "显示的页码，起始值为1。",
        "en": "The number of the page to return. Pages start from page 1."
      }
    },
    {
      "name": "PageSize",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每页显示的条数。取值范围：1~50\n\n默认值：10",
        "en": "The number of entries to return on each page. Valid values: 1 to 50.\n\nDefault value: 10"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询集群元列表",
    "en": "ListClustersMeta"
  },
  "example": {
    "unifiedCli": "aliyun ehpc list-clusters-meta --page-number 1 --page-size 10 --api-version 2018-04-12 --region cn-hangzhou",
    "legacyCli": "aliyun ehpc ListClustersMeta --region cn-hangzhou --PageSize 10 --PageNumber 1"
  }
}