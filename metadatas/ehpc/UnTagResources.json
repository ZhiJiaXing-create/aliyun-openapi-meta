{
  "name": "UnTagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解绑资源上全部的标签。当请求中未设置TagKey.N时，该参数才有效。取值范围：\n\n- true\n\n- false\n\n默认值：false。",
        "en": "Specifies whether to remove all tags from the resource. This parameter is valid only when the TagKey.N parameter is not specified. Valid values:\n\n*   true\n*   false\n\nDefault value: false."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源所属的地域ID。",
        "en": "The region ID of the resource."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID，最多50个。",
        "en": "The resource IDs. You can specify up to 50 resource IDs."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型。目前仅支持cluster，即E-HPC集群。",
        "en": "The type of the resource from which you want to remove tags. Set the value to cluster, which indicates E-HPC clusters."
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源的标签键，最多20个。",
        "en": "The tag key of the resource. You can specify up to 20 tag keys."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为指定资源解绑标签",
    "en": "UnTagResources"
  },
  "example": {
    "unifiedCli": "aliyun ehpc un-tag-resources --biz-region-id cn-hangzhou --resource-type cluster --resource-id ehpc-hz-xxxx --tag-key TestKey --all false --api-version 2018-04-12 --region cn-hangzhou",
    "legacyCli": "aliyun ehpc UnTagResources --region cn-hangzhou --All false --ResourceId.1 'ehpc-hz-xxxx' --TagKey.1 TestKey --ResourceType cluster --RegionId 'cn-hangzhou'"
  }
}