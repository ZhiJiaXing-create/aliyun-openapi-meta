{
  "name": "DeleteCluster",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定释放的集群ID。\n\n您可以通过调用[ListClusters](~~87116~~)获取集群ID。",
        "en": "The ID of the cluster.\n\nYou can call the [ListClusters](~~87116~~) operation to obtain cluster IDs."
      }
    },
    {
      "name": "ReleaseInstance",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否释放由E-HPC创建的ECS实例。\n\n默认值：true",
        "en": "Specifies whether to release Elastic Compute Service (ECS) instances that are created by using Elastic High Performance Computing (E-HPC).\n\nDefault value: true."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "释放一个集群",
    "en": "DeleteCluster"
  },
  "descriptions": {
    "zh": "## 接口说明\n集群释放后不可恢复，集群中按量付费的节点将立即释放，到期前的包年包月节点会保留，到期后的包年包月节点会自动释放。如果您需要释放到期前的包年包月节点，请申请退款或转为按量付费实例后再释放。请确认不再使用该集群后再进行释放集群的操作。",
    "en": "After a cluster is released, the pay-as-you-go nodes and the subscription nodes that are expired are automatically released. The subscription nodes that are expired are retained. If you need to release subscription nodes that are not expired, change the billing method to pay-as-you-go. Before you release a cluster, make sure that you no longer use the cluster."
  },
  "example": {
    "unifiedCli": "aliyun ehpc delete-cluster --cluster-id ehpc-hz-QKKVqO0000 --release-instance true --api-version 2018-04-12 --region cn-hangzhou",
    "legacyCli": "aliyun ehpc DeleteCluster --region cn-hangzhou --ClusterId 'ehpc-hz-QKKVqO0000' --ReleaseInstance true"
  }
}