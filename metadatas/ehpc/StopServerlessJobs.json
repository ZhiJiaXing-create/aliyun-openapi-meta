{
  "name": "StopServerlessJobs",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "集群ID。\n\n您可以通过调用[ListClusters](~~87116~~)获取集群ID。",
        "en": "The ID of the E-HPC cluster.\n\nYou can call the [ListClusters](~~87116~~) operation to query the cluster ID."
      }
    },
    {
      "name": "JobIds",
      "position": "Query",
      "type": "Array",
      "required": true,
      "description": {
        "zh": "Serverless作业ID或子任务ID（数组作业）列表。\n> - 当传入数组作业的作业ID时，会停止该数组作业下的所有子任务。\n> - 当传入数组作业下的子任务ID时，仅停止该子任务。",
        "en": "The serverless job IDs or subtask IDs (array jobs).\n\n*   If you specify the job ID of an array job, all subtasks under the array job are stopped.\n*   If you specify the ID of a subtask of an array job, only the subtask is stopped."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "批量停止Serverless作业",
    "en": "StopServerlessJobs"
  },
  "example": {
    "unifiedCli": "aliyun ehpc stop-serverless-jobs --cluster-id ehpc-hz-FYUr320000 --job-ids 5 --api-version 2018-04-12 --region cn-hangzhou",
    "legacyCli": "aliyun ehpc StopServerlessJobs --region cn-hangzhou --ClusterId 'ehpc-hz-FYUr320000' --JobIds.1 5 --method POST --force"
  }
}