{
  "name": "DescribeVServerGroups",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "虚拟服务器组名称。",
        "en": "The name of the vServer group."
      }
    },
    {
      "name": "IncludeListener",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否返回关联的监听信息。取值：\n\n- **true**：是。\n- **false**（默认值）：否。",
        "en": "Specifies whether to return information about the associated listeners. Valid values:\n\n- **true**: yes\n\n- **false** (default): no"
      }
    },
    {
      "name": "IncludeRule",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否返回关联的转发规则信息。取值：\n\n- **true**：是。\n- **false**（默认值）：否。",
        "en": "Specifies whether to return the forwarding rules associated with the vServer groups. Valid values:\n\n- **true**: yes\n\n- **false** (default): no"
      }
    },
    {
      "name": "LoadBalancerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "传统型负载均衡实例ID。",
        "en": "The ID of the CLB instance."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "传统型负载均衡实例的地域ID。\n\n您可以通过调用[DescribeRegions](~~27584~~)接口获取地域ID。",
        "en": "The region ID of the Classic Load Balancer (CLB) instance.\n\nYou can call the [DescribeRegions](~~27584~~) operation to query the most recent region list."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源标签。",
        "en": "The tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签键。N的取值范围：**1~20**。一旦输入该值，则不允许为空字符串。\n\n最多支持64个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The key of tag N. Valid values of N: 1 to 20. The tag key cannot be an empty string.\n\nThe tag key can be up to 64 characters in length, and cannot contain `http://` or `https://`. The tag key cannot start with `aliyun` or `acs`:."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "资源的标签值。N的取值范围：**1~20**。一旦输入该值，可以为空字符串。\n最多支持128个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag value. Valid values of N: 1 to 20. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length and cannot start with `acs:` or `aliyun`. The tag value cannot contain `http://` or `https://`."
          }
        }
      ]
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询虚拟服务器组列表",
    "en": "DescribeVServerGroups"
  },
  "example": {
    "unifiedCli": "aliyun slb describe-vserver-groups --biz-region-id cn-hangzhou --load-balancer-id lb-bp1o94dp5i6ea0000000 --include-rule false --include-listener false --tag Value=TestValue Key=TestKey --description Group3 --api-version 2014-05-15 --region cn-hangzhou",
    "legacyCli": "aliyun slb DescribeVServerGroups --region cn-hangzhou --Tag.1.Key TestKey --Description Group3 --IncludeListener false --Tag.1.Value TestValue --LoadBalancerId 'lb-bp1o94dp5i6ea0000000' --RegionId 'cn-hangzhou' --IncludeRule false"
  }
}