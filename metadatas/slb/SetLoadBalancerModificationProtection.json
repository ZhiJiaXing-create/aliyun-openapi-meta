{
  "name": "SetLoadBalancerModificationProtection",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "LoadBalancerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "传统型负载均衡实例ID。",
        "en": "The ID of the CLB instance."
      }
    },
    {
      "name": "ModificationProtectionReason",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "设置修改保护状态的原因，长度为1~80个字符，必须以字母或中文开头，支持数字、半角句号（.）、下划线（_）和短划线（-）。\n\n> 仅在**ModificationProtectionStatus**为**ConsoleProtection**时有效。",
        "en": "The reason why the configuration read-only mode is enabled. The value must be 1 to 80 characters in length. It must start with a letter and can contain letters, digits, periods (.), underscores (\\_), and hyphens (-).\n\n> This parameter is valid only if the **ModificationProtectionStatus** parameter is set to **ConsoleProtection**."
      }
    },
    {
      "name": "ModificationProtectionStatus",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "传统型负载均衡修改保护状态：\n\n- **NonProtection**：不限制修改保护，设置后会清空之前设置的**ModificationProtectionReason**。\n\n- **ConsoleProtection**：实例控制台修改保护状态。\n\n> 当取值为**ConsoleProtection**，即开启修改保护后，用户不能通过负载均衡控制台修改实例配置，但可以通过调用API修改实例配置。",
        "en": "Specifies whether to enable the configuration read-only mode. Valid values:\n\n- **NonProtection**: disables the configuration read-only mode. After you disable the configuration read-only mode, the value of **ModificationProtectionReason** is cleared.\n\n- **ConsoleProtection**: enables the configuration read-only mode.\n\n> If you set this parameter to **ConsoleProtection**, you cannot use the CLB console to modify instance configurations. However, you can call API operations to modify instance configurations."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "传统型负载均衡实例的地域ID。\n\n您可以通过调用[DescribeRegions](~~27584~~)接口查询地域ID。",
        "en": "The region ID of the CLB instance.\n\nYou can call the [DescribeRegions](~~27584~~) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "设置实例修改保护状态",
    "en": "SetLoadBalancerModificationProtection"
  },
  "example": {
    "unifiedCli": "aliyun slb set-load-balancer-modification-protection --biz-region-id cn-hangzhou --load-balancer-id lb-bp1b6c719dfa08e00000 --modification-protection-status ConsoleProtection --modification-protection-reason Configuration-change --api-version 2014-05-15 --region cn-hangzhou",
    "legacyCli": "aliyun slb SetLoadBalancerModificationProtection --region cn-hangzhou --ModificationProtectionReason 'Configuration-change' --LoadBalancerId 'lb-bp1b6c719dfa08e00000' --RegionId 'cn-hangzhou' --ModificationProtectionStatus ConsoleProtection"
  }
}