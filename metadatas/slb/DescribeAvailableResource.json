{
  "name": "DescribeAvailableResource",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AddressIPVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "IP地址类型。\n\n取值：\n\n- **ipv4**\n- **ipv6**",
        "en": "The type of the IP address.\n\nValid values: **ipv4 and ipv6**."
      }
    },
    {
      "name": "AddressType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "网络类型。\n\n取值：\n\n- **vpc**：专有网络的私网负载均衡实例。\n- **classic_internet**：公网负载均衡实例。\n- **classic_intranet**：经典网络的私网负载均衡实例。",
        "en": "The network type.\n\nValid values: **vpc, classic-internet, and classic-intranet**.\n\nvpc: an internal Classic Load Balancer (CLB) instance that is deployed in a virtual private cloud (VPC).\n\nclassic\\_internet: a public-facing CLB instance.\n\nclassic\\_intranet: an internal CLB instance that is deployed in a classic network."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询资源售卖情况",
    "en": "DescribeAvailableResource"
  },
  "descriptions": {
    "zh": "> 只返回支持售卖的可用区及资源类型。",
    "en": "> Only the available resources and zones are returned."
  },
  "example": {
    "unifiedCli": "aliyun slb describe-available-resource --biz-region-id cn-hangzhou --address-type vpc --address-ip-version ipv4 --api-version 2014-05-15 --region cn-hangzhou",
    "legacyCli": "aliyun slb DescribeAvailableResource --region cn-hangzhou --AddressIPVersion ipv4 --RegionId 'cn-hangzhou' --AddressType vpc"
  }
}