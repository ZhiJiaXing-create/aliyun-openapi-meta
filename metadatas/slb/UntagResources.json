{
  "name": "UntagResources",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "All",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否全部删除，只针对**TagKey.N**为空时有效。\n\n取值：**true**或**false**。",
        "en": "Specifies whether to remove all tags from the specified resource. This parameter takes effect only if **TagKey.N** is empty.\n\nValid values: **true** and **false**."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "传统型负载均衡实例所在的地域ID。\n\n您可以通过调用[DescribeRegions](~~27584~~)接口查询地域ID。",
        "en": "The region ID of the Classic Load Balancer (CLB) instance.\n\nYou can call the [DescribeRegions](~~27584~~) operation to query the most recent region list."
      }
    },
    {
      "name": "ResourceId",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "资源ID，最多支持20个资源ID。\n> **listener**的**ResourceId**取值为**LoadBalancerId\\_监听协议\\_Port**，其中LoadBalancerId为实例ID，port为监听端口。",
        "en": "The ID of the resource. You can specify at most 20 resources.\n\n> Set **ResourceId** of the **listener** to **LoadBalancerId\\_Listener protocol\\_Port**. Set LoadBalancerId to the ID of the CLB instance, and Port to the listener port."
      }
    },
    {
      "name": "ResourceType",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "资源类型定义，取值：\n\n- **instance**：负载均衡实例。\n\n- **certificate**：证书。\n\n- **acl**：访问控制。\n\n- **listener**：监听。\n\n- **vservergroup**：虚拟服务器组。\n\n- **masterslaveservergroup**：主备服务器组。",
        "en": "The type of resource. Valid values:\n\n- **instance**: CLB instance\n\n- **certificate**: certificate\n\n- **acl**: access control list (ACL)\n\n- **listener**: listener\n\n- **vservergroup**: vServer group\n\n- **masterslaveservergroup**: primary/secondary server group"
      }
    },
    {
      "name": "TagKey",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "资源的标签键。最多支持20个标签键。一旦传入该值，则不允许为空字符串。\n\n最多支持64个字符，不能以`aliyun`和`acs:`开头，不能包含`http://`或者`https://`。\n> 如果不指定**TagKey**，这个实例的所有标签将被删除。",
        "en": "The tag key. You can specify at most 20 tag keys. The tag key cannot be an empty string.\n\nThe tag key can be up to 64 characters in length and cannot contain `http://` or `https://`. The tag key cannot start with `aliyun` or `acs:`.\n\n> If you do not specify **TagKey**, all tags are removed from the instance."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为指定的资源列表统一解绑标签",
    "en": "UntagResources"
  },
  "example": {
    "unifiedCli": "aliyun slb untag-resources --biz-region-id cn-hangzhou --resource-type instance --all true --resource-id lb-bp16qjewdsunr41m10000 --tag-key FinanceDept --api-version 2014-05-15 --region cn-hangzhou",
    "legacyCli": "aliyun slb UntagResources --region cn-hangzhou --All true --ResourceId.1 'lb-bp16qjewdsunr41m10000' --TagKey.1 FinanceDept --ResourceType instance --RegionId 'cn-hangzhou'"
  }
}