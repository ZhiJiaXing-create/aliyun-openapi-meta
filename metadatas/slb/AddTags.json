{
  "name": "AddTags",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "LoadBalancerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "负载均衡实例ID。",
        "en": "The ID of the Server Load Balancer (SLB) instance."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "负载均衡实例的地域ID。",
        "en": "The ID of the region to which the SLB instance belongs."
      }
    },
    {
      "name": "Tags",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "需要添加的Tag列表。",
        "en": "The list of tags that need to be added."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "为指定的负载均衡实例添加标签",
    "en": "AddTags"
  },
  "descriptions": {
    "zh": "为指定的负载均衡实例添加标签。\n\n调用该接口时，请注意：\n\n* 每个负载均衡实例最多可绑定10个Tag。\n\n* 单次绑定的标签数最多为5对。\n\n* 一个负载均衡实例下的所有Tag和Key不能重复。\n\n* 当添加的标签与原有标签Key相同，但Value不同时，则覆盖原有的标签。",
    "en": "# Limits\n\nBefore you call this API, note the following limits:\n\n- You can add up to 10 tags to each SLB instance.\n\n- You can add up to five pairs of tags at a time.\n\n- All the tags and keys added to an SLB instance must be unique.\n\n- If you add a tag of which the key is the same as that of an existing tag, but the value is different, the new tag overwrites the existing one."
  },
  "example": {
    "unifiedCli": "aliyun slb add-tags --biz-region-id cn-hangzhou --load-balancer-id lb-bp1kuzyb000000 --tags TagKey_:_Key1_,_TagValue_:_Value1_,_TagKey_:_Key2_,_TagValue_:_Value2 --api-version 2014-05-15 --region cn-hangzhou",
    "legacyCli": "aliyun slb AddTags --region cn-hangzhou --LoadBalancerId 'lb-bp1kuzyb000000' --RegionId 'cn-hangzhou' --Tags 'TagKey_:_Key1_,_TagValue_:_Value1_,_TagKey_:_Key2_,_TagValue_:_Value2'"
  }
}