{
  "name": "StartLoadBalancerListener",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ListenerPort",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "负载均衡实例前端使用的端口。\n\n取值：**1-65535**。",
        "en": "The frontend port that is used by the CLB instance.\n\nValid values: **1 to 65535**."
      }
    },
    {
      "name": "ListenerProtocol",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "负载均衡实例前端使用的协议。\n\n> 相同端口存在不同协议监听时，此字段必填。",
        "en": "The frontend protocol that is used by the CLB instance.\n\n> This parameter is required if the same port is used by listeners of different protocols."
      }
    },
    {
      "name": "LoadBalancerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "负载均衡实例的ID。",
        "en": "The CLB instance ID."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "负载均衡实例的地域。\n\n您可以通过调用[DescribeRegions](~~27584~~)接口查询地域ID。",
        "en": "The region where the CLB instance is created.\n\nYou can call the [DescribeRegions](~~27584~~) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "启动监听",
    "en": "StartLoadBalancerListener"
  },
  "descriptions": {
    "zh": "在调用该接口时，注意：\n\n* 监听状态必须为stopped时，才可以调用该接口。\n\n* 接口调用成功后，监听进入starting状态。\n\n* 当监听所属负载均衡实例的状态为locked时，调用此接口会失败。",
    "en": "When you call this operation, take note of the following items:\n\n- You can call the operation only when the listener is in the Stopped state.\n\n- If the operation is successful, the listener switches to the Starting state.\n\n- You cannot perform this operation when the Classic Load Balancer (CLB) instance to which the listener belongs is in the Locked state."
  },
  "example": {
    "unifiedCli": "aliyun slb start-load-balancer-listener --biz-region-id cn-hangzhou --load-balancer-id lb-bp13jaf5qli5000000000 --listener-port 80 --listener-protocol https --api-version 2014-05-15 --region cn-hangzhou",
    "legacyCli": "aliyun slb StartLoadBalancerListener --region cn-hangzhou --ListenerPort 80 --LoadBalancerId 'lb-bp13jaf5qli5000000000' --RegionId 'cn-hangzhou' --ListenerProtocol https"
  }
}