{
  "name": "SetRule",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Cookie",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "服务器上配置的Cookie。\n\n长度为1~200个字符，只能包含ASCII英文字母和数字字符，不能包含半角逗号（,）、半角分号（;）或空格，也不能以美元（$）开头。\n\n> 当**StickySession**为**on**且**StickySessionType**为**server**时必选且有效。",
        "en": "The cookie configured on the backend server.\n\nThe cookie must be 1 to 200 characters long and can contain only ASCII letters and digits. It cannot contain commas (,), semicolons (;), or spaces. It cannot start with a dollar sign ($).\n\n> This parameter is required and takes effect only when **StickySession** is set to **on** and **StickySessionType** is set to **server**."
      }
    },
    {
      "name": "CookieTimeout",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "Cookie超时时间。单位：秒。取值范围：**1**~**86400**。\n\n> 当**StickySession**为**on**且**StickySessionType**为**insert**时必选且有效。",
        "en": "The timeout period of the cookie, in seconds. Valid values: **1** to **86400**.\n\n> This parameter is required and takes effect only when **StickySession** is set to **on** and **StickySessionType** is set to **insert**."
      }
    },
    {
      "name": "DryRun",
      "position": "Query",
      "type": "Boolean",
      "required": false
    },
    {
      "name": "HealthCheck",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启健康检查。取值：\n\n- **on**：开启。\n- **off**：不开启。\n\n> **ListenerSync**为**off**时必选且有效，其他情况会忽略此参数。",
        "en": "Specifies whether to enable health checks. Valid values:\n\n- **on**: Enables health checks.\n\n- **off**: Disables health checks.\n\n> This parameter is required and takes effect only when **ListenerSync** is set to **off**."
      }
    },
    {
      "name": "HealthCheckConnectPort",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查使用的端口。取值范围：**1**~**65535**。\n\n> **HealthCheck**为**on**时有效。",
        "en": "The port used for health checks. Valid values: **1** to **65535**.\n\n> This parameter takes effect only when **HealthCheck** is set to **on**."
      }
    },
    {
      "name": "HealthCheckDomain",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于健康检查的域名，取值：\n\n* **$_ip**： 后端服务器的私网IP。当指定了$_ip或HealthCheckDomain未指定时，负载均衡会使用各后端服务器的私网IP当做健康检查使用的域名。\n\n* **domain**：域名长度为1~80字符，只能包含字母、数字、半角句号（.）和短划线（-）。\n\n>** HealthCheck**为**on**时有效。",
        "en": "The domain name used for health checks. Valid values:\n\n- **$\\_ip**: The private IP of a backend server. If you set this value or leave the parameter empty, Server Load Balancer uses the private IP of each backend server as the domain for health checks.\n\n- **domain**: A domain name. It must be 1 to 80 characters long and can contain only letters, digits, periods (.), and hyphens (-).\n\n> This parameter takes effect only when **HealthCheck** is set to **on**."
      }
    },
    {
      "name": "HealthCheckHttpCode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "健康检查正常的HTTP状态码，多个状态码用半角逗号（,）分割。\n\n取值：**http_2xx**或**http_3x**x或**http_4xx**或**http_5xx**。\n\n> **HealthCheck**为**on**时必选且有效。",
        "en": "The HTTP status codes that indicate a successful health check. Separate multiple HTTP status codes with commas (,).\n\nValid values: **http\\_2xx**, **http\\_3xx**, **http\\_4xx**, and **http\\_5xx**.\n\n> This parameter is required and takes effect only when **HealthCheck** is set to **on**."
      }
    },
    {
      "name": "HealthCheckInterval",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查的时间间隔。单位：秒。取值范围：**1**~**50**。\n\n> **HealthCheck**为**on**时必选且有效。",
        "en": "The interval between health checks, in seconds. Valid values: **1** to **50**.\n\n> This parameter is required and takes effect only when **HealthCheck** is set to **on**."
      }
    },
    {
      "name": "HealthCheckTimeout",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "接收来自运行状况检查的响应需要等待的时间。如果后端ECS在指定的时间内没有正确响应，则判定为健康检查失败。单位：秒。取值范围：**1**~**300**。\n\n> **HealthCheck**为**on**时必选且有效。",
        "en": "The time, in seconds, to wait for a health check response. If a backend server does not respond within this time, the health check fails. Valid values: **1** to **300**.\n\n> This parameter is required and takes effect only when **HealthCheck** is set to **on**."
      }
    },
    {
      "name": "HealthCheckURI",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于健康检查的URI。\n\n>**HealthCheck**为**on**时必选且有效。",
        "en": "The URI used for health checks.\n\n> This parameter is required and takes effect only when **HealthCheck** is set to **on**."
      }
    },
    {
      "name": "HealthyThreshold",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查连续成功多少次后，将后端服务器的健康检查状态由**失败**判定为**成功**。\n\n取值范围：**2**~**10**。\n\n> **HealthCheck**为**on**时必选且有效。",
        "en": "The number of consecutive successful health checks required to declare a backend server **healthy**.\n\nValid values: **2** to **10**.\n\n> This parameter is required and takes effect only when **HealthCheck** is set to **on**."
      }
    },
    {
      "name": "ListenerSync",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否继承监听的健康检查、会话保持和调度算法配置。取值：\n\n- **on**：继承监听配置。\n\n- **off**：不继承监听配置。转发规则自定义健康检查及会话保持配置。",
        "en": "Specifies whether to inherit the health check, sticky session, and scheduling algorithm configurations from the listener. Valid values:\n\n- **on**: Inherits the configurations from the listener.\n\n- **off**: Does not inherit the configurations. You can customize the health check and sticky session settings for the forwarding rule."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "负载均衡实例的地域ID。\n\n您可以通过调用[DescribeRegions](~~27584~~)接口查询地域ID。",
        "en": "The region ID of the SLB instance.\n\nYou can call the [DescribeRegions](~~27584~~) operation to query region IDs."
      }
    },
    {
      "name": "RuleId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "转发规则ID。",
        "en": "The ID of the forwarding rule."
      }
    },
    {
      "name": "RuleName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "转发规则名称，长度限制为1~40个字符，支持中文、字母、数字、短划线（-）、正斜线（/）、半角句号（.）和下划线（_）这些字符。\n\n> 同一个监听内不同规则的名称必须唯一。",
        "en": "The name of the forwarding rule. The name must be 1 to 40 characters long and can contain letters, digits, Chinese characters, hyphens (-), forward slashes (/), periods (.), and underscores (\\_).\n\n> Forwarding rules for the same listener must have unique names."
      }
    },
    {
      "name": "Scheduler",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "调度算法。取值：\n\n* **wrr**：权重值越高的后端服务器，被轮询到的次数（概率）也越高。\n\n* **rr**：按照访问顺序依次将外部请求依序分发到后端服务器。\n\n>在**ListenerSync**为**off**时必选且有效。",
        "en": "The scheduling algorithm. Valid values:\n\n- **wrr**: Weighted Round Robin. Backend servers with higher weights receive more requests than backend servers with lower weights.\n\n- **rr**: Round Robin. Requests are distributed to backend servers in sequence.\n\n> This parameter is required and takes effect only when **ListenerSync** is set to **off**."
      }
    },
    {
      "name": "StickySession",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启会话保持，取值：\n\n- **on**：开启。\n\n- **off**：不开启。\n\n在**ListenerSync**为**off**时必选且有效。",
        "en": "Specifies whether to enable sticky sessions. Valid values:\n\n- **on**: Enables sticky sessions.\n\n- **off**: Disables sticky sessions.\n\nThis parameter is required and takes effect only when **ListenerSync** is set to **off**."
      }
    },
    {
      "name": "StickySessionType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Cookie的处理方式。取值：\n\n- **insert**：植入Cookie。\n 客户端第一次访问时，负载均衡会在返回请求中植入Cookie（即在HTTP或HTTPS响应报文中插入后端服务器ID），客户端下次携带此Cookie访问时，负载均衡服务会将请求定向转发给第一次访问时记录到的后端服务器上。\n\n- **server**：重写Cookie。\n 负载均衡发现用户自定义Cookie，将会对原来的Cookie进行重写，下次客户端携带新的Cookie访问时，负载均衡服务会将请求定向转发给之前记录到的后端服务器。\n\n>**StickySession**为**on**时必选且有效。",
        "en": "The method used to manage cookies. Valid values:\n\n- **insert**: Inserts a cookie.\n  When a client makes the first request, Server Load Balancer inserts a cookie into the response. The cookie identifies the backend server that processes the request. Subsequent requests from the client that contain this cookie are forwarded to the same backend server.\n\n- **server**: Rewrites a cookie.\n  When Server Load Balancer detects a user-defined cookie, it overwrites the original cookie. Subsequent requests from the client that contain the new cookie are forwarded to the same backend server.\n\n> This parameter is required and takes effect only when **StickySession** is set to **on**."
      }
    },
    {
      "name": "UnhealthyThreshold",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查连续失败多少次后，将后端服务器的健康检查状态由**成功**判定为**失败**。\n\n取值范围：**2**~**10**。\n\n> **HealthCheck**为**on**时必选且有效。",
        "en": "The number of consecutive failed health checks required to declare a backend server **unhealthy**.\n\nValid values: **2** to **10**.\n\n> This parameter is required and takes effect only when **HealthCheck** is set to **on**."
      }
    },
    {
      "name": "VServerGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "转发规则的目标服务器组ID。",
        "en": "The ID of the destination server group."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "修改目标虚拟服务器组的转发规则",
    "en": "SetRule"
  },
  "example": {
    "unifiedCli": "aliyun slb set-rule --biz-region-id cn-hangzhou --rule-id rule-3ejhkt0000 --vserver-group-id rsp-cige60000 --rule-name doctest --listener-sync off --scheduler wrr --sticky-session off --sticky-session-type insert --cookie-timeout 123 --cookie 23ffsa --health-check off --health-check-domain ip --health-check-uri /example --healthy-threshold 4 --unhealthy-threshold 4 --health-check-timeout 20 --health-check-interval 20 --health-check-connect-port 80 --health-check-http-code http_2xx --dry-run true --api-version 2014-05-15 --region cn-hangzhou",
    "legacyCli": "aliyun slb SetRule --region cn-hangzhou --CookieTimeout 123 --Cookie 23ffsa --VServerGroupId 'rsp-cige60000' --HealthCheckInterval 20 --UnhealthyThreshold 4 --Scheduler wrr --HealthCheckURI '/example' --RuleId 'rule-3ejhkt0000' --HealthCheck off --DryRun true --HealthCheckTimeout 20 --StickySession off --HealthCheckConnectPort 80 --HealthyThreshold 4 --ListenerSync off --HealthCheckDomain ip --RegionId 'cn-hangzhou' --StickySessionType insert --HealthCheckHttpCode http_2xx --RuleName doctest"
  }
}