{
  "name": "CreateLoadBalancerHTTPListener",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AclId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "监听绑定的访问策略组ID。\n\n> 当**AclStatus**参数的值为**on**时，该参数必选。",
        "en": "The ID of the network access control list (ACL) that is associated with the listener.\n\n> If **AclStatus** is set to **on**, this parameter is required."
      }
    },
    {
      "name": "AclStatus",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启访问控制功能。取值：\n- **on**：是。\n- **off**（默认值）：否。",
        "en": "Specifies whether to enable access control. Valid values:\n\n- **on**: yes\n\n- **off** (default): no"
      }
    },
    {
      "name": "AclType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问控制类型。取值：\n\n- **white**： 仅转发来自所选访问控制策略组中设置的IP地址或地址段的请求，白名单适用于只允许特定IP访问的场景。设置白名单存在一定业务风险。一旦设置白名单，就只有白名单中的IP可以访问负载均衡监听。\n \n  如果开启了白名单访问，但访问策略组中没有添加任何IP，则负载均衡监听会转发全部请求。\n\n- **black**： 来自所选访问控制策略组中设置的IP地址或地址段的所有请求都不会转发，黑名单适用于只限制某些特定IP访问的场景。\n\n  如果开启了黑名单访问，但访问策略组中没有添加任何IP，则负载均衡监听会转发全部请求。\n\n>当**AclStatus**参数的值为**on**时，该参数有效。",
        "en": "The type of access control. Valid values:\n\n- **white**: Only requests from IP addresses and CIDR blocks on the whitelist are forwarded by the listener. Your service may be adversely affected if the whitelist is not properly configured. If a whitelist is configured, the listener forwards only requests from IP addresses that are added to the whitelist.\n\n  If you configure a whitelist but do not add an IP address to the whitelist, the listener forwards all requests.\n\n- **black**: Requests from the IP addresses and CIDR blocks on the blacklist are blocked.\n\n  If you configure a blacklist but do not add an IP address to the blacklist, the listener forwards all requests.\n\n> When **AclStatus** is set to **on**, this parameter takes effect."
      }
    },
    {
      "name": "BackendServerPort",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "实例后端使用的端口。\n\n取值：**1**~**65535**。\n\n> 如果不使用虚拟服务器组（不指定VServerGroupId参数），则该参数必选。",
        "en": "The backend port that is used by the CLB instance.\n\nValid values: **1** to **65535**.\n\n> If the VServerGroupId parameter is not specified, this parameter is required."
      }
    },
    {
      "name": "Bandwidth",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "监听的带宽峰值，单位：Mbps。取值：\n\n<props=\"intl\">**-1**：不限制带宽峰值。</props>\n\n<props=\"china\">\n\n- **-1**：不限制带宽峰值。\n\n- **1**~**5120**：监听的带宽峰值，所有监听的带宽峰值之和不能超过实例的带宽峰值。\n\n</props>",
        "en": "The maximum bandwidth of the listener. Unit: Mbit/s. Valid values:\n\n**-1**: specifies that the bandwidth of the listener is unlimited."
      }
    },
    {
      "name": "Cookie",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "服务器上配置的Cookie。\n\n长度为1~200个字符，只能包含ASCII英文字母和数字字符，不能包含半角逗号（,）、半角分号（;）或空格，也不能以美元符号（$）开头。\n\n>当**StickySession**为**on**且**StickySessionType**为**server**时，该参数必选。",
        "en": "The cookie configured for the server.\n\nThe cookie must be 1 to 200 characters in length, and can contain only ASCII letters and digits. It cannot contain commas (,), semicolons (;), space characters, or start with a dollar sign ($).\n\n> This parameter is required when the **StickySession** parameter is set to **on** and the **StickySessionType** parameter is set to **server**."
      }
    },
    {
      "name": "CookieTimeout",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "Cookie超时时间。单位：秒。\n\n取值：**1**~**86400**。\n\n> 当**StickySession**为**on**且**StickySessionType**为**insert**时，该参数必选。",
        "en": "The timeout period of a cookie. Unit: seconds.\n\nValid values: **1** to **86400**.\n\n> If **StickySession** is set to **on** and **StickySessionType** is set to **insert**, this parameter is required."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自定义监听名称。\n\n长度限制为1~256个字符，支持中文、字母、数字、短划线（-）、正斜线（/）、半角句号（.）和下划线（_）。",
        "en": "The name of the listener.\n\nThe name must be 1 to 256 characters in length, and can contain letters, digits, hyphens (-), forward slashes (/), periods (.), and underscores (\\_)."
      }
    },
    {
      "name": "ForwardPort",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "HTTP至HTTPS的监听转发端口。",
        "en": "The listening port that is used to redirect HTTP requests to HTTPS."
      }
    },
    {
      "name": "Gzip",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启`Gzip`压缩，对特定文件类型进行压缩。取值：\n- **on**（默认值）：是。\n- **off**：否。",
        "en": "Specifies whether to enable `GZIP` compression to compress specific types of files. Valid values:\n\n- **on** (default)\n\n- **off**"
      }
    },
    {
      "name": "HealthCheck",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "是否开启健康检查。取值：\n\n- **on**：开启健康检查。\n\n- **off**：不开启健康检查。",
        "en": "Specifies whether to enable the health check feature. Valid values:\n\n- **on**: yes\n\n- **off**: no"
      }
    },
    {
      "name": "HealthCheckConnectPort",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查的后端服务器的端口。\n\n取值： **1**~**65535**。\n\n> 在**HealthCheck**值为**on**时才会有效。",
        "en": "The backend port that is used for health checks.\n\nValid values: **1** to **65535**.\n\n> This parameter takes effect only if the **HealthCheck** parameter is set to **on**."
      }
    },
    {
      "name": "HealthCheckDomain",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于健康检查的域名，取值：\n\n- **$_ip**： 后端服务器的私网IP。当指定了IP或该参数未指定时，负载均衡会使用各后端服务器的私网IP当做健康检查使用的域名。\n\n- **domain**：域名长度为1~80字符，只能包含字母、数字、半角句号（.）和短划线（-）。\n\n>在**HealthCheck**值为**on**时才会有效。",
        "en": "The domain name that is used for health checks. Valid values:\n\n- **$\\_ip**: the private IP address of a backend server. If an IP address is specified, or this parameter is not specified, the CLB instance uses the private IP address of each backend server as the domain name for health checks.\n\n- **domain**: The domain name must be 1 to 80 characters in length, and can contain letters, digits, periods (.), and hyphens (-).\n\n> This parameter takes effect only if the **HealthCheck** parameter is set to **on**."
      }
    },
    {
      "name": "HealthCheckHttpCode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "健康检查正常的HTTP状态码，多个状态码用逗号分隔。\n\n取值：**http\\_2xx**（默认值）、**http\\_3xx**、**http\\_4xx**或**http\\_5xx**。\n\n>在**HealthCheck**值为**on**时才会有效。",
        "en": "The HTTP status code for a successful health check. Separate multiple HTTP status codes with commas (,).\n\nValid values: **http\\_2xx** (default), **http\\_3xx**, **http\\_4xx**, and **http\\_5xx**.\n\n> This parameter takes effect only if the **HealthCheck** parameter is set to **on**."
      }
    },
    {
      "name": "HealthCheckInterval",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查的时间间隔。单位：秒。\n\n取值： **1**~**50**。\n\n>在**HealthCheck**值为**on**时才会有效。",
        "en": "The interval between two consecutive health checks. Unit: seconds.\n\nValid values: **1** to **50**.\n\n> This parameter takes effect only if the **HealthCheck** parameter is set to **on**."
      }
    },
    {
      "name": "HealthCheckMethod",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "监听HTTP类型健康检查的健康检查方法。取值：**head**或**get**。\n\n> **HealthCheck**值为**on**时才会有效。",
        "en": "The health check method used in HTTP health checks. Valid values: **head** and **get**.\n\n> This parameter takes effect only if the **HealthCheck** parameter is set to **on**."
      }
    },
    {
      "name": "HealthCheckTimeout",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "接收来自运行状况检查的响应需要等待的时间。如果后端ECS在指定的时间内没有正确响应，则判定为健康检查失败。单位：秒。\n\n取值：**1**~**300**。\n\n> 在**HealthCheck**值为**on**时才会有效。",
        "en": "The timeout period of a health check response. If a backend ECS instance does not respond within the specified timeout period, the ECS instance fails the health check. Unit: seconds\n\nValid values: **1** to **300**.\n\n> This parameter takes effect only if the **HealthCheck** parameter is set to **on**."
      }
    },
    {
      "name": "HealthCheckURI",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于健康检查的URI。\n\n长度限制为1~80个字符，只能使用字母、数字和短划线（-）、正斜线（/）、半角句号（.）、百分号（%）、半角问号（?）、井号（#）和and（&）这些字符。URI不能只为正斜线（/），但必须以正斜线（/）开头。\n\n>在**HealthCheck**值为**on**时才会有效。",
        "en": "The URI that is used for health checks.\n\nThe URI must be 1 to 80 characters in length, and can contain letters, digits, hyphens (-), forward slashes (/), periods (.), percent signs (%), question marks (?), number signs (#), and ampersands (&). The URI must start with a forward slash (/) but cannot be a single forward slash (/).\n\n> This parameter takes effect only if the **HealthCheck** parameter is set to **on**."
      }
    },
    {
      "name": "HealthyThreshold",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查连续成功多少次后，将后端服务器的健康检查状态由**失败**判定为**成功**。\n\n取值：**2**~**10**。\n\n>在**HealthCheck**值为**on**时才会有效。",
        "en": "The number of times that an unhealthy backend server must consecutively pass health checks before it is declared healthy. In this case, the health status is changed from **fail** to **success**.\n\nValid values: **2** to **10**.\n\n> This parameter takes effect only if the **HealthCheck** parameter is set to **on**."
      }
    },
    {
      "name": "IdleTimeout",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定连接空闲超时时间。单位：秒。\n\n默认值为**15**，取值范围：**1**~**60**。\n\n在超时时间内一直没有访问请求，负载均衡会暂时中断当前连接，直到一下次请求来临时重新建立新的连接。",
        "en": "The timeout period of an idle connection. Unit: seconds.\n\nDefault value: **15**. Valid values: **1** to **60**.\n\nIf no request is received within the specified timeout period, SLB closes the connection. When a request is received, SLB establishes a new connection."
      }
    },
    {
      "name": "ListenerForward",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启HTTP至HTTPS的转发。取值：\n- **on**：是。\n- **off**（默认值）：否。",
        "en": "Specifies whether to enable HTTP-to-HTTPS redirection. Valid values:\n\n- **on**: yes\n\n- **off** (default): no"
      }
    },
    {
      "name": "ListenerPort",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "实例前端使用的端口。\n\n取值：**1**~**65535**。",
        "en": "The frontend port that is used by the CLB instance.\n\nValid values: **1** to **65535**."
      }
    },
    {
      "name": "LoadBalancerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "传统型负载均衡实例ID。",
        "en": "The ID of the CLB instance."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "传统型负载均衡实例的地域ID。\n\n您可以从[地域和可用区](~~40654~~)列表或通过调用[DescribeRegions](~~27584~~)接口获取地域ID。",
        "en": "The ID of the region where the CLB instance is deployed.\n\nYou can query the region ID from the [Regions and zones](~~40654~~) list or by calling the [DescribeRegions](~~27584~~) operation."
      }
    },
    {
      "name": "RequestTimeout",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "指定请求超时时间。单位：秒。\n\n默认值为**60**，取值范围为**1**~**180**。\n\n在超时时间内后端服务器一直没有响应，负载均衡将放弃等待，给客户端返回` HTTP 504 `错误码。",
        "en": "The timeout period of a request. Unit: seconds.\n\nDefault value: **60**. Valid values: **1** to **180**.\n\nIf no response is received from the backend server within the specified timeout period, CLB sends an `HTTP 504` error code to the client."
      }
    },
    {
      "name": "Scheduler",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "调度算法。取值：\n\n- **wrr**（默认值）：权重值越高的后端服务器，被轮询到的概率也越高。\n\n- **rr**：按照访问顺序依次将外部请求依序分发到后端服务器。",
        "en": "The scheduling algorithm. Valid values:\n\n- **wrr** (default): Backend servers with higher weights receive more requests than those with lower weights.\n\n- **rr**: Requests are distributed to backend servers in sequence."
      }
    },
    {
      "name": "StickySession",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启会话保持。取值：\n\n- **on**：是。\n\n- **off**（默认值）：否。",
        "en": "Specifies whether to enable session persistence. Valid values:\n\n- **on**\n\n- **off** (default)"
      }
    },
    {
      "name": "StickySessionType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "Cookie处理方式。取值：\n\n- **insert**：植入Cookie。\n\n    客户端第一次访问时，负载均衡会在返回请求中植入Cookie，下次客户端携带此Cookie访问，负载均衡服务会将请求定向转发给之前记录到的后端服务器上。\n\n- **server**：重写Cookie。\n\n  负载均衡发现用户自定义了Cookie，将会对原来的Cookie进行重写，下次客户端携带新的Cookie访问，负载均衡服务会将请求定向转发给之前记录到的后端服务器。\n\n> 当**StickySession**值为**on**时，必须指定该参数。",
        "en": "The method that is used to handle cookies. Valid values:\n\n- **insert**: inserts a cookie.\n\n  The first time a client accesses CLB, CLB inserts a cookie into the response packet. Subsequent requests from the client that carry the cookie are distributed to the same backend server as the first request.\n\n- **server**: rewrites the original cookie.\n\n  CLB rewrites the custom cookies in requests from a client. Subsequent requests from the client that carry the new cookie are forwarded to the same backend server as the first request.\n\n> This parameter is required if the **StickySession** parameter is set to **on**."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表。",
        "en": "The tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "监听的标签键。N的取值范围：**1**~**20**。一旦传入该值，则不允许为空字符串。最多支持128个字符，不能以`aliyun`或`acs:`开头，不能包含`http://`或`https://`。",
            "en": "The tag key of the bastion host. Valid values of N: **1 to 20**. The tag key cannot be an empty string.\n\nThe tag key can be at most 64 characters in length, and cannot contain `http://` or `https://`. It must not start with `aliyun` or `acs:`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "监听的标签值。N的取值范围：**1**~**20**。一旦传入该值，可以为空字符串。最多支持128个字符，不能以`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The tag value. Valid values of N: **1 to 20**. The tag value can be an empty string.\n\nThe tag value can be up to 128 characters in length and cannot start with `acs:` or `aliyun`. The tag value cannot contain `http://` or `https://`."
          }
        }
      ]
    },
    {
      "name": "UnhealthyThreshold",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查连续失败多少次后，将后端服务器的健康检查状态由**成功**判定为**失败**。\n\n取值：**2**~**10**。\n\n>在**HealthCheck**值为**on**时才会有效。",
        "en": "The number of times that a healthy backend server must consecutively fail health checks before it is declared unhealthy. In this case, the health status is changed from **success** to **fail**.\n\nValid values: **2** to **10**.\n\n> This parameter takes effect only if the **HealthCheck** parameter is set to **on**."
      }
    },
    {
      "name": "VServerGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "虚拟服务器组ID。",
        "en": "The ID of the vServer group."
      }
    },
    {
      "name": "XForwardedFor",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启通过`X-Forwarded-For`头字段获取来访客户端IP。取值：\n- **on**（默认值）：是。\n- **off**：否。",
        "en": "Specifies whether to use the `X-Forwarded-For` header to retrieve client IP addresses. Valid values:\n\n- **on** (default)\n\n- **off**"
      }
    },
    {
      "name": "XForwardedFor_ClientSrcPort",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否通过`XForwardedFor_ClientSrcPort`头字段获取访问负载均衡实例客户端的端口。取值：\n- **on**：是。\n- **off**：否。",
        "en": "Specifies whether to use the `XForwardedFor_ClientSrcPort` header to retrieve the client port. Valid values:\n\n- **on**\n\n- **off**"
      }
    },
    {
      "name": "XForwardedFor_SLBID",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否通过`SLB-ID`头字段获取负载均衡实例ID。取值：\n- **on**：是。\n- **off**（默认值）：否。",
        "en": "Specifies whether to use the `SLB-ID` header to retrieve the ID of the CLB instance. Valid values:\n\n- **on**\n\n- **off** (default)"
      }
    },
    {
      "name": "XForwardedFor_SLBIP",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否通过`SLB-IP`头字段获取客户端请求的VIP（Virtual IP address）。取值：\n- **on**：是。\n- **off**（默认值）：否。",
        "en": "Specifies whether to use the `SLB-IP` header to retrieve the virtual IP address of the client. Valid values:\n\n- **on**\n\n- **off** (default)"
      }
    },
    {
      "name": "XForwardedFor_SLBPORT",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否通过`XForwardedFor_SLBPORT`头字段获取负载均衡实例的监听端口。取值：\n- **on**：是。\n- **off**：否。",
        "en": "Specifies whether to use the `XForwardedFor_SLBPORT` header to retrieve the listener port of the CLB instance. Valid values:\n\n- **on**\n\n- **off**"
      }
    },
    {
      "name": "XForwardedFor_proto",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否通过`X-Forwarded-Proto`头字段获取负载均衡实例的监听协议。取值：\n- **on**：是。\n- **off**（默认值）：否。",
        "en": "Specifies whether to use the `X-Forwarded-Proto` header to retrieve the listener protocol. Valid values:\n\n- **on**\n\n- **off** (default)"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建HTTP监听",
    "en": "CreateLoadBalancerHTTPListener"
  },
  "descriptions": {
    "zh": "### 注意事项\n\n 新建的监听的状态为**stopped**。创建完成后，调用[StartLoadBalancerListener](~~27597~~)接口启动监听来转发流量。\n\n### 前提条件\n\n您已经创建传统型负载均衡，具体操作，请参见[CreateLoadBalancer](~~27577~~)。",
    "en": "A newly created listener is in the **stopped** state. After a listener is created, you can call the [StartLoadBalancerListener](~~StartLoadBalancerListener~~) operation to start the listener. After the listener is started, the listener can forward traffic to backend servers.\n\n## Prerequisites\n\nA Classic Load Balancer (CLB) instance is created. For more information, see [CreateLoadBalancer](~~StartLoadBalancerListener~~)."
  },
  "example": {
    "unifiedCli": "aliyun slb create-load-balancer-http-listener --biz-region-id cn-hangzhou --load-balancer-id lb-bp1c9vixxjh92q83tw00000 --bandwidth -1 --listener-port 80 --backend-server-port 80 --xforwarded-for on --scheduler wrr --sticky-session off --sticky-session-type insert --tag Value=TestValue Key=TestKey --cookie-timeout 500 --cookie testCookie --health-check on --health-check-method get --health-check-domain 172.16.0.0/12 --health-check-uri /test/index.html --healthy-threshold 4 --unhealthy-threshold 4 --health-check-timeout 3 --health-check-connect-port 80 --health-check-interval 5 --health-check-http-code http_2xx --vserver-group-id rsp-cige6j00000 --xforwarded-for-slb-ip on --xforwarded-for-slb-id on --xforwarded-for-proto on --gzip on --acl-id acl-uf60jw000000 --acl-type white --acl-status off --description HTTP_443 --listener-forward off --forward-port 443 --idle-timeout 3 --request-timeout 6 --xforwarded-for-slbport off --xforwarded-for-client-src-port off --api-version 2014-05-15 --region cn-hangzhou",
    "legacyCli": "aliyun slb CreateLoadBalancerHTTPListener --region cn-hangzhou --CookieTimeout 500 --VServerGroupId 'rsp-cige6j00000' --Description HTTP_443 --UnhealthyThreshold 4 --Scheduler wrr --HealthCheckURI '/test/index.html' --HealthCheck on --IdleTimeout 3 --LoadBalancerId 'lb-bp1c9vixxjh92q83tw00000' --BackendServerPort 80 --XForwardedFor_SLBID on --HealthCheckConnectPort 80 --XForwardedFor_SLBPORT off --HealthCheckMethod get --Bandwidth '-1' --Gzip on --StickySessionType insert --XForwardedFor_SLBIP on --HealthCheckHttpCode http_2xx --Cookie testCookie --ListenerPort 80 --RequestTimeout 6 --Tag.1.Key TestKey --HealthCheckInterval 5 --AclId 'acl-uf60jw000000' --HealthCheckTimeout 3 --ListenerForward off --StickySession off --AclStatus off --ForwardPort 443 --HealthyThreshold 4 --Tag.1.Value TestValue --XForwardedFor on --XForwardedFor_ClientSrcPort off --HealthCheckDomain '172.16.0.0/12' --XForwardedFor_proto on --AclType white --RegionId 'cn-hangzhou'"
  }
}