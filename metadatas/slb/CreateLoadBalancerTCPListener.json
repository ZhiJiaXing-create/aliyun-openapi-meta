{
  "name": "CreateLoadBalancerTCPListener",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AclId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "监听绑定的访问策略组ID。\n\n> 当**AclStatus**参数的值为**on**时，该参数必选。",
        "en": "The ID of the network ACL that is associated with the listener.\n\n> If **AclStatus** is set to **on**, this parameter is required."
      }
    },
    {
      "name": "AclStatus",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启访问控制功能。取值：\n\n- **on**：开启。\n- **off**（默认值）：不开启。",
        "en": "Specifies whether to enable access control. Valid values:\n\n- **on**: yes\n\n- **off** (default): no"
      }
    },
    {
      "name": "AclType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "访问控制类型：\n\n* **white**： 仅转发来自所选访问控制策略组中设置的IP地址或地址段的请求，白名单适用于只允许特定IP访问的场景。\n设置白名单存在一定业务风险。\n \n 一旦设置白名单，就只有白名单中的IP可以访问负载均衡监听。如果开启了白名单访问，但访问策略组中没有添加任何IP，则负载均衡监听会转发全部请求。\n\n* **black**： 来自所选访问控制策略组中设置的IP地址或地址段的所有请求都不会转发，黑名单适用于只限制某些特定IP访问的场景。\n\n 如果开启了黑名单访问，但访问策略组中没有添加任何IP，则负载均衡监听会转发全部请求。\n\n> 当**AclStatus**参数的值为**on**时，该参数有效。",
        "en": "The type of the ACL. Valid values:\n\n- **white**: a whitelist. Only requests from the IP addresses or CIDR blocks in the network ACL are forwarded. Whitelists apply to scenarios where you want to allow only specific IP addresses to access an application.\n\n  Your service may be adversely affected if the whitelist is not properly configured.\n\n  If a whitelist is configured, only requests from IP addresses that are added to the whitelist are forwarded by the listener. If a whitelist is configured but no IP address is added to the whitelist, the listener forwards all requests.\n\n- **black**: a blacklist. All requests from the IP addresses or CIDR blocks in the ACL are rejected. Blacklists apply to scenarios where you want to block access from specified IP addresses to an application.\n\n  If a blacklist is configured for a listener but no IP address is added to the blacklist, the listener forwards all requests.\n\n> If **AclStatus** is set to **on**, this parameter is required."
      }
    },
    {
      "name": "BackendServerPort",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "传统型负载均衡实例后端使用的端口。\n\n取值范围：**1**~**65535**。\n\n如果不使用服务器组（不指定**vServerGroupId**），则该参数必选。",
        "en": "The backend port used by the CLB instance.\n\nValid values: **1** to **65535**.\n\nIf the **VServerGroupId** parameter is not set, this parameter is required."
      }
    },
    {
      "name": "Bandwidth",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "监听的带宽峰值，单位：Mbps。取值：\n\n- **-1**：对于按流量计费的公网负载均衡实例，可以将带宽峰值设置为-1，即不限制带宽峰值。\n\n- **1**~**5120**： 对于按带宽计费的公网负载均衡实例，可以设置每个监听的带宽峰值，但所有监听的带宽峰值之和不能超过实例的带宽峰值。",
        "en": "The maximum bandwidth of the listener. Unit: Mbit/s. Valid values:\n\n- **-1**: For a pay-by-data-transfer Internet-facing CLB instance, this value can be set to -1, which specifies unlimited bandwidth.\n\n- **1** to **5120**: For a pay-by-bandwidth Internet-facing CLB instance, you can specify the maximum bandwidth of each listener. The sum of the maximum bandwidth values that you set for all listeners cannot exceed the maximum bandwidth of the CLB instance."
      }
    },
    {
      "name": "ConnectionDrain",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启连接优雅中断。取值：\n\n- **on**：开启。\n\n- **off**：不开启。",
        "en": "Specifies whether to enable connection draining. Valid values:\n\n- **on**: yes\n\n- **off**: no"
      }
    },
    {
      "name": "ConnectionDrainTimeout",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "设置连接优雅中断超时时间。单位：秒。\n\n取值范围：**10**~**900**。\n\n> 当**ConnectionDrain**取值为**on**时，该选项必选。",
        "en": "The timeout period of connection draining. Unit: seconds.\n\nValid values: **10** to **900**.\n\n> This parameter is required if **ConnectionDrain** is set to **on**."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "自定义监听名称。\n\n长度限制为1~256个字符，支持中文、字母、数字、短划线（-）、正斜线（/）、半角句号（.）和下划线（_）。",
        "en": "The name of the listener.\n\nThe name must be 1 to 256 characters in length, and can contain letters, digits, hyphens (-), forward slashes (/), periods (.), and underscores (\\_)."
      }
    },
    {
      "name": "EstablishedTimeout",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "连接超时时间。单位：秒。\n\n取值范围：**10**~**900**。",
        "en": "The timeout period of a connection. Unit: seconds\n\nValid values: **10** to **900**."
      }
    },
    {
      "name": "HealthCheckConnectPort",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查使用的端口。\n\n取值范围：**1**~**65535**。\n\n不设置此参数时，表示使用后端服务端口（**BackendServerPort**）。",
        "en": "The port that is used for health checks.\n\nValid values: **1** to **65535**.\n\nIf this parameter is not set, the backend port specified by **BackendServerPort** is used for health checks."
      }
    },
    {
      "name": "HealthCheckConnectTimeout",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "每次健康检查响应的最大超时时间。单位：秒。\n\n取值范围：**1**~**300**。\n\n默认值：**5**。",
        "en": "The maximum timeout period of a health check response. Unit: seconds\n\nValid values: **1** to **300**\n\nDefault value: **5**"
      }
    },
    {
      "name": "HealthCheckDomain",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于健康检查的域名。取值：\n\n* **$_ip**： 后端服务器的私网IP。当指定了IP或该参数未指定时，负载均衡会使用各后端服务器的私网IP当做健康检查使用的域名。\n\n* **domain**：域名长度为1~80，只能包含字母、数字、半角句号（.）和短划线（-）。",
        "en": "The domain name that you want to use for health checks. Valid values:\n\n- **$\\_ip**: the private IP address of a backend server. If you do not set the HealthCheckDomain parameter or set the parameter to $\\_ip, the CLB instance uses the private IP address of each backend server for health checks.\n\n- **domain**: The domain name must be 1 to 80 characters in length, and can contain letters, digits, periods (.), and hyphens (-)."
      }
    },
    {
      "name": "HealthCheckHttpCode",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "健康检查正常的HTTP状态码，多个状态码用半角逗号（,）分割。取值：\n\n- **http_2xx**（默认值）\n- **http_3xx**\n- **http_4xx**\n- **http_5xx**",
        "en": "The HTTP status code for a successful health check. Separate multiple HTTP status codes with commas (,). Valid values:\n\n- **http\\_2xx**(default)\n\n- **http\\_3xx**\n\n- **http\\_4xx**\n\n- **http\\_5xx**"
      }
    },
    {
      "name": "HealthCheckSwitch",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "是否开启健康检查。取值：\n\n- **on**（默认值）：开启。\n- **off**：不开启。",
        "en": "Specifies whether to enable the health check feature. Valid values:\n\n- **on** (default): yes\n\n- **off**: no"
      }
    },
    {
      "name": "HealthCheckType",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "健康检查类型。取值：\n\n- **tcp**（默认值）\n- **http**",
        "en": "The type of health checks. Valid values:\n\n- **tcp** (default)\n\n- **http**"
      }
    },
    {
      "name": "HealthCheckURI",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "用于健康检查的URI。长度限制为1~80，只能使用字母、数字、短划线（-）、正斜线（/）、半角句号（.）、百分号（%）、井号（#）和and（&）这些字符。 URI不能只为正斜线（/），但必须以正斜线（/）开头。\n\n当TCP监听需要使用HTTP健康检查时可配置此参数，如不配置则按TCP健康检查。",
        "en": "The URI that is used for health checks. The URI must be 1 to 80 characters in length, and can contain only digits, letters, hyphens (-), forward slashes (/), periods (.), percent signs (%), number signs (#), and ampersands (&). The URI must start with a forward slash (/) but cannot be a single forward slash (/).\n\nYou can set this parameter when the TCP listener requires HTTP health checks. If you do not set this parameter, TCP health checks are performed."
      }
    },
    {
      "name": "HealthyThreshold",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查连续成功多少次后，将后端服务器的健康检查状态由**fail**判定为**success**。\n\n取值范围： **2**~**10**。",
        "en": "The number of times that an unhealthy backend server must consecutively pass health checks before it is declared healthy. In this case, the health status is changed from **fail** to **success**.\n\nValid values: **2** to **10**"
      }
    },
    {
      "name": "ListenerPort",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "传统型负载均衡实例前端使用的端口。\n\n取值范围：**1**~**65535**。",
        "en": "The frontend port used by the CLB instance.\n\nValid values: **1** to **65535**."
      }
    },
    {
      "name": "LoadBalancerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "传统型负载均衡实例的ID。",
        "en": "The ID of the CLB instance."
      }
    },
    {
      "name": "MasterSlaveServerGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "主备服务器组ID。\n\n> 虚拟服务器组ID和主备服务器组ID只能选择一个。",
        "en": "The ID of the primary/secondary server group.\n\n> You cannot set both VServerGroupId and MasterSlaveServerGroupId."
      }
    },
    {
      "name": "PersistenceTimeout",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "会话保持的超时时间。单位：秒。\n\n取值范围：**0~3600**。\n\n默认值：**0**，表示关闭会话保持。",
        "en": "The timeout period of session persistence. Unit: seconds\n\nValid values: **0 to 3600**\n\nDefault value: **0**. If the default value is used, the system disables session persistence."
      }
    },
    {
      "name": "ProxyProtocolV2Enabled",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否支持通过Proxy Protocol协议携带客户端源地址到后端服务器。取值：\n\n- **true**：是。\n- **false**（默认值）：否。",
        "en": "Specifies whether to use the Proxy protocol to pass client IP addresses to backend servers. Valid values:\n\n- **true**: yes\n\n- **false** (default): no"
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "传统型负载均衡实例的地域ID。\n\n您可以从[地域和可用区](~~40654~~)列表或通过调用[DescribeRegions](~~2401682~~)接口查询地域ID。",
        "en": "The ID of the region where the Classic Load Balancer (CLB) instance is deployed.\n\nFor the most recent region list, refer to [Regions and zones](~~40654~~) or call the [DescribeRegions](~~2401682~~) operation the retrieve the information."
      }
    },
    {
      "name": "Scheduler",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "调度算法。取值：\n\n- **wrr**（默认值）：权重值越高的后端服务器，被轮询到的次数（概率）也越高。\n\n- **rr**：按照访问顺序依次将外部请求依序分发到后端服务器。\n\n- **sch**：基于源IP地址的一致性hash，相同的源地址会调度到相同的后端服务器。\n\n- **tch**：基于四元组的一致性hash（源IP+目的IP+源端口+目的端口），相同的流会调度到相同的后端服务器。\n\n> 仅性能保障型实例支持**sch**和**tch**一致性hash算法。",
        "en": "The scheduling algorithm. Valid values:\n\n- **wrr** (default): Backend servers with higher weights receive more requests than those with lower weights.\n\n- **rr**: Requests are distributed to backend servers in sequence.\n\n- **sch**: specifies consistent hashing that is based on source IP addresses. Requests from the same source IP address are distributed to the same backend server.\n\n- **tch**: specifies consistent hashing that is based on four factors: source IP address, destination IP address, source port, and destination port. Requests that contain the same information based on the four factors are distributed to the same backend server.\n\n> Only high-performance CLB instances support the **sch** and **tch** consistent hashing algorithms."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表。",
        "en": "The tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "监听的标签键。N的取值范围：**1**~**20**。一旦传入该值，则不允许为空字符串。最多支持64个字符，不能以`aliyun`或`acs:`开头，不能包含`http://`或`https://`。",
            "en": "The key of the tag. You can specify up to 20 tag keys. The tag key cannot be an empty string.\n\nThe tag key must be 1 to 64 characters in length and cannot start with `aliyun` or `acs:`. It cannot contain `http://` or `https://`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "监听的标签值。N的取值范围：**1**~**20**。一旦传入该值，可以为空字符串。最多支持128个字符，不能以`acs:`开头，不能包含`http://`或者`https://`。",
            "en": "The value of the tag. You can specify up to 20 tag values. The tag value can be an empty string.\n\nThe tag value can be at most 128 characters in length and cannot start with `acs:` or `aliyun`. It cannot contain `http://` or `https://`."
          }
        }
      ]
    },
    {
      "name": "UnhealthyThreshold",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查连续失败多少次后，将后端服务器的健康检查状态由**success**判定为**fail**。\n\n取值范围：**2**~**10**。",
        "en": "The number of times that a healthy backend server must consecutively fail health checks before it is declared unhealthy. In this case, the health status is changed from **success** to **fail**.\n\nValid values: **2** to **10**"
      }
    },
    {
      "name": "VServerGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "虚拟服务器组ID。",
        "en": "The ID of the vServer group."
      }
    },
    {
      "name": "healthCheckInterval",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "健康检查的时间间隔。单位：秒。\n\n取值范围：**1**~**50**。",
        "en": "The interval between two consecutive health checks. Unit: seconds.\n\nValid values: **1** to **50**."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建TCP监听",
    "en": "CreateLoadBalancerTCPListener"
  },
  "descriptions": {
    "zh": "> 新建的监听的状态为**stopped**。创建完成后，调用[StartLoadBalancerListener](~~2401757~~)接口启动监听来转发流量。",
    "en": "> Newly created listeners are in the **stopped** state. After a listener is created, call the [StartLoadBalancerListener](~~2401757~~) operation to enable the listener to forward network traffic."
  },
  "example": {
    "unifiedCli": "aliyun slb create-load-balancer-tcp-listener --biz-region-id cn-hangzhou --load-balancer-id lb-bp1b6c719dfa08ex0000 --listener-port 80 --backend-server-port 80 --tag Value=TestValue Key=TestKey --bandwidth -1 --scheduler wrr --persistence-timeout 0 --established-timeout 500 --healthy-threshold 4 --unhealthy-threshold 4 --health-check-connect-timeout 100 --health-check-connect-port 80 --health-check-interval 3 --health-check-domain 172.XX.XX.6 --health-check-uri /test/index.html --health-check-http-code http_2xx,http_3xx --health-check-type tcp --vserver-group-id rsp-cige6j0000 --master-slave-server-group-id rsp-0bfucw0000 --acl-id acl-uf60jwfiv6000000 --acl-type black --acl-status off --description tcp_80 --connection-drain off --connection-drain-timeout 300 --proxy-protocol-v2-enabled false --health-check-switch on --api-version 2014-05-15 --region cn-hangzhou",
    "legacyCli": "aliyun slb CreateLoadBalancerTCPListener --region cn-hangzhou --VServerGroupId 'rsp-cige6j0000' --Description tcp_80 --UnhealthyThreshold 4 --ProxyProtocolV2Enabled false --Scheduler wrr --HealthCheckURI '/test/index.html' --LoadBalancerId 'lb-bp1b6c719dfa08ex0000' --BackendServerPort 80 --healthCheckInterval 3 --ConnectionDrainTimeout 300 --PersistenceTimeout 0 --HealthCheckConnectPort 80 --Bandwidth '-1' --HealthCheckHttpCode 'http_2xx,http_3xx' --EstablishedTimeout 500 --ListenerPort 80 --Tag.1.Key TestKey --AclId 'acl-uf60jwfiv6000000' --HealthCheckConnectTimeout 100 --ConnectionDrain off --HealthCheckSwitch on --AclStatus off --HealthyThreshold 4 --Tag.1.Value TestValue --HealthCheckDomain '172.XX.XX.6' --MasterSlaveServerGroupId 'rsp-0bfucw0000' --AclType black --RegionId 'cn-hangzhou' --HealthCheckType tcp"
  }
}