{
  "name": "RemoveListenerWhiteListItem",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ListenerPort",
      "position": "Query",
      "type": "Integer",
      "required": true,
      "description": {
        "zh": "监听端口。",
        "en": "The listening port."
      }
    },
    {
      "name": "ListenerProtocol",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "负载均衡实例前端使用的协议。\n\n>相同端口存在不同协议监听时，此字段必填。",
        "en": "The frontend protocol that is used by the CLB instance.\n\n> This parameter is required when listeners that use different protocols listen on the same port."
      }
    },
    {
      "name": "LoadBalancerId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "负载均衡实例的ID。",
        "en": "The ID of the CLB instance."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "负载均衡实例的地域。\n\n您可以通过调用[DescribeRegions](~~27584~~)接口查询地域ID。",
        "en": "The region where the Classic Load Balancer (CLB) instance is created.\n\nYou can call the [DescribeRegions](~~27584~~) operation to query the most recent region list."
      }
    },
    {
      "name": "SourceItems",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "访问控制列表。支持输入IP地址或IP地址段（CIDR block形式），多个IP地址或地址段用逗号（,）分隔。\n\n> 如果所有IP都被删除，则无法访问该监听。",
        "en": "The list of IP addresses or CIDR blocks that you want to remove from the whitelist. Separate multiple IP addresses or CIDR blocks with commas (,).\n\n> If all IP addresses are removed from the whitelist, the listener does not forward requests."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除监听白名单中的IP",
    "en": "RemoveListenerWhiteListItem"
  },
  "example": {
    "unifiedCli": "aliyun slb remove-listener-white-list-item --biz-region-id cn-hangzhou --load-balancer-id lb-8vb86hxixo8lvsja80000 --listener-port 80 --source-items 192.168.XX.XX --listener-protocol https --api-version 2014-05-15 --region cn-hangzhou",
    "legacyCli": "aliyun slb RemoveListenerWhiteListItem --region cn-hangzhou --ListenerPort 80 --LoadBalancerId 'lb-8vb86hxixo8lvsja80000' --RegionId 'cn-hangzhou' --ListenerProtocol https --SourceItems '192.168.XX.XX'"
  }
}