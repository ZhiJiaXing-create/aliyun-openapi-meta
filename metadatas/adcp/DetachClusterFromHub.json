{
  "name": "DetachClusterFromHub",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClusterId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "舰队ID。您可以调用[DescribeHubClusters](~~424404~~)接口获取已创建的舰队Fleet实例。",
        "en": "The fleet ID. You can call the [DescribeHubClusters](~~424404~~) operation to obtain the IDs of created fleet instances."
      }
    },
    {
      "name": "ClusterIds",
      "position": "Body",
      "type": "String",
      "required": true,
      "description": {
        "zh": "从舰队中待移除的子集群ID数组。您可以调用[DescribeManagedClusters](~~424408~~)接口获取主集群下已添加的子集群ID。\n\n取值格式为JSON格式的字符串，可以被解析为一个字符串数组。",
        "en": "The array of IDs for the child clusters to remove from the fleet. You can call the [DescribeManagedClusters](~~424408~~) operation to obtain the IDs of child clusters that are added to the fleet.\n\nThe value is a JSON string that can be parsed into a string array."
      }
    },
    {
      "name": "DetachFromMesh",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否仅从服务网格（ASM）中移除子集群。\n- true：仅从服务网格（ASM）移除子集群。\n- false：同时从服务网格（ASM）和ACK One中移除子集群。\n\n若该参数未设置，默认按照取值为**false**处理，即默认子集群同时从服务网格（ASM）和ACK One中移除。",
        "en": "Specifies whether to remove the child cluster only from the Service Mesh (ASM) instance.\n\n- true: Removes the child cluster only from the ASM instance.\n\n- false: Removes the child cluster from both the ASM instance and ACK One.\n\nIf this parameter is not set, the default value is **false**. The child cluster is removed from both the ASM instance and ACK One."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "从舰队Fleet实例中移除子集群",
    "en": "DetachClusterFromHub"
  },
  "example": {
    "unifiedCli": "aliyun adcp detach-cluster-from-hub --cluster-id cb09fda0dc2f94a8397c76638c7ec0000 --cluster-ids c1c731554c1ec4a1ca9bb690ff9ed0000 --detach-from-mesh false --region cn-hangzhou",
    "legacyCli": "aliyun adcp DetachClusterFromHub --region cn-hangzhou --ClusterId cb09fda0dc2f94a8397c76638c7ec0000 --DetachFromMesh false --ClusterIds c1c731554c1ec4a1ca9bb690ff9ed0000"
  }
}