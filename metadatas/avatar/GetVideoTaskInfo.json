{
  "name": "GetVideoTaskInfo",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET",
  "pathPattern": "",
  "parameters": [
    {
      "name": "App",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "代表一个业务配置",
        "en": "Represents a business configuration"
      }
    },
    {
      "name": "TaskUuid",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "任务UUID，提交对应视频生成任务时返回的TaskUuid。",
        "en": "The job UUID, which is returned when you submit the corresponding Video generation job."
      }
    },
    {
      "name": "TenantId",
      "position": "Query",
      "type": "Long",
      "required": true,
      "description": {
        "zh": "从虚拟数字人开放平台中获取的TenantId",
        "en": "The TenantId obtained from the digitized virtual human Open Platform."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询视频合成任务详情",
    "en": "Query details of a video synthesis job"
  },
  "descriptions": {
    "zh": "根据在成功提交视频合成任务之后获取到的TaskUuid可以通过该接口查询视频合成任务状态，由于视频合成任务是离线运行，所以任务执行时间不定，可能需要调用方能够定时调用该接口查询任务状态，直到任务状态为已完成（成功或者失败），针对成功的任务接口会返回任务合成详情， 包含视频和字幕的下载地址，失败的任务会返回失败原因。",
    "en": "After successfully submitting a video synthesis Job, you can use the obtained TaskUuid to query the Job status via this API. Since video synthesis Jobs run offline, the running time varies. Therefore, the caller should periodically invoke this API to check the Job status until it reaches a final state (either Succeeded or failed). For Succeeded Jobs, the API returns synthesis details, including download URLs for the Video and caption. For failed Jobs, the API returns the failure reason."
  }
}