{
  "name": "CreateCommand",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "CommandContent",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "命令内容。您需要注意：\n\n- 指定参数`EnableParameter=true`可在命令内容中启用自定义参数功能：\n- 用{{}}包含的方式定义自定义参数，在`{{}}`内参数名前后的空格以及换行符会被忽略。\n- 自定义参数个数不能超过20个。\n- 自定义参数名允许a-zA-Z0-9-_的组合，不支持其余字符，参数名不区分大小写。\n- 单个自定义参数名不能超过64字节。",
        "en": "The content of the command. Note the following:\n\n- Set `EnableParameter` to \\`true\\` to enable custom parameters in the command content.\n\n- Define custom parameters in the \\`{{}}\\` format. Spaces and line feeds before and after parameter names within \\`{{}}\\` are ignored.\n\n- You can define up to 20 custom parameters.\n\n- Custom parameter names can contain letters, digits, hyphens (-), and underscores (\\_). The names are case-insensitive.\n\n- A single custom parameter name cannot exceed 64 bytes in length."
      }
    },
    {
      "name": "Description",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "命令描述，支持全字符集。长度不得超过512个字符。",
        "en": "The description of the command. All character sets are supported. The description can be up to 512 characters long."
      }
    },
    {
      "name": "EnableParameter",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "创建的命令是否使用自定义参数。\n\n默认值为false。",
        "en": "Specifies whether to use custom parameters in the command.\n\nDefault value: false."
      }
    },
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "命令名称，支持全字符集。长度不得超过128个字符。",
        "en": "The name of the command. All character sets are supported. The name can be up to 128 characters long."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "地域ID。您可以调用[ListRegions](~~189315~~)查询可用地域。",
        "en": "The region ID. Call [ListRegions](~~189315~~) to query the available regions."
      }
    },
    {
      "name": "ResourceGroupId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "资源组ID。",
        "en": "The ID of the resource group."
      }
    },
    {
      "name": "Tag",
      "position": "Query",
      "type": "RepeatList",
      "required": false,
      "description": {
        "zh": "标签列表，最多包含20个子项。",
        "en": "The tags. You can add up to 20 tags."
      },
      "sub_parameters": [
        {
          "name": "Key",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "为命令指定的标签键。N 的取值范围：1~20。\n\n一旦传入该值，则不允许为空字符串。最多支持 64 个字符，不能以 aliyun 和 acs:开头，不能包含 http://或者 https://。",
            "en": "The key of tag N to add to the command. Valid values of N: 1 to 20.\n\nThe tag key cannot be an empty string. It can be up to 64 characters in length and cannot start with \\`aliyun\\` or \\`acs:\\`. It cannot contain \\`http\\://\\` or \\`https\\://\\`."
          }
        },
        {
          "name": "Value",
          "position": "Query",
          "type": "String",
          "required": false,
          "description": {
            "zh": "为命令指定的标签值。N 的取值范围：1~20。\n\n一旦传入该值，可以为空字符串。最多支持 64 个字符，不能包含 http://或者 https://。",
            "en": "The value of tag N to add to the command. Valid values of N: 1 to 20.\n\nThe tag value can be an empty string. It can be up to 64 characters in length and cannot contain \\`http\\://\\` or \\`https\\://\\`."
          }
        }
      ]
    },
    {
      "name": "Timeout",
      "position": "Query",
      "type": "Long",
      "required": false,
      "description": {
        "zh": "设置命令在服务器实例中的超时时间，单位为秒。\n\n当执行命令的任务超时后，命令助手将强制终止任务进程。可设定超时时间取值范围为10～86400秒（24小时）。\n\n默认值：60。",
        "en": "The timeout period for the command to run on the server instance, in seconds.\n\nWhen a command task times out, Cloud Assistant forcibly stops the task process. The timeout period must be between 10 and 86,400 seconds (24 hours).\n\nDefault value: 60."
      }
    },
    {
      "name": "Type",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "命令的语言类型。取值范围：\n\n- RunBatScript：适用于Windows实例的Bat命令。\n- RunPowerShellScript：适用于Windows实例的PowerShell命令。\n- RunShellScript：适用于Linux实例Shell命令。",
        "en": "The language type of the command. Valid values:\n\n- RunBatScript: Bat commands for Windows instances.\n\n- RunPowerShellScript: PowerShell commands for Windows instances.\n\n- RunShellScript: Shell commands for Linux instances."
      }
    },
    {
      "name": "WorkingDir",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "您创建的命令在ECS实例中运行的目录。\n\n默认值： \n- Linux实例：管理员root用户的home目录下，具体为`/root`目录。  \n- Windows实例：云助手客户端进程所在目录，例如`C:\\Windows\\System32`。",
        "en": "The directory where the command is run on the ECS instance.\n\nDefault values:\n\n- For Linux instances: The home directory of the \\`root\\` user, which is the `/root` directory.\n\n- For Windows instances: The directory where the Cloud Assistant client process is located, such as `C:\\Windows\\System32`."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "创建命令",
    "en": "CreateCommand"
  }
}