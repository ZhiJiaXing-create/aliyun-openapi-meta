{
  "name": "AttachKeyPair",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。**ClientToken**只支持ASCII字符，且不能超过64个字符。更多信息，请参见[如何保证幂等性](~~25693~~)。",
        "en": "The client token that is used to ensure the idempotence of the request. You can use the client to generate the token, but you must make sure that the token is unique among different requests. The **token** can contain only ASCII characters and cannot exceed 64 characters in length. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "InstanceIds",
      "position": "Query",
      "type": "RepeatList",
      "required": true,
      "description": {
        "zh": "轻量应用服务器实例ID。最多支持50个实例ID。",
        "en": "The IDs of simple application servers. You can specify a maximum of 50 IDs of simple application servers."
      }
    },
    {
      "name": "KeyPairName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "密钥对名称。长度为2~64个英文或中文字符。必须以大小写字母或中文开头，不能以http://或https://开头。可以包含数字、半角冒号（:）、下划线（_）或者短划线（-）。",
        "en": "The name of the key pair that you want to bind to the simple application server. The name must be 2 to 64 characters in length and can contain letters, digits, colons (:), underscores (\\_), and hyphens (-). The name must start with a letter but cannot start with http\\:// or https\\://."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定的轻量数据库实例所属的地域ID。\n您可以调用[ListRegions](~~189315~~)查看支持的阿里云地域列表。",
        "en": "The region ID of the simple application server. You can call the [ListRegions](~~189315~~) operation to query the most recent region list."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "绑定密钥对",
    "en": "AttachKeyPair"
  },
  "descriptions": {
    "zh": "一台轻量应用服务器只支持在控制台绑定一个密钥对。若您选择的轻量应用服务器已经绑定过其他密钥对，新绑定的密钥对将会覆盖已绑定的密钥对。",
    "en": "You can bind only one key pair to a simple application server in the Simple Application Server console. If a simple application server has a key pair bound, the new key pair overwrites the original key pair."
  }
}