{
  "name": "ImportKeyPair",
  "deprecated": false,
  "protocol": "HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ClientToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "保证请求幂等性。从您的客户端生成一个参数值，确保不同请求间该参数值唯一。**ClientToken**只支持ASCII字符，且不能超过64个字符。更多信息，请参见[如何保证幂等性](~~25693~~)。",
        "en": "A client token used to ensure the idempotence of the request. Generate a unique value for this parameter from your client. **ClientToken** can contain only ASCII characters and must be no more than 64 characters long. For more information, see [How to ensure idempotence](~~25693~~)."
      }
    },
    {
      "name": "KeyPairName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "密钥对名称。长度为2~64个英文或中文字符。必须以大小写字母或中文开头，不能以http://或https://开头。可以包含数字、半角冒号（:）、下划线（_）或者短划线（-）。",
        "en": "The name of the key pair. The name must be 2 to 64 characters in length. It must start with a letter or a Chinese character. It cannot start with http\\:// or https\\://. The name can contain digits, colons (:), underscores (\\_), and hyphens (-)."
      }
    },
    {
      "name": "PublicKeyBody",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "密钥对的公钥内容。",
        "en": "The content of the public key of the key pair."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "指定的轻量应用服务器实例所属的地域ID。\n您可以调用[ListRegions](~~189315~~)查看支持的阿里云地域列表。",
        "en": "The ID of the region where the Simple Application Server instance is located.\nCall [ListRegions](~~189315~~) to view a list of supported Alibaba Cloud regions."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "导入密钥对",
    "en": "ImportKeyPair"
  },
  "descriptions": {
    "zh": "如果您已有密钥对，可以将已有密钥对导入控制台，便于使用已有密钥对登录轻量应用服务器。但已有密钥对必须为支持的加密方式，具体说明请参见[Q2：导入已有密钥对支持哪些加密方式？](~~59085~~)。",
    "en": "You can import an existing key pair to the console to log on to your Simple Application Server instances. The key pair must use a supported encryption method. For more information, see [Q2: What encryption methods are supported for importing an existing key pair?](~~59085~~)."
  }
}