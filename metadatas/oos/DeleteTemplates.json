{
  "name": "DeleteTemplates",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "AutoDeleteExecutions",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "删除模版时，是否顺带删除其相关的执行。",
        "en": "Specify whether to delete the related executions when deleting the templates."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID",
        "en": "The region ID."
      }
    },
    {
      "name": "TemplateNames",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "将要删除模版名称列表",
        "en": "The list of template names to be deleted."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除多个模版",
    "en": "DeleteTemplates"
  },
  "example": {
    "unifiedCli": "aliyun oos delete-templates --biz-region-id cn-hangzhou --template-names t1_,_t2 --auto-delete-executions false --region cn-hangzhou",
    "legacyCli": "aliyun oos DeleteTemplates --region cn-hangzhou --TemplateNames 't1_,_t2' --AutoDeleteExecutions false --RegionId 'cn-hangzhou'"
  }
}