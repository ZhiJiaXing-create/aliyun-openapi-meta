{
  "name": "ListActions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小。取值范围：10-100。默认为50。",
        "en": "The number of entries to return on each page. Valid values: 20 to 100. Default value: 50."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "翻页标记的Token。",
        "en": "The token that is used to retrieve the next page of results."
      }
    },
    {
      "name": "OOSActionName",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "动作名称。名称未包含关系，最终列出包含所填动作名称的所有动作。",
        "en": "The name of the action. All actions whose names contain the specified action name are returned."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID",
        "en": "The ID of the region."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "查询可用动作",
    "en": "ListActions"
  },
  "example": {
    "unifiedCli": "aliyun oos list-actions --biz-region-id cn-hangzhou --oos-action-name MyTemplate --max-results 50 --next-token - --region cn-hangzhou",
    "legacyCli": "aliyun oos ListActions --region cn-hangzhou --OOSActionName MyTemplate --NextToken '-' --MaxResults 50 --RegionId 'cn-hangzhou'"
  }
}