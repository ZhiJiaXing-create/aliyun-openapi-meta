{
  "name": "GetSecretParametersByPath",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "MaxResults",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "分页大小。取值范围：1-10。默认为10。",
        "en": "The number of entries to return on each page. Valid values: 1 to 10. Default value: 10."
      }
    },
    {
      "name": "NextToken",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "翻页标记的Token。",
        "en": "The pagination token."
      }
    },
    {
      "name": "Path",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "参数路径，参数长度小于200个字符串。例如参数/secretParameter/mySecretParameter，其中/secretParameter为此参数的路径。",
        "en": "The path of the parameter. The path must be less than 200 characters in length. For example, if the name of a parameter is /secretParameter/mySecretParameter, the path of the parameter is /secretParameter."
      }
    },
    {
      "name": "Recursive",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否递归查询路径。如参数/secretParameter/mySecretParameter与/secretParameter/secretParameter1/mySecretParameter，取值范围：\n- true（搜索出//secretParameter/mySecretParameter与/secretParameter/secretParameter1/mySecretParameter两个结果）\n- false（仅能搜索出/secretParameter/mySecretParameter这一个结果）",
        "en": "Specifies whether to recursively query parameters in a path. For example, you have the parameters /secretParameter/mySecretParameter and /secretParameter/secretParameter1/mySecretParameter. Valid values:\n\n- true: Returns both /secretParameter/mySecretParameter and /secretParameter/secretParameter1/mySecretParameter.\n\n- false: Returns only /secretParameter/mySecretParameter."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "WithDecryption",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解密参数值。\n取值范围：\n- true\n- false（默认）",
        "en": "Specifies whether to decrypt the parameter value.\nValid values:\n\n- true\n\n- false (Default)"
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "通过路径获取加密参数",
    "en": "GetSecretParametersByPath"
  },
  "example": {
    "unifiedCli": "aliyun oos get-secret-parameters-by-path --biz-region-id cn-hangzhou --path /secretParameter --recursive false --next-token MTRBMDc0NjAtRUJFNy00N0NBLTk3NTctMTJDQzA --max-results 10 --with-decryption true --region cn-hangzhou",
    "legacyCli": "aliyun oos GetSecretParametersByPath --region cn-hangzhou --Path '/secretParameter' --NextToken MTRBMDc0NjAtRUJFNy00N0NBLTk3NTctMTJDQzA --MaxResults 10 --RegionId 'cn-hangzhou' --Recursive false --WithDecryption true"
  }
}