{
  "name": "DeleteExecutions",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "ExecutionIds",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "执行 ID（列表）。\n可以由多个 ID 组成一个 JSON 数组，格式为`[\"xxxxxxxxx\", \"yyyyyyyyy\", … \"zzzzzzzzz\"]`，最多支持 100 个 ID，ID `之间用半角逗号（,）隔开。",
        "en": "The execution IDs.\n\nYou can specify multiple execution IDs in a JSON array in the format of `[\"xxxxxxxxx\", \"yyyyyyyyy\", ... \"zzzzzzzzz\"]`. You can specify up to 100 execution IDs at a time. Separate multiple IDs with commas (,)."
      }
    },
    {
      "name": "Force",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否强制删除执行中的任务，默认为false。",
        "en": "Whether to force delete the running task, the default value is false."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域 ID",
        "en": "The region ID."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "删除执行",
    "en": "DeleteExecutions"
  },
  "example": {
    "unifiedCli": "aliyun oos delete-executions --biz-region-id cn-hangzhou --execution-ids exec-xxx --biz-force false --region cn-hangzhou",
    "legacyCli": "aliyun oos DeleteExecutions --region cn-hangzhou --Force false --RegionId 'cn-hangzhou' --ExecutionIds 'exec-xxx'"
  }
}