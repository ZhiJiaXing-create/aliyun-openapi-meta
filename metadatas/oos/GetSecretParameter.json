{
  "name": "GetSecretParameter",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "Name",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "参数名称。取值范围：内容限制为字母、数字、中划线、下划线，长度不能超过180字符，且不能以ALIYUN、ACS、ALIBABA、ALICLOUD、OOS开头。",
        "en": "The name of the parameter. The name can be up to 180 characters in length and can contain letters, digits, hyphens (-), and underscores (\\_). The name cannot start with ALIYUN, ACS, ALIBABA, ALICLOUD, or OOS."
      }
    },
    {
      "name": "ParameterVersion",
      "position": "Query",
      "type": "Integer",
      "required": false,
      "description": {
        "zh": "参数版本号。取值范围：1-100。",
        "en": "The version number of the parameter. Valid values: 1 to 100."
      }
    },
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID。",
        "en": "The region ID."
      }
    },
    {
      "name": "WithDecryption",
      "position": "Query",
      "type": "Boolean",
      "required": false,
      "description": {
        "zh": "是否解密参数值。只有参数为true时，返回值的value才会有结果，否则为空。",
        "en": "Specifies whether to decrypt the parameter value. The Value field in the response is returned only if this parameter is set to true. Otherwise, the Value field is empty."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取一个加密参数和参数值",
    "en": "GetSecretParameter"
  },
  "example": {
    "unifiedCli": "aliyun oos get-secret-parameter --biz-region-id cn-hangzhou --name MySecretParameter --parameter-version 1 --with-decryption false --region cn-hangzhou",
    "legacyCli": "aliyun oos GetSecretParameter --region cn-hangzhou --ParameterVersion 1 --RegionId 'cn-hangzhou' --WithDecryption false --Name MySecretParameter"
  }
}