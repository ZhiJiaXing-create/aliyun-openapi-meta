{
  "name": "GetTemplate",
  "deprecated": false,
  "protocol": "HTTP|HTTPS",
  "method": "GET|POST",
  "pathPattern": "",
  "parameters": [
    {
      "name": "RegionId",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "地域ID",
        "en": "The ID of the region."
      }
    },
    {
      "name": "TemplateName",
      "position": "Query",
      "type": "String",
      "required": true,
      "description": {
        "zh": "模板名称。内容限制为字母、数字、中划线、下划线，长度200字符。",
        "en": "The name of the template. The name can be up to 200 characters in length and can contain letters, digits, hyphens (-), and underscores (\\_)."
      }
    },
    {
      "name": "TemplateVersion",
      "position": "Query",
      "type": "String",
      "required": false,
      "description": {
        "zh": "模板版本。格式：**“v”+数字**。取值范围：**v1-v200**。",
        "en": "The version of the template. The format is v followed by a number. Valid values: `v1` to `v200`."
      }
    }
  ],
  "security": [
    "AK"
  ],
  "title": {
    "zh": "获取模版",
    "en": "GetTemplate"
  },
  "example": {
    "unifiedCli": "aliyun oos get-template --biz-region-id cn-hangzhou --template-name MyTemplate --template-version v1 --region cn-hangzhou",
    "legacyCli": "aliyun oos GetTemplate --region cn-hangzhou --TemplateName MyTemplate --TemplateVersion v1 --RegionId 'cn-hangzhou'"
  }
}